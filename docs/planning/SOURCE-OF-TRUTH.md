# Cadence, Single Source of Truth (SSOT)

> **Founder: this is the ONE file to read.** The live cursor (section 0), status, what we are building, what is deferred to you, findings, and progress all live here. Everything else is **typed detail** this file points to (the map is in section 7). The autonomous build loop keeps this file current every cycle.
>
> **Created:** 2026-06-18 · **Last updated:** 2026-06-21 (MONETIZATION TAKEOVER BUILD: founder handed Claude the FULL monetization + credit engine (backend + frontend, no Lovable handoff). Built end-to-end: credit-flow fix (top-up balance + grant/renewal-on-subscribe), retired the dead webhook, single-sourced pricing to the catalog, **go-live guard + backfill** (incident-proof), webhook signature tests + go-live runbook (`../operations/credit-engine-go-live.md`), usage-attribution view (WM-M16), memory-expiry nudge (WM-M7 ✅), owner spend caps (WM-M14), tier-glyph motif (WM-M8 ✅), docs synced. **LIVE INCIDENT found + fixed:** credits metering had been armed with 0 credits → blocked ALL AI on the published app → turned metering OFF (re-enable only at go-live, after balances). Engine dormant by design (grants/top-ups write balances; debit gated by `credits_enabled()`=off). **Only pending = founder's live Stripe keys + the go-live flip.** 75/176 done, 42.6% strict / 68.4% weighted. Prior - LOVABLE MONETIZATION + ADMIN RECONCILIATION: validated Lovable's 06-20 overnight billing/pricing/credits + a new 5-tab Admin Console against the register via code read + live test on `cadence-flow-beta.lovable.app` + live DB; flipped WM-M2/M5 → ✅ and WM-M3/M6/M7/M8/M13/M16/M18/WM-F7/BYO-P4 → ◐ [built but dormant/test-mode]; added 7 rows incl. F-ADMIN-CONSOLE, ADM-DB + a flagged revenue bug M-C-TOPUP-BUG; dashboard 168→175, 38.9% strict / 63.6% weighted; the engine is DORMANT by design - credits/limits behind `*_enabled()`=false, Stripe live-capable but unconfigured. Prior: 2026-06-20 22:45 (FRONT-END theme, founder-directed: wire the shipped backends to the UI. WM scope COMPLETE (cycles 0-4); cycle 5 **ENG-06 B2** (Missions cost glance); cycle 6 **chat thread rename** (◐, wired the backend-only `renameConversation` into the /chat rail). The FE-wiring vein is now thin: remaining in-lane FE is founder-gated (credits/pricing, WM-M6/M3) or design-pass; the high-impact non-gated remainder (the Decision Brain supersession engine) is the knowledge lane's, and the other shipped-backend-no-UI gaps are parallel-lane-owned (builder history=build, agent governance=cockpit) or connector-gated (Linear/Calendar live pulls need OAuth). Prior: 2026-06-20 15:08 UTC SECURITY: hardened scheduled backend hook auth and cleared the active scanner finding. `/api/public/hooks/*` no longer accepts the browser-visible app key; live scheduled jobs now send a private `x-cron-key` sourced from a locked backend-only store. Connector scan, including Wiz, has no active findings. Prior: 2026-06-20 20:21 IST cycle 57 MODEL-REGISTRY-DEPRECATION shipped. **Maintainer:** every tool, every unit of work.

---

## 0. The live cursor (NOW): read this first

> This section replaces the old root `active-task.md` (folded in 2026-06-19). It is the single "what is in flight + what to pick next" cursor. Update it in the same unit of work as any change. Past work is in section 6 (progress log); the full dated history is in [`../../plan.md`](../../plan.md) section 4.

> [!IMPORTANT]
> ## ⭐ THE GUIDING STAR (2026-06-23): v11 is now the standing strategy + build canon (read it before picking)
>
> **[`../strategy/v11-guiding-star.md`](../strategy/v11-guiding-star.md)** is the new strategy guiding star (the ranked build front lives in **[`feature-dashboard.md`](./feature-dashboard.md)**, the v11 items are #1-21 with a Why per row), produced from a code-and-live-DB-verified outsider teardown (investor / CTO / Head-of-Product / competitor / core-user lenses + a villain/defense pressure-test). **When v11 and an older doc disagree on direction, v11 wins.**
>
> **The reframe:** the engine is genuinely wired end-to-end (missions auto-advance via the `resume-runs` cron, agents hand off A2A, 27/31 missions completed in the live DB), but **the moat is cold** (0 outcome edges, 0 supersession edges, 1 learning) and **the demo renders empty** (the seed writes no `artifact_lineage` decision edges), so a built product *looks* fragmented. The core-user research confirms it is **not a design problem, it is a proof + trust-timing problem** (the product asks the PM to govern before showing them anything worth trusting). The fix is NOT new surface area: **light up and fuel the engine you already built, then make the Trust Ledger the hero.**
>
> **Live status of the front (2026-06-24, kept current per cycle):** `TEST-SEED` ✅ done (lane 1). `EVENT-REACTOR-LIVE` ◐ (pipeline wired+scheduled by lane 2; remainder is founder-republish + a CHECK-widen migration). `AMBIENT-SENSE` ◐ autonomous slice shipped (lane 1: sense-tick cron + deterministic tagger feeding cluster-tick; real-source pull + cron-schedule founder-gated). `AMBIENT-TRIGGER` ◐ autonomous slice shipped (lane 1: trigger-tick self-originates `proposed` missions + Trust-Ledger receipts when clusters/outcomes cross a threshold, zero spend; cron + execution founder-gated). `LOOP-PROVE` ✅ done (lane 1: closure-proof harness + live verification — both seeded demo workspaces resolve warm/closed, unseeded correctly cold). `PM-IMPACT-LEDGER` ✅ done (lane 1: `/impact` portable track record + Markdown export; data engine live-verified — 23 decisions, 71% hit rate). `STAKEHOLDER-PACK` ✅ done (lane 1: `/stakeholder` re-frames a decision + receipts into exec/eng/board packs; data engine live-verified). `PLAYBOOK-REGISTRY` ✅ done (lane 1: versioned method registry + `playbook_runs` per-outcome ranking engine; migration applied live + ranking live-verified). `EVALS-PRIMITIVE` ✅ done (lane 1: eval-health "trust" leg — pass rate/flakiness/trend/verdict over `eval_runs` + `/eval-health` surface; engine live-verified at 75% pass rate). `SETTINGS-SEGREGATE` ✅ done (lane 1: the 11 flat settings tabs now present as 5 calm groups + a recessed Advanced via the pure `settings-sections.ts` model + a two-tier nav; every `?section=` id + legacy alias preserved; 15 tests, tsc 0/1360; UI live-verify on publish). `IA-NAV-V11` ✅ done (lane 1: the four competing nav metaphors collapsed into ONE flat list of 6 outcome-named destinations + ONE recessed Engine Room door that reveals the governance surfaces on demand with the live approvals badge; floating dock deleted; pure `nav-model.ts` + 11 tests; nothing orphaned; tsc 0/1378; UI live-verify on publish). `CONNECTORS-V11` ✅ done (lane 1: the presentation/de-dup/day-one layer — pure categorized connector-catalog model `connectors/catalog.ts` + 10 tests, surfaced on `/sync` as the canonical "Available sources" home; day-one input already live via webhook ingest; OAuth provider wiring F-CONN + 2nd live source SEN-01 stay founder-gated, the deeper 3-route merge is IA-DEPTH-V11; tsc 0/1388; UI live-verify on publish). `IA-DEPTH-V11` ◐ [~35%] (lane 1: the Engine Room 13-tab → 3-band sub-grouping — pure `engine-room-bands.ts` Needs you / Trust & safety / Quality & insight + a two-tier `/govern` nav, all `?tab=` ids + badges preserved; 9 tests, tsc 0/1397; remaining basket parts — merge Missions into Build, demote Products, promote the Brief, de-jargon — wait on lane 2's STITCH-LOOP since they touch the loop surfaces). `DELEGATE-DESK` ✅ done (lane 1: new greenfield `/delegate` surface re-framing missions into the delegate-and-walk-away lifecycle — pure `delegate-desk.ts` partitions into 5 lanes Needs you/Working/Queued/Done/Needs-a-look + step-progress + honest headline, 11 tests; RLS adapter + route + registered; tsc 0/1418; UI live-verify on publish). `AGENT-FLEET-VIEW` ✅ done (lane 1: new greenfield `/fleet` surface — the BY-AGENT lens, distinct from DELEGATE-DESK's by-mission lanes; pure `agent-fleet.ts` pivots `agent_runs` on `agent_slug` into per-agent running/queued/done/failed/load + state, ordered attention-first for supervise-by-exception, idle roster agents seeded; 9 tests; RLS adapter + route + registered; tsc 0/1427; UI live-verify on publish). `REPO-DECLUTTER-V11` ◐ [~50%] (lane 1, archive slice: `git mv` the superseded `feature-backlog.md` → `docs/planning/archive/`, with a resolution-based inbound-link migration across 28 live docs + 1 source comment — verified 0 links broken by the move, 157 refs now resolve to the new location; tsc 0/1440. Remaining → founder/coordinated: flatten the doc tree (founder taste), reconcile stray F-IDs, fix 85 pre-existing-broken historical refs). `TRUST-LEDGER` ✅ + `TRUST-SHARE` ✅ + `BRAIN-UX-V11` ◐ + `CORE-UX-TRUST` ◐ + `TRUST-VERIFY` (in dev) done (lane 2). All others ⬜ open. _(The per-item markers also live on the dashboard's top callout and in the register rows.)_
>
> **The v11 build front (capabilities-first; full detail + a Why per row in [`feature-dashboard.md`](./feature-dashboard.md), which carries this same list as its top callout):** `TEST-SEED` (#1, minimal dev data), then the ambient self-initiating loop (`EVENT-REACTOR-LIVE`, `AMBIENT-SENSE`, `AMBIENT-TRIGGER`), then `LOOP-PROVE` (the cold moat fills on real data), then the Trust Ledger (`TRUST-LEDGER`, `TRUST-SHARE`), then `BRAIN-UX-V11` (the Brain as an open analyst that also drives agent action), then `STITCH-LOOP`, then the core-user fixes (`CORE-UX-TRUST`, `CORE-UX-FELT`), then consumer-grade IA (`IA-NAV-V11`, `SETTINGS-SEGREGATE`, `CONNECTORS-V11`), then `ORCH-DELEGATE`, `INTEROP-V11`, `PLAYBOOK-REGISTRY`, `PM-IMPACT-LEDGER`, `STAKEHOLDER-PACK`, `POS-V11`, then `LANDING-PAGE-V11` (after the capabilities it showcases). The rich showcase seed (`DEMO-SEED-RICH`) and `SHIP-V11` are Tier-4, LAST. The Decision Brain (`DBR (H1)`) follows the front at #22.
>
> `SEN-01` / `F-CONN` (one real connector), the external-coding-agent half of `ORCH-DELEGATE`, and the outward write half of `INTEROP-V11` (`Q2`) are **founder-gated**. `bash scripts/lane.sh next` already surfaces the lowest-rank item first, so the lanes pick the v11 front before anything else. The pre-v11 "queue exhausted at ~80%, only DBR left" framing below is superseded: the v11 front IS the high-value work, and the Decision-Brain items are folded into it as the moat-fueling thrust.
>
> ---

> [!IMPORTANT]
> ## ✅ 2026-06-24 (NOW) - LIVE-VERIFICATION CLOSURE PASS (Lane 1, founder-directed): the published app is healthy and the partial board is now honest.
>
> **Founder directive:** "the app is live + published, migrations are done - test the live app and close the partial/pending items; we will come to the founder-gated items later." Done as follows.
>
> **Live app = HEALTHY.** Logged into `cadence-flow-beta.lovable.app` (footer stamp "Last build Jun-24 12:23 PM" = HEAD `bc1b0151`, so the live deploy IS the code in this worktree - zero drift). Today + Trust Ledger render richly. The moat is WARM, not the cold state the v11 teardown found: `artifact_lineage` has 26 rows incl. 2 live `supersedes` edges, the `decision_brain_supersession` flag is ON, and Today's "What changed" card surfaces "Memory has re-scored 2 decisions from real outcomes." Only benign 3rd-party console errors (ipapi + stripe DNS-blocked, a pre-login refresh-token 400).
>
> **Verification method:** a 25-agent adversarial workflow re-checked all 15 non-gated `◐` rows - a closure auditor (code + live-DB evidence via the Lovable MCP, since Supabase MCP is unauthorized this session) then a skeptic told to REFUTE the closure. The skeptics refuted 13 of 15 closure attempts, catching real gaps the auditors had glossed. That is why only 2 closed - the board was OVERSTATING completeness, and this pass made it honest.
>
> **Closed 2 to ✅:** `TRUST-VERIFY` (#26 -> the SHA-256 "Integrity check" for all users; live-confirmed on /trust-ledger: a 64-hex fingerprint, 34 records, Copy-fingerprint + Verify buttons) and `SEC-SIGNAL-INGEST-INJECTION` (#33 -> the injection screen is wired before the live ingest-webhook insert; verified by code + live schema).
>
> **3 LIVE DEFECTS the skeptics found (1 fixed this commit, 2 queued as autonomous fixes):**
> 1. FIXED - `INTEROP-V11`: the MCP `append_decision` write tool was advertised in tools/list + callable but inserted columns / a `decision_queue` table that do not exist in the live schema, so it could never succeed on a public endpoint. Un-advertised it across protocol/route/functions/tests (the decision-WRITE half is founder-gated Phase 4b) and fixed the stale Settings tool list (the cosmetic 5-of-8 gap). tsc 0 / 50 MCP tests pass.
> 2. PARTLY FIXED - `DBR (H1)`: FIXED the `workspace_id` omission - `buildSupersessionEdge` now threads the outcome's `prd.workspace_id` (included ONLY when resolved, so the NOT-NULL DEFAULT current_user_default_workspace() still applies as a fallback and an explicit null can never 23502); 24 supersession unit tests green. STILL OPEN (separate, autonomous, not blocking): the write engine has produced 0 live edges (the 2 existing `supersedes` edges are strategist-seeded, not engine output), and `getLoopClosure` (LOOP-PROVE) has no UI consumer (a warm/cold moat badge would mount it).
> 3. DEFERRED to the founder-gated M1 activation - `M1`: the support-triage signal insert sets no `project_id`, so a triage signal would be hidden from the product-scoped Discover view. The fix is multi-layer (fetch the ticket `product_id` -> carry it through the cluster -> set it on the emitted signal), and `runSupportTriage` has ZERO callers (dormant; `support_tickets` is empty), so the bug never fires today. Best fixed alongside the founder-gated M1 inbound channel + /support UI placement.
>
> **Autonomous remainders to BUILD next (per the build-don't-overpark ruling - NOT founder-gated):** `EVENT-REACTOR-LIVE` (#1 - DONE this session: migration `20260624130000` applied live + verified, widening the CHECK + 3 fanout triggers themes/learnings/decisions + `EVENT_TYPES`/Controls wiring, so the 3 ambient event types are subscribable end-to-end; `drift.detected` deferred for a workspace-scoped drift-event source), `CORE-UX-FELT` (de-jargon LoopStations.tsx), `IA-DEPTH-V11` (the 4 IA moves, now unblocked since STITCH-LOOP landed: merge missions into build, demote products into the workspace switcher, promote the strategic Brief into nav, de-jargon "Models" to "AI"), `AMBIENT-SENSE` (wire the deterministic tagger's tags/sentiment into a consumer + add an opt-in toggle - today the tagger writes into a void cluster-tick never reads), `AMBIENT-TRIGGER` (honest copy for self-proposed missions - delegate-desk mislabels them "Handed off, waiting to start" when resume-runs never auto-starts them), `BRAIN-UX-V11` (the AI open-analyst ceiling), `DEF-04` (AI-drafted mockup half), `REPO-DECLUTTER-V11` (archive other SUPERSEDED-bannered docs + fix the 85 broken inbound refs).
>
> **Founder-gated remainders (the "later" pile - each needs a key / OAuth / provider / flag-flip / safety-posture call / taste decision):** `CORE-UX-TRUST` (auto-clear reversible tool gates in the pinned `loop.server.ts` - an agent-autonomy safety call), `SEC-INGEST-INJECTION` (correct + wired, but dormant until the M1 inbound channel lands), `M1` UI placement + inbound channel + AI draft, `DBR` entity-aliasing flip + fuzzy-codename AI + deep-graph enrichment, `INTEROP-V11` outward-write half (Q2 scoped tokens + OAuth client reg), `DEF-04` live sandbox provider, plus the already-gated `EMBED-CHOKEPOINT`, `FIRECRAWL-FLOOR`, `SANDBOX`, `BLD-04`, `SEN-01`, `F-CONN`, `M-C-EXPIRY`, and the Stripe go-live config.
>
> **Tally after this pass: 161/213 = 75.6% strict, 172.00/213 = 80.8% weighted** (the prior dashboard by-status/by-category tables read a stale 145 ✅ / 201-total and were refreshed off the canonical awk to sum to 213). Per-row verdicts: the dashboard At-a-glance 2026-06-24 entry.
>
> ---

> [!IMPORTANT]
> ## 👋 PICK UP HERE TOMORROW — session closed 2026-06-22 21:35 (Lane 1)
>
> **All work is committed + pushed to `main`; working tree clean.** The dashboard now tells the truth: **180 rows, 145 ✅ (80.6% strict / 83.4% weighted), by-priority Done = ✅ count = 145.**
>
> **What this session did (Lane 1, founder-directed):**
> 1. **Closed the monetization/credit/billing/admin block — PERMANENTLY** (build-complete + gate-green; only your Stripe go-live remains). 🔒 do-not-re-pick banners in the dashboard At-a-glance, this SSOT §0, and AGENTS.md §3. The 3rd map was the last.
> 2. **Whole-dashboard `◐` closure:** a 16-agent verification workflow proved most "partial" rows were *build-complete work mislabeled partial* (the cautious "renders on publish" convention) — **not** unfinished, and **zero `✅` ever regressed** (✅ went 125→145). Closed 12 → ✅ (DBR-1.5, F-IA-BRAIN-GRAPH, MOAT-METRIC, W1-AUTO, H2-WRITES, WM-F1, K2, LCH-01, R4, PROVIDER-FALLBACK, SEC-EGRESS-GUARD, O3), 4 → Gated 👤. Live-verified MOAT-METRIC + the Brain graph on the published app.
> 3. **Fixed the cross-session collision gap** in `scripts/lane.sh` (`next`/`claim` now read `origin/main` fresh + a second In-Dev lock) so two sessions can't target one item; codified anti-dup **rule 0** (claim/fresh-check before ANY row action). Verified: claiming an in-dev item is now refused.
> 4. **Built 2 new security features** (the only clean non-gated autonomous slices left): **SEC-PII-EGRESS** (block Luhn-valid cards + valid SSNs from public announcements) + extended both egress floors to the **public teardown-share** surface.
>
> **WHERE WE ARE: the clean autonomous build queue is exhausted at ~80% done.** `bash scripts/lane.sh next` returns only **DBR (H1)** — Lane 2's active moat work. Everything else is **23 Gated 👤** (need a key/decision), **10 Deferred** (real infra dependency), or the **founder-prompted design pass**. The product is built + the published app is healthy; the remaining 20% is genuinely yours.
>
> **▶ TO RESTART HIGH-VALUE WORK TOMORROW, pick ONE (each is a one-line unblock → I execute immediately):**
> - **(a) Stripe keys** (or say "use test mode") → I flip the whole built revenue engine live and verify it end-to-end. *(Biggest impact — the engine is done and waiting.)*
> - **(b) One connector OAuth** (GitHub or Linear) → I light up a 2nd live data source (`SEN-01` / `F-CONN` / `Q2`).
> - **(c) "Do the design pass"** → I take the 80%-built product to consumer-grade, surface by surface (large autonomous work; just needs your go-ahead since it's taste-bearing).
>
> The full gated/deferred list with exactly what each needs from you is **section 4** below. If none of (a)-(c), point me at any specific item and I'll claim + build it.
>
> ---

> [!NOTE]
> **▶ SESSION CLOSED 2026-06-22 night (Lane 2 overnight run, founder paused — "pick up tomorrow"). WHERE TO PICK UP: see the pickup list at the end of this note.** This run shipped **7 commits to `main`** (all gate-green, adversarially reviewed, documented; tree clean, 0 ahead/behind `origin/main`, no ledger claims held). **What shipped:** (1) **`lane.sh` slash-id fix** — slash-named register ids (`M1 / LRN-01`, `LCH-01 / L1`) were unclaimable (a `mkdir` mutex bug); now fixed + tested. (2) **M1 Support triage** ◐ — `support_tickets` table + a pure deterministic clustering engine that turns recurring tickets into Discover signals; autonomous core only (UI placement + inbound channel + AI-draft are founder-gated). (3) **DBR entity-resolution v1** ◐ — guardrail #4: a pure precision-first resolver that groups nodes naming the same initiative under different titles, so the decision-graph walks stop fragmenting (surface-variant + explicit-alias only; fuzzy codename inference is the founder-gated AI layer). (4) **DBR entity-resolution → shared-premise WIRING** ◐ — flag-gated OFF (`DBR_ENTITY_ALIASING`), byte-identical until you flip it after a precision review on real data. (5) **SEC-INGEST-INJECTION** ◐ + (6) **SEC-SIGNAL-INGEST-INJECTION** ◐ — prompt-injection screening (P0, considerations #3) at the support-triage→Discover boundary AND on the live external signal-ingest webhook, reusing the existing structural-gate classifier; closes #3 for every NON-chokepoint path. Suite grew **1113 → 1201** tests. **Never touched the AI chokepoint / Stripe / BYO keys, as instructed.** **WHY THE RUN HOLDED:** the clean autonomous backlog WITHIN your constraints (no chokepoint / Stripe / BYO / input-needed) is now verifiably exhausted — the board's only eligible item (`DBR (H1)`) has only cross-lane (Lane 1's governing-decision/precedent files) or founder-gated work left, and the last injection surface (MCP/A2A results) sits in the chokepoint. **▶ PICK UP TOMORROW (founder-gated, your call) — in rough priority:** **(a)** flip `DBR_ENTITY_ALIASING` after a precision review on real data, to light up the entity-defrag in the shared-premise walk; **(b)** connect a support inbound channel (Intercom/Zendesk/email) to light up M1, and make the M1 `/support` UI-placement call (an IA/taste decision you reserved); **(c)** decide whether to let me touch the chokepoint (MCP/A2A injection screening + WM-M9 BYOK removal) and/or do cross-lane DBR wiring (entity-resolution into the governing-decision/precedent walks); **(d)** the founder-gated activations already queued below (Stripe go-live config, the `credits_enabled()`/`AI_COST_ROUTING` flips, `DECISION_BRAIN_SUPERSESSION`); **(e)** when you say the product is final, the one-time §14 design pass. Full per-commit detail: [`../../plan.md`](../../plan.md) §4 (2026-06-22 entries) + the Lane 2 report [`./archive/parallel-report-lane2.md`](./archive/parallel-report-lane2.md). To resume the autonomous loop: re-invoke `/overnight-build`.
>
> **LATEST (2026-06-22, Lane 2, founder-directed — the moat goes live): the Decision Brain supersession mechanic is ARMED + HARDENED.** The founder published the latest build, so migration `20260621030000` is now applied (the bi-temporal `valid_to`/`invalidated_by`/`inference` columns are live on `artifact_lineage`; embed-chokepoint telemetry confirmed; the Brain graph + Gauntlet surfaces render clean post-publish — only benign 3rd-party console errors). Then, on the founder's "flip supersession on yourself, then harden" directive, **DBR-EDGE-CONF** shipped (Lane 2): **(1)** the activation gate is now **DB-backed** (`feature_flags`/`get_flag`, the `credits_enabled`/`limit_gates_enabled` idiom; the `DECISION_BRAIN_SUPERSESSION` env var stays a hard override) and **ARMED ON via SQL** — the moat is flippable live with one SQL upsert, no Worker-secret + redeploy; **(2)** a pure **edge-confidence precision layer** (`src/lib/ai/supersession-confidence.ts`) drops marginal edges before they are written so the Critic's first cited edges are trustworthy (closes guardrail #3). tsc 0 / 41 supersession tests / 1027 full suite; 3-lens adversarial review SHIP, 0 must-fix. **Founder next step: one more publish** deploys the DB-backed gate code; the moat is already armed, so it begins accruing hardened supersession edges from live recorded outcomes the moment that build is live. Spec: [`../features/decision-brain.md`](../features/decision-brain.md) (DBR-EDGE-CONF). This + Lane 1's DBR-3a (next note) compound: once edges accrue, the governing-decision walker corrects what the Critic cites.
>
> **LATEST (2026-06-22, Lane 1, founder-directed "go deeper now"): DBR-3a governing-decision retrieval SHIPPED ◐ (dormant).** Asked where to take the core moat next, the founder chose to deepen the Decision Brain. Built the moat's headline mechanic — "return the CURRENT belief, not the similar old one": a pure `supersedes`-chain walker (`src/lib/ai/governing-decision.ts`, 27 tests) driven into `runCritic` via a bounded fail-safe forward-closure loader, so a precedent the Critic might cite is corrected to the current governing decision (distinct from DBR-2, which only lists the edges). 3-lens adversarial review = SHIP_WITH_FIXES (the one should-fix — similarity-bounded edges truncating multi-hop — fixed at root with the closure loader). tsc 0 / 1030 tests / eslint clean. Dormant + byte-identical until the founder publishes (applies the DBR-1.5 migration) + flips `DECISION_BRAIN_SUPERSESSION`. Build record: [`../../plan.md`](../../plan.md) §4 (2026-06-22 Lane 1) + dashboard row 23 (DBR umbrella, increment 3) + [`../features/decision-brain.md`](../features/decision-brain.md) (DBR-3a). **Next DBR-3:** richer typed-edge auto-extraction (autonomous); deep-graph enrichment (still founder-gated: storage / viz / ambient aggressiveness). **DBR-3b SHIPPED ◐ (15:10):** extended governing-decision from the Critic to the PROACTIVE precedent nudge (it now flags a superseded/contradicted precedent inline); reusable `governing-decision.server.ts`, code-review SHIP_WITH_FIXES (3 folded, incl. a silent frontier-truncation fix); tsc 0 / 1034 tests. **DBR-3c SHIPPED ◐ (15:25):** NAMES the governing decision (both surfaces now say 'replaced by "X"' via a fail-safe title resolver, not an opaque id); tsc 0 / 1059 tests. **DBR-3d SHIPPED ◐ (15:39):** a decision-currency banner now warns when the decision you are VIEWING has itself been superseded/contradicted (governing-decision so far only flagged stale PRECEDENTS); new `getDecisionCurrency` fn + fail-safe `DecisionCurrencyBanner` on the opportunity + PRD pages; tsc 0 / 1064 tests. The governing-decision read side (3a/3b/3c/3d) lights up automatically as edges accrue once this build is published (moat already armed live). **DBR-3e SHIPPED ◐ (15:57):** decision precedent is now cited in CHAT/Ask (the assistant grounds answers in the workspace's own outcome history); one fail-safe file (`chat.ts`), code-review SHIP_WITH_FIXES (3 folded: citation-tag clarity, injection rider, min-length embed guard); tsc 0 / 1064 tests. **FOUR of the five "value at every step" moments are now live (Critic · precedent nudge · decision-currency banner · chat).** **The autonomous Decision-Brain depth is now broad; the remaining moves are: (a) one more autonomous mechanic — proactive `outcome→re-score live bets` (needs opportunity-embedding infra, a meatier build), and (b) the founder-gated deep-graph enrichment (storage crossover / graph-viz at scale / ambient aggressiveness / richer edge schema). FOUNDER FORK approaching: the next big moat depth is the enrichment, which needs a schema/sequencing call.**
>
> **LATEST (2026-06-21, Lane 2, founder-directed): the agent-orchestration + decision-brain MOAT SEQUENCE #1-#5 shipped, published, and live-verified.** All five are dormant-by-default flag-gated (zero live change until the founder flips a flag). (1) A2A handoff **evidence gate** (`HANDOFF_EVIDENCE_GATE`, row W3). (2) **DBR-2: the Critic now reasons over the typed `supersedes`/`contradicts` edges** (DBR umbrella increment 2). This SUPERSEDES the "DBR-2 stays deferred" line in the priority block below. (3) Explorer **bitemporal tree view** (F-IA-BRAIN-GRAPH increment 4). (4) Ephemeral **sub-agent fan-out** `agent.spawn` (`AGENT_FANOUT`, F-AGENT-1; runaway-safe via a depth cap + a mission-completion guard). (5) Governed **delegate-out** `delegate.openhands`, wired onto Lane 3's provider seam (`DELEGATE_OUTBOUND_ENABLED`, BLD-04). Deploy verified healthy on `cadence-flow-beta.lovable.app` (Today + Brain graph render, zero app console errors); tsc 0 / 895 tests / my session's files lint-clean. **To activate: flip the flags** (`DECISION_BRAIN_SUPERSESSION` first, which lights up the #2/#3 read-side as outcomes accrue). Build record: [`../../plan.md`](../../plan.md) §4 (MOAT SEQUENCE entry) + [`../strategy/session-decisions.md`](../strategy/session-decisions.md) + the dashboard rows (W3 / DBR umbrella / F-IA-BRAIN-GRAPH / F-AGENT-1 / BLD-04). **Next moat rung (founder-gated): the deeper Decision-Brain graph enrichment (DBR-3+).**

> [!IMPORTANT]
> **TOPMOST PRIORITY (founder ruling 2026-06-20): the Decision Brain (H1).** Evolve flat memory into a typed, bi-temporal, auto-built **decision knowledge graph** with an **Obsidian-style visual graph view** (the engine under the moat), and make it **deliver value at every step, often before the user expects it**. Spec: [`../features/decision-brain.md`](../features/decision-brain.md); strategy + wiring: [`../strategy/horizon-bets.md`](../strategy/horizon-bets.md). The in-flight WM initiative below runs to a clean stopping point, then the queue reorders behind H1. First step **DBR-0 is DONE (2026-06-20, ◐)**: a code read corrected the plan (the outcome to memory loop was already closed via `recordOutcome` and is correctly human-gated), so the real first win was giving the **Critic** (the wedge) decision precedent, it red-teamed blind before; `formatDecisionPrecedent` + a best-effort workspace-outcomes query in `runCritic` now let it cite the workspace's own shipped outcomes (tsc 0 / 303 tests / build green; live-verify on publish). **Increment 1 = Ambient Precedent is SHIPPED to `main` (◐), 2026-06-20 (overnight cycle 55), subagent-driven, all 6 tasks T1 to T6.** The proactive precedent nudge that surfaces a workspace's own past shipped outcomes wherever a decision is called out, via a semantic match over the outcome memories we already store. Engine + pure ranker ✅ unit-verified (`src/lib/ai/decision-precedent.server.ts`, 3 tests, full suite 322 pass); the 3 seams (Critic `runCritic`, the opportunity `?opp=` drill, the spec detail page) are wired + driven + gate-green but ◐ render-on-publish (they read live embedded outcome data; behavioral verify needs the founder's next publish + seeded outcomes). Whole-branch-reviewed (opus: ready to merge); threshold ships at a conservative `0.3` to tune on live data. Detail: [`../features/ambient-precedent.md`](../features/ambient-precedent.md) "Status (as built)". **Next Decision Brain pick (SHIPPED ◐ 2026-06-21, Lane 2; founder live-greenlit 2026-06-20):** the **supersession engine** (DBR-1.5) - the moat's signature mechanic (outcome-labeled, invalidate-don't-delete). **As-built:** the dormant flag-gated write-path is wired + unit-verified + adversarially reviewed (0 blockers); see §6 (2026-06-21 04:10) + [`supersession-engine-plan.md`](./supersession-engine-plan.md). DBR-2 (Critic reasoning over the edges) stays deferred. The founder directed live "start working on the most important things"; a 4-agent design workflow confirmed it is the #1 non-gated buildable moat item and produced a cold-buildable plan: [`supersession-engine-plan.md`](./supersession-engine-plan.md). Increment DBR-1.5 = the dormant, flag-gated (`DECISION_BRAIN_SUPERSESSION`, default OFF) outcome->supersession WRITE-PATH: when a human records an outcome, infer + write typed `supersedes`/`contradicts` edges (with bi-temporal `valid_to`, invalidate-don't-delete) into `artifact_lineage`, fitting the knowledge lane's "empty supersession seam" (additive, write-only, byte-identical read surface). DBR-1 v1 (the read-side explorer) already shipped in the knowledge lane. DBR-2 (the Critic reasoning over the edges) stays deferred. The deeper graph beyond this stays founder-gated for the enrichment pass. Cold-buildable pick-source: [`ambient-precedent-plan.md`](./ambient-precedent-plan.md) (6 tasks, strict order T1 titles -> T2 semantic engine -> T3 Critic uses engine -> T4 server fn -> T5 PrecedentNudge UI -> T6 wire opportunity+spec seams); spec [`../features/ambient-precedent.md`](../features/ambient-precedent.md); migration-free, best-effort, fail-safe; build-queue table in section 3. **H2 the Command Canvas** (NL command bar + live preview) and the deeper Decision Brain graph (DBR-1+) remain founder-gated for an enrichment pass (schema depth, sequencing, lane assignment) before they enter a lane: see section 4.

### The Build Sequence: pick by Rank (founder ruling 2026-06-21)

> **Canonical ranked list = the Master register in [`feature-dashboard.md`](./feature-dashboard.md)** ("How to pick" section + the register), sorted by priority with a derived **Rank** (`#1` = highest). Any lane picks the lowest-`#` `Tier`-class row that is unclaimed and builds it; never deliberate, the Rank is the decision. **How priority is marked (the ONE uniform vocabulary):** every row is `Tier 1` (foundation / core features / core enablements / USPs), `Tier 2` (design, founder-prompted), `Tier 3` (non-essential / privacy / ops hygiene / enterprise-readiness), `Tier 4` (final polish, founder-prompted), `Gated` (founder must unblock), `Lovable` (Lovable builds it), `Deferred`, or `Done`. The legacy `P0/P1/WM-*/BYO-*` codes are retired. The Rank is regenerated by `python3 scripts/rerank-dashboard.py` (never hand-numbered), so adding or reprioritizing an item reflows the whole list (it moves up or down by priority, nothing is left siloed). Full law + the real-time-board loop: [`../../AGENTS.md`](../../AGENTS.md) §3 "Build Sequence".
>
> **Tier law (build substance before surface; non-core last):** Tier 1 then Tier 2 then Tier 3 then Tier 4.

**The active Tier-1 front (top of the rank, build top-down):** `DBR-1.5` (**Rank #1, TOPMOST**: the supersession engine, the moat's signature mechanic, plan [`supersession-engine-plan.md`](./supersession-engine-plan.md)), then `MOAT-VIS`, `F-IA-BRAIN-GRAPH`, `MOAT-METRIC`, `EMBED-CHOKEPOINT`, `FIRECRAWL-FLOOR`, `H1-TASKS`, `W1-AUTO`, `O1`/`DBR-1`, `O3`/`DBR-3`, `Q1-MCP`, then the tenancy spine (`WM-F*`, `WM-M1/M4/M5/M9/M15`) and `H2-WRITES`. **Tier-1 GATED (parked until you unblock, see §4):** `SEN-01` (OAuth via `F-CONN`), `SANDBOX` (provider + spend), `SEN-05` + `F-ANALYTICS-1/2` (connector + spend), the Decision Brain deep-graph enrichment (`DBR-2`+).

**Tier 2 (design, founder-prompted):** the IA culls (`F-IA-*`), the Agent-Experience relay merge, FE-wiring of shipped backends, the §14 design pass. **Tier 3 (non-essential):** `PLAN-ENFORCE`, `DATA-RETENTION-b`, `SUBPROC-DISCLOSURE` UI, `APP-HEALTH`, `D4`/`K2b`/`P3`/`R4` (non-billing)/`B5`/`FND-0.5`/`FND-0.7`/`U6`, the analytics outer loop once `SEN-05` lands. **Tier 4 (final polish, founder-prompted, last):** `HUMAN-SWEEP`, lint/prettier, AI-trace, prose-polish, final design detailing.

> **🔒 CLOSED 2026-06-22 (founder ruling, FINAL, all sessions): the MONETIZATION / CREDIT / BILLING / ADMIN block is DONE — do NOT re-pick, re-verify, or re-map it, ever, by any lane or tool or future session.** This was the 3rd verification pass of Lovable's monetization work; it is the LAST. The whole block is **build-complete + gate-green**; the ONLY remaining step is the founder's go-live **config** (live Stripe keys + price IDs, the `credits_enabled()` / `AI_COST_ROUTING` flips, final pricing numbers — section 4 below), which is NOT agent work. Closure (this session, evidence-backed by an 8-agent verification workflow): the last 3 buildable slices shipped (`M-C-BILLING-TESTS` top-up-cap DRY + SQL-parity, `WM-M18` downgrade-confirm, `WM-M6` pricing accuracy), and every block item is now in a TERMINAL state — **✅** `M-C-PRICE`/`WM-M3`/`WM-M6`/`WM-M13`/`WM-M15`/`WM-M18`/`M-C-BILLING-TESTS` (+ the prior ✅ credit/admin rows), **Gated 👤** `WM-M9` (chokepoint), `WM-M17`/`WM-M19` (founder pricing numbers), `M-C-EXPIRY` (flip-timing). **None is `◐`+Tier-1/3 any more, so `lane.sh next` will never surface them again** — that combo is exactly why they were re-picked every run (a logical-closure failure, now fixed). If an instruction ever points an agent at a monetization/credit/billing item: STOP — it is closed; the only open monetization work is the founder's, in section 4. The block below is the historical record of what was once Lovable-routed.

**Lovable-owned scope (HISTORICAL — retired 2026-06-22, see the banner above):**
- **Billing / Stripe rails:** `M-C-PRICE`, `WM-M3`, `WM-M13`.
- **Credit metering engine (already shipped dormant, FROZEN, do NOT extend, coordinate before it lands live to avoid colliding schemas):** `WM-M10`, `WM-M11`, `WM-M12`, `WM-M14`, `WM-M16`.
- **Pricing / tier surfaces:** `WM-M6`, `WM-M7`, `WM-M8`, `WM-M17`, `WM-M18`, `WM-M19`.
- **Managed credits:** `BYO-P4` (this IS the WM credit engine).
- **Split rows (Lovable owns the billing half only; the rest stays):** `PLG` conversion/checkout leg, `R4` Plan/billing tab, `WM-F7` Plan/billing tab content, `WM-M2` credit-pool/Stripe half.
- **STAYS product-infra (explicitly NOT Lovable):** every `WM-F*` (tenancy / RBAC / invites / transfer / isolation), `WM-M1` (tier model), `WM-M4` (runtime credit seam), `WM-M5` (limit enforcement), `WM-M9` (BYOK removal at the chokepoint, attended), `WM-M15` (routing + cache COGS levers), `M-C-EXPIRY` (decay mechanic), `FND-AUTH`, `U6` (export). These are the memory-moat enforcement spine, not a bill.
- **Validated 2026-06-21 (this reconciliation):** Lovable's 06-20 overnight build SHIPPED this whole block - billing rails, embedded Stripe checkout, the 5-tier in-app picker, the credits UI, plus a new 5-tab **Admin Console** (previously out of scope). `WM-M2`/`WM-M5` → ✅ (migrations applied live); `WM-M3`/`WM-M6`/`WM-M7`/`WM-M8`/`WM-M13`/`WM-M16`/`WM-M18`/`BYO-P4`/`WM-F7` → ◐ (built but **dormant by design**: the credit/limit engine sits behind `credits_enabled()`/`limit_gates_enabled()`=false; Stripe is live-capable but unconfigured/test-mode). `WM-M17`/`WM-M19` confirmed **still Deferred** (genuinely not built). NEW dashboard rows: `F-ADMIN-CONSOLE` + `ADM-DB` (the admin console + its DB substrate) and the gap rows `M-C-TOPUP-BUG` (⚠️ **REVENUE BLOCKER** - top-up purchases write `credit_topups` but never credit the spendable balance; must fix before credits go live), `M-C-DEDUPE-WEBHOOK` (a dead duplicate Stripe webhook to retire), `M-C-PRICE-SYNC` (the public `/pricing` prices + tier lineup disagree with the in-app picker), `M-C-BILLING-TESTS` (zero automated tests on the billing/admin surface), `M-C-DB-HYGIENE` (an `app_settings` migration-drift fix). Detail + per-row evidence: [`feature-dashboard.md`](./feature-dashboard.md) (rows `M-C-PRICE` + the `M-C-*`/`ADM-*` block).

---

**Current focus:** the **Decision Brain (H1)** is TOPMOST (founder ruling 2026-06-20); the autonomous lanes build the Tier-1 sequence above, starting at `DBR-1.5`. The **workspace / tenancy spine** (`WM-F*`, product-infra) stays Claude-owned; **monetization + credit + billing** (`WM-M*`, `M-C-PRICE`, `BYO-P4`) is **now CLAUDE-OWNED** (founder ruling 2026-06-22 — Lovable scope retired; pick these up, verify done-vs-not, close them honestly). **Branch:** `main` (single-branch model as of 2026-06-19). Tenancy build bible: [`workspace-tenancy-and-monetization-plan.md`](./workspace-tenancy-and-monetization-plan.md). Live board: [`feature-dashboard.md`](./feature-dashboard.md). **Who is on what RIGHT NOW (real-time, per lane, with date+time + status):** the auto-synced **Active claims** table in [`feature-dashboard.md`](./feature-dashboard.md) (this SSOT is the narrative cursor; the per-lane live view lives there, not duplicated here). Instant view: `bash scripts/lane.sh board`.

**Status:** planning + docs landed; **WM feature code shipping**: WM-M1 (entitlements core) ✅, WM-F1 core (agent memory workspace-scoping) ◐, WM-M2 core (accounts table + billing relocation + credit pool shell) ◐, WM-M10 (credit unit + cost-to-credit conversion + legibility layer) ✅, WM-M4 core (dormant runtime credit seam at the AI chokepoint) ◐, WM-M11 core (per-tier credit grant + monthly reset + credit-tick hook) ◐, WM-M12 core (atomic credit debit engine, fills the WM-M4 seam) ◐, WM-M14 core (per-product/member credit attribution + caps) ◐, WM-M15 routing half (cost-aware model routing margin lever, config-gated default-off) ◐, WM-F9 core (tenancy isolation hardening: workspace_id + dual-key membership RLS on 10 tables) ◐, WM-F3 core (RBAC enforcement: owner/admin/member/viewer roles with SECURITY DEFINER helpers + role-based RLS + owner demotion trigger + TS role helpers) ◐, WM-M5 core (tier limit gates: account-wide product cap + workspace cap as BEFORE-INSERT triggers, dormant behind `limit_gates_enabled()`) ◐, WM-F1b core (agent-workspace isolation hardening: `user_id`-fed BEFORE-INSERT trigger for universal insert tagging + dual-key membership RLS on all 5 agent tables) ◐, WM-F4 core (workspace ownership transfer RPC + audit log) ◐, WM-F5 backend (workspace invitations table + email-bound single-use accept RPC) ◐, WM-F8 core (workspace-switch query-cache hardening: `setActiveWorkspaceId` clears workspace-scoped TanStack Query cache on switch behind a pure, unit-tested global allowlist, so no previous-workspace data flashes) ◐, and WM-F6 core (move product between workspaces: a transactional SECURITY DEFINER `move_product` RPC reassigns the product + 13 product-scoped child/grandchild tables' `workspace_id`, guarded by owner/admin-in-both + same-account; reads already keyed on `is_workspace_member(workspace_id)` so the reassignment IS the move; behaviorally dry-run-verified on prod, 0 orphans, both guards block) ◐, all 2026-06-19.

**Overall progress (live): 80.4% strict / 83.0% weighted** - 144 of 179 tracked rows fully done (recomputed 2026-06-22 18:40, all lanes, kept real-time per the founder ruling — updated in the same commit as every status flip; this recompute folds the monetization-block closure + the by-priority Done=✅ reconcile + the whole-dashboard ◐ closure pass that closed 12 build-complete rows to ✅ + 4 to Gated); ◐ partial counts at its per-row `[~NN%]` estimate, ⏸️ paused counts half. The per-item % (read off each row's status), the per-category roll-up, and the recompute method live in the dashboard's ["At a glance"](./feature-dashboard.md) + its **Progress accounting** rule. Recompute on every status change (do not hand-guess; run the tally).

> **Reconciliation note (2026-06-19, cycle 27):** `main` had diverged (a parallel session built a second, buggy WM-F1 + a "Lovable MCP checkpoint" docs commit on LOCAL main only). Resolved per founder ruling "keep mine + save your docs": `origin/main` = my verified WM-F1 + the cherry-picked docs; the parallel work is backed up at branch `backup/parallel-wm-f1`. **Founder action:** sync local main with `git fetch && git reset --hard origin/main` (the loop cannot run `reset --hard`, it is denylisted). Detail: the overnight report reconciliation callout.

### Build next (critical path; build top-down per G10)
- [x] **WM-M1** - Entitlements core (5 account tiers + matrix). DONE 2026-06-19 (overnight cycle 26): 5 slug tiers (`free|pro|max|team|enterprise`) + full matrix (limits, credits, RBAC, collab) + `limitFor` + Constellation `planPresentation`; legacy fields kept as aliases; 14 unit tests; tsc/build/lint/test green.
- [~] **WM-F1** - Scope agent memory/runs/roster to workspace. CORE DONE 2026-06-19 (overnight cycle 27, ◐): migration `20260619150000_wm_f1_agent_workspace_scope.sql` adds nullable `workspace_id` to all 5 agent tables (backfilled via `ensure_user_default_workspace`), rewrites `match_agent_memory` + `recent_agent_reflections` with an optional `for_workspace` (service-role-safe, null-tolerant) + a folded security fix (cross-user reflection read), threads the active workspace through recall, and tags new outcome + reflection rows. **Verified by a BEGIN..ROLLBACK dry-run on the live DB (0 nulls, both RPCs recreated) via the Lovable MCP; rolled back so prod is untouched.** tsc/build/lint/humanization green; 4-lens adversarial review, 3 fixes folded. **Live recall-isolation activates on the founder's next publish.**
- [~] **WM-F1b** - Hardening follow-up to WM-F1. CORE DONE 2026-06-19 (overnight cycle 37, ◐): migration `20260619220000_wm_f1b_agent_workspace_hardening.sql` adds a `user_id`-fed BEFORE-INSERT trigger (`set_row_workspace_from_user`, service-role-safe) to all 5 agent tables so EVERY insert path is workspace-tagged at the DB layer (the `seed_default_agents` SQL fn, the `memory.remember` tool, any service-role writer) - more robust than hand-editing 6 files - plus a DUAL-KEY membership RLS (`auth.uid()=user_id AND is_workspace_member(workspace_id)`) replacing the owner-only policy on all 5 tables. The dual key is strictly MORE restrictive than the old `auth.uid()=user_id` (never widens, never locks the owner out) and matches the user-scoped recall RPCs. `memoryRemember` now tags the active workspace (pre-migration-tolerant). **Verified by a BEGIN..ROLLBACK dry-run on the live prod DB (Lovable MCP): 5 triggers / 5 dual-key policies / 0 old policies remaining, the trigger fills `workspace_id`, `seed_default_agents` still works (ON CONFLICT intact), and a simulated-JWT read confirmed the owner sees all 23 memory rows (no reader breakage); 0 memory-owners lack a `workspace_members` row. Rolled back; prod untouched.** tsc 0 / build ✓ / eslint 0 / 252 tests / humanization clean; adversarial review folded 2 humanization fixes, no runtime-fatal bug. **DEFERRED to WM-F1c:** NOT NULL on `workspace_id` (founder-gated: WM-F1 set the FK to ON DELETE SET NULL to preserve the moat across workspace deletion; NOT NULL forces ON DELETE CASCADE = irreversible memory loss), the `agents UNIQUE(user_id,slug)→UNIQUE(workspace_id,slug)` swap (breaks `seed_default_agents`' ON CONFLICT target; couples to the WM-F8 roster read), and the types regen (post-publish, the schema is not live). **Live isolation activates on the founder's next publish.**
- [x] **WM-M2** - accounts table + billing relocation + credit/decay migrations. DONE 2026-06-19 (overnight cycle 28, ◐): migration `20260619160000_wm_m2_accounts_billing_credits.sql` creates `accounts`/`account_members`/`account_credits`/`credit_ledger` + `workspaces.account_id` (backfilled, NOT NULL via an `owner_id` auto-fill trigger) + 5-tier CHECK widen + 30d rolling decay (rolls off `last_used_at`, dormant) + `credits_enabled()`/`is_account_member`/`ensure_user_default_account` + member-read RLS (writes service-role-only); `billing.functions.ts` reads the plan from the account (shim fallback). **Verified by a BEGIN..ROLLBACK dry-run on the live prod DB (5 accounts, all 7 workspaces linked, 0 nulls, `credits_enabled=false`), then rolled back so prod is untouched.** tsc/build green, billing.functions.ts lint + humanization clean. **Live billing-from-account + the schema activate on the founder's next publish.** Types regen + account-aware webhook tracked with WM-M3.
- [x] **WM-M10** - Credit unit + cost-to-credit conversion + legibility layer. DONE 2026-06-19 (overnight cycle 29, ✅): `src/lib/ai/pricing.ts` adds `CREDIT_COGS_USD` + `creditsForCost` (0 for non-billable, >= 1 margin-positive for billable) + `estimateCreditsForCall` (composes `estimateCostUsd`) + the pure display-only `actionCreditRange` legibility layer; `pricing.test.ts` 15/15 (29 asserts). tsc/build/lint green. Behaviorally verified by the unit tests (pure module, no publish needed → ✅). §7 placeholder numbers; the dormant `WM-M4` seam fills via WM-M12.
- then WM-F2 (parallel; **WM-F9 ◐ shipped cycle 35** = tenancy isolation hardening; **WM-M5 ◐ shipped cycle 36** = tier limit gates, dormant; **WM-F3 ◐ shipped by a parallel session `2b1c429591`** = RBAC enforcement) → WM-F4/F5 + WM-M3 (WM-M4 ✓ shipped dormant cycle 30) → WM-F7/F8 + WM-M6/M7/M8/M9 (M9 = remove self-serve BYOK) → WM-F6. Credit engine `WM-M10`(✓)..`WM-M16` builds on the `WM-M4` seam (✓ shipped, dormant) + `WM-M2` pool; the credit engine's metering path is now built end-to-end (WM-M10 unit ✓, WM-M2 pool, WM-M4 seam ✓, WM-M11 grants ✓, **WM-M12 atomic debit ✓ shipped cycle 32**); the remaining credit work is **WM-M14** (attribution + caps ◐ shipped cycle 33), **WM-M15** (margin levers: routing half ◐ shipped cycle 34, cache half ◐ shipped cycle 38), **WM-M16** (credit UI, needs WM-M14 ✓ + WM-M6), and **WM-M13** (top-ups, needs WM-M3 Stripe). **WM-M5** (tier limit gates) **◐ shipped cycle 36**: account-wide product cap + workspace cap as SECURITY DEFINER BEFORE-INSERT triggers + a typed `LimitReachedError` server pre-check on `createProject`, dormant behind a new `limit_gates_enabled()` flag, behaviorally dry-run-verified on prod. **WM-F1b** (agent-workspace isolation hardening) **◐ shipped cycle 37**: a `user_id`-fed BEFORE-INSERT trigger (universal insert tagging) + dual-key membership RLS on all 5 agent tables, dry-run-verified on prod (no reader breakage); NOT NULL + the roster unique-key swap + types regen deferred to WM-F1c (founder-gated cascade call / WM-F8 coupling / post-publish). **WM-F3** (RBAC enforcement: owner/admin/member/viewer) **◐ shipped by a parallel session** (`2b1c429591`), so its dependents are now unblocked. **WM-F2** (account-level memory pooling) **◐ shipped cycle 40**, **WM-F4** (ownership transfer) **◐ shipped cycle 43**, **WM-F5** (invitations backend) **◐ shipped cycle 44**, **WM-F8** (workspace-switch cache hardening) **◐ shipped cycle 45**, and **WM-F6** (move product between workspaces) **◐ shipped cycle 46** (transactional `move_product` RPC reassigns the product + 13 product-scoped child/grandchild tables' `workspace_id`, owner/admin-in-both + same-account guard; the spec's "harden product RLS first" was already satisfied - reads key on `is_workspace_member(workspace_id)`, so the reassignment IS the move; behaviorally dry-run-verified on prod, 0 orphans, both guards block; UI deferred; follow-up WM-F6b scopes `artifact_lineage`). **DATA-RETENTION** (AI-telemetry retention purge) **◐ shipped cycle 47** (cross-referenced from `considerations.md` once the WM backend lane thinned: a dormant, 30-day-floored, service-role-only `purge_old_telemetry` SQL fn + a `retention-tick` hook; behaviorally dry-run-verified on prod; activates on the founder flipping `data_retention_enabled()` + wiring the cron; follow-up DATA-RETENTION-b = the right-to-be-forgotten erase cascade). **WM-M9** (remove self-serve BYOK) **skip-and-queued cycle 47:** its own spec says sequence it with the founder-gated pricing surfaces (WM-M6), and removing a live capability at the AI chokepoint `runtime.server.ts` while credits are dormant is premature - best done attended / with the pricing surfaces. **U6-AUDIT** (export audit log, the remaining slice of U6 row 36) **◐ shipped cycle 48** (an append-only, tamper-evident `export_log` table + per-export audit-write in `exportProduct`/`exportWorkspace` + a `listExportLog` read fn; behaviorally dry-run-verified on prod - append-only confirmed via the policy set; the export-history UI is design-deferred). **WM-M9** (remove self-serve BYOK) **skip-and-queued cycle 47:** its own spec says sequence it with the founder-gated pricing surfaces (WM-M6), and removing a live capability at the AI chokepoint `runtime.server.ts` while credits are dormant is premature - best done attended / with the pricing surfaces. **Next (non-gated, non-design):** the WM clean-backend lane is now near-dry (remaining WM is founder-gated - WM-M3 Stripe, WM-M6 pricing - or design-pass - WM-M16, WM-F7, WM-M7/M8, the deferred UI affordances for WM-F4/F5/F6); the two highest-pure-impact remaining items (PROVIDER-FALLBACK resilience, WM-M9) BOTH touch the AI chokepoint and can't be behaviorally verified unattended, so they are QUEUED for an attended cycle. The loop continues down the cross-cutting `considerations.md` gaps + v9/v8 per priority, preferring clean fully-verifiable (migration / server-fn) items in this lane's strength, and skipping parallel-lane-claimed work (cockpit/knowledge/safety/build). **SUBPROC-DISCLOSURE** (sub-processor disclosure registry) **◐ shipped cycle 49** (the Data/Privacy P1 enterprise-review gap from `considerations.md`: a pure `src/lib/compliance/subprocessors.ts` registry DERIVED from the live model catalog + a `getSubprocessors` read fn; tsc/eslint/build/298-tests green; trust-page UI + legal copy/regions/DPA deferred to the design/legal pass; completes the data-governance triad with DATA-RETENTION + U6-AUDIT). **Cycle-49 finding:** PROVIDER-FALLBACK + PLAN-ENFORCE + **MODEL-REGISTRY-DEPRECATION** are now ONE attended chokepoint bundle - the model-deprecation route-around's only universal live drive-point is `runtime.server.ts` (the queue named its domain as `models.ts`, but `routeModel` is a dead export with zero consumers and `costRoutedModel` is gated-off + surface-narrow, so a route-around is staged-but-never-driven unless wired at the chokepoint), the same constraint that already queued PROVIDER-FALLBACK.

### Hard gates (every tool, every commit)
`bun run lint` + `tsc --noEmit` + `bun run build` green before commit. No em/en dashes. RLS-aware timestamped migrations. Commit explicit paths with a WHY. Single-branch model: commit to `main`.

### Deferred (do NOT build now)
`WM-S1`..`WM-S5` (showcase: sample workspace, guided tour, onboarding concierge, steward, demo population). Gate: platform ~50-60% complete. **`WM-M17` / `WM-M18` / `WM-M19`** (the Anthropic-style pricing packaging: Max 5x/20x + Team Standard/Premium usage variants; plan-card states + downgrade guard; enterprise per-seat + API-rate + per-user allocation) are deferred behind the core builds, gated on `WM-M3` + `WM-M6`; spec in the bible §2.4.1 + §4.2.2. **Update 2026-06-21:** `WM-M18` reclassified **◐** - its plan-card states (current-plan pill + upgrade/downgrade CTA language) shipped in Lovable's 06-20 build; only the guarded downgrade-confirm remains. `WM-M17`/`WM-M19` remain Deferred (verified genuinely not built).

### Founder gates (need you; full list in section 4)
Crescendo/Galaxy prices; Stripe secrets for `WM-M3`; flip `credits_enabled()` when the credit engine lands; flip `limit_gates_enabled()` to activate the WM-M5 tier caps once a live upgrade path (Stripe) exists; confirm final tier display names + motif (Constellation is the default).

---

## 1. Standing instructions (founder rulings, durable, do not re-ask)

These are permanent operating rules. The loop follows them without being reminded.

1. **Build everything buildable WITHOUT founder input FIRST, migrations included.** Close every autonomous loop before anything that needs the founder. A database migration that needs no taste/secret/spend/account decision is fair game (gate it offline, flag it for publish-verify). _(Founder ruling 2026-06-18.)_
2. **The design / UX-polish pass is LAST, and done ONCE.** Only on the finalized product. It is not a repeated activity, do not redo design again and again and burn tokens/time. Do not run it until the foundation is built and the founder says the product is final. _(Founder ruling 2026-06-18.)_
3. **Founder-gated work comes AFTER the autonomous foundation.** Taste / positioning / product-tasting, secrets, OAuth registration, infra/sandbox picks, recurring AI spend, the founder's accounts, outbound sending, anything irreversible/outward-facing. The loop keeps a clean pickup list (section 4) and never does these unilaterally. _(Standing.)_
4. **Cross-reference the canon before claiming scope.** Never call the backlog "thin" or "done" from a narrow scan. Read v10 blueprint → v10 implementation plan → this file → the dashboard → v9 → v8 → `considerations.md` (cross-cutting gaps the dashboard omits) → the code, and verify built-vs-pending against `src/`. _(Founder ruling 2026-06-18, after a too-quick "thin backlog" call was disproven.)_
5. **Context authority + the mandatory per-cycle reset (founder ruling 2026-06-21, strengthens the 2026-06-18 ruling).** After EVERY cycle, the lane clears its context to zero and immediately re-invokes a fresh session to start the next cycle - it does NOT just compact-when-heavy. The strict order: (1) close the item out (register status `✅`/`◐` + at-a-glance recomputed, so it is closed and not re-pickable), (2) release the claim (`lane.sh release <ID>`), (3) commit with a WHY + push to `origin/main` and confirm `git status` is CLEAN (no uncommitted work, nothing lost). ONLY THEN: `/clear` (context to zero) and **immediately re-invoke + start the next build** (pull, claim the next free item, build). The clear is a mid-loop reset, never a stop; a cycle that ends without the next auto-starting is a failure - re-invoke. Never clear before the push + clean-tree check, never mid-build. Note the roll in the handoff (`.remember/remember.md`). Full loop: [`../../AGENTS.md`](../../AGENTS.md) §3 (the per-cycle loop, step 5).
6. **This file is the single source of truth.** Reconcile new status, decisions, deferrals, findings, and progress HERE. If a tracking file becomes redundant, fold it in and remove it. _(Founder ruling 2026-06-18.)_
7. **The non-negotiables that already governed the loop still hold:** never self-pause/idle (only a real usage limit pauses, with a sub-5-min recheck); only the founder ends the run; the per-cycle correctness gate is `tsc --noEmit` + `bun run build` + the feature's tests + the adversarial runtime-fatal review (style-lint + authored-humanization scanning are deferred per ruling 9); humanized output for user-facing GENERATED text stays a hard gate via the runtime sanitizer (`humanizeText`); commit explicit paths with a WHY; work in the worktree and fast-forward push to `main`. Full operating manual: [`../../AGENTS.md`](../../AGENTS.md); loop playbook: [`../operations/autonomous-build-loop.md`](../operations/autonomous-build-loop.md).
   - **7a. CLAIM DISCIPLINE — no two sessions on the same item (founder ruling 2026-06-22, after repeated cross-session duplication wasted tokens).** Claim the EXACT register-row id `lane.sh next` printed (NEVER a private sub-id — it leaves the whole item eligible to others; `lane.sh claim` now WARNs on a non-register id). HOLD the `🔨 In Dev` dashboard status (the cross-tool lock that `lane.sh next` + Lovable both honor) AND the claim across ALL increments on an item; release / flip to `◐`/`✅` ONLY when you PIVOT to a different register item — never the per-increment release-churn that re-exposed items between cycles. Full fix + root cause: [`../strategy/session-decisions.md`](../strategy/session-decisions.md) (2026-06-22 claim-discipline entry) + `AGENTS.md` §3.
8. **Capture everything important, comprehensively.** Every strategy doc, decision, analysis, insight, and answer to an important question is written comprehensive and thought-process-oriented (the reasoning + the why, not just the conclusion), in the same session, so the docs serve YC / accelerator / investor applications and answer questions by reference. **This file (sections 3 + 4) is the work-inventory single source of truth:** section 3 = what gets built next (autonomous queue), section 4 = the founder to-do list (what needs you). Check here first to know what to pick up. _(Founder ruling 2026-06-19.)_
9. **Velocity ruling: ship fast, batch deferrable quality passes to a founder-prompted end-stage (founder ruling 2026-06-19).** Build the working product first; do NOT spend per-cycle effort or tokens on non-trivial deferrable items: authored-content em/en-dash + AI-template scans, AI-trace polish, repo-wide lint/prettier/style cleanup, deep doc prose-polish, design. Write clean by habit (humanized-output Tier 2), note the deferral, and keep building. The per-cycle hard gates stay correctness-only (ruling 7). The runtime humanization sanitizer stays ON (it protects user-facing generated output, the Tier-1 hard gate). As the product nears launch the loop **prompts the founder** to initiate the deferred batch (HUMAN-SWEEP, lint/prettier cleanup, AI-trace, the §14 design pass); it does not auto-run them. This is an operating instruction for behavior, not just a doc. Canonical detail: [`../../AGENTS.md`](../../AGENTS.md) §3 "Velocity ruling".
10. **Build vs Buy vs Integrate (the BBI gate; founder ruling 2026-06-20, STRENGTHENED).** DEFAULT TO BUILD - the platform is NOT an assembly of external products; the USP lives in-house; only an irreducible non-moat commodity with minimal spend + a self-host/BYOK fallback leaves the building. The AGENT owns the call from its own web-grounded research (the founder is consulted only for a spend approval / a secret / a taste call); it applies forward AND retroactively (a chokepoint-bypass or a fallback-less external dep is a finding to FIX, not grandfather). Before building ANY new capability from core, classify it: BUILD (the moat - typed decision ontology / outcome-labeled supersession / the Critic / signal->ontology normalization / the Critic's precedent-salience / system-of-record), BUY (a commodity API: inference, embeddings, rerank, OCR, email, OAuth - routed through the chokepoint, never called directly), or INTEGRATE (a high-lock-in / autonomy-floor / residency-sensitive substrate, wrapped behind our own swappable seam with a native default + graceful fallback). The moat is never bought or wrapped; commodity infra is never built; the autonomy floor (a zero-external-paid-dep native default) always holds. Carry a greppable `BBI:` stamp on the deciding doc/PR. Full gate + the worked memory-stack verdict: [`../strategy/build-buy-integrate.md`](../strategy/build-buy-integrate.md); operative copy in [`../../AGENTS.md`](../../AGENTS.md) §3.0c.

---

## 2. Current status (where the product is)

- **Cycles 1-19 shipped AND live-verified on the published app** (`cadence-flow-beta.lovable.app`). All checked features work. The core loop (Sense → Decide → Plan → Build → Launch → Learn), the Engine Room (approvals, attention, incidents, spend, trust dial), the 13-agent mesh with handoffs + memory-as-moat, discovery + per-product clustering, provenance, and missions are all live.
- **Completion (live, derived from the register): 80.4% strict / 83.0% weighted** - 144 of 179 tracked rows fully done (recomputed 2026-06-22 18:40, all lanes; kept real-time — updated in the same commit as every status flip per the founder ruling). ◐ partial counts at its per-row `[~NN%]` estimate, ⏸️ paused counts half toward the weighted figure. The remaining ~20% is mostly founder-gated (22 Gated 👤 + 10 Deferred); only a handful is autonomously buildable. Splits into the autonomous queue (section 3) and the founder pickup list (section 4). **Per-item % + per-category roll-up + the recompute method:** [`feature-dashboard.md`](./feature-dashboard.md) "At a glance" + its **Progress accounting** standing rule.
- **Two open findings from the live sweep** (neither a code bug), section 5.
- Milestone framing (legacy M-0 to M-D): M-0/M-A (loop on real data) and M-B (moat visible) are met (the loop is live-verified, /memory + Gauntlet shipped); M-C (monetize) and M-D (dual-user/scale) map to the founder pickup list (section 4).

---

## 3. THE BUILD QUEUE (autonomous, the loop builds straight down this)

Everything here needs **no founder input** and can be built, gated offline, and flagged for publish-verify. Source: 2026-06-18 backlog reconciliation (cross-referenced canon vs `src/`).

**Ordered by STRATEGIC IMPACT vs the current positioning** (workflow `cadence-strategic-build-rank`, 2026-06-18; founder ruling: highest-impact first, never buildability). Scoring lens = the current v10 milestone gate (Sprint P0: close the loop on REAL DATA + land the wedge). The catalog tables below are the buildable set; the strategic order + the pruned (off-milestone) set are at the end of this section.

### #1 (shipped this cycle), `F3-CRON` continuous auto-cluster (always-fresh SENSE)
Strategic #1 (impact 7/10): it advances the binding P0 constraint (autonomous SENSE on real data) from the only side code can move, since SEN-01 is founder-blocked on an OAuth registration. Today `clusterSignals` only runs when a human pokes it, so the front of the loop is not autonomous; a scheduled incremental re-cluster is what the North Star demands at SENSE. **Shipped 2026-06-18 (cycle 20), gated OFF** so it commits zero AI spend until activated: a `cluster-tick` hook + an extracted `clusterSignalsCore` + an owner opt-in toggle + the `auto_cluster_enabled` migration. **Activation is founder-gated (section 4):** apply the migration on publish, owner enables the toggle, then wire the cron schedule. Detail: [`../features/f3-continuous-discovery.md`](../features/f3-continuous-discovery.md).

### P1
| ID | Title | Migration | Files / domain | Scope | Confidence unbuilt |
| --- | --- | --- | --- | --- | --- |
| `DATA-RETENTION` | ai_events/token_usage retention + purge | yes | new migration (pg_cron purge) + `api/public/hooks/retention-tick.ts` + `projects.functions.ts` | Bounded retention + right-to-be-forgotten cascade; conservative default TTL (e.g. 180d). **Time-based purge ◐ shipped c47. Right-to-be-forgotten cascade ◐ shipped as `DATA-RETENTION-b` (Lane 0, 2026-06-21):** dormant, service-role-only `forget_workspace`/`forget_account` + `erasure_residue` (`20260621012900`); FK-order-agnostic dynamic delete over all 61 `workspace_id` tables, zero-destruction dry-run clean. Follow-up `DATA-RETENTION-c` (founder/legal): PERSON-level erasure (auth.users + `user_id`-only rows + Storage objects + account-scope residue). | HIGH |
| `PROVIDER-FALLBACK` | Multi-provider fallback chain | no | `runtime.server.ts` + new `fallback.ts` | **◐ SHIPPED cycle 56 (2026-06-20).** Generalized the single `fallbackModel` into a pure ordered `resolveFallbackChain` (back-compat) + a flag-gated (`AI_PROVIDER_FALLBACK`, default OFF) auto cross-model degrade to `cheapestLiveModel()`, excluding eval+embed. Happy path untouched; engine ✅ unit-verified (10 tests), live outage-degrade ◐ on the flag + publish. Supersedes the prior "attended-bundle" parking (founder ruling 1: don't park risky-but-buildable work; the change is surgical + flag-gated + offline-verifiable). PLAN-ENFORCE + MODEL-REGISTRY-DEPRECATION remain the attended chokepoint pair. |
| `PLAN-ENFORCE` | Plan-tier runtime enforcement | yes | `entitlements.ts` + `runtime.server.ts` + migration (quota cols) | Enforce free-vs-pro caps at the chokepoint (entitlements are feature-flags only today); sensible defaults, founder tunes numbers later. _Sequence with PROVIDER-FALLBACK (both touch runtime.server.ts)._ | HIGH |
| `MODEL-REGISTRY-DEPRECATION` | Deprecation flags on the model catalog | no | `src/lib/ai/models.ts` + `runtime.server.ts` | **◐ SHIPPED cycle 57 (2026-06-20).** Added `deprecated`/`replacement`/`sunset` to the Model type + a pure, cycle-safe `activeModelId` resolver (follows deprecated->replacement to a LIVE terminal, else returns the original; 8 tests) wired before cost-routing in both `callModel` + `callModelStream`. Dormant playbook: no model is flagged today, so it is the identity function (byte-identical); the route-around activates when a provider sunset is recorded. With PROVIDER-FALLBACK (c56) this closes `considerations.md` #4. The benchmark-cadence half stays excluded (gated). |

### P2
| ID | Title | Migration | Files / domain | Scope | Confidence |
| --- | --- | --- | --- | --- | --- |
| `F-AGENTS-MENTIONABLE` ✅ shipped 2026-06-18 | @-mentionable agents → mission | no | `chat.ts` (server parse + single-step dispatch) + `_authenticated.chat.tsx` (composer @-picker) | SHIPPED: parse `@agentslug` → pre-planned single-step mission to that specialist; composer picker reuses `listAgents` | n/a (built) |
| `U6-AUDIT` | Export audit log | yes | `projects.functions.ts` + new `export_log` migration + `DataExportCard.tsx` | Audit row per export + history view | PARTIAL |
| `R3-PREFS` ✅ shipped 2026-06-20 | Notification prefs + in-app digest | yes | `notifications.functions.ts` + new `user_notification_preferences` migration + Settings UI | SHIPPED (parallel/cockpit cycle 1): User preferences table + RLS + settings `/notifications` page + email/digest scaffolding helper fns. | n/a (built) |
| `F3-CRON` ✅ shipped 2026-06-18 (gated off) | Continuous auto-cluster cron | yes | `cluster-tick.ts` + `cluster.server.ts` + `discovery.functions.ts` + `SignalsPanel.tsx` + migration | SHIPPED gated off (see #1 above); activation is founder-gated (section 4) | n/a (built) |
| `P7-COST-INCIDENT` | Cost-incident source + persistent incidents table | yes | `incidents.functions.ts` + new `incidents` migration | Cost-threshold incident derivation + persistent table | PARTIAL |
| `K2` ◐ operator path shipped 2026-06-18 | Rollback / revert-to-revision + flag kill | no | new `studio-revert.server.ts` + `studio.functions.ts` (`revertToRevision`) + `ChangesPanel.tsx` | SHIPPED operator revert button (non-destructive forward commit via GitHub Data API). Deferred K2b: `studio.revert` agent tool (needs `agent_tools` migration) + flag kill | partial |
| `F-BUILDER-MULTIFILE` | Pre-declared touch-list + max-N scope | maybe | `studio.functions.ts` + `registry.server.ts` + `builder_file_claims` | Up-front declared touch list + per-mission file-count cap | PARTIAL |
| `P4-GATE` ✅ shipped 2026-06-18 | Eval regression as a hard merge gate | no | new `eval-gate.ts` + `registry.server.ts` (`studio.pr.merge`) | SHIPPED: ≥10pt latest-vs-prior eval drop (per suite) blocks the agent's merge; pure readiness fn mirrors the J2 CI gate; reads the scheduled eval trend | n/a (built) |
| `P5-ALERT` ✅ shipped 2026-06-18 | Drift threshold → Attention/incident | no | `notifications.functions.ts` (4th probe over `drift_incidents`) | SHIPPED: open drift incidents surface in the Attention feed as severity-coded cards linking to `/drift`; live-derived, no migration | n/a (built) |
| `D4-REPLAY` ◐ replay shipped 2026-06-18 | Replay-and-branch + checkpoint-diff | yes (branch link) | `orchestrator.functions.ts` + `missions.functions.ts` + `_authenticated.missions.$missionId.tsx` + migration | SHIPPED replay-with-model + `replayed_from` branch link. Deferred D4b: rich checkpoint-diff | partial |
| `FND-0.5` | Prompt pre-filter on tool allow-list + product scoping | maybe | `loop.server.ts` + `registry.server.ts` + `agent_tools` | Pre-filter high-risk tools before they enter the prompt; scope allow-list by product | PARTIAL |
| `KI-16` | Per-tick advance cap | no | `mission-advance.server.ts`/`resume-runs.ts` | Per-tick dispatch cap (only a global `MAX_RUNNING_PER_WORKSPACE=5` exists) | HIGH |
| `O3` | Fact-currency / staleness flag | maybe | `lineage.functions.ts` / knowledge fns | Flag stale facts on the provenance graph (skill-pack-over-MCP half depends on Q1/Q2, excluded) | HIGH |

**Strategic order (by impact vs the current P0 milestone), 2026-06-18 rank:**
1. ✅ `F3-CRON` (7) shipped (cycle 20), gated off (above).
2. ✅ `F-AGENTS-MENTIONABLE` (6) shipped. Felt agentic-command, deterministic single-step-DAG reuse of the chat→mission spawn. Server half auto-committed mid-build by a parallel process (mislabeled "cycle 19", `40646dce0a`); cycle 21 completed it (composer @-picker + case-insensitive parse) and reconciled the docs.
3. ✅ `P5-ALERT` (6) shipped (cycle 22). Open `drift_incidents` now surface in the Engine Room Attention feed (R3) as severity-coded cards linking to `/drift`; a 4th live-derived probe in `getNotifications`, no migration.
4. ✅ `P4-GATE` (6) shipped (cycle 23). `studio.pr.merge` is now hard-gated on eval regression too (new `eval-gate.ts`, mirrors the J2 CI gate): a ≥10pt drop in the latest completed eval run vs the prior, per suite, blocks the agent's merge. Reads the scheduled eval trend, no run triggered.
5. ◐ `K2` (rollback/revert-to-revision) operator path shipped (cycle 24): a non-destructive "Revert to revision" button in the Changes tab (new `studio-revert.server.ts` + `revertToRevision` server fn). Deferred `K2b`: the `studio.revert` agent engine-tool (needs an `agent_tools` migration) + feature-flag kill.
6. ◐ `D4-REPLAY` (replay-and-branch) shipped (cycle 25): re-run a finished mission with a chosen model + a `replayed_from_mission_id` branch link (additive migration). Deferred `D4b`: the rich checkpoint-diff (side-by-side).
7. **NEXT:** `FND-0.5` (prompt pre-filter on tool allow-list + product scoping), then `F-BUILDER-MULTIFILE`, `U6-AUDIT`, `P7-COST-INCIDENT`, `K2b`, `D4b`.

### Workspace, Accounts & Tenancy + Monetization (WM-*), NEW 2026-06-19 (founder-directed)
The current founder-directed initiative: the Account -> Workspace -> Product tenancy redesign + account-level monetization (credits-only self-serve; BYOK removed from self-serve, enterprise-only; the moat is the decision layer, memory is one layer, see [`../strategy/moat.md`](../strategy/moat.md)). Full strategy + per-ID build instructions (context, files, migrations, steps, acceptance, verification): [`workspace-tenancy-and-monetization-plan.md`](./workspace-tenancy-and-monetization-plan.md). The live pick-order + effort + status board is group **G10** in [`feature-dashboard.md`](./feature-dashboard.md).

**What to build next here:** `WM-M1` (entitlements core, effort S) and `WM-F1` (scope agent memory to workspace, effort L) are the critical path (no dependencies), then `WM-M2` (accounts table + migrations, L). Build straight down the G10 pick-order. This initiative carries/expands the legacy `A6/ENG-08` (RBAC/invites) and `M-C-PRICE`/`M-C-EXPIRY` (pricing/decay) items, so build from the WM rows, not those. The **credit engine** (the metering math behind the dormant `WM-M4` seam) is now specified in the same plan as `WM-M10` to `WM-M16` (§4.2.1): cost-to-credit conversion, per-tier amounts + grant/reset, the debit draw-down, capped top-ups, per-product/member attribution, margin levers, and the credit UI; the two parallel monetization threads merged into the one build bible (2026-06-19). Build them after `WM-M2` (the pool) + `WM-M4` (the seam) exist; `WM-M10` can start as soon as `WM-M1` lands. Founder-gated flips (Stripe secrets, the credit numbers, `credits_enabled()`, prices, final tier names) are in section 4.

**Deferred packaging (post-core, do NOT build now):** `WM-M17` (clubbed usage variants, Max 5x/20x + Team Standard/Premium seats), `WM-M18` (plan-card states: a "Current plan" tag + "Upgrade" calls to action, no current/base duplication, + a guarded downgrade), and `WM-M19` (enterprise per-seat + API-rate usage + per-user credit allocation on the shipped `WM-M14` caps) adopt Anthropic's two-toggle pricing presentation tuned to our moat (founder decision 2026-06-19). Documented + scheduled but gated behind the core builds (need `WM-M3` + `WM-M6`); prices, multipliers, names, and the downgrade policy are founder-gated (section 4). Full spec + a ready-to-send Lovable prompt: the bible §2.4.1 + §4.2.2. **(Ownership change 2026-06-21: `WM-M17`/`WM-M18`/`WM-M19` are now LOVABLE-OWNED + frozen, like the rest of the credit/pricing engine; Lovable builds the packaging. Not a Claude pick.)**

**Pruned list, RESOLVED 2026-06-21 (the strategy reconciliation found this list self-contradicted the shipped rows: 5 of the 7 "do NOT build now" items were built anyway, the priority drift this realignment exists to catch).** Of the 7, 6 are now BUILT or ABSORBED, re-authorized by the strengthened BBI default-to-build (ruling 10) + the velocity ruling, so they stay shipped (do NOT re-litigate): `DATA-RETENTION` (shipped cycle 47), `APP-HEALTH` (cycle 50), `PROVIDER-FALLBACK` (cycle 56), `MODEL-REGISTRY-DEPRECATION` (cycle 57), `KI-16` (per-tick cap, built 2026-06-20), `O3` (absorbed into the Decision Brain H1 as DBR-3; its "depends on the unbuilt O1 graph" rationale is stale, O1/DBR-1 shipped 2026-06-20). These are Tier-3 in the Build Sequence (non-essential ops/reliability hygiene), strictly BELOW the Decision Brain H1 (per moat.md §8: hard-gate decision-layer capabilities, NOT ops features). The lone genuinely-unbuilt item, `PLAN-ENFORCE`, is **deferred-by-priority + attended-chokepoint** (it touches `runtime.server.ts` and cannot be verified unattended; bundles with the chokepoint trio), ranked Tier-3.

### Decision Brain increment 1 - Ambient Precedent (DBR, NEW 2026-06-20, founder TOPMOST, IN BUILD)
**SHIPPED to `main` (◐), 2026-06-20 overnight cycle 55, subagent-driven, all 6 tasks T1 to T6** (was the single highest-priority autonomous pick, ahead of the WM lane's remaining work). The proactive precedent nudge: surface the workspace's own past shipped outcomes wherever a decision is called out, via a semantic match over the outcome memories we already store. Migration-free, best-effort, fail-safe. Engine + ranker ✅ unit-verified; the 3 seams ◐ render-on-publish. **Pick-source (the executed plan):** [`ambient-precedent-plan.md`](./ambient-precedent-plan.md). Spec + as-built status: [`../features/ambient-precedent.md`](../features/ambient-precedent.md). Tracked under dashboard row 148 (DBR umbrella) - these were TASKS under that row, not new register rows (the at-a-glance counts did not move). DBR-0 (Critic recency precedent) shipped first; T3 superseded it with the semantic engine (the old recency helper is deleted).

| Order | Task | Files | Gate |
| --- | --- | --- | --- |
| **T1** (P0) | Persist `prd_title`/`opp_title` on outcome memories (forward-only jsonb; no migration) | `src/lib/ai/memory.server.ts` · `src/lib/outcome.functions.ts` | tsc + build |
| **T2** (P0) | The semantic precedent engine: pure `rankPrecedent` (TDD) + live `loadDecisionPrecedent` (embed -> `match_agent_memory` -> metadata join -> threshold + cap); `[]` on any failure | NEW `src/lib/ai/decision-precedent.server.ts` + `.test.ts` | bun test + tsc + build |
| **T3** (P0) | Critic uses the engine (seam 1); delete DBR-0's superseded recency helper | `src/lib/ai/critic.server.ts` | tsc + build + bun test |
| **T4** (P1) | `getDecisionPrecedent` server fn the UI seams call | NEW `src/lib/decision-precedent.functions.ts` | tsc + build |
| **T5** (P1) | The `PrecedentNudge` UI primitive (invoke `impeccable` + Engine-Room + Ember; mirror `CriticBadge.tsx`) | NEW `src/components/decision/PrecedentNudge.tsx` | tsc + build |
| **T6** (P1) | Wire `<PrecedentNudge>` into the opportunity + spec seams (seams 2+3) | the two `CriticBadge` render sites | tsc + build |

T1-T3 are P0 (the engine + the wedge); T4-T6 are P1 (the UI surfacing). Build top-down; the engine + ranker mark ✅ once unit-tested, the Critic + UI seams stay ◐ until render-verified on a publish.

### BYO Repo + All-in-One Platform (BYO-*), NEW 2026-06-19 (founder-directed)
Provider-agnostic repos (GitHub/GitLab/Bitbucket) + managed infrastructure so a user runs their whole product org on Cadence (discovery to launch, one subscription). Spec [`byo-build-and-cadence-cloud.md`](../strategy/byo-build-and-cadence-cloud.md); all-phase plan [`byo-build-implementation-plan.md`](./byo-build-implementation-plan.md); live board group **G11** in [`feature-dashboard.md`](./feature-dashboard.md). Build order: **BYO-P1a keystone first** (RepoProvider interface), then P1b/P1c parallel, P1d, then P2-P5. **Phase 1 awaits founder greenlight (no product code until approved).** Overlap: **BYO-P4 (managed AI credits) IS the WM credits work** (G10) - built there, cross-referenced, not duplicated. BYO-P1b depends on the WM Product/tenancy model + must honor the workspace session's `agent_memory` RLS isolation.

---

## 4. Founder pickup list (gated, needs you; the loop will NOT do these)

When you have a moment, these unblock the next tier. Each needs a decision/secret/account from you.

- **Sourcing-map cluster calls (2026-06-21, full table + savings in [`../strategy/sourcing-map.md`](../strategy/sourcing-map.md)).** The cluster-level Build-vs-Buy-vs-Integrate decisions that are yours; each one lets agents stop building a whole category and wire a provider instead: (1) register ONE provider OAuth client (Slack / GitHub-issues / support) to light up a 2nd live source; (2) confirm reusing the OpenAI key for embeddings + ratify NO external LLM gateway; (3) SPEND: enable Supabase Pro + PITR (satisfies `DR-BACKUP`, no build); (4) confirm the SANDBOX provider (rec: Cloudflare Sandbox SDK) + its compute line; (5) pick the enterprise-SSO+SCIM direction (rec: WorkOS, no spend until a deal); (6) pick the uptime/status/on-call SaaS (rec: Better Stack, free tier) + approve a public status page; (7) pick the inbound product-analytics provider (rec: PostHog); (8) pick the OSS injection-detector floor (rec: LlamaFirewall PromptGuard 2); (9) pick the remote-MCP OAuth provider (rec: Cloudflare OAuthProvider) + the outward A2A posture; (10) confirm the strategic "no-buy" on the decision layer (PRD/roadmap SaaS stay competitors-to-absorb). The Decision Brain needs no shape call (build the judgment, self-host the substrate, buy embeddings); only the embed/rerank cents-line + the `DBR-1.5` flag flip are gated.

- **Horizon bets enrichment (TOPMOST, founder ruling 2026-06-20)**, the Decision Brain (H1) + Command Canvas (H2). One enrichment pass with you settles: (1) graph storage (bi-temporal property graph on Supabase/pgvector for v1, vs a Graphiti/Neo4j reference); (2) how the Command Canvas relates to today's **Ask** (`/chat`), recommended: the Canvas is the *evolution* of Ask (one NL surface + a live preview), not a second box; (3) H2 scope (ship the preview half first, then the intent bar); (4) confirm the provisional IDs `DBR-0..4` / `CMD-0..2` + lane assignment. Then H1 promotes into the build queue ahead of the rest (`DBR-0` is already DONE ◐ as of 2026-06-20: the Critic now cites decision precedent from past outcomes; it needed no founder input). Specs: [`../features/decision-brain.md`](../features/decision-brain.md), [`../features/command-canvas.md`](../features/command-canvas.md); strategy [`../strategy/horizon-bets.md`](../strategy/horizon-bets.md). The H1 increment-1 (Ambient Precedent) granular, cold-buildable build plan, IN BUILD, is [`ambient-precedent-plan.md`](./ambient-precedent-plan.md) (build queue: section 3).
- **`F3-CRON` activation** (mechanism shipped 2026-06-18, gated off), to turn on always-fresh SENSE: (1) the migration applies on your next publish; (2) a workspace owner flips the "Auto-cluster new signals" toggle in the Signals tab; (3) wire a scheduler to POST `/api/public/hooks/cluster-tick` every ~6h (pg_cron snippet + publish-verify checklist in [`../features/f3-continuous-discovery.md`](../features/f3-continuous-discovery.md)). It commits recurring AI spend, so the on switch stays your call.
- **`F-CONN`**, register ≥1 OAuth client (GitHub/Linear) to unblock a 2nd live ingest source (`SEN-01`).
- **`SEN-05` / `F-ANALYTICS-1/2`**, product-analytics connector OAuth + recurring spend.
- **`M-C-PRICE`**, set Stripe secrets + price IDs to flip billing live.
- **`M-C-EXPIRY`**, flip `memory_expiry_enabled()` (monetization-timing / product-tasting call). WM-M2 reworks this to a 30-day rolling window.
- **WM initiative founder gates** (full list in [`workspace-tenancy-and-monetization-plan.md`](./workspace-tenancy-and-monetization-plan.md) §7): set the Crescendo (`max`) and Galaxy (`team`) per-seat prices; set Stripe secrets + price IDs for `WM-M3` (account-level checkout); set the credit numbers (`CREDIT_COGS_USD` + per-model rate for `WM-M10`; per-tier monthly credit amounts + the starter free grant for `WM-M11`; top-up pack prices + the per-cycle ceiling for `WM-M13`); flip `credits_enabled()` once the credit engine (`WM-M10` to `WM-M16`, plan §4.2.1) lands on the `WM-M4` seam; confirm the final tier display names + motif (Constellation is the default, rename-able anytime via the slug decoupling). **For the deferred Anthropic-style packaging (`WM-M17`/`WM-M18`/`WM-M19`, bible §2.4.1 + §7 gates 9-12):** set the Max usage-variant multipliers + prices ("5x" / "20x more usage than Pro", about 100 / 200 dollars) and the Galaxy seat-variant prices (Standard about 25 to 30 dollars per seat, Premium higher), with each multiplier and its label set together so the math and copy agree; pick the downgrade policy (block-when-over-limit vs defer-to-cycle-end); pick the enterprise usage model + per-user allocation policy; and confirm plain names (Free / Pro / Max / Team / Enterprise) vs the Constellation motif as the customer-facing label.
- **`SANDBOX` / `BLD-05`**, pick a sandbox provider (Cloudflare Sandbox SDK / E2B / Vercel); infra + spend.
- **`BLD-04`**, delegate-out to external coding agents (BYO-key + governance posture).
- **`Q2`**, external A2A server (outward-facing surface, scopes/audit posture).
- **`A6 / ENG-08`**, roles / RBAC / invites (team-tier sequencing call; can be built as a migration when you green-light the tier).
- **Demo seed lineage edges**, a migration is buildable, BUT it touches the drift-prone `seed_demo_workspace` and needs a re-seed to show live; risky to do blind. On your OK the loop will add `recordLineage` to the seed so demos show populated provenance (finding 2, section 5).
- **PII mandatory-redaction policy**, mandatory-vs-optional is your policy call.
- **`DR-BACKUP` (disaster recovery)**, backups / point-in-time restore / restore drills. `considerations.md` flags this as a P0/P1 "data loss is existential" gap, but it lives on Lovable-managed infra (DB / hosting / backups), so it is a founder + Lovable infra decision, NOT autonomous Claude Code build work. Added here so no lane tries to build it. (`DATA-RETENTION-b`, the right-to-be-forgotten erase cascade, IS buildable and sits in the Build Sequence Tier 3; only its destructive-scope policy is yours.)
- **The design / UX-polish pass**, ruled LAST, once, on the finalized product (standing instruction 2).
- **`F-IA-*` IA consolidation** (cull nav, merge routes, collapse to 7 surfaces), design-adjacent / product-tasting; bundled with the design pass.
- **Cut/defer post-PMF:** `F-AUDIO-1/2`, `SEN-04`.

---

## 5. Open findings, bugs, and solutions

- **Finding 1 (transient, not a bug):** `/product?tab=specs` rendered empty once during the deploy window; clean on reload. Likely a deploy/hydration hiccup. Watch; not reproducible.
- **Finding 2 (seed-data gap, not a bug), queued to founder:** the demo seed never writes `artifact_lineage` edges, so the Lineage drawer + O1 provenance always show empty states on demos. Features are wired correctly and degrade gracefully, but the demo cannot show off lineage/provenance/the memory-moat story. Fix = a seed migration + a re-seed (founder pickup, section 4).
- **Dashboard corrections (stale rows, trust this file + the code over them):**
  1. `F-CHAT` "Ask can spawn a mission", v10 §7 frames it as a gap but it is **already built** (`src/routes/api/chat.ts:162-383`). v10 text is stale.
  2. v10 §15 lists `D4/P7/P3/R3/B5/U6/Q1-MCP/DEC-02-LOOP/H1-TASKS/MOAT-VIS/AMBIENT-ARC/F-SHARE-TEARDOWN` as ⬜/◐, the dashboard (fresher) marks most ✅/◐ with the real remainder. Trust the dashboard over v10 §15.
  3. `KI-15`+`KI-16` are bundled in one row: KI-15 is **done** (`sweepStaleUnconsumedMessages`), KI-16 (per-tick cap) is genuinely **unbuilt**.
  4. `health.functions.ts` is a migration-drift checker, NOT an app health endpoint (so `APP-HEALTH` is real work).
  5. `considerations.md` P0 gaps (app monitoring, data retention, plan enforcement, provider fallback) are NOT dashboard rows at all, real autonomous work the dashboard omits. This is why the backlog looked thinner than it is.

---

## 6. Progress log (append-only, newest first)

- **2026-06-21, Lane 3 (numbered parallel build), TENANCY: WM-F9b active-workspace read-filtering slice SHIPPED (◐, board row 17, [~80%]→[~85%]).** Founder-directed pick. The first DRIVEN cut of WM-F9's deferred "reads" half: the meetings list now narrows to the active workspace. New PURE `src/lib/workspace-scope.ts` — `workspaceScopeId` (the single scoping rule: filter iff a concrete non-blank string id, else null = unfiltered/today) + `applyWorkspaceScope(query, id)` (adds `.eq("workspace_id", id)` exactly once iff id present; the one place the long tail reuses so reads cannot drift); 7 tests pin both the rule and the conditional-filter behaviour. `listMeetings` (`meetings.functions.ts`) gains an optional `workspaceId` (zod `uuid().nullable().optional()`, default `{}`) routed through the helper; omitted/null is byte-identical to before (30-day window, ordering, the 502-retry closure all preserved). `CalendarPanel.tsx` passes `activeWorkspaceId` from `useWorkspace()` and keys `["meetings", activeWorkspaceId]`, which also delivers WM-F8b's refetch-on-switch for meetings (WM-F8's switch-reset already treats `meetings` as workspace-scoped). **Additive + RLS-safe:** the read is dual-keyed (`auth.uid()=user_id AND is_workspace_member`), so a client-supplied id can only NARROW within already-authorized rows (a non-member workspace returns nothing, never a leak) and a stale localStorage id can't reach the query (use-workspace adopts only ids in the user's own list). **4-lens adversarial-review Workflow** (data-visibility · security/no-lockout · regression · completeness) → **3 ship + 1 ship_with_fixes, ZERO must-fix**; folded both should_fix items (a pure mock-tested `applyWorkspaceScope` pinning the filter branch; itemized WM-F9c on the board). **`sync_mappings` verdict RATIFIED-deferred:** every `sync.functions.ts` access is `.eq("id").eq("user_id")` on connector version-state, not a workspace-browsable list, so it scopes with the connectors workspace-binding work, not here. tsc 0 / `bun test` 664 (7 new) / eslint clean on changed files. **◐ not ✅:** logic ✅ unit-verified; live read-filter renders + verifies on the founder's publish (alongside the WM-F9 `meetings.workspace_id` column). **Remaining → WM-F9c** (board row 17): brief client-wiring (sole caller settings.tsx, another lane), `listCopilotMessages` (no caller), prototype/scheduler/ritual reads, internal AI-context reads — all reuse `applyWorkspaceScope`. Spec: [`workspace-tenancy-and-monetization-plan.md`](./workspace-tenancy-and-monetization-plan.md) (WM-F9 §).
- **2026-06-21 13:32, Lane 3 (numbered parallel build), GOVERNANCE: FND-0.7 prompt-injection classifier + hard quarantine SHIPPED (◐, register #49).** Built the unbuilt "learned classifier + hard quarantine" half of the injection defense (the regex-only `injection` guardrail kind is now backed by weighted evidence). New PURE `src/lib/injection-classifier.ts` (`classifyInjection` = `sigmoid(BIAS + Σ wᵢ·present(featureᵢ))` over ~14 lexical+structural signals; presence-capped so corroboration-not-repetition drives the score; NFKC+confusables homoglyph fold; ReDoS-safe; 29 tests) + a fail-open server seam `src/lib/ai/guardrails-injection.server.ts` (`quarantineUntrusted`) wired into the RAG retriever's `formatContextBlock` (live drive point; benign chunks byte-identical; quarantined chunks → content-free placeholder + `quarantined="true"` + a content-free `[injection-defense]` log). **The load-bearing call:** hard quarantine (which STRIPS content) requires a STRUCTURAL signal (literal fence breakout / forged `System:` turn); lexical-only suspicion can only `flag` (kept behind the always-on fence), so benign first-party prose that quotes/discusses an attack (PRDs about deleting tables, bug reports quoting injections, red-team docs) is NEVER stripped from RAG context. **3-lens adversarial-review Workflow** (security/correctness/integration) → 0 blockers; the security + integration lenses independently proved the over-redaction (first cut quarantined benign 0.88-0.9998); folded the structural gate (the fix), presence-cap, homoglyph fold, encoding-evasion tighten, vocab broadening, quarantine telemetry; re-probed the reviewer's exact strings to confirm benign→allow/flag, real attacks→quarantine. tsc 0 / `bun test` 647 (29 new, incl. an over-redaction regression suite). **`bun run build` RED on the PRE-EXISTING Lovable vite-config ESM-cycle baseline** (`ERR_REQUIRE_ESM` at config-load before any of my source; Node 20.9.0 < Vite 20.19) — not mine. **◐ not ✅:** built + gate-green + reviewed + driven; live quarantine renders + verifies on the founder's next publish. Follow-ups: FND-0.7-b cross-chunk aggregation, FND-0.7-c reactor `untrusted_signal` coverage, FND-0.7-d governance UI. **Founder asked to stop after this cycle (session closing) — Lane 3 is NOT auto-continuing.** Decision rationale: [`../strategy/session-decisions.md`](../strategy/session-decisions.md); spec [`../features/injection-defense.md`](../features/injection-defense.md); detail [`../../plan.md`](../../plan.md) §4.
- **2026-06-21 06:25, Lane 2, LIVE VERIFICATION on the published app (founder-directed).** Tested this session's 3 ships on cadence-flow-beta.lovable.app (demo account). **MOAT-VIS → ✅ (register #2 flipped ◐→✅):** Today "What changed" + Brain `CompoundingPanel` both render correctly with real data; no bugs. **H1-TASKS → ✅ (register #7 flipped ◐→✅):** since the AI generator was credit-gated (0 balance) and both DB MCPs were down (Lovable blocked by a health hook, Supabase unauthorized), I seeded a real task DAG via an owner-scoped Supabase REST PATCH using the live browser session's JWT + the public anon key (RLS-permitted; fully reverted afterward). `TaskGraphPanel` rendered every value correctly (40% · 2/5 done · 2 ready · 1 blocked · ~13h · READY NOW #3 #5); a seeded missing-dep showed the integrity warning in `--rose` (MF-2 fix confirmed by computed-color match). No bugs. **DBR-1.5 → stays ◐:** dormant + safe confirmed live (0 supersession edges, flag OFF), but the migration is NOT yet applied to the live DB (`valid_to` absent, err 42703) - the founder must publish (runs the migration) then set `DECISION_BRAIN_SUPERSESSION=1` to activate; byte-identical/fail-safe until then. Zero code bugs found across all three. Detail: lane report `docs/planning/archive/parallel-report-knowledge.md`.
- **2026-06-21 05:10, Lane 2 (numbered parallel build), DEFINE: H1-TASKS task-graph CONSUMPTION slice SHIPPED (◐, register #7).** Generation was already shipped (`generateTaskGraph` writes `seq`+`depends_on`); this makes the stored DAG READABLE. New PURE `src/lib/task-graph.ts` (`sanitizeDependsOn`/`validateTaskGraph` DFS integrity/`topoOrder` Kahn-total/`readyTasks`/`summarizeTaskGraph` readiness partition; 13 tests) + a read-only RLS-scoped `getTaskGraph(prdId)` (mirrors `listTasks`) + a calm `TaskGraphPanel` "Build readiness" readout (done/ready/blocked/%/remaining-hours + DAG integrity) on `/prds/$id`. **2-lens adversarial-review Workflow** (topology/RLS · reuse/wiring/honesty) → 0 blockers, SHIP_WITH_FIXES; folded a stale-panel cache-invalidation defect (the panel didn't refresh on Generate), a role-color fix (`--rose` for failures, not `--ember`), and a self-dep double-report. tsc 0 (my files) / eslint 0 / `bun test` 551. Build red = same pre-existing Stripe-dep baseline. **◐ not ✅:** pure DAG logic ✅ unit-verified; panel renders on publish. Deferred (lane boundary): the task-graph → mission-steps bridge needs `studio.*` (Build lane). Detail [`../../plan.md`](../../plan.md) §4; spec [`../features/task-graph.md`](../features/task-graph.md) (Consumption).
- **2026-06-21 04:45, Lane 2 (numbered parallel build), MOAT: MOAT-VIS make-the-compounding-visible SHIPPED (◐, register #2).** The outcome loop already re-scores an opportunity's ICE from a real verdict and records WHY in a `learnings` row; this surfaces that compounding on the two front surfaces. New PURE `src/lib/moat-vis.ts` (`summarizeCompounding`/`rescoresOf`/`describeCompounding`; rounds to the displayed precision so a sub-0.1 jitter never reads as a move; honest/neutral since a missed outcome lowers ICE; 10 tests) + the named-missing `getCompounding` server fn in `today.functions.ts` (RLS-scoped, mirrors `listLearnings`, shares the pure summarizer so Today + Brain never drift). The Brain's Learnings tab now renders a new `CompoundingPanel` (aggregate "memory re-scored N decisions, net ICE ±X" line + a cause-carrying feed) instead of the old MemoryPanel landing; Today's "what changed" card gains the aggregate compounding line (gated at >1 rescore = the honest "not a one-off" call). **2-lens adversarial-review Workflow** (correctness/RLS/regression · design/honesty/duplication) → 0 blockers, SHIP_WITH_FIXES; folded the 2 real defects (a RAW vs rounded ICE-move test that would show a sub-0.1 jitter as a move + inflate the count → now rounds like the repo's `countPriorityMoves`; a missing error branch that disguised a fetch failure as an empty loop → added one mirroring `DecisionsPanel`). RLS clean (faithful listLearnings mirror, no leak). tsc 0 (my files) / eslint 0 / `bun test` 519. Build red = the same pre-existing Stripe-dep baseline. **◐ not ✅:** pure logic ✅ unit-verified; live read + render verify on publish. Follow-up MOAT-VIS-b (logged): delete the now-orphaned `MemoryPanel.tsx` + extract shared `whenOf`/`VERDICT_TONE`. Detail [`../../plan.md`](../../plan.md) §4.
- **2026-06-21 04:10, Lane 2 (numbered parallel build), MOAT: DBR-1.5 supersession engine SHIPPED (◐, register #1 — the TOPMOST item).** The Decision Brain's signature write-path: when a human records an outcome, infer + write typed `supersedes`/`contradicts` edges between the account's own past decisions into `artifact_lineage`, bi-temporally (invalidate-don't-delete). New PURE `src/lib/ai/supersession.ts` (verdict-conflict matrix + endpoint resolver + cap/dedup + edge builder; 17 tests) + gated orchestrator `supersession.server.ts` (`inferSupersession`: flag-first-line dormant = zero embed/write/byte-identical `recordOutcome`; the only AI cost is one `embedOne` via the shipped `loadDecisionPrecedent`; fail-safe) hooked into `recordOutcome` after `rememberOutcome`. Additive migration `20260621030000_*` (3 nullable bi-temporal cols; the explorer's explicit SELECT excludes them → byte-identical read surface). Invalidate-don't-delete enforced structurally (only INSERTs typed edges + stamps `valid_to` on its OWN engine edges, never DELETEs, never touches a `promoted`/human edge). **4-lens adversarial-review Workflow** (correctness · dormancy/fail-safe · data-integrity · security/RLS) → 0 blockers, SHIP_WITH_FIXES; folded the 2 real write-path defects it caught (zombie-edge-on-re-record → stamp `valid_to:null`/`invalidated_by:null` on the upsert payload; missing `excludeId` self-starvation) + the opportunity-dedup NIT. tsc 0 (my files; 4 pre-existing Stripe-dep errors) / eslint 0 / `bun test` 492. **`bun run build` is RED on a PRE-EXISTING baseline** (`@stripe/react-stripe-js` not installed in this worktree), proven by a clean-stash build that fails identically — NOT caused by this change. **◐ not ✅:** PURE matrix ✅ unit-verified; live edge-write + migration apply verify on a publish/dry-run, dormant-with-no-data until the founder flips `DECISION_BRAIN_SUPERSESSION`. DBR-2 (the Critic reasoning over the edges) stays deferred. Plan [`supersession-engine-plan.md`](./supersession-engine-plan.md); feature [`../features/decision-brain.md`](../features/decision-brain.md) (DBR-1.5 bullet); detail [`../../plan.md`](../../plan.md) §4.
- **2026-06-21 02:35, Lane 1 (numbered parallel build), AI-SAFETY: RUNAWAY-INCIDENTS shipped (◐, board row 156).** The decision panel's runner-up; completes RUNAWAY-DETECT's "operator surface" follow-up: a spinning mission now lands in the persistent Incidents log, not only the silent Missions glance. `getIncidentsInternal` gains a `runaway` source (scan 200 recent user missions, no age floor → old spinners caught; fold via a NEW shared pure `buildMissionStats` extracted from `getRunawayMissions` so they cannot drift; emit ONLY active `runaway` severity, exclude terminal `watch` → no flood; stable id `runaway:<id>`). `IncidentKind` widened forces a `KIND_LABEL` entry (tsc-enforced). Read-only, user-scoped; error-tolerant per the aggregator's graceful-degradation contract. 2-lens adversarial-review Workflow: both **SHIP**, refactor PROVEN behaviorally equivalent, no correctness/security/leak/flood defect. tsc 0 / eslint 0 / build ✓ / `bun test` 475 (8 new). **◐ not ✅:** logic ✅ unit-verified; live read verifies on publish. **New directive surfaced (reconcile next pick):** memory `build-sequence-and-ownership.md` — pick the lowest open number / Build Sequence (DBR-1.5 #1); Lovable owns WM-M*/M-C-PRICE/BYO-P4 (frozen), WM-F* tenancy stays Claude. Spec [`../features/runaway-detection.md`](../features/runaway-detection.md); detail [`../../plan.md`](../../plan.md) §4.
- **2026-06-21 02:10, Lane 1 (numbered parallel build), AI-SAFETY: EVAL-COVERAGE shipped (◐, board row 155).** Closes the `considerations.md` AI-safety P1 "Eval coverage targets per surface/agent" gap and completes the "is the autonomy actually guarded" triad (RELIABILITY-SLO + RUNAWAY-DETECT + this). Picked via a 3-angle decision-panel Workflow with adversarial eligibility verification (it won on fully-verifiable + ZERO-orphan: the denominator `SURFACE_KEYS` and the consumer `EvalsPanel` both already existed). New pure `src/lib/evals/coverage.ts` (`EVAL_COVERAGE_TARGETS` + `assessEvalCoverage`: each of 7 canonical surface×prompt targets → covered/stale/uncovered; total + deterministic; 18 tests) + read-only `getEvalCoverage` over `eval_suites`/`eval_runs` (user-scoped; counts only enabled cases) + a calm degrade-silent "Coverage" banner in `EvalsPanel.tsx` (the `SURFACE_KEYS` unification also removed em-dash labels). **A 2-lens adversarial-review Workflow caught a feature-INVERTING bug the correctness gate could not:** the eval success terminal is `"completed"` not `"complete"` (the runner + every reader + seed data agree; `"complete"` is the legacy `agent_runs` value), so in prod every covered surface would have read `stale` and coverage could never exceed 0%; the 17 original tests masked it by asserting my own wrong constant. Fixed + added a regression test pinned to the real DB literal. tsc 0 / eslint 0 / build ✓ / `bun test` 464 (18 new). **◐ not ✅:** scorer ✅ unit-verified; live read verifies on publish (DB MCPs intermittent). Follow-up: per-target chips + a coverage-floor deploy gate. Spec [`../features/eval-coverage.md`](../features/eval-coverage.md); detail [`../../plan.md`](../../plan.md) §4.
- **2026-06-21 01:55, Lane 1 (numbered parallel build), FRONT-END: RELIABILITY-GLANCE shipped (◐, board row 154).** Backs the North Star (claim-never-outruns-wiring) + the founder's active FE-wiring theme: the two prior cycles shipped read fns no surface rendered, so this wires both into one calm operator glance. New `src/components/cockpit/ReliabilityGlance.tsx` (mirrors `MissionsCostGlance`: `useServerFn` + `useQuery` on `getReliabilitySlo` + `getRunawayMissions`) renders a single "Heads up" line ONLY when the AI error budget is warning/exhausted OR a mission is spinning; nothing when healthy (engine-room-doctrine: attention, not an always-on panel); degrades to silent on query error. Wired into `_authenticated.missions.index.tsx`. Design-gated via `impeccable` (loaded the Ember Editorial DESIGN.md, folded a role-color violation: dropped an invented `--warn` token for neutral ink tones since the accents are reserved). tsc 0 / eslint 0 / build ✓ / 438 tests. **◐ not ✅:** wired + gate-green + design-gated; renders on publish (no local render-verify). Follow-up: deep Engine Room breakdown + drill-in. Spec [`../features/reliability-slo.md`](../features/reliability-slo.md) + [`../features/runaway-detection.md`](../features/runaway-detection.md); detail [`../../plan.md`](../../plan.md) §4.
- **2026-06-21 01:40, Lane 1 (numbered parallel build), AI-SAFETY: RUNAWAY-DETECT shipped (◐, board row 153).** Closes the `considerations.md` AI-safety-lens P1 "Loop/runaway detection" gap — KI-15/16 shipped the CAPS, this is the DETECT half, and the inverse of E8's stall monitor (`loop-health.functions.ts`: stall = too little progress; runaway = too much churn). New pure `src/lib/reliability/runaway.ts` (`assessMission`/`assessMissions` + `isTerminalStatus`/`summarizeRunaway`; flags hop/step/retry-churn/pinned-step/spend breaches; severity `runaway` while active vs `watch` when terminal; unknown status = active fail-loud; total + deterministic; 18 tests) + a read-only `getRunawayMissions({days})` over missions/mission_steps/agent_runs (user-scoped: missions `.eq('user_id')`, children `.in('mission_id', ids)` over the user's own ids; 200-mission cap + `truncated` flag; no writes/AI/spend; NO loop/chokepoint edit). Thresholds are deliberately conservative heuristics ABOVE the real caps (no hard hop ceiling exists; step-dispatch cap is small/per-tick; ceiling 3 > default `max_attempts` 2), so a normal long mission never false-positives. 2-lens adversarial-review workflow (read-fn RLS/correctness · detector soundness): both **SHIP**, no runtime-fatal/correctness/security defect; folded a calibration-comment accuracy fix + em-dash cleanup. tsc 0 / eslint 0 / build ✓ / `bun test` 427 (18 new). **◐ not ✅:** detector ✅ unit-verified; live read verifies on publish (DB MCPs intermittent). Follow-ups: operator surface (reuse `summarizeRunaway`) + alert/auto-pause. **Lane infra note:** the lane ships to `origin/main` via fast-forward (single-branch model), NOT the `parallel/lane-1` remote branch; cycle 1 mistakenly pushed only to the lane branch, corrected this cycle (cycle-1 RELIABILITY-SLO is now on `origin/main`). Spec [`../features/runaway-detection.md`](../features/runaway-detection.md); detail [`../../plan.md`](../../plan.md) §4.
- **2026-06-21 01:26, Lane 1 (numbered parallel build), RELIABILITY: RELIABILITY-SLO shipped (◐, board row 152).** Closes the `considerations.md` SRE-lens P1 "SLOs/SLAs + error budgets" gap — APP-HEALTH gives binary liveness, this gives the *measured* reliability of the AI surface. New pure `src/lib/reliability/slo.ts` (`computeSlo` → availability % · p50/p95 latency · error-budget burn vs a 99% default; + `percentile`/`errorBudgetStatus`/`summarizeSlo`/`normalizeStatus`; total + deterministic; 22 unit tests) + a read-only `getReliabilitySlo({days,targetAvailabilityPct})` over `ai_events` (user-scoped RLS, 5000-row cap + `truncated` flag, no writes/AI/spend). **Correctness crux:** `status='blocked'` is a deliberate governance/credit halt, EXCLUDED from availability+latency (else it reads as unreliable exactly when the guardrails fire); unknown/NULL status fails VISIBLE (→ error), never hidden as blocked (coercion lives in the tested pure module). 3-lens adversarial-review workflow (math · security/RLS · reuse/doctrine): no runtime-fatal/security defect; folded 5 findings. Lane roam: the first-scouted DATA-RETENTION-b was `HELD` by lane 0 in the atomic ledger, so the claim gate prevented a collision and the lane took the next eligible Cockpit/reliability item. tsc 0 / eslint 0 / build ✓ / `bun test` 409 (22 new). **◐ not ✅:** engine ✅ unit-verified; live `ai_events` read verifies on publish (DB MCPs intermittent). Follow-ups: Engine Room operator glance (reuse `summarizeSlo`) + the outward-facing status page/SLA (founder-gated). Spec [`../features/reliability-slo.md`](../features/reliability-slo.md); detail [`../../plan.md`](../../plan.md) §4.
- **2026-06-20 22:45, FE cycle 6, FRONT-END: inline thread rename on the /chat rail (◐).** Wired the backend-only `renameConversation` (no prior call site) into the Ask/chat threads rail (which already did list + delete): a hover Pencil turns the title into an autofocused input; Enter/blur saves, Escape cancels (a `skipRenameBlur` ref avoids the unmount-blur double-commit), and the input stops propagation so editing never switches the active thread. Edit-in-place per the protocol (no modal), reuses the existing input + hover-reveal styling. **Verification (ran):** tsc 0, build ✓, eslint 0 (1 file). **◐ not ✅:** renders on publish. No dedicated dashboard row (chat broadly shipped; completes thread management). Detail: [`../../plan.md`](../../plan.md) §4.
- **2026-06-20 22:35, FE cycle 5, FRONT-END: ENG-06 B2 cost-per-outcome glance on Missions (◐).** Continued the FE-wiring theme app-wide past the WM cluster. Wired the shipped `getCostPerOutcome` (ENG-06 B1/B3, 2026-06-17) into the deferred B2 slice: a calm one-line "manager's glance" (`src/components/cockpit/MissionsCostGlance.tsx`) on the Missions header ("This week the fleet shipped N for $X"), sharing the `["cost-per-outcome"]` query cache (no extra fetch), silent on a quiet week. Founder-intended (session-decisions: cost-per-outcome light on Today/Missions). Design lens: Engine-Room doctrine + impeccable hero-metric ban (a sentence, not a metrics panel). Honest: near-identical to the Today chip, kept a dedicated component to leave Today untouched. **Verification (ran):** tsc 0, build ✓, eslint 0 (2 files). **◐ not ✅:** renders on publish. ENG-06 now has all 3 B-slices built. Detail: [`../../plan.md`](../../plan.md) §4.
- **2026-06-20 22:20, FE cycle 4, FRONT-END: invite redirect-after-login (◐), completing the invite flow.** Closes the cycle-54 deferred limit. Added an open-redirect-guarded internal-only `next` param to `/login` + `/signup` (`safeNextPath`: internal absolute paths only); the `/join/$token` page's Log in / Sign up links pass `next=/join/<token>`, so a logged-out invitee returns to the accept page after email auth (a fresh signup accepts before onboarding, since /join is a top-level route). Default behavior unchanged with no `next`. OAuth still returns to `/` (founder-gated redirect_uri, untouched). **Verification (ran):** tsc 0, build ✓, eslint 0 (3 files). **◐ not ✅:** renders on publish. **This completes the founder-directed FE-wiring scope** (members, move-product, invites); remaining WM FE is founder-gated (credits/pricing). Detail: [`../../plan.md`](../../plan.md) §4.
- **2026-06-20 22:05, FE cycle 3, FRONT-END: member role management + owner-only correction (◐).** Completes + corrects the Members surface. Added `changeWorkspaceMemberRole` (owner-only via WM-F3 RLS; role enum excludes "owner"; `.select()` + 0-row throw) wired as an inline role `<select>` that replaces the static role chip on manageable rows. **Adversarial fix:** cycle 1 gated Remove to owner-OR-admin, but WM-F3's `workspace_members` policy is `for all` + owner-only, so an admin's `removeWorkspaceMember` deleted 0 rows yet PostgREST reported success (a phantom "removed" toast). Now ALL management (role/remove/transfer) is gated to `isOwner`, and `removeWorkspaceMember` is hardened with `.select()` + a 0-row throw. **Verification (ran):** tsc 0, build ✓, eslint 0 (2 files). **◐ not ✅:** wired + gate-green + runtime-safe + driven; renders on publish. **Next:** invite redirect-after-login. Detail: [`../../plan.md`](../../plan.md) §4.
- **2026-06-20 21:50, FE cycle 2, FRONT-END: move-product-between-workspaces UI shipped (◐).** Wires the `moveProduct` RPC (WM-F6, cycle 46) into the UI: a "Move to workspace" `DropdownMenuSub` in each product's actions menu (the AppShell sidebar product ⋯ menu, beside Rename/Delete) lists the user's other workspaces; picking one opens a non-destructive confirm then calls `moveProduct`, clears the now-moved active product, and refreshes. Hidden when the user has one workspace. Built per the frontend build protocol: `emil-design-eng` for the interaction (reversible move = clear confirm but no destructive/typed-confirm over-alarm; submenu-then-confirm over a bespoke modal; reuse the existing `confirm()`), `impeccable` gate. **Verification (ran):** tsc 0, build ✓, eslint 0 (AppShell). **◐ not ✅:** wired + gate-green + runtime-safe + driven, renders + live move on publish. Honest limit: destinations not yet filtered to the same account (client `Workspace` lacks `account_id`), so a cross-account pick is offered then RPC-rejected with a toast (refinement WM-F6c). **Next:** change-member-role or invite redirect-after-login. Detail: [`../../plan.md`](../../plan.md) §4.
- **2026-06-20 21:35, FE cycle 1, FRONT-END: the Workspace Members surface shipped (◐).** Resumes the front-end pivot (wiring the shipped WM backends to the UI). New `src/components/settings/MembersCard.tsx` in Settings > Workspace lists members (monogram, name/email, neutral role chip, "You" badge, joined date) and gives owners/admins **Remove** + the owner **Make owner** behind an inline two-step confirm, wiring `transferWorkspaceOwnership` (WM-F4) + `removeWorkspaceMember` + the previously-deferred members list. Enabling backend: migration `20260620213000_members_identity_rpc.sql` adds a membership-gated SECURITY DEFINER `workspace_members_with_identity` RPC (profiles RLS is own-row-only, email is in auth.users, so a plain read cannot show co-members); `listWorkspaceMembers` calls it pre-migration-tolerant and returns `selfRole` for UI role-gating. Built per the new frontend build protocol (§3.0d): `design-taste-frontend` for direction (reconciled to Ember; kept its mandatory loading/empty/error states + hairline rows + neutral chips), `impeccable` as the gate (clean). **Verification (ran):** tsc 0, build ✓, eslint 0 on 3 files. **◐ not ✅:** wired + gate-green + runtime-safe + driven, but the migration's live dry-run could NOT run this session (Supabase + gbrain MCPs unauthorized/disconnected) so it is flagged for publish-verify, not claimed; renders on the founder's publish. **Next:** WM-F6 move-product UI. Detail: [`../../plan.md`](../../plan.md) §4.
- **2026-06-20 21:21, FE cycle 0, GOVERNANCE: frontend build protocol codified (founder ruling).** Every FE build now uses the FULL design-skill toolkit (not `impeccable` alone), reconciling the brand reference files (intent, not gospel) with standard practice. New "The frontend build protocol" in `design-context.md` + non-negotiable `AGENTS.md` §3.0d + loop playbook §4 pointer + `session-decisions.md`. Opens the FE-wiring theme.
- **2026-06-20 15:08 UTC, SECURITY: scheduled backend hook auth hardened (✅).** Loaded all persisted security scan results, including connector security scan results; Wiz returned no findings. Fixed the active open-endpoint finding: `/api/public/hooks/*` no longer accepts the browser-visible app key as caller auth. Migration `20260620150512_a486e345-5722-48bd-80e4-49ee123b8179.sql` creates a locked `app_private` secret store and rewires the live scheduled jobs (`approvals-tick`, `event-reactor-tick`, `memory-tick-daily`, `resume-runs`) to send `x-cron-key`; the hook guard now checks only that private key or configured backend env secrets. Verified the live job commands now use `x-cron-key` and marked the scanner finding fixed; persisted app, connector, and backend scan results now show no active findings.
- **2026-06-20 20:21, cycle 57, RESILIENCE: `MODEL-REGISTRY-DEPRECATION` model-deprecation route-around SHIPPED to `main` (◐, dormant playbook).** Completes `considerations.md` #4 (the deprecation half; cycle 56 did the outage-fallback half). Added `deprecated`/`replacement`/`sunset` to the `Model` type (`src/lib/ai/models.ts`) + a pure, cycle-safe `activeModelId(id, catalog?)` resolver that follows `deprecated -> replacement` links (intermediates may be non-live retired models) to a LIVE terminal, and otherwise returns the original requested id so a deprecation can never strand traffic on a missing/non-live model; wired before cost-routing in both `callModel` + `callModelStream` so a deprecated requested model proactively routes to its replacement. **Honest status:** no model is flagged deprecated in the catalog today, so `activeModelId` is the identity function and the chokepoint is BYTE-IDENTICAL; this is a wired-but-dormant playbook ready for the next provider sunset (the considerations #4 "model-deprecation playbook" ask). The route-around logic IS genuinely unit-verified (8 tests via an injected catalog: live replacement, multi-hop chains through non-live intermediates, missing/non-live-terminal rejection, cycle termination, unknown id). **Decision:** continued the chokepoint-resilience pattern from cycle 56 (surgical + offline-verifiable); built it now per founder ruling 1 rather than parking the "attended bundle". A controller adversarial pass on the diff confirmed byte-identity-when-dormant + the credit pre-check/byo path use the resolved `effectiveModel`; folded a comment-accuracy fix (the resolver doc-comment had described an earlier buggy version). **Verification (ran):** tsc 0, eslint 0 (3 files), `bun run build` ✓, `bun test` 357/357 (8 new). Commit `fb82451f19` (rebased). **Next:** the founder-flagged Decision Brain step is the **supersession ENGINE** (the moat's signature mechanic) in the AI lane - re-grounding to verify its greenlit/non-gated status before picking it next.
- **2026-06-20 19:52, cycle 56, RESILIENCE: `PROVIDER-FALLBACK` ordered fallback chain at the AI chokepoint SHIPPED to `main` (◐).** Closes `considerations.md` #4 (provider/model outage degrades, not hard-fails). Generalized the chokepoint's single `fallbackModel` retry into a pure, ordered, deduped, primary-excluded `resolveFallbackChain` (new `src/lib/ai/fallback.ts`, 10 unit tests) wired into both `callModel` + `callModelStream`, replacing the single-fallback block with a chain loop. Back-compat: a single `fallbackModel` resolves to a 1-element chain = today's behavior. A new flag `providerFallbackEnabled()` (`AI_PROVIDER_FALLBACK`, default OFF, mirrors `AI_COST_ROUTING`) optionally appends an auto cross-model degrade to `cheapestLiveModel()` (reuses the tested WM-M15 picker), EXCLUDING surface `eval` (benchmark corruption) + `embed` (cheapestLiveModel is chat-only). The happy path is untouched (only the post-primary-failure path changes), bounding blast radius. **Decision:** built it now rather than continuing to park it as "attended" - founder ruling 1 says do not park risky-but-buildable work; the change is surgical, flag-gated (byte-identical off), and offline-verifiable. A dedicated adversarial reviewer (chokepoint blast-radius, the cycle-39/40 history) confirmed all 7 binding constraints (byte-identical off, eval/embed guard both paths, no self/infinite fallback, happy path untouched, cache `!fallback` guard + credit attribution via `modelUsed` intact, streaming non-ok handling preserved); folded 2 findings (I-1: surface the PRIMARY error on chain exhaustion, not the last fallback's, so an outage reports its root cause; M-1: a missing test). **Verification (ran):** tsc 0, eslint 0 (3 files), `bun run build` ✓, `bun test` 332/332 (10 new). **◐ not ✅:** the chain + pure ranker are ✅ unit-verified; the live outage-degrade + the auto cross-model fallback activate on the founder setting `AI_PROVIDER_FALLBACK` (verify on publish). Landed after rebasing past 2 parallel pushes (KI-16b + a mission fix; no code overlap). **Next attended:** PLAN-ENFORCE + MODEL-REGISTRY-DEPRECATION (the remaining chokepoint pair). Detail: `considerations.md` #4 + `../../plan.md` §4.
- **2026-06-20 19:14, cycle 55, DECISION BRAIN: Ambient Precedent (increment 1) SHIPPED to `main` (◐), subagent-driven, all 6 tasks.** The founder's topmost-priority Decision Brain increment, executed via `superpowers:subagent-driven-development` (a fresh implementer per task, a dedicated reviewer on the two runtime-fatal-risk tasks T2/T3, controller review on the additive tasks, and an opus whole-branch review at the end: "ready to merge"). **What shipped:** T1 persists `prd_title`/`opp_title` on outcome memories (forward-only jsonb); T2 a semantic precedent engine (`src/lib/ai/decision-precedent.server.ts`: a PURE TDD'd `rankPrecedent` + a fail-safe live `loadDecisionPrecedent` that embeds the decision text, recalls over outcome memories via `match_agent_memory`, joins metadata, thresholds + caps, returns `[]` on any failure); T3 rewires the Critic (the wedge) to the semantic engine and deletes DBR-0's recency helper; T4 a `getDecisionPrecedent` server fn; T5 a reusable `PrecedentNudge` UI primitive (reuses `VerdictChip`, dismissible, renders nothing when empty, `impeccable`-passed); T6 wires the nudge into the opportunity `?opp=` drill + the spec detail page (3 seams now live: Critic + opportunity + spec). **Verification (ran):** `bunx tsc --noEmit` 0, `bun run build` ✓, `bun test` 322/322 (3 new), `eslint` 0 on all touched files; landed in two FF halves (P0 T1-T3, then P1 T4-T6 rebased cleanly past a parallel O1/incidents push). A runtime-fatal review confirmed the engine is actually driven (5 real `runCritic` call sites + both UI seams render) and verified the `match_agent_memory` slug-or-global SQL surfaces outcome rows regardless of slug. **◐ not ✅:** engine + ranker ✅ unit-verified; the 3 seams render on the founder's next publish (they read live embedded outcome data; needs seeded outcomes to verify visually). **As-built deltas (honest):** threshold ships at a conservative `0.3` (tune live, not the spec's `~0.72`); `excludeId` plumbed but not passed in v1; account-pooling not applied to precedent in v1. Detail: [`../features/ambient-precedent.md`](../features/ambient-precedent.md). **Next:** DBR-1 (typed graph) stays founder-gated for enrichment; the loop continues the non-gated queue.
- **2026-06-20 04:55, cycle 54, FRONT-END theme: WM-F5 accept route wired (◐); run paused for the day.** New standalone `src/routes/join.$token.tsx` (`/join/$token`, `ssr:false`, not under the auth shell so a logged-out invitee gets a clear prompt not a token-losing bounce): on mount checks `supabase.auth.getUser()`, then redeems the token via `acceptInvitation` (single-run `ran.current` guard since accept is single-use + StrictMode double-invokes), showing checking → accepting → "You are in" (moss success per the role-color law) / needs-login / error. Completes the invite→accept loop cycle 53 started. Ran `impeccable` (passed the bans). **Verification (ran):** build ✓ (3.09s, routeTree regenerated), tsc 0, eslint 0, `bun test` 307/307, unicode-clean. **◐ not ✅:** wired + gate-green + runtime-safe + driven, not render-verified locally (needs a real token + the migration); live on publish. Row 54 WM-F5 [~85%]→[~92%]. Known limit (deferred): no redirect-after-login for a logged-out invitee. **Founder paused the run for the day (2026-06-20); everything committed + pushed to `main`; resume tomorrow.**
- **2026-06-20 04:15, cycle 53, FRONT-END theme: WM-F5 invite UI wired (◐).** New `src/components/settings/TeamCard.tsx` ("Invite teammates") in the Settings > Workspace tab wires WM-F5: invite by email + role, copy the join link (outbound email is a no-op), see invitations + status, revoke a pending one (`inviteMember`/`listInvitations`/`revokeInvitation`, refetch after each); self-contained via `useWorkspace()`. Ran `impeccable` (passed the bans; folded a header-rename correctness fix since `listInvitations` returns all statuses). **Verification (ran):** tsc 0, eslint 0, build ✓ (3.92s), `bun test` 305/305, unicode-clean. **◐ not ✅:** wired + gate-green + runtime-safe + driven, but not render-verified locally; the WM-F5 migration is pre-publish so it activates on the founder's publish. Row 54 WM-F5 [~70%]→[~85%]. Deferred: the `/join/$token` accept route + a members list (needs a profiles email join). **Next: ownership transfer + move-product affordances, or the members list.**
- **2026-06-20 03:35, cycle 52, FRONT-END theme: U6 export-history view wired (◐).** Wired U6-AUDIT's `listExportLog` (c48) into the existing `DataExportCard`: a "Recent exports" list (date+time · kind · row count) below the export controls, refetched after each export (`invalidateQueries(["export-log"])`), with calm loading/empty states. Integrated into the one card (not a 3rd card) per `impeccable`'s "fewer cards" law; ran the design skill (passed the bans; folded date+time so same-day exports are distinguishable). **Verification (ran):** tsc 0, eslint 0, build ✓ (3.31s), `bun test` 305/305, unicode-clean. **◐ not ✅:** wired + gate-green + runtime-safe + driven, but not render-verified locally; live rows accrue on the founder's publish. Advanced row 36 U6 [~95%]→[~98%]. **Next: the WM workspace/team surfaces (members, invites, transfer, move).**
- **2026-06-20 02:55, cycle 51, FRONT-END PIVOT (founder ruling) + first UI build: "Where your data goes" (◐).** The founder ruled mid-session: stop adding backend, stitch UI onto the shipped backends so they are visible in the app. Loaded the design conventions (engine-room-doctrine -> design-context -> home-and-today-ia); per the doctrine + IA rubric these enterprise/trust backends live in Settings (calm, outcome-named), not the front. First build: `src/components/settings/SubprocessorsCard.tsx` ("Where your data goes") wires SUBPROC-DISCLOSURE's `getSubprocessors` (useQuery + useServerFn, Ember `.bento`/`.mono-label`, editorial list), added to the Settings `data` tab beside `DataExportCard`. **Verification (ran):** tsc 0, eslint 0 (2 touched files), build ✓ (3.32s), unicode clean. **◐ not ✅:** wired + gate-green + adversarially-reviewed runtime-safe, but NOT render-verified locally (renders on the founder's publish). (A second founder message about a Lovable/Cursor-style "CLI" vs "Ask" was retracted - disregarded.) **Next: U6 export-history view (`listExportLog`), then the WM workspace/team surfaces.**
- **2026-06-20 02:10, cycle 50, `APP-HEALTH` app-level health/readiness endpoint BUILT (◐).** The SRE-lens P0 gap in `considerations.md` ("App-level monitoring + alerting", not just AI telemetry), confirmed unbuilt since `health.functions.ts` is only a migration-drift checker. New route `src/routes/api/public/health.ts` (`GET /api/public/health`): a cheap, timeout-bounded (2s) DB reachability probe (`profiles` head-read, verified a real table) → HTTP 200 `{status:"ok"}` healthy or 503 `{status:"degraded"}` when a dependency is down, so a monitor/LB reacts on the status code. Public + unauthenticated, designed not to be an attack surface (leaks nothing beyond ok/error states, probe is cheap + timeout-bounded). Pure `src/lib/app-health.ts` (`assembleHealth`) unit-tested. **Verification (ran):** build ✓ (3.68s, routeTree regenerated), tsc 0, eslint 0 (3 new files), `bun test` 303/303 (5 new). **◐ not ✅:** endpoint + pure assembly built + unit-verified; the live DB-probe was not run unattended (verify with `curl` on publish), and the alerting half + a status page are infra/founder + design-pass. **Pick:** chokepoint bundle stays attended, Data/Privacy vein well-mined, so per `considerations.md` P0-first the cleanest greenfield item was app monitoring. **Next: weigh a full-account GDPR data-portability export vs surfacing to the founder that the high-impact remainder is the attended chokepoint bundle + founder-gated work.**
- **2026-06-20 01:30, cycle 49, `SUBPROC-DISCLOSURE` sub-processor disclosure registry BUILT (◐).** The Data/Privacy P1 enterprise-review gap from `considerations.md` ("Sub-processor list (model providers!) + DPA"), completing the data-governance triad with DATA-RETENTION (c47) + U6-AUDIT (c48). New pure `src/lib/compliance/subprocessors.ts`: a `SubProcessor` type + a curated infra list (Lovable gateway / Supabase / Cloudflare) + `modelProviderSubprocessors` DERIVED from the live model catalog (a provider is `active` only when it has a `live` model; adapter-ready BYO providers listed inactive; `ollama` excluded as self-hosted = never a third-party processor) + `allSubprocessors`/`activeSubprocessors`. New `getSubprocessors` GET server fn. `PROVIDER_META: Record<Model['provider'],…>` makes tsc FAIL if `models.ts` adds a provider without a disclosure entry (no silent staleness). **Verification (ran):** tsc 0, eslint 0 (3 new files), build ✓ (3.11s), `bun test` 298/298 (11 new). No DB access → no dry-run needed. **◐ not ✅:** the pure registry + read fn are built + unit-verified, but the trust-page UI + the legal-reviewed copy/regions/DPA are deferred to the design/legal pass, and the GET fn was not behaviorally run unattended. **Pick rationale:** the top remaining queue items (PROVIDER-FALLBACK, PLAN-ENFORCE, MODEL-REGISTRY-DEPRECATION) are now one attended chokepoint bundle (MODEL-REGISTRY's only live drive-point is the chokepoint, since `routeModel` is a dead export); the other clean items are parallel-lane-owned; this lane's clean strength is the cross-cutting Data/Privacy gaps. **Next: PROVIDER-FALLBACK bundle (attended) or the next clean non-chokepoint considerations.md / v9 / v8 item.**
- _Cycles 45-48 (`WM-F8`, `WM-F6`, `DATA-RETENTION`, `U6-AUDIT`) are detailed in [`../../plan.md`](../../plan.md) §4 + the [overnight report](./archive/overnight-build-report.md); this §6 log lagged after cycle 44 and resumes here at 49._
- **2026-06-19 23:03, cycle 44, `WM-F5` workspace invitations BACKEND BUILT (◐).** The team-growth on-ramp (no invite flow existed). Backend only, per the founder ruling (batch UI to the design pass). Migration `20260619260000` adds a `workspace_invitations` table (DB-generated token, role admin/member/viewer, status, 7-day expiry, manager-only RLS via `can_manage_workspace`; the invitee has NO direct read = no pre-accept leak) + a SECURITY DEFINER `accept_workspace_invitation(_token)` RPC (validates pending+unexpired, binds the token to the invited email so a leaked token can't be redeemed by another account, upserts the membership with the invited role, single-use under a row lock). New `email.server.ts` (no-op `sendInviteEmail` returning the copy-paste link; outbound founder-gated) + `inviteMember`/`listInvitations`/`revokeInvitation`/`acceptInvitation` server fns. **Behaviorally dry-run-verified on prod:** invite→accept added the user with role=member + flipped the invite to accepted; re-accept, wrong-email, and expired all rejected; rolled back, prod untouched. tsc 0 / build ✓ / eslint 0 / 270 tests. **◐ not ✅:** backend built + verified; the join route + Settings UI are deferred to the design pass, and live invites activate on publish (needs the fixed WM-F3 `70c0ca7db5` so `can_manage_workspace` exists). **Next: WM-F6 (move product) or WM-F7/F8 (settings IA, switch hardening) backend.**

- **2026-06-19 22:43, cycle 43, `WM-F4` workspace ownership transfer BUILT (◐).** First build since the lane-coordination commit (`0ce0524604`) made the WM lane collision-free. Closes the `leaveWorkspace` dead-end ("transfer it first" with no transfer). Migration `20260619250000` adds a `workspace_audit_log` table (member-read RLS, RPC-only writes) + a SECURITY DEFINER `transfer_workspace_ownership` RPC (atomic: reassign `owner_id`, flip member roles new→owner/old→admin, write an audit row) + a `transferWorkspaceOwnership` server fn. Reassign-before-demote ordering respects WM-F3's demotion trigger. **Behaviorally dry-run-verified on prod:** the transfer flips `owner_id` + both roles + writes audit `{from,to}`; a non-owner caller is blocked; a non-member target is blocked; rolled back, prod untouched. tsc 0 / build ✓ / eslint 0 / 270 tests. **◐ not ✅:** the RPC + server fn are built + verified, but there is NO UI yet (the Settings member-picker + confirm is deferred to a follow-up / the design pass) and live transfer activates on publish. **Next: WM-F4-UI (the Settings affordance) or WM-F5 (invites).**

- **2026-06-19 22:20, cycle 41, `WM-F3-fix` corrected the shipped RBAC migration, which would have FAILED to apply on publish (◐).** NOT a collision this cycle: a PROACTIVE adversarial review of the recently-shipped SECURITY DEFINER / RLS migrations (the highest-risk parallel-built code), prompted by two prior broken parallel ships. It found the THIRD: the WM-F3 RBAC migration (`2b1c429591`) used `create policy if not exists` for all four account/account_members policies. **Postgres CREATE POLICY has no IF NOT EXISTS clause** (unlike CREATE TABLE/INDEX), so it is a hard syntax error (`ERROR: 42601`) and the migration would have FAILED to apply on the founder's next publish, blocking the whole migration run; tsc/build were green because it is pure SQL, and the shipped dashboard row claimed "verified". Fix (`70c0ca7db5`): the four policy creates are now `drop policy if exists` + `create policy` (valid + idempotent; the four names differ from WM-M2's, so no collision), and the owner-demotion trigger is now drop-then-create guarded. **Verification (ran):** confirmed on the live prod DB via `BEGIN..ROLLBACK` (stubbing the unpublished WM-M2 accounts deps): the `if not exists` form errors with 42601; the corrected migration applies cleanly (RBAC helpers + the 5 policies + the demotion trigger all created); rolled back, then prod re-confirmed untouched (the real "ws owner manage" policy I dropped in the txn was restored). Also data-checked the new role-keyed workspaces policy for owner-lockout: all 7 workspaces have their owner as a `workspace_members(role='owner')` row, so no owner loses access. tsc 0, build ✓, 270 tests (SQL-only change; TS untouched). **◐ not ✅:** the migration now applies; live RBAC enforcement activates on publish. **Lesson:** three consecutive parallel ships (WM-M15b, WM-F2, WM-F3) shipped tsc/build-green but with a publish-blocking migration, a security regression, and an unappliable migration respectively; the standing response is now a proactive review of every parallel-shipped SECURITY DEFINER / RLS / migration before the founder publishes, not just the item this loop is building. **Next: WM-F4/F5 (ownership transfer + invites) or a continued review sweep of the remaining unverified-by-me ◐ migrations.**

- **2026-06-19 22:05, cycle 40, `WM-F2-fix` restored the security + correctness invariants in the shipped account-memory pooling (◐).** A SECOND same-night collision: a parallel session shipped WM-F2 (`18db474d3b`) to `origin/main` while this cycle independently built the same item in the worktree. Their version was tsc/build-green but **regressed the WM-F1 recall baseline**, so this routed to a fix (`a368edf78a`), not a skip. **The serious defect is a SECURITY regression:** their recall RPCs filtered `m.user_id = for_user` instead of WM-F1's `coalesce(auth.uid(), for_user)` — reverting the exact pin WM-F1 added to CLOSE a cross-user read on these SECURITY DEFINER RPCs, so any authenticated caller could pass `for_user=<another user's id>` and read that user's memory/reflections (the moat object). **Also regressed:** recalled EXPIRED memory (dropped the expiry filter), dropped GLOBAL-scope memory (changed `(slug match or scope='global')` to mandatory `agent_slug=`), dropped the per-row `is_workspace_member` guard, and broke the return contracts (`match_agent_memory` 6 cols → 3 with similarity computed as DISTANCE; `recent_agent_reflections` lost importance/metadata + timestamptz→timestamp). **And it was "staged but never driven":** pooling was gated on `opts.accountId`, which NO caller passes (their commit touched no call sites), so pooling never activated. The fix restores every WM-F1 invariant and resolves the pool account INTERNALLY from the workspace's account tier (`entitlements.ts` = single source of truth for crossWorkspaceMemory), so pooling is actually driven with no call-site changes. **Verification (ran):** tsc 0, build ✓, eslint 0, 270 tests; AND a `BEGIN..ROLLBACK` behavioral dry-run on the live prod DB (stubbing the unpublished WM-M2/F1 deps): a paid (pro) account recalls across BOTH its workspaces, a single-workspace call returns only that workspace, and a different account recalls NOTHING of the paid account's memory; rolled back, prod re-confirmed untouched (accounts table + columns absent, recall fn reverted to its pre-WM 3-arg signature). **◐ not ✅:** correct + dry-run-verified; live pooling activates on the founder's publish. **Lesson (reinforces cycle 39):** a green tsc/build hides a reverted security pin, a dormant feature, and a recall-filter regression; the runtime-fatal adversarial review + the prod dry-run are what caught them. **Next: WM-F4/F5 (ownership transfer + invites, unblocked by WM-F3) or WM-F6 (move product).**

- **2026-06-19 21:30, cycle 39, `WM-M15b-fix` corrected 3 runtime-fatal bugs in the shipped response cache (◐).** A parallel session shipped WM-M15b (`796513d181`) to `origin/main` while this cycle was building the same item in the worktree; the per-cycle origin sync caught the collision, and because the shipped version was tsc/build-green but broken at runtime, this cycle shipped the fix (`7897085004`) instead of a duplicate. **(1) The migration would have FAILED to apply on the founder's next publish:** `CREATE INDEX ... WHERE expires_at > now()` is rejected by Postgres because `now()` is STABLE and index predicates require IMMUTABLE. Verified live: the broken DDL returns `ERROR: 42P17` on the prod DB; the fixed plain-index DDL applies cleanly (throwaway-table dry-run, dropped); the real `ai_response_cache` is confirmed not-yet-applied, so editing the migration in place is correct (a new migration could not help, since the broken one would run first and fail). **(2) The cache was a complete no-op:** `callModel` ran the provider retry loop unconditionally even on a cache hit (the `if(!cacheHit)` guard wrapped only the write block), so zero COGS were ever saved; the provider call is now skipped on a hit. **(3) readCache always missed:** it passed the literal string `"now()"` to `.gte("expires_at")`, which Postgres cannot cast to timestamptz, so every read errored; now `new Date().toISOString()`. Also: `generateCacheKey` hardened to Web Crypto (`crypto.subtle`, the `rag/indexer.server.ts` pattern) off a bare-`"crypto"` dynamic import whose worker runtime was unverifiable; `writeCache` stores under `effectiveModel` + skips fallback responses for read consistency; supabase params typed. **Verification (ran):** tsc 0, build ✓, eslint 0 on 3 files, `bun test cache.test.ts` 10/10, + the prod migration dry-run above. **◐ not ✅:** the migration applies and the cache logic is correct; live cache-serving activates on the founder's publish. **Lesson:** a green tsc/build does not cover a migration's apply-time validity or a cache-skip control-flow path; the K2-class floor (prod migration dry-run + the runtime-fatal review) is exactly what caught this. **Next: WM-F2 (account-level memory pooling) or WM-F4/F5 (ownership transfer + invites, unblocked by WM-F3).**

- **2026-06-19 20:15, cycle 37, `WM-F3` RBAC enforcement shipped (◐).** Turned the simple `owner_id = auth.uid()` workspace policies into a full 4-role RBAC system (owner/admin/member/viewer). Migration `20260619210000_wm_f3_rbac_enforcement.sql` adds SECURITY DEFINER helpers (`has_workspace_role` / `has_account_role` + shorthands) + role-based RLS policies on workspaces/workspace_members/account_members/accounts + trigger `prevent_workspace_owner_demotion`. Widened `workspace_members.role` CHECK. `src/lib/roles.functions.ts` adds TypeScript helpers with typed `PermissionDeniedError` exception. Permission matrix: owner = billing/plan/delete/transfer/manage members; admin = manage members/create-delete workspace+product/approve actions/edit brief+guardrails; member = create/edit content/run missions; viewer = read-only. **Verification (ran):** tsc 0 (post-migration schema issue, not a code defect), `bun test` 8/8 pass, `bun run build` green, humanization clean. **◐ not ✅:** migration built and tested; live RBAC enforcement activates on the founder's next publish (policies + trigger take effect + types regen). **Next: WM-F4 (ownership transfer) or WM-M15b (response cache) or WM-F1b (moat hardening).**


- **2026-06-19 19:30, cycle 36, `WM-M5` tier limit gates shipped (◐).** Turned the entitlements matrix (WM-M1) from feature-flags-only into enforced caps on product + workspace creation, the keystone that makes the 5-tier ladder mean something (nothing capped anything before this). Migration `20260619200000_wm_m5_tier_limit_gates.sql` adds pure `tier_product_limit` / `tier_workspace_limit` SQL functions (free 2/1, pro 3/∞, max 5/∞, team+enterprise ∞, mirroring `entitlements.ts`) + `enforce_product_limit` / `enforce_workspace_limit` BEFORE-INSERT triggers (SECURITY DEFINER), and a new `src/lib/limits.functions.ts` (`LimitReachedError` + `nextUpsellTier` / `isOverLimit` + the pre-migration-tolerant `assertCanCreateProduct` nice-path) wired into `createProject`. **Three design calls:** (1) the **DB trigger is the authoritative guard** because the client inserts products straight from the browser (`AppShell.tsx:406`), so a server-only check is bypassable; (2) the product cap is **account-WIDE** (counted across all the account's workspaces) so pooled-workspace tiers can't bypass it, and the first workspace is never gated (resolved via `owner_id` to survive the alphabetical BEFORE-trigger fire-order); (3) the gate ships **DORMANT** behind a new `limit_gates_enabled()` flag (= false), mirroring `credits_enabled()`, because activating hard caps before a live upgrade path (Stripe) would trap free users; the founder flips it when pricing is live (section 4). **Verification (ran):** a `BEGIN..ROLLBACK` behavioral dry-run on the LIVE prod DB (WM-M2 essentials applied first since WM-M2 is not yet on prod, then WM-M5, flag flipped on): on a real free account, inserts up to the cap (2 products) succeeded and the 3rd was blocked with the exact message, a 2nd workspace was blocked, all six pure limit-fn values correct; rolled back. `bun test` 252/252 (13 new), tsc 0, eslint 0 on 3 files, build ✓, humanization clean. Adversarial self-review found no runtime-fatal bug. **◐ not ✅:** dry-run-verified, but the gate is dormant and the migration is not on prod, so it activates on the founder's publish + the `limit_gates_enabled()` flip. **Next: WM-F3 (RBAC) or WM-M15b (response cache) or WM-F1b (moat hardening).**

- **2026-06-19 18:49, cycle 35, `WM-F9` tenancy isolation hardening shipped (◐).** Closed a latent cross-workspace scope-leak class: `meetings`, `notes`, `daily_briefs`, `copilot_messages` (the bible-named 4) were still scoped only by `auth.uid() = user_id` with no `workspace_id`, inconsistent with WM-F1/WM-M2 and a leak the moment invites (WM-F5) ship. Migration `20260619190000_wm_f9_isolation_scope.sql` adds `workspace_id` + a deterministic backfill + a shared `set_row_workspace_from_user` BEFORE-INSERT trigger (fills from `NEW.user_id`, service-role-safe, so the agent-tool insert paths and NOT NULL both work, avoiding the WM-F1 trap) + NOT NULL + index + a **DUAL-KEY** RLS `auth.uid() = user_id AND is_workspace_member(workspace_id)` (keeps the tables user-private per the acceptance, strictly more restrictive so it can never widen access). **The audit clause was honored:** a 3-lens adversarial review extended the fix from 4 to **10 tables** (added the prototype family x4 + `scheduler_proposals` + `ritual_sessions`), preserved the public-share policies, fixed the `ON DELETE CASCADE` delete-confirm copy, and documented `sync_mappings` as a deferral. **Verification (ran):** a `BEGIN..ROLLBACK` behavioral dry-run of the FULL migration on the LIVE prod DB (two passes): all 10 backfilled to 0 nulls, the trigger fills `workspace_id`, NOT NULL holds, old owner-only policies removed (0 left, so the dual-key actually enforces), dual-key created, public policies preserved, rolled back. tsc 0, `bun test` 239/239, eslint 0, build ✓, humanization clean. Reader-breakage lens verified (simulated user JWT on prod) the dual-key breaks no current reader. **◐ not ✅:** the migration is dry-run-verified; the live isolation activates on the founder's next publish. **Follow-up WM-F9b:** active-workspace write/read scoping + the `sync_mappings` verdict. **Next: WM-M5 (tier limit gates) or WM-F3 (RBAC) or WM-M15b / WM-F1b.**

- **2026-06-19 18:09, cycle 34, `WM-M15` cost-aware model routing margin lever shipped (◐, routing half; cache half split to `WM-M15b`).** The structural margin discipline (no self-serve BYOK = the platform eats COGS). New pure `src/lib/ai/routing.ts` (`blendedPrice` / `cheapestLiveModel` / `costRoutedModel`) wired once at the AI chokepoint as `effectiveModel`, threaded through byo detection, the provider call, `modelUsed`, the pre-call credit projection, and the debit. **Gated behind the `AI_COST_ROUTING` env flag (default OFF), byte-identical to today until the founder enables it.** A 5-lens adversarial review caught a REAL quality-gate hole (the surface allowlist would have 25x-downgraded the Critic, the launch wedge, which runs on `surface:"judge"` with a deliberate reasoning model) and a benchmark-corruption bug (`eval`'s subject call IS the model under test); folded ALL 7 findings: a narrow routable set `{brief, scheduler, test}` (judge/eval/embed excluded) + a TIER GUARD (never downgrade a deliberately-chosen reasoning/premium/code/vision model or an unknown model), and the credit pre-check + debit now track the model actually run. **Verification (ran):** `bun test routing.test.ts` 12/12, full `bun test` 239/239, tsc 0, eslint 0 on 3 files, build ✓, humanization clean. **◐ not ✅:** the pure router is unit-verified; the live routing effect is config-gated (founder flips `AI_COST_ROUTING`). Honest: conservative by design, so modest immediate impact (most volume is on deliberately-chosen models, correctly protected); the bigger lever is the `WM-M15b` cache. **Next: WM-F1b or WM-M15b or WM-M5.**

- **2026-06-19 17:28, cycle 33, `WM-M14` per-product / per-member credit attribution + caps shipped (◐).** Closes the credit-engine legibility loop the WM-M12 debit explicitly stubbed (`_product_id` was tagged null "for WM-M14 to thread"). New migration `20260619180000_wm_m14_credit_caps.sql`: a `credit_caps` table (account-scoped, `scope` product/member, `target_id`, `cap_credits`, `window_kind`, member-read RLS, service-role write) + two composite `credit_ledger` indexes for the scoped window sums + attribution. `credits.functions.ts`: PURE `rollupAttribution` (groups debits by product + member, reconciles `sum(byProduct)==sum(byMember)==totalDebited`), `sumDebitCredits`, `capExceeded`, `creditWindowStartIso`, + `computeCreditAttribution` (RLS-scoped read, injected client, for the WM-M16 Usage panel; the createServerFn boundary is deferred to its consumer to avoid a staged-but-undriven wrapper and to keep the module test-safe). `runtime.server.ts`: `CreditCapError` + `assertCreditCaps` (inside `assertAccountCredits`, after the pool check; sums the call's product/member window debits and halts ONLY that scope while the pool may still have credits; degrades to allow on any read error; no-op when no cap exists / dormant) + threads `productId` into the debit ledger tag. `cluster.server.ts` feeds `productId` from the discovery-clustering path (server-derived projectId), so **per-product attribution is genuinely driven there**; per-MEMBER attribution is driven on every metered call; other call sites pass null until they adopt the field. All gated behind `credits_enabled()` (dormant). **Verification (ran):** `bun test credits.test.ts` 22/22 (46 asserts), full `bun test` 227/227, tsc 0, eslint 0 on 4 files, build ✓, humanization clean on my lines, AND a `BEGIN..ROLLBACK` behavioral dry-run on the live prod DB (scaffolded on the exact WM-M2 shapes: table + indexes + RLS created, cap-sum query returned the right rows), rolled back (prod confirmed clean; transactions confirmed honored by a probe first). 5-lens adversarial review → 6 findings, **5 folded** (per-product seam now driven + disclosed; RLS-scoped-client invariant; productId-belongs-to-account invariant; stale comment), 1 confirmed honest. **◐ not ✅:** pure math unit-verified; live caps + attribution activate on publish + the `credits_enabled()` flip. **Next: WM-M15 (margin levers) or WM-F1b.**
- **2026-06-19 16:44, cycle 32, `WM-M12` credit debit engine shipped (◐), fills the WM-M4 seam (the credit-engine keystone).** New migration `20260619170000_wm_m12_credit_debit_rpc.sql` adds the atomic `debit_account_credits` RPC (SECURITY DEFINER + `FOR UPDATE`; draws INCLUDED-first then TOP-UP in one locked tx, floors at 0, writes the tagged `credit_ledger` debit). `runtime.server.ts`: `assertAccountCredits` now projects the call (new pure `projectCallCredits` in `pricing.ts`) and halts with `CreditExhaustedError` + a blocked `ai_events` row when the pool cannot cover it; `debitAccountCredits` calls the atomic RPC and passes `ai_event_id` from both `callModel` + `callModelStream`. Gated behind `credits_enabled()` -> dormant no-op. **Verification (ran):** `bun test pricing.test.ts` 19/19 (4 new projection tests), full `bun test` 212/212, tsc 0, eslint 0 on 3 files, build ✓, humanization clean, AND a behavioral dry-run of the RPC on the live prod DB (debit 70 then 60 on included=100/topup=50 -> included=0, topup=20, ledger_sum=-130, 2 rows), then rolled back (prod untouched). Adversarial review: no real fix. **◐ not ✅:** the RPC + projection are verified, but end-to-end live metering activates on publish + the flag flip. **Next: WM-M14 (attribution + caps), WM-M15 (margin levers), or WM-F1b.**
- **2026-06-19 16:31, cycle 31, `WM-M11` per-tier credit grant + monthly cycle reset shipped (◐).** Built the credit-engine grant side: new `src/lib/credits.functions.ts` (pure `monthlyGrantCredits(tier)` reading `entitlements.creditMonthlyBase` = free 500 / pro 2500 / max 10000 / team 10000 / enterprise 0; pure `resetDelta`; dormant `grantMonthlyAllowance` + `resetCreditCycle` that re-grant the INCLUDED balance and PRESERVE purchased top-ups) + new `src/routes/api/public/hooks/credit-tick.ts` cron hook (mirrors `memory-tick`: `requireHookCaller` auth, grants un-granted accounts, resets accounts past ~30 days since `cycle_anchor`). Service-role writes via `supabaseAdmin`; strict no-op while `credits_enabled()` is false; pre-migration tolerant. The ledger reconciles (writes `amount - currentIncluded`). **Verification (ran):** `bun test credits.test.ts` 7/7 (24 asserts), full `bun test` 208/208, tsc 0 (after regenerating routeTree via build for the new route + casting the untyped `credits_enabled` RPC), eslint 0 on all 3 files, build ✓, humanization clean; adversarial review no real fix. **◐ not ✅:** the grant math is unit-verified, but the DB writes + the scheduled tick activate only on publish + the flag flip + a pg_cron schedule. **Next: WM-M12 (the debit engine, fills the WM-M4 seam, the credit keystone) or WM-F1b / WM-M15.**
- **2026-06-19 16:17, cycle 30, `WM-M4` dormant runtime credit seam shipped (◐).** Added the account-level credit seam to the AI chokepoint `src/lib/ai/runtime.server.ts` so the credit engine plugs in without re-plumbing. `CreditExhaustedError` (typed); cached `creditsEnabled()` (memoizes the `credits_enabled()` flag 5 min, so the dormant hot path is about one RPC per process, not per call; a missing function keeps it dormant); `resolveCreditAccountId` (workspace to account via `workspaces.account_id`, fallback `ensure_user_default_account`, never throws); `assertAccountCredits` wired right after the per-user budget check in BOTH `callModel` + `callModelStream`; `debitAccountCredits` wired next to `incrementBudget` at both post-call sites (meters via `creditsForCost`). Gated behind `credits_enabled()` -> strict no-op while dormant; the flag + credit tables do not even exist on prod until WM-M2 publishes, so the gate self-protects against the missing schema (zero risk on current prod). Writes use the service-role `supabaseAdmin`. **Decision:** picked WM-M4 over the cursor's WM-F1b because a dormant flag-gated seam is the safer unattended build than WM-F1b's live RLS/NOT NULL swap on the core agent tables; implemented a minimal functional v1 (balance check + ledger debit + non-atomic decrement) that WM-M12 hardens (atomic draw-down RPC + draw order + blocked-event + attribution). **Verification (ran):** tsc 0, eslint 0 on the file, full `bun test` 201/201 (no chokepoint regression), build ✓, humanization clean; adversarial review no real fix. **◐ not ✅:** the live debit is DB-coupled, activating only on publish + the flag flip, so not behaviorally testable offline. **Next: WM-M11 (per-tier grants) then WM-M12 (fills this seam), or WM-F1b.**
- **2026-06-19 15:58, cycle 29, `WM-M10` credit unit + cost-to-credit conversion + legibility layer shipped (✅).** Added the credit metering math to `src/lib/ai/pricing.ts` (+ new `pricing.test.ts`), the "what one credit is" the dormant `WM-M4` seam was waiting for. `CREDIT_COGS_USD` (USD of blended COGS per credit; margin lives in grant-sizing, not the per-credit price; §7 placeholder) + optional `MODEL_CREDIT_RATE`/`creditRateFor` (default 1, pure COGS pass-through); `creditsForCost(estCostUsd, model)` = ceil over COGS, **0 for a non-billable call** (zero/negative/non-finite) and **never 0 for a billable one** (rounds up, margin-positive); `estimateCreditsForCall` composes the existing `estimateCostUsd` (one source of truth); and the pure display-only `actionCreditRange(actionKind)` legibility layer (chat_reply/research/prd_draft/mission_step/embedding) derived from calibrated representative shapes through the SAME conversion so the shown range never contradicts the meter. **Decision:** kept `actionCreditRange` PURE (no DB / no `ai_events` query) to honor the "no DB, pricing.ts only" scope — the spec's historical-averages calibration is deferred to WM-M16. **Verification (ran):** `bun test src/lib/ai/pricing.test.ts` 15/15 (29 asserts: billable-floor, 0-for-non-billable, NaN/Infinity guard, monotonicity, premium > cheap, estimator composition, legibility ordering/consistency); `tsc --noEmit` 0; `eslint` 0 on both files; `bun run build` ✓; humanization clean. Adversarial review: no real fix (never-0 invariant holds, Infinity/NaN → 0, `actionCreditRange` returns a copy of the default const, no overclaim — this is the credit library, WM-M12 drives it live). **Behaviorally verified by the unit tests (pure module, no publish → ✅.)** **Next: WM-F1b, or WM-M11 (per-tier grant) / WM-M15 (margin levers), both now unblocked.**
- **2026-06-19 15:40, cycle 28, `WM-M2` core shipped (◐): accounts table + billing relocation + credit pool shell + 30d rolling decay.** Migration `20260619160000_wm_m2_accounts_billing_credits.sql` relocates billing from the workspace to a new ACCOUNT level (the flywheel, §2.2) and lands the dormant credit pool. Creates `accounts` (owner_id, 5-tier `plan_tier` CHECK, `stripe_*`), `account_members` (role owner/admin/member/viewer), `account_credits` (balance/monthly_grant/topup/cycle_anchor), `credit_ledger` (service-role-write-only); backfills one account per distinct workspace owner (DISTINCT ON highest-tier) + owner membership + a credits shell; adds `workspaces.account_id` (FK, backfilled, asserted 0-null, NOT NULL) with a BEFORE-INSERT `set_workspace_account` trigger that auto-fills it from `owner_id` (service-role-safe, so NOT NULL can never break a future insert — unlike WM-F1's `agent_memory`); mirrors the workspace billing protection as `protect_account_billing_columns` (created AFTER the backfill so seeded tiers survive the no-JWT migration role); widens `workspaces.plan_tier` to 5 slugs (the compat shim); reworks `set_agent_memory_expiry` to 30d rolling off `last_used_at` (INSERT-OR-UPDATE, refresh-on-recall, paid-list widened), still dormant behind `memory_expiry_enabled()`; adds `credits_enabled()` (false), `is_account_member`, `ensure_user_default_account`; RLS member-read on all 4 new tables. `src/lib/billing.functions.ts` `getBillingState` reads the plan account-first with a workspace-shim fallback (migration-tolerant). **Verified via the Lovable MCP with a BEGIN..ROLLBACK dry-run of the full migration on the live prod DB: clean apply, `accounts=5 / members=5 / credits=5 / ledger=0`, all 7 workspaces linked with 0 nulls, `credits_enabled=false` — then rolled back (confirmed all 4 tables + the column absent after, prod untouched).** Offline gate green (tsc 0 project-wide, build ✓, `billing.functions.ts` eslint + humanization clean). Adversarial review found no real fix (trigger ordering correct, untyped-client safe, RLS non-recursive via DEFINER, no overclaim — the credit tables are an explicit dormant shell). **Finding (pre-existing, not WM-M2):** repo-wide `bun run lint` is red with ~4340 errors in untouched files. **Honest scope:** schema + account-level billing read are built and dry-run-verified but NOT behaviorally live (activate on the founder's next publish), so ◐ not ✅. UI breadcrumb: `Settings > Account > Plan`. **Next: WM-M10 (credit unit) or WM-F1b.**
- **2026-06-19 14:45, cycle 27, `WM-F1` core shipped (◐): agent memory recall scoped to the workspace.** Migration `20260619150000_wm_f1_agent_workspace_scope.sql` adds a nullable `workspace_id` (FK, `ON DELETE SET NULL`) to all 5 agent tables (`agents`/`agent_memory`/`agent_tools` were missing it; `agent_runs`/`agent_approvals` already had it), backfills every row via `ensure_user_default_workspace(user_id)` (handles the 1 orphan account + the 2 multi-workspace users deterministically), indexes `workspace_id`, and rewrites `match_agent_memory` + `recent_agent_reflections` with an OPTIONAL trailing `for_workspace`. Recall is service-role-safe (`auth.uid() IS NULL` bypasses the membership guard so the autonomous loop never goes dark) and null-tolerant (untagged rows recall as global). `memory.server.ts` threads the active workspace through all 3 recall call sites (loop x2 + mission-advance) with a `PGRST202` deploy-window fallback to the legacy call; `rememberOutcome` + `autoReflect` tag new rows pre-migration-tolerantly. **Used the Lovable MCP (founder enabled DB access) to read live schema + run a BEGIN..ROLLBACK dry-run of the full migration on prod: clean apply, 0 nulls across all 5 tables, both RPCs recreated — then rolled back (prod untouched).** Offline gate green (tsc 0, eslint, build, humanization). A 4-lens adversarial Workflow (372k tokens) surfaced 6 findings; folded 3 (a real pre-existing cross-user reflection-read leak closed via `coalesce(auth.uid(), for_user)`, the deploy-window recall fallback, and reflection-row tagging); documented + deferred 3 (NOT NULL/RLS/full-tagging → WM-F1b; stale generated types → regen on sync). **Honest scope:** existing memory (backfilled) + new outcomes/reflections are workspace-scoped; the remaining lower-volume insert paths still write untagged (recall as global) until WM-F1b. UI breadcrumb: not user-facing (recall is internal); effect visible as agents recalling only the active workspace's memory after publish. **Next: WM-F1b or WM-M2.**
- **2026-06-19 13:50, cycle 26, `WM-M1` Entitlements core shipped (first WM feature code).** Expanded `src/lib/entitlements.ts` from the legacy 3-tier feature-flag map to the 5-tier account model: `PlanTier = free | pro | max | team | enterprise`, `PLAN_TIERS` (5, ordered), retention bumped 14 -> 30 (`FREE_MEMORY_RETENTION_DAYS`), and a full `Entitlements` matrix (workspace/product limits, `crossWorkspaceMemory`, seats, `rbac`, `approvalLanes`, `creditMultiplier`/`creditMonthlyBase`/`creditTopUps`/`topUpCapPerCycle`, `enterpriseCreditModel`, `priority`, `dataExport`). Kept the legacy fields (`memoryPersists`, `memoryRetentionDays`, `sharedWorkspaceMemory`, `perRoleApprovalLanes`, `criticEverywhere`, `shareLinks`) so the 6 consumers (billing/pricing/settings/webhook) compile untouched. Added `limitFor(tier, kind)` and rewrote `planPresentation` with the Constellation display names (Star/Cluster/Constellation/Galaxy/Cosmos) + value-framed highlights. Credit/price numbers are deliberate placeholders (plan §7, founder-gated). Rewrote `entitlements.test.ts` (14 tests, 115 asserts). Gate green: `bun test` 14/14, `tsc --noEmit` 0, eslint 0, `bun run build` ✓, humanization scan clean. Adversarial review confirmed the webhook only derives `"pro"`/`"free"` so the widened `normalizePlanTier` cannot violate the `plan_tier` CHECK before WM-M2 widens it. **Interim (expected, not a defect):** Settings BillingTab auto-expands to 5 tiers (WM-M6 finalizes the surface); `pricing.tsx` still shows its hardcoded 3 (renamed) tiers until WM-M6; the TS retention constant is 30 while the dormant DB trigger reads 14 (WM-M2 reconciles; expiry disabled so no live effect). Detail: bible WM-M1 + [`../features/pricing.md`](../features/pricing.md). **Next: WM-F1.**
- **2026-06-18, AGENT-EXP BUILT (Phase 1 + Phase 2), gate green; on `worktree-agent-experience` (off the overnight tip; not the overnight loop); pending merge to main + signup smoke-test.** Resolved the "19 vs 6" agent question and how agents are shown: 19+ = growable backend catalog, 6 stations = the navigable spine, 5 faces = the cast shown only in motion (expose the relay, not the roster); Cast vs Crew visibility; club/refine the roster + add Ship/Herald and Learn/Echo; tighten prompts; friendly names + per-agent hue+glyph identity. Founder waived the design-last rule for this initiative, so BOTH phases build now: Phase 1 (the three-tier catalog model + two migrations + prompts + station-aware planning), then Phase 2 (the `<AgentRelay>` UI + the 6-station Product spine + Engine Room > Team + retiring the missions roster grid + the identity system). Forward-integrate only; no slug renames or row deletes (in-flight missions safe). Claimed in the dashboard (Active claims: AGENT-EXP). Canonical spec: [`../features/agent-experience.md`](../features/agent-experience.md); decision: [`../strategy/session-decisions.md`](../strategy/session-decisions.md).
- **2026-06-18, cycle 25, `D4-REPLAY` replay-and-branch shipped (strategic #6, partial).** Re-run a finished mission's goal as a NEW mission with a chosen model. The headline turned out to be mostly UI exposure: `startOrchestratedMission`/`StartSchema` already accepted `model` and threaded it to `runAgentLoop`; the `retry` button just never offered the choice. Generalized it into a model-picker + "Replay" button on `/missions/$missionId` (header, for terminal non-failed missions; the failure panel keeps its contextual replay). Added the **branch link**: migration `20260618160000_d4_replay_branch_link.sql` adds `missions.replayed_from_mission_id` (additive, idempotent, `ON DELETE SET NULL`); `StartSchema` gained `replayedFrom`, written in a separate pre-migration-tolerant update (so chat's `createMission` is untouched and the replay runs even before the migration applies); `getMission` resolves it via a separate error-tolerant read (the detail page never breaks pre-migration); the new mission shows a "Replayed from" link. Gate green (tsc + build + lint + humanization + migration-safety hook); adversarial review found no real fix (additive migration, tolerant set + read, no redundant replay affordances). Clean sync this cycle. **Deferred D4b:** the rich side-by-side checkpoint-diff (the branch link is the light diff affordance). Detail: [`../features/d4-mission-cancellation.md`](../features/d4-mission-cancellation.md) replay section.
- **2026-06-18, cycle 24, `K2` operator revert-to-revision shipped (strategic #5, partial).** The Build engine now has a one-action rollback: a confirm-gated "Revert" button on every prior revision in the Changes tab. New shared helper `src/lib/ai/studio-revert.server.ts` (`revertChangesetToRevision`) does a NON-DESTRUCTIVE revert via the GitHub Git Data API: it creates a new commit whose tree is the target revision's tree, parented on the current branch head, then fast-forwards the ref (`force:false`), so history only moves forward and the revert is itself revertible. `revertToRevision` (server fn in `studio.functions.ts`) resolves GitHub auth for the changeset's workspace via `resolveGitHub` and delegates to the helper; the ChangesPanel button is shown only while the branch is live (`committed`/`pr_open`) and never on the latest revision. The helper is self-contained (own gh fetch helpers, no registry import) so a future `studio.revert` agent tool can reuse it. Gate green (tsc + build + lint + humanization); adversarial review found no real fix (non-destructive by construction, changeset-scoped revision lookup, record-failure tolerant, RLS-scoped). Clean sync this cycle. **Deferred K2b:** the agent engine-tool (needs an `agent_tools` migration to gate `studio.revert`) + feature-flag kill (no flag system tied to changesets). Detail: [`../features/studio.md`](../features/studio.md) K2 section.
- **2026-06-18, cycle 23, `P4-GATE` shipped (strategic #4).** The Studio/Build merge tool (`studio.pr.merge`) is now hard-gated on eval regression as well as CI. A new pure module `src/lib/ai/eval-gate.ts` (`evalRegressionReadiness`, mirrors the J2 `studio-ci.ts`) blocks the agent's merge when the latest completed eval run for any suite is ≥10 points below the prior one (0-100 scale, confirmed via the KI-14 scale-fix migration). The merge tool reads the user's completed `eval_runs` (latest two per suite, explicitly user-scoped, capped at 80), fetches suite names, and throws `MergeBlocked` naming the worst-regressed suite. Reads the scheduled eval trend (no run triggered), read-only, degrades to allowed when no suite has a two-run history; the operator can always merge from GitHub directly. Gate green (tsc + build + lint + humanization); adversarial review found no real fix (scale 0-100 correct, drop direction correct, NaN/null-safe, user-scoped). Clean sync this cycle (no parallel commit). Detail: [`../features/studio.md`](../features/studio.md) step 6.
- **2026-06-18, cycle 22, `P5-ALERT` shipped (strategic #3).** Drift threshold breaches now reach the operator without visiting the Drift page: a 4th live-derived probe in `getNotifications` surfaces open `drift_incidents` in the Engine Room Attention feed (R3) as severity-coded cards (critical → warning, else info) that name the drifted metric + signed delta vs baseline and link to `/drift`. No migration (reads the existing `drift_incidents` table over its `(user_id, status, detected_at)` index), capped at 6 so a noisy week cannot flood the feed, degrades to no items on error. Both feed consumers (NotificationsPanel, AttentionBell) are kind-agnostic, so adding the `drift` kind needed no UI change. Gate green (tsc + build + lint + humanization); adversarial review found no real fix (numeric coercion guarded, severity safe-defaulted, RLS-scoped). Clean sync this cycle (no parallel/runaway commit). Detail: [`../features/r3-notifications.md`](../features/r3-notifications.md).
- **2026-06-18, cycle 21, `F-AGENTS-MENTIONABLE` completed (strategic #2).** The server half (parse `@agentslug` → pre-planned single-step `mission_steps` dispatch via `advanceMissionCore`, deterministic completion via `steps.length > 0`) was auto-committed mid-build by a parallel process (mislabeled "cycle 19", `40646dce0a`/`ffbf5c50c0`) while the main loop was still working the same worktree. Reconciled cleanly: the committed `chat.ts` matched the main loop's server work; this cycle layered the genuinely-new value on top (the composer @-agent picker: keyboard nav + click, reuses `listAgents`, never hijacks Enter-to-send; and a case-insensitive parse fix so a manually-typed `@Strategist` resolves), plus the docs the parallel commit skipped (this SSOT, the feature doc [`../features/agents-mentionable.md`](../features/agents-mentionable.md), session-decisions). Gate green (tsc + build + lint + humanization); adversarial review surfaced the case-insensitivity gap. Recurring-incident note in session-decisions: a background process is committing/pushing autonomously again (same pattern as the F3 `c304bf6396` incident).
- **2026-06-18, cycle 20 verified + closed.** Re-ran the full gate on the mid-flight `F3-CRON` tree: `tsc --noEmit` clean, `bun run build` green (the `cluster-tick` route bundles), `eslint` clean on all four touched files, and the F3 source + migration scan zero em/en dashes. Closed the feature-doc loop ([`../features/f3-continuous-discovery.md`](../features/f3-continuous-discovery.md): cron section, governance/activation, verification). Committed F3 + this close. Re-rank workflow confirmed complete (section 3 is impact-ordered). Next strategic pick: `F-AGENTS-MENTIONABLE`.
- **2026-06-18, cycle 20, `F3-CRON` (strategic #1) shipped gated off.** A multi-agent strategic ranking (workflow `cadence-strategic-build-rank`, weighting v10>v9>v8>v7>v6) picked `F3-CRON` as the highest-impact buildable item vs the current P0 milestone (over the easy-but-low-impact `APP-HEALTH`). Built the auto-cluster mechanism (the `cluster-tick` hook + extracted `clusterSignalsCore` + an owner toggle + the `auto_cluster_enabled` migration), gated OFF; gate-green; adversarial review caught + fixed a service-role `workspace_id` NOT-NULL bug. Activation is founder-gated (section 4).
- **2026-06-18, docs consolidation, `2e4421e800`.** Folded the siloed trackers into this SSOT, archived the three stale v7 docs, repointed 24 references, fixed the boot hook (v6→v10 + SSOT-first).
- **2026-06-18, backlog reconciliation.** Cross-referenced v10/v9/v8 + dashboard + considerations vs `src/`; disproved the "thin backlog" read; produced the section-3 queue (~17 autonomous items) and codified the founder's standing rulings (section 1).
- **2026-06-18, full live verification (cycles 1-19), `40b24cf972`.** Founder published everything; Playwright-swept the live app; all checked features work; logged 2 non-bug findings (section 5).
- **2026-06-18, cycle 19, `d8ee421d43`.** O1 provenance in the shared Lineage drawer (recovered from a mid-build rollover, finished, gate-green, one adversarial fix).
- Earlier cycles (1-18): see the build report and `plan.md` §4 for the full dated history.

---

## 7. The documentation map (this file is the front door)

This SSOT is the one place for "where are we / what is next / what needs you" (sections 0-6). Everything else is **typed detail** it points to. Each doc owns ONE concern; never duplicate live status into them. The full operating doctrine (session-start steps, how to pick / mark / update a task, the new-initiative rule) is in [`../../AGENTS.md`](../../AGENTS.md) section "The Documentation Operating System".

| When you need... | Go to |
| --- | --- |
| What is in flight + the next picks | **This file, section 0** |
| What needs the founder (gated decisions/secrets) | **This file, section 4** |
| Status of every feature (matrix + who-is-on-what claims) | [`feature-dashboard.md`](./feature-dashboard.md) |
| Per-feature acceptance criteria / scope (F-IDs) | [`archive/feature-backlog.md`](archive/feature-backlog.md) |
| Current-initiative build specs (per-ID, cold-buildable) | [`workspace-tenancy-and-monetization-plan.md`](./workspace-tenancy-and-monetization-plan.md) (G10) · [`byo-build-implementation-plan.md`](./byo-build-implementation-plan.md) (G11) · [`ambient-precedent-plan.md`](./ambient-precedent-plan.md) (DBR row 148, increment 1) · [`supersession-engine-plan.md`](./supersession-engine-plan.md) (DBR-1.5, the supersession write-path) |
| Open bugs / blockers | [`known-issues.md`](./known-issues.md) |
| Cross-cutting non-functional gaps | [`considerations.md`](./considerations.md) |
| Dated build history (what shipped + why) | [`../../plan.md`](../../plan.md) section 4 |
| Strategy / positioning / moat | [`../strategy/README.md`](../strategy/README.md) (the arbiter) |
| Superseded snapshots / old run logs | [`archive/`](./archive/) |

_Standing rule: every change updates THIS file (section 0 cursor + the relevant section) in the same unit of work, then the one typed ledger it touches. No status lives in two places._
