# Parallel build report - Cockpit / observability lane

> _Per-lane audit trail. The WM/overnight lane reports separately in `overnight-build-report.md`; do not write there._
> Branch: `parallel/cockpit` · Worktree: `cadence-cockpit` (sibling of the repo) · Lane scope: this worktree's `.remember/LANE.md`

This lane builds notification preferences and the persistent incidents / cost-incident log. Queue, owned paths, forbidden paths, and the stay-in-lane rules live in `.remember/LANE.md`. Follow `docs/operations/autonomous-build-loop.md` section 15.

> **▶️ RESUMED + ACTIVE 2026-06-21 04:38.** The Build Sequence is now live in the repo (numbered Master register + pick-by-Rank law in `feature-dashboard.md`), which resolved the prior pause. Picked the highest-Rank row Lane 1 can cleanly own: **#4 MOAT-METRIC** (Tier-1, Governance, the scale-independent investor proof) — #1 DBR-1.5 + #2 MOAT-VIS + #3 F-IA-BRAIN-GRAPH are knowledge-lane / Brain-surface forbidden for this lane. Shipped the unbuilt **lift half** (the memory-depth split: does validated-outcome share rise as the store compounds), an HONEST correlational metric gated by a size floor + depth-contrast + a two-proportion 95% CI noise gate, designed + adversarially red-teamed via two multi-agent workflows (final review verdict ship, 0 must-fix); tsc 0 / 492 tests / build green; ◐ on publish. This is the Tier-1 moat substance the pause was waiting for — the loop continues down the Build Sequence. _(The prior session's 5 ◐ "is-the-autonomy-guarded" cycles — board rows 152-156 — are the Tier-3 ops-hygiene category, now correctly deprioritized behind Tier-1 moat work.)_ Lane active, ledger clean.

## Status (rewritten each cycle; date + time every row)
| Date · time | Cycle | Item | State | Notes |
| --- | --- | --- | --- | --- |
| 2026-06-21 07:45 | 5 (numbered Lane 1) | board dry (lane 1) — long-polling | wait | After 4 ships this session, lane 1's CLEAN buildable board is exhausted. `lane.sh next` returns 8 ids, ALL in §15.6's dry categories FOR LANE 1: DBR-1.5 / O1 / O3 (AI-core + knowledge-lane; engine shipped, remainder founder-flag/forbidden), F-IA-BRAIN-GRAPH (knowledge-graph* forbidden), EMBED-CHOKEPOINT (chokepoint-pinned), Q1/ENG-07 (founder-gated Phase 4b), W1-AUTO (publish-verify only), H2-WRITES (chokepoint agent-wiring + a P2-marginal bulk-commit). Owned features near-complete: EVAL-COVERAGE ~92% (remainder = CI-wiring of the floor), H2-WRITES ~85% (remainder = chokepoint + bulk-commit). Cockpit/Governance peers shipped/complete (ENG-06 per-agent telemetry already built via getAgentSpendBreakdown+getUnitEconomics; APP-HEALTH complete; RELIABILITY-SLO/GLANCE/RUNAWAY shipped). Swept ALL considerations.md lenses: clean valuable gaps shipped; rest are forbidden families / infra (CI/CD, DR) / docs+founder (ToS, status page) / large+overlap (product-analytics). Building the only strictly-clean leftover (bulk-commit) would violate the founder velocity ruling ("nothing speculative / minimum code"). NOT a false dry — every candidate is enumerated. Long-polling ~25 min (ScheduleWakeup); a released claim / new row / founder push reactivates. |
| 2026-06-21 07:35 | 4 (numbered Lane 1) | EVAL-COVERAGE one-click guard | done ◐ | Actionability-loop completion: each uncovered/stale coverage chip is now a button opening `CreateSuiteForm` pre-targeted to that surface (label-seeded name; target validated vs SURFACE_KEYS w/ safe fallback; form re-keyed on prefill; manual/empty-state opens stay unseeded). Single-agent adversarial review (feature-dev:code-reviewer) → SHIP, 0 defects; folded a defensive prefill-clear. tsc 0 / eslint 0 / `bun test` 608. Pushed `4b30734386`. Spec `eval-coverage.md`; row #56 → ~92%. |
| 2026-06-21 07:25 | 3 (numbered Lane 1) | H2-WRITES move-provenance render | done ◐ | Read-side completion of cycle 2: `RoadmapHistory.tsx` "why" popover now renders a re-prioritization as "moved · now → next" from the captured `from_bucket` (new `eventBuckets` helper; arrow only when a source was actually recorded — legacy/backlog-origin null-source falls back to destination, never fabricating a from). Trivial display helper, self-verified 4 cases, gates green (proportionate, no Workflow). tsc 0 / eslint 0 / 608 tests. Pushed `a88bdb3339`. Row #12 → ~85%. |
| 2026-06-21 07:12 | 2 (numbered Lane 1) | H2-WRITES move-audit provenance | done ◐ | Roamed to the H2-WRITES "richer write surface" remaining. The roadmap-decision audit's "move" was hollow (`recordRoadmapDecision` never threaded `from_bucket`; move call-site carried no outcome). New pure `classifyRoadmapWrite(prev,next)` drives `updateRoadmapItem` to record full-provenance audits (move carries from_bucket+outcome; in-place re-declaration = commit; combined relocate+re-declare emits BOTH; killed a phantom move on unchanged re-save). No migration, no chokepoint. 2-lens adversarial review (logic+security) both SHIP, 0 blockers; folded combined-write completeness + normalization. tsc 0 / eslint 0 / `bun test` 608 (roadmap-audit 5→14). Pushed `e781e1e037`. Spec `h2-writes.md`. |
| 2026-06-21 06:36 | 1 (numbered Lane 1) | EVAL-COVERAGE follow-up | done ◐ | Per-surface coverage chips + a dormant-by-default coverage-floor deploy-gate primitive (`evaluateCoverageFloor`, `configured:false`⇒pass; env-driven, never blocks until the founder opts in). Chips: covered=quiet emerald / uncovered=rose / stale=neutral-dashed (colorblind-safe + off the celebration token), aria-labelled. 3-lens adversarial review (logic/security/UX, 0 blockers); folded 5 fixes incl. the unrounded-ratio floor gate (86 not met by true 85.7%). tsc 0 / eslint 0 / `bun test` 599 (coverage.test 18→34). Pushed `0244e542f0`. Spec `eval-coverage.md`; row #56. |
| 2026-06-21 05:17 | 10 (numbered Lane 1) | FIRECRAWL-FLOOR | done ◐ | Continued (loop never stops). After MOAT-METRIC, Lane 1's clean Tier-1 Governance/Cockpit vein was exhausted — #1 DBR-1.5 (lineage-forbidden + ◐ Lane 2), #2 MOAT-VIS (held by Lane 2, ledger blocked my claim), #3 F-IA-BRAIN-GRAPH (Brain forbidden), #5 EMBED-CHOKEPOINT (chokepoint), #7 H1-TASKS (stale: backlog ✅, core needs AI chokepoint). Per NEVER-STOP ("nothing clean left" is NOT a pause reason), roamed to the lowest-Rank eligible row: **#6 FIRECRAWL-FLOOR** (a leaf web tool, NOT the chokepoint pin; BBI ACT-autonomously self-host item). Made the agent's `web.search` provider-agnostic: additive seam in `webSearch` — FIRECRAWL_API_KEY → existing path byte-identical; else SEARXNG_URL → native self-host SearXNG floor (honest snippets, no fabricated markdown); else a clear error naming both. Pure `web-search-fallback.ts` (backend selection + injection-safe URL build + defensive normalization, 19 tests) + `searxng.server.ts` shell. webFetch/webMap/webCrawl stay Firecrawl-only (no SearXNG scraper); untrusted-input guardrail contract preserved. **2-lens adversarial review: ship, 0 must-fix.** Gate: tsc 0 / 528 tests (19 new) / build ✓; routeTree churn reverted. ◐ live-verify on a deployment with SEARXNG_URL. **Follow-up FIRECRAWL-FLOOR-b:** widen research.server.ts L319 web gate to accept SEARXNG_URL (review-confirmed out-of-scope, not a regression). Spec: `docs/features/firecrawl-floor.md`. |
| 2026-06-21 04:38 | 9 (numbered Lane 1) | MOAT-METRIC (lift half) | done ◐ | RESUMED post-pause; picked by Rank (#4, the highest-Rank cleanly-ownable Tier-1 row for Lane 1; #1-3 are lineage/Brain-forbidden). Built the unbuilt lift half: a within-account **memory-depth split** — splits reviewed bets by `depth = count(agent_memory dated on/before the bet)` into earlier/sparse vs later/rich halves, reports the validated-share difference in whole pp. HONEST by construction: a size floor (8/side, N≥16), a material depth-contrast guard, AND a two-proportion 95% CI noise gate (no external dep) → never prints noise; below the gates "not enough data yet" with a typed reason; measured-0 is a real 0, negatives as-is; CORRELATIONAL not causal (neutral colour either sign; time/practice/survivorship confound named inline + footnote). Pure `computeMemoryLift` (memory-compounding.ts, 17 tests) + `getMemoryLift` (gauntlet.functions.ts) + `MemoryDepthSplitCard` (GauntletMetricsPanel.tsx). **Two multi-agent workflows:** a design+red-team (honesty/stats/data lenses) that hardened the construction (added the CI gate, raised the floor 5→8, median-separation, whole-pp, truncation refusal), then an adversarial diff review (runtime/spec/honesty) → **verdict ship, 0 must-fix** (folded a dead-branch em-dash → ASCII + cap-refusal copy). Gate: tsc 0 / 492 tests (17 new) / build ✓; routeTree churn reverted. ◐ renders + behaviorally verifies on publish (reads live learnings + agent_memory). UI: Govern > Gauntlet > "Memory-depth split". Spec: `docs/features/gauntlet-metrics.md`. |
| 2026-06-19 | 0 | (scaffold) | ready | Worktree + bypass settings + LANE.md provisioned. Awaiting first `/overnight-build`. |
| 2026-06-20 00:53 | 1 | R3-PREFS | done | Notification preferences table + settings route + scaffolding. In-app filters applied. |
| 2026-06-20 02:24 | 2 | P7 | done | Persistent incidents table (`cost_incidents`) + RLS policies + cost incident detector (detects budget cap block alerts from `ai_budget_alerts` as incidents) + detailed cost badge integration. Verified via unit tests + tsc/build/lint green. |
| 2026-06-21 01:26 | 4 (numbered Lane 1) | RELIABILITY-SLO | done ◐ | Roamed: first scouted DATA-RETENTION-b (right-to-be-forgotten erase) but the atomic ledger returned `HELD by lane 0`, so the claim gate prevented a collision and I took the next eligible Cockpit/reliability item. Built the AI-surface SLO/error-budget primitive (considerations.md SRE P1): pure `src/lib/reliability/slo.ts` (`computeSlo` + helpers, 22 tests) + read-only `getReliabilitySlo` over `ai_events` (user-scoped RLS, 5000-cap + `truncated` flag, no writes/AI/spend). Crux: `blocked` halts excluded from availability/latency; unknown status fails VISIBLE (→ error). 3-lens adversarial-review workflow (no runtime-fatal/security defect; folded 5 findings). Gate: tsc 0 / eslint 0 / build ✓ / 409 tests (22 new). ◐ live-verify on publish (DB MCPs intermittent). Engine Room glance + status page = follow-ups. Spec: `docs/features/reliability-slo.md`. **Infra:** discovered the lane ships to `origin/main` via fast-forward (single-branch model), not the `parallel/lane-1` remote branch; corrected (this work is now on `origin/main`). |
| 2026-06-21 02:35 | 8 (numbered Lane 1) | RUNAWAY-INCIDENTS | done ◐ | Continued (the decision panel's runner-up). Wired the runaway detector into the persistent Incidents log: a spinning mission now lands in the operator's "what went wrong" record, not only the silent glance. Extracted a shared pure `buildMissionStats` (from `getRunawayMissions`) so the incidents source + the read fn cannot drift; new `runaway` IncidentKind (tsc forces a KIND_LABEL entry); emit ONLY active `runaway` (terminal `watch` excluded → no flood); stable id `runaway:<id>`; read-only, user-scoped, error-tolerant per the aggregator convention. Tests: buildMissionStats + an incidents runaway-source test (thenable mock). **2-lens adversarial review: both SHIP** — refactor PROVEN behaviorally equivalent, no correctness/security/leak/flood defect. Gate: tsc 0 / eslint 0 / build ✓ / 475 tests (8 new). ◐ live-verify on publish. **New directive surfaced (next pick): memory `build-sequence-and-ownership.md` — pick lowest open number / Build Sequence (DBR-1.5 #1); Lovable owns WM-M*/M-C-PRICE/BYO-P4, WM-F* stays Claude.** Spec: `docs/features/runaway-detection.md`. |
| 2026-06-21 02:10 | 7 (numbered Lane 1) | EVAL-COVERAGE | done ◐ | Continued. Picked via a 3-angle decision-panel Workflow (considerations gaps · board · in-flight completion) with adversarial eligibility verification; EVAL-COVERAGE won (fully verifiable + zero-orphan: denominator SURFACE_KEYS + consumer EvalsPanel already existed). Closes AI-safety P1 "Eval coverage targets per surface/agent" + completes the "is the autonomy guarded" triad. Pure `src/lib/evals/coverage.ts` (covered/stale/uncovered per surface×prompt; 18 tests) + read-only `getEvalCoverage` + a calm Evals banner; SURFACE_KEYS unified (also dropped em-dash labels). **2-lens adversarial review caught a feature-inverting bug the gate missed:** success status is `completed` not `complete` (my constant was the lone outlier; the 17 original tests masked it by asserting the wrong constant) → coverage would have read 0% forever; fixed + regression-tested against the real DB literal; also folded enabled-only case counting. Gate: tsc 0 / eslint 0 / build ✓ / 464 tests (18 new). ◐ live-verify on publish. Follow-up: per-target chips + a coverage-floor deploy gate. Spec: `docs/features/eval-coverage.md`. |
| 2026-06-21 01:55 | 6 (numbered Lane 1) | RELIABILITY-GLANCE | done ◐ | Continued (loop never stops). Wired the two prior cycles' read fns into the UI (the adversarial review flagged them orphaned; backs the North Star + the founder's FE-wiring theme). New `src/components/cockpit/ReliabilityGlance.tsx` (mirrors `MissionsCostGlance`) renders a single calm "Heads up" line ONLY when the AI error budget is warning/exhausted OR a mission is spinning; silent when healthy; degrades to silent on query error. Wired into `_authenticated.missions.index.tsx`. Design-gated via `impeccable`: loaded the Ember Editorial DESIGN.md, folded a role-color violation (dropped an invented `--warn` token for neutral ink tones since the accents are reserved). Gate: tsc 0 / eslint 0 / build ✓ / 438 tests. ◐ renders on publish. Follow-up: deep Engine Room breakdown + drill-in. Spec: `docs/features/reliability-slo.md` + `runaway-detection.md`. |
| 2026-06-21 01:40 | 5 (numbered Lane 1) | RUNAWAY-DETECT | done ◐ | Continued (loop never stops). Closed the AI-safety P1 "Loop/runaway detection" gap (KI-15/16 = caps, this = detect; inverse of E8 stall). Pure `src/lib/reliability/runaway.ts` (`assessMission`/`assessMissions` + helpers; hop/step/retry-churn/pinned-step/spend thresholds → runaway vs watch; unknown status = active fail-loud; 18 tests) + read-only `getRunawayMissions` over missions/mission_steps/agent_runs (user-scoped: missions `.eq('user_id')`, children `.in('mission_id', ids)` over own ids; 200-cap + `truncated`; no writes/AI/spend; NO loop edit). Thresholds conservative ABOVE the real caps (no false positives on a normal mission). 2-lens adversarial review: both SHIP, folded a calibration-comment accuracy fix + dash cleanup. Gate: tsc 0 / eslint 0 / build ✓ / 427 tests (18 new). ◐ live-verify on publish. Operator surface + alerting = follow-ups. Spec: `docs/features/runaway-detection.md`. |
| 2026-06-20 19:01 | 3 | P7-FIX | done | Re-verified the dry lane, then adversarially audited the shipped R3-PREFS + P7. Audit found the cost-incident detector dead: it filtered `ai_budget_alerts` on `kind="block"`, but the producer (`runtime.server.ts`) only ever writes `kind="warn"` (a real cap halts by throwing), so it matched zero rows in prod (the unit test masked it with a synthetic block row). Fix (owned `incidents.functions.ts` only; producer is the forbidden chokepoint): `.in("kind",["warn","block"])` surfacing the real threshold-crossing alerts, true pct/window copy, escalates at pct>=100; added window_kind to the badge + 2dp money. Verified the producer contract directly (warn fires at ~80%, pct capped at 100), correcting the audit's `pct>=100` suggestion (would also have been near-dead). TDD red->green + regression test. Gate: tsc 0 / 11 tests / build green. No migration. Lane DRY again post-fix. |

> **Post-dry (2026-06-20 19:31):** the lane queue is dry. Per the founder's autonomous-mode override (a dry lane is NOT a stop), this session continued as a general autonomous builder pulling from the master sheet (SSOT build queue / dashboard), claim-first. Out-of-lane work shipped from here is logged in `plan.md` §4 + the dashboard register, not as cockpit-lane rows. Shipped post-dry (all gated green, no migration): **KI-16b** (per-mission per-tick dispatch cap, dashboard row 41 → ✅); **MISSION-DEADLOCK-FIX** (a failed step no longer hangs the mission `running` forever — a skip-cascade in `mission-advance.server.ts`); **HANDOFF-CAS-FIX** (no double-consume of a handoff under same-agent fan-out — CAS claim + retry in `consumeInboundHandoff`); **LOST-QUEUED-RECOVERY** (a step whose child run is stuck `queued` is recovered instead of hanging — `reflectStepStatusFromRuns`); **DECISION-ATTRIBUTION-FIX** (mission decision capture now attributes the rationale to the run that finished last, not the one enqueued last — `maybeCompleteMission`). All found by the reliability-cluster adversarial audit, and **all four in-scope findings are now shipped**. The one remaining reliability-cluster finding is out-of-scope (a non-owner-workspace-member `getMission` empty-hops gap needing an `agent_runs` RLS migration — flagged for the WM/tenancy lane).

A second adversarial audit (loop front-end: ingest security · reactor dispatch · cron-hook auth) found 8 confirmed bugs. Shipped from it: **INGEST-INJECTION-FIX (KI-25, CRITICAL)** — ingested signal text can no longer prompt-inject the agent instruction channel (`<untrusted_signal>` fence in `goalForEvent`). Flagged founder-gated (recorded as KI-): **KI-26 (CRITICAL)** cron hooks guarded by the client-exposed publishable key (needs a server-only secret + pg_cron reconfig); **KI-27 (HIGH)** reactor staleness reaper + retry cap (needs a migration). All THREE in-scope loop-frontend findings now shipped: **KI-25** (prompt-injection fence), **KI-28** (reactor double-dispatch claim-first CAS), **KI-29** (ingest rate-limiter fail-open). Remaining from that audit: KI-26 (founder-gated cron-auth), KI-27 (reactor reaper/retry, migration), and the eval-tick idempotency MEDIUM (in-scope, next pick). Total shipped this session: 9 commits across two reliability audits + the original P7/KI-16b work.

## Pending published-app verification
| Date | Item | Class | Note |
| --- | --- | --- | --- |
| 2026-06-20 | R3-PREFS | Needs publish first | `/notifications` preferences page and settings need the migration to be live. |
| 2026-06-20 | P7 | Needs publish first | Persistent cost incidents table and components need the schema migration to be live. |

## Session close — autonomous general-backlog run (2026-06-20 ~21:55)

Post-dry, per the founder's autonomous-mode override, this session ran as a general autonomous builder across the master backlog (collision-safe, claim-first, no chokepoint / claimed-lane / founder-gated edits). **13 commits to `main`, all green** (~10 real bugs fixed + KI-16b feature), driven by **5 adversarial audits** (reliability · loop-frontend · discovery/clustering · connectors · projects/budgets), each finding verified against source before fixing.

**Shipped (in-scope, verified, gated):** P7 dead detector · KI-16b dispatch cap · mission-deadlock skip-cascade · handoff double-consume CAS · lost-`queued` recovery · decision attribution · KI-25 prompt-injection fence (CRITICAL) · KI-28 reactor double-dispatch CAS · KI-29 rate-limiter fail-open · KI-30 eval-tick double-spend · KI-31 cluster cross-workspace + atomic claim · KI-32 roadmap terminal-item filter.

**Safe-fix queue DRY:** the 5th audit returned zero in-scope-fixable findings; projects.functions.ts is sound (RLS workspace-keyed, no export leak). All remaining work is founder/migration/attended/product-gated, recorded in `known-issues.md`, priority order: **KI-34 (CRITICAL cross-tenant credential theft)** · **KI-26 (CRITICAL cron-auth public key)** · KI-35/KI-36 (connector HIGH) · KI-27 (reactor reaper/retry migration) · KI-37 (token caps enforce-or-remove) · KI-33 (roadmap dual-store) · agent_runs RLS gap. Lane stops here ("queue dry, awaiting founder") rather than burn ~500k tokens/audit for near-zero fixable yield. **Recommend an attended security cycle for KI-34 + KI-26 first.**

## Session close 2 — founder-authorized security cycle (2026-06-20 ~22:35)

Founder authorized the security cycle + enabled live publishing (live DB access via the Lovable MCP; dry-runs on prod). Delivered, all green on `main`:
- **KI-26 (CRITICAL cron-auth public key):** confirmed already code-fixed by another session (`requireHookCaller` now requires a private `CRON_SECRET`/`HOOK_CRON_SECRET`; no publishable-key fallback). Founder step remains: provision the secret + point pg_cron at `x-cron-key`.
- **KI-34 (CRITICAL cross-tenant credential theft):** CLOSED — app-layer guard in `resolveProviderAuth` (`bindingConnectionAllowed`) + RLS migration `20260620220000` validating connection ownership (dry-run-verified on prod).
- **KI-36 (HIGH binding hijack):** CLOSED — same RLS migration (only creator/admin can mutate a binding).
- **KI-27 (HIGH reactor reaper/retry):** CLOSED — migration `20260620220500` ('processing' status + attempt_count/next_attempt_at) + reaper + bounded retry.
- **RLS-posture sweep:** every public table has RLS + ≥1 policy — no gaps.

In-scope autonomous queue is DRY (RLS posture clean; no safe code-fixable items left). Founder/WM-gated remainder, priority order: **founder** — provision `CRON_SECRET` (KI-26); decide KI-37 (token caps: enforce-in-chokepoint vs remove-the-inputs); decide KI-33 (roadmap dual-store canonical column + backfill); the gateway ownership API for KI-35. **WM/tenancy lane** — the agent_runs cross-member SELECT (agent-table RLS is theirs). The autonomous lane stops here ("queue dry, awaiting founder/WM"), per the founder's standing stop-when-genuinely-dry allowance.
