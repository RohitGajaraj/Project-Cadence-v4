# Parallel build report - Knowledge / Sense lane

> _Per-lane audit trail. The WM/overnight lane reports separately in `overnight-build-report.md`; do not write there._
> Branch: `parallel/knowledge` · Worktree: `cadence-knowledge` (sibling of the repo) · Lane scope: this worktree's `.remember/LANE.md`

This lane builds the typed knowledge-graph explorer, fact-currency/drift flags, and versioned skill-pack export. Queue, owned paths, forbidden paths, and the stay-in-lane rules live in `.remember/LANE.md`. Follow `docs/operations/autonomous-build-loop.md` section 15.

## Status (rewritten each cycle; date + time every row)
| Date · time | Cycle | Item | State | Notes |
| --- | --- | --- | --- | --- |
| 2026-06-21 07:15 | L2-dry-3 | board still dry - long-polling | noted | Recheck 3. Change since L2-dry-2: lane 1 claimed #12 H2-WRITES (a former candidate, now taken) + shipped an EVAL-COVERAGE follow-up. Still no clean autonomous lane-safe pick: `lane.sh next 2` = DBR-1.5 (#1, code done - founder must publish+flip flag) + F-IA-BRAIN-GRAPH (#3, design/IA gated). O3 skill-pack still under-specified (founder spec needed); rest chokepoint/design/horizon/founder-gated/Lovable. The logged MOAT-VIS-b cleanup stays velocity-deferred (founder-prompted pre-launch stage). Long-polling per §15.6 (~25 min). NEVER hard-stopping. |
| 2026-06-21 06:50 | L2-dry-2 | board still dry - long-polling | noted | Recheck after lane 1 released Q1-MCP. Re-scanned via `lane.sh next` (surfaced DBR-1.5 + F-IA-BRAIN-GRAPH, both founder-gated remainders). Confirmed STILL no clean autonomous lane-safe pick: #1 DBR-1.5 = code done, remainder is founder action (publish→migration + flip flag); #3 F-IA-BRAIN-GRAPH = design/IA placement; #5/#26 chokepoint; #24/#28/#29-32 design/IA/horizon; #11 Q1 Phase 4b = founder-gated (OAuth/SSE/write-CRUD); #12 agent-tool+UI. **#10 O3 skill-pack export (the one Q1 just unblocked) is UNDER-SPECIFIED** - no skill-pack schema/spec exists anywhere (only one-line "export versioned skill bundles over MCP" mentions), so building it = inventing the bundle schema speculatively (violates "don't guess / nothing speculative") + it couples to lane 1's MCP server. **Founder unblock for O3:** decide the skill-pack contents + format (versioned JSON of the decision graph/lineage/outcomes?) so the lane can build the export bundle. Long-polling per §15.6 (~25 min). NEVER hard-stopping. |
| 2026-06-21 06:25 | L2-VERIFY | Live verification on the published app | done | Founder-directed live test on cadence-flow-beta.lovable.app (demo account). **MOAT-VIS → ✅:** Today "What changed" card + Brain `CompoundingPanel` both render correctly with real data ("Memory has re-scored 1 decision · net ICE +0.4", feed row "ICE 8.3 ↑ 8.7"); math correct, no bugs. **H1-TASKS → ✅:** the credit gate (0 balance) blocked the AI `generateTaskGraph`, and both DB MCPs were down (Lovable blocked by a health-check hook, Supabase unauthorized), so I seeded a real DAG via an **owner-scoped Supabase REST PATCH using the live browser session's JWT + the public anon key** (RLS-permitted, fully reverted afterward to the exact seeded state). `TaskGraphPanel` rendered "40% · 2/5 done · 2 ready · 1 blocked · ~13h left · READY NOW #3 #5" (all values correct); a seeded missing-dep produced "1 GRAPH ISSUE · Task 5 depends on missing task 4" with computed color === `--rose` (MF-2 fix confirmed); read-path `sanitizeDependsOn` stripped an injected forward-ref live. No bugs. **DBR-1.5 → stays ◐:** confirmed dormant + safe on the live DB (0 supersession-engine edges, flag OFF), but the migration is NOT yet applied live (`valid_to` column absent, err 42703) - the founder must publish (applies the migration) then flip `DECISION_BRAIN_SUPERSESSION=1` to activate; correctly byte-identical until then. No code bugs found in any of the three. |
| 2026-06-19 | 0 | (scaffold) | ready | Worktree + bypass settings + LANE.md provisioned. Awaiting first `/overnight-build`. |
| 2026-06-20 21:14 | 1 | O1 v1 (List tree) | shipped ◐ | Parallel-session lean tree explorer (`buildLineageTree` + `hydrateTreeTitles` + `getLineageTree`) shipped to main; deterministic projection over `artifact_lineage`, zero migrations, 6 unit tests, tsc/build green. Kept as the "List" view of the merged tab. |
| 2026-06-20 | 1b | O1 / DBR-1 v1 (visual graph + reconcile) | shipped ◐ | Found the lane queue (O1/O3) absorbed into the founder-gated Decision Brain bet; founder enriched + approved the "B+" cut, built the bounded **visual graph** (`projectGraph` over `artifact_lineage`; RLS-scoped fail-safe server fn with batched `.in()`; `GraphExplorer`/`GraphNodeStory`/`GraphCanvasView`; truthful "as of" time axis + empty supersession seam; 13 TDD tests), then RECONCILED with the parallel lean tree per founder ruling "keep both": a `GraphPanel` **Graph | List toggle** at `/knowledge?tab=graph`, my code relocated to `knowledge-graph-view.*`, their test fixed `vitest`→`bun:test`. Gate: tsc 0 / build green / 19 graph unit tests (13 visual + 6 tree). Adversarial review folded (empty-graph singleton + `.in()` batching); `filterByTime` maxRing-gap = non-blocking debt. Spec: `docs/features/knowledge-graph-explorer.md`. |
| 2026-06-20 | 2 | O3 drift/currency slice | shipped ◐ | Deterministic fact-staleness on the graph: pure `computeStaleness` (newest-incident-edge age vs a 90d threshold; honest, never counts undated nodes) + 3 tests; surfaced as a stale count + a dashed amber ring in the Graph view. Gate: tsc 0 / build green / 16 graph unit tests. Remaining O3: contradiction-drift (supersession engine, AI lane) + the skill-pack export over MCP (needs Q1). |
| 2026-06-21 04:10 | L2-1 | DBR-1.5 supersession engine (#1 register row) | shipped ◐ | The moat's signature write-path, filling the empty seam DBR-1 reads. New PURE `src/lib/ai/supersession.ts` (verdict-conflict matrix `classifySupersession` + `resolveEndpoints` + `selectSupersessions` cap+dedup + `buildSupersessionEdge`; 17 tests) + gated orchestrator `supersession.server.ts` (`inferSupersession`: flag-first-line dormant = zero embed/write/byte-identical; fail-safe) hooked into `recordOutcome` after `rememberOutcome`. Additive migration `20260621030000_*` (3 nullable bi-temporal cols: `valid_to`/`invalidated_by`/`inference`; explorer SELECT excludes them = byte-identical read). Invalidate-don't-delete: only INSERTs typed edges + stamps `valid_to` on its OWN engine edges, never DELETEs, never touches a promoted/human edge. **4-lens adversarial-review Workflow** (correctness · dormancy/fail-safe · data-integrity · security/RLS) → 0 blockers, SHIP_WITH_FIXES; folded 2 real write-path defects (zombie-edge-on-re-record → stamp `valid_to:null`/`invalidated_by:null` on the upsert payload; missing `excludeId` → self-memory starved a real candidate) + the opportunity-fallback dedup NIT. Gate: tsc 0 (my files; 4 pre-existing Stripe-dep errors unrelated) / eslint 0 / 492 tests. Build red is a PRE-EXISTING baseline failure (`@stripe/react-stripe-js` not installed in this worktree), proven by a clean-stash build. Deferred follow-ups (logged): `getLineage` `select("*")` wire-shape drift (NIT-3, in knowledge-lane-owned `lineage.functions.ts`, NOT touched per the plan/lane boundary); cross-workspace-within-account inference is by design (artifact_lineage is user-scoped) — document at flag activation. Plan: `docs/planning/supersession-engine-plan.md`. |

| 2026-06-21 04:45 | L2-2 | MOAT-VIS make compounding visible (#2 register row) | shipped ◐ | Surfaces the outcome->learning->ICE-rescore compounding on Today + Brain. New PURE `src/lib/moat-vis.ts` (`summarizeCompounding`/`rescoresOf`/`describeCompounding`; rounds to display precision so a sub-0.1 jitter never reads as a move; 10 tests) + the named-missing `getCompounding` server fn in `today.functions.ts` (RLS-scoped, mirrors `listLearnings`, shares the pure summarizer). Brain Learnings tab now renders a new `CompoundingPanel` (aggregate "memory re-scored N decisions, net ICE ±X" line + cause-carrying feed) instead of the old MemoryPanel landing; Today's "what changed" card gains the aggregate compounding line (>1-rescore gate). **2-lens adversarial-review Workflow** → 0 blockers, SHIP_WITH_FIXES; folded the RAW-vs-rounded honesty regression (a sub-0.1 jitter shown as a move) + the error-disguised-as-empty defect (added an error branch mirroring DecisionsPanel). RLS clean. Gate: tsc 0 (my files) / eslint 0 / 519 tests. Build red = same pre-existing Stripe-dep baseline. Follow-up MOAT-VIS-b (logged): delete the now-orphaned `MemoryPanel.tsx` + extract shared `whenOf`/`VERDICT_TONE`. |

| 2026-06-21 05:10 | L2-3 | H1-TASKS task-graph consumption (#7 register row) | shipped ◐ | Makes the stored PRD task DAG (seq+depends_on, written by generateTaskGraph) READABLE. New PURE `src/lib/task-graph.ts` (sanitizeDependsOn/validateTaskGraph DFS-integrity/topoOrder Kahn-total/readyTasks/summarizeTaskGraph; 13 tests) + read-only RLS-scoped `getTaskGraph` (mirrors listTasks) + a calm `TaskGraphPanel` "Build readiness" readout (done/ready/blocked/%/remaining-hours + DAG integrity) on `/prds/$id`. **2-lens adversarial-review Workflow** (topology/RLS · reuse/wiring/honesty) → 0 blockers, SHIP_WITH_FIXES; folded the stale-panel cache-invalidation defect (panel didn't refresh on Generate) + a role-color fix (--rose for failures) + a self-dep double-report. Gate: tsc 0 (my files) / eslint 0 / 551 tests. Build red = same pre-existing Stripe-dep baseline. Deferred (lane boundary): the task-graph→mission-steps bridge needs studio.* (Build lane). Scoped built-vs-pending via an Explore agent first. |

| 2026-06-21 05:25 | L2-dry | board dry - long-polling | noted | Shipped #1 DBR-1.5, #2 MOAT-VIS, #7 H1-TASKS this session. Full register re-scanned: every eligible row is blocked for an autonomous lane-safe build - #5/#26 chokepoint (attended), #24/#28/#29-32 design/IA/horizon (founder-gated), #4/#6/#11 claimed by lane 1 (Q1-MCP in flight), #3 IA placement, #8/#34/#48 publish-verify-only, #10 blocked on Q1, #38 founder-gated AI spend, #45/#46 outward/connector-gated, rest Gated/Lovable/Deferred. Long-polling per §15.6 (ScheduleWakeup ~25 min); a released claim / new row / founder push reactivates. NEVER hard-stopping. |

## Pending published-app verification
| Date | Item | Class | Note |
| --- | --- | --- | --- |
| 2026-06-21 | H1-TASKS Build-readiness panel | UI render | `/prds/$id`: generate a task graph, then the "Build readiness" panel shows N-of-M done / ready / blocked / %, the ready-now seqs, and any DAG integrity issues; regenerating refreshes the panel. Verify on publish with a real generated graph. |
| 2026-06-21 | MOAT-VIS Brain panel + Today card | UI render | `/knowledge?tab=learnings` lands on the CompoundingPanel (aggregate line + rescore feed; each row drills to `?learning=`); `/` (Today) "what changed" shows the latest cause + the aggregate compounding line once >1 outcome is recorded. Verify on publish with seeded learnings. |
| 2026-06-20 | O1 List view (tree) | UI render | `/knowledge?tab=graph` → List: focus a node and the downstream lineage tree renders (expand/collapse), stats compute. Verify on publish. |
| 2026-06-20 | O1 Graph view (visual) | UI + read fn | `/knowledge?tab=graph` → Graph: the bounded visual graph renders on real data, node click traces lineage + "Center the graph here" recenters, the "as of" filter scrubs by date. Verify on publish. |
| 2026-06-21 | DBR-1.5 migration apply | migration | `BEGIN; <20260621030000 alter>; ROLLBACK;` on prod confirms the 3 additive nullable columns apply cleanly (additive + `IF NOT EXISTS` → safe by construction; verify when DB access is available). |
| 2026-06-21 | DBR-1.5 live edge-write | write path + AI | With `DECISION_BRAIN_SUPERSESSION=1`: record an outcome that conflicts a prior decision → a typed `supersedes`/`contradicts` edge appears in `artifact_lineage` (and renders on `/knowledge?tab=graph`); re-record → the edge stays current (`valid_to IS NULL`), no zombie. Dormant-with-no-data until the founder flips the flag. |
