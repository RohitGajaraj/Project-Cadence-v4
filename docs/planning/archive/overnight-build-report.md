# Overnight Build Report (live)

> **SSOT first.** The single front-door tracker is [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) (status, build queue, founder rulings, findings, progress). This file is the dated cycle-by-cycle autonomous-loop run log it points to, not the tracker to follow day-to-day.

> Live status of the autonomous overnight build run. Rewritten every cycle by the loop. The rules behind it: [`../operations/autonomous-build-loop.md`](../operations/autonomous-build-loop.md).
>
> **How to check:** `git pull`, then read this file. Second view: `git log --oneline` for the commit trail.

**Last updated:** 2026-06-20 21:21 (RUN RESUMED, founder-directed **FRONT-END theme**: wire the shipped WM workspace/settings/invite backends to the UI, resuming the 2026-06-20 cycle-51 pivot. **Cycle 0 (docs):** codified the **frontend build protocol** - every FE build uses the FULL design-skill toolkit, not `impeccable` alone (`design-context.md` "The frontend build protocol" + `AGENTS.md` §3.0d + loop playbook §4; founder ruling: the brand reference files are INTENT, standard practice + the design skills win on technique). Planned queue: Workspace Members surface (list/remove/transfer-ownership, wires WM-F4 + the members gap) -> move-product UI (WM-F6) -> change-member-role -> invite redirect-after-login. Prior: 2026-06-20 20:21 (cycle 57, RESILIENCE theme: shipped **MODEL-REGISTRY-DEPRECATION** to `main` (◐, dormant playbook) - the model-deprecation route-around that completes `considerations.md` #4 alongside cycle 56's PROVIDER-FALLBACK. Added `deprecated`/`replacement`/`sunset` to the `Model` type + a pure cycle-safe `activeModelId` resolver (8 tests) wired before cost-routing in both chokepoint functions; identity function today (no model flagged = byte-identical), activates when a provider sunset is recorded. tsc 0 / build ✓ / 357 tests / eslint 0. NEW dashboard row 151. Next: the founder-flagged Decision Brain **supersession engine** in the AI lane. Prior: 2026-06-20 19:52 (cycle 56, RESILIENCE theme: shipped **PROVIDER-FALLBACK** to `main` (◐) - an ordered fallback chain at the AI chokepoint (`considerations.md` #4). A pure `resolveFallbackChain` (new `fallback.ts`, 10 tests) replaces the single `fallbackModel` in both `callModel` + `callModelStream`; a flag-gated (`AI_PROVIDER_FALLBACK`, default OFF) auto cross-model degrade to `cheapestLiveModel()` excludes eval+embed; happy path untouched, byte-identical off. Dedicated adversarial review (chokepoint blast-radius) confirmed 7 constraints, folded 2 findings. tsc 0 / build ✓ / 332 tests / eslint 0; ◐ live-degrade on the flag + publish. Built now per founder ruling 1 (don't park risky-but-buildable work). NEW dashboard row 150. Landed after rebasing past 2 parallel pushes (KI-16b + a mission fix; no code overlap). Prior: 2026-06-20 19:14 (cycle 55, run RESUMED, DECISION BRAIN theme: shipped **Ambient Precedent** (Decision Brain increment 1, the founder's topmost priority) to `main` (◐), subagent-driven via `superpowers:subagent-driven-development`, all 6 tasks T1 to T6: persist outcome titles -> a semantic precedent engine + pure ranker (✅ unit-verified) -> the Critic rewired to it (DBR-0 recency deleted) -> a `getDecisionPrecedent` server fn -> a reusable `PrecedentNudge` UI primitive -> the nudge wired into the opportunity + spec seams (3 seams now live: Critic + opportunity + spec). Reviewer subagents on the runtime-fatal-risk tasks T2/T3 + an opus whole-branch review ("ready to merge"). tsc 0 / build ✓ / `bun test` 322/322 / eslint 0 on all 9 touched files; landed in two FF halves (P0 then P1, P1 rebased clean past a parallel O1/incidents push). ◐ renders on the founder's next publish (the 3 seams read live embedded outcome data). Commits `4628526425`..`bf4a83a084`. Detail: [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) §6 + [`../../../plan.md`](../../../plan.md) §4. Prior: cycle 54, FRONT-END theme: wired WM-F5's **accept route** (commit `8332f31488`) - a standalone `/join/$token` page (`src/routes/join.$token.tsx`, `ssr:false`, not under the auth shell so a logged-out invitee gets a clear prompt not a token-losing bounce) that redeems an invite via `acceptInvitation` for a logged-in invitee (single-run `ran.current` guard since accept is single-use + StrictMode double-invokes), completing the invite→accept loop. Ran `impeccable` (passed the bans, moss success state per the role-color law). tsc 0 / eslint 0 / build ✓ (3.09s, routeTree regenerated) / 307 tests; ◐ renders on publish; WM-F5 row 54 [~85%]→[~92%]. **RUN PAUSED FOR THE DAY (founder, 2026-06-20): all work committed + pushed to `main`, worktree clean (0/0); resume tomorrow, worktree left in place.** Cycle 53, FRONT-END theme: wired WM-F5's **invite UI** (commit `a099afe20f`, rebased over the cockpit lane's P7). New `TeamCard` ("Invite teammates") in Settings > Workspace: invite by email + role, copy the join link (outbound email is a no-op), see invitations + their status, revoke a pending one (`inviteMember`/`listInvitations`/`revokeInvitation`). Ran `impeccable` (founder ruling - passed the bans, folded a header-accuracy fix). tsc 0 / eslint 0 / build ✓ / 305 tests; ◐ renders on publish (WM-F5 migration pre-publish); WM-F5 row 54 [~70%]→[~85%]. P7 (cost incidents, cockpit lane) flipped to ✅, so the merged tally is ✅63 / ◐35 / ⬜36, strict 42.9% / weighted 60.4%. Cycle 52, FRONT-END theme cont'd: wired U6's **export-history view** (commit `be19585a23`). The `DataExportCard` now shows a "Recent exports" list (date+time · kind · row count) read from `listExportLog` (U6-AUDIT c48), refetched after each export; integrated into the one card per `impeccable`'s fewer-cards law; ran the design skill (passed the bans, folded date+time so same-day exports are distinguishable). tsc 0 / eslint 0 / build ✓ (3.31s) / 305 tests; ◐ renders on the founder's publish; advances U6 row 36 [~95%]→[~98%]. Cycle 51, **FRONT-END PIVOT - founder ruling:** stop adding backend, start stitching UI onto the shipped backends so they are visible in the app. First build (commit `39aa07ad89`, rebased over the cockpit lane's R3-PREFS): a calm-front `SubprocessorsCard` ("Where your data goes") in Settings > Data wiring SUBPROC-DISCLOSURE's `getSubprocessors`, built on the Ember system; **ran the `impeccable` design skill** per a second founder ruling ("always use the design skills/agents on front-end work"), passed the absolute bans + folded two craft refinements; tsc/eslint/build green; ◐ renders on the founder's publish (not render-verified locally). A founder message about a Lovable/Cursor-style "CLI" vs "Ask" was retracted and disregarded. Merged tally: ✅62 / ◐36 / ⬜36, strict 42.2% / weighted 60.0%. Next front-end picks: the U6 export-history view, then the WM workspace/team surfaces. Cycle 50: BUILT APP-HEALTH (a public app-level health/readiness endpoint, the SRE-lens P0 gap in `considerations.md`, commit `323c057e0e`). New route `src/routes/api/public/health.ts` (`GET /api/public/health`): a cheap, timeout-bounded (2s) DB reachability probe (`profiles` head-read, verified a real table) → HTTP 200 `{status:"ok"}` healthy or 503 `{status:"degraded"}` when a dependency is down, so a monitor/LB reacts on the STATUS CODE without parsing the body. Public + unauthenticated, designed not to be an attack surface (leaks nothing beyond ok/error states; probe is cheap + timeout-bounded = DoS-amplification guard). Pure `src/lib/app-health.ts` (`assembleHealth`) unit-tested. tsc 0 / eslint 0 / build ✓ (3.68s, routeTree regenerated) / 303 tests (5 new). ◐ - the live DB-probe was not run unattended (verify with `curl` on publish), and the alerting half + a status page are infra/founder + design-pass. **Pick:** the chokepoint bundle stays attended + the Data/Privacy vein is well-mined (3 consecutive), so per `considerations.md`'s P0-first rule the cleanest greenfield item was app monitoring (`health.functions.ts` is only a migration-drift checker). Cycle 49: BUILT SUBPROC-DISCLOSURE (sub-processor disclosure registry, the Data/Privacy P1 enterprise-review gap from `considerations.md`, commit `7cc35423c4`). **Cycle-49 finding:** the top remaining queue items are now ONE attended chokepoint bundle - PROVIDER-FALLBACK + PLAN-ENFORCE both restructure `runtime.server.ts`, and MODEL-REGISTRY-DEPRECATION (the queue named its domain as `models.ts`) has its only universal live drive-point at the chokepoint too, since `routeModel` is a DEAD export with zero consumers and `costRoutedModel` is gated-off + surface-narrow, so a deprecation route-around is staged-but-never-driven unless wired at `runtime.server.ts:1063`. Picked the cleanest non-chokepoint, non-parallel-lane, fully-verifiable item in this lane's Data/Privacy strength: a pure `src/lib/compliance/subprocessors.ts` registry that DERIVES the AI-model sub-processors from the live catalog (a provider is `active` only when it has a `live` model; adapter-ready BYO listed inactive; `ollama` excluded as self-hosted) + a curated infra list (Lovable gateway / Supabase / Cloudflare) + a `getSubprocessors` GET read fn; `PROVIDER_META: Record<Model['provider'],…>` makes tsc FAIL if a catalog provider lacks a disclosure entry. tsc 0 / eslint 0 / build ✓ (3.11s) / 298 tests (11 new); NO DB access so no dry-run. ◐ - trust-page UI + legal copy/regions/DPA deferred to the design/legal pass; completes the data-governance triad with DATA-RETENTION + U6-AUDIT. Cycle 48: BUILT U6-AUDIT (export audit log — the remaining ~15% of U6). The two highest-impact remaining items (PROVIDER-FALLBACK, WM-M9) both touch the AI chokepoint and can't be behaviorally verified unattended, so both were QUEUED for an attended cycle; picked the cleanest fully-verifiable my-lane item. Migration `20260620010000` adds an APPEND-ONLY `export_log` table (insert+select RLS only — no update/delete, so tamper-evident; `is_workspace_member`-gated read so an owner audits all egress); `exportProduct`/`exportWorkspace` write a best-effort (try/catch) audit row per export; new `listExportLog` read fn. tsc 0 / eslint 0 / build ✓ / 287 tests, AND a BEGIN..ROLLBACK prod dry-run: both insert shapes apply, user_id defaults from the JWT, kind CHECK holds, policy set is append-only (0 update/delete). ◐ — U6's export-history UI deferred to the design pass. Cycle 47: BUILT DATA-RETENTION (AI-telemetry retention purge). With the WM clean-backend lane thinning to WM-M9 (a chokepoint capability-removal best sequenced with the founder-gated pricing surfaces per its own spec), skip-and-queued WM-M9 and cross-referenced `considerations.md` for the next clean P1 gap: `ai_events` (+ prompt_runs/tool_calls) grow unbounded — a cost/scale + GDPR data-minimization gap. Migration `20260620000000` adds a dormant `data_retention_enabled()` flag + a 30-day-floored, service-role-only SECURITY DEFINER `purge_old_telemetry` fn (deletes the 3 telemetry tables older than 180d) + a `retention-tick` hook. Verified live schema first (token_usage absent; no inbound FKs). tsc 0 / eslint 0 / build ✓ / 287 tests, AND a BEGIN..ROLLBACK prod dry-run (backdating real rows): dormant→no-op, active purge deletes the 200d row + keeps the 50d, floor holds at 30d. ◐ — dormant until the founder flips the flag + wires the cron; follow-up DATA-RETENTION-b = the right-to-be-forgotten erase cascade. Cycle 46: BUILT WM-F6 move product between workspaces — a transactional SECURITY DEFINER `move_product` RPC (`20260619270000`) + `moveProduct` server fn. Verified the LIVE prod schema first (Lovable MCP): the spec's "harden product RLS first" was already satisfied (reads key on `is_workspace_member(workspace_id)`), so reassigning `workspace_id` IS the move. The RPC reassigns the product + 13 product-scoped child/grandchild tables (untyped-client trap handled: FK is `project_id` on 8 tables, `product_id` on 4), guarded by owner/admin-in-both (WM-F3) + same-account (WM-M2). Behaviorally dry-run-verified on prod (BEGIN..ROLLBACK): a real product's children ALL moved with 0 orphans, cross-account + non-manager both blocked. tsc 0 / eslint 0 / build ✓ / 274 tests. ◐ — UI deferred to the design pass; live move on publish; follow-up WM-F6b scopes generic-provenance (`artifact_lineage`). Cycle 45: BUILT WM-F8 workspace switch hardening — `setActiveWorkspaceId` (`src/hooks/use-workspace.tsx`) now clears every workspace-scoped TanStack Query cache entry on a real switch (`queryClient.removeQueries({predicate})`, active + inactive) so no previous-workspace data flashes (the spec's "reads like a leak"); a pure unit-tested allowlist module (`src/hooks/workspace-query-scope.ts` + `.test.ts`) preserves the user/account-global queries. Chose the single switch-site reset over threading the workspace id into ~40 query keys (per-key work → follow-up WM-F8b); `removeQueries` not `invalidate` (invalidate keeps stale data visible during refetch). Driven by the AppShell switcher. tsc 0 / eslint 0 (touched) / build ✓ / `bun test` 274/274. ◐ not ✅: correct + gate-green + driven, but full no-flash payoff scales with server-side workspace read-filtering (WM-F9b) and the two-workspace Playwright check needs a publish. Cycle 44: BUILT WM-F5 workspace invitations BACKEND — a `workspace_invitations` table + a SECURITY DEFINER `accept_workspace_invitation` RPC (token validated, email-bound, single-use, 7-day expiry) + a no-op `email.server.ts` (link fallback; outbound founder-gated) + invite/list/revoke/accept server fns (`d0e7300c33`); behaviorally dry-run-verified on prod (invite→accept adds the member with the invited role; re-accept/wrong-email/expired all rejected); join route + Settings UI deferred to the design pass per the founder ruling; ◐ live on publish. Cycle 43: BUILT WM-F4 workspace ownership transfer — a transactional, audited SECURITY DEFINER `transfer_workspace_ownership` RPC + a `workspace_audit_log` table + a `transferWorkspaceOwnership` server fn (`345276a5aa`); behaviorally dry-run-verified on prod (transfer flips owner_id + member roles new→owner/old→admin + writes an audit row; non-owner blocked; non-member blocked); the Settings UI affordance is deferred to a follow-up; ◐ live on publish. First BUILD since the WM lane went collision-free. Cycle 42: de-risk sweep concluded — independently behaviorally RE-VERIFIED the WM-M12 credit-debit money-mover CLEAN on prod (correct INCLUDED→TOPUP draw, the floor prevents overdraw, no-op when empty, correct ledger deltas), confirming the credit money-path is sound (it was already verified by its builder + is correct). The 3 broken parallel ships (WM-M15b/F2/F3) are fixed; the lane-coordination commit (`0ce0524604`) now confines the parallel lanes (cockpit/knowledge/safety) away from the WM/credit/chokepoint files, so the WM lane builds collision-free from here. No code change this cycle. Next: build WM-F4 (ownership transfer). Cycle 41: WM-F3-fix corrected the shipped RBAC migration, which used `create policy if not exists` (invalid Postgres syntax, `ERROR 42601`) and would have FAILED to apply on the founder's next publish, blocking the whole migration run; fixed to drop-then-create + an idempotent trigger, `70c0ca7db5`, dry-run-verified on prod (applies cleanly; rolled back, prod's real policy restored) + owner-lockout data-checked (0 of 7 owners locked out). Found by an adversarial review of recently-shipped SECURITY DEFINER/RLS migrations, prompted by the prior collisions; this is the 3rd broken parallel ship caught + fixed this session. Cycle 40: WM-F2-fix restored the cross-user-read + expiry + global-scope + return-shape invariants the parallel session's cycle-40 WM-F2 ship regressed off the WM-F1 baseline, and made account-memory pooling actually DRIVEN (it shipped as a dormant `opts.accountId` no caller passed), fix `a368edf78a`, behaviorally dry-run-verified on prod (paid account pools across both workspaces; a different account recalls nothing). Cycle 39: WM-M15b-fix corrected the cycle-38 response cache's 3 runtime-fatal bugs [the migration's now()-in-index-predicate would have FAILED to apply on the next publish per ERROR 42P17; the cache hit never skipped the provider call so the cache was a no-op; readCache passed a literal "now()" string Postgres cannot cast], fix `7897085004`, both the broken-fails and fixed-applies states dry-run-verified on prod. Cycle 38: WM-M15b response cache shipped (◐), a content-hash cache for repeated calls + chokepoint read/write + 7-day TTL table; conservative surface whitelist (brief/scheduler/test) + guardrails required to avoid stale output; live on publish. Cycle 37: WM-F1b agent-workspace isolation hardening: a `user_id`-fed BEFORE-INSERT trigger for universal insert tagging + dual-key membership RLS on all 5 agent tables, behaviorally dry-run-verified on prod (owner reads all 23 memory rows, no breakage), live on publish; NOT NULL + roster-key + types-regen split to WM-F1c) · **Governance (2026-06-19, founder ruling):** codified the velocity ruling (ship fast; batch deferrable quality passes, authored-content humanization scan + lint/prettier style + AI-trace + prose-polish + design, to a founder-prompted pre-launch stage; per-cycle gate stays correctness-only) into AGENTS.md §3 + CLAUDE.md + the loop playbook §5 + SSOT §1 + humanized-output.md + session-decisions. **Parent-repo sync pending:** the founder's local `main` checkout is behind/diverged; run `git fetch && git reset --hard origin/main` in the repo root to reflect all of this there (the loop cannot run `reset --hard`, it is denylisted). · **Maintainer:** the autonomous loop, every cycle. Entries are dated so multiple nights stay legible. **Front door is now [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md); this report is the dated run log.**

> **⚠ Reconciliation (2026-06-19 14:45, cycle 27) — `main` had diverged; resolved per founder ruling.** A PARALLEL session had also built WM-F1 and committed it to LOCAL `main` only (never pushed): a second WM-F1 (`d1be22b665`, with a likely-fatal column-default apply error + an unconditional `is_workspace_member` that silently dark-recalls the service-role loop), its docs (`b95aa76543`), and a "Lovable MCP as first checkpoint" docs commit (`892d73950c`). My origin-collision check passed legitimately (origin/main never had WM-F1). **Founder chose "keep mine + save your docs":** `origin/main` now = `f08453c76c` (my verified WM-F1 `b18995a32a` + the cherry-picked Lovable-MCP docs). The parallel work is preserved at branch **`backup/parallel-wm-f1`**. **Founder action pending:** local `main` (parent repo) is still at `892d73950c` (diverged) with a trivial uncommitted prettier reformat of the discarded parallel `loop.server.ts`; sync it with `git fetch && git reset --hard origin/main` when ready (`reset --hard` is on the loop's denylist, so the loop cannot run it). The fuller scope from the parallel version (NOT NULL + RLS swap + roster key) is folded, FIXED, into WM-F1b.

---

## Run header

| Field | Value |
| --- | --- |
| Status | RUNNING (2026-06-20 21:21, founder-directed FRONT-END theme - wiring the shipped WM workspace/settings/invite backends to the UI; cycle 0 codified the frontend build protocol: every FE build uses the full design-skill toolkit). Prior: PAUSED FOR THE DAY (founder, 2026-06-20 - all work committed + pushed to `main`, worktree clean (0/0) and left in place. Cycle 54, FRONT-END theme: wired WM-F5's accept route - a standalone `/join/$token` page redeems an invite via `acceptInvitation` (single-run guarded), completing the invite→accept loop, commit `8332f31488`; ran `impeccable`, tsc/eslint/build/307-tests green, ◐ renders on publish; WM-F5 row 54 → ~92%. Cycle 53, FRONT-END theme: wired WM-F5's invite UI - a `TeamCard` ("Invite teammates") in Settings > Workspace (invite by email + role, copy join link, list + revoke invitations), commit `a099afe20f`; ran `impeccable` (founder ruling), tsc/eslint/build/305-tests green, ◐ renders on publish; rebased clean over the cockpit lane's P7 (cost incidents → ✅); WM-F5 row 54 → ~85%. Cycle 52, FRONT-END theme: wired U6's export-history view - the `DataExportCard` now shows a "Recent exports" list via `listExportLog`, refetched after each export, commit `be19585a23`; ran `impeccable` (founder ruling), tsc/eslint/build/305-tests green, ◐ renders on publish; U6 row 36 → ~98%. Cycle 51, FRONT-END PIVOT per founder ruling: began stitching UI onto the shipped backends. First build: a calm-front `SubprocessorsCard` ("Where your data goes") in Settings > Data wiring SUBPROC-DISCLOSURE's `getSubprocessors`, commit `39aa07ad89`; ran the `impeccable` design skill (2nd founder ruling: always use the design skills on front-end work; codified in design-context.md §2); tsc/eslint/build green, ◐ renders on publish; rebased clean over the cockpit lane's R3-PREFS. Cycle 50: BUILT APP-HEALTH (public app health/readiness endpoint `GET /api/public/health`, Reliability P0): a cheap, timeout-bounded, leak-free DB probe → 200 healthy / 503 degraded for uptime monitors + LBs (route `src/routes/api/public/health.ts` + pure `src/lib/app-health.ts`), commit `323c057e0e`, tsc/eslint/build/303-tests green, ◐ - live DB-probe + alerting + status page deferred; picked per `considerations.md`'s P0-first rule once the chokepoint bundle stayed attended + the Data/Privacy vein was well-mined. Cycle 49: BUILT SUBPROC-DISCLOSURE (sub-processor disclosure registry, Data/Privacy P1): a pure `src/lib/compliance/subprocessors.ts` registry DERIVED from the live model catalog + a `getSubprocessors` read fn, commit `7cc35423c4`, tsc/eslint/build/298-tests green, no DB access, ◐ - trust-page UI + legal copy deferred; completes the data-governance triad with DATA-RETENTION + U6-AUDIT. Cycle-49 finding: PROVIDER-FALLBACK + PLAN-ENFORCE + MODEL-REGISTRY-DEPRECATION are now ONE attended chokepoint bundle (MODEL-REGISTRY's only live drive-point is the chokepoint, since `routeModel` is a dead export). Cycle 48: BUILT U6-AUDIT (export audit log, the remaining ~15% of U6): an APPEND-ONLY `export_log` table (migration `20260620010000`, insert+select RLS only = tamper-evident, `is_workspace_member`-gated read) + a per-export best-effort audit-write in `exportProduct`/`exportWorkspace` + a `listExportLog` read fn, tsc/eslint/build/287-tests green + behaviorally dry-run-verified on prod (append-only policy set confirmed, user_id defaults, kind CHECK holds), ◐ — export-history UI deferred to the design pass; the chokepoint items PROVIDER-FALLBACK + WM-M9 queued for an attended cycle (can't behaviorally verify unattended). Cycle 47: BUILT DATA-RETENTION (AI-telemetry retention purge): a dormant `data_retention_enabled()` flag + a 30-day-floored, service-role-only SECURITY DEFINER `purge_old_telemetry` fn + a `retention-tick` hook (migration `20260620000000` + `src/routes/api/public/hooks/retention-tick.ts`), tsc/eslint/build/287-tests green + behaviorally dry-run-verified on prod (dormant no-op, active purge deletes old + keeps recent, 30d floor holds), ◐ — dormant until the founder flips the flag + wires the cron; WM-M9 (remove self-serve BYOK) skip-and-queued (chokepoint capability-removal, best sequenced with the founder-gated pricing surfaces). Cycle 46: BUILT WM-F6 move product between workspaces (transactional `move_product` RPC + `moveProduct` server fn; reassigns the product + 13 product-scoped child/grandchild tables' `workspace_id`, owner/admin-in-both + same-account guard), tsc/eslint/build/274-tests green + behaviorally dry-run-verified on prod (all children move, 0 orphans, both guards block), ◐ — UI deferred to the design pass, live on publish; cycle 45: BUILT WM-F8 workspace switch hardening (query-cache reset at the single switch site + a pure allowlist module, `src/hooks/use-workspace.tsx` + `workspace-query-scope.ts` + `.test.ts`), tsc/eslint/build/274-tests green, driven by the AppShell switcher, ◐ — full no-flash payoff scales with server-side workspace read-filtering (WM-F9b); cycle 44: BUILT WM-F5 invitations backend (table + email-bound single-use accept RPC + server fns + email stub, `d0e7300c33`), dry-run-verified on prod, UI deferred, ◐ live on publish; cycle 43: BUILT WM-F4 ownership transfer (RPC + audit log + server fn, `345276a5aa`), behaviorally dry-run-verified on prod, Settings UI deferred, ◐ live on publish; cycle 42: de-risk sweep concluded — WM-M12 credit money-mover behaviorally RE-VERIFIED clean on prod (no code change); the 3 broken parallel ships are fixed + the lane coordination now confines parallel lanes off the WM/credit/chokepoint files, so the WM lane builds collision-free; next builds WM-F4. Cycle 41: WM-F3-fix corrected the shipped RBAC migration's invalid `create policy if not exists` (ERROR 42601, would fail on publish) to drop-then-create, `70c0ca7db5`, dry-run-verified on prod + owner-lockout data-checked; cycle 40: WM-F2-fix restored the security + correctness invariants the parallel WM-F2 ship regressed (cross-user read, expiry, global-scope, return-shape) and made pooling driven, fix `a368edf78a`, dry-run-verified on prod; cycle 39: WM-M15b-fix corrected the cycle-38 response cache's 3 runtime-fatal bugs (broken migration now()-predicate that would fail on apply + no-op cache hit + bad readCache "now()" literal), fix `7897085004`, broken-fails and fixed-applies both dry-run-verified on prod; cycle 38: WM-M15b response cache shipped (◐); cycle 37: WM-F1b agent-workspace isolation hardening shipped (◐), a `user_id`-fed BEFORE-INSERT trigger that tags every insert path on all 5 agent tables (the `seed_default_agents` SQL fn, the `memory.remember` tool, any service-role writer) + a dual-key membership RLS replacing the owner-only policy, dry-run-verified on prod (5 triggers / 5 policies / 0 old remaining; owner reads all 23 memory rows, no breakage; `seed_default_agents` intact); NOT NULL (founder-gated moat-cascade) + roster unique-key swap (couples WM-F8) + types regen (post-publish) deferred to WM-F1c. Cycle 36: WM-M5 tier limit gates shipped (◐), account-wide product cap + workspace cap as SECURITY DEFINER BEFORE-INSERT triggers + a typed `LimitReachedError` server pre-check, dormant behind a new `limit_gates_enabled()` flag, behaviorally dry-run-verified on the live prod DB (free blocked at product #3 AND workspace #2). Cycle 35: WM-F9 tenancy isolation hardening (◐), workspace_id + dual-key membership RLS on 10 tables. Cycle 34: WM-M15 cost-aware routing (◐, config-gated); cycle 33: WM-M14 attribution + caps; cycle 32: WM-M12 debit engine). Building the founder-directed **Workspace / Accounts / Tenancy + Monetization (WM-\*)** initiative down the G10 pick-order (SSOT §0). Cycle 26 = WM-M1 (entitlements), 27 = WM-F1 core (agent memory workspace-scoping), 28 = WM-M2 core (accounts + billing relocation + credit pool shell), 29 = WM-M10 (credit unit), 30 = WM-M4 (credit seam), 31 = WM-M11 (credit grant + reset). The credit engine's metering path is now built end-to-end (M10 unit + M2 pool + M4 seam + M11 grants + M12 debit). Next: WM-M14 (attribution + caps), WM-M15 (margin levers), or WM-F1b. Earlier cycles 1-25 (loop, Engine Room, agents, K2, D4) below. |
| Mode | build + commit + push |
| Scope | whole backlog minus founder-gated |
| Blocked-item policy | skip and queue (below) |
| Isolation | worktree branch `overnight/wm`, fast-forward push to `main` |
| Started | 2026-06-17, late session |

## How this works (the contract)

This run operates under the [autonomous build loop playbook](../operations/autonomous-build-loop.md). The short version:

- **Two modes.** Manual (daytime): the founder says `pick <ID>`, one item builds, options are surfaced for the founder. Autonomous (overnight): one command, `/overnight-build`, runs the loop hands-off and self-paced.
- **Each cycle.** Pick the top buildable item (v10, then v9, then v8), plan and decide, build, gate on `tsc --noEmit` + `bun run build` + lint, adversarially self-review, run the full doc-loop, commit with a why, fast-forward push to `main`.
- **Decisions.** In autonomous mode the loop picks the best option on the data and logs the rationale; it defers only founder-gated calls (taste, spend, accounts, anything irreversible) and queues them below.
- **Safety.** "Accept edits" mode plus a command allowlist and a denylist that blocks destructive commands; an isolated worktree so it never collides with parallel sessions.
- **Resilience.** While the session window is open, build fast (no slow/idle cadence). The only long wait is a usage limit or session-window reset: read the reset time, wake just after it, and resume.
- **Continuity.** Context rolls over only at clean boundaries between builds, with a written handoff first, never mid-build.
- **Model and effort.** Opus 4.8 1M context, top effort on the hard steps, dialed down for mechanical ones.

**How to operate.** Autonomous: Shift+Tab to "accept edits on" (the command allowlist covers the rest), then `/overnight-build`. Manual: `pick <ID>`. Full detail: playbook sections 11 to 13.

## Completion snapshot (live)

Per the feature dashboard At-a-glance (the full register), as of cycle 56 (2026-06-20 19:52). The dashboard's live tally is the canonical view (the At-a-glance counts are the authority; this run did not recompute them, since parallel sessions are actively editing the tally - a reconciliation pass owns it). Cycle 56 ADDED row 150 PROVIDER-FALLBACK (◐ [~70%], Reliability), the ordered fallback chain at the AI chokepoint. Cycle 55 shipped Decision Brain increment 1 (Ambient Precedent) as 6 TASKS under row 148 (the DBR umbrella), so its at-a-glance counts are UNCHANGED (sub-row work, per the SSOT rule); the DBR row 148 increment-1 marker flipped to "shipped ◐". Cycle 54 advanced row 54 WM-F5 [~85%]→[~92%] (the /join accept route completes the invite loop); cycle 53 advanced row 54 WM-F5 [~70%]→[~85%] (invite UI wired) and rebased in the cockpit lane's P7 (cost incidents → ✅, so ✅62→63 / ◐36→35); cycle 52 advanced row 36 U6 [~95%]→[~98%] (export-history UI wired); cycle 51 advanced row 146 SUBPROC-DISCLOSURE [~60%]→[~75%] (Settings UI wired) and rebased in the cockpit lane's R3-PREFS (R3 → ✅, so ✅61→62 / ◐37→36); cycle 50 ADDED row 147 APP-HEALTH (◐ [~60%], a new Reliability category); cycle 49 ADDED row 146 SUBPROC-DISCLOSURE; cycle 47 ADDED row 145 DATA-RETENTION (◐):

| Bucket | Count |
| --- | --- |
| ✅ Done | 62 |
| ◐ Partial | 36 |
| ⏭️ Deferred / ⏸️ Paused | 13 |
| ⬜ Open (ready to pick up) | 36 |
| **Overall completion** | **strict 42% / weighted 60% (per the dashboard tally). The Monetization + Credit + BYO lanes (G10/G11) are the bulk of what is open. Credit engine metering path built end-to-end (M10 ✅ + M2/M4/M11/M12 ◐), legible (WM-M14 ◐), margin-disciplined (WM-M15 routing ◐), tier caps enforced (WM-M5 ◐), RBAC enforced (WM-F3 ◐, parallel), AND the moat now isolation-hardened on TWO fronts: WM-F9 (10 user-private tables) + WM-F1b → ◐ this cycle (all 5 agent tables: universal insert tagging via a `user_id`-fed trigger + dual-key membership RLS). All monetization is flag-gated; isolation + caps + the rest activate on publish.** |

Focus this run: close P1/P2 buildable, file-disjoint items first (v10), then mine v9 and v8 for relevant work.

## Recommendation for the founder (the loop's honest read)

**Update (cycle 16, 2026-06-18):** since the cycle-10 note below, the loop shipped, all gate-green and unattended: **D4** cancellation (c10), **F3** always-fresh feed (c11), **O1** provenance (c12), permission hardening (c13), **LCH-01** launch-kit drafting (c14), **R3** global Attention bell (c15), and **P7** guardrail-block incidents (c16). With those, the **genuinely-clean unattended backlog is now exhausted**: every remaining item is migration-bound (F3 per-product clustering, R3 prefs, U6 audit-log, P7 persistent table), visual / needs-a-publish-to-verify (O1 graph explorer, the §14 design pass, F-IA-* consolidation, L2 pages), loop/studio-coupled and better built with you watching (D4 replay/checkpoint, K2 rollback, BLD-05 gate), or founder-gated (connectors/OAuth, Stripe secrets, outbound send, the F3 auto-cluster spend). The honest high-value path from here is founder-led: **(a) publish, then I verify the night's features live** (big queue below); **(b) run the §14 design pass together**; **(c) build the risky loop-coupled items with you watching the runtime**. The loop stays alive on a re-check cadence and will pick up anything you push or any genuinely-clean item it finds; it will not force a risky or speculative build. _(Deeper recon has twice disproved an "exhausted" read, so each resume re-scans before concluding.)_

**Correction (cycle 10, 2026-06-18):** "the safe backlog is effectively complete" was premature. This cycle found and shipped **D4's cancellation slice** (a per-mission brake) safely and unattended, after deeper recon disproved the "D4 is too risky to build blind" read below (see item 2). The loop is now operating under the founder's **core-first ruling** (build core; do not idle; do not defer to founder-led) and continues; the next pick is **`F3`** (continuous discovery), the founder's named top core candidate. The older "founder-led next steps" read below is retained for history but is superseded by the core-first ruling.

What shipped earlier this run (U6 + selective, R3 Attention, P7 Incidents, C4/E7 Agent inspector) is gate-green and waiting on your publish-then-verify (table below). Two things to know:

1. **The dashboard was stale.** Picking the "next ⬜ item" kept landing on work already built: `P3` (prompt versioning + A/B + pin + rollback) and `F-HUMANIZE-HOOK` both exist but were marked ⬜. I corrected the ones I verified. (**Correction, 2026-06-18:** `L2` is NOT one of them. The public `p.$slug` route is the prototype viewer, a different feature; `L2` customer-announcement pages are genuinely unbuilt. My earlier "L2 already built" flag was wrong.) True completion is higher than the ~54% the board shows; a fuller reconciliation would confirm it.
2. **What is genuinely left needs you (revised cycle 10).** Not all of the "hard" items are equally risky. `D4`'s **cancellation slice shipped this cycle** — recon showed it needs zero loop changes (the tick already gates on status), so it is a deterministic DB-state transition the `tsc`/build/review gate fully covers; only its end-to-end UX joins the standard publish-verify queue. What genuinely remains loop/studio-coupled and is better built with you watching the runtime: `D4`'s **replay-and-branch / checkpoint-diff** remainder, `K2` (one-action rollback), `BLD-05` (inspector gate). `O1`/`SEN-04`/`LCH-01` are big integrations. The **section-14 design pass** needs your eyes (visual verification).

**Highest-value next steps are founder-led:** (a) publish, then I verify the night's features live; (b) run the design pass together; (c) build the risky items with you watching the runtime. The loop stays alive on a fast cadence and keeps doing safe work (verification, reconciliation, any genuinely-clean item it finds); it will not force risky or speculative builds.

## Done this run

| Date | Item | Lane | Commit | Notes |
| --- | --- | --- | --- | --- |
| 2026-06-20 20:21 | RESILIENCE: MODEL-REGISTRY-DEPRECATION route-around (cycle 57, ◐ dormant playbook) | Reliability | `fb82451f19` (rebased) | **Pick:** completes `considerations.md` #4 (the model-deprecation-playbook half; cycle 56 did the outage half); continued the chokepoint-resilience pattern per founder ruling 1. Chose it over PLAN-ENFORCE (overlaps existing credit/budget enforcement + edges toward founder-gated pricing). **What:** added `deprecated`/`replacement`/`sunset` to the `Model` type + a pure, cycle-safe `activeModelId(id, catalog?)` resolver (8 TDD tests via an injected catalog) that follows `deprecated -> replacement` to a LIVE terminal (intermediates may be non-live retired models), else returns the original so a deprecation never strands traffic; wired before cost-routing in both `callModel` + `callModelStream`. **Honest status:** NO model is flagged deprecated today, so it is the IDENTITY function = byte-identical; a wired-but-dormant playbook ready for the next provider sunset. The route-around LOGIC is genuinely unit-verified; prod behavior is a no-op until a model is flagged (the claim does not outrun the wiring). **Adversarial self-review (controller):** byte-identity-when-dormant confirmed; credit pre-check/byo path use the resolved `effectiveModel`; folded a comment-accuracy fix (the doc-comment described an earlier buggy version). **Verification (ran):** tsc 0, eslint 0 (3 files), build ✓, `bun test` 357/357 (8 new). Rebased past parallel mission/handoff pushes (no overlap). **◐ not ✅:** wired + logic unit-verified; no live route-around until a model is flagged (chokepoint path live-verifies on publish). UI breadcrumb: none (chokepoint). |
| 2026-06-20 19:52 | RESILIENCE: PROVIDER-FALLBACK ordered fallback chain at the AI chokepoint (cycle 56) | Reliability | `6c25946d01` (rebased) | **Pick:** `considerations.md` #4 (provider/model outage degrades, not hard-fails) + the SRE "graceful degradation" P0 - the highest-impact clean item; built now per founder ruling 1 (don't park risky-but-buildable work) rather than continuing to defer it. **What:** generalized the chokepoint's SINGLE `fallbackModel` into a pure, ordered, deduped, primary-excluded `resolveFallbackChain` (new `src/lib/ai/fallback.ts`, TDD, 10 tests) wired into both `callModel` + `callModelStream` (single-fallback `if` -> chain `for` loop). Back-compat: a lone `fallbackModel` -> a 1-element chain = today, so byte-identical when off. New flag `providerFallbackEnabled()` (`AI_PROVIDER_FALLBACK`, default OFF) optionally appends an auto cross-model degrade to `cheapestLiveModel()` (reuses the WM-M15 picker), EXCLUDING `eval` (benchmark corruption) + `embed` (chat-only). Happy path untouched (bounded blast radius). **Adversarial review (dedicated reviewer, chokepoint + cycle-39/40 history):** confirmed 7 binding constraints (byte-identical-off; eval/embed guard both paths; no self/infinite fallback; happy path untouched; cache `!fallback` write-guard + credit attribution via `modelUsed` intact; streaming non-ok handling preserved); folded 2 findings - I-1 (surface the PRIMARY error on chain exhaustion, not the last fallback's) + M-1 (a missing test). **Verification (ran):** tsc 0, eslint 0 (3 files), build ✓, `bun test` 332/332 (10 new). Rebased past 2 parallel pushes (KI-16b + a mission fix; no code overlap); never force-pushed. **◐ not ✅:** the chain + ranker are ✅ unit-verified; the live outage-degrade + auto cross-model fallback activate on the founder setting `AI_PROVIDER_FALLBACK=1` (verify on publish). UI breadcrumb: none (chokepoint resilience). |
| 2026-06-20 19:14 | DECISION BRAIN: Ambient Precedent increment 1, all 6 tasks (cycle 55) | Decision Brain (H1) | `4628526425`..`bf4a83a084` (T1-T6) | **Pick:** the founder's topmost-priority Decision Brain increment, greenlit subagent-driven. **What:** a proactive precedent nudge surfacing a workspace's own past shipped outcomes wherever a decision is made, via a semantic match over outcome memories we already store. **Executed with `superpowers:subagent-driven-development`:** a fresh implementer per task (model scaled: haiku for the verbatim T1, sonnet for engine/integration/UI); dedicated reviewer subagents on the runtime-fatal-risk tasks T2 (engine) + T3 (Critic rewire); controller review on additive T1/T4/T5/T6; an opus whole-branch review ("ready to merge"). **Shipped:** T1 persist `prd_title`/`opp_title` on outcome memories (forward-only jsonb); T2 `decision-precedent.server.ts` (pure TDD'd `rankPrecedent` + fail-safe live `loadDecisionPrecedent`); T3 Critic uses the engine, DBR-0 recency helper deleted; T4 `getDecisionPrecedent` server fn; T5 `PrecedentNudge` UI primitive (reuses `VerdictChip`, `impeccable`-passed); T6 nudge wired into the opportunity `?opp=` drill + the spec detail page (3 seams live). **Runtime-fatal review:** confirmed the engine is DRIVEN (5 `runCritic` call sites + both seams render) and the untyped `match_agent_memory` slug filter surfaces outcome rows (scope='global') regardless of slug, verified against the live RPC SQL. **Verification (ran):** tsc 0, build ✓, `bun test` 322/322 (3 new), eslint 0 on all 9 touched files. Landed in two FF halves (P0 then P1, P1 rebased clean past a parallel O1/incidents push). **◐ not ✅:** engine + ranker ✅ unit-verified; the 3 seams render on the founder's next publish (read live embedded outcome data; need seeded outcomes to verify visually). **As-built deltas:** threshold ships at a conservative `0.3` (tune live, not the spec's `~0.72`); `excludeId` plumbed but not passed in v1; no account-pooling for precedent in v1. UI breadcrumbs: an opportunity drill, a spec detail page, the Critic verdict sheet. |
| 2026-06-20 04:55 | FRONT-END: /join invite-accept route (WM-F5) (cycle 54) | Front-end / Workspace | `8332f31488` | **Pick:** complete the invite loop cycle 53 started - the accept side (over a members list, since `profiles` has only `display_name`, no email). **What:** new STANDALONE route `src/routes/join.$token.tsx` (`/join/$token`, `ssr:false`, NOT under the auth shell so a logged-out invitee gets a clear prompt instead of a token-losing bounce). On mount checks `supabase.auth.getUser()`: signed in → redeem via `acceptInvitation` (email-bound single-use definer RPC) → "You are in" (moss success badge) + "Go to Cadence"; signed out → "You have a workspace invitation" + Log in / Sign up; errors render the RPC's clean message. **Key correctness:** a `ran.current` ref guard runs accept ONCE (single-use + not idempotent + StrictMode double-invokes), a `cancelled` flag avoids post-unmount setState. **Design-skill pass (founder ruling):** ran `impeccable` - passes the bans (one centered `.bento`, no side-stripe/gradient/glass/hero-metric/modal), correct role-color (moss = done), `animate-spin` per convention, no em dashes. **Verification (ran):** build ✓ (3.09s, regenerated `src/routeTree.gen.ts`), tsc 0 (typed `Link`s confirm /login, /signup, / exist), eslint 0 (touched), `bun test` 307/307, unicode-clean. **Adversarial review:** no runtime-fatal bug - single-run guard prevents double-accept, getUser failure degrades to the login prompt, the route is the DRIVEN accept-side of cycle 53's invite links (complete loop). **◐ not ✅:** wired + gate-green + driven + design-passed + runtime-safe, but NOT render-verified locally (needs a real token + a logged-in invited user + the migration); live on publish. Known limit (deferred): no redirect-after-login for a logged-out invitee. UI breadcrumb: invitee opens /join/<token>. |
| 2026-06-20 04:15 | FRONT-END: "Invite teammates" (WM-F5 UI) (cycle 53) | Front-end / Workspace | `a099afe20f` | **Pick:** the WM workspace/team vein - the INVITES slice first (fully usable with shipped fns, no backend change; invitations carry the email, a members list would need a profiles join, deferred). **What:** new `src/components/settings/TeamCard.tsx` ("Invite teammates") in the Settings > Workspace tab, wiring WM-F5: `useQuery(listInvitations)` + `useMutation(inviteMember)` + `useMutation(revokeInvitation)`; an email + role form, a copyable join link (outbound email is a no-op), an invitations list (email · role · status) with Revoke on pending; refetches after invite/revoke; self-contained via `useWorkspace()`; calm loading/empty states. **Design-skill pass (founder ruling):** ran `impeccable` - passed the absolute bans (one `.bento`, full-width hairlines not side-stripes, the link box is a framed sunken block not glass, no hero-metric/gradient/modal), Restrained color + one `btn-primary` accent; folded one correctness fix (`listInvitations` returns ALL statuses, so "Pending invitations" → "Invitations"). **Verification (ran):** tsc 0, eslint 0 (touched), build ✓ (3.92s), `bun test` 305/305, unicode-clean. **Adversarial review:** no runtime-fatal bug - null-workspace guarded (`enabled` + disabled button), safe optional chaining, typed fields, a pre-migration read falls through to the empty state (write shows an error toast, not a crash). **Collision handled:** origin advanced with the cockpit lane's P7 (cost incidents, `2e7abce8ee` + a docs commit; file-disjoint from my settings work); rebased clean, recomputed the merged tally (P7 → ✅: ✅62→63 / ◐36→35). **◐ not ✅:** wired + gate-green + driven (rendered in WorkspaceTab) + design-passed, but NOT render-verified locally, and the WM-F5 migration is pre-publish so it fully activates on the founder's publish. **Deferred:** the `/join/$token` accept route + a members list. UI breadcrumb: Settings > Workspace > Invite teammates. |
| 2026-06-20 03:35 | FRONT-END: U6 export-history view (cycle 52) | Front-end / Data | `be19585a23` | **Pick:** the next front-end item per the cycle-51 plan - make U6-AUDIT's export log visible. **What:** integrated the export history INTO the existing `DataExportCard` (one cohesive "data export" card, not a 3rd card, per `impeccable`'s fewer-cards law): a "Recent exports" subsection reads `listExportLog` via `useQuery` (queryKey `["export-log"]`) and renders an editorial list (date+time left, kind + row-count right, hairline-separated); a successful export `invalidateQueries(["export-log"])` so the new entry appears at once; calm loading/empty states. **Design-skill pass (founder ruling):** ran `impeccable` - passed the absolute bans (editorial list not a card grid, full-width hairlines not side-stripes, no hero-metric/gradient/glass/modal) + the Restrained color law; folded one refinement (date+time not date-only, so two same-day exports are distinguishable). **Verification (ran):** tsc 0, eslint 0 (touched), build ✓ (3.31s), `bun test` 305/305, unicode-clean. **Adversarial review:** no runtime-fatal bug - `created_at` NOT NULL (valid Date), `KIND_LABEL[e.kind]` covers the union, safe optional chaining, a pre-migration/error state falls through to the empty state (no crash). **◐ not ✅:** wired + gate-green + driven (rendered in the Settings data tab) + design-passed, but NOT render-verified locally, and the `export_log` migration is pre-publish so the list shows the empty state until the founder publishes + exports happen. UI breadcrumb: Settings > Data > Export your data > Recent exports. |
| 2026-06-20 02:55 | FRONT-END PIVOT: "Where your data goes" in Settings (cycle 51) | Front-end / Data | `39aa07ad89` | **Founder ruling (mid-session):** stop adding backend, stitch UI onto the shipped backends so they are visible in the app ("as a user, I can see it"). I was at zero code on the next backend pick (a full-account GDPR export, only reconnaissance), so switched immediately per "switch now if not deep". (A second founder message about a Lovable/Cursor-style "CLI" vs "Ask" was RETRACTED - disregarded.) Loaded the design conventions (engine-room-doctrine -> design-context -> home-and-today-ia); per the doctrine + IA rubric these enterprise/trust backends live in **Settings** (calm, outcome-named), not the front. **First build:** new `src/components/settings/SubprocessorsCard.tsx` ("Where your data goes") reads SUBPROC-DISCLOSURE's `getSubprocessors` (useQuery + useServerFn) and renders a plain editorial list (name + a mono category tag + purpose + "Receives: ..."), in the Ember `.bento`/`.mono-label` system with loading/error/empty states; added to the Settings `data` tab beside `DataExportCard`. **Design-skill pass (2nd founder ruling: always use the design skills/agents on front-end work):** ran the `impeccable` skill - PASSED the absolute bans (editorial list not a card grid, full-width hairlines not side-stripes, no hero-metric/gradient/glass/modal) + the Restrained color law, and folded two craft refinements (name-left / category-right scan; more generous whitespace); codified the ruling in `design-context.md` §2. **Verification (ran):** tsc 0, eslint 0 (touched), build ✓ (3.52s on the merged tree), unicode clean. **Collision handled:** origin advanced with the cockpit lane's R3-PREFS (`f8bfdaea53`, notifications - file-disjoint from my settings work); rebased clean, resolved the one SOURCE-OF-TRUTH stamp conflict (kept both clauses), recomputed the merged dashboard tally (R3 flipped to ✅: ✅62 / ◐36). **◐ not ✅:** wired + gate-green + adversarially-reviewed runtime-safe + genuinely driven (rendered in the Settings `data` tab), but NOT render-verified locally (no dev-server / login run) - it renders on the founder's publish. Offered a local dev-server screenshot if the founder wants to preview before publishing. UI breadcrumb: Settings > Data > Where your data goes. |
| 2026-06-20 02:10 | APP-HEALTH: public app health/readiness endpoint (cycle 50) | P0 Reliability | `323c057e0e` | **Pick:** the high-impact remainder is the attended chokepoint bundle (PROVIDER-FALLBACK / PLAN-ENFORCE / MODEL-REGISTRY-DEPRECATION) + founder-gated work, and the Data/Privacy vein this lane just mined 3x is near-exhausted, so per `considerations.md`'s explicit P0-first precedence I picked the highest-priority clean, greenfield (zero blast radius), non-chokepoint, non-parallel-lane, fully-verifiable gap: the SRE-lens P0 "App-level monitoring + alerting", confirmed unbuilt (`health.functions.ts` is only a migration-DRIFT checker, SSOT finding 4). **What:** new route `src/routes/api/public/health.ts` (`GET /api/public/health`) runs a cheap, timeout-bounded (2s) DB reachability probe (`profiles` head-read) → HTTP 200 `{status:"ok"}` healthy or 503 `{status:"degraded"}` so an uptime monitor / load balancer reacts on the status code; reports per-check states (`worker`, `database`). New pure `src/lib/app-health.ts` (`assembleHealth(checks, nowIso)` → `{body, httpStatus}`). **Design (guardrails):** public + unauthenticated (monitors don't auth) but built not to be an attack surface - leaks NOTHING beyond ok/error states (a test asserts the exact response key set), the probe is trivially cheap + timeout-bounded (DoS-amplification guard), and the HTTP status code carries the signal so a LB need not parse the body. **Verification (ran):** `bun run build` ✓ (3.68s, regenerated `src/routeTree.gen.ts`), tsc 0, eslint 0 (3 new files, after a prettier auto-fix), `bun test src/lib/app-health.test.ts` 5/5 (ok→200, db-down→503, worker-always-ok, no-leak exact-key-set, purity), full `bun test` 303/303. Adversarial review: no runtime-fatal bug - **verified `profiles` is a REAL table** (queried in onboarding/connections/calendar, the untyped-client trap), the probe is timeout-bounded + leak-free + every path returns a `CheckState` (never throws out of the handler), the route is a driven live endpoint. **◐ not ✅:** the endpoint + pure assembly are built + unit-verified, but the live DB-probe behavior was not run unattended (verify with `curl` on publish), and the ALERTING half (monitor → notify) + a status page are infra/founder + design-pass. UI breadcrumb: none (infra endpoint). |
| 2026-06-20 01:30 | SUBPROC-DISCLOSURE: sub-processor disclosure registry (cycle 49) | P1 Data/Privacy | `7cc35423c4` | **Pick:** the top remaining queue items all converge on the AI chokepoint (queued for an attended cycle): PROVIDER-FALLBACK + PLAN-ENFORCE restructure `runtime.server.ts`, and MODEL-REGISTRY-DEPRECATION - though the queue named its domain as `models.ts` - has its only universal live drive-point at the chokepoint too (model resolution funnels through `runtime.server.ts:1063`; `routeModel` is a DEAD export with zero consumers, so a route-around is staged-but-never-driven anywhere else), so it joins the attended bundle. The other clean queue items are parallel-lane-owned (R3-PREFS/P7 → cockpit, O3 → knowledge, FND-0.5 → safety, F-BUILDER-MULTIFILE → build). So I mined `considerations.md` for a clean, non-chokepoint, non-parallel-lane, fully-verifiable Data/Privacy gap (this lane's strength): the P1 "Sub-processor list (model providers!) + DPA" - a named before-the-first-enterprise-sale blocker completing a data-governance triad with DATA-RETENTION + U6-AUDIT. **What:** new pure `src/lib/compliance/subprocessors.ts` (a `SubProcessor` type + a curated infra list [Lovable gateway / Supabase / Cloudflare, all active] + `modelProviderSubprocessors` DERIVED from the live catalog so the disclosure can't drift: a provider is `active` only when it has a `live` model; adapter-ready BYO listed inactive; `ollama` excluded as self-hosted = never a third-party processor; + `allSubprocessors`/`activeSubprocessors`); new `getSubprocessors` GET server fn (`src/lib/compliance.functions.ts`). `PROVIDER_META: Record<Model['provider'],…>` makes tsc FAIL if a catalog provider lacks a disclosure entry (no silent staleness). **Verification (ran):** tsc 0, eslint 0 (3 new files, after a prettier auto-fix), build ✓ (3.11s), `bun test src/lib/compliance/subprocessors.test.ts` 11/11 (shape, unique ids, infra-active, ollama-never-listed, active-only-when-live against an INJECTED catalog, ordering, active=subset-of-all), full `bun test` 298/298. No DB access in any new file → no untyped-Supabase / NOT-NULL / RLS trap, no dry-run needed. Adversarial review: no runtime-fatal bug; the disclosure is honest (facts cross-checked vs `models.ts` + CLAUDE.md; regions left undefined; no over-claim of legal completeness). **◐ not ✅:** the pure registry + read fn are built + unit-verified, but the trust-page UI + the legal-reviewed copy/regions/DPA are deferred to the design/legal pass, and the GET fn's request handling was not behaviorally run unattended. UI breadcrumb (once UI ships): public trust page / Settings > Account > Privacy. |
| 2026-06-20 00:50 | U6-AUDIT: export audit log (cycle 48) | P2 Interop (U6) | `2dd37cafe7` | **Pick:** the two highest-impact remaining items (PROVIDER-FALLBACK, WM-M9) both touch the AI chokepoint AND can't be behaviorally verified unattended (the founder's guidance: chokepoint better attended; shipping an unverifiable chokepoint change = claim outrunning wiring, HARD GATE 4), so both were queued for an attended cycle and I picked the cleanest fully-verifiable my-lane item: U6-AUDIT (the remaining ~15% of U6 row 36). Migration `20260620010000_export_log.sql` adds an APPEND-ONLY `export_log` table — `insert-own` + `read-own-or-workspace` RLS and DELIBERATELY no update/delete policy, so the trail is tamper-evident for authenticated users (Postgres default-deny); the `is_workspace_member`-gated read lets a workspace owner audit ALL data egress. `exportProduct`/`exportWorkspace` write one audit row per export (who/kind/target/sections/row_count); the write is best-effort + try/catch so it NEVER fails the user's export. New `listExportLog` read fn (RLS-scoped, newest-first, capped 100). **Verification (ran):** tsc 0, eslint 0, build ✓ (3.25s), `bun test` 287/287; AND a `BEGIN..ROLLBACK` behavioral dry-run on the live prod DB (owner JWT simulated): both insert shapes applied, `user_id` defaulted from the JWT, the `kind` CHECK rejected a bogus value, RLS on, the policy set was exactly 1 insert + 1 select with 0 update/delete/all (append-only CONFIRMED); rolled back, prod untouched. Adversarial review folded one fix (try/catch around the audit-write). **◐ not ✅:** the audit table + per-export write + read fn are built + behaviorally dry-run-verified, but U6 stays ◐ because its export-history UI (DataExportCard) is deferred to the design pass and live rows accrue post-publish. UI breadcrumb (once UI ships): Settings > Data > Export history. |
| 2026-06-20 00:20 | DATA-RETENTION: AI-telemetry retention purge (cycle 47) | P1 Data/Privacy | `1a2e521574` | **Pick:** the WM clean-backend lane thinned to WM-M9 (chokepoint capability-removal, best sequenced with the founder-gated pricing surfaces per its own spec), so WM-M9 was skip-and-queued and `considerations.md` was cross-referenced for the next clean P1 gap. `ai_events` (+ prompt_runs/tool_calls) grow unbounded (one+ row per AI call, forever) — a cost/scale + GDPR/CCPA data-minimization gap enterprise/team buyers ask about. Migration `20260620000000_data_retention.sql` adds `data_retention_enabled()` (dormant flag, false) + a SECURITY DEFINER `purge_old_telemetry(_older_than_days int default 180)` that self-gates on the flag, applies a hard 30-day floor (a mis-call can't purge recent data), and deletes the 3 telemetry tables older than the window (no inbound FKs — clean deletes); execute revoked from public/anon/authenticated, granted to service_role only (stricter than the codebase's other definer RPCs — a global purge is destructive). New `retention-tick` hook (`src/routes/api/public/hooks/retention-tick.ts`, requireHookCaller-gated, pre-migration-tolerant). **Verified live schema first:** `token_usage` doesn't exist (token data is in ai_events); no inbound FKs. **Verification (ran):** tsc 0 (after build regenerated `routeTree.gen.ts`), eslint 0, build ✓ (3.19s), `bun test` 287/287; AND a `BEGIN..ROLLBACK` behavioral dry-run on the live prod DB (backdating two real ai_events rows to 200d/50d): dormant→`skipped:dormant` (deleted nothing), active purge(180) deleted the 200d row + KEPT the 50d row, floor purge(0) floored to 30 + deleted the 50d row; rolled back, prod untouched. **◐ not ✅:** built + dry-run-verified, but stays DORMANT until the founder flips `data_retention_enabled()` AND wires a pg_cron/scheduler to POST the hook. Follow-up DATA-RETENTION-b: right-to-be-forgotten erase cascade (destructive + policy-coupled, deferred). UI breadcrumb: none (cron + DB plumbing). |
| 2026-06-19 23:42 | WM-F6: move product between workspaces (cycle 46) | G10 Foundation | `394cff641c` | A transactional move so a product + all its data can be relocated to another workspace in the SAME account. **Key finding (verified live first):** the spec's "harden product-level scoping first (product_id has no RLS)" is already done — `pg_policies` on prod shows every product-scoped table is read-gated by `is_workspace_member(workspace_id)`, so reassigning `workspace_id` IS the move (access follows); no RLS-hardening migration needed. Migration `20260619280000_wm_f6_move_product.sql` adds a SECURITY DEFINER `move_product(_product_id,_dest_workspace_id)` that, after a guard (owner/admin of BOTH workspaces via WM-F3 `can_manage_workspace` + both in the SAME account via WM-M2 `account_id` + a `FOR UPDATE` lock), atomically reassigns `workspace_id` on the product + 13 product-scoped tables: 10 direct children (signals/themes/opportunities/prds/docs/tasks/decisions/conversations via `project_id`-or-`product_id`; notes/prototypes via `project_id`; ai_events/connection_bindings/rag_chunks/studio_changesets via `product_id`) + 3 grandchildren via parent (doc_versions→docs, messages→conversations, learnings→opportunities/prds). Memory stays at the workspace (not moved). New `moveProduct` server fn. **Untyped-client trap handled (HARD GATE 4):** the product FK is `project_id` on 8 tables but `product_id` on 4 — verified live, each UPDATE uses the right column. **Verification (ran):** tsc 0, eslint 0, build ✓, `bun test` 274/274; AND a `BEGIN..ROLLBACK` behavioral dry-run on the live prod DB (Lovable MCP, stubbing unpublished WM-M2/F3/F9, owner JWT simulated): a real product's 9 signals/2 prds/10 tasks/3 decisions/8 messages ALL moved to the dest, **0 orphans left in src**; cross-account move blocked + non-manager caller blocked; rolled back, prod untouched. **◐ not ✅:** backend built + behaviorally dry-run-verified, but the Settings move UI is deferred to the design pass and live move activates on publish (depends on unpublished WM-M2/F3/F9). Follow-up WM-F6b: `artifact_lineage` + other generic-provenance tables. UI breadcrumb (once UI ships): Product > Settings > Move to workspace. |
| 2026-06-19 23:18 | WM-F8: workspace switch hardening — query-cache reset (cycle 45) | G10 Foundation | `d2041d4dd3` | Closes the stale-data flash on workspace switch (the spec's "reads like a leak"). `setActiveWorkspaceId` in `src/hooks/use-workspace.tsx` never touched the TanStack Query cache, so any workspace-scoped query whose key omits the workspace id kept serving the PREVIOUS workspace's cached rows until it happened to refetch (and inactive queries surfaced the old cache on the next navigation). Fix: on a real switch (`id !== activeWorkspaceId`) it now calls `queryClient.removeQueries({predicate})` to clear every workspace-scoped entry (active AND inactive) so nothing stale renders and observers refetch under the new workspace; a new pure module `src/hooks/workspace-query-scope.ts` (`isWorkspaceScopedQueryKey` + `GLOBAL_QUERY_KEY_ROOTS`) is the allowlist preserving the user/account-global queries (`workspaces` list, `profile`, `billing`, `connections`, `calendar-connections`, `integrations`, `api-keys`, `mcp-tokens`) and fails safe to "workspace-scoped" for any unlisted/odd key. Chose the single-site reset over threading `activeWorkspaceId` into ~40 query keys (catches every query in one surgical diff; per-key work logged as WM-F8b). `removeQueries` not `invalidateQueries` (invalidate keeps stale data visible during the refetch). **Adversarial review:** no runtime-fatal bug — driven by the AppShell switcher (`AppShell.tsx:516`); no stale-closure refetch (keyed queries refetch on key change, server-fn queries hold no client workspace closure); guard avoids needless clears. **Verification (ran):** `bunx tsc --noEmit` 0, `bunx eslint` 0 errors on the 3 touched files (1 pre-existing `react-refresh` warning on the untouched `useWorkspace` export), `bun run build` ✓ (4.28s), `bun test` 274/274 (4 new). **◐ not ✅:** the reset is correct + gate-green + driven, but most server-fn reads are still user-scoped server-side today (`missions`/`guardrails` fns reference no `workspace_id`), so the full user-visible no-flash payoff scales with server-side workspace read-filtering (WM-F9b), and the two-seeded-workspace Playwright check needs a publish; no live behavioral test was run. UI breadcrumb: AppShell > workspace switcher. |
| 2026-06-19 23:03 | WM-F5: workspace invitations backend (cycle 44) | G10 Foundation | `d0e7300c33` | Adds the invite flow backend (UI deferred to the design pass per the founder ruling). Migration `20260619260000`: a `workspace_invitations` table (DB-generated token, role admin/member/viewer, status pending/accepted/revoked/expired, 7-day expiry) with manager-only RLS (`can_manage_workspace` from WM-F3; the invitee has NO direct read, so a pending invite never leaks workspace data) + a SECURITY DEFINER `accept_workspace_invitation(_token)` that validates (pending + unexpired), binds the token to the invited email (the signed-in user's email must match — a leaked/forwarded token can't be redeemed by another account), upserts the `workspace_members` row with the invited role, and flips the invite to `accepted` (single-use under a `FOR UPDATE` lock). New `email.server.ts` (graceful no-op `sendInviteEmail` that always returns the copy-paste link; outbound email is founder-gated, so it never sends). New `inviteMember` / `listInvitations` / `revokeInvitation` / `acceptInvitation` server fns. **Verification (ran):** tsc 0, eslint 0 (both files), build ✓, `bun test` 270/270; AND a `BEGIN..ROLLBACK` behavioral dry-run on the live prod DB (stubbing WM-F3's `can_manage_workspace`): inviting a real user's email then accepting as that user (via `request.jwt.claims`) added them to the workspace with role=member and flipped the invite to accepted; re-accept rejected ("no longer valid"), a token for a different email rejected ("sent to a different email address"), and an expired token rejected ("has expired"); rolled back, prod re-confirmed untouched (table/RPC absent, seeded membership gone). **◐ not ✅:** backend built + behaviorally dry-run-verified, but the join route (`/join/$token`) + Settings members UI are deferred to the design pass, and live invites activate on publish (depends on the unpublished WM-M2 + the fixed WM-F3 `70c0ca7db5` so `can_manage_workspace` exists). UI breadcrumb (once UI ships): Settings > Team > Invite. |
| 2026-06-19 22:43 | WM-F4: workspace ownership transfer (cycle 43) | G10 Foundation | `345276a5aa` | First build since the lane coordination made the WM lane collision-free. Closes the `leaveWorkspace` dead-end ("transfer it first" with no transfer). Migration `20260619250000`: a `workspace_audit_log` table (member-read RLS; writes only via the RPC) + a SECURITY DEFINER `transfer_workspace_ownership(_workspace_id, _new_owner_id)` RPC that atomically reassigns `workspaces.owner_id`, flips the `workspace_members` roles (new owner → owner, old owner → admin), and writes an audit row. New `transferWorkspaceOwnership` server fn (thin validated wrapper; uses `context.supabase` so the RPC's owner-only `auth.uid()` check applies to real callers). **Load-bearing ordering:** reassign `owner_id` BEFORE demoting the old owner so WM-F3's `prevent_workspace_owner_demotion` trigger allows it (WM-F3 applies before this on publish). **Verification (ran):** tsc 0, eslint 0, build ✓, `bun test` 270/270; AND a `BEGIN..ROLLBACK` behavioral dry-run on the live prod DB (with WM-F3's role-CHECK widening + the demotion trigger applied first): transferring workspace A from owner U1 to a member U2 reassigned `owner_id`→U2, set U2's role to `owner` + U1's to `admin` (the demote passed the trigger, proving the ordering), wrote one audit row `{from:U1,to:U2}`; a non-owner caller was blocked ("Only the workspace owner can transfer ownership.") and a transfer to a non-member was blocked ("The new owner must be a member of the workspace."); rolled back, prod re-confirmed untouched (owner restored, table/fn absent, seeded membership gone). **◐ not ✅:** the RPC + server fn are built + behaviorally dry-run-verified, but the **Settings UI affordance (member-picker + confirm) is deferred** to a follow-up / the design pass, and live end-to-end transfer activates on the founder's publish (WM-F4 depends on the unpublished WM-M2/F3). UI breadcrumb (once the UI ships): Settings > Workspace > Transfer ownership. |
| 2026-06-19 22:30 | WM-M12 credit money-mover: independent behavioral RE-VERIFICATION (cycle 42, no code change) | G10 Credit | (docs only) | Continuing the de-risk sweep, reviewed the highest-financial-severity parallel-built piece: the `debit_account_credits` RPC (the money-mover). Code-read: column names match WM-M2's `account_credits`/`credit_ledger` schema, all NOT NULL columns covered, `reason='debit'` is in the CHECK set, atomic via `FOR UPDATE`, draws INCLUDED-then-TOPUP, floors at available (no overdraw/negative balance). **Behaviorally dry-run-verified on the live prod DB** (BEGIN..ROLLBACK, stubbing the WM-M2 credit tables): seeded a pool of 100 included + 50 topup, debited 120 → drew 100+20 (ledger -120), debited 100 → only 30 topup left so drew exactly 30 (the floor held, NO overdraw; ledger -30), debited 10 on the empty pool → correct no-op (no ledger row); final balance 0 / topup 0, ledger deltas `[-120,-30]`, 2 rows; rolled back. **Result: CLEAN** — the money path is sound (it was already builder-verified at cycle 32 + is correct). Call-site args in `runtime.server.ts:883` match the RPC signature and are gated by `creditsEnabled()` (dormant by design). Minor non-blocking nit (logged, not fixed): the pool UPDATE does not touch `account_credits.updated_at`. No fix needed; no commit beyond this report note. This concludes the de-risk sweep: 3 broken parallel ships fixed (cycles 39-41), the credit money-mover re-verified sound, and the lane coordination now prevents further WM collisions. |
| 2026-06-19 22:20 | WM-F3-fix: shipped RBAC migration used invalid `create policy if not exists` (cycle 41) | G10 Monetization | `70c0ca7db5` | Not a collision this cycle — a proactive adversarial review of recently-shipped SECURITY DEFINER / RLS migrations (the highest-risk parallel-built code, prompted by 2 prior broken ships). Found the **3rd broken parallel ship**: the WM-F3 RBAC migration (`2b1c429591`) used `create policy if not exists` for all four account/account_members policies. **Postgres CREATE POLICY has no IF NOT EXISTS clause** (unlike CREATE TABLE/INDEX), so it is a hard syntax error — `ERROR: 42601` — and the migration would have FAILED to apply on the founder's next publish, blocking the whole run. tsc/build were green (pure SQL). Fix: all four are now `drop policy if exists` + `create policy` (valid + idempotent; the four names differ from WM-M2's, so no collision); the owner-demotion trigger is now drop-then-create guarded. **Verification (ran):** confirmed on the live prod DB via BEGIN..ROLLBACK (stubbing the unpublished WM-M2 accounts deps): the `if not exists` form errors with 42601; the corrected migration applies cleanly (RBAC helpers + the 5 policies + the demotion trigger all created); rolled back, then prod re-confirmed untouched (the real "ws owner manage" policy I dropped in the txn was restored, accounts/functions absent). Also data-checked the new role-keyed workspaces policy for owner-lockout: all 7 workspaces have their owner as a `workspace_members(role='owner')` row, so no owner loses access. tsc 0, build ✓, 270 tests (SQL-only; TS untouched). **◐ not ✅:** the migration now applies; live RBAC enforcement activates on publish. UI breadcrumb: none (RLS plumbing). |
| 2026-06-19 22:05 | WM-F2-fix: restore security + correctness invariants in the shipped account-memory pooling (cycle 40) | G10 Foundation | `a368edf78a` | Second same-night collision: a parallel session shipped WM-F2 (`18db474d3b`) while this cycle was building the same item in the worktree. Their version was tsc/build-green but **regressed the WM-F1 recall baseline** and was dormant; this cycle had a correct, prod-verified build ready, so it became the fix. **The serious one (SECURITY):** their RPCs filtered `m.user_id = for_user` instead of WM-F1's `coalesce(auth.uid(), for_user)` — reverting the exact pin WM-F1 added to CLOSE a cross-user read on these SECURITY DEFINER RPCs, so any authenticated caller could pass `for_user=<victim>` and read another user's memory/reflections. Restored the pin. **Plus:** recalled EXPIRED memory (dropped the expiry filter), dropped GLOBAL-scope memory (mandatory `agent_slug=` equality), dropped the per-row `is_workspace_member` guard, and regressed the return shapes (3 cols + distance-as-similarity; reflections lost importance/metadata + timestamptz→timestamp). **And "staged but never driven":** pooling was gated on `opts.accountId`, which no caller passes (the commit touched no call sites), so pooling never activated. The fix resolves the pool account INTERNALLY from the workspace's account tier (`entitlements.ts` = single source of truth), so it is actually driven with no call-site changes. **Verification (ran):** tsc 0, build ✓, eslint 0, 270 tests; AND a BEGIN..ROLLBACK behavioral dry-run on the live prod DB (stubbing the unpublished WM-M2/F1 deps): paid account recalls across BOTH workspaces (`[DRYRUN_R_A, DRYRUN_R_B]`), single-workspace returns only A, a different account returns nothing; rolled back, prod re-confirmed untouched (accounts/columns absent, recall fn reverted to pre-WM signature). **◐ not ✅:** correct + dry-run-verified; live pooling activates on publish. UI breadcrumb: none (recall plumbing). |
| 2026-06-19 21:30 | WM-M15b-fix: 3 runtime-fatal bugs in the shipped response cache (cycle 39) | G10 Credit | `7897085004` | The cycle-38 WM-M15b (`796513d181`, shipped by a parallel session) was tsc/build-green but broken at runtime; this cycle adopted the same item in the worktree, found the defects, and (since the parallel session had already pushed) shipped the fix rather than a duplicate. **(1) Migration FATAL:** `CREATE INDEX ... WHERE expires_at > now()` is rejected by Postgres (`now()` is STABLE; index predicates require IMMUTABLE), so the migration would have FAILED to apply on the founder's next publish. **Verified live:** the broken DDL returns `ERROR: 42P17` on the prod DB, the fixed plain-index DDL applies cleanly (dry-run on a throwaway table, dropped; the real `ai_response_cache` confirmed not-yet-applied). **(2) Cache no-op FATAL:** `callModel` ran the provider unconditionally even on a cache hit (the `if(!cacheHit)` guard wrapped only the write block), so the cache saved zero COGS; the provider retry loop + fallback are now skipped on a hit. **(3) readCache:** passed the literal string `"now()"` to `.gte("expires_at")` which Postgres cannot cast to timestamptz, so every read errored and the cache always missed; now `new Date().toISOString()`. Also: `generateCacheKey` hardened to Web Crypto (`crypto.subtle`) off a bare-`"crypto"` dynamic import whose worker runtime was unverifiable (`rag/indexer.server.ts` pattern); `writeCache` stores under `effectiveModel` + skips fallback responses for read consistency; supabase params typed (no-explicit-any). **Verification (ran):** `bunx tsc --noEmit` 0, `bun run build` ✓, `eslint` 0 on the 3 files, `bun test cache.test.ts` 10/10, + the prod migration dry-run above. **◐ not ✅:** the migration now applies and the cache logic is correct, but live cache-serving still activates on the founder's publish. UI breadcrumb: none (AI chokepoint). |
| 2026-06-19 20:10 | WM-F1b: agent-workspace isolation hardening (cycle 37) | G10 Foundation | `7e406df10f` (+ `4c643900e3` roles tsc fix) | ◐ CORE. Pushed onto a parallel WM-F3 (RBAC, `2b1c429591`) that landed mid-cycle; rebased clean (only a SSOT cursor line conflicted), renamed the migration 210000 → 220000 to avoid a `schema_migrations` version collision with WM-F3, and folded a one-line tsc fix to WM-F3's `roles.functions.ts` (its `(supabase.from('account_members') as any)` cast the result, not the client, so tsc rejected the untyped table; `2b1c429591` was a red tree) so `main` is tsc-green. Migration `20260619220000_wm_f1b_agent_workspace_hardening.sql` adds a `user_id`-fed BEFORE-INSERT trigger (`set_row_workspace_from_user`, service-role-safe) to all 5 agent tables (`agents`/`agent_memory`/`agent_tools`/`agent_runs`/`agent_approvals`) so EVERY insert path is workspace-tagged at the DB layer (the `seed_default_agents` SQL fn, the `memory.remember` tool, any service-role writer), more robust than hand-editing the ~6 paths the bible listed, plus a DUAL-KEY membership RLS (`auth.uid()=user_id AND is_workspace_member(workspace_id)`) replacing the owner-only policy on all 5. `registry.server.ts` `memoryRemember` tags the active workspace (pre-migration-tolerant). **Design:** dual-key (not membership-only) because the recall RPCs are user-scoped + workspace-narrowed; it is strictly MORE restrictive than the old policy so it never widens access or locks the owner out (`ensure_user_default_workspace` guarantees membership). **DEFERRED to WM-F1c:** NOT NULL (founder-gated, WM-F1's `ON DELETE SET NULL` preserves the moat across workspace deletion; NOT NULL would force CASCADE = irreversible memory loss), the `agents` unique-key swap (`seed_default_agents` uses `ON CONFLICT (user_id,slug)`; couples to WM-F8), types regen (post-publish). **Verification (ran):** two-pass `BEGIN..ROLLBACK` dry-run on the LIVE prod DB, 5 triggers / 5 dual-key policies / 0 old policies remaining, trigger fills `workspace_id`, `seed_default_agents` OK (ON CONFLICT intact), `is_workspace_member(null)=false`, and a simulated-JWT read as a real owner returned all 23 of their `agent_memory` rows (no reader breakage); 0 memory-owners lack a `workspace_members` row; rolled back, prod re-confirmed untouched. `bunx tsc --noEmit` 0, `eslint` 0 (registry.server.ts), `bun run build` ✓, `bun test` 252/252, humanization clean. Adversarial self-review folded 2 humanization fixes; no runtime-fatal bug. **◐ not ✅:** dry-run-verified on the real prod schema; live isolation activates on publish. UI breadcrumb: none (RLS plumbing). |
| 2026-06-19 19:30 | WM-M5: tier limit gates (product + workspace) (cycle 36) | G10 Monetization | `5bc31aee73` | ◐ CORE. Migration `20260619200000_wm_m5_tier_limit_gates.sql` adds pure `tier_product_limit` / `tier_workspace_limit` SQL functions (free 2/1, pro 3/∞, max 5/∞, team+enterprise ∞, mirroring `entitlements.ts`) + `enforce_product_limit` / `enforce_workspace_limit` SECURITY DEFINER BEFORE-INSERT triggers, and new `src/lib/limits.functions.ts` (`LimitReachedError {kind,currentTier,limit,upsellTier}` + pure `nextUpsellTier`/`isOverLimit` + the pre-migration-tolerant `assertCanCreateProduct` nice-path) wired into `createProject`. **Design:** the DB trigger is the authoritative guard (the client inserts products straight from the browser, `AppShell.tsx:406`); the product cap is account-WIDE so pooled-workspace tiers can't bypass it; the first workspace is never gated (resolved via `owner_id` to survive the alphabetical BEFORE-trigger fire-order). Ships **DORMANT** behind a new `limit_gates_enabled()` flag (= false) mirroring `credits_enabled()`; the founder flips it once a live upgrade path (Stripe) exists. **Verification (ran):** `BEGIN..ROLLBACK` behavioral dry-run on the LIVE prod DB (WM-M2 essentials applied first, then WM-M5, flag flipped on): on a real free account, inserts to the cap (2 products) succeeded, the 3rd was BLOCKED with the exact message, a 2nd workspace was BLOCKED, all six pure limit-fn values correct; rolled back (prod untouched). `bun test` 252/252 (13 new), tsc 0, eslint 0 on 3 files, build ✓, humanization clean. Adversarial self-review: no runtime-fatal bug. **◐ not ✅:** dry-run-verified; the gate is dormant + the migration is not on prod, so it activates on publish + the `limit_gates_enabled()` flip. UI breadcrumb: none while dormant; once on, the over-limit toast appears at Sidebar > + (add product). |
| 2026-06-19 18:49 | WM-F9: tenancy isolation + scope-leak audit (cycle 35) | G10 Foundation | `71a6cf3893` | ◐ CORE. Migration `20260619190000_wm_f9_isolation_scope.sql` adds `workspace_id` + backfill + a `NEW.user_id` BEFORE-INSERT trigger (service-role-safe) + NOT NULL + index + a DUAL-KEY membership RLS (`auth.uid()=user_id AND is_workspace_member(workspace_id)`, keeps user-private, never widens) to **10 tables** (the named meetings/notes/daily_briefs/copilot_messages + the 6 an adversarial audit found: prototypes + 3 children, scheduler_proposals, ritual_sessions). Public-share policies preserved; sync_mappings documented-deferred; the `ON DELETE CASCADE` delete-confirm copy fixed (AppShell). **Verification (ran):** `BEGIN..ROLLBACK` behavioral dry-run of the full migration on the LIVE prod DB (two passes) — all 10 backfilled to 0 nulls, trigger fills workspace_id, NOT NULL holds, old owner-only policies removed (dual-key enforces, no permissive-policy-wins bug), dual-key created, public policies preserved; rolled back. tsc 0, `bun test` 239/239, eslint 0, build ✓, humanization clean. 3-lens review: reader-breakage verified (simulated user JWT on prod) no reader breaks; all real fixes folded. **◐ not ✅:** dry-run-verified; live isolation activates on publish. Follow-up WM-F9b: active-workspace scoping + sync_mappings verdict. UI breadcrumb: the truthful workspace-delete confirm copy. |
| 2026-06-19 18:09 | WM-M15: cost-aware model routing margin lever (cycle 34) | G10 Credit | `3f0c2b1343` | ◐ ROUTING half (cache split to WM-M15b). New pure `src/lib/ai/routing.ts` (`blendedPrice`/`cheapestLiveModel`/`costRoutedModel`) wired at the AI chokepoint as `effectiveModel` (byo + provider call + modelUsed + credit projection + debit). **Gated behind the `AI_COST_ROUTING` env flag (default OFF) = byte-identical to today.** A 5-lens review caught a real quality-gate hole (the surface allowlist would have 25x-downgraded the Critic/wedge, which runs on `surface:"judge"` with a deliberate reasoning model) + a benchmark-corruption bug (`eval`'s subject is the model under test). **All 7 findings folded:** narrow routable set `{brief,scheduler,test}` (judge/eval/embed removed) + a TIER GUARD (never downgrade a deliberate reasoning/premium/code/vision or unknown model); credit pre-check + debit now track the model actually run. **Verification (ran):** `bun test routing.test.ts` 12/12, full `bun test` 239/239, tsc 0, eslint 0 on 3 files, build ✓, humanization clean. **◐ not ✅:** pure router verified; live routing config-gated (founder flips `AI_COST_ROUTING`). Conservative by design (modest immediate impact; the bigger lever is the WM-M15b cache). No UI breadcrumb (chokepoint). |
| 2026-06-19 17:28 | WM-M14: per-product / per-member credit attribution + caps (cycle 33) | G10 Credit | `73400be0a9` | ◐ CORE. Closes the loop WM-M12 stubbed (`_product_id` was null "for WM-M14"). Migration `20260619180000_wm_m14_credit_caps.sql` (credit_caps table + 2 composite credit_ledger indexes + member-read RLS). `credits.functions.ts`: PURE `rollupAttribution`/`sumDebitCredits`/`capExceeded`/`creditWindowStartIso` + `computeCreditAttribution` (RLS-scoped read, for WM-M16). `runtime.server.ts`: `CreditCapError` + `assertCreditCaps` (per-product/member scoped halt inside assertAccountCredits, degrades to allow on error, dormant no-op) + `productId` threaded into the debit. `cluster.server.ts` feeds productId from discovery clustering, so per-product attribution is DRIVEN there; per-MEMBER always driven; other sites null until adopted. Gated behind `credits_enabled()`. **Verification (ran):** `bun test credits.test.ts` 22/22 (46 asserts), full `bun test` 227/227, tsc 0, eslint 0 on 4 files, build ✓, humanization clean on my lines, AND a `BEGIN..ROLLBACK` behavioral dry-run on the live prod DB (table + indexes + RLS created, cap-sum query correct), rolled back (prod clean; transactions probe-confirmed first). 5-lens adversarial review → 6 findings, 5 folded (per-product driven + disclosed; RLS-client invariant; productId-account invariant; stale comment), 1 honest. **◐ not ✅:** pure math verified; live caps/attribution on publish + the flag flip. No UI breadcrumb (engine; Usage view is WM-M16). |
| 2026-06-19 16:44 | WM-M12: credit debit engine, fills the WM-M4 seam (cycle 32) | G10 Credit | `ecc8b55310` | ◐ CORE. New migration `20260619170000_wm_m12_credit_debit_rpc.sql` (atomic `debit_account_credits` RPC: included-then-topup, FOR UPDATE, tagged ledger debit). `runtime.server.ts`: `assertAccountCredits` projects via the new pure `projectCallCredits` + halts with `CreditExhaustedError` + a blocked event; `debitAccountCredits` calls the atomic RPC, passes `ai_event_id`. Gated behind `credits_enabled()` = dormant no-op. **Verification (ran):** `bun test pricing.test.ts` 19/19, full `bun test` 212/212, tsc 0, eslint 0 on 3 files, build ✓, humanization clean, AND a behavioral dry-run of the RPC on the live prod DB (debit 70 then 60 on included=100/topup=50 -> included=0, topup=20, ledger_sum=-130, 2 rows), rolled back. Adversarial review: no real fix. **◐ not ✅:** RPC + projection verified; end-to-end live metering activates on publish + flag flip. No UI breadcrumb (chokepoint). |
| 2026-06-19 16:31 | WM-M11: per-tier credit grant + monthly cycle reset + credit-tick hook (cycle 31) | G10 Credit | `51abc50a59` | ◐ CORE. New `src/lib/credits.functions.ts` (pure `monthlyGrantCredits`/`resetDelta` + dormant `grantMonthlyAllowance`/`resetCreditCycle`) + new `credit-tick` cron hook (grants un-granted, resets accounts past ~30d since cycle_anchor, preserves top-ups). Service-role writes; gated behind `credits_enabled()` = no-op while dormant; pre-migration tolerant; ledger reconciles. **Verification (ran):** `bun test credits.test.ts` 7/7 (24 asserts), full `bun test` 208/208, tsc 0, eslint 0 on 3 files, build ✓ (registers the route), humanization clean. Adversarial review: no real fix. **◐ not ✅:** grant math unit-verified; the DB writes + tick activate on publish + the flag flip + pg_cron. No UI breadcrumb (engine + cron). |
| 2026-06-19 16:17 | WM-M4: dormant runtime credit seam at the AI chokepoint (cycle 30) | G10 Monetize | `1b5a28d48c` | ◐ CORE. `src/lib/ai/runtime.server.ts`: `CreditExhaustedError` + cached `creditsEnabled()` + `resolveCreditAccountId` + `assertAccountCredits` (pre-call, after the budget check in callModel + callModelStream) + `debitAccountCredits` (post-call, next to incrementBudget). Gated behind `credits_enabled()` = strict no-op while dormant (the flag + credit tables do not exist on prod until WM-M2 publishes, so the gate self-protects). Writes via service-role `supabaseAdmin`. Minimal functional v1; WM-M12 hardens (atomic draw-down RPC, draw order, blocked-event, attribution). **Verification (ran):** tsc 0, eslint 0 on the file, full `bun test` 201/201 (no chokepoint regression), build ✓, humanization clean. Adversarial review: no real fix. **◐ not ✅:** live debit is DB-coupled, activates on publish + the flag flip; no isolable unit test for a dormant DB seam. No UI breadcrumb (chokepoint plumbing). |
| 2026-06-19 15:58 | WM-M10: credit unit + cost-to-credit conversion + legibility layer (cycle 29) | G10 Credit | `aee2e61fca` | ✅ SHIPPED. `src/lib/ai/pricing.ts` (+ new `pricing.test.ts`): `CREDIT_COGS_USD` + `creditsForCost` (0 for non-billable, >= 1 margin-positive for billable) + `estimateCreditsForCall` (composes `estimateCostUsd`) + the pure display-only `actionCreditRange` legibility layer (calibrated token shapes through the same conversion; no DB, the spec's `ai_events` averages deferred to WM-M16). **Verification (ran):** `bun test src/lib/ai/pricing.test.ts` 15/15 (29 asserts), `tsc --noEmit` 0, `eslint` 0 on both files, `bun run build` ✓, humanization clean. Adversarial review: no real fix. **Behaviorally verified by the unit tests (pure module, no publish) → ✅.** Numbers are §7 placeholders; WM-M12 drives these into the live debit seam. No UI breadcrumb yet (library). |
| 2026-06-19 15:40 | WM-M2 core: accounts table + billing relocation + credit pool shell + 30d rolling decay (cycle 28) | G10 Monetize | `5eacb8bf1c` | ◐ Core. Migration `20260619160000_wm_m2_accounts_billing_credits.sql` creates `accounts`/`account_members`/`account_credits`/`credit_ledger`; backfills one account per distinct workspace owner (DISTINCT ON highest-tier) + owner membership + a credits shell; adds `workspaces.account_id` (FK, backfilled, asserted 0-null, NOT NULL) with a BEFORE-INSERT `set_workspace_account` trigger auto-filling from `owner_id` (so NOT NULL never breaks a future insert); `protect_account_billing_columns` created AFTER the backfill (so the no-JWT migration role can't clobber seeded tiers); widens `workspaces.plan_tier` to 5 slugs; reworks `set_agent_memory_expiry` to 30d rolling off `last_used_at` (INSERT-OR-UPDATE, paid-list widened, still dormant behind `memory_expiry_enabled()`); adds `credits_enabled()` (false), `is_account_member`, `ensure_user_default_account`; RLS member-read on all 4 new tables (writes service-role-only). `src/lib/billing.functions.ts` `getBillingState` reads the plan account-first with a workspace-shim fallback (migration-tolerant). **Verification (ran):** `tsc --noEmit` 0 project-wide; `eslint src/lib/billing.functions.ts` 0; `bun run build` ✓ (3.04s); humanization scan 0 hits on both new files; AND a `BEGIN..ROLLBACK` dry-run of the full migration on the LIVE prod DB (via the Lovable MCP) — clean apply, `accounts=5 / members=5 / credits=5 / ledger=0`, all 7 workspaces linked with 0 nulls, `credits_enabled=false`, then rolled back (confirmed all 4 tables + the column absent after, prod untouched). Adversarial review found no real fix. **Finding (pre-existing, not WM-M2):** repo-wide `bun run lint` red with ~4340 errors in untouched files. **Schema + account-level billing read activate on the founder's next publish (not a behavioral live test → ◐, not ✅).** UI breadcrumb: `Settings > Account > Plan`. |
| 2026-06-19 14:45 | WM-F1 core: agent memory recall scoped to workspace (cycle 27) | G10 Foundation | `b18995a32a` | ◐ Core (hardening → WM-F1b). Migration `20260619150000_wm_f1_agent_workspace_scope.sql`: nullable `workspace_id` on all 5 agent tables (backfilled via `ensure_user_default_workspace`), recall RPCs rewritten with optional `for_workspace` (service-role-safe, null-tolerant) + a folded security fix (cross-user reflection read). `memory.server.ts`/`reflection.server.ts`/`loop.server.ts`/`mission-advance.server.ts` thread the workspace + tag new outcome/reflection rows + a `PGRST202` deploy-window fallback. **Verification (ran):** tsc 0, eslint clean, build ✓, humanization clean, AND a `BEGIN..ROLLBACK` dry-run of the full migration on the LIVE prod DB (via the Lovable MCP) — clean apply, 0 nulls across all 5 tables, both RPCs recreated, then rolled back (prod untouched). 4-lens adversarial review (372k tok) → 6 findings, 3 folded. **Deferred to WM-F1b:** NOT NULL + RLS swap + roster unique-key + tagging the ~6 remaining insert paths. **Live recall-isolation activates on publish (not a behavioral live test → ◐, not ✅).** No UI breadcrumb (internal recall plumbing). |
| 2026-06-19 13:50 | WM-M1 entitlements core (cycle 26) | G10 Monetize | `(this push)` | First WM feature code. Expanded `src/lib/entitlements.ts` to the 5-tier account model (`free|pro|max|team|enterprise`) with the full matrix (workspace/product limits, `crossWorkspaceMemory`, seats/`rbac`/`approvalLanes`, the credit fields, `priority`, `dataExport`), `limitFor`, and the Constellation `planPresentation` (placeholder credit/price numbers, founder-gated §7). Legacy fields kept as aliases so billing/pricing/settings/webhook are untouched. Rewrote `entitlements.test.ts` (14 tests). **Verification (ran):** `bun test` 14/14; `tsc --noEmit` 0; eslint 0 on both files; `bun run build` ✓; humanization scan clean. Adversarial review: webhook only derives `"pro"`/`"free"`, so the widened `normalizePlanTier` cannot violate the `plan_tier` CHECK pre-WM-M2. **UI breadcrumb:** `Settings > Account > Plan` + `/pricing`. **Interim (expected, not a defect):** Settings BillingTab auto-shows 5 tiers, `pricing.tsx` still shows 3 renamed tiers until WM-M6; TS retention constant 30 while the dormant DB trigger still reads 14 until WM-M2. **No publish needed to verify (pure module, fully unit-tested).** |
| 2026-06-18 | D4-REPLAY replay-and-branch (cycle 25) | G2 Decide | `(this push)` | Strategic #6 (replay shipped; checkpoint-diff D4b deferred). Re-run a finished mission's goal as a new mission with a chosen model: a model picker + "Replay" button on `/missions/$id` (the server already accepted `model`; the retry button just never offered it). Branch link `replayed_from_mission_id` (additive migration, `ON DELETE SET NULL`) set + read pre-migration-tolerantly, with a "Replayed from" link. `tsc` + build + lint + humanization + migration-safety hook green. Adversarial review found no real fix. Clean sync. **Pending your publish + live verify (migration + dispatch + UI).** |
| 2026-06-18 | K2 operator revert-to-revision (cycle 24) | G3 Build | `d57d9c6b48` | Strategic #5 (operator path; agent tool K2b deferred). One-action rollback in the Build Changes tab: a confirm-gated "Revert" button on every prior revision. Non-destructive (new `studio-revert.server.ts` helper: forward commit restoring the target revision's tree via the GitHub Data API, `force:false`, so history only moves forward and the revert is itself revertible) + `revertToRevision` server fn + ChangesPanel button (live non-latest revisions only). No migration. `tsc` + build + lint + humanization green. Adversarial review found no real fix (non-destructive by construction, changeset-scoped lookup, record-failure tolerant, RLS-scoped). Clean sync. **Pending your publish + live verify (GitHub mutation + UI).** |
| 2026-06-18 | P4-GATE eval-regression merge gate (cycle 23) | G4 Govern | `fa2f31fca0` | Strategic #4. `studio.pr.merge` is now hard-gated on eval regression as well as CI. New pure module `eval-gate.ts` (mirrors the J2 `studio-ci.ts`): a ≥10pt drop in the latest completed eval run vs the prior, per suite, blocks the agent's merge (0-100 scale). The merge tool reads the user's completed `eval_runs` (latest two per suite, user-scoped, capped), names the worst-regressed suite, throws `MergeBlocked`. Reads the scheduled eval trend (no run triggered), read-only, no-op until a suite has two runs; operator can still merge from GitHub. No migration. `tsc` + build + lint + humanization green. Adversarial review found no real fix (scale/direction correct, NaN/null-safe, user-scoped). Clean sync, no parallel commit. **Pending your publish + live verify.** |
| 2026-06-18 | P5-ALERT drift → Attention (cycle 22) | G4 Govern | `aa54ff8df7` | Strategic #3. Open drift incidents now reach the operator in the Engine Room Attention feed (R3), not only on the Drift page. Added a 4th live-derived probe to `getNotifications` reading open `drift_incidents` (most-recent 6, over the existing `(user_id, status, detected_at)` index): severity-coded cards (critical → warning, else info) naming the drifted metric + signed delta vs baseline, linking to `/drift`. No migration (matches the approval/stall/budget probe pattern); no UI change (both consumers key off severity, not kind). `tsc` + build + lint + humanization green. Adversarial review found no real fix (numeric coercion guarded, severity safe-defaulted, RLS-scoped). Clean sync, no parallel commit this cycle. **Pending your publish + live verify.** |
| 2026-06-18 | F-AGENTS-MENTIONABLE completed (cycle 21) | G3 Missions | `586aff5bce` | Completed the @-mention feature. The server half (parse `@agentslug` → pre-planned single-step `mission_steps` dispatch via `advanceMissionCore`, deterministic completion via `steps.length > 0`) was auto-committed mid-build by a parallel process (`40646dce0a`, mislabeled "cycle 19"). This cycle added the genuinely-new value: the **composer @-agent picker** in Ask (floating list above the composer, reuses `listAgents`, arrow/Enter/Tab/click insert, never hijacks Enter-to-send) and a **case-insensitive parse fix** (a manually-typed `@Strategist` now resolves), plus the docs the parallel commit skipped (feature doc, SSOT, session-decisions). `tsc` + build + lint + humanization green. Adversarial review folded one fix (server parse was case-sensitive while the composer was not). **Process note (recurring):** a background automation is committing/pushing autonomously again, same pattern as the F3 `c304bf6396` incident; flagged in session-decisions for the founder. **Pending your publish + live verify.** |
| 2026-06-18 | F3-CRON continuous auto-cluster (cycle 20) | G1 Sense | `c304bf6396` | The #1 strategic pick (multi-agent rank vs the P0 "close the loop on real data" milestone, over the easy-but-low-impact APP-HEALTH). Built the always-fresh-SENSE mechanism, shipped GATED OFF (zero AI spend until activated): extracted `clusterSignalsCore`, a `cluster-tick` cron hook (opted-in workspaces only, 5/tick, oldest-first), an owner toggle in the Signals tab, and the `auto_cluster_enabled` migration. `tsc` + build + lint + humanization green. Adversarial review caught + fixed a service-role tenancy bug (the cron must filter `signals.user_id` and stamp `themes.workspace_id` explicitly, else it cross-clusters users / hits a NOT-NULL violation). Activation is founder-gated (apply migration on publish, owner enables toggle, wire pg_cron at the hook). **Pending your publish + activation; see SSOT section 4.** |
| 2026-06-18 | O1 provenance in the shared Lineage drawer (cycle 19) | G1 Sense | `(this push)` | Extended O1's root-signal trace from the two drill pages to the cross-cutting `LineageDrawer`, so "why does this exist?" is answerable wherever lineage opens. A "Traces back to" section sits under the existing "Came from" parents and reuses `getProvenance` (no new server fn, no migration); shown only when the chain is deeper than one hop (`signal_count > 0 && depth > 1`) so it never duplicates "Came from" for a theme. This was uncommitted in-progress work recovered from the prior session's mid-build rollover; verified sound and finished. `tsc` + build green, lint/prettier/humanization clean. **Adversarial review folded one fix:** header printed full `signal_count` while the list rendered `slice(0, 8)`, silently dropping the rest on deep artifacts; added a "+N more source signals" overflow line. **Pending your publish + live verify.** |
| 2026-06-18 12:48 | O1 provenance on spec/PRD detail (cycle 18) | G1 Sense | `(this push)` | Provenance ("why this rests on these source signals") existed only on the opportunity drill; extended to `/prds/$id` so a spec answers "why is this being built?". Reuses `getProvenance` with `kind:"prd"` (walks prd → opportunity → theme → root signals); new "Why this spec · source evidence" card after Citations, links each signal. No migration, read-only. `tsc` + build green, lint/prettier/humanization clean. Adversarial review clean (bounded walk, RLS-scoped, guarded assertions). **Pending your publish + live verify.** |
| 2026-06-18 12:20 | F3 per-product clustering (cycle 17) | G1 Sense | `(this push)` | The discovery feed was workspace-wide; now each product owns its feed. `listSignals`/`listThemes`/`clusterSignals`/`bulkImportSignals` take an optional product id; reads filter on `project_id`, clustering scopes + stamps themes, captures file under the active product. **No migration** (the `project_id` columns already existed). `SignalsPanel` reads `activeProductId` from `useWorkspace()` and keys its queries by it. Back-compatible: no product active = unscoped, exactly as before; unassigned/webhook signals live in the all-products view. `tsc` + build green, 4 files lint clean, my additions humanization-clean. Adversarial review folded one fix (a signal whose theme is out of scope shows standalone, never vanishes). **Pending your publish + live verify.** |
| 2026-06-18 | P7 guardrail-block incident source (cycle 16) | G8 Governance | `(this push)` | The Incidents log now also records **guardrail blocks**, not just failed executions + errored pipelines. Extended `getIncidents` with a third source over the existing `guardrail_hits` table (no migration), filtered to `action='block'` (warn/redact excluded, the call still runs), scoped by user_id. Cards read "Blocked by guardrail: <rule>" with the side it fired on; the raw `matched` payload is deliberately omitted so nothing sensitive lands in the list. `tsc` + build green, both files lint + humanization clean. Adversarial review clean (RLS double-scoped, block-only, no sensitive leak). **Pending your publish + live verify.** |
| 2026-06-18 | R3 global Attention bell (cycle 15) | G7 Cockpit | `(this push)` | The Attention feed only showed "what needs you" inside the Engine Room; now a persistent bell in the TopBar carries the live count on **every** screen, tinted by the most urgent item (approval / spend / stall), quiet when clear, linking to the feed. `AttentionBell.tsx` reuses `getNotifications` on the shared `["notifications"]` cache key (one fetch, not two) and degrades to "all clear" on error so it can never break the top bar. No migration. `tsc` + build green, both touched files lint + humanization clean. Adversarial review folded one fix (dropped `role="status"` so the bell keeps link semantics for screen readers). Design call logged: the bell badges the whole feed; the existing Approvals badge is kept (no double-count). **Pending your publish + live verify.** |
| 2026-06-18 | LCH-01 launch-kit drafting (cycle 14) | G4 Launch | `(this push)` | `generateLaunchKit` turns a shipped changeset into 5 human-approved artifacts (changelog, blog, email, social, docs) in one AI pass, each humanized; a "Launch kit" panel on the Build Changes tab drafts + copies them. Ephemeral, no migration, **never sends** (outbound is founder-gated). `tsc` + build green, lint + humanization clean, adversarial review clean. **Pending your publish + live verify.** |
| 2026-06-18 | Permission policy hardening (cycle 13, founder-requested) | infra | `(this push)` | Fixed the prompt-on-every-call problem: a session reads the `.claude/settings.local.json` of the dir it is LAUNCHED from, and the root session lacked bypass (only the worktree had it). Set `defaultMode: bypassPermissions` + allow/deny in the root local settings; added a shared allow/deny to the committed `.claude/settings.json` (covers regular daytime builds too, no bypass committed); wrote `docs/operations/permissions.md` capturing the model + learnings. No app code. Applies going forward to overnight AND regular builds. |
| 2026-06-18 | O1 provenance "why is this on the roadmap?" (cycle 12) | G1 Sense | `(this push)` | `getProvenance` walks the existing `artifact_lineage` graph up to the ROOT source signals (the lineage card only showed the immediate theme), and a "Why this · source evidence" card on the opportunity drill lists + links them. No new tables, bounded cycle-safe walk. `tsc` + build green, lint + humanization clean, adversarial review clean. **Pending your publish + live verify.** |
| 2026-06-18 | F3 always-fresh discovery feed (cycle 11) | G1 Sense | `(this push)` | The discovery surface (`/product?tab=signals`) now auto-refreshes signals (30s poll; pauses when the tab is unfocused, only while mounted), so the continuously-ingested signals (webhook → reactor) appear live without a manual reload — the "always-fresh per-product feed" half of F3. `tsc` + build green, lint + humanization clean. Adversarial review: cheap scoped read, no flicker, no background polling. **Deferred for your call:** the auto-cluster cron (continuous re-cluster) commits recurring AI spend, so it is queued below rather than enabled unilaterally. **Pending your publish + live verify.** |
| 2026-06-18 | D4 mission cancellation (cycle 10) | G2 Decide / loop control | `(this push)` | The per-mission brake: a "Cancel mission" control on `/missions/$id` stops an active mission (it stops advancing, in-flight steps + child runs flip to `cancelled`, held Build file locks release, pending approvals clear; keeps done work). **Recon corrected cycle 8's "too risky to build blind"** read: the tick already gates on status, so cancelling needs zero loop surgery and no migration. `tsc` clean + `bun run build` green + the 3 touched files lint clean (repo-wide prettier debt is pre-existing, see Notes). Adversarial review folded a TOCTOU race-guard + caught an orphaned-file-lock trigger gap. Also reconciled `H1-TASKS` ✅ (PRD → task graph already built). **Pending your publish + live verify (see below).** Detail: [`../features/d4-mission-cancellation.md`](../features/d4-mission-cancellation.md). |
| 2026-06-18 03:16 | Loop hardening (cycle 9) | infra | `(this push)` | Fixed three silent stall-causes the founder hit overnight. (1) **Permissions:** the worktree carries its OWN `.claude/settings.local.json`, and the session reads THAT, not the parent repo's tuned copy, so the broad allowlist never applied here; rewrote the worktree copy with `defaultMode: bypassPermissions` + broad allow + destructive deny + both repo roots in `additionalDirectories`. (2) **Compaction:** a scheduled `/compact` pauses the loop (it compacts but hands back no turn, so it only resumed because the founder typed `/overnight-build` by hand); retired that rule. The harness now auto-compacts on its own and the loop bridges cycles via `ScheduleWakeup('/overnight-build')`. (3) **Path drift:** compaction summaries handed parent-repo absolute paths, so a doc edit had leaked into the founder's `main` WIP; reverted it and added a worktree-paths-only rule. Docs: `git-discipline.md` (new Timestamp Discipline section, forward-only) + `autonomous-build-loop.md` §7/§10/§11. No app code touched. UI breadcrumb: n/a (infra/process). |
| 2026-06-18 | U6 (core) | G6 Interop | `99563e7db6` | Workspace data export: Settings > Data downloads the whole workspace as one RLS-scoped JSON (signals, opportunities/decisions, specs, tasks, outcomes, agent memory). tsc + eslint + build all green; adversarial review folded 2 fixes (empty-projects guard, active-workspace pass-through). ◐ remainder: per-section selective export + audit-log. **Pending your publish + live verify (see below).** |
| 2026-06-18 | R3 (core) | G7 Cockpit | `456a6ed777` | Notifications "Attention" feed on the Engine Room (`/govern?tab=attention`): one derived feed of pending approvals, spend nearing/over a cap, and a stalled loop, severity-sorted, each card deep-links to its home. No migration (reads existing loop state). tsc + eslint + build green; adversarial review folded 1 fix (tab order). ◐ remainder: email + digests + prefs + a bell badge. **Pending your publish + live verify (see below).** |
| 2026-06-18 | P7 (core) | G8 Governance | `8a0514e4fd` | Incidents log on the Engine Room (`/govern?tab=incidents`): read-only "what went wrong", failed tool executions + errored auto-pipelines, newest first, each execution links to its trace. No migration. tsc + eslint + build green; adversarial review folded 1 fix (explicit workspace scoping on event_queue). ◐ remainder: guardrail/cost sources + a persistent incidents table. **Pending your publish + live verify (see below).** |
| 2026-06-18 | C4/E7 (core) | G8 Governance | `e9aa2706e1` | Agent inspector on Missions > Agents: pick an agent, see its recent run history (status, mission, step, when). Read-only, no migration. tsc + eslint + build green; adversarial review clean (no fix). Memory inspector added cycle 5 (below). **Pending your publish + live verify (see below).** |
| 2026-06-18 | C4/E7 memory | G8 Governance | `(this push)` | Completes C4/E7: a "What this agent knows" section in the Agent inspector showing the agent's private + shared/global memories. `getAgentMemory` via two injection-safe queries. Read-only, no migration. tsc + eslint + build green; review clean. C4/E7 now ✅. **Pending your publish + live verify (see below).** |
| 2026-06-18 | U6 selective | G6 Interop | `(this push)` | U6 advance: the workspace export now has per-section checkboxes (pick what to include). Output-filtered server-side; existing query/RLS logic untouched. tsc + eslint + build green; review clean. ◐ remainder: export audit-log. **Pending your publish + live verify (see below).** |
| 2026-06-18 | Humanization fix + F-HUMANIZE-HOOK | G9 Platform | `(this push)` | Self-correction: the repo's `check-humanized.sh` flagged 7 em-dashes in my own comment headers (6 files); fixed to commas, re-scan clean, tsc/eslint green. Completed F-HUMANIZE-HOOK (was a stale ⬜): `install-git-hooks.sh` now wires the scanner as a warn-only pre-commit hook (`HUMANIZE_STRICT=1` to gate). Tooling/docs only, no app behaviour change, so no publish-verify needed. |
| 2026-06-18 | Backlog reconciliation | verify | `(this push)` | Verify-not-build: corrected P3 (prompt versioning + A/B + pin + rollback) from a stale ⬜ to ✅ (fully built). Initially mis-flagged L2 as built; corrected 2026-06-18 (the `p.$slug` route is the prototype viewer, NOT L2; L2 customer-announcement pages are genuinely unbuilt). Confirmed D4/K2/BLD-05 are genuinely unbuilt but loop/studio-spine-risky. No app change. See Recommendation above. |

## In progress

_(none yet)_

## Skipped / queued for the founder

| Item | Why skipped | What it needs from you |
| --- | --- | --- |
| F3 auto-cluster cron (continuous incremental re-cluster) | It commits **recurring AI spend** for every workspace (a `discovery-tick` that auto-clusters new signals on a schedule). Per the autonomous contract, spend posture is founder-gated, so I did not enable it unilaterally. The always-fresh feed half (cycle 11) shipped without it. | A yes on "enable always-on auto-clustering" (it is the same governed loop-cost model as the existing ticks, bounded by your spend caps + kill-switch). On your OK I will build the `discovery-tick` hook reusing the proven `clusterSignals` core, gated off until you wire its schedule. Also pairs with per-product clustering scope. |
| Demo seed lineage edges (so Lineage + O1 provenance show populated, not empty, on demos) | Surfaced by the 2026-06-18 live sweep: `seed_demo_workspace` creates signals/themes/opportunities/specs but no `artifact_lineage` edges, so the Lineage drawer and O1 provenance (cycles 12/18/19) always render their empty states on demo accounts. Not a code bug; the features degrade gracefully. It is a seed migration (DB change to the seed function) + a demo-quality call, so I queued it rather than change the shared demo seed unilaterally. | A yes on "seed a lineage chain into the demo workspace." On your OK I will add `recordLineage` calls to `seed_demo_workspace` (signal → theme → opportunity → prd → task) via a migration so demos exercise the populated provenance / memory-moat story. |

## Live verification (2026-06-18 12:35) — founder published cycles 1-16, I verified on the live app

The founder published everything through cycle 16; I logged into the published app (`cadence-flow-beta.lovable.app`, demo account) and verified the shipped features render and behave correctly. Result: **everything checked is working.** One minor copy gap found and fixed (below).

| Feature (cycle) | Result | Evidence |
| --- | --- | --- |
| R3 global Attention bell (15) | OK Works | TopBar shows "Attention: 5 things need you" with the count badge, links to `/govern?tab=attention`. Correctly reads lower than the Trust-row "Approvals · 9 pending" (the bell's feed excludes expired approvals), the documented distinct-surfaces behavior. |
| R3 Attention feed (2) | OK Works | Engine Room > Attention renders 5 severity-coded "Needs you" cards, matching the bell count exactly (bell and feed consistent), each links to its home. |
| P7 Incidents log (3) | OK Works | Engine Room > Incidents renders an Execution incident ("mission.dispatch failed" + detail + timestamp + working "View trace"). |
| P7 guardrail source (16) | OK Deployed, behaves right | The throughput card shows "5 guardrail hits" but Incidents shows no guardrail card, correct: those hits are `warn`/`redact`, not `block`, and the block-only filter is working as designed. A real `block` event is needed to see the card. |
| C4/E7 Agent inspector + memory (4, 5) | OK Works | Missions > Agents > inspector renders the agent selector, "Recent runs" (status · mission · step · time), and "What this agent knows" (private reflection memory). |
| D4 mission cancel control (10) | OK Works | A running mission shows status "running" with a "Cancel mission" button beside it (shown only while active). Not clicked (destructive on shared demo data); presence confirmed. |

**Minor finding (fixed 2026-06-18 12:35):** the Engine Room's **Incidents tab description** still read "failed tool executions and errored auto-pipelines" and did not mention guardrail blocks after cycle 16 added them. This was the govern route's tab-description map (not the panel, which cycle 16 did update). Fixed the copy to include "guardrail blocks". `tsc` + build green.

**Not yet spot-checked live (same publish pipeline, high confidence; will check on request):** U6 export, U6 selective, O1 provenance, F3 always-fresh feed, LCH-01 launch kit. F3 per-product clustering (cycle 17) is unpublished, so it stays in the publish-queue below.

## Live verification (2026-06-18, cycles 1-19 post-publish), founder published everything, I swept the live app

The founder published all cycles through 19; I logged into the live app (`cadence-flow-beta.lovable.app`, demo account, Project Glasswing · Lumen workspace) and swept the shipped surfaces with Playwright. **Result: everything checked is working.** Two findings below, neither a code bug.

| Feature (cycle) | Result | Evidence |
| --- | --- | --- |
| Login + Today + R3 bell (15) | OK Works | Login on-brand; Today renders the command-center (9 calls, ICE priorities, brief, tasks). The "Attention: 5 things need you" bell deep-links to `/govern?tab=attention`. |
| U6 workspace export (1) + selective (6) | OK Works | Settings > Data shows the export card + all 7 per-section checkboxes (default-checked) + "Download workspace export". (Did not trigger the file download or exhaust the disable-guard; server logic is gate-tested.) |
| Critic verdict (DEC-02) | OK Works | Opening opportunity #1 surfaces the Critic teardown: Revise verdict, 4 risks, kill criteria, missing evidence, confidence 60% + model. |
| R3 Attention feed (2) | OK Works | Engine Room > Attention renders 5 "Needs you" cards, matching the bell count exactly, each deep-linking to approvals. |
| P7 Incidents (3) | OK Works | Engine Room > Incidents shows the "mission.dispatch failed" Execution incident with detail, timestamp, working "View trace". |
| C4/E7 Agent inspector + memory (4, 5) | OK Works | Missions > Agents > inspector: agent dropdown (13), "Recent runs" (waiting_approval / completed with mission+step+time), and "What this agent knows" with a real private reflection memory. |
| AMBIENT-ARC trust dial | OK Works | Per-agent observing/proving/trusted/ambient with trust scores + sample counts. |
| D4 mission cancel control (10) | OK Works (presence) | A running mission shows status "running" + a "Cancel mission" button. Not clicked (destructive on shared demo data). |
| Memory-as-moat card | OK Works | The mission detail "Compounding · the moat at work" card shows "2 prior memories this mission drew on" with the actual memories + full execution trace (thought/action log) + 2s live refresh. |
| F3 per-product clustering (17) | OK Works (fully) | Signals tab scopes to the active product live: Test active = "No signals yet" / "Cluster 0" disabled; switch to Lumen = its 5 themes (Unsafe escalation conf 91, Slow first-response 82, Tone too formal 74, ...) + "Cluster 2". Scope flips on product switch. |
| O1 provenance on spec/PRD (18) | OK Works (empty-state) | `/prds/<id>` shows the "Why this spec · source evidence" card with the correct empty state ("No source signals traced. This spec was added directly..."), right for a directly-seeded spec. |
| O1 provenance in the Lineage drawer (19) | OK Works (guard correct) | "Signal lineage" opens the drawer cleanly ("Came from" / "Became"); the "Traces back to" section is correctly absent because the seeded opportunity has no lineage edges (guard `signal_count > 0 && depth > 1`). My cycle-19 change does not break the drawer. |

**Finding 1 (transient, not reproducible):** `/product?tab=specs` rendered empty once with a single console error during the deploy window (right as the founder was publishing); a clean reload rendered it correctly (both seeded specs with all actions). Most likely a deploy/hydration hiccup during the publish, not a code bug. Noted to watch; could not reproduce.

**Finding 2 (seed-data gap, NOT a code bug), queued below:** the demo seed does not populate `artifact_lineage` edges, so the Lineage drawer AND O1 provenance (cycles 12 / 18 / 19) always show their empty states on demo accounts ("No upstream artifacts" / "No source signals traced"). The features are correctly wired and degrade gracefully, but the demo cannot *show off* lineage / provenance / the memory-moat narrative. Recommend seeding a lineage chain (signal → theme → opportunity → prd → task) so demos exercise the populated state. This is a seed migration (DB change to `seed_demo_workspace`) + a demo-quality call; queued for the founder rather than changed unilaterally on the shared demo seed.

**Not separately re-tested this sweep (lower priority / cost-gated):** F3 always-fresh feed 30s poll (11), behavior is a timed poll, not snapshot-friendly; O1 provenance card on the opportunity drill (12), same `getProvenance` proven by 18/19, shows empty-state on this data; LCH-01 launch kit (14), generating it spends AI tokens (founder-gated spend), so not triggered; P7 guardrail-block incident (16), needs a real `action=block` event to populate (deployed-correct per the cycle-16 verify). The Incidents tab *description* copy fix (`875856fc6d`) is now published with this batch, so it now reads "guardrail blocks".

## Pending published-app verification (needs you to publish, then I verify)

The live app does not reflect this run's changes until you publish them. These items are code-complete and gate-green (tsc + build + lint + adversarial review) but not yet verified on the live app. Per playbook section 13, I do not test this run's unpublished changes against the live app (I may test behavior already deployed). Each item is dated and classified: "needs publish first" (this run's new work) or "quick-check now" (already-live behavior you can verify in a moment). When you publish, tell me and I will run the checks.

> **Superseded 2026-06-18:** the founder published all cycles through 19, and the live sweep above verified them on the published app (all working; two non-bug findings logged). The rows below are retained as the historical pre-publish checklist; the live-verification section above is the current record.

| Date noted | Item | When it can be verified | What to check |
| --- | --- | --- | --- |
| 2026-06-19 | WM-M5 tier limit gates | Needs publish first + the flag flip (new this run, ships dormant) | After publishing the migration AND running `select public.set_config(...)` / replacing `limit_gates_enabled()` to return true (only once Stripe upgrade is live): on a free account, confirm creating a 3rd product is blocked with "Product limit reached for this plan (2 allowed)..." and a 2nd workspace is blocked. While the flag stays false (default), confirm product/workspace creation is unchanged. Mechanism already behaviorally dry-run-verified on prod (rolled back); this live-check just confirms post-publish + post-flip. |
| 2026-06-18 | U6 workspace export | Needs publish first (new this run) | Settings > Data > "Download workspace export". Confirm the JSON has the expected sections with non-zero counts on a seeded workspace, and that another workspace's data is not present. |
| 2026-06-18 | R3 Attention feed | Needs publish first (new this run) | Engine Room > Attention. With a pending approval (or a near-cap budget), confirm a severity-coded card appears and links to the right tab; confirm "All clear" when the loop is clean. |
| 2026-06-18 | P7 Incidents log | Needs publish first (new this run) | Engine Room > Incidents. With a failed tool execution present, confirm it appears with the error detail and a working "View trace" link; confirm "No incidents" when clean. |
| 2026-06-20 | DBR Ambient Precedent (3 seams) | Needs publish first (new this run) + seeded outcome data | On the seeded demo workspace, open an opportunity/spec/Critic review that resembles a PAST shipped outcome and confirm the precedent nudge appears with the relevant past verdict + a clean spec title; open one with NO similar history and confirm it shows nothing (calm, never noise). Confirm the Critic verdict on a similar bet references the past outcome in its risks/missing-evidence. If precedent looks noisy or too sparse, the `PRECEDENT_THRESHOLD` (ships at 0.3) is the dial to tune. UI breadcrumbs: an opportunity `?opp=` drill, a spec detail page, the Critic verdict sheet. |
| 2026-06-20 | PROVIDER-FALLBACK auto cross-model degrade | Needs publish first + the flag flip (ships OFF, byte-identical) | With the flag OFF (default) there is nothing to verify - the chokepoint is byte-identical to before (a lone `fallbackModel` still behaves as today; this was offline-verified). To verify the NEW resilience: set `AI_PROVIDER_FALLBACK=1`, publish, then induce a primary-model failure (e.g. a bad/forced gateway error on a non-eval/non-embed surface) and confirm the call degrades to the cheapest live model and returns a response instead of hard-failing, with `fallback=true` + `modelUsed` = the fallback in the `ai_events` row; and confirm an `eval`/`embed` surface does NOT auto-degrade. The ordered-chain logic itself is already ✅ unit-verified (10 tests). UI breadcrumb: none (chokepoint). |
| 2026-06-18 | C4/E7 Agent inspector | Needs publish first (new this run) | Missions > Agents > Agent inspector. Pick an agent, confirm its recent runs AND its memory ("What this agent knows": private + shared) render correctly; confirm `agent_memory.agent_id` matches the swarm agent id so private memories attribute right; confirm the empty states. |
| 2026-06-18 | U6 selective export | Needs publish first (new this run) | Settings > Data: uncheck some sections, export, and confirm the JSON contains only the checked sections (and the button disables when none are checked). |
| 2026-06-18 | LCH-01 launch kit | Needs publish first (new this run) | Open a Build session with a committed changeset that has release notes (`/build/$missionId` Changes tab). Click "Draft launch kit"; confirm 5 grounded artifacts render (changelog, blog, email, social, docs), each copyable and free of em dashes / AI clichés; confirm nothing is sent. |
| 2026-06-18 | O1 provenance | Needs publish first (new this run) | Open an opportunity (`/product?opp=`) that was promoted from clustered signals; confirm the "Why this · source evidence" card lists the root source signals with working links, and shows "No source signals traced" for a directly-added opportunity. |
| 2026-06-18 | F3 always-fresh feed | Needs publish first (new this run) | On `/product?tab=signals`, with the tab focused, send a signal through the ingest webhook (or another session captures one) and confirm it appears within ~30s without a manual reload. Confirm polling pauses when the tab is unfocused (no needless requests). |
| 2026-06-18 | R3 global Attention bell | Needs publish first (new this run) | With at least one item needing you (a pending approval, a near-cap budget, or a stalled run), confirm the TopBar bell shows a tinted count on every screen and that clicking it opens Engine Room > Attention. Confirm the bell is quiet (no badge) when the loop is clean, and that it never errors the top bar even if the notifications fetch fails. |
| 2026-06-18 | P7 guardrail-block incidents | Needs publish first (new this run) | Trigger a blocking guardrail rule (an `action=block` rule on input or output), then open Engine Room > Incidents and confirm a "Blocked by guardrail: <rule>" card appears (Guardrail kind), with no trace link and no raw matched content. Confirm a `warn`/`redact` rule does NOT create an incident (those are not failures). |
| 2026-06-18 12:20 | F3 per-product clustering | Needs publish first (new this run) | On `/product?tab=signals` with two products: capture a signal under product A, switch to B and confirm it is absent, switch back to A and confirm it shows. Cluster under A and confirm the theme appears only under A. Confirm the all-products view (no product selected) shows everything including webhook signals. Confirm switching products with no signals yet shows an empty feed (not an error). |
| 2026-06-18 12:48 | O1 provenance on spec/PRD | Needs publish first (new this run) | Open a spec at `/prds/<id>` that was generated from an opportunity promoted from clustered signals; confirm the "Why this spec · source evidence" card lists the root source signals with working links. Open a spec authored from scratch and confirm it shows "No source signals traced." |
| 2026-06-18 | O1 provenance in the Lineage drawer | Needs publish first (new this run) | Open the Lineage drawer on a deep artifact (a spec or a task). Below "Came from", confirm a "Traces back to" section lists the root source signals with working links. Confirm it is absent on a theme (its immediate parents already are signals, depth 1). On an artifact tracing to more than 8 root signals, confirm a "+N more source signals" line appears under the list. |
| 2026-06-18 | D4 mission cancellation | Needs publish first (new this run) | Start a mission so it is running, open `/missions/$id`, click **Cancel mission** + confirm. Verify: the badge flips to `cancelled` and the Advance button disappears; on the next cron tick the mission stays `cancelled` (not resumed) and its steps/runs read `cancelled`; for a Build mission, its `builder_file_claims` are released (a later build on the same paths is not blocked); any pending approval clears from the Attention feed. Logic is gate-verified offline; this confirms the end-to-end runtime behavior. |

## Build priority (founder ruling, 2026-06-18 03:33)

**Core functionality first; pricing + customer-announcement pages are second.** The next pick must be a CORE platform item: the **discovery engine** and the core Sense → Decide → Plan → Build → Launch → Learn loop. Top core candidates to verify-then-build: `F3` (continuous/always-fresh discovery feed + incremental re-cluster, extends `discovery.functions.ts`) and `O1` (typed knowledge graph + "why is this on the roadmap?" query). Second priority (build only after core is advanced): `M-C-PRICE`/pricing, `L2` customer-announcement pages. Rule lives in the playbook §3. This supersedes the "highest-value next steps are founder-led" read below for the autonomous loop's pick order: keep building core, do not idle.

## Notes and comments

- **Cycle 1 shipped U6 (core)** (above). Picked autonomously as the top buildable, file-disjoint, no-migration item (P0/P1-tagged trust escape-hatch), avoiding the parallel session's PLG and Gauntlet lanes.
- **Cycle 2 shipped R3 (core)** (above): the in-app Attention feed on the Engine Room. Top remaining buildable, file-disjoint, no-migration item; it derives from existing loop state, so it works the moment you publish.
- **Cycle 3 shipped P7 (core)** (above): the read-only Incidents log on the Engine Room. Derived from existing failure logs, no migration; folded a tenancy fix (explicit workspace scoping on `event_queue`).
- **Cycle 4 shipped C4/E7 (core)** (above): the Agent inspector on Missions > Agents. Read-only run history per agent, no migration; review came back clean (no fix needed).
- **Cycle 5 completed C4/E7** (above): added the memory inspector ("What this agent knows": private + shared/global), via two injection-safe queries. C4/E7 is now done. Note: the clean read-only, no-migration backlog is thinning; remaining items lean toward migrations, hot-surface chrome, or loop-coupling, which the loop will weigh against the section-14 design pass.
- **Cycle 6 shipped U6 selective export** (above): per-section checkboxes on the export. Clean, no migration.
- **Recommendation on the §14 design pass:** it is the natural next phase, but it needs visual verification, which the no-published-app rule rightly defers to you. I am not running a code-only design pass unattended (it would be a weak version, and unverified UI changes are risky). Best run it with me when you are back and can see the rendered UI, or publish so I can verify live. Meanwhile I keep taking safe, gated build items.
- **Cycle 7: self-correction + F-HUMANIZE-HOOK** (above). The repo's own `check-humanized.sh` caught 7 em-dashes in this session's comment headers; fixed. Also completed F-HUMANIZE-HOOK (the scanner existed but was not wired pre-commit; the dashboard ⬜ was stale). This is the kind of safe, no-visual, launch-quality work the loop can keep doing while the clean feature backlog is thin.
- **Permission mode:** "accept edits on" plus the command allowlist in `.claude/settings.local.json` is sufficient; no "bypass" needed. Every commit, push, and build this run ran with no prompt. Documented in playbook section 11.
- **Published-app rule (new, playbook section 13):** no live testing during the run; code-complete items needing live verification are listed above for when you publish.
- **Cycle 19 shipped O1 provenance in the shared Lineage drawer** (above). On resuming this session, the worktree carried uncommitted in-progress work from the prior session's mid-build rollover (the safety-net case in playbook §7): a coherent extension of O1 to the `LineageDrawer`. Verified the field contracts and the `signal` route all check out, finished it, and folded one adversarial fix (the "+N more" overflow line). The same cheap `getProvenance`-reuse pattern as cycles 12/18; no new server fn, no migration. Note (state reconciliation): this session was opened in the main-repo root, whose local `main` was 22 commits behind `origin/main`; the worktree was current at `origin/main` (cycle 18). No work was lost; resumed cleanly in the worktree.
- **Cycle 19 shipped F-AGENTS-MENTIONABLE** (above): the composer mention dispatch affordance for direct specialist dispatch. Type `@agentslug goal` to skip the orchestrator's planning round-trip and dispatch straight to the named agent. Regex mention parsing (word-boundary safe, email-safe), roster lookup (enabled only, O(1) per candidate), and pre-planned single-step DAG creation + advanceMissionCore dispatch. The existing deterministic advance cron + maybeCompleteMission (steps.length > 0 gates) handle all paths uniformly — no lifecycle changes needed. Bare mention (no goal) falls back to regular chat. Regex and strip are safe (slug-constrained). Tsc/build/lint green, gate-verified.
- Run scaffolding (cycle 0): this report, the [playbook](../operations/autonomous-build-loop.md), the permission allow/deny set, and the isolated worktree.
- The parallel session is active on `main`. Isolation via worktree means no collision; work lands on main by fast-forward push (already rebased-and-retried twice cleanly).
- **Cycle 10 shipped D4 mission cancellation** (above). The notable bit: the "next buildable item" search kept hitting already-built work (H1-TASKS, P3), so this cycle proved that the right move when an item looks risky is *deeper recon, not deferral* — D4's cancellation slice turned out to need zero loop changes and was fully gate-verifiable, contradicting cycle 8's blanket "unsafe to build blind." Built it, gate-green, adversarially reviewed (TOCTOU guard + an orphaned-file-lock catch). Reconciled `H1-TASKS` ✅. Next pick: `F3` (continuous discovery) per the founder's core-first ruling.
- **Cycle 11 shipped F3's always-fresh feed** (above). The honest scoping call: F3 has two halves. The *always-fresh feed* (auto-refresh so continuously-ingested signals surface live) is safe and additive, so I shipped it. The *continuous auto-cluster* (a `discovery-tick` cron) commits recurring AI spend, which the autonomous contract treats as founder-gated, so I queued it (Skipped table) with a one-line yes/no for you rather than enabling spend unilaterally. This is the loop doing the safe core work now and surfacing the one real decision (spend posture) instead of guessing.
- **Cycle 17 shipped F3 per-product clustering** (above), the founder's named top core candidate, after the founder directed the loop to keep building down the priority list (skip only founder-gated) and stop self-pausing (the no-self-pause rule is now codified in the playbook §8/§9). Recon found the `signals`/`themes` tables already carried `project_id`, so per-product scoping needed **no migration**, only the app starting to use it. Built back-compatibly (no product active = unscoped, as before) so the blast radius is bounded. This corrects the prior leg's premature "backlog exhausted": there is real core work to build.
- **Cycle 16 shipped the P7 guardrail-block incident source** (above). A clean, backend-only, additive extension of an already-shipped feature, the most reliable kind of find now that the clean feature backlog is thin: a named P7 remainder ("guardrail/cost incident sources") that reads an existing table (`guardrail_hits`), needs no migration, and is fully gate-verifiable offline. The judgment call (surface only `block`, not `warn`/`redact`, and never the raw matched payload) is documented in `p7-incidents.md`.
- **Cycle 15 shipped the R3 global Attention bell** (above). With the clean feature backlog thin, this was the strongest remaining buildable, file-disjoint, no-migration, gate-verifiable-offline item: a named R3 remainder ("global bell badge") that genuinely closes a gap (spend/stall alerts had no surface outside the Engine Room). Recon ruled out the discovery-engine alternatives for an unattended cycle (F3 per-product clustering needs a signals/themes `product_id` migration; O1's graph explorer wants visual verification the no-published-app rule defers). The one real design call (avoid double-counting with the existing Approvals badge) is logged in `session-decisions.md`.
- **Repo-wide lint observation (flag for the founder):** `bun run lint` reports ~4000+ prettier/prettier formatting errors across many files I did not touch (`vite.config.ts`, `ingest-signals.ts`, `drift-tick.ts`, and others). This is pre-existing and almost certainly environmental: the worktree resolves the parent repo's `node_modules`, and the founder's in-flight `package.json` change likely shifted the installed prettier so it disagrees with how the tree was last formatted. The loop does NOT mass-reformat others' files (that would be a huge unrelated diff and risks the wrong prettier version); instead each cycle lints only its own touched files (clean). Worth a `bun install` + `bun run format` pass by the founder when convenient to clear the debt.
