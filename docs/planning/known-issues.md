# Known issues — live tracker

> _Created: 2026-06-11 · Last updated: 2026-06-20_

> **SSOT first.** The single front-door tracker is [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) (status, build queue, founder rulings, findings, progress). This file is the live KI- bug/blocker tracker it points to, not the tracker to follow day-to-day.

> **What this is.** The live register of open bugs, blockers, and workarounds — with stable `KI-` IDs. This is the repo's `KNOWN_ISSUES.md` (per the constitution concordance in [`../../Ai_Cofounder.md`](../../Ai_Cofounder.md)) and, until 2026-06-22, it doubles as the **M1 Golden Path demo punch list**.
>
> **Update rule.** Add a row the moment an issue is confirmed; flip status in the same commit that resolves it. An issue is not "known" until it is in this table. Standing design gaps (not bugs) stay in [`considerations.md`](./considerations.md).
>
> **Related:** [`feature-backlog.md`](./feature-backlog.md) (Live status board · Blocked/stuck), [`considerations.md`](./considerations.md) (gap register), [`archive/foundation-audit.md`](./archive/foundation-audit.md) (point-in-time audit), [`../operations/fnd-runtime-restart-playbook.md`](../operations/fnd-runtime-restart-playbook.md), [`../../plan.md`](../../plan.md) §4.

## Open

> **2026-06-14: all pending migrations are applied via the Lovable sync.** The migration-gated rows below (KI-09, KI-12 migration part, KI-13, KI-14, KI-17) are now applied on the live DB; verify live where noted. Their per-row wording predates the sync.

| ID    | Issue                                                                                                                                                                                                                                                               | Impact                                                                                                                                          | Unblocks when                                                                                                                     |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| KI-01 | Calendar per-user OAuth is blocked — awaiting two provider client-ID secrets (Google/Microsoft) to be set as wrangler secrets.                                                                                                                                      | Calendar tab of `/knowledge` can't connect per-user accounts; demo uses seeded data.                                                            | Founder provisions the two provider client IDs and sets the secrets.                                                              |
| KI-02 | FND-RUNTIME durable-resume is unverified — the foundation-audit row stays amber until an operator-run kill-test proves checkpoint resume with no duplicate writes.                                                                                                  | Long missions may not survive a worker restart; risk for live demos.                                                                            | Operator runs [`../operations/fnd-runtime-restart-playbook.md`](../operations/fnd-runtime-restart-playbook.md).                   |
| KI-03 | Lovable's Knowledge field is stale — `.lovable-config.txt` was updated 2026-06-11 (constitution + read order) but takes effect only when re-pasted into Lovable Settings → Knowledge.                                                                               | Lovable sessions won't see the constitution or updated read order until re-pasted.                                                              | Founder re-pastes the file contents into Lovable Settings → Knowledge.                                                            |
| KI-06 | Local `.env` has no AI-gateway key (`LOVABLE_API_KEY`) — only Supabase vars. Found during the v5 walkthrough 2026-06-11. **Mitigation shipped same day:** `resolveGateway()` fallback routes `google/*` models via `GEMINI_API_KEY` when the Lovable key is absent. | Until a key is set, model paths are dead in local dev (brief, missions, Critic). DB paths work fine. Embeddings remain Lovable-only either way. | Founder adds `GEMINI_API_KEY` (free, aistudio.google.com) **or** `LOVABLE_API_KEY` to local `.env`, then restarts the dev server. |

| KI-09 | `F-V5-INGEST-WEBHOOK` migration (`20260611190000_f_v5_ingest_webhook_tokens.sql`, idempotent) committed, awaiting Lovable sync to apply + deploy the frontend. | Webhook door inert (token fns error, endpoint 401s) until Lovable syncs. | Founder opens the Lovable project (same proven path as KI-08). |
| KI-10 | Public ingest endpoint has **no rate limiting** — a valid token allows unlimited POSTs (≤50 signals each), and every insert can fan out to the auto-discovery pipeline (AI cost). | A leaked token could drive signal spam + agent runs; budgets/kill-switch are the backstop, token rotation the mitigation. | Add a per-token rate cap (e.g. N requests/min) post-demo; tokens stored plaintext — consider hashing then too. |

| KI-11 | The repo lives in iCloud-synced `Documents/`; macOS evicted `node_modules` file contents once ("dataless" files — sizes intact, reads return 0 bytes), breaking ESLint/Vite until `rm -rf node_modules && bun install`. | Random tool breakage that looks like corruption; can recur any time iCloud optimizes storage. | Exclude the repo (or at least `node_modules`) from iCloud optimization, or move the repo out of `Documents/`. Workaround when it strikes: reinstall deps. |
| KI-12 | `F-CONN` migration (`20260612080000_f_conn_connector_platform.sql`, idempotent) committed, awaiting Lovable sync to apply + deploy. GitHub App connect additionally gates on founder registering the app (checklist in the SSOT section 0, the live cursor, incl. PKCS#8 key conversion). | Connections/bindings UI inert until sync; GitHub card shows "setup pending" until app secrets exist. Env-var fallback keeps all current GitHub behavior working meanwhile. | Founder opens Lovable (sync) + registers the GitHub App + adds the 6 secrets. |
| KI-13 | 🔁 **Standing watch; currently resilient.** Signup needs a resilient `handle_new_user` (each seed step in its own `BEGIN..EXCEPTION` subtransaction so a failing seed logs a WARNING and signup still completes). Lovable's schema model has regenerated this trigger BOTH ways: the **2026-06-16 sync (`20260616165601`) shipped a FRAGILE bare-INSERT body**, which 500s signup (`Database error saving new user`) on any seed failure; the **2026-06-17 sync (`20260617125218`) regenerated it RESILIENT again**. Re-fixed defensively by `20260617140000`, which is the last-applied `handle_new_user` migration, so the live trigger ends up resilient regardless of sync order. | When a fragile body is live, NO new account can be created platform-wide; blocks the SCN-01 "founder registers" step. As of the 06-17 sync the trigger is resilient. | **Standing watch:** because the model has produced both bodies, re-verify a fresh signup after any sync touching the auth trigger and re-apply `20260617140000` (idempotent) if a fragile body slipped in. Permanent fix = own the Supabase backend (runbook [`../operations/auth-backend-migration-runbook.md`](../operations/auth-backend-migration-runbook.md), deferred). Verify live after the next Publish. |
| KI-14 | ✅ **FIX LANDED 2026-06-14** (`20260614160000`, applies on next Lovable sync) — **Eval score scale was mixed**: `eval_runs.avg_score` / `eval_case_results.score` were `numeric(4,3)` (0–1) while the runner writes 0–100 and `pass_threshold` is 0–100, so a live run ≥ 10 overflowed and seeded 0–1 read "score 1 · below gate 80". | Standardized on **0–100**: widen both columns → `numeric(6,3)` (precision-lossless) + rescale existing 0–1 rows ×100 across `eval_runs`, `eval_case_results`, AND the derived `drift_snapshots.avg_eval_score`; the drift UI formatter renders the 0–100 integer. No app-logic change (trust reads `ai_evals`; drift uses a scale-invariant pct-delta) — verified by a 3-lens adversarial review (12 findings). | **Residual (operator-gated):** `seed_demo_workspace` still emits 0–1 on a manual re-seed — `docs/operations/demo-credentials.md` now warns + ships the corrected re-seed (seed, then the same three ×100 normalizations). |
| KI-15 | **Zero-step mission with a stale unconsumed handoff message can sit `running`** — `maybeCompleteMission` returns early while any `agent_messages` row for the mission is unconsumed (pre-existing message-queue guard). A non-orchestrated single mission whose handoff was never consumed (e.g. a `consumeInboundHandoff` read-race) has no path to completion; the v6 P1 sweeper now calls `maybeCompleteMission` every tick but only no-ops. | A rare single mission lingers `running` indefinitely (cosmetic — no runaway work). | ✅ RESOLVED 2026-06-14: `sweepStaleUnconsumedMessages` (`mission-advance.server.ts`, wired in `advanceMissionCore`) completes an orphaned mission after a 15-minute unconsumed-message timeout, gated on no live receiver run remaining so a starved receiver keeps its handoff payload (a review catch). Adversarial-reviewed safe-to-land. |
| KI-17 | ✅ **FIX LANDED 2026-06-14** (`20260614180000`, applies on next Lovable sync) — **The prototype `/p/$slug` surface had the same anon-leak as the decisions viral loop**: `prototypes` / `prototype_files` granted anon a TABLE-WIDE SELECT (`20260602204826`) and their public-read policies had no `TO anon` scope, so anon could read owner `user_id`/`prd_id`/`project_id` via a direct PostgREST call, and the unscoped policies ORed into authenticated reads (cross-tenant). | Anon owner-id leak + cross-tenant read on a LIVE surface — now closed. | Fixed by mirroring the decisions hardening: COLUMN-scoped anon grants (safe columns only), policies scoped `FOR SELECT TO anon`. Neither table was in the realtime publication. No app change (`/p/$slug` already selects only safe columns). Security-verified SAFE TO LAND. |
| KI-16 | **Mission auto-advance is capped at 20 running missions/tick** (`MISSION_BATCH` in `resume-runs.ts`), ordered by `updated_at` asc. A workspace with > 20 simultaneously-running missions advances the oldest-untouched first; the rest wait a tick. | High-scale only — at demo/early scale (a handful of concurrent missions) zero effect. | ✅ RESOLVED 2026-06-14: `MISSION_BATCH` is now env-tunable (`MISSION_ADVANCE_BATCH`, default 50, floor 1) in `resume-runs.ts`, with oldest-updated-first ordering preserved. |
| KI-20 | Two agent-resolution surfaces still ignore the `enabled` flag after KI-19 closed the dispatch chokepoint: the run-start lookup in `loop.server.ts` (an already-queued child run whose target was disabled in the gap between dispatch and execution) and the operator-initiated `runAgent` in `agents.functions.ts` (selects by id, no enabled filter). | Narrow and low-impact: KI-19 closed every NEW off-roster dispatch; these are the already-queued-then-disabled race and an explicit operator action (RLS-scoped to the owner). | ✅ RESOLVED 2026-06-14: both fresh-dispatch surfaces (`runAgentLoop` in `loop.server.ts`, `runAgent` in `agents.functions.ts`) now refuse a disabled agent with a clear error before any run row; an enabled agent is unchanged. The one remaining path, the `resumeAgentLoop` sweeper-resume, is tracked as KI-21. |
| KI-21 | `resumeAgentLoop` (`src/lib/ai/loop.server.ts`, the resume-runs sweeper-resume path) selects the agent by id and resumes WITHOUT re-checking `enabled`. After KI-20 closed the two fresh-dispatch surfaces, this is the one remaining path: a child run queued while its target was enabled, then disabled, then resumed by the sweeper, still executes. | Narrow and benign: `resolveAgent` already blocks disabled agents from being freshly enqueued, so only a disable-after-enqueue race reaches here. | Add the same `enabled === false` re-check at resume in `resumeAgentLoop`. Surfaced by the KI-20 review. |
| KI-22 | ✅ **RESOLVED 2026-06-15 (verified live)** (true fix: `loop.server.ts` `model:` removal, commit `f1d4e8df41`; plus secondary hardening in `orchestrator.server.ts` + `handoff.server.ts`). **Every orchestrated mission completed HOLLOW (0 steps, 0 hops) yet was labeled "completed".** Proven by a live trace (`6471cbaa`, 10 hops, 21,183 tokens): `mission.plan`'s planner emits a colloquial slug (`discovery`) but the seeded roster slug is `discovery-scout`, and the strict exact-match validator threw, dropping the whole plan; the cron then completed the 0-step mission. Governance-halt theory ruled out (kill-switch off, real tokens spent); the 2026-06-14 approval-gating fix is verified working live (no new control-flow approvals queued). | No orchestrated mission could run end to end. This was the M-0 blocker. | Fixed in two layers: (1) `mission.plan` resolves each planned slug against the live roster (exact, then a single unambiguous prefix or substring match) so `discovery` maps to `discovery-scout`; (2) `maybeCompleteMission` marks a never-planned 0-step terminal mission `failed` (retryable), never hollow-`completed`, and skips decision-capture. **TRUE ROOT CAUSE + VERIFIED 2026-06-15:** the actual blocker was upstream of both — `loop.server.ts` inserted a `model:` field into `agent_runs` (a column the table does not have), so the orchestrator's launch INSERT was silently rejected by Postgres (no run row, no trace, no error surfaced). Lovable removed `model:` from both insert sites (commit `f1d4e8df41` on `main`), the founder Published, and it is VERIFIED LIVE: mission `973800a8` planned a real 6-step DAG, persisted steps, and dispatched a handoff to the Strategist (cost $0.02, trace `bd4f5677`, executed unattended). The slug-resolver and loud-fail guard are real hardening but were secondary. RESOLVED. |
| KI-24 | ✅ **SEED-LAYER FIX LANDED 2026-06-18** (`onboarding.functions.ts`, no migration; applies on next Lovable publish). **Onboarding track-pick crashed every new user** with _"new row violates row-level security policy for table 'projects'"_ — the published demo could not get past Setup step 1. ROOT CAUSE: `seedWorkspaceForTrack` inserted `projects`/`signals`/`opportunities` without `workspace_id`, leaning on the `current_user_default_workspace()` column default; the tenancy retrofit (`20260530120200_tenancy_c`) made those tables' write RLS membership-keyed (`is_workspace_member(workspace_id)`). A brand-new user reaches onboarding (first write) before a `workspace_members` row reliably exists — `ensure_default_workspace` runs in a swallow-all block in `handle_new_user` (KI-13), is NOT in our migrations (Lovable-managed/drift-prone), and demo accounts skip it — so the default resolved NULL and `is_workspace_member(NULL)` was false. | Demo-blocking: no new account could enter the app; every track button failed identically. | Fixed at the seed layer: new `ensureDefaultWorkspace(supabase, userId)` resolves-or-creates the workspace + owner membership (RLS permits the user-scoped client) and every insert sets `workspace_id` explicitly. `tsc`/`eslint`/`build` green. **Residual (related to KI-13):** the deeper signup-provisioning fragility (`ensure_default_workspace` absent from repo migrations + swallowed in the trigger) is unfixed — other first-writes outside onboarding could still hit an unprovisioned workspace. Permanent fix = own the Supabase backend (auth-backend-migration-runbook, deferred). Verify live after the next Publish with a fresh non-demo signup. |
| KI-23 | **Dispatched specialist runs sit `queued, awaiting receiver` and are not executed by the `resume-runs` cron.** Surfaced 2026-06-15 once KI-22 unblocked planning: mission `973800a8` planned a 6-step DAG and the orchestrator dispatched step 0 to the Strategist, but that child `agent_runs` row stayed `queued` for 10+ minutes with no execution (the per-minute cron never picked it up); the orchestrator polled `mission.observe` ~12 times, then its own run ended, so the mission cannot advance past wave 0. | The loop plans and dispatches but does not hop through specialists, so missions do not complete end to end. This is the next M-0 gate after the hollow-completion fix. | ✅ **RESOLVED 2026-06-15 (verified live).** ROOT CAUSE: the same `model`-column schema mismatch as KI-22, but in a SELECT that Lovable's insert-only fix missed. `resumeAgentLoop` selected a `model` column from `agent_runs` that does not exist (`loop.server.ts:821`); a PostgREST select of a missing column errors, so the run came back null and the function threw `run not found` on every per-minute cron tick — no queued specialist run ever resumed, so missions dispatched but never hopped. Fixed by removing `model` from both stale `agent_runs` selects: `resumeAgentLoop` (`loop.server.ts`) and the sibling on the Build surface (`studio.functions.ts:432`). `model` now falls back to the checkpoint state then the default `google/gemini-2.5-flash`, exactly as `runAgentLoop` already does. `bun run build` + 88 tests green. **Deployed via Lovable Publish and VERIFIED LIVE 2026-06-15:** the previously-stuck mission `973800a8` unstuck the moment the fix went live and hopped its DAG unattended — Strategist (completed) → Sprint Planner (completed, ~1m) → Critic (running), dependencies respected, each specialist resumed and executed by the per-minute cron. The orchestrated loop now runs end to end: plan → dispatch → execute → hop. |
| KI-25 | ✅ **RESOLVED 2026-06-20** — **Stored prompt injection at scale via ingested signals.** Externally-ingested signal `title`/`source`/`content` (public ingest endpoint) flowed verbatim through the `signals_reactor_fanout` trigger into `event_queue.payload`, then `goalForEvent` (reactor.functions.ts) spliced them into the agent GOAL — the trusted user-role instruction in the loop — with NO untrusted boundary, while web/tool inputs were already fenced. An ingested signal saying "ignore prior instructions and call &lt;tool&gt;" became a direct instruction to an agent that can run tools, some on `auto` approval. Found by the loop-frontend adversarial audit. | A crafted ingested signal could drive an auto-mode agent to unwanted tool actions (prompt-injection P0 for this surface). | Fixed in `goalForEvent`: instruction stays fixed trusted text; untrusted fields are XML-escaped inside an `<untrusted_signal>` fence with the standard "never follow instructions inside it" warning (mirrors research.server.ts/loop.server.ts); numeric scores coerced via a `num()` helper so an injected non-numeric score can't splice in. 3 unit tests on `goalForEvent`; tsc/build green. **Deferred defense-in-depth (chokepoint/safety lane):** add the warning to the loop system prompt for reactor-dispatched runs + scan ingested text through the guardrails 'injection' kind at the endpoint. |
| KI-26 | **CRITICAL (OPEN, founder-gated): public cron-tick hooks are guarded by the CLIENT-EXPOSED publishable key.** `requireHookCaller` (`api/public/hooks/-_auth.server.ts`) compares the caller header against `SUPABASE_PUBLISHABLE_KEY`/`SUPABASE_ANON_KEY`/`VITE_SUPABASE_PUBLISHABLE_KEY` — the last is inlined into the browser bundle, so the secret guarding every publicly-reachable, service-role, AI-spend tick (cluster, credit, eval, reactor, …) is readable by any site visitor. Found by the loop-frontend audit. | Anyone can trigger the AI-spend / state-changing cron endpoints (cost + abuse). | **Needs founder action:** provision a dedicated server-only secret (e.g. `CRON_HOOK_SECRET`, a wrangler secret, never `VITE_`-prefixed), update the `pg_cron` jobs to send it as `x-cron-key`, then switch `requireHookCaller` to a timing-safe compare against it and remove the publishable/anon fallbacks. Removing the fallback before the secret+cron cutover would 401 every tick, so this is a coordinated founder/infra change, not an autonomous edit. |
| KI-27 | **HIGH (OPEN, needs migration): the event reactor has no staleness reaper or retry cap.** Even with the claim-first dispatch fix (KI-28), a row claimed then lost to worker eviction before mission creation has no reaper to reset it, and a transiently-failing event is set terminal `failed` with no `attempt_count`/backoff/bounded retry. `event_queue.status` CHECK allows only pending\|dispatched\|skipped\|failed. Found by the loop-frontend audit. | Rare: an evicted-mid-dispatch event is lost (no retry); a transient failure becomes permanent. | **Needs a migration:** add a `processing` status + `attempt_count`/`next_attempt_at` columns, a reaper that resets rows stuck `processing` past a TTL, and bounded exp-backoff retry (cap ~3). Deferred to a founder/attended migration cycle. |
| KI-28 | ✅ **RESOLVED 2026-06-20** — **Event-reactor double-dispatch race.** The cron tick (`event-reactor-tick.ts`) SELECTed status='pending' rows then ran the full runAgentLoop in `dispatchEvent` BEFORE flipping status, leaving the row 'pending' for the whole (>60s) run; with pg_cron firing every minute, two overlapping ticks re-selected and re-dispatched the same event → duplicate missions + duplicate billed agent runs. `decideEventDispatch` (operator confirm path) had the same read-then-act gap. Found by the loop-frontend audit. | Duplicate missions + duplicate AI billing per overlapping tick / concurrent approve. | Fixed migration-free with a claim-first CAS reusing the existing 'dispatched' status: before dispatchEvent, `update({status:'dispatched',...}).eq('id',id).eq('status','pending').select('id')` — only the claimer proceeds; losers skip. Applied in both the cron tick and `decideEventDispatch` (approve + reject both atomic). tsc 0, build green. No migration. The deeper 'processing' status + staleness reaper + retry cap is KI-27 (migration, deferred). |
| KI-29 | ✅ **RESOLVED 2026-06-20** — **Ingest rate-limiter fails open after the first window.** The window-reset path in `ingest-ratelimit.server.ts` called `.upsert({token_id, request_count:1, ...}).eq('token_id', id)` — the trailing `.eq()` is a no-op on an upsert, and with no `onConflict` PostgREST resolves on the PK; the row already exists for a returning token, so a fresh INSERT violated `UNIQUE(token_id)`, errored, and the catch failed OPEN. Net: the 100/hr per-token cap silently stopped enforcing for any token after its first hour. Found by the loop-frontend audit. | The per-token ingest cap stopped enforcing after the first window — a token holder could flood the agent pipeline. | Fixed: `.upsert({...}, { onConflict: 'token_id' })` (dropped the no-op `.eq()`), so a second-window reset UPDATEs the existing row. Regression test added (asserts the reset upsert passes `onConflict:'token_id'`; the prior tests asserted literals only). tsc 0, build green. No migration. **Residual (lower-sev, noted not fixed):** per-token only (no global/IP cap) + still fails open on a genuine DB error (consistent with the sibling decisions limiter). |
| KI-30 | ✅ **RESOLVED 2026-06-20** — **Eval-tick double-spend under overlapping ticks.** `eval-tick.ts` read existing ai_evals rows, filtered out done event_ids, then called the PAID judge() AI call BEFORE inserting; two overlapping every-minute ticks both computed the same pending set and both paid judge() on the same events (only the second INSERT lost on the unique index `ai_evals_event_idx`, after the spend). Found by the loop-frontend audit. | Duplicate paid judge() AI calls on every overlapping tick (a 20-event batch can take >1 min, so overlap is the norm). | Fixed migration-free (the schema already allows a scoreless `status='pending'` row, no CHECK): RESERVE before judge — insert a 'pending' row keyed on event_id (unique index = race guard); on conflict reclaim ONLY a STALE pending (`updated_at` past a 10-min TTL) so an abandoned mid-judge reserve self-heals without stealing a fresh in-flight one; only the reserver calls judge(), then UPDATEs to complete/error. Pure `selectEvalCandidates` seam unit-tested. tsc 0, 4 tests, build green. No migration. |

## Resolved

| ID    | Issue                                                                                                                                                                                                                                                                                                                                                                                                                                       | Resolution                                                                                                                                                                                                                                                                                                                      |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| KI-04 | `plan.md` §6 heading was corrupted — spliced mid-line into a legacy-log bullet at ~line 396, clobbering that bullet's tail.                                                                                                                                                                                                                                                                                                                 | 2026-06-11 — heading restored to its own line; lost tail noted inline (recoverable from git history).                                                                                                                                                                                                                           |
| KI-05 | 18 redirect stubs (root + `docs/`) pointed into the retired `project-Cadence-v3` repo via absolute `file://` links.                                                                                                                                                                                                                                                                                                                         | 2026-06-11 — all retargeted to live in-repo relative paths; anti-rot rule added to [`../../AGENTS.md`](../../AGENTS.md) §7.                                                                                                                                                                                                     |
| KI-07 | Orchestrator mission stayed `running` forever at 0 hops when the model call failed (no halt/failed transition).                                                                                                                                                                                                                                                                                                                             | 2026-06-11 — `F-V5-LOOP-CLOSE`: non-governance model errors now set `agent_runs.status='failed'` + `missions.status='halted'` in `loop.server.ts`.                                                                                                                                                                              |
| KI-08 | `F-V5-LOOP-CLOSE` migration awaited Lovable's sync (founder-chosen Lovable-operated DB path).                                                                                                                                                                                                                                                                                                                                               | 2026-06-11 — Lovable pulled + applied the migration (REST probe confirms `learnings` + `prds.shipped_at` live); founder then verified the full loop e2e on the hosted app, incl. a real GitHub issue close.                                                                                                                     |
| KI-19 | `resolveAgent` resolved a target agent by slug with no `enabled` filter, so a disabled or off-roster agent could still be dispatched a child run (via `agent.handoff` or the mission dispatcher).                                                                                                                                                                                                                                           | 2026-06-14: closed at the single resolution chokepoint. `resolveAgent` (`handoff.server.ts`) now filters `enabled = true` and throws a clear slug-named error; the throw is caught per step so a bad target terminalizes one step, not the run. Adversarial-reviewed safe-to-land. Residual adjacent surfaces tracked as KI-20. |
| KI-18 | Event-reactor unseeded-slug drift: `seed_default_event_subscriptions` seeded `signal.created` to target slug `'discovery'`, but the roster slug is `discovery-scout`, so `dispatchEvent` (`reactor.functions.ts:222`) threw "Target agent not found" for every new account, killing the autonomous `signal.created` SENSE fan-out (fault-isolated, so silent). The v7 tracker mis-attributed this to `mission.plan` (which is roster-safe). | 2026-06-14: migration `20260614200000` (commit `5b0b0b93ea`) corrected the seed to `discovery-scout`, backfilled existing default rows, and dropped the stale example slugs from the orchestrator prompt; applies on next Lovable sync. RCA via 10-agent workflow `wf_71a7dc78`; migration reviewed byte-clean.                 |
