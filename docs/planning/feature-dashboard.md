 # Feature Dashboard - the one master prioritized feature register

> [!IMPORTANT]
> ## ⭐ THE v11 BUILD FRONT: PICK THESE FIRST (Rank #1-21, topmost, 2026-06-23)
>
> **Strict instruction to every agent and every parallel lane:** the rows below are **Rank #1 through #21, Tier 1, the topmost priority.** `bash scripts/lane.sh next` already returns them first (it picks the lowest Rank). **Build them in this order, capabilities-first.** They all come from the **[v11 Guiding Star](../strategy/v11-guiding-star.md)** (the standing strategy canon; read it before building). _(Reading note: these are at the top of the **Rank `#`** column. The register TABLE starts ~line 185 of this file, after the legends and the "At a glance" summary, so a high file-line number is NOT a low priority. The Rank `#` is the only priority signal.)_
>
> **Live status per item** (✅ done · ◐ partial, autonomous slice shipped, gated remainder named in the row · 🔨 in dev right now · ⬜ open). These markers are kept in sync with the register rows below every cycle (founder ruling 2026-06-24: item-level status must be visible at the top, not only in the table).
>
> 1. ✅ **TEST-SEED** - minimal dev/test data so the data surfaces render while we build them (rich showcase seed is LAST). _v11._ **Done (lane 1, 2026-06-24).**
> 2. ◐ **EVENT-REACTOR-LIVE** - turn on the dormant event reactor: the nervous system of the self-initiating loop. _v11 North Star._ **Pipeline wired+scheduled + KI-27 migration applied live via Lovable MCP (lane 2, 2026-06-24; KI-38 resolved, auto path unblocked); remainder is the new-event-types CHECK-widen migration + live proof (LOOP-PROVE).**
> 3. 🔨 **AMBIENT-SENSE** - continuous sense / normalize / tag / cluster, no human start. _v11 North Star._ **Autonomous slice shipped (lane 1, 2026-06-24). 🔨 In Dev (2, 2026-06-25): pg_cron schedule activation (founder-unlocked).**
> 4. 🔨 **AMBIENT-TRIGGER** - self-initiate a mission at the right phase when a signal/outcome/insight crosses a threshold. _v11 North Star (the self-driving behavior)._ **Autonomous slice shipped (lane 1, 2026-06-24). 🔨 In Dev (2, 2026-06-25): pg_cron schedule activation + proposed→running promotion UI (founder-unlocked).**
> 5. ✅ **LOOP-PROVE** - prove the loop closes on real data so the cold moat (DBR / LRN-02 / W1-AUTO) visibly fills. _v11 (fuels the Decision Brain)._ **Done (lane 1, 2026-06-24): closure-proof harness + LIVE verification — both seeded demo workspaces resolve warm/closed (outcome→supersession→governing all non-zero), unseeded workspace correctly diagnosed cold.**
> 6. ✅ **TRUST-LEDGER** - the receipts (what changed / why / evidence / who / was-it-right) as the hero surface. _v11 pillar 3 (trust is what people pay for; corroborates v9 wedge)._ **Done (lane 2, 2026-06-24): `/trust-ledger` surface live (sidebar Trust row), composes decisions+approvals+bitemporal supersession; 14 tests, adversarially reviewed.**
> 7. ✅ **TRUST-SHARE** - shareable provenance artifact: the viral + regulated-buyer proof. _v11._ **Done (lane 2, 2026-06-24): Share affordance on the ledger + public `/d/$slug` receipt with honest standing/superseded (private overrides never leak); security-reviewed.**
> 8. ◐ **BRAIN-UX-V11** - the Brain as a human-useful open analyst that also DRIVES action (discover -> derive -> act), not a node graph. _v11 (builds on F-IA-BRAIN-GRAPH / DBR)._ **Floor + human lenses shipped (lane 2, 2026-06-24): `/knowledge` Insights tab (now default) — beliefs (standing/revised), what-we-learned + hit rate, month timeline, plain-language observations, PLUS the per-decision "why" (rationale + what revised it) and the "what is unresolved" lens (open contradictions + mixed outcomes); 22 tests, adversarially reviewed (revised-vs-contested kept disjoint, honest counts). Remaining (◐): only the AI open-analyst CEILING — predictions / next-best-action / volunteered intelligence (chokepoint-gated).**
> 9. ✅ **STITCH-LOOP** - wire the surfaces into one continuous felt loop (fixes the "fragmented" feel). _v11._ **Done (lane 2, 2026-06-24): a route-derived LoopThread strip on every loop surface (Today → Product → PRD → Build → Missions → Brain → Trust; current emphasized, next + "{produces} → {next}" handoff caption, cyclic glyph) makes the one engine-loop felt; pure loop-surfaces model + 10 tests; wired once in TopBar + PRD detail, NO AppShell/nav-model touch (lane-1 IA-NAV).**
> 10. 🔨 **CORE-UX-TRUST** - move trust to the point of decision; kill the babysitting tax. _v11 core-user._ **Core + rejection-learning shipped (lane 2, 2026-06-24). 🔨 In Dev (2, 2026-06-25): auto-clear reversible tool gates in loop.server.ts (founder-attended chokepoint edit, now unlocked).**
> 11. ◐ **CORE-UX-FELT** - first-run hardening + chief-of-staff brief + de-jargon the front. _v11 core-user._ **Felt slices shipped (lane 2, 2026-06-24): (1) the chief-of-staff brief now LEADS WITH STAKES not counts — pending gates folded into reversibility/blast-radius via the static tool-consequences catalogue (pure `copilot-brief.ts`, 7 tests); (2) FOUNDER ASK — the Today home no longer lists every gate approval (over-populated/babysitting feel); collapsed into ONE calm `PendingApprovalsBar` (count + a quiet high-stakes hint) that opens the full detail in Govern → Approvals; the per-agent track record (CORE-UX-TRUST) relocated there too. tsc 0/1346; adversarially reviewed (1 HIGH cross-user-scope fix folded). Remaining (◐): cold-gateway first-run hardening (WedgeTeardown) + de-jargon the LoopStations/govern surfaces.**
> 12. ✅ **IA-NAV-V11** - collapse the left nav to 5 + one recessed Engine Room door. _v11 consumer-grade IA (operationalizes v8)._ **Shipped (lane 1, 2026-06-24): the four competing metaphors (Workspace rail · "Loop" NavGroup · 5-icon Trust row · floating QuickAccessDock) collapsed into ONE flat list of 6 outcome-named destinations (Today · Ask · Product · Build · Missions · Brain) + ONE recessed Engine Room door that reveals the governance surfaces on demand (carrying the live approvals badge); dock deleted. Pure `nav-model.ts` + 11 tests; no surface orphaned (Trust Ledger + Connectors stay reachable behind the door since ⌘K doesn't index them).**
> 13. ✅ **SETTINGS-SEGREGATE** - 11 settings tabs into 5 clear groups. _v11 consumer-grade IA._ **Shipped (lane 1, 2026-06-24): the 11 flat tabs now present as 5 calm groups (Account · Workspace · Connections · AI & keys · Billing) + a recessed Advanced (Health/Data); pure `settings-sections.ts` grouping model + two-tier nav (group TabRow → SubTabs), 15 tests; the `?section=` ids + legacy map preserved unchanged so every deep link still lands.**
> 14. ✅ **CONNECTORS-V11** - one connectors home, day-one input, de-dup the repetitive lists. _v11 consumer-grade._ **Shipped (lane 1, 2026-06-24): the presentation/consolidation/de-dup/day-one layer — a pure categorized connector-catalog model (`connectors/catalog.ts`, single source of truth: every user-facing source grouped Code/Issues/Docs/Calendar/Design with a flow + connect descriptor; 10 tests) surfaced on `/sync` as the canonical "Available sources" home, with day-one input already live via the webhook ingest. OAuth provider wiring (F-CONN) + a 2nd live source (SEN-01) stay founder-gated; the deeper literal 3-route merge is IA-DEPTH-V11.**
> 15. ⬜ **ORCH-DELEGATE** - build = orchestration of external coding agents, not codegen. _v11._
> 16. ◐ **INTEROP-V11** - how agents + platforms access Cadence (read-only MCP + scoped A2A/API). _v11._ **Read surface repaired + completed (lane 2, 2026-06-24): live-schema audit via Lovable MCP found 3 of the existing MCP read tools broken by schema drift (search_signals/search_opportunities/get_prd selected columns/tables that do not exist in prod) — all FIXED; ADDED search_prds (spec discovery) + get_roadmap (now/next/later buckets); HARDENED every search tool against PostgREST .or() filter-injection; honest ICE filter (no NULL-drop). 8 MCP tools, adversarially reviewed (security: tenant-isolation clean). Remaining (◐): the outward WRITE/A2A scoped-token surface (founder-gated on scopes/audit).**
> 17. ✅ **PLAYBOOK-REGISTRY** - institutional product judgment as software, bound to your decision memory. _v11 (the embed-skills verdict)._ **Done (lane 1, 2026-06-24): versioned registry engine (JTBD/RICE/discovery/PRD/positioning) + `playbook_runs` per-outcome ranking; migration applied live + ranking verified live (jtbd 100% ahead of discovery-interview 67%).**
> 18. ✅ **PM-IMPACT-LEDGER** - the PM's portable decision+outcome track record: career asset + retention hook. _v11._ **Done (lane 1, 2026-06-24): `/impact` surface + portable Markdown export, composes existing decisions/learnings/lineage; data engine live-verified (demo workspace: 23 decisions, 71% hit rate, beliefs revised).**
> 19. ✅ **STAKEHOLDER-PACK** - audience-tuned alignment/persuasion artifacts from a decision + its receipts. _v11._ **Done (lane 1, 2026-06-24): `/stakeholder` surface composes any decision + its receipts into exec/eng/board packs (copy/download .md); data engine live-verified on real demo decisions.**
> 20. ⬜ **POS-V11** - reconcile positioning/persona/specs + cascade to README/AGENTS/moat. _v11._
> 21. ⬜ **LANDING-PAGE-V11** - rebuild the public landing page (only what a visiting PM wants to see). _v11; sequenced HERE on purpose, after the capabilities it showcases._
>
> Then the **Decision Brain (DBR (H1))** at #22, then Tier-2 design (`DESIGN-V11`, `IA-DEPTH-V11`), Tier-3 future bets (`EVALS-PRIMITIVE`, `AGENT-FLEET-VIEW`), and Tier-4 closure (`DEMO-SEED-RICH` rich showcase seed + `SHIP-V11`), LAST. **Logical-sequencing rule:** showcase/dependent surfaces (landing page, rich seed) build AFTER the features they present. Every row below also carries a one-line **Why**. Full reasoning: **[v11 Guiding Star](../strategy/v11-guiding-star.md)**.


> **SSOT first.** The single front-door tracker is [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) (live cursor, status, build queue, founder rulings, findings, progress). This file is the per-feature master register + live In-Dev claims (machine/loop view) it points to, not the tracker to follow day-to-day.

> **What this is.** ONE canonical, at-a-glance, prioritized register of **every** feature - what is built, in dev, partial, paused, deferred, blocked, or open - each with a one-line "what it does", a category, and a priority, so any session can pick the top open item cold. This is the **front door** to status. Per-feature acceptance detail lives in [`archive/feature-backlog.md`](archive/feature-backlog.md); current-initiative build specs live in [`workspace-tenancy-and-monetization-plan.md`](./workspace-tenancy-and-monetization-plan.md) (G10) + [`byo-build-implementation-plan.md`](./byo-build-implementation-plan.md) (G11).
>
> **Created:** 2026-06-16 · **Last updated:** 2026-06-21 (MONETIZATION TAKEOVER BUILD - Claude built the full monetization engine end to end: credit-flow fix (top-up balance + grant/renewal-on-subscribe), retired the dead webhook, single-sourced pricing to the catalog, go-live guard + backfill (incident-proof), webhook signature tests, the usage-attribution view (WM-M16), the memory-expiry upgrade nudge (WM-M7 ✅), owner spend caps (WM-M14), the tier-glyph motif (WM-M8 ✅), and synced all monetization docs + a go-live runbook. Also FOUND + FIXED a live incident (metering armed with 0 credits blocked all AI -> turned OFF). 75/176 done, 42.6% strict / 68.4% weighted. Only pending: founder's live Stripe keys + the go-live flip. Prior - CLAUDE OWNS MONETIZATION - founder handed the FULL monetization + credit engine to Claude, backend AND frontend, no Lovable handoff. Shipped the credit-flow fix (top-up balance propagation + grant/renewal-on-subscribe; `M-C-TOPUP-BUG` ✅), retired the dead duplicate Stripe webhook (`M-C-DEDUPE-WEBHOOK` ✅), single-sourced public pricing to the catalog numbers + full ladder (`M-C-PRICE-SYNC` ✅), `app_settings` migration hygiene (`M-C-DB-HYGIENE` ◐); the credit engine is now wired end-to-end in sandbox (`WM-M11/M12/M13/BYO-P4` ◐, debit stays dormant until go-live). FOUND + FIXED A LIVE INCIDENT: credits metering had been toggled ON with 0 credits granted, blocking ALL AI on the published app - turned metering OFF (re-enable only at go-live, after balances exist). 72/175 done, 41.1% strict / 66.9% weighted. Prior - LOVABLE MONETIZATION + ADMIN RECONCILIATION: mapped Lovable's 06-20 overnight build - billing/payments/pricing/credits + a new Admin Console - against the register, validated by code read + live test + live DB; flipped WM-M2/M5 → ✅ and WM-M3/M6/M7/M8/M13/M16/M18/WM-F7/BYO-P4 → ◐ [built, dormant/test-mode]; added 7 rows [F-ADMIN-CONSOLE, ADM-DB, M-C-TOPUP-BUG, M-C-DEDUPE-WEBHOOK, M-C-PRICE-SYNC, M-C-BILLING-TESTS, M-C-DB-HYGIENE]; → 175 rows, strict 38.9% / weighted 64.2%. Prior - STRATEGY RECONCILIATION: imposed the numbered **Build Sequence** + the pick-by-number law [AGENTS.md §3]; marked the credit/pricing block **Lovable-owned + frozen** (`M-C-PRICE`, `WM-M3/M6/M7/M8/M10/M11/M12/M13/M14/M16/M17/M18/M19`, `BYO-P4`); kept the `WM-F*` tenancy spine Claude-owned; added 9 Tier-1 rows (#152-160: `DBR-1.5` + loop-closers + 2 BBI fixes); reparented `O1`/`O3` to the Decision Brain + `WM-M15` out of Credit; superseded `F-COCKPIT-MACHINE-MODE`; **made the Priority column uniform** (exactly one of Tier 1/2/3/4 · Gated · Lovable · Deferred · Done, retiring the P0/P1/WM-*/BYO-* mix) and the `#` a **derived priority Rank** (#1 = highest, regenerated by `scripts/rerank-dashboard.py`), which also fixed the duplicate + lettered serials from the parallel merge; tally 64/167 = 38.3% strict / 60.2% weighted). Prior: 2026-06-20 22:20 (FE cycles 0-4, founder-directed FRONT-END theme wiring the shipped WM backends to the UI: **cycle 0** codified the frontend build protocol (§3.0d, full design-skill toolkit); **cycle 1** the **Members surface** (`MembersCard`, WM-F4 transfer + remove + members list via a new membership-gated identity RPC); **cycle 2** the **move-product UI** (WM-F6); **cycle 3** member **role management** + an OWNER-ONLY correction (fixed a cycle-1 admin silent-failure + hardened the write fns); **cycle 4** invite **redirect-after-login** (an open-redirect-guarded `next` param on /login + /signup completes the invite flow end-to-end). The founder-directed FE-wiring scope (members, move-product, invites) is COMPLETE; remaining WM FE is founder-gated (credits/pricing, WM-M6/M3). ◐ live on publish; the identity-RPC migration live dry-run is pending (Supabase MCP unauthorized this session, flagged for publish-verify). Prior: 2026-06-19 22:20 (WM-F3-fix → cycle 41: the shipped RBAC migration used invalid `create policy if not exists` (ERROR 42601) and would have FAILED to apply on publish; fixed to drop-then-create, `70c0ca7db5`, dry-run-verified on prod + owner-lockout data-checked. This is the 3rd broken parallel ship caught + fixed this session; WM-F2-fix → cycle 40: restored the cross-user-read + expiry + global-scope + return-shape invariants the cycle-40 WM-F2 ship regressed, and made account pooling actually driven (resolved internally, not a dormant opt), `a368edf78a`, dry-run-verified on prod; WM-M15b-fix → cycle 39: corrected 3 runtime-fatal bugs in the cycle-38 response cache (broken migration now()-predicate that would fail on apply + no-op cache hit + bad readCache "now()" literal), `7897085004`, dry-run-verified on prod; WM-F1b → ◐: agent-workspace isolation hardening, `user_id`-fed BEFORE-INSERT trigger (universal insert tagging) + dual-key membership RLS on all 5 agent tables, dry-run-verified on prod, overnight cycle 37; WM-F9 → ◐: tenancy isolation hardening, workspace_id + dual-key membership RLS on 10 tables, overnight cycle 35; pricing-strategy packaging: added 3 deferred rows WM-M17/M18/M19 (Anthropic-style usage variants, plan-card states, enterprise usage model), register 141 → 144; WM-M15 → ◐: cost-aware model routing margin lever, config-gated default-off, overnight cycle 34; WM-M14 → ◐: per-product/member credit attribution + caps, overnight cycle 33; WM-M12 → ◐: credit debit engine fills the WM-M4 seam, atomic draw-down + halt, overnight cycle 32; WM-M11 → ◐ cycle 31; WM-M4 → ◐ cycle 30; WM-M10 → ✅ cycle 29; earlier 05:30 IST: restructured into a single master prioritized register: every G0-G11 row is one numbered row; sorted open-first by priority, then done-by-category). **Maintainer rule:** Tier 1, continuous (update in the same commit as any status change; stamp the precise time on every update; recompute the "At a glance" % per the **Progress accounting** rule below).

---

## STANDING RULE - REGISTER FIRST, THEN BUILD (non-negotiable, read before any feature work)

**No feature is built that is not tracked here first.** Every tool (Claude Code, Antigravity, Gemini, Lovable, a future session) must:

1. **`git pull origin main`**, then read this register. It is checked before any activity starts so a new or parallel session knows what is already in flight, deferred, or done, and does not collide or redo.
2. **REGISTER a new feature HERE FIRST.** Before any code, add a row with an **ID + Category + Priority** (and a one-line "What it does"). If it is not in this sheet with an ID, it does not get built.
3. **Respect the claims.** If a row is `🔨 In Dev`, another session may be on it - do not start it. Pick a different row or coordinate. Check the **Active claims** table first.
4. **On pickup:** flip Status to `🔨 In Dev (<tool>, YYYY-MM-DD)`, add a line to the **Active claims** table, and commit + push immediately so others see the claim - same commit, before you write feature code. (The literal `In Dev (` substring is preserved so the session-start hook can grep live claims.)
5. **On completion:** flip to `✅` (or `◐` if partial) with the date, remove the Active-claims line, and update the linked detail doc + `plan.md` §4 in the same unit of work (the closed-doc loop).
6. **On defer/pause/block:** flip to `⏭️` / `⏸️` / `🚧` and put the SHORT REASON in the **Comments** column (e.g. "deferred: post-PMF", "blocked: needs founder OAuth").

> Same shared-cursor discipline as the SSOT live cursor (section 0) and [`archive/feature-backlog.md`](archive/feature-backlog.md). When they disagree, fix all in the same commit.

### How to pick something up
Say **"pick `<ID>`"** (e.g. "pick WM-M1", "start SEN-01", "do F-IA-V4") and the agent resolves the ID here, reads the row, opens the linked detail (backlog / WM plan / BYO plan / feature doc), and builds. The IDs are stable and shared with [`archive/feature-backlog.md`](archive/feature-backlog.md).

### STANDING RULE - PROGRESS ACCOUNTING (keep the % live, never hand-guess it)

**This is how the "what percentage is done" question is answered at a glance, forever, without the number rotting.** The overall % and the per-category roll-up are **derived from the register rows**, not separately hand-kept, so they stay true as long as you follow this:

1. **Every row already carries its own % - read it off the status icon** via the **Item %** column in the Status legend below (✅ 100%, ◐/⏸️/🔨 50%, ⬜/⏭️/🚧 0%). That is the *individual-item* completion. A ◐ partial may override its default 50% with an explicit `[~NN%]` token in its Comments when the remaining split is known (e.g. "remaining D4b ... `[~80%]`").
2. **The overall number is two figures, both shown in "At a glance":** *strict* = fully-done rows / total; *weighted* = sum of every row's Item % / total. Strict is the honest floor; weighted credits half-built work.
3. **Deferred rows stay in the denominator** - they are real planned scope, just not now. (For "% of active scope", subtract the deferred count from the denominator; the dashboard reports against all rows.)
4. **On ANY status change, recompute in the same commit.** Do not eyeball it - run the override-aware tally and paste the results in:
   ```
   awk -F'|' '/^\| [0-9]+ \|/{s=$3;gsub(/^[ \t]+|[ \t]+$/,"",s);c[s]++;t++; w=(s=="✅")?1:(s=="◐")?(match($0,/\[~[0-9]+%\]/)?substr($0,RSTART+2,RLENGTH-4)/100:0.5):(s=="⏸️"||s=="🔨")?0.5:0; W+=w} END{for(k in c)print k,c[k]; printf "total=%d strict=%.1f%% weighted=%.2f (%.1f%%)\n",t,(c["✅"]/t)*100,W,(W/t)*100}' docs/planning/feature-dashboard.md
   ```
   then update the "At a glance" strict/weighted/by-status/by-category numbers and the "Last recomputed" stamp.
5. **One number, one place.** [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) (sections 0 + 2) shows the same headline % and points back here for the method. Never maintain a second independent figure.

### Status legend (each status carries a completion weight - the **Item %** is the per-item progress you read off any row)
| Mark | Meaning | Item % |
| --- | --- | --- |
| ✅ | **Done** - built, on `main`, verified (date where known) | **100%** |
| 🔨 | **In Dev** - actively being built this/another session (see Active claims); keeps the literal `In Dev (` for the hook | **50%** |
| ◐ | **Partial** - foundation built, real remaining work; Comments says what is left | **50%** (override with `[~NN%]` in Comments) |
| ⏸️ | **Paused** - built or started but intentionally idle; reason in Comments | **50%** (built but idle) |
| ⏭️ | **Deferred** - deliberately not now (gate/sequence reason in Comments) | **0%** |
| 🚧 | **Blocked** - cannot proceed until a dependency clears (founder action / KI); reason in Comments | **0%** |
| ⬜ | **Open** - not started, ready to pick up | **0%** |

### Category legend (11 categories; mapped from the source groups)
| Category | What it covers | Source group |
| --- | --- | --- |
| Foundational | Core autonomous loop, memory engine, platform/auth/runtime infra | G0 + G9 |
| Sense | Get real signal in, cluster it, keep it fresh | G1 |
| Decide | Turn signal into governed decisions + specs (incl. the wedge) | G2 |
| Build | The autonomous Build -> QA -> Ship execution chain | G3 |
| Launch | Ship to market, learn from outcomes, feed it back | G4 |
| Monetization | Pricing, entitlements, tenancy/RBAC/billing, PLG | G5 + G10 (tenancy/RBAC/billing rows) |
| Credit | The credit metering engine (WM-M10..WM-M16) | G10 (credit rows) |
| Interop | External agents/tools (MCP, A2A), team interop, export | G6 |
| Cockpit | IA, observability, the operator's view of the machine | G7 |
| Governance | Trust, safety, evals, drift, critic, incidents, humanization | G8 |
| BYO | Provider-agnostic repos + managed Cadence Cloud runtime | G11 |

---

## ⚡ HOW TO PICK - the priority ranking (founder ruling 2026-06-21)

> **MANDATORY, non-negotiable.** The Master register below is **sorted by priority and ranked `#1, #2, #3 ...`** (`#1` = highest). **To pick, run `bash scripts/lane.sh next`** - it deterministically prints the next eligible item IDs (lowest Rank first: `⬜`/`◐` Tier-1/Tier-3, unclaimed, not DONE). **Do NOT judge, score, or decide what to pick - the command is the decision.** Build the first whose claim wins: `for id in $(bash scripts/lane.sh next); do bash scripts/lane.sh claim "$id" <laneN> "<globs>" && break; done`. A `◐` is PARTIAL, it has remaining work, so you **CONTINUE** it; **never skip a `◐` as "done" or "publish-verify-only"** (that false judgment idled all lanes with 60 items waiting). `Gated`/`Lovable`/`Done`/`Deferred` and chokepoint-pinned files are auto-excluded (by `next` and by the claim). Flip the row to `🔨 In Dev` on claim and `✅`/`◐` on finish, commit + push immediately on each. "Board dry" is real ONLY when `lane.sh next` prints nothing. Full law: [`../../AGENTS.md`](../../AGENTS.md) §3 + [`../operations/autonomous-build-loop.md`](../operations/autonomous-build-loop.md) §15.

### How priority is marked (the ONE uniform vocabulary - the Priority column is always exactly one of these)

| Priority class | Meaning | In the pick-order? |
| --- | --- | --- |
| **Tier 1** | Fundamentals / foundation / core features / core enablements / USPs (the moat, the loop, the wedge, chokepoint integrity). Build first. | yes, ranked first |
| **Tier 2** | Design + eye-for-detail on the core surfaces. Founder-prompted, once, after the core is solid. | yes, after Tier 1 |
| **Tier 3** | Non-essential / non-foundational (privacy, ops / reliability hygiene, enterprise-readiness, edge). Never outranks core. | yes, after Tier 2 |
| **Tier 4** | Final detailing + comprehensive polish (humanization sweep, lint, AI-trace, final design). Founder-prompted, last. | yes, last |
| **Gated** | Founder-gated; ranked in place but parked until the founder unblocks it (OAuth / secret / spend / taste). | skip until unblocked |
| **Lovable** | Lovable-owned (billing / credit / pricing); built in parallel by Lovable. | no by default; **yes if the founder explicitly hands it back to an agent** |
| **Deferred** | Cut / post-PMF / superseded. | no |
| **Done** | Shipped. | no |

**The tier law (build substance before surface; non-core last):** Tier 1 then Tier 2 then Tier 3 then Tier 4, in that strict order. This is why the rank puts every Tier-1 row above every Tier-2 row, and so on. It fixes the drift where the loop built Tier-3 ops hygiene while the Tier-1 moat work had no rows.

### Before you build a picked item: the sourcing gate + the autonomy doctrine (founder ruling 2026-06-21)

The Rank decides WHAT to build next; this decides HOW to source it, and WHO decides. Apply it to every item before writing code; the agent owns this call from its own web-grounded research (carry a greppable `BBI:` stamp on the deciding doc/PR). **The per-cluster verdict (what to build vs what to source elsewhere) lives in the [Sourcing Map](../strategy/sourcing-map.md): look up a picked item's cluster there FIRST, and do NOT build any sub-item the map marks obviated by a buy/integrate (wire the named provider behind the cluster's seam instead).** Canon: [`../strategy/build-buy-integrate.md`](../strategy/build-buy-integrate.md) + [`../../AGENTS.md`](../../AGENTS.md) §3.0c.

**The four sourcing decisions:**

- **BUILD** (in-house, from base) - the moat / USP: the typed decision ontology, the supersession engine, the Critic + precedent-salience, signal->ontology normalization, outcome->memory write-back, the system-of-record. **Never wrapped, never bought.** Default for anything that IS the differentiator.
- **BUY** (a commodity, always via the AI chokepoint `runtime.server.ts`, never called directly) - inference, embeddings, rerank, OCR, email, OAuth.
- **INTEGRATE** (a high-lock-in substrate, behind our OWN swappable seam, with a native default + graceful fallback) - e.g. the sandbox provider, a heavier graph engine only if we outgrow Postgres.
- **SELF-HOST** (open-source we run ourselves - the autonomy floor: a zero-external-paid-dep native default that must always hold) - e.g. SearXNG (`FIRECRAWL-FLOOR`), Postgres + pgvector for the graph.

**The Decision Brain verdict (the strongest opinion, since this was the open question):** the brain's INTELLIGENCE is **BUILD, in-house, forever** - the ontology, supersession (`DBR-1.5`), the Critic, the salience ranker. Never make the brain's reasoning a wrapper around someone else's product; that IS the moat. Its SUBSTRATE is **SELF-HOST/native** (Postgres + pgvector now), swappable to an **INTEGRATE'd** graph engine (Graphiti / Neo4j) behind a `GraphStore` seam only if scale demands; its embeddings + inference are **BUY** via the chokepoint; the Obsidian-style view **INTEGRATE**s a viz library (don't build a render engine). Net: the thinking is ours; only the plumbing is swappable.

**Autonomy doctrine (act on your own; escalate only when you must):**

- **ACT autonomously** - build the moat; buy a commodity via the chokepoint with NO new recurring spend; self-host an OSS native default; integrate behind a seam when a free/native default exists; any migration / server-fn needing no secret/spend/taste (gate it offline, flag for publish-verify).
- **THINK it through autonomously** (the agent decides via web research - do NOT ask the founder for this) - the sourcing call above, the architecture, the seam design, moat-vs-commodity. Carry the `BBI:` stamp on the deciding doc/PR.
- **ASK the founder** (escalate; mark the row `Gated`) - a new recurring SPEND; a SECRET / OAuth / paid-provider pick; a TASTE / positioning / pricing / product-tasting call; anything IRREVERSIBLE or OUTWARD-FACING (sending, publishing, the founder's accounts); a legal / policy call (data-retention scope, PII). When genuinely unsure whether it is a taste / spend / irreversible call, ask.

### The ranking is DERIVED - never hand-number it

The `#` Rank is computed, like the % tally. **To add or reprioritize an item:** add or edit its row with the right **Priority class** (and, for a Tier-1 item, place it among the Tier-1 rows in the order you want), then run **`python3 scripts/rerank-dashboard.py`**. It re-sorts the whole register by priority and renumbers `#1..N`, so a new high-priority item moves up and everything below it shifts down (nothing is left siloed, stuck on top, or stranded at the bottom). Then recompute the "At a glance" % (the awk in the Progress-accounting rule). Both run in the same commit as the change. The Tier-1 fine order lives in `scripts/rerank-dashboard.py` (the `T1` list).

### 🔁 Lovable-owned (mark `Lovable`; never a Claude pick - Lovable builds these in parallel)

> **TAKEOVER 2026-06-21 (founder ruling):** the **entire monetization + credit engine + admin category is now CLAUDE-owned, end to end (backend AND frontend) - NOT handed to Lovable.** Claude builds/fixes/tweaks all of it (existing UI may be tweaked; new backend gets new UI). The `Lovable` marks on the `M-C-*` / `WM-M*` / `BYO-P4` rows below are historical; treat them as Claude-owned now. Lovable's overnight build is the BASE; Claude owns it from here. Only pending founder action: provide live Stripe keys + flip `credits_enabled()` at go-live (see the go-live checklist).

Billing / Stripe: `M-C-PRICE`, `WM-M3`, `WM-M13`. Credit engine (already shipped dormant, do NOT extend; coordinate before live): `WM-M10`, `WM-M11`, `WM-M12`, `WM-M14`, `WM-M16`. Pricing / tier surfaces: `WM-M6`, `WM-M7`, `WM-M8`, `WM-M17`, `WM-M18`, `WM-M19`. Managed credits: `BYO-P4`. Admin Console + 06-20 follow-ups (Lovable's overnight build): `F-ADMIN-CONSOLE`, `ADM-DB`, `M-C-TOPUP-BUG`, `M-C-DEDUPE-WEBHOOK`, `M-C-PRICE-SYNC`, `M-C-BILLING-TESTS`, `M-C-DB-HYGIENE`. Split rows (Lovable owns the billing half only): `PLG` conversion leg, `R4` Plan/billing tab, `WM-F7` Plan/billing tab, `WM-M2` credit-pool/Stripe half. **STAYS product-infra (NOT Lovable):** every `WM-F*`, `WM-M1`, `WM-M4`, `WM-M5`, `WM-M9`, `WM-M15`, `M-C-EXPIRY`, `FND-AUTH`, `U6`. (The founder may ask Lovable to flip its rows to Done as that parallel work lands.)

---

## Active claims (who is on what, right now)

> **There is no separate status file - status lives in the register below (founder doctrine: no new docs).** Who is building what RIGHT NOW = the register rows whose Status is `🔨 In Dev`, plus the real-time CLI view `bash scripts/lane.sh board`. The committed register is the durable record; the **atomic ledger at `~/.cadence-parallel` is the real-time shared space** every session/worktree on this Mac sees instantly (no git needed). A background agent keeps this file fast-forwarded so your view stays current without a manual pull.
>
> **THE COLLISION GATE - two parts, both MANDATORY, both MECHANICAL (this is what stops two sessions doing the same work):**
> 1. **Claim before any work.** Run `bash scripts/lane.sh claim <ID> <laneN> "<globs>"` and proceed ONLY on success (exit 0). `HELD` = another lane holds it; `CONFLICT` = overlapping files; **`DONE` = another lane already COMPLETED it** (the ledger now remembers completions, so a finished item can never be re-picked) - any of these means STOP and pick the next item. **Never read, plan, or build before a successful claim.**
> 2. **Commit + push immediately on the two state changes** (founder ruling 2026-06-21): the instant you CLAIM (flip the register row to `🔨 In Dev (laneN, date)`) and the instant you finish (flip to `✅` and run `bash scripts/lane.sh done <ID>`, or `◐` + `lane.sh release <ID>` for a partial), commit with a WHY and **push right then** - so every other session sees it in git at the next pull, and `git status` stays clean. The ledger gives instant local visibility; the immediate push gives durable cross-session/cross-machine visibility.

---

## At a glance

> **Live progress (derived from the rows, recompute on every status change - see the Progress accounting rule above).** **2026-06-24 (LIVE-VERIFICATION CLOSURE PASS, Lane 1, founder-directed):** founder published HEAD `bc1b0151` + applied migrations, then asked to test the live app and close the partial items. Verified the live app healthy (login works; Today + Trust Ledger render; only benign 3rd-party console errors). A 25-agent adversarial workflow (a closure auditor, then a refute-it skeptic per row) re-checked all 15 non-gated `◐` rows against the live deploy, which IS this exact HEAD, so local code equals deployed code. Closed **2 to ✅**: `TRUST-VERIFY` (the SHA-256 "Integrity check", live-confirmed on /trust-ledger: 34 records, Copy fingerprint + Verify) and `SEC-SIGNAL-INGEST-INJECTION` (the injection screen is wired before the live ingest-webhook insert; verified by code + live schema). The skeptics caught **3 live defects** + several genuine autonomous remainders the auditors had glossed (full per-row verdicts in [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) §0, 2026-06-24 entry). Fixed defect #1 in this commit: the MCP `append_decision` write tool was advertised + callable but inserted columns / a `decision_queue` table that do not exist, so it could never succeed; un-advertised it (the write half is founder-gated Phase 4b) and corrected the stale Settings tool list. New tally off the canonical awk: **161/213 = 75.6% strict, 172.00/213 = 80.8% weighted** (up from 159; the prior by-status + by-category tables read a stale 145 ✅ / 201-total and are now refreshed to sum to 213). **2026-06-22 18:40 (WHOLE-DASHBOARD ◐ CLOSURE, Lane 1, founder-directed):** the founder asked why work that was DONE showed as `◐` partial. Verified: ZERO regression (✅ went 125→132→**144**, never decreased; no ✅ ever became ◐). The cause was the rerank re-sorting the active build-front (`◐`/`⬜`) to the TOP (#1-18) so partial items LOOK like "what's on top now". A 16-agent verification workflow then proved most ◐ rows were build-complete work mislabeled "partial" under the cautious "renders on publish" convention. Closed 12 → ✅ (gate-green, tsc 0 / 1110 tests, founder PUBLISHED 2026-06-22) + 4 → Gated 👤. New tally: **179 rows; strict 144/179 = 80.4%, weighted 149.00/179 = 83.2%** (up from 132/179). by-priority Done(144) == ✅(144). **LIVE-VERIFIED on the published app** (build Jun-22 18:58): Engine Room + Today + loop render clean; MOAT-METRIC's memory-depth card + F-IA-BRAIN-GRAPH's brain graph both render correctly (benign 3rd-party console error only). **2026-06-22 17:31 (MONETIZATION CLOSURE + by-priority reconcile, Lane 1):** closed the monetization/credit/billing block (7 rows ◐→✅ this session: M-C-PRICE/WM-M3/WM-M6/WM-M13/WM-M15/WM-M18/M-C-BILLING-TESTS; WM-M9/M17/M19 → Gated) and ran `rerank-dashboard.py` so by-priority **Done (132) now equals the ✅ count (132)** — fixing the founder-flagged 81-vs-125 gap (44 ✅ rows had carried a Tier priority). New tally: **179 rows; strict 132/179 = 73.7%, weighted 144.38/179 = 80.7%** (up from 125/179 = 69.8% / 79.1%). Emptied the dead `Lovable` set in the rerank script. **Earlier 2026-06-22 (metrics reconcile):** recomputed the live tally off the canonical awk after the latest closures (this session: BLD-05 / L2 / FND-0.7 → ✅ live-verified on prod; the monetization key-readiness pass — webhook Basil-blocker, voucher rollback, free-tier copy, voucher redeem UI, seat-cap enforcement, top-up cap, 2 SQL↔TS parity guards). **179 rows; strict 121/179 = 67.6%, weighted 137.05/179 = 76.6%** (up from the prior 107/179 = 59.8% / 73.8%). Refreshed the By-status + By-category tables to match the rows. **2026-06-21 (LOVABLE MONETIZATION + ADMIN MAP):** mapped Lovable's 06-20 overnight build (billing/payments/pricing/credits + a new Admin Console) against the register, validated by code read + live test on `cadence-flow-beta.lovable.app` + live DB. **WM-M2 / WM-M5 → ✅** (migrations applied live). **WM-M3 / WM-M6 / WM-M7 / WM-M8 / WM-M13 / WM-M16 / WM-M18 / WM-F7 / BYO-P4 → ◐** (built but dormant by design: the credit/limit engine sits behind `credits_enabled()`/`limit_gates_enabled()`=false; Stripe is live-capable but unconfigured/test-mode). **WM-M17 / WM-M19 confirmed still Deferred.** Added **7 rows**: `F-ADMIN-CONSOLE` + `ADM-DB` and `M-C-TOPUP-BUG` / `M-C-DEDUPE-WEBHOOK` / `M-C-PRICE-SYNC` / `M-C-BILLING-TESTS` / `M-C-DB-HYGIENE`. Now **175 rows; strict 68/175=38.9%, weighted 112.32/175=64.2%**. **Prior - 2026-06-21 (strategy reconciliation, this commit):** imposed a UNIFORM priority convention (every row's Priority is exactly one of Tier 1/2/3/4/Gated/Lovable/Deferred/Done) and a single DERIVED **Rank** (`#1` = highest, regenerated by `scripts/rerank-dashboard.py`), retiring the old `P0/P1/WM-*/BYO-*` chaos and the duplicate + lettered serials. Recomputed off the live awk: **total 167, strict 64/167 = 38.3%, weighted 100.47/167 = 60.2%** (167 because the tally now counts the lanes' lettered-serial rows the old `^\| \d+ \|` awk silently missed; dropped 1 duplicate `DATA-RETENTION-b`). Added the supersession engine `DBR-1.5` (Rank #1, TOPMOST) + the loop-closers `MOAT-VIS`/`MOAT-METRIC`/`W1-AUTO`/`H1-TASKS`/`F-IA-BRAIN-GRAPH` + the BBI fixes `EMBED-CHOKEPOINT`/`FIRECRAWL-FLOOR` as claimable Tier-1 rows; marked the credit/pricing block **Lovable-owned**; reparented `O1`/`O3` into the Decision Brain + `WM-M15` out of Credit; superseded `F-COCKPIT-MACHINE-MODE`. **Prior: Decision Brain merge (2026-06-20): NEW rows 148 DBR (H1, ⬜) + 149 CMD (H2, parked, ⬜)** are 0% horizon cues, so total 147→149, ⬜ 36→38, strict 63/149=42.3%, weighted 88.80/149=59.6% (the weighted sum is unchanged at 88.80). Prior: **2026-06-20 04:55 IST** (cycle 54, FRONT-END theme: wired WM-F5's accept route - a standalone `/join/$token` page that redeems an invite via `acceptInvitation`, completing the invite→accept loop; row 54 WM-F5 [~85%]→[~92%], weighted 88.73→88.80 = 60.4%, counts unchanged; ran `impeccable`. cycle 53, FRONT-END theme: wired WM-F5's invite UI - a `TeamCard` ("Invite teammates") in Settings > Workspace (invite by email + role, copy join link, list + revoke invitations); row 54 WM-F5 [~70%]→[~85%]; then rebased in the cockpit lane's P7 (which flipped P7 → ✅), so the canonical awk now reads ✅63 / ◐35 / ⬜36 / ⏸️3 / ⏭️10, strict 42.9% / weighted 88.73 = 60.4%; ran `impeccable`. cycle 52, FRONT-END theme: wired U6's export-history view - the DataExportCard now shows a "Recent exports" list via `listExportLog` (row 36 U6 [~95%]→[~98%]); weighted 88.15→88.18, counts unchanged; ran `impeccable` per the founder ruling. cycle 51, FRONT-END PIVOT per founder ruling: started stitching UI onto shipped backends so they are visible in the app. First build: a calm-front `SubprocessorsCard` ("Where your data goes") in Settings > Data wiring SUBPROC-DISCLOSURE's `getSubprocessors` (row 146 [~60%]→[~75%]). Rebased in the cockpit lane's R3-PREFS (which flipped R3 → ✅), so the canonical awk now reads ✅62 / ◐36 / ⬜36 / ⏸️3 / ⏭️10, strict 42.2% / weighted 88.15 = 60.0%. cycle 50: NEW row 147 APP-HEALTH → ◐ [~60%], a public app health/readiness endpoint (`/api/public/health`) + pure status-assembly module, Reliability P0; canonical awk now ✅61 / ◐37 / ⬜36 / ⏸️3 / ⏭️10, strict 41.5% / weighted 87.60 = 59.6%. cycle 49: NEW row 146 SUBPROC-DISCLOSURE → ◐ [~60%], a catalog-derived sub-processor disclosure registry + read fn, Data/Privacy P1; the awk recompute also corrected drift in the hand-kept status counts. cycle 48: U6-AUDIT backend (row 36 U6) shipped — an append-only `export_log` audit table + per-export audit-write in `exportProduct`/`exportWorkspace` + a `listExportLog` read fn, behaviorally dry-run-verified on prod (append-only confirmed: insert+select policies only); U6 [~85%]→[~95%], weighted 83.06→83.16 (counts unchanged, U6 stays ◐ pending its history UI); earlier cycle 47: NEW row 145 DATA-RETENTION → ◐) from the register (the per-status counts and per-category roll-up are tallied, not hand-kept).

- ### Overall completion: **74.6% (strict) / 80.4% (weighted)**  _(live recompute 2026-06-24, all lanes, via `bash scripts/dashboard-tally.sh`; kept real-time, updated in the same commit as every status flip)_
  - **2026-06-23/24 (v11 GUIDING STAR):** added **26 new build rows** (the v11 build front + design/future/closure + 2 validated horizon items, [`../strategy/v11-guiding-star.md`](../strategy/v11-guiding-star.md)): the **v11 Tier-1 front is #1-21** (TEST-SEED ... LANDING-PAGE-V11), plus Tier-2 (DESIGN-V11, IA-DEPTH-V11), Tier-3 (EVALS-PRIMITIVE, AGENT-FLEET-VIEW, DEF-04, REPO-DECLUTTER-V11, DELEGATE-DESK, TRUST-VERIFY), Tier-4 (DEMO-SEED-RICH rich-showcase-seed, SHIP-V11). Strict moved 80.6% -> 68.1% NOT by regression but by adding real new scope. The Decision Brain (DBR (H1)) follows at #22.
  - **Strict** = fully-done rows / total = **159 / 213 = 74.6%** (the honest floor).
  - **Weighted** = sum of every row's Item % / total = **171.20 / 213 = 80.4%** (✅ 100%; ◐ partial at its per-row `[~NN%]` estimate; ⏸️ paused + 🔨 in-dev 50%).
  - **Remaining: 25.4% open** - **54 of 213** rows not fully done; of those, **22 are `Gated` (👤 founder)** + 10 Deferred + the v11 Tier-1 build front.
  - 🔒 _**MONETIZATION / CREDIT / BILLING / ADMIN BLOCK — CLOSED (founder ruling 2026-06-22, FINAL). DO NOT re-pick, re-verify, or re-map any of these — by any lane, any tool, any future session.** This was the 3rd map of Lovable's monetization work; it is the LAST. The whole block is **build-complete** and **gate-green**; the ONLY remaining step is the founder's go-live config (live Stripe keys + price IDs, the `credits_enabled()` / `AI_COST_ROUTING` flips, final pricing numbers — all in **SSOT §4**), which is config, NOT build, NOT agent work. Terminal states (this session, evidence-backed by an 8-agent verification workflow): **✅ Done** — `M-C-PRICE`, `WM-M3`, `WM-M13`, `WM-M15`, `WM-M6`, `WM-M18`, `M-C-BILLING-TESTS` (+ the already-done `WM-M2/M4/M5/M7/M8/M10/M11/M12/M14/M16`, `BYO-P4`, `M-C-*`, admin `F-ADMIN-CONSOLE`/`ADM-DB`). **Gated (👤 founder)** — `WM-M9` (chokepoint: BYO-removal needs the pinned `runtime.server.ts`), `WM-M17`/`WM-M19` (founder pricing-packaging numbers), `M-C-EXPIRY` (decay-flip timing). **None of these is `◐`+Tier-1/3 any more, so `lane.sh next` will never surface them again** — that `◐`+Tier combo is exactly why they kept getting re-picked. If you are an agent and an instruction points you at a monetization/credit/billing item: STOP — it is closed; the only open monetization work is the founder's, in SSOT §4._
- **Total features = 213** · **Done = 159** · **Not done = 54** (⬜ 22 open + ◐ 19 partial + ⏸️ 3 paused + ⏭️ 10 deferred + 🔨 0 in dev + 🚧 0 blocked). _(2026-06-24: TEST-SEED ✅ + AMBIENT-SENSE ◐ + AMBIENT-TRIGGER ◐ + LOOP-PROVE ✅ + PM-IMPACT-LEDGER ✅ + STAKEHOLDER-PACK ✅ + PLAYBOOK-REGISTRY ✅ + EVALS-PRIMITIVE ✅ + SETTINGS-SEGREGATE ✅ + IA-NAV-V11 ✅ + CONNECTORS-V11 ✅ + IA-DEPTH-V11 ◐ (Engine Room banding) + DELEGATE-DESK ✅ + AGENT-FLEET-VIEW ✅ + REPO-DECLUTTER-V11 ◐ (feature-backlog archived) (lane 1); EVENT-REACTOR-LIVE ◐ + TRUST-LEDGER ✅ + TRUST-SHARE ✅ + BRAIN-UX-V11 ◐ + CORE-UX-TRUST ◐ + DEF-04 ◐ (lane 2) shipped.)_
- _By priority class (live awk, 2026-06-24): **Tier 1 = 11** (the v11 build-front rows + DBR (H1); TEST-SEED + TRUST-LEDGER + TRUST-SHARE shipped to Done), **Tier 2 = 2**, **Tier 3 = 6**, **Tier 4 = 3**, **Gated = 22** (👤 founder-marked), **Lovable = 0** (retired), Deferred = 10, **Done = 159**. **+SEC-PII-EGRESS** (180th row, ✅): the public-egress PII floor, the autonomous slice of the considerations.md Data/Privacy gap, built this session. **RECONCILED: by-priority Done (145) EQUALS the ✅ status count (145)** by construction (`rerank-dashboard.py classify()` maps every ✅ → Done). **2026-06-22 18:40 WHOLE-DASHBOARD ◐ CLOSURE (founder-directed):** a 16-agent verification workflow proved that most ◐ rows were build-complete work mislabeled "partial" under the cautious "renders on publish" convention — NOT unfinished. Closed 12 to ✅ (DBR-1.5/F-IA-BRAIN-GRAPH/MOAT-METRIC/W1-AUTO/H2-WRITES/WM-F1/K2/LCH-01/R4/PROVIDER-FALLBACK/SEC-EGRESS-GUARD/O3 — all gate-green + founder-PUBLISHED 2026-06-22) and 4 to Gated 👤 (EMBED-CHOKEPOINT chokepoint, FIRECRAWL-FLOOR founder-deploy, SANDBOX provider-pick, BLD-04 founder-endpoint). The autonomous Tier build-front is now nearly empty; pick the lowest open `#` via `bash scripts/lane.sh next`._

- **By status (of 179 total), with weighted contribution to the roll-up:**

| Status | Count | Item % | Weighted contribution |
| --- | --- | --- | --- |
| ✅ Done | 161 | 100% | 161.0 |
| ◐ Partial | 17 | per-row `[~NN%]` | 9.5 |
| ⏸️ Paused | 3 | 50% | 1.5 |
| 🔨 In Dev | 0 | 50% | 0.0 |
| ⬜ Open (ready to pick up) | 22 | 0% | 0.0 |
| ⏭️ Deferred | 10 | 0% | 0.0 |
| 🚧 Blocked | 0 | 0% | 0.0 |
| **Total** | **213** | - | **172.00 / 213 = 80.8%** |

- **By category (Total / Done / Open / Weighted %), most-complete first** - shows which lanes are nearly closed and which are barely started:

| Category | Total | Done | Open | Weighted % |
| --- | --- | --- | --- | --- |
| Data/Privacy | 3 | 3 | 0 | 100% |
| Credit | 7 | 7 | 0 | 100% |
| Decide | 15 | 15 | 0 | 100% |
| Reliability | 3 | 3 | 0 | 100% |
| Governance | 28 | 27 | 1 | 98% |
| Cockpit | 32 | 25 | 7 | 86% |
| Foundational | 33 | 25 | 8 | 85% |
| Build | 16 | 11 | 5 | 78% |
| Monetization | 35 | 26 | 9 | 76% |
| Interop | 6 | 4 | 2 | 75% |
| Launch | 14 | 9 | 5 | 68% |
| Sense | 13 | 5 | 8 | 54% |
| BYO | 8 | 1 | 7 | 12% |
| **Total** | **213** | **161** | **52** | **80.8%** |

> **Pick-order is THE BUILD SEQUENCE above** (founder ruling 2026-06-21): build the lowest open number, do not deliberate. The tier law (foundation/core/USP, then design, then non-essential, then final polish) and the rationale live in the Build Sequence section + [`../../AGENTS.md`](../../AGENTS.md) §3 "Build Sequence" + the SSOT ([`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) §0). Positioning rationale: v10 ([`v10-master-blueprint`](../strategy/v10-master-blueprint.md) §15-16) + moat.md. **The TOPMOST priority is the Decision Brain (H1); the monetization + credit + billing + admin block (WM-M*, M-C-*, BYO-P4) is CLOSED 🔒 (build-complete; founder go-live config only — do NOT re-pick, see the 🔒 banner above); the WM tenancy spine (WM-F*) stays Claude-owned; BYO (BYO-*) awaits founder greenlight.** The Priority column on each row is now superseded by the Build Sequence number for pick-order; it stays as a coarse tier hint.

**Priority key (uniform, founder ruling 2026-06-21):** every row's Priority is exactly one of `Tier 1` · `Tier 2` · `Tier 3` · `Tier 4` · `Gated` · `Lovable` · `Deferred` · `Done` (defined in the "How to pick" table above). The `#` column is the **Rank** (priority order, `#1` = highest); build the lowest-`#` `Tier`-class row that is unclaimed. The legacy `P0/P1/P2/WM-*/BYO-*` codes were retired into this scheme.

---

## Master register

> **Sorted by Rank = priority** (Tier 1 > Tier 2 > Tier 3 > Tier 4 > Gated > Lovable > Deferred > Done; within a tier by build order). The `#` column is the **Rank** (`#1` = highest priority); it is DERIVED, regenerated by `python3 scripts/rerank-dashboard.py` (never hand-numbered), so adding or reprioritizing a row reflows the whole list. Build the lowest-`#` `Tier`-class row that is unclaimed.

| # | Status | ID | Feature | What it does | Category | Priority | Added | Comments |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | ◐ | EVENT-REACTOR-LIVE | Activate the dormant event reactor | Turn on + exercise `event_subscriptions`/`event_queue`: emit events from the real write paths (signal clustered, outcome recorded, decision made, drift) and consume them into the trigger layer | Foundational | Tier 1 | 2026-06-23 | **v11, ambient substrate.** _Verified live 2026-06-24 (lane 2, Lovable MCP vs prod): the pipeline is ALREADY wired+scheduled — 3 `*_reactor_fanout` triggers + 12 enabled default subscriptions (emit), `event-reactor-tick` pg_cron active `* * * * *` (consume). Cold for INPUT volume only (TEST-SEED/AMBIENT-SENSE own that), not a code gap. Trigger emit is path-agnostic so no non-redundant TS emit to add._ **Retired from auto-pick (`done`).** (a) ✅ **KI-27 migration applied live 2026-06-24 via Lovable MCP** (KI-38 resolved) — `attempt_count`/`next_attempt_at` present, consume-tick query verified clean, `auto` path unblocked; (b) ✅ **new event types WIRED 2026-06-24 (lane 1, migration `20260624130000` applied live + verified on prod):** the `event_type` CHECK now allows `signal.clustered`/`outcome.recorded`/`decision.made`, 3 new `*_reactor_fanout` triggers (themes/learnings/decisions) fan them into `event_queue`, and `EVENT_TYPES` + the Controls picker offer them, so a workspace can subscribe an agent to them end-to-end (generic dispatch). Additive + dormant: no default subscription is seeded, so no overlap with the ambient trigger-tick. `drift.detected` is deferred (drift_snapshots has no workspace_id and is a per-bucket metrics rollup, not a discrete drift event, so it needs a designed drift-event source first); (c) live proof overlaps LOOP-PROVE. Detail: `docs/features/f-agent-3-event-reactor.md`, KI-38. |
| 2 | 🔨 | AMBIENT-SENSE | Continuous sensing: ingest, normalize, tag, cluster | Scheduled sensing agents that pull from a bound source + internal signal, normalize to the signal ontology, tag, and cluster into opportunities with no human start (wire the `cluster-tick` hook to a scheduler) | Sense | Tier 1 | 2026-06-23 | **v11, corrected North Star** (self-initiate at every phase). Uses a demo feed until SEN-01/F-CONN bind a real source (Gated). **◐ AUTONOMOUS SLICE SHIPPED (lane 1, 2026-06-24):** new `sense-tick` cron (`src/routes/api/public/hooks/sense-tick.ts`) + pure deterministic tagger (`src/lib/sensing/normalize.ts`, 13 tests): per opted-in workspace, with no human start, it normalizes + auto-tags untagged signals to the ontology and tops up a bounded demo feed when near-empty, feeding the existing `cluster-tick`. Migration `20260624040000_ambient_sense.sql` adds `auto_sense_enabled`/`last_auto_sense_at` (off by default; demo workspaces enabled for dev) + index; applied live via Lovable. Rule-based = ZERO AI spend. Gate-green (tsc 0, 1214 tests, lint clean). **Retired from auto-pick (`done`); remainder is FOUNDER-GATED:** (a) real-source pull via SEN-01/F-CONN; (b) the pg_cron schedule activation (recurring infra); (c) AI enrichment (spend). |
| 3 | 🔨 | AMBIENT-TRIGGER | Self-initiate a mission at the right phase | A policy layer: when a signal cluster / recorded outcome / competitor move crosses a threshold, self-originate a mission at the right station (Decide re-rank, Learn review, Critic re-eval), governed by reversibility | Sense | Tier 1 | 2026-06-23 | **v11, the self-driving behavior.** No human start; HITL only at irreversible side-effects; records its trigger + rationale to the Trust Ledger. Needs EVENT-REACTOR-LIVE + AMBIENT-SENSE. **◐ AUTONOMOUS SLICE SHIPPED (lane 1, 2026-06-24):** pure policy `src/lib/sensing/trigger.ts` (`evaluateTriggers`: clusters over freq/severity threshold + missed outcomes → mission proposals, 10 tests) + `trigger-tick` cron (`src/routes/api/public/hooks/trigger-tick.ts`): per opted-in workspace, with no human start, self-originates a mission in status `'proposed'` (which resume-runs IGNORES = zero AI spend, reversibility-gated) + writes a `decisions` receipt to the Trust Ledger. Migration `20260624050000_ambient_trigger.sql` adds `auto_trigger_enabled`/`last_auto_trigger_at` (off by default; demo workspaces enabled) + index; applied live (demo input verified: 6 hot clusters + 8 missed outcomes). Rule-based, zero AI spend. Gate-green (tsc 0, 1238 tests, lint clean). **Retired from auto-pick (`done`); remainder FOUNDER-GATED:** (a) the pg_cron schedule activation; (b) promotion `proposed`→`running` = mission EXECUTION (AI spend / HITL); (c) competitor-move triggers need a real source (SEN-01/F-CONN). |
| 4 | ◐ | BRAIN-UX-V11 | Make the Brain human-useful + an open analyst (not just a node graph) | Human lenses on the decision/memory graph (temporal "how your decisions evolved the past 3 months", proactive insights + patterns, plain-language "why did we decide X / current belief / what is unresolved", the "what Cadence has learned" story) PLUS open-ended agent-derived intelligence: predictions, contradiction/risk alerts, next-best-action, self-knowledge patterns, cost-of-inaction, hidden connections - whatever the data supports, flagged proactively | Cockpit | Tier 1 | 2026-06-23 | **Why:** the decision/memory graph IS the moat, but a node graph is how the AGENT sees it, not how a PM derives value. The lenses are the FLOOR (legibility); the open CEILING is the agent VOLUNTEERING whatever useful intelligence the data supports (the whole point of the intelligence layer; founder steer 2026-06-23: keep horizons wide). Builds on F-IA-BRAIN-GRAPH + DBR. Infra verdict (build the intelligence, self-host Postgres+pgvector, integrate a viz lib, swap to a graph engine behind a seam only if scale demands): [`../strategy/v11-guiding-star.md`](../strategy/v11-guiding-star.md). _Floor shipped 2026-06-24 (lane 2): `/knowledge` **Insights** tab (now the default) — `getBrainInsights` composes decisions+lineage+learnings into beliefs (standing/revised), what-we-learned + hit rate, a month timeline, and plain-language rule-based observations; pure lens math + 11 unit tests; RLS-scoped; no migration/chokepoint. `docs/features/brain-insights.md`. **Why + unresolved lenses shipped 2026-06-24 (lane 2):** `getBrainInsights` now also returns per-decision `rationale` + `revisedBy` (the "why we believe this" lens, supersedes-only so a contradiction is never mislabeled a revision) and an `unresolved` lens (active `contradicts` pairs touching a decision, settled-by-supersession excluded, + mixed outcomes); new pure helpers `supersedesParentMap`/`resolvedChildIds`/`activeContradictions`/`deriveUnresolved` + `supersedingIdFor`; InsightsPanel renders both (honest empty states); 22 tests; 2 adversarial-review defects fixed (revised-vs-contested disjoint; pre-cap honest count) + regression-guarded; tsc 0/1278 tests/security-clean; no migration/chokepoint. **Remaining (◐):** ONLY the AI open-analyst CEILING — predictions/next-best-action/volunteered intelligence (chokepoint-gated)._ |
| 5 | 🔨 | CORE-UX-TRUST | Move trust to the point of decision | Per-agent track record inline on the DecisionCard ("Scout: approved 44/47, 0 rollbacks") + auto-clear reversible tool gates (only gate consequential) + visible rejection-learning | Cockpit | Tier 1 | 2026-06-23 | **v11 core-user fix** (felt-experience research): the approval queue is a babysitting tax, trust payoff deferred. Files: `DecisionCard.tsx`, the loop approval-mode logic, `tool-consequences.ts`. **Core shipped 2026-06-24 (lane 2) [~75%]:** per-agent TRACK RECORD at the point of decision — `getNeedsYou` returns `trackByAgent` (decided agent_approvals tallied by the new pure `src/lib/agent-track-record.ts`: approved/total per agent, honest — approved+executed+failed = a yes, rollbacks NOT fabricated), rendered as a quiet "approved 44/47" chip on the gate `DecisionCard`. 10 tests; tsc 0/1300; security-clean; adversarial TS review fixed (failed-exec counts as approved). No migration, no chokepoint. **Rejection-learning SHIPPED 2026-06-24 (lane 2):** the queue now shows, at the point of decision, a quiet "declined Nx before" signal on a pending gate whose (agent, tool) the caller has declined before - pure `src/lib/rejection-learning.ts` (`summarizeRejections` groups the caller's REJECTED decided rows per agent+tool, keeps the latest reason; 6 tests) from the SAME RLS-scoped decided-history fetch that feeds the track record (no extra query), surfaced on the `ApprovalsPanel` card. tsc 0/1452; ts+security reviewed (security CLEAN on tenant-isolation; folded a HIGH var-shadow). **Remaining (gated, NOT autonomously buildable):** auto-clear reversible tool gates = a loop approval-mode change in the pinned `ai/loop.server.ts` chokepoint + a founder-gated agent-behavior call. |
| 6 | ◐ | CORE-UX-FELT | The felt fixes: first-run, brief, language | Harden the teardown's cold-gateway path (never dead-end on action #1) + a chief-of-staff daily brief with stakes (not counts) + de-jargon the front for the non-founder PM (missions/stations/Engine Room) | Cockpit | Tier 1 | 2026-06-23 | **v11 core-user fix.** Protects the best first-run moment + fixes deflation/alienation. Files: `WedgeTeardown.tsx`, `copilot.functions.ts`, `LoopStations.tsx`, `govern.tsx`. **Shipped 2026-06-24 (lane 2) [~85%]:** (1) brief leads with STAKES not counts — `ensureTodayBrief` folds pending gates into reversibility/blast-radius via pure `src/lib/copilot-brief.ts` (`summarizeGateStakes`/`describeStakes`, 7 tests) + a reframed prompt; (2) FOUNDER ASK — Today no longer lists every gate approval; collapsed to one calm `PendingApprovalsBar` (`src/components/today/PendingApprovalsBar.tsx`: count + a quiet "needs a closer look" hint) linking to Govern → Approvals; the per-agent track record (CORE-UX-TRUST) moved to `ApprovalsPanel` (its new point of decision) + the orphaned Today track query removed. tsc 0 / 1346 tests; adversarial ts+security review — folded 1 HIGH (added the missing `.eq(user_id)` on the brief gate query); security clean. ****Cold-gateway first-run hardening SHIPPED 2026-06-24 (lane 2):** the wedge's hard-failure path (gateway throwing / network / DB) no longer shows a raw toast on the most important first screen - pure `src/lib/wedge-cold.ts` (`classifyWedgeFailure` -> cold / transient / other; 8 tests) drives a calm inline banner with honest guidance, the form preserved for retry (429 rate-limit correctly classed transient per review). tsc 0/1459; ts+security reviewed CLEAN (no ReDoS, no new info-leak, no XSS). **Remaining (marginal / other-lane, NOT a clean autonomous slice):** de-jargon `LoopStations` (marginal wording) + `govern` (lane-1 IA-DEPTH territory). |
| 7 | ⬜ | ORCH-DELEGATE | Build = orchestration, not codegen | Dispatch the formed PRD + scoped task graph to an external coding agent OR the existing PRD->task->Linear path and govern the result; frame Build as conducting the builders | Build | Tier 1 | 2026-06-23 | **v11.** The Linear-dispatch path is buildable now; the external coding-agent half is BLD-04 (Gated). Do not build codegen (Lovable/Cursor/Devin own it). |
| 8 | ◐ | INTEROP-V11 | How agents + platforms access Cadence (inbound) | Expose Cadence to the outside: a read-only MCP server so external agents (Claude/ChatGPT/Cursor) can query the decision brain/specs/roadmap; A2A Agent Cards + scoped tokens so peer agents can call governed actions; a scoped API for platforms - all gated by scopes + audit | Interop | Tier 1 | 2026-06-23 | **Why:** an agentic platform must be reachable BY other agents/platforms, not just reach out, and today this is unsolved end to end. The read-only MCP server is buildable now; the outward WRITE/A2A surface (Q2) is founder-gated on a scopes/audit security call. Builds on the `mcp_tokens` infra + the A2A card. References Q1-MCP + Q2 (gated). Also a platform/moat play: Cadence as the governed layer other agents plug into. _Floor shipped 2026-06-24 (lane 2): read-only `search_decisions` MCP tool — exposes the decision brain (decisions + standing/superseded outcome, reusing the Trust Ledger supersession rule) to external agents, workspace-scoped + audited like the existing read tools; catalog + dispatch + 4 tests, tsc+1260 green. `docs/features/q1-mcp.md`. **Read surface repaired + completed 2026-06-24 (lane 2):** a live-schema audit via Lovable MCP found 3 existing read tools broken by drift — `search_signals` selected `summary` (real: `content`) + a non-existent `products` embed, `search_opportunities` selected `predicted_ice`/`roadmap_status` (real: `ice_score`/`roadmap_bucket`), `get_prd` queried table `prd` (real: `prds`) with non-existent cols — ALL fixed against the verified prod schema. ADDED `search_prds` (keyword/status spec discovery — the find half of get_prd) + `get_roadmap` (opportunities grouped now/next/later/unbucketed via pure `groupByRoadmapBucket`). HARDENED every search tool with `sanitizeIlikeQuery` against PostgREST `.or()` filter-injection; ICE floor now skips when min=0 so unscored opps are not silently dropped. 8 MCP tools; new tests; tsc 0/1318; adversarial ts+security review (tenant-isolation clean, injection guard added). **Remaining (◐):** the outward WRITE/A2A scoped-token surface (founder-gated on scopes/audit)._ |
| 9 | ⬜ | POS-V11 | Reconcile positioning, persona, dated specs | Apply the v11 reconciliation: one positioning sentence, one front-of-funnel persona ladder, the decision-and-outcome-layer moat thesis, refresh dated model/crypto specifics, run the strategy README cascade + the moat.md ripple | Foundational | Tier 1 | 2026-06-23 | **v11.** Doc + narrative integrity for the pitch. Files: `README.md`, `AGENTS.md` §0, the strategy docs. Positioning/pricing wording is a founder taste call. |
| 10 | ⬜ | LANDING-PAGE-V11 | Rebuild the public landing page | Rethink what the public landing page shows: lead with the outcome (the autonomous decision-and-outcome loop + the trust ledger), one clear value story, one demo/teardown CTA; strip the clutter | Launch | Tier 1 | 2026-06-23 | **Why:** the current landing page is cluttered and does not communicate what Cadence is or why a PM should care; it is the first impression for every prospect AND the pitch, so a clear, outcome-led page is high-leverage for acquisition + the raise. Content/strategy thought-through in [`../strategy/v11-guiding-star.md`](../strategy/v11-guiding-star.md). Files: the public landing route + marketing sections. |
| 11 | ◐ | DBR (H1) | The Decision Brain (typed decision knowledge graph) | Evolve flat vector recall into a typed, bi-temporal, auto-built decision graph (nodes: signal/opportunity/assumption/decision/PRD/outcome; edges: cites/supersedes/validates/contradicts) with an Obsidian-style visual graph view; signature mechanic = outcome-labeled supersession | Foundational | Tier 1 | 2026-06-22 | **Lane 2 entity-resolution + shared-premise arc COMPLETE + RELEASED (2026-06-22): guardrail #4 — v1 resolver ◐ + wired into the shared-premise walk ◐ flag-gated OFF (`DBR_ENTITY_ALIASING`); adversarial reviews SOUND (folded 2 HIGH false-merge bugs + chunked `.in()` + per-kind collapse); never touches the AI chokepoint; tsc 0 / 1189 suite. Released for the next lane — remaining DBR work is cross-lane (entity-res wiring into the governing-decision/precedent walks, Lane 1's files) or founder-gated (flip the precision-review flag on real data; the fuzzy codename AI layer; deeper-graph enrichment).** **HORIZON BET, enrich first** (schema/sequencing/lane). Spec [`../features/decision-brain.md`](../features/decision-brain.md); strategy [`../strategy/horizon-bets.md`](../strategy/horizon-bets.md). Absorbs + elevates O1 (knowledge graph, partial) + O3 (drift/skill-packs, pending). DBR-0 slice DONE 2026-06-20 ◐ (a code read corrected the plan: the outcome loop was already closed via `recordOutcome`; the real first win was giving the **Critic** decision precedent: `formatDecisionPrecedent` + a workspace-outcomes query in `runCritic`; tsc 0 / 303 tests / build green; live-verify on publish). Increment 1 = Ambient Precedent ([`../features/ambient-precedent.md`](../features/ambient-precedent.md)) is **SHIPPED to `main` (◐), 2026-06-20 cycle 55, subagent-driven, all 6 tasks** - the proactive precedent nudge over our existing outcome memories. Done: T1 outcome titles -> T2 semantic engine + pure ranker (✅ unit-verified, 3 tests) -> T3 Critic uses the engine, DBR-0 recency deleted -> T4 getDecisionPrecedent fn -> T5 PrecedentNudge UI -> T6 nudge wired into the opportunity + spec seams (3 seams now live: Critic + opportunity + spec, ◐ render-on-publish). tsc 0 / build ✓ / 322 tests / eslint 0; opus whole-branch review "ready to merge". These are TASKS under this umbrella row, NOT new register rows, so the at-a-glance counts are unchanged. Build record: SSOT §3 + §6. The deeper graph (DBR-1+) stays ⬜ pending enrichment. **Increment 2 = DBR-2 (the Critic reasons over the typed graph) SHIPPED (Lane 2, 2026-06-21):** `runCritic` now walks the `supersedes`/`contradicts` edges in `artifact_lineage` bearing on the decision under review OR on its semantically-similar precedents (their prd/opportunity ids), and injects a "Contradiction history" block so the Critic cites "a prior decision like this was CONTRADICTED/SUPERSEDED by a later outcome", which the flat precedent RAG cannot answer. New PURE `src/lib/ai/contradiction-history.ts` (`selectContradictionHistory` incident-first then current-before-retired then newest ranking + dedupe + cap; `formatContradictionHistory`; 13 tests) + a migration-tolerant, fail-safe loader in `critic.server.ts` (reuses `resolveLineageCols` to read bi-temporal `valid_to` when live and fall back pre-migration; uuid-validated `.or` filter; newest-first; `.limit(50)`). Byte-identical + fail-safe when the graph has no edges, so dormant-correct until DBR-1.5 is published + flipped on, then it lights up automatically. tsc 0 / 801 tests / eslint+dash clean; 3-lens adversarial review all SHIP, fixes folded (read valid_to so the retired distinction is live; parent-role "this" vs "a" framing; incident-first ordering; ordered query). [increments 1+2 shipped ◐] **Increment 3 = DBR-3a (governing-decision retrieval) SHIPPED ◐ (Lane 1, 2026-06-22 14:45; founder-directed "go deeper now"):** the moat's headline "return the CURRENT belief, not the similar old one". New PURE `src/lib/ai/governing-decision.ts` (`resolveGoverning` walks the `supersedes` chain child→parent to the terminal still-current node; `selectGoverningDecisions` keeps only stale precedents; `formatGoverningDecisions` renders a corrective block naming the replacement; `nextSupersessionFrontier` for closure expansion; 27 tests) DRIVEN into `runCritic` (`critic.server.ts`): when a precedent the Critic might cite has been superseded, a NEW bounded forward-closure loader (`loadSupersedesClosure`, BFS child_id→parent_id, ≤8 rounds, fail-safe, migration-tolerant) assembles the full chain so the walk reaches the TRUE current decision, then a "Governing decision" prompt block tells the Critic to rely on it, not the stale match. Distinct from DBR-2 (which red-teams the edges; this NAMES the replacement). Byte-identical + fail-safe until DBR-1.5 is published + flipped on. **3-lens adversarial review (logic/runtime/over-claim) = SHIP_WITH_FIXES;** folded the one cross-lens should-fix (the focus-bounded edge set truncated multi-hop and could name a stale intermediate as current → fixed at root with the closure loader + a loop-simulation test). tsc 0 / 1030 tests / eslint clean (my files). ◐ render-on-publish (live LLM/DB path verifies on the founder's next publish). Next DBR-3 slices: richer typed-edge auto-extraction (autonomous); deep-graph enrichment (founder-gated, schema/sequencing). **Increment 4 = DBR-3b (governing-decision on the PROACTIVE precedent nudge) SHIPPED ◐ (Lane 1, 2026-06-22 15:10):** extended DBR-3a from the Critic (a review surface) to the proactive precedent nudge ("value before you ask"). Extracted the closure loader into a reusable `src/lib/ai/governing-decision.server.ts` (`loadSupersedesClosure` + `loadContradictsForNodes` + `resolveGoverningForNodes`); refactored `critic.server.ts` to use it (DRY, behavior-preserved, verified by review); enriched `getDecisionPrecedent` (`decision-precedent.functions.ts`) to annotate each precedent row with its governing decision; `PrecedentNudge.tsx` now flags a superseded/contradicted precedent inline ("Superseded: a later decision replaced this; rely on the current one."). Pure `findGoverningFor` (+4 tests, 31 total). **Focused code-review = SHIP_WITH_FIXES; folded all 3:** skip the redundant contradicts query when the Critic already passes focus edges; **fixed a silent frontier-truncation** (chunk the BFS frontier + a 500-node cap instead of `.slice(0,50)`); documented the prd-first single-match as intentional. tsc 0 / 1034 tests / eslint+prettier clean. Byte-identical + fail-safe until DBR-1.5 is published + flipped. Breadcrumb: opportunity detail + PRD page → Precedent nudge. **Increment 5 = DBR-3c (NAME the governing decision) SHIPPED ◐ (Lane 1, 2026-06-22 15:25):** completes "return the governing DECISION, not the nearest text" — the Critic block + nudge now NAME the replacement by title ("replaced by 'New checkout flow'") instead of an opaque id. Added optional `governingTitle` to `GoverningDecisionItem`; a fail-safe server resolver `attachGoverningTitles` (batch-queries `prds`/`opportunities` by id, keyed `kind:id`, RLS-scoped, zero query when no stale items) wired into `resolveGoverningForNodes` so BOTH surfaces get titles with no `critic.server.ts` change; `formatGoverningDecisions` + `PrecedentNudge` render the name. +1 test (32). Self-reviewed (additive on already-reviewed code); table/column names confirmed by existing call sites. tsc 0 (folded a PromiseLike-vs-Promise type fix) / 1059 tests / eslint+prettier clean. Collision-safe (disjoint from Lane 2's `knowledge-graph-view.*`). ◐ render-on-publish. **Increment 6 = DBR-3d (decision-currency banner) SHIPPED ◐ (Lane 1, 2026-06-22 15:39):** governing-decision so far flagged stale PRECEDENTS; this flags the decision the user is VIEWING if IT has itself been superseded/contradicted (a user shouldn't unknowingly act on a stale decision). New server fn `getDecisionCurrency` (`src/lib/decision-currency.functions.ts`, resolves the viewed entity's own currency via the shared `resolveGoverningForNodes` + `findGoverningFor`) + a fail-safe `DecisionCurrencyBanner.tsx` placed above the precedent nudge on the opportunity detail + PRD page ("This decision was superseded by 'New checkout flow'. Act on the current one, not this."). Self-reviewed; folded a real UI copy bug (double "by"). tsc 0 / 1064 tests / eslint clean (the 1 warning on the PRD page is pre-existing react-hooks/exhaustive-deps, not mine). Renders nothing until the entity is graph-marked stale → byte-identical until edges accrue. Breadcrumb: Discovery → opportunity / Specs → PRD → (banner above the precedent nudge). **Increment 7 = DBR-3e (decision precedent cited in chat/Ask) SHIPPED ◐ (Lane 1, 2026-06-22 15:57):** the 5th "value at every step" moment — the brain now volunteers DECISION precedent IN CONVERSATION. One file (`src/routes/api/chat.ts`): a fail-safe block loads `loadDecisionPrecedent` for the user's message and pushes a `formatDecisionPrecedent` block into the chat answer's `systemParts`, so the assistant grounds in the workspace's OWN outcome history ("you shipped a similar bet and it missed") instead of generic advice. Threshold-gated (conservative 0.3) + fail-safe + byte-identical when no outcomes match. **Focused code-review (feature-dev:code-reviewer) = SHIP_WITH_FIXES; folded all 3:** (1) clarified the `[VALIDATED]` tags are labels not `[n]` citations (research-mode collision); (2) added the passive-text / no-injection rider the RAG block has; (3) min-length guard (>= 4 words) so trivial messages skip the embed (cost). tsc 0 / 1064 tests / eslint clean. Collision-safe (chat.ts not chokepoint-pinned). ◐ render-on-publish (live chat citing precedent verifies on publish). Breadcrumb: any chat conversation (Ask) on a decision-bearing question. **Increment 8 = DBR-3f (shared-premise precedent) SHIPPED ◐ (Lane 2, 2026-06-22):** the multi-hop graph-over-vectors query "what happened last time we reasoned this SAME way" - walks the DERIVATION graph UP to the decision's premise ancestors (signal/opportunity/theme) then DOWN to their OTHER descendants (cousins sharing a premise, off the target's own path) and reports each cousin PRD's recorded outcome, missed-first. The signal flat vectors structurally can't give ("the last PRD from this same opportunity MISSED" - shared premise, not similar text). Distinct from DBR-2/3 (those walk the supersedes/contradicts reversal edges; this walks promoted/cites/derived-from/depends-on). PURE walk + ranking + format (`src/lib/ai/shared-premise.ts`, 21 tests incl. a depth-complete subtree-exclusion regression) + two bounded fail-safe BFS closures + a `prds.outcome` join (`src/lib/ai/shared-premise.server.ts`) + an independent fail-safe block in `runCritic` (`src/lib/ai/critic.server.ts`). 3-lens adversarial-review Workflow (correctness/runtime-safety/over-claim) = unanimous SHIP, 0 must-fix; folded the depth-complete exclusion + an honesty-softening of the block framing. tsc 0 / 1102 tests. Byte-identical + fail-safe until derivation edges + outcomes both exist. Spec: [`../features/decision-brain.md`](../features/decision-brain.md) DBR-3f. **Increment 9 = DBR-3g (shared-premise precedent on the proactive nudge) SHIPPED ◐ (Lane 2, 2026-06-22):** extended DBR-3f from the Critic to the proactive nudge (mirrors DBR-3b's Critic -> nudge fan-out). Refactored the server resolver to a structured `resolveSharedPremiseItems` (one graph in, both surfaces out; the Critic formatter is now a thin wrapper, behavior-preserved) + a new `getSharedPremisePrecedent` server fn (`src/lib/shared-premise.functions.ts`, RLS-scoped, fail-safe) feeding a new `SharedPremiseNudge.tsx` mounted on the opportunity + PRD detail surfaces. Distinct from PrecedentNudge (text-similar): this shows decisions built on the SAME upstream premise + how each landed. Focused code-review = SHIP_WITH_FIXES; folded both (null-summary trailing-colon; `enabled: !!targetId` guard). tsc 0 / 1110 tests. Renders nothing until derivation edges + outcomes exist. Files: `src/lib/shared-premise.functions.ts` + `src/components/decision/SharedPremiseNudge.tsx` (new) + `src/lib/ai/shared-premise.server.ts` + `src/components/product/OpportunityDetail.tsx` + `src/routes/_authenticated.prds.$id.tsx`. **Increment 10 = DBR-3h (NAME the shared premise) SHIPPED ◐ (Lane 2, 2026-06-22):** the precedent now names WHICH premise is shared ("the same opportunity 'X'") instead of "the same ground" - felt-voice completion. `collectSharedPremiseCousins` additively tracks premise provenance (a `seedOf` map; BFS first-reach = the CLOSEST common ancestor/LCA); `attachPremiseTitles` (user_id-scoped, fail-safe) resolves the premise title; format + nudge name it, generic fallback. Focused code-review = SHIP_WITH_FIXES; folded both (user_id defense-in-depth on title queries; deterministic `ancestorKind` via child_kind). +3 tests (24). tsc 0 / 1113 tests. Files: `src/lib/ai/shared-premise.{ts,server.ts,test.ts}` + `src/components/decision/SharedPremiseNudge.tsx`. |
| 12 | ◐ | IA-DEPTH-V11 | The deeper consumer-grade IA refinements | Merge Missions into Build; one connections home (Settings->Connections, `/sync` the deep view); demote the Products list into the workspace switcher; sub-group the 13-tab Engine Room into 3 bands (Needs you / Trust & safety / Quality & insight); promote the Strategic Brief out of settings as a first-class steering entry; de-jargon residual labels ("Models"->"AI", retire "Loop"/"Trust") | Cockpit | Tier 2 | 2026-06-23 | **v11 IA depth** (founder-prompted, after the P0 nav+settings). Removes duplicate homes + mechanism-naming per the engine-room doctrine. Files: AppShell.tsx, settings.tsx, govern.tsx, build/missions/knowledge routes. **Slice shipped 2026-06-24 (lane 1) [~35%]:** the **Engine Room 13-tab → 3 bands** sub-grouping — new pure `src/lib/engine-room-bands.ts` (Needs you = controls/attention/approvals · Trust & safety = team/guardrails/budgets/incidents · Quality & insight = prompts/evals/analytics/gauntlet/traces/drift; `bandForTab`/`tabsInBand`/`primaryTabForBand`; 9 tests) drives a two-tier nav on `/govern` (a `SubTabs` band selector + the existing `TabRow` filtered to the active band, badges + per-tab descriptions preserved); every `?tab=` id unchanged so deep links land; the default `controls` lands in the first band. Also: "one connections home" is largely covered by CONNECTORS-V11 (`/sync` catalog). tsc 0 / 1397 tests; no migration/chokepoint. **Remaining (◐):** merge Missions into Build + demote Products into the workspace switcher + promote the Strategic Brief + final de-jargon — several touch the loop surfaces lane 2 holds (STITCH-LOOP), so they wait for that to land. UI live-verify on publish. |
| 13 | ⬜ | DESIGN-V11 | Consumer-grade design relook of the v11 surfaces | The per-surface design pass to consumer-grade on the v11 heroes (Today, Trust Ledger, the wedge, the brief, the stitched loop) once the core is solid | Cockpit | Tier 2 | 2026-06-23 | **v11 design relook** (founder-prompted, after core). Extends the standing §14 design pass; loads the Ember system + the engine-room/design-context conventions. |
| 14 | ◐ | DEF-04 | Designer scaffolds (spec to mockup to preview) | Generate a design scaffold from a spec: a mockup + a sandbox preview + a design-token + accessibility check, so a PRD can become a previewable design before build | Build | Tier 3 | 2026-06-23 | **Why:** salvaged from the now-superseded feature-backlog as a genuinely-pending feature not yet on the board; lets the loop produce a previewable design from a spec (M3 scope, post-pitch). Relates to SANDBOX (preview) + DESIGN-V11. **◐ Shipped (lane 2, 2026-06-24) [~50%], the deterministic half:** pure `src/lib/design-readiness.ts` (`analyzeDesignReadiness` scans a PRD spec for the 8 things design needs named - states/edge-cases/a11y/responsive/copy/permissions/data/flow - and returns a score + level + per-dimension gaps; `readinessGaps`; markdown-stripping `normalize`; 6 tests) + `DesignReadinessPanel` on the PRD page (a calm "Design readiness - N/8 - level" bar + the gaps to close before design; client-side on the loaded body, silent on an empty spec). No AI/key/DB/chokepoint. tsc 0 / 1437 tests; ts+security reviewed CLEAN (ReDoS-safe regexes, no XSS - the body is only scanned, never rendered). **Deferred (gated remainder):** the generative half - an AI-drafted mockup (needs a CallSurface in the pinned ai/* chokepoint) + a live sandbox preview (the SANDBOX provider-pick, founder-gated). |
| 15 | ◐ | REPO-DECLUTTER-V11 | Declutter the repo structure + archive stale docs | A careful pass: physically archive the superseded `feature-backlog.md` + other stale/redundant docs into archive/, updating ALL inbound links in the same commit (anti-rot rule); reconcile any stray F-IDs into the dashboard; flatten the doc tree where it is overwhelming | Foundational | Tier 3 | 2026-06-23 | **Why:** the founder finds the project structure overwhelming (the consumer-grade lens applied to the repo). The superseded `feature-backlog.md` alone had ~25+ live inbound links, so a clean archive needs a link-update pass, not a rushed move; tracked here so it is done carefully per the doc-closure + anti-rot rules. **Archive slice shipped 2026-06-24 (lane 1) [~50%]:** `git mv docs/planning/feature-backlog.md → docs/planning/archive/feature-backlog.md` (verified superseded — it carries its own SUPERSEDED banner since 2026-06-23), with a RESOLUTION-BASED inbound-link migration (a Python pass that, per referencing file, only rewrote tokens that actually resolved to the old path — handling 25+ distinct relative-path variants safely) across 28 live docs + 1 source comment (`outcome.functions.ts`). **Verified: 0 links broken by the move** (157 refs now resolve to the new location; 85 pre-existing-broken refs in frozen archive/historical docs were left untouched — they never resolved to the real file, not this move's regression). tsc 0 / 1440 tests. **Remaining (◐ → founder/coordinated):** flatten the doc tree (consumer-grade structure — founder taste), reconcile stray F-IDs into the dashboard, and fix the 85 pre-existing broken historical refs; archiving further "stale/redundant docs" needs a founder call on what is stale. |
| 16 | ◐ | M1 / LRN-01 | Support triage loop | Tickets to drafted replies to bug clusters to signals; support feeds back into Discover | Launch | Tier 3 | 2026-06-22 | **Autonomous CORE shipped (Lane 2, 2026-06-22, increment 1):** `support_tickets` migration (`20260622090000`) + pure Unicode-aware greedy-leader triage engine + dormant draft seam + server fns — `runSupportTriage` emits recurring clusters as `source='support-triage'` signals → feeds Discover. Never touches the AI chokepoint. 2 adversarial reviews (no fatal; folded a HIGH single-link bridge-merge fix). tsc 0 / 33 support tests / 1146 suite. **`done`-marked: no further AUTONOMOUS slice — all three remainders need the founder:** (1) UI surface PLACEMENT is a taste/IA call (engine-room/home-and-today doctrine gates new surfaces); (2) inbound channel = a connector OAuth + spend; (3) AI-written draft = chokepoint + spend. Verify the core on next publish (migration applies then). [~55%] |
| 17 | ◐ | SEC-INGEST-INJECTION | Injection screening on untrusted ingest | Run the existing injection-classifier on untrusted ingested content (support tickets first) so a poisoned input cannot inject an autonomous action into the Discover → agent pipeline (`considerations.md` #3, P0 — the product's defining risk: untrusted input feeding agents that take actions) | Governance | Tier 3 | 2026-06-22 | **SHIPPED ◐ (Lane 2, 2026-06-22):** the support-ticket boundary is screened — `runSupportTriage` runs `classifyInjection`/`assessCorpusInjection` (pure `src/lib/support/screening.ts`) on each cluster's raw text before it can become a Discover signal: a structural attack quarantines the cluster (never emitted), a lexical-only override is tagged `needs-review`, and a ticket merely QUOTING an injection is NOT over-dropped (the classifier's structural gate). Reuses existing infra, no schema change, never touches the AI chokepoint. tsc 0 / 40 support tests / 1196 suite. **`done`-marked: support-triage scope complete; dormant with support-triage (no live channel) — verify on publish.** The analogous screen on the LIVE signal-ingest webhook + external MCP/A2A results is a separate future item. |
| 18 | ⬜ | DEMO-SEED-RICH | Rich showcase seed (closure, LAST) | A deterministic, realistic seed that writes real `artifact_lineage` decision + supersession edges + recorded outcomes so every panel renders a full, believable product story for EXTERNAL showcase | Launch | Tier 4 | 2026-06-23 | **Why:** the moat/provenance panels must render full + believable when we show the platform externally, but only AFTER the capabilities are built, so the showcase reflects a REAL product, not a faked one. **Founder ruling 2026-06-23: rich seeding is LAST, not first** (demoted from #1). Build the capabilities (which generate real data), then this rich showcase pass + a final content re-clean before going external. Minimal dev/test data is TEST-SEED (#1). |
| 19 | ⬜ | SHIP-V11 | Ship the v11 loop to consumer-ready | The consumer-ready shipping pass across the full stitched loop: QA every path, the humanization sweep, performance + accessibility, and the go-live config; the final gate to a user-ready platform | Foundational | Tier 4 | 2026-06-23 | **v11 final ship** (founder-prompted, last). Composes with HUMAN-SWEEP. The end of the build -> stitch -> wire -> design -> ship journey. |
| 20 | ⏭️ | HUMAN-SWEEP | Full-product humanization sweep | Sweep all UI strings + seed data for AI fingerprints | Foundational | Tier 4 | - | deferred: pre-launch gate, so screen churn does not force a re-sweep |
| 21 | ◐ | EMBED-CHOKEPOINT | Route embeddings through the AI chokepoint | embed.server.ts calls the gateway directly today (no cost/credit/BYO/logging); route via callModel + add model_id/dimension cols + backfill | Foundational | Gated | 2026-06-21 | BBI build-now #1 (a flagged chokepoint-bypass). **Bypass CLOSED (Lane 1, 2026-06-21, founder-attended):** `src/lib/rag/embed.server.ts` is now the embeddings chokepoint instead of fetching the gateway raw. New `embedThroughChokepoint(inputs, opts?)` + `resolveEmbedRoute` (byoOverride > auto-loaded OpenAI BYO key > Lovable gateway) + `estimateEmbedTokens` + best-effort `ai_events` telemetry (surface `embed`), REUSING the shared primitives the completion chokepoint uses (`estimateCostUsd` from pricing.ts, `loadBYOKey` from the vault, the `ai_events` table) rather than bloating the 1968-line `runtime.server.ts` (embeddings are a distinct API shape that does not fit `callModel`; this also keeps zero risk to the completion hot path). `embedTexts`/`embedOne` stay back-compat (opts optional) so the 8 callers (incl. the pinned `memory.server.ts`/`registry.server.ts`) are UNCHANGED; the bulk path (`indexer.server.ts`) threads `{supabase,userId,surfaceRef}` so it is genuinely DRIVEN (logs + BYO-routes). 1-agent security+correctness review: 2 real fixes folded (dropped the speculative `byoOverride.baseUrl` SSRF/key-exfil vector → OpenAI-only fixed endpoint; redact `sk-` tokens from error bodies), 1 finding rejected as a false positive (the cross-batch array is dense — `out[i+row.index]` fills 0..N-1 and the indexer reads all of `toEmbedIdx`). tsc 0 / eslint 0 / `bun test` 704 (8 new). **◐ not ✅:** routing + cost + telemetry are unit-verified; the live gateway/BYO call + embed `ai_events` rows verify on the founder's next publish. **INCREMENT 2 SHIPPED (Lane 1, 2026-06-21):** context threaded into the 4 remaining NON-pinned embed callers — the RAG query embedder (`retriever.server.ts`, the chat-retrieval path), `decision-precedent.server.ts`, `findings.server.ts`, `reflection.server.ts` — so each now logs an embed `ai_events` row + BYO-routes (was an unattributed gateway call). A BYO OpenAI key now covers the chat-retrieval embedding path (not just bulk indexing), readying clean live testing once credits land. 4 one-line additive edits (graceful catches preserved); proportionate self-review (trivial additive threading on already-reviewed infra). tsc 0 / eslint 0 / `bun test` 767. **Remaining:** the 2 PINNED callers (`memory.server.ts` recall, `registry.server.ts` memory.remember) need the chokepoint pin (attended); the forward-looking `embedding_model`/`dims` columns + backfill (only when the model/dims change). Spec: [`../strategy/build-buy-integrate.md`](../strategy/build-buy-integrate.md). · **2026-06-22 (Lane 1, closure pass): → Gated.** Verifier: the embeddings chokepoint infra is BUILT + gate-green (2 increments, 767 tests). The remaining two callers (memory.server.ts:102, registry.server.ts:245) live in PINNED chokepoint files an attended founder session must edit. -> Gated 👤 (chokepoint/attended), off the autonomous board. Not a buildable slice for an autonomous lane. |
| 22 | ◐ | FIRECRAWL-FLOOR | Firecrawl autonomy-floor fallback | firecrawl.server.ts hard-throws on a missing key; add a native SearXNG self-host sibling fallback | Foundational | Gated | 2026-06-21 | SHIPPED ◐ (lane 1, 2026-06-21 05:17): the agent's `web.search` is now provider-agnostic. Additive seam in `webSearch` (pure `web-search-fallback.ts` backend selection + SearXNG normalization, 19 tests; `searxng.server.ts` shell): FIRECRAWL_API_KEY → existing path byte-identical; else SEARXNG_URL → native self-host SearXNG floor; else a clear error naming both. Leaf web tool only (no registry/chokepoint edit); untrusted-input guardrail contract preserved. 2-lens adversarial review: **ship, 0 must-fix** (query-injection-safe via URLSearchParams; wire-shape-defensive). tsc 0 / 528 tests / build green. ◐ live-verify on a deployment with SEARXNG_URL. **FIRECRAWL-FLOOR-b SHIPPED (Lane 3, 2026-06-21):** the /research pipeline now inherits the floor. `runResearch`'s web gate (`research.server.ts:319`) read `!!FIRECRAWL_API_KEY` alone, so a SearXNG-only deployment got no web research from /research even though `webSearch` already routed to the SearXNG floor one layer down. The gate now reuses the SAME `selectWebBackend(...) !== "none"` predicate `webSearch` uses (no drift); Firecrawl deployments byte-identical, SearXNG-only now reaches the web leg, `gatherWeb`'s `.catch` degradation preserved. tsc 0 / 875 tests (gate covered transitively by the selector's tests) / eslint 0. Single-agent adversarial review: **APPROVE, 0 findings** (no regression, graceful degrade intact, gate/selector drift impossible, no orphaned raw guard, no new injection surface). ◐ live-verify on a `SEARXNG_URL`-only deployment. Spec: docs/features/firecrawl-floor.md. [~95%] · **2026-06-22 (Lane 1, closure pass): → Gated.** Verifier: code fully built + gate-green (19 tests, adversarial-review 0 findings); both the main floor + the /research gate shipped. Remaining is OPERATIONAL DEPLOYMENT the founder owns: deploy a SearXNG instance, set SEARXNG_URL, unset FIRECRAWL_API_KEY, then verify web.search + /research reach the web leg. -> Gated 👤 (founder deploy + verify), off the autonomous board. |
| 23 | ◐ | SANDBOX | Sandbox / preview spine | A sandbox to run agent tests + preview a build before merge | Build | Gated | - | needs founder to pick a provider (Cloudflare Sandbox SDK / E2B / Vercel) + infra/spend ⬆️ UN-GATED 2026-06-21: build the ExecProvider seam + the GitHub Actions $0 CI floor autonomously now; the paid sandbox provider (Cloudflare Sandbox SDK, ~$5/mo) is a small founder spend-confirm, not a blocker to start (sourcing-map). **◐ SHIPPED 2026-06-21 (lane 3): the ExecProvider seam + the $0 CI floor.** New `src/lib/exec/provider.ts` = the `ExecProvider` seam: `githubActionsProvider` (the $0 floor, `available:true`, `verdictFromChecks` reuses `studio-ci.ts` `overallFromChecks`/`mergeReadinessFromCi` so it can never drift from the `studio.pr.merge` gate) + `RESERVED_PROVIDER_IDS` (cloudflare-sandbox/e2b/vercel, reserved-not-wired) + `resolveExecProvider` (floor-default; reserved/unknown/absent pref → floor, never strands). New `.github/workflows/ci.yml` = the **$0 CI floor**: runs `tsc --noEmit` + `bun test` on push-to-main + PR (build + full lint deliberately excluded — pre-existing red baselines: Lovable vite ESM cycle, ~4k legacy eslint); `permissions: contents:read`, concurrency-cancel, `--frozen-lockfile` (review empirically verified the install + gates pass). 9 tests. tsc 0 / bun test 711 (9 new) / eslint clean / YAML valid. **3-lens adversarial review (seam-reuse · CI-green+safe · doctrine): all 3 ship, 0 blockers.** Un-gated prep only — ZERO founder-only actions (no metered spend, no secret, no paid account, no outward surface). Spec: [`../features/sandbox-spine.md`](../features/sandbox-spine.md). **Remaining (founder-gated / next):** the Cloudflare Sandbox SDK adapter (founder spend-confirm = sourcing-map call #4) + wiring the seam into a live consumer (e.g. route `studio.pr.merge` through `resolveExecProvider`, or a preview surface). [~55%] · **2026-06-22 (Lane 1, closure pass): → Gated.** Verifier: the ExecProvider seam + $0 GitHub-Actions CI floor are built + gate-green (9 tests); resolver never strands (falls back to the $0 floor). Remaining needs the founder to pick the Cloudflare Sandbox SDK provider + its spend (sourcing-map call #4); the live-consumer wiring is the next increment, best done with that choice. -> Gated 👤 (provider pick + spend), off the autonomous board. |
| 24 | ◐ | BLD-04 | Delegate-out to external coding agents | A2A-style hand-off of build work, still governed | Build | Gated | 2026-06-21 | **DORMANT SEAM SHIPPED (lane 3, 2026-06-21):** new self-contained `src/lib/delegate/**` — a `DelegateProvider` seam + OpenHands self-host adapter mirroring the SANDBOX `ExecProvider` pattern (pure contract + request/response mapping in `provider.ts`; env/HTTP adapter + `resolveDelegateProvider`/`submitDelegation` in `openhands.server.ts`). **Structurally dormant:** `available` needs BOTH `DELEGATE_OUTBOUND_ENABLED` + `OPENHANDS_ENDPOINT`; default resolves to a null floor that accepts nothing with NO network call (unit-tested: fetch is never reached when dormant). Fail-safe (transport error/non-2xx/malformed -> refusal, never throws; no phantom acceptance), bounded (30s timeout + task-size cap). Touches ZERO existing files. tsc 0 / eslint 0 / 811 tests (16 new). 2-lens adversarial review. **Deferred (the wiring increment, needs pinned chokepoint files + founder config):** the `delegate.openhands` ToolDef + `TOOL_REGISTRY` registration (`registry.server.ts` pinned), the `tool-consequences.ts` high-risk classification + `loop.server.ts` review-force/pause, the job-id persistence + callback, and the founder's BYO endpoint/key. **WIRING INCREMENT SHIPPED (Lane 2, 2026-06-21):** the deferred governance wiring is now done. New `delegate.openhands` ToolDef in `registry.server.ts` (+ TOOL_REGISTRY registration) that calls lane 3's `submitDelegation(...)` from `src/lib/delegate/openhands.server.ts` — NO duplicate seam (an earlier same-session duplicate at `src/lib/ai/delegate*` was discarded once lane 3's seam was found). Governance layer on top of the dormant adapter: the tool REQUIRES `evidence_ids` (zod `.min(1)`, non-blank kind/id, the moat's "external work must be justified"); it is added to `HIGH_RISK_FORCE_REVIEW` in `loop.server.ts` so a human ALWAYS approves before a delegation leaves (the gate lane 3 said the caller owns); and it is catalogued in `tool-consequences.ts` as irreversible + external → `toolRisk` high → counted by the per-agent blast-radius cap. Mission-only. Dormant-safe: with `DELEGATE_OUTBOUND_ENABLED` off (default) `submitDelegation` resolves to the null floor and returns `accepted:false` with no network call. tsc 0 / 857 tests (lane 3's 16 seam tests + a delegate.openhands classification test) / eslint+dash clean; focused adversarial verification of the prior approach + the established gating pattern. Remaining deferred: the job-id persistence + callback to fold the external result back as a mission_step, and the founder's `OPENHANDS_ENDPOINT`/key. ◐ live on founder config + the result-callback increment. Spec: [`../features/bld04-delegate-out.md`](../features/bld04-delegate-out.md). · **2026-06-22 (Lane 1, closure pass): → Gated.** Verifier: the delegate-out seam (Lane 3) + the governance wiring (Lane 2: delegate.openhands ToolDef + evidence-id gate + HIGH_RISK_FORCE_REVIEW) are built + dormant + tested (857 tests). The remaining result-callback increment (job-id persistence + poll/fold-as-mission_step) is only testable end-to-end once the founder sets OPENHANDS_ENDPOINT/key, so it is best built WITH that config. -> Gated 👤 (founder endpoint + key), off the autonomous board until then. |
| 25 | ⬜ | WM-M9 | Remove BYOK from self-serve (enterprise-only) | Credits-only self-serve; retire the user-key path; model-agnostic routing (our keys) stays | Monetization | Gated | 2026-06-22 17:30 | effort S; needs WM-M1 ⚖️ STAYS (2026-06-21): BYOK removal at the chokepoint is product-infra, attended; sequence with Lovable's pricing surfaces. · **2026-06-22 (Lane 1): → Gated (off the autonomous board).** Verifier: completing this REQUIRES removing the BYO routing branch from the pinned chokepoint `src/lib/ai/runtime.server.ts` (lines ~1163/1223) — a UI/function-gating-only slice would leave the routing live and circumventable, which is worse. 👤 FOUNDER/attended: this is a chokepoint edit, sequenced with the founder. Reclassified Gated so no autonomous lane re-picks a chokepoint item. NOT part of Lovable's billing work; closed here only to stop the monetization re-pick churn. |
| 26 | ⏸️ | SEN-01 | Connector dock: 2nd live ingest | Adds a second real ingest source (Slack / GitHub issues / support) so the loop closes on real data | Sense | Gated | - | paused: needs founder to register one provider OAuth client (F-CONN) 👤 FOUNDER: register 1 provider OAuth client (Slack / GitHub-issues / a support tool) - a secret/account only you can create; the adapter ships the moment one source connects. |
| 27 | ⬜ | Q2 | A2A server/client + Agent Cards + scopes/audit | Peer agents discover and call Cadence, governed by scopes + audit | Interop | Gated | - | needs founder decision on outward-facing surface + scopes/audit posture 👤 FOUNDER: approve exposing an OUTWARD A2A surface to external peer agents + the scopes/audit posture - a security/trust call (the append path stays human-gated). |
| 28 | ⬜ | SEN-05 | Quant analytics inbound | Ingests PostHog/Amplitude/Mixpanel product metrics as a first-class signal | Sense | Gated | - | gated on a product-analytics connector OAuth + recurring spend 👤 FOUNDER: connect a product-analytics provider (PostHog recommended) - an OAuth connection + recurring spend, your call. |
| 29 | ⬜ | F-ANALYTICS-1 | Cohort metrics + telemetry ingestion | Released features get real usage data into `product_analytics` | Launch | Gated | - | depends on SEN-05 👤 FOUNDER-DEPENDENT: needs the analytics connection (SEN-05); no separate action from you. |
| 30 | ⬜ | F-ANALYTICS-2 | Opportunity impact eval | Post-release cohort feeds Product Memory and auto-adjusts ICE; the loop learns if a bet paid | Launch | Gated | - | depends on F-ANALYTICS-1 👤 FOUNDER-DEPENDENT: needs F-ANALYTICS-1's cohort data; the auto-ICE logic builds itself once data flows. |
| 31 | ⬜ | BYO-P1a | RepoProvider interface + GitHub adapter | Lifts GitHub calls behind a provider-agnostic interface (behavior-preserving) | BYO | Gated | 2026-06-19 | keystone; pending founder greenlight (no code until approved) 👤 FOUNDER: ONE greenlight unblocks the whole BYO lane - a strategic scope + infra/spend call (the all-in-one platform), correctly sequenced BEHIND the Decision Brain. Not now; yours to greenlight when the loop is proven. |
| 32 | ⬜ | BYO-P1b | Product-level repo binding + per-Product RLS | Moves binding workspace -> product; per-Product UI + RLS | BYO | Gated | 2026-06-19 | pending greenlight; parallel P1a; must honor workspace `agent_memory` RLS 👤 FOUNDER: ONE greenlight unblocks the whole BYO lane - a strategic scope + infra/spend call (the all-in-one platform), correctly sequenced BEHIND the Decision Brain. Not now; yours to greenlight when the loop is proven. |
| 33 | ⬜ | BYO-P1c | Managed / auto-create repo (user's own org) | User creates a repo in their own account/org; portable, value-locked not hostage | BYO | Gated | 2026-06-19 | pending greenlight; needs BYO-P1a, BYO-P1b 👤 FOUNDER: ONE greenlight unblocks the whole BYO lane - a strategic scope + infra/spend call (the all-in-one platform), correctly sequenced BEHIND the Decision Brain. Not now; yours to greenlight when the loop is proven. |
| 34 | ⬜ | BYO-P1d | Calm-front Build surface | One product-framed decision on a new repo; git behind the Engine Room | BYO | Gated | 2026-06-19 | pending greenlight; needs BYO-P1a, BYO-P1b 👤 FOUNDER: ONE greenlight unblocks the whole BYO lane - a strategic scope + infra/spend call (the all-in-one platform), correctly sequenced BEHIND the Decision Brain. Not now; yours to greenlight when the loop is proven. |
| 35 | ⬜ | BYO-P2 | Multi-provider (GitLab; Bitbucket demand-gated) | GitLab launch pair; Bitbucket demand-gated; each adapter bounded by the interface | BYO | Gated | 2026-06-19 | pending greenlight; needs BYO-P1a 👤 FOUNDER: ONE greenlight unblocks the whole BYO lane - a strategic scope + infra/spend call (the all-in-one platform), correctly sequenced BEHIND the Decision Brain. Not now; yours to greenlight when the loop is proven. |
| 36 | ⬜ | BYO-P3 | Autonomous Build to Ship + capture | Agent runs the whole chain; deploy capture + in-app changelog; PRD join | BYO | Gated | 2026-06-19 | pending greenlight; effort L; needs BYO-P1d 👤 FOUNDER: ONE greenlight unblocks the whole BYO lane - a strategic scope + infra/spend call (the all-in-one platform), correctly sequenced BEHIND the Decision Brain. Not now; yours to greenlight when the loop is proven. |
| 37 | ⬜ | BYO-P5 | Managed end-to-end runtime | DB + auth + hosting so the user launches without leaving Cadence | BYO | Gated | 2026-06-19 | founder-gated; sequenced last (needs BYO-P3 + loop proven) 👤 FOUNDER: ONE greenlight unblocks the whole BYO lane - a strategic scope + infra/spend call (the all-in-one platform), correctly sequenced BEHIND the Decision Brain. Not now; yours to greenlight when the loop is proven. |
| 38 | ⏸️ | F-CONN | Connector platform (OAuth) | The connector engine that brings external sources in; built but parked pending a founder OAuth-client registration | Sense | Gated | - | parked: needs founder to register an OAuth client; unblocks SEN-01 👤 FOUNDER: register the OAuth client(s) (GitHub App / Linear) - secrets/accounts only you hold. Unblocks SEN-01. |
| 39 | ⏸️ | M-C-EXPIRY | Memory-expiry enforcement engine | Free-tier memory expiry, built but gated off via memory_expiry_enabled() | Monetization | Gated | - | dormant: gated off; WM-M2 reworks it to a 30-day rolling window ⚖️ STAYS (2026-06-21): memory decay is a product lock-in mechanic; only the live flip is monetization-timing. 👤 FOUNDER: flip free-tier memory-expiry when first-win is reliable - a product-timing/taste call. The decay engine is already built; only the on-switch is yours. |
| 40 | ⬜ | CMD (H2) | The Command Canvas (NL command bar + live preview) | A natural-language intent bar (Lovable/Cursor-like, NOT a syntax CLI) as the primary surface with a live preview/canvas pane on the right; calm-front compliant (Linear pattern); preview half first. Resolves the overlap with today's Ask (`/chat`): the Canvas is the evolution of Ask, not a second box | Cockpit | Gated | 2026-06-20 | **PARKED 2026-06-20 (founder): only H1 (Decision Brain) is active; preserved, not deleted.** Spec [`../features/command-canvas.md`](../features/command-canvas.md). Would evolve the Ask/`CommandPalette.tsx` (⌘K nav → NL intent) + adds the canvas pane (CMD-0). GUI fallback preserved. 0% [not started] 👤 FOUNDER: parked pending your H2 enrichment (scope + sequencing); only H1 (Decision Brain) is active - a scope/taste call, not buildable until you un-park. |
| 41 | ⏭️ | WM-M17 | Clubbed usage variants (Max 5x/20x + Team Standard/Premium seats) | One plan name, multiple priced usage variants (Anthropic-style), chosen at checkout | Monetization | Gated | 2026-06-22 17:30 | Deferred behind core builds; needs WM-M3, WM-M6, WM-M11. M17a Max (per-account); M17b Team seats (per-seat). Spec: bible §2.4.1 + §4.2.2. · 2026-06-21 reconcile: confirmed NOT built — the per-tier credit-volume dropdown is NOT the Anthropic-style usage variant. · **2026-06-22 (Lane 1): → Gated, part of the closed monetization block.** 👤 FOUNDER: this is a pricing-packaging build whose multipliers/labels/prices are founder-gated numbers (Max "5x"/"20x" ≈ $100/$200, Team seat Standard/Premium) — SSOT §4. Off the autonomous board; build only when the founder sets the packaging. |
| 42 | ⏭️ | WM-M19 | Enterprise usage model (per-seat + API-rate usage + per-user allocation) | Per-seat + metered API-rate usage + per-user credit allocation / spend limits (on WM-M14 caps) | Monetization | Gated | 2026-06-22 17:30 | Deferred; needs WM-M3, WM-M14. Enterprise sequenced post-loop-proof. Spec: bible §4.2.2 · 2026-06-21 reconcile: confirmed NOT built — Cosmos is a static contact-sales card; no per-seat + API-rate metered usage. · **2026-06-22 (Lane 1): → Gated, part of the closed monetization block.** 👤 FOUNDER: the enterprise usage model + per-user allocation policy is a founder decision (model + numbers — SSOT §4). Off the autonomous board; build only when the founder picks the model. |
| 43 | ⬜ | SEN-04 | Researcher watchtower | Scheduled competitor crawl briefs; ambient competitive signal without manual research | Sense | Deferred | - | cut/defer: post-PMF |
| 44 | ⬜ | F-AUDIO-1 | Speech transcription + chunking | Upload meeting audio to transcript to diarized chunks | Sense | Deferred | - | cut/defer: post-PMF |
| 45 | ⬜ | F-AUDIO-2 | Action-item extraction from transcripts | Meetings become drafted opportunities/PRDs citing the transcript | Sense | Deferred | - | cut/defer: post-PMF; depends on F-AUDIO-1 |
| 46 | ⏭️ | K1-deploy | Cadence-triggered deploy gate | Trigger the actual deploy from Cadence | Build | Deferred | - | deferred: needs a Cloudflare/Lovable deploy hook + founder config; deploy is external today |
| 47 | ⏭️ | F-COCKPIT-MACHINE-MODE | Human <-> Machine mode toggle | Full-screen dispatch board, the "watch the factory" view | Cockpit | Deferred | - | absorbed by F-IA-V4 ⏭️ SUPERSEDED 2026-06-21 by the Engine-Room Doctrine (a full-screen control-room mode is the control-room creep the doctrine forbids). Do NOT build as a distinct mode. |
| 48 | ⏭️ | WM-S1 | Sample workspace for every new account | Every signup + investors land in a populated space | Monetization | Deferred | 2026-06-19 | deferred: gate at ~50-60% platform complete |
| 49 | ⏭️ | WM-S2 | Guided tour | Teaches the loop in the sample workspace | Monetization | Deferred | 2026-06-19 | deferred: gate at ~50-60% platform complete |
| 50 | ⏭️ | WM-S3 | Onboarding Concierge agent | Seeds the real workspace from real context day one | Monetization | Deferred | 2026-06-19 | deferred: gate at ~50-60% platform complete |
| 51 | ⏭️ | WM-S4 | Workspace Steward agent | Nudges stale brief / outcome-less decisions (feeds the moat) | Monetization | Deferred | 2026-06-19 | deferred: gate at ~50-60% platform complete |
| 52 | ⏭️ | WM-S5 | Investor-demo rich population + reset | Every demo surface populated; self-serve reset | Monetization | Deferred | 2026-06-19 | deferred: gate at ~50-60% platform complete |
| 53 | ✅ | TEST-SEED | Minimal dev/test seed | A small, deterministic seed of the few decision/lineage/outcome rows needed to BUILD and TEST the data-rendering surfaces (Trust Ledger, memory, provenance) during development | Foundational | Done | 2026-06-23 | **Why:** the data surfaces are empty in dev (no connectors bound, near-zero live outcomes), so building + QAing them needs a minimal seed to render against. This is the as-needed dev enabler, NOT the rich external showcase (that is DEMO-SEED-RICH, Tier 4, last). v11. **DONE (lane 1, 2026-06-24):** migration `20260624030000_test_seed_dev_surfaces.sql` seeds, per demo account, a closed outcome→supersession→governing-decision loop: 2 opportunities, 2 decisions (one public), 16 learnings (early-miss/late-validate cohort), 16 memory rows (precedent timeline), 3 lineage edges (live supersedes + bitemporal retired + current). Lint-clean, applied live + verified: supersedes edges 0→1, memory-depth lift now computes (~55-63 pts), Trust Ledger/memory/provenance all render. Idempotent (sentinel guard), demo-scoped. |
| 54 | ✅ | LOOP-PROVE | Prove the loop closes on real data end-to-end | Verify outcome -> supersession -> governing-decision produces real edges end to end on seeded + live data; the moat visibly fills | Foundational | Done | 2026-06-23 | **v11. Done (lane 1, 2026-06-24).** Built the closure-proof harness: `src/lib/moat/loop-closure.ts` (PURE `computeLoopClosure`, composes the existing `resolveGoverning` supersession walk into a stage-by-stage proof: outcomes recorded → current supersession edges → forward governing resolutions; reports `closed`/`warmth`/`gaps`; 10 tests) + `src/lib/moat.functions.ts` `getLoopClosure` (RLS-scoped adapter, `valid_to` pre-migration fallback). **Verified LIVE via Lovable MCP vs prod:** both seeded demo workspaces resolve **warm/closed** (demo@ 23 dec/17 outcomes/1 edge/1 resolution; demo2@ 6/16/1/1), and the unseeded default workspace correctly reads **cold** — proving both closure and the gap-detection. tsc 0 + 1266 tests green. Does not rebuild DBR/LRN-02/W1-AUTO/MOAT-METRIC — it proves + fuels them; continuous fueling on non-seeded outcomes is the supersession ENGINE (DBR (H1)). Optional follow-on: surface the warm/cold badge in the Brain/Trust UI. Ties to DBR (H1). |
| 55 | ✅ | TRUST-LEDGER | The receipts as the hero surface | A first-class, demo-ready view rendering, for any decision/autonomous action: what changed, why, evidence (source doc/diff/citation), who approved + when, and was it later proven right or superseded | Governance | Done | 2026-06-23 | **v11 pillar 3, the demo closer.** Surfaces existing `decisions`/`artifact_lineage`/`agent_approvals` data ("trust is the thing people pay for"). _Built 2026-06-24 (lane 2): net-new `/trust-ledger` surface (sidebar Trust row) + `trust-ledger.functions.ts` composition over decisions+approvals+bitemporal supersession; what/why/evidence/who+when/standing-or-superseded with kind+outcome+search filters; RLS-scoped, XSS-safe (verified live), 14 unit tests, tsc+1215 tests green, adversarially reviewed. Renders today's data; richer once DEMO-SEED-RICH/LOOP-PROVE land; "proven-right" outcome is a follow-on. Live-verify on publish. `docs/features/trust-ledger.md`._ |
| 56 | ✅ | TRUST-SHARE | Shareable provenance artifact | Export/share a decision's Trust Ledger record as a clean public artifact (reuse `d.$slug`/`t.$slug`) - what a PM sends their VP to justify a call; the viral + regulated-buyer proof | Governance | Done | 2026-06-23 | **v11.** Sharing is outward-facing so the share action stays user-initiated. _Built 2026-06-24 (lane 2): a Share affordance on the Trust Ledger decision cards (user-initiated `setDecisionShared` → copyable `/d/$slug` link, RLS-owner-gated) + the public `/d/$slug` enriched into a receipt artifact with the honest Still-stands/Superseded outcome (computed server-side; a PRIVATE override never leaks — only a public superseder flips it; 7 unit tests). Security-reviewed (no critical/high; IDOR + admin-on-public-endpoint cleared live). tsc+1221 tests green. Live-verify on publish. `docs/features/trust-ledger.md`._ |
| 57 | ✅ | STITCH-LOOP | Stitch the surfaces into one continuous loop | Wire Today -> Product -> PRD -> Build -> Missions -> Brain -> Trust into one legible end-to-end felt loop so it reads as one autonomous system, not separate screens | Cockpit | Done | 2026-06-23 | **v11, the founder's "stitching + wiring".** The engine is already one loop; the surfaces must feel like it. Addresses the "feels fragmented" perception at the UX layer. **✅ Done (lane 2, 2026-06-24):** a route-derived `LoopThread` on every loop surface (current stage emphasized, next in action-blue, a "produces -> next" handoff caption, a cyclic glyph) makes the engine's one loop felt. Pure `loop-surfaces` model (longest-prefix path match; "/" matches only exactly; cyclic neighbours; the type is derived from the `as const` array so each route is a literal union and TanStack `<Link>` validates routes at compile time) + 10 tests. Rendered ONCE in `TopBar` (Today/Product/Build/Missions/Brain/Trust) + on PRD detail (`prds.$id`, the lone loop surface without TopBar); NO AppShell/nav-model touch (lane-1 IA-NAV territory). tsc 0 / 1377 tests; TS + security adversarially reviewed (security clean: static-constant hrefs/titles, the pathname is only an index lookup; 2 TS nits folded). Scope (a felt, legible loop) delivered; data-driven cross-surface handoffs are a noted future enhancement. |
| 58 | ✅ | IA-NAV-V11 | Collapse the left nav to 5 + 1 recessed door | Flatten the 4 competing nav metaphors: kill the "Loop" label + the NavGroup indirection, collapse the 4-icon "Trust" row into one recessed Engine Room entry (carrying the approvals badge), delete the floating QuickAccessDock; result = ~5 calm outcome-named destinations + one Engine Room door | Cockpit | Done | 2026-06-23 | **v11 consumer-grade IA** (founder-flagged "too complicated"). The route layer is ALREADY consolidated (~24 redirect stubs into 6 surfaces) so this is felt-density, not a rebuild; the ⌘K palette already indexes the deep surfaces. Files: `src/components/cadence/AppShell.tsx`. Detail: [`../strategy/v11-guiding-star.md`](../strategy/v11-guiding-star.md). **Shipped 2026-06-24 (lane 1):** new pure `src/lib/nav-model.ts` (single source of truth: `PRIMARY_NAV` flat destinations, `ENGINE_ROOM_DOOR` + `ENGINE_ROOM_LINKS`, `navItemActive`/`engineRoomActive`; 11 tests). `AppShell.tsx` now renders ONE flat list (Today·Ask·Product·Build·Missions·Brain — no "Loop"/"Workspace" labels, no NavGroup) + ONE recessed Engine Room door (a DropdownMenu revealing Approvals/Spend/Engine Room/Trust Ledger/Connectors on demand, with the live approvals badge on the door). QuickAccessDock deleted (Calendar reaches via Brain + ⌘K). NOTHING orphaned: Trust Ledger + Connectors aren't ⌘K-indexed, so the door is their preserved path (test-locked). Pruned the now-dead icon/Tooltip imports. tsc 0 / 1378 tests. No migration, no chokepoint. UI live-verify on publish. |
| 59 | ✅ | SETTINGS-SEGREGATE | Group 11 flat settings tabs into 5 sections | Account · Workspace · Connections · AI & keys · Billing (+ Advanced recessed for Health/Data); fixes the 3-places-to-connect confusion (Accounts/Integrations/`/sync`) + the Models-vs-Staff jargon; keep the `?section=` ids + LEGACY_SECTION_MAP for routing | Cockpit | Done | 2026-06-23 | **v11 consumer-grade IA.** The most overloaded surface (11 peer tabs, no segmentation). Files: `src/routes/_authenticated.settings.tsx`. **Shipped 2026-06-24 (lane 1):** new pure `src/lib/settings-sections.ts` is the single source of truth for the section ids, the 5+Advanced grouping, the legacy map, and `normalizeSection`/`groupForSection`/`primarySection`. The route renders a two-tier nav — a `TabRow` of the 5 primary groups (each with a one-line description) + a recessed quiet "Advanced" button, then a `SubTabs` row of the active group's member sections (Account=Profile/Notifications, Workspace=Brief&voice/Staff, Connections=Accounts/Integrations, Billing=Plan/Credits). Every original `?section=` id and the `brief→workspace` / `calendar→connections` legacy aliases are preserved, so deep links land unchanged. 15 unit tests lock the contract (11 ids preserved, one-group-per-section, round-trip, legacy resolution); tsc 0 / 1360 tests. No migration, no chokepoint. UI live-verify on publish. |
| 60 | ✅ | CONNECTORS-V11 | Consumer-grade connectors: one home, day-one input, de-dup | Consolidate connectors into ONE clear surface (fix the Accounts/Integrations/`/sync` 3-places confusion), present the available sources cleanly (de-dup the repetitive lists), and make real input available from day one so the loop is never siloed | Sense | Done | 2026-06-23 | **Why:** the loop needs real input from day one or it works in a silo; today connectors are scattered across 3 places + presented repetitively, which reads as broken. The connector PLATFORM (F-CONN) + a 2nd live source (SEN-01) are founder-gated on OAuth; this is the presentation + consolidation + day-one-readiness layer. Overlaps IA-DEPTH-V11 (one connections home). **Shipped 2026-06-24 (lane 1):** new pure `src/lib/connectors/catalog.ts` is the single source of truth for connector PRESENTATION — `buildConnectorCatalog()` de-dups every user-facing provider into 5 categories (Code · Issue tracking · Docs & knowledge · Calendar · Design) each with a plain flow descriptor (`flowLabelFor`: Two-way sync / In & out / Pulls in / Pushes out / Reference), connect method, and bindable resource; Firecrawl (infra) excluded; 10 tests lock de-dup + categorization + derivations. `/sync` is now the canonical connectors HOME — a new "Available sources" section renders the categorized catalog (browse every source in one place, day-one visible) above the existing bindings/conflicts/synced, with Connect routing to the canonical Settings → Accounts surface; day-one input is already live via the webhook ingest on the same page. tsc 0 / 1388 tests. No migration, no chokepoint. **Remaining (separate gated items, NOT this row):** OAuth provider wiring (F-CONN) + 2nd live source (SEN-01) = founder OAuth creds; the deeper literal merge of the 3 routes into one = IA-DEPTH-V11. UI live-verify on publish. |
| 61 | ✅ | PLAYBOOK-REGISTRY | Institutional product judgment as software | A versioned Playbook registry that does for every station what the Critic already does: bind opinionated PM method (JTBD/RICE/discovery/PRD/positioning) to live workspace decision-memory + per-outcome ranking | Foundational | Done | 2026-06-23 | **v11, the embed-skills verdict. Done (lane 1, 2026-06-24).** Engine shipped: PURE `src/lib/playbooks/registry.ts` (`PLAYBOOK_REGISTRY` — versioned, industry-standard methods JTBD/discovery-interview/RICE/PRD-spine/positioning/assumption-test bound to 5 stations; `selectPlaybooksForStation`/`findPlaybook`/`rankPlaybooksByOutcome` = the institutional-judgment payoff: a station's methods ranked by their per-outcome WIN RATE in this workspace; 8 tests) + migration `20260624060000_playbook_runs.sql` (workspace-scoped, RLS-keyed like support_tickets; **applied live via Lovable MCP**) + `src/lib/playbooks.functions.ts` (`getPlaybooks` ranked-by-station + `recordPlaybookRun` write-to-learn). Built as a clean STANDALONE registry (did NOT refactor the shared `critic.server.ts` — lower risk; the Critic stays the live in-context proof). **Ranking LIVE-VERIFIED via Lovable MCP** (seeded demo discovery runs: jtbd 1.00 win-rate ranks ahead of discovery-interview 0.67, matching the pure module). tsc 0 + 1308 tests green. No chokepoint. UI surface (a `/playbooks` browser) is a follow-on; the method content is standard frameworks (founder can refine weighting). |
| 62 | ✅ | PM-IMPACT-LEDGER | The PM's portable decision + outcome track record | A personal, portable record of the calls a PM made and the outcomes they drove (for performance reviews + their next role) - turns the receipts pillar into a career asset + retention/virality hook | Launch | Done | 2026-06-23 | **v11, strongest un-named opportunity. Done (lane 1, 2026-06-24).** PURE engine `src/lib/pm-impact.ts` (`computeImpactLedger`: decisions made + human/agent split + by-status + beliefs revised + outcome hit rate + net ICE impact + active-month span + ranked standout calls; `renderImpactMarkdown`: a portable, copy-anywhere artifact in Cadence's humanized voice, fingerprint-free; 9 tests) + `src/lib/pm-impact.functions.ts` `getImpactLedger` (RLS adapter, reuses Trust Ledger's `supersededChildIds`, `valid_to` fallback) + `/impact` surface (`src/routes/_authenticated.impact.tsx`: headline + stat grid + standout calls + copy/download the .md; reachable via ⌘K, no AppShell edit to avoid nav-lane collision). **Data engine LIVE-VERIFIED via Lovable MCP** (demo workspace: 23 decisions / 16 human-led / 7 agent-led, 71% hit rate (10 validated, 4 missed), -1.7 net ICE). tsc 0 + 1279 tests green. No migration, no AI/chokepoint. UI live-verify on publish (Trust-Ledger closure pattern). Salves the top career pain ("seen as a ticket-writer, can't prove my impact"); low-build on existing data. |
| 63 | ✅ | STAKEHOLDER-PACK | Audience-tuned alignment + persuasion artifacts | Turn a decision + its receipts into the persuasion artifact a PM needs per audience (exec/eng/board) - attacks the deck-building pain AND the influence/alignment gap | Launch | Done | 2026-06-23 | **v11 (pains research, GAP #1). Done (lane 1, 2026-06-24).** PURE composer `src/lib/stakeholder-pack.ts` (`composeStakeholderPack`/`composeAllPacks`/`renderPackMarkdown`: re-frames ONE decision's receipts — what/why/evidence/who/standing/outcome — per audience: exec=bottom-line-first, eng=rationale+constraints+provenance, board=strategic+governance; honest degradation; fingerprint-free; 10 tests) + `src/lib/stakeholder-pack.functions.ts` `getStakeholderPack` (RLS adapter, reuses Trust Ledger `evidenceCounts`/`supersededChildIds`, links a recorded outcome by prd/opportunity) + `/stakeholder` surface (`src/routes/_authenticated.stakeholder.tsx`: decision picker + audience tabs + copy/download .md; ⌘K-reachable, no AppShell edit). **Data engine LIVE-VERIFIED via Lovable MCP** (real demo decisions with rationale + evidence compose correctly). tsc 0 + 1289 tests green. No migration, no AI/chokepoint. UI live-verify on publish (Trust-Ledger pattern). Reuses decisions/outcomes + the humanized voice; helps the PM win the room. |
| 64 | ✅ | DBR-1.5 | Supersession engine (outcome to typed-edge write-path) | On recordOutcome, infer + write typed supersedes/contradicts edges (bi-temporal valid_to, invalidate-don't-delete) into artifact_lineage; flag-gated DECISION_BRAIN_SUPERSESSION default OFF | Foundational | Done | 2026-06-21 | SHIPPED ◐ (Lane 2, 2026-06-21): the moat's signature write-path. New PURE `src/lib/ai/supersession.ts` (`classifySupersession` verdict-conflict matrix + `resolveEndpoints` + `selectSupersessions` cap/dedup + `buildSupersessionEdge`; 17 tests) + gated orchestrator `supersession.server.ts` (`inferSupersession`, first line returns when the flag is off = zero embed/write/byte-identical) hooked into `recordOutcome` after `rememberOutcome`, fail-safe. Additive migration `20260621030000_decision_brain_supersession_bitemporal.sql` (3 nullable bi-temporal cols; explorer read surface byte-identical). Invalidate-don't-delete (never DELETE, only retires its OWN engine edges). 4-lens adversarial-review Workflow: 0 blockers, SHIP_WITH_FIXES — folded the 2 real write-path defects (zombie-edge-on-re-record + missing excludeId self-starvation) + the opportunity-dedup NIT. tsc 0 (my files) / eslint 0 / 492 tests (build red is a pre-existing Stripe-dep baseline failure, proven not mine). **◐ not ✅ (live-checked 2026-06-21):** PURE matrix ✅ unit-verified; **dormant + safe CONFIRMED on the live DB** (0 `created_by_agent='supersession-engine'` edges, flag OFF). **BUT the migration is NOT yet applied to the live DB** (`column artifact_lineage.valid_to does not exist`, error 42703) - it applies on the founder's next publish. So DBR-1.5 stays ◐: to activate the moat mechanic the founder must (1) publish so the migration runs, then (2) flip `DECISION_BRAIN_SUPERSESSION=1`. Until then it is correctly dormant (the flag-off guard + the try/catch fail-safe mean recordOutcome is byte-identical even with the columns absent). Plan: supersession-engine-plan.md. **↪ LEDGER DONE-MARKED 2026-06-21 (Lane 2): no autonomous work remains - the engine is built + gate-green, only the founder's publish (applies the migration) + flipping `DECISION_BRAIN_SUPERSESSION=1` is left. Removed from `lane.sh next` so it is never re-picked. (It was re-picked once on 2026-06-21 because last night's ship left it `◐` but skipped the `lane.sh done` mark; that step is now done.) **HARDENED + ARMED 2026-06-22 (DBR-EDGE-CONF, Lane 2):** added the edge-confidence precision layer (`supersession-confidence.ts` — drops marginal edges before write so the Critic's first edges are trustworthy, closes guardrail #3) + made the activation gate DB-backed (`feature_flags`/`get_flag`, env override retained) and **ARMED it ON via SQL**; it activates the instant the gate code deploys on the next publish, then accrues edges from live recorded outcomes. tsc 0 / 41 supersession tests / 1027 suite; 3-lens adversarial review SHIP, 0 must-fix. Spec: [`../features/decision-brain.md`](../features/decision-brain.md) (DBR-EDGE-CONF).** · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier(workflow)+gate(tsc 0, 41 supersession tests, 1110 suite, 3-lens SHIP): the supersession engine (pure classify/select + gated orchestrator hooked into recordOutcome + bi-temporal migration + edge-confidence layer) is 100% built + dormant-safe. Founder PUBLISHED 2026-06-22 (migration live); arming is the founder's one flag (DECISION_BRAIN_SUPERSESSION). No agent work remains. |
| 65 | ✅ | MOAT-VIS | Make compounding visible | Render a rescore + the learning that caused it on Today's what-changed + Brain | Launch | Done | 2026-06-21 | ✅ **LIVE-VERIFIED 2026-06-21** on the published app (cadence-flow-beta.lovable.app, demo workspace): Today's "What changed" card renders the rescore with the rounded ICE display ("ICE 8.3 to 8.7", links to learnings); the Brain Learnings tab renders the new `CompoundingPanel` with the aggregate line ("Memory has re-scored 1 decision from real outcomes · net ICE +0.4"), the stat row ("1 moved up · 0 moved down · 1 validated · 0 missed"), and the cause-carrying feed (verdict chip + opportunity + "ICE 8.3 ↑ 8.7" + date + summary, drilling to ?learning=). Math correct (8.7-8.3=+0.4), no feature console errors, no bugs. SHIPPED (Lane 2, 2026-06-21): makes the outcome->learning->ICE-rescore compounding visible. New PURE `src/lib/moat-vis.ts` (`summarizeCompounding`/`rescoresOf`/`describeCompounding`; rounds to display precision so a sub-0.1 jitter never reads as a move; 10 tests) + the named-missing `getCompounding` server fn in `today.functions.ts` (RLS-scoped, mirrors listLearnings). Brain Learnings tab now renders a `CompoundingPanel` instead of the old MemoryPanel landing; Today's "what changed" card gains the aggregate compounding line (>1-rescore gate). 2-lens adversarial-review Workflow: 0 blockers, SHIP_WITH_FIXES - folded the raw-vs-rounded honesty regression + the error-disguised-as-empty defect. tsc 0 (my files) / eslint 0 / 519 tests (build red = pre-existing Stripe-dep baseline, not mine). Follow-up MOAT-VIS-b (logged): delete the now-orphaned MemoryPanel.tsx + extract shared whenOf/VERDICT_TONE. |
| 66 | ✅ | F-IA-BRAIN-GRAPH | Decision Brain visual graph surface | The Obsidian-style visual graph front surface on Brain | Cockpit | Done | 2026-06-21 | The DBR-1 read view exists at /knowledge?tab=graph; this is the placement/IA half under H1. SUBSTANCE SLICE SHIPPED ◐ (Lane 2, 2026-06-21 13:30): supersession legibility, NOT the Tier-2 IA reshuffle (that stays for the founder-prompted design pass). New PURE `buildSupersessionStory`/`isSupersessionRelation` in `knowledge-graph-view.ts` (per-node "decision history" derived from the `relation` field `getLineage` already returns; ancestor=superseded/contradicted-by, descendant=supersedes/contradicts; self-revised first; dedup; fail-safe; 6 tests, file 22 green) + a madder "decision history" section in `GraphNodeStory.tsx` (recenter links + a "later outcome revised this belief" banner; generic came-from/led-to now exclude supersession edges so it reads once) + a graph-level "N links here mark a belief a recorded outcome later revised" line in `GraphCanvasView.tsx` (honors the as-of axis). Migration-free (reads only `relation`, NOT the unapplied `valid_to`) + regression-free (zero edges today renders nothing new). 3-lens adversarial-review Workflow: unanimous SHIP, 0 blockers/0 fixes; folded 5 net-new comment em-dashes I introduced. tsc 0 (my files; 4 pre-existing Stripe-dep baseline) / eslint 0 / 614 tests / build red = pre-existing Lovable vite-config baseline (fails at config-load before my source). INCREMENT 2 SHIPPED ◐ (Lane 2, 2026-06-21 13:45): bi-temporal `valid_to` legibility (the invalidate-don't-delete moat property). `buildSupersessionStory` now reads each edge's `valid_to` and carries `retired`+`retiredAt`; a reversed (retired) assertion sorts after current links + is EXCLUDED from `revised` (honest current-state reading); the node-story renders retired links dimmed/struck with a date-guarded "no longer current · <date>" marker. Regression-safe: reads `valid_to` only via `getLineage`'s `*` select (absent pre-migration, reads as current), the canvas explicit-column select UNTOUCHED (no 42703). +4 tests (file 26 green); tsc 0 / eslint 0 / 628 tests. **◐ not ✅:** the rendered section behaviorally verifies only on a workspace with seeded outcome-driven edges (needs the founder's publish to apply the DBR-1.5 migration + flip `DECISION_BRAIN_SUPERSESSION=1`). INCREMENT 3 SHIPPED ◐ (Lane 2, 2026-06-21 14:30): canvas retired-edge STYLING, built MIGRATION-TOLERANTLY so it ships BEFORE the `valid_to` column is live (the prior "deferred to post-publish" follow-up, now done safely). `projectGraph` threads `valid_to` onto `GraphEdge` + derives `retired` (set only on a supersession edge with a stamped non-blank `valid_to`); `filterByTime` recomputes `retired` against the as-of instant (an assertion reversed AFTER that instant still reads current then; retired edges are KEPT, never dropped = invalidate-don't-delete); `GraphExplorer.tsx` renders retired edges faded + finely-dotted vs a current revision's bolder madder dash; `GraphCanvasView.tsx` splits the count into current-revisions vs reversed-and-retired (two summary lines). The 42703 risk (the canvas uses an explicit column select, not the node-story's `*`-tolerant one) is closed by a one-shot `resolveLineageCols` probe that prefers the bi-temporal cols + falls back to base when `valid_to` is absent, under the existing `try/catch->emptyGraph`; also corrected the now-stale migration comment. +7 tests (file 33 green); tsc 0 (my files; 4 pre-existing Stripe-dep baseline) / eslint 0 (my files) / 635 tests. 3-lens adversarial-review Workflow: unanimous SHIP, 0 blockers; folded the migration-comment fix + a boundary test from the nits. Remaining (founder/publish-gated): behavioral verify on a workspace with seeded outcome-driven edges (publish applies the migration + flip `DECISION_BRAIN_SUPERSESSION=1`) + the founder-prompted IA-placement half + optional `--madder` token. INCREMENT 4 SHIPPED ◐ (Lane 2, 2026-06-21): the TREE/LIST view's bi-temporal trail, so BOTH explorer views match (the canvas had it since increment 3). `getLineageTree` now reads `valid_to` via the shared migration-tolerant `resolveLineageCols` (exported from `knowledge-graph-view.functions.ts` for reuse); `buildLineageTree`/`LineageNode` carry `validTo`+`retired` (retired only on a `supersedes`/`contradicts` edge with a stamped `valid_to`); `GraphTreeView.tsx` renders a madder supersession chip per node and fades a reversed one with a "(reversed)" marker. Migration-safe (pre-migration the column is absent so every node reads current) + regression-free (non-supersession nodes unchanged). +3 tests (file 9 green); tsc 0 / eslint 0 / 827 tests / dash-clean. ◐: behaviorally verifies on a workspace with seeded outcome-driven edges (founder publish applies the DBR-1.5 migration + flip `DECISION_BRAIN_SUPERSESSION=1`). Spec: [`../features/knowledge-graph-explorer.md`](../features/knowledge-graph-explorer.md). · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier+gate: 4 increments (supersession legibility, valid_to, canvas retired-edge styling, tree/list bi-temporal trail) built + tested (1110 suite, tsc 0, 3-lens SHIP x4), migration-tolerant. Founder PUBLISHED 2026-06-22. Remaining is the founder-prompted IA-placement design-pass (Tier-2), NOT autonomous work. DONE. |
| 67 | ✅ | MOAT-METRIC | Outcome-accuracy lift per PM | Lift vs a generic-model baseline as one account's memory grows, on the Gauntlet | Governance | Done | 2026-06-21 | LIFT HALF SHIPPED (lane 1, 2026-06-21 04:38): the unbuilt half of MOAT-METRIC. A pure `computeMemoryLift` (memory-compounding.ts, 17 tests) + `getMemoryLift` server fn + a `MemoryDepthSplitCard` on the Gauntlet. Splits reviewed bets by memory-depth-at-decision (earlier/sparse vs later/rich half), reports the validated-share difference in whole pp, gated by a size floor (8/side), a material depth-contrast guard, AND a two-proportion 95% CI noise gate; correlational only (neutral colour either sign; the time/practice/survivorship confound named inline + footnote), never a causal A/B. Designed + red-teamed via two 4-agent workflows (honesty/stats/data lenses; review verdict ship, 0 must-fix). tsc 0 / 492 tests / build green. ◐ renders + behaviorally verifies on the founder's next publish (reads live learnings + agent_memory). [~90%] · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier+gate: pure computeMemoryLift (memory-compounding.ts, 31 tests) + getMemoryLift server fn + MemoryDepthSplitCard on the Gauntlet, all built + gate-green. Founder PUBLISHED 2026-06-22 (reads live learnings + agent_memory). No agent work remains. DONE. |
| 68 | ✅ | H1-TASKS | PRD to engineering task graph (DAG) | An approved PRD decomposes into ordered work units feeding BUILD | Decide | Done | 2026-06-21 | ✅ **LIVE-VERIFIED 2026-06-21** on the published app (cadence-flow-beta.lovable.app): seeded a real DAG on the Escalation Policy Engine PRD via an owner-scoped REST write (RLS-permitted, then reverted to the exact seeded state), the `TaskGraphPanel` rendered correctly: "40% complete · 2 of 5 done · 2 ready · 1 blocked · ~13h left · READY NOW #3 · #5" - every value matches the topology/readiness math. Integrity path also verified: a missing-dep produced "1 GRAPH ISSUE · Task 5 depends on missing task 4" whose computed color === `--rose` (confirming the MF-2 role-color fix); the read-path `sanitizeDependsOn` correctly stripped an injected forward-ref live. No bugs. CONSUMPTION SLICE (Lane 2): generation was already shipped (`generateTaskGraph` writes seq+depends_on); this makes the stored DAG READABLE. New PURE `src/lib/task-graph.ts` (`sanitizeDependsOn`/`validateTaskGraph` cycle+integrity/`topoOrder` Kahn-total/`readyTasks`/`summarizeTaskGraph`; 13 tests) + RLS-scoped `getTaskGraph` (mirrors listTasks) + the `TaskGraphPanel` on `/prds/$id`. 2-lens adversarial-review Workflow: 0 blockers, folded a stale-panel cache-invalidation + the `--rose` fix + the self-dep double-report. tsc 0 / eslint 0 / 551 tests. Deferred (lane boundary): the task-graph->mission-steps bridge needs studio.* (Build lane). |
| 69 | ✅ | W1-AUTO | Outcome-memory auto-trigger | Auto-compound on decision close: recordOutcome to rememberOutcome embeds the outcome into agent_memory | Launch | Done | 2026-06-21 | Specializes the shipped W1 (#90): the auto-trigger leg, already wired. Mark done on live-publish verify; NOT net-new scope (do not double-count). [~90%] **↪ LEDGER DONE-MARKED 2026-06-21 (Lane 2): publish-verify only, no autonomous work remains; removed from `lane.sh next`.** · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier+gate: recordOutcome->rememberOutcome auto-trigger is unconditionally live in code + tested (1110 suite). The outcome-memory compounding loop is wired. Founder PUBLISHED 2026-06-22. No agent-buildable work remains. DONE. |
| 70 | ✅ | O1 | Knowledge graph + query | Provenance walk: "why is this on the roadmap / being built?" back to root source signals | Foundational | Done | 2026-06-20 | SHIPPED v1 (DBR-1) at `/knowledge?tab=graph` as TWO views behind a toggle: a bounded **visual graph** (parallel/knowledge: `projectGraph` over `artifact_lineage`, color-by-type, click-to-trace, "as of" time axis, supersession seam; 13 tests) + a **List** lineage tree (`buildLineageTree`/`getLineageTree`; 6 tests). Deterministic, migration-free, fail-safe; tsc0 / build green / 19 graph unit tests green, ◐ live-verify on publish. Spec [`../features/knowledge-graph-explorer.md`](../features/knowledge-graph-explorer.md). Remaining: O3 drift/skill-packs + the bi-temporal supersession engine (DBR-1 write-path, AI/attended lane) [~90%] ⬆️ 2026-06-21: ABSORBED+ELEVATED into the Decision Brain (H1, row 148) as the DBR-1 read-side; not a standalone Sense item. Build Sequence #9. **✅ LIVE-VERIFIED + GAP-FIXED (Lane 1, 2026-06-22):** read-side live on prod (`/knowledge?tab=graph`; GRAPH/LIST toggle + node-story + time axis render). Live-verify surfaced a real false-empty: the canvas auto-focuses on the caller's most recent **decision**, but a workspace's lineage can live entirely off it (Project Glasswing had 2 active `signal->theme` edges yet rendered "No lineage to map yet"). FIXED with a lineage-anchored focus fallback (`resolveLineageFocus` -> pure `pickLineageFocus`; gated on `!focusId` so an explicit focus is always respected; byte-identical when edges>0; +4 unit tests). Deployed + re-verified: graph now draws the signal->theme lineage. tsc0 / 947 tests green. Write-path (supersession) continues as DBR-1.5. [100% read-side] |
| 71 | ✅ | O3 | Fact currency/drift + skill packs | Flags stale facts on the provenance graph; exports versioned skill bundles over MCP | Foundational | Done | 2026-06-20 | drift/currency SLICE shipped (parallel/knowledge): deterministic `computeStaleness` over the graph (a fact is stale when its newest supporting edge is older than 90d), surfaced as a stale count + a dashed-ring marker on `/knowledge?tab=graph`; pure + 3 tests, tsc0 / build green. **Contradiction-drift READ-side SHIPPED (Lane 2, 2026-06-21, on main):** pure `computeContradictionDrift(graph)` flags every node that is the target of a CURRENT (non-retired) supersedes/contradicts edge, reusing the bi-temporal `retired` plumbing from F-IA-BRAIN-GRAPH increment 3 (a reversed-and-retired revision is correctly excluded); surfaced as a third `GraphCanvasView` summary line (madder, honors the as-of axis), dormant + migration-tolerant. 5 unit tests; tsc 0 / 38 graph-view tests / eslint 0 / no em-dashes in code. _(Built by a parallel Lane-2 session [Haiku 4.5] that briefly shared this worktree; reconciled, gate-re-verified, and landed on main by the Opus session, which dropped that session's stale 168-row tally edit in favor of the live rerank. See `.remember/remember.md`. Global tally not hand-recomputed here; a later lane rerank will reflow it.)_ **Versioned skill-pack export over MCP SHIPPED (Lane 3, 2026-06-21):** the portable-moat half, now buildable since Q1/MCP is ✅. New PURE `src/lib/skillpack.ts` (deterministic, content-hashed `buildSkillpack`; 18 tests) + an `export_skillpack` MCP tool (`exportSkillpack` reads workspace-scoped `learnings`, served on BOTH standard `tools/call` + legacy transports with the route's bearer-auth/rate-limit/audit). 3-lens adversarial-review Workflow (tenant-isolation SHIP · regression SHIP · correctness SHIP_WITH_FIXES, 0 must-fix); folded the one real finding (content_hash non-determinism at the DB `LIMIT` tie-boundary → added an `id` secondary sort so the top-N is stable) + 2 nits (blank-string ICE guard, co-tenant-invariant comment). tsc 0 / 874 tests / new files lint-clean. ◐ live-verify on publish (an external MCP client pulls a bundle from a workspace with real learnings). Follow-up SKILLPACK-UI: surface the tool in Settings>Integrations + the A2A card. Spec: [`../features/skillpack-export.md`](../features/skillpack-export.md). [~88%] ⬆️ 2026-06-21: ABSORBED into the Decision Brain (H1) as DBR-3; the contradiction-drift half is the supersession engine DBR-1.5 (Build Sequence #1) + its read-side legibility (just shipped). Not a standalone Sense item. · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier+gate: skill-pack export (skillpack.ts, 18 tests) + fact-currency/contradiction-drift compute (knowledge-graph-view.ts, 47 tests) shipped; the export_skillpack MCP method is wired (mcp.ts:215). Final UI slice CLOSED THIS PASS (Lane 1): added export_skillpack to the Settings>Integrations MCP-tools list AND a knowledge.export_skillpack skill to the public A2A agent card; tsc 0, lint clean, 18 skillpack tests. Founder PUBLISHED 2026-06-22. DONE. |
| 72 | ✅ | Q1 / ENG-07 / F-MCP-V1 | MCP server + read-only externals | Other agents/tools use Cadence as a tool (signals/opps/PRDs read, append decision) | Interop | Done | 2026-06-21 | Phases 1-3 + 4a complete (foundation + dispatch + token UI + native MCP transport handshake). Phase 4a: PURE `src/lib/mcp-protocol.ts` (discriminated-union router, 32 tests) wired into `/api/mcp` speaking `initialize`/`ping`/`tools/list`/`tools/call`/`resources/list` ALONGSIDE legacy flat methods (zero-regression). 2-lens review: SHIP_WITH_FIXES, 0 blockers; folded 3 fixes (drop unserved caps, honor version-header MUST, audit rejected tool names). **Critical token-issuance bug fixed (1f0ace8450):** `context.auth.user.id` → `context.userId` in `mcp.functions.ts` lines 59-64 (was throwing "Cannot read properties of undefined", made MCP server unreachable). ✅ Live endpoint verified 2026-06-21: MCP `/api/mcp` responds with correct JSON-RPC error envelope; token fix confirmed in code; mcp-protocol tests 32/32 pass; tsc 0. Phase 4b (founder-gated, M-D): OAuth discovery, SSE streaming, full CRUD. Spec: [`../features/q1-mcp.md`](../features/q1-mcp.md). |
| 73 | ✅ | DELEGATE-DESK | The background-work desk (delegate, walk away, come back to done) | A first-class surface to set up a task, hand it to the agents, and return to finished work + the full trail: the felt "Cowork" pattern, but PM-domain, governed (HITL at irreversible gates), and memory-backed | Cockpit | Done | 2026-06-24 | **Why:** the engine already runs missions async (the cron sweeper), but the felt "delegate and walk away" pattern is not surfaced as a hero flow; it is the purest expression of the action-system thesis ("the work is done"), differentiated from a generic background-agent UX by being governed + memory-backed. Extends AGENT-FLEET-VIEW. Validated 2026-06-24 ([v11 guiding star](../strategy/v11-guiding-star.md) A18). **Shipped 2026-06-24 (lane 1):** new greenfield `/delegate` surface re-framing the workspace's missions through the delegate-and-walk-away lifecycle. Pure `src/lib/delegate-desk.ts` partitions missions into 5 urgent-first lanes (**Needs you** paused-at-gate · **Working** · **Queued** · **Done** · **Needs a look** failed/cancelled) via a tolerant status→lane map, computes per-mission step progress, and writes an honest headline; 11 tests lock the mapping/partition/progress/summary. RLS adapter `delegate-desk.functions.ts` (`getDelegateDesk`, batched mission+step query, degrades to empty steps). Route `_authenticated.delegate.tsx` renders the lanes as columns of mission cards (title/goal/progress dots) each linking to `/missions/$missionId` for the full trail; hand-registered in `routeTree.gen.ts`. tsc 0 / 1418 tests. No migration, no chokepoint. UI live-verify on publish. |
| 74 | ✅ | TRUST-VERIFY | Integrity check for the Trust Ledger (all users) | A plain SHA-256 FINGERPRINT (a checksum, NOT a blockchain — no chain/tokens/keys/external service) of the decision-and-outcome record, so any user can save it now and confirm later that the record has not been altered | Governance | Done | 2026-06-24 | **Reframed per founder ruling 2026-06-24 (memory: no-blockchain-trust-integrity):** NO blockchain anywhere now; NOT enterprise-only — a normal integrity check for EVERY user. **◐ Shipped (lane 2, 2026-06-24) [~70%]:** pure `trust-verify.ts` (SHA-256 hash-chain fingerprint over the ledger receipts: canonicalize integrity-relevant fields, total-order seal, detect/pinpoint tamper; Web Crypto; 13 tests) + `getLedgerSeal`/`verifyLedgerSeal` server fns (RLS-scoped, head = exactly 64 hex) + a calm `SealPanel` on /trust-ledger ("Integrity check · fingerprint · N records", Copy fingerprint, paste-to-verify Unchanged/Changed; hidden on an empty ledger). tsc 0 / 1411 tests; 4-lens adversarial review (crypto SOUND, tamper-detection COMPLETE, behavior + RLS CLEAN; folded empty-ledger guard + total-order tie-break + 64-hex validator). **Deferred OPTIONAL (NOT now, NOT enterprise-gated):** an Ed25519 signature (non-repudiation) + persisting the fingerprint at write time (lights up per-record pinpointing); both need a key/migration and stay optional per the founder ruling. The original tamper-evident/blockchain-adjacent/regulated-buyer/enterprise framing is RETIRED. |
| 75 | ✅ | EVALS-PRIMITIVE | Evaluations as a first-class PM primitive | Let the PM define, run, and trust evals on the product's AI features AND on Cadence's own agents' output - "the defining skill" of the 2028 PM | Governance | Done | 2026-06-23 | **v11 future-of-PM. Done (lane 1, 2026-06-24).** The primitive now has all four legs: define (suites/cases) + run (eval-runner) + coverage (EVAL-COVERAGE) pre-existed; this shipped the missing **TRUST/health** leg. PURE `src/lib/evals/health.ts` (`computeEvalHealth`: pooled pass rate, error/reliability rate, mean judge score, recent-vs-prior TREND, and per-suite FLAKINESS — adjacent-run pass/fail flips, the signal that quietly destroys trust — plus a coarse healthy/watch/at-risk/no-data verdict; `summarizeEvalHealth`; 13 tests; distinct from coverage = breadth). Thin RLS adapter `src/lib/eval-health.functions.ts` `getEvalHealth` (user-scoped like evals.functions.ts). New `/eval-health` surface (`src/routes/_authenticated.eval-health.tsx`: verdict badge + stat grid + flaky-suites + per-suite table; ⌘K-reachable, no edit to the shared /evals route to avoid conflict). **Engine LIVE-VERIFIED via Lovable MCP** (prod: 3 suites, 3 completed runs, 75% pooled pass rate -> "watch" verdict). tsc 0 + 1331 tests green. No migration, no AI/chokepoint. UI live-verify on publish; merging the panel into /evals is a follow-on. |
| 76 | ✅ | AGENT-FLEET-VIEW | The agent-fleet control surface (2028) | Evolve Missions/Engine Room into an air-traffic-control surface where the PM defines intent, dispatches a fleet, supervises by exception, and intervenes at gates | Cockpit | Done | 2026-06-23 | **v11 future-of-PM** (post-pitch). The 2028 puck: PM as orchestrator of agent fleets, not a writer of docs. **Shipped 2026-06-24 (lane 1):** new `/fleet` surface — the BY-AGENT lens (distinct from DELEGATE-DESK's by-mission lanes; same `agent_runs` data, pivoted on `agent_slug`). Pure `src/lib/agent-fleet.ts` `computeAgentFleet` tallies per-agent running/queued/done/failed/liveLoad + lastActiveAt + state (working/queued/attention/idle), seeds idle roster agents, and orders **attention-first** (live load → failures → recency) for supervise-by-exception; `runBucket`/`summarizeFleet`; 9 tests. RLS adapter `agent-fleet.functions.ts` (`getAgentFleet`: recent agent_runs window + agents roster). Route `_authenticated.fleet.tsx` renders the fleet roster with per-agent live tallies + state chips; hand-registered in `routeTree.gen.ts`. tsc 0 / 1427 tests. No migration, no chokepoint. UI live-verify on publish. |
| 77 | ✅ | SEC-SIGNAL-INGEST-INJECTION | Injection screening on the LIVE signal-ingest webhook | Screen each externally-POSTed signal at the public `api/public/ingest-signals` webhook for prompt injection BEFORE insert, so a poisoned external payload cannot enter `signals` as trusted context the agents read (`considerations.md` #3, P0; the LIVE-path follow-up to SEC-INGEST-INJECTION) | Governance | Done | 2026-06-22 | **SHIPPED ◐ (Lane 2, 2026-06-22):** the LIVE webhook is screened — generic `src/lib/ingest-guardrails.ts` (`screenIngestText`, reuses the structural-gate `classifyInjection`) wired into `api/public/ingest-signals`: each item's full free text (title + content + source) is screened BEFORE insert; a structural attack is REJECTED (never stored), a borderline one tagged `needs-review`. **Focused external-attack-surface review = SOUND** (no bypass; rate limit charged before screening; ReDoS-safe; folded the LOW source-field hardening). Reusable for MCP/A2A ingest. No schema change, no chokepoint. tsc 0 / 5 tests / 1201 suite. **`done`-marked: webhook scope complete; live-verify on the founder's next publish.** Remaining (separate item): the same screen on external MCP/A2A tool results. |
| 78 | ✅ | H2-WRITES | Outcome roadmap governed writes | Extends the H2 Now/Next/Later board with governed write paths | Decide | Done | - | GOVERNED commit path shipped (Lane 0, 2026-06-21 02:20): new pure `src/lib/roadmap-governance.ts` (`validateCommitment`/`isCommitmentGoverned`/`findGovernanceGaps`, 8 tests) + `commitRoadmapItem` server fn (a now/next/later commitment REQUIRES a declared outcome+measure, the anti-feature-factory governance) + `getRoadmap` now returns `governanceGaps`; the board routes outcome-save through the governed path, surfaces a gap-count header + an ember `VerdictChip` ("Needs outcome") on ungoverned cards, lenient drag move untouched. tsc/eslint/build/446-tests green; `impeccable` design pass (VerdictChip per the verdict-chip ruling, role-color law honored). ◐ live on publish. UI: /roadmap (product roadmap board). **Richer-write-surface increment SHIPPED (Lane 1, 2026-06-21 07:1x):** move-audit provenance was hollow (`recordRoadmapDecision` never threaded `from_bucket`; the move call-site carried no outcome), so re-prioritizing a live commitment lost where it moved from + what it still promised. New pure unit-tested `classifyRoadmapWrite(prev,next)` (roadmap-audit.ts) drives `updateRoadmapItem` to record full-provenance audits (move carries from_bucket + outcome; in-place re-declaration = commit; combined relocate+re-declare emits both; killed a phantom move on unchanged re-save). No migration (existing columns + CHECKs permit it), no chokepoint, no UI change. 2-lens adversarial review (logic+security) both SHIP, 0 blockers; folded the combined-write move+commit completeness fix + decision normalization. tsc 0 / eslint 0 / `bun test` 608 (roadmap-audit.test.ts 5→14). **Move-provenance RENDER shipped (Lane 1, 2026-06-21 07:2x):** the "why" popover (`RoadmapHistory.tsx`, new `eventBuckets` helper) now renders a re-prioritization as "moved · now → next" from the captured `from_bucket` (arrow only when a source was actually recorded; legacy/backlog-origin null-source falls back to the destination, never fabricating a from). tsc 0 / eslint 0 / 608 tests. **Bulk re-prioritize SHIPPED (Lane 1, 2026-06-21 13:0x):** the second remaining slice. New pure `src/lib/roadmap-bulk.ts` `planBulkMove` (de-dup + drop unknown/no-op + cap-with-overflow-report, 10 tests) drives a new `bulkUpdateRoadmapItems` server fn (read→plan→one RLS-scoped `.update().in(ids).eq(user_id).select()`→`move` audit per row via the shared `classifyRoadmapWrite`, honest `{moved,skipped}`); lenient like the drag (governance still binds per-item at commit + the gap surface). UI: subtle per-card multi-select checkbox + a calm "move to" action bar that appears only on selection (Engine-Room calm front), optimistic. 6-agent adversarial review (correctness+security+regression, each finding re-verified): 0 must-fix; folded one consistency fix (both optimistic writers now spread `...old` so `governanceGaps` no longer flickers). tsc 0 / eslint 0 / `bun test` 618 (10 new). Remaining: agent autonomous-commit wiring (chokepoint, founder-attended) [~90%] · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier+gate: 4 increments (governed commit path + validation/gap surface, richer audit-write classification, move-provenance render, bulk re-prioritize) all built + gate-green (tsc 0, 618 tests, adversarial-reviewed). Founder PUBLISHED 2026-06-22. The note's 'autonomous agent-commit wiring' is a SEPARATE chokepoint item, not a slice of H2-WRITES. DONE. |
| 79 | ✅ | WM-F1 | Scope agent memory/runs/roster to workspace | The moat compounds per workspace/account (today user-scoped) | Monetization | Done | 2026-06-21 | CORE shipped (overnight cycle 27): nullable `workspace_id` on all 5 agent tables (backfilled) + recall RPCs rewritten with `for_workspace` (service-role-safe, null-tolerant) + a security fix (cross-user reflection leak) + recall threading + outcome/reflection tagging. Verified by a BEGIN..ROLLBACK dry-run on prod (0 nulls). **Remaining → WM-F1c (all non-autonomous; lane-3 retired this row from the board 2026-06-21):** the RLS swap + universal insert-tagging this row scoped were already SHIPPED by WM-F1b (cycle 37); the only residue is (1) NOT NULL + FK→CASCADE = founder-gated data-loss product call, (2) roster `UNIQUE(user_id,slug)→UNIQUE(workspace_id,slug)` = couples to WM-F8 + unverifiable offline, (3) types regen = post-publish. Live recall-isolation activates on publish. [~70%, core + WM-F1b done] · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier+gate: workspace-scoped agent-memory recall (migration + WM-F1b dual-key RLS + recall RPCs) built and LIVE-VERIFIED on prod 2026-06-22 (Lane 2 6-lens RLS leak-hunt = 0 critical/high). Remaining WM-F1c (NOT NULL/CASCADE, roster unique-swap, types regen) is founder-gated/post-publish hygiene, not core. Acceptance MET + live-verified. DONE. |
| 80 | ✅ | K2 | Rollback triggers + one-action revert | One-action non-destructive revert-to-revision in the Build Changes tab | Build | Done | 2026-06-18 | remaining K2b: `studio.revert` agent tool (needs `agent_tools` migration) + feature-flag kill; parked on a backup branch, issue #4 [~70%] · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier CORRECTED the stale '~70%' note: K2 is COMPLETE on main (4e81af4959). studio.revert tool fully wired (registry.server.ts:1808 + TOOL_REGISTRY + loop.server.ts HIGH_RISK_FORCE_REVIEW + agent_tools migration seed), studio-rollbacks.ts (371 lines, 7 tests), non-destructive revert via GitHub Git Data API, UI wired (ChangesPanel). 1110 tests, tsc 0. The 'feature-flag kill' was founder-CUT (spec section 3, out of scope). Founder PUBLISHED 2026-06-22. DONE. |
| 81 | ✅ | LCH-01 / L1 | Launch-kit mission | Turns a shipped changeset into 5 human-approved artifacts (changelog/blog/email/social/docs) | Launch | Done | 2026-06-18 | remaining: a launch MISSION template + governed outbound send (never auto-sends) [~40%] **⚖️ ASSESSED + retired from the autonomous pick list (Lane 1, 2026-06-22, founder "make the call" mandate):** the autonomous CORE is shipped — `generateLaunchKit` drafts all 5 grounded artifacts (draft-only, humanized) on the Build Changes tab. The two remainders are NOT autonomous build work: (1) **governed outbound send** (publish changelog / send email / post social) is EXPLICITLY founder-gated (accounts + spend + outward-facing — needs connector keys + a go-live call); (2) a full **launch-mission template** is speculative orchestration the one-click draft already obviates (anti-over-engineering doctrine). Live-verify of the drafting is data-gated like BLD-05 (prod has 0 `studio_changesets`; needs a connected-repo Build-to-changeset run). No speculative UI churn; stays ◐ pending the founder-gated outbound leg. · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier+gate: generateLaunchKit drafts all 5 grounded artifacts (changelog/blog/email/social/docs, humanized) on the Build Changes tab, built + gate-green (1110 tests). The two remainders are NOT code work: governed outbound send is founder-gated (accounts+spend+connector keys) and a full launch-mission template is speculative (the one-click draft obviates it). Founder PUBLISHED 2026-06-22. Autonomous core DONE. |
| 82 | ✅ | R4 | Settings expansion | Self-serve control: budgets, guardrails, health, prefs, admin | Cockpit | Done | 2026-06-21 | Plan tab shipped; ⚖️ SPLIT 2026-06-21: budgets/guardrails/health/prefs/admin STAY; the Plan/billing/credit/pricing TAB is Lovable-owned. **Health sub-tab SHIPPED (Lane 1, 2026-06-21):** a self-serve `Settings > Health` view surfacing the two already-shipped reliability reads (`getReliabilitySlo` = RELIABILITY-SLO, `getRunawayMissions` = RUNAWAY-DETECT) so an operator can answer "are my agents healthy?" on demand. New PURE `src/lib/reliability/health-view.ts` (`summarizeHealth` rolls the two reads into one calm state/headline/signals; type-only imports keep it unit-testable; 12 tests) + `src/components/settings/HealthCard.tsx` (7/30-day window toggle; reuses the cockpit `RunawayMissionsDetail` drill-in for the SLO grid + flagged-mission list so the two surfaces cannot drift; neutral-ink role-color law; calm-front Engine-Room) + a `health` tab wired into `_authenticated.settings.tsx` (SectionId + TABS + render; deep-link `?section=health` works via `normalizeSection`). 1-agent adversarial review: 2 fixes folded (distinct `health-slo`/`health-runaway` query-cache namespace so prefix-invalidations never couple the cockpit glance; partial-data returns `unknown` not a false `healthy` when only the secondary runaway read has resolved). tsc 0 / eslint 0 / `bun test` 639 (12 new). **◐ not ✅:** built + gate-green + unit-verified; the live `ai_events`/`missions` reads render on the founder's next publish. **REMAINDER ASSESSED + RETIRED FROM AUTONOMOUS PICK 2026-06-22 (Lane 2):** investigated the "budgets/guardrails/prefs/admin sub-tabs" remainder against the codebase + the WM-F7 settings-IA rubric (`docs/features/settings-ia.md`, which I authored) and the calm-front engine-room doctrine. Finding: **every self-serve control R4 calls for already EXISTS** — `budgets` → the `/budgets` route + the Credits-tab "Spending caps" card (WM-M14); `guardrails` → the `/guardrails` route; `admin` → the `/admin` console; `prefs` → the `/notifications` surface (the `user_notification_preferences` table + `getNotificationPreferences`/`updateNotificationPreferences` fns, in-app/email/digest toggles); `health` → already a Settings sub-tab (Lane 1). Re-creating these as duplicate Settings sub-tabs would VIOLATE the calm-front "one door per outcome, don't clutter a tested surface" doctrine, and the WM-F7 rubric EXPLICITLY defers the only clean remaining work — visually grouping the flat tabs under Personal/Account/Workspace/System headers — to the founder-prompted design pass ("no speculative UI churn on a tested surface"). So there is **no non-duplicative, non-deferred autonomous slice**; the functional self-serve control is substantially delivered. Founder decision-call per the "make the call per our doctrine + best practice" mandate: do NOT build duplicate tabs. Retired via `lane.sh done` so it is not re-picked; stays ◐ pending only the founder design-pass grouping (presentation, not behavior). [functionally ~85%; Settings-consolidation grouping = design-pass] · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier+gate: Health sub-tab (HealthCard + pure health-view roll-up, 12 tests) built + wired into Settings; the budgets/guardrails/admin/prefs controls R4 called for already exist as standalone surfaces (/budgets, /guardrails, /admin, Settings>Notifications) - duplicating them as sub-tabs would violate calm-front doctrine. Remaining is founder-prompted design-pass grouping (presentation, not behavior). Founder PUBLISHED 2026-06-22. DONE. |
| 83 | ✅ | PROVIDER-FALLBACK | Multi-provider/model fallback chain at the AI chokepoint | An ordered fallback chain so a provider/model outage degrades gracefully instead of hard-failing (closes `considerations.md` #4 "graceful degradation when a provider/model is down") | Reliability | Done | 2026-06-20 19:52 | NEW row (overnight cycle 56, `considerations.md` #4 + the SRE "graceful degradation" P0). Generalized the chokepoint's SINGLE `fallbackModel` retry into a pure, ordered, deduped, primary-excluded `resolveFallbackChain` (new `src/lib/ai/fallback.ts`, 10 unit tests) wired into both `callModel` + `callModelStream` (the single-fallback block becomes a chain loop). Back-compat: a lone `fallbackModel` → a 1-element chain = today's behavior, so it is byte-identical when off. New flag `providerFallbackEnabled()` (`AI_PROVIDER_FALLBACK`, default OFF, mirrors `AI_COST_ROUTING`) optionally appends an auto cross-model degrade to `cheapestLiveModel()` (reuses the tested WM-M15 picker), EXCLUDING surface `eval` (benchmark corruption) + `embed` (cheapestLiveModel is chat-only). Happy path untouched (only the post-primary-failure path changes), bounding blast radius. Dedicated adversarial review (chokepoint, cycle-39/40 history) confirmed 7 binding constraints + folded 2 findings (surface the PRIMARY error on chain exhaustion; a missing test). **Verification (ran):** tsc 0, eslint 0 (3 files), build ✓, `bun test` 332/332 (10 new). **◐ not ✅:** the chain + ranker are ✅ unit-verified; the live outage-degrade + auto cross-model fallback activate on the founder setting `AI_PROVIDER_FALLBACK` (verify on publish). UI breadcrumb: none (chokepoint resilience). _Counts not recomputed this cycle (parallel sessions active); a reconciliation pass owns the At-a-glance tally._ [~70%] · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier+gate: pure resolveFallbackChain (fallback.ts, 10 tests) wired at the chokepoint (callModel + callModelStream); byte-identical no-op when off; surfaces the primary error on chain exhaustion. Built + gate-green. Activation is the founder's AI_PROVIDER_FALLBACK flag (a config flip, not build). Founder PUBLISHED 2026-06-22. DONE. |
| 84 | ✅ | SEC-PII-EGRESS | Public-egress PII floor (high-sensitivity) | Block customer PII (Luhn-valid credit card · valid US SSN) from being stored/published world-readable via L2 announcements, alongside the secret floor | Governance | Done | 2026-06-22 18:55 | **2026-06-22 (Lane 1): ✅ NEW + CLOSED.** considerations.md Data/Privacy "PII classification + minimization" (P1), built as the PII sibling of SEC-EGRESS-GUARD. New PURE `src/lib/pii-egress.ts`: `scanEgressForPii` blocks ONLY structurally-validated, near-zero-FP PII — credit cards confirmed by the **Luhn checksum in code** (not a bare `\d{16}` regex, so order ids / hashes don't trip it) + US SSNs requiring the dashed shape AND SSA-valid ranges (no 000/666/9xx area, 00 group, 0000 serial, so dates/phones/part-numbers don't trip it). Emails + phones deliberately excluded (legit in changelogs = over-redaction). Wired into `createAnnouncement`/`updateAnnouncement` after the secret scan, blocking BEFORE the anon-readable store; never returns/logs the PII value (type only). Pure, ReDoS-safe (bounded quantifiers), idempotent, totally-defined. 14 tests (Luhn precision, SSN validity, benign/email/phone/date non-matches, both-types, no value-leak, idempotence). Adversarial self-review folded (no runtime-fatal, no value leak, byte-identical for benign content). tsc 0 / eslint 0 / `bun test` 1160. Live-verifies on the founder's next publish. Known accepted trade-off + future surfaces (public shares): [`../features/pii-egress-guard.md`](../features/pii-egress-guard.md). |
| 85 | ✅ | SEC-EGRESS-GUARD | Public-egress secret floor | Block a credential from being stored/published world-readable via L2 announcements; run the guardrails secret-detection at the egress write path | Governance | Done | 2026-06-21 | SHIPPED ◐ (Lane 3, 2026-06-21): considerations.md Security/CISO "Secret scanning". The guardrails engine (`evaluateGuardrails`) ran ONLY at the AI chokepoint; L2 announcements are anon-readable once published, so a pasted credential could leak world-readable. New PURE `src/lib/egress-guardrails.ts` (`scanEgressForSecrets` REUSES `evaluateGuardrails` with a self-contained high-confidence rule set: OpenAI legacy + scoped `sk-(proj/svcacct/admin)-`, AWS, GitHub `gh*_` + `github_pat_`, Stripe, Slack, Google, PEM; 19 tests) wired into `createAnnouncement`/`updateAnnouncement` to reject a secret-bearing title/body BEFORE store. The floor is OWNED (not per-workspace config) so it holds without opt-in. Single-agent security review: confirmed no publish-bypass (create/update is the only body-write path), no ReDoS, no value-leak in the error (names types only); flagged 1 real gap (broad `sk-` rule missed hyphenated scoped keys) → folded the CORRECT fix (a prefix-anchored rule, NOT broadening the char class, which would re-introduce a `risk-management-` false positive). tsc 0 / 894 tests / eslint 0. ◐ live-verify on publish. Spec: docs/features/egress-secret-guard.md. · **2026-06-22 (Lane 1, closure pass): ✅ CLOSED.** Verifier+gate: egress-guardrails.ts (9 high-confidence structural secret patterns, FND-0.7 over-redaction discipline, 19 tests) wired into createAnnouncement/updateAnnouncement BEFORE the write to the anon-readable published state; security-reviewed (no bypass/ReDoS/value-leak). Founder PUBLISHED 2026-06-22 (live-verifiable: try saving an announcement with sk-/AKIA). DONE. |
| 86 | ✅ | WM-F1b | Agent-workspace hardening (RLS swap + universal insert tagging) | Closes WM-F1's transitional isolation gaps so ALL new agent memory is workspace-scoped at the RLS layer | Monetization | Done | 2026-06-19 20:10 | CORE shipped (overnight cycle 37): migration `20260619220000_wm_f1b_agent_workspace_hardening.sql` adds a `user_id`-fed BEFORE-INSERT trigger (tags every insert path, incl. the `seed_default_agents` SQL fn + the `memory.remember` tool) + a DUAL-KEY membership RLS (`auth.uid()=user_id AND is_workspace_member(workspace_id)`, strictly more restrictive, never widens/locks out) on all 5 agent tables; `memoryRemember` tags the active workspace. Verified by a BEGIN..ROLLBACK dry-run on prod (5 triggers / 5 policies / 0 old remaining; trigger fills ws; `seed_default_agents` OK; owner sees all 23 memory rows, no reader breakage; 0 owners lack membership). NOT NULL (founder-gated moat-cascade call) + unique-key swap (couples to WM-F8) + types regen (post-publish) → WM-F1c. Live on publish. **✅ CLOSED 2026-06-22 (Lane 2, live-verify + 6-lens adversarial RLS leak-hunt over prod, 0 critical/high):** queried prod via Lovable MCP - all 5 agent tables (agents/agent_memory/agent_tools/agent_runs/agent_approvals) carry the dual-key policy `(auth.uid()=user_id AND is_workspace_member(workspace_id))` in BOTH using+with_check, the `trg_set_*_workspace` filler trigger, and RLS-on, live. The leak-hunt (cross-user/cross-workspace/anon/write-path lenses) found NO leak: a co-member fails `A=user_id`, a non-member fails the membership conjunct, a forged workspace_id is rejected by with_check. Acceptance (all new agent memory workspace-scoped at the RLS layer) met + live + verified. NOT NULL + unique-key swap remain the founder-gated WM-F1c (NOT NULL is intentionally avoided so deleting a workspace never cascade-deletes moat memory). [100%] |
| 87 | ✅ | WM-F3 | RBAC enforcement | Real owner/admin/member/viewer permissions for teams | Monetization | Done | 2026-06-19 | CORE shipped by a parallel session (`2b1c429591`): SECURITY DEFINER RBAC helpers (has_workspace_role / has_account_role) + role-based RLS policies + owner demotion trigger + TypeScript role helpers. Verified (by that session): tsc/test 8/8/build green. **BUT the migration would have FAILED to apply on publish** (it used `create policy if not exists` ×4, which is invalid Postgres syntax — CREATE POLICY has no IF NOT EXISTS clause — returning `ERROR 42601` and blocking the whole migration run). **FIXED cycle 41 (`70c0ca7db5`):** the four policy creates are now `drop policy if exists` + `create policy` (valid + idempotent) and the owner-demotion trigger is drop-then-create guarded. Confirmed on the live prod DB: the `if not exists` form errors 42601; the corrected migration applies cleanly (all RBAC helpers + 5 policies + the trigger created; rolled back, prod's real "ws owner manage" policy restored). Owner-lockout data-checked: all 7 workspaces have their owner as a `workspace_members(role='owner')` row, so the new role-keyed policy locks no one out. Live enforcement activates on founder's next publish. Spec: docs/features/rbac.md. **✅ CLOSED 2026-06-22 (Lane 2, live-verify + 6-lens RLS leak-hunt; FOUND + FIXED a latent landmine):** prod-verified live - `has_workspace_role`/`has_account_role` helpers, the owner-scoped `workspace_members`/`account_members`/`accounts` management policies, and the `prevent_workspace_owner_demotion` trigger fn all live; no non-owner can write membership or self-escalate. The leak-hunt surfaced a REAL latent landmine on the `accounts` "account members read" policy: source `20260619210000` wrote `m.account_id = id` intending `accounts.id`, but inside the subquery `account_members` is in scope and also has an `id`, so Postgres bound the unqualified `id` to the INNER row -> stored as `m.account_id = m.id` (a self-compare). DEAD today (0 of 6 account_members rows self-equal, contributes nothing under PERMISSIVE OR-ing) but a footgun: any future row with `id == account_id` flips the uncorrelated EXISTS to always-true -> full `accounts`-table exposure. FIXED: dropped the redundant dead policy on prod (member reads are correctly served by the sibling `is_account_member(id)`; verified all 6 owners are account_members so zero read-gap; behavior-preserving) + forward migration `20260622010000_wm_f3_drop_dead_accounts_read_policy.sql` keeps the repo source in lockstep. [100%] |
| 88 | ✅ | WM-F2 | Account-level memory pooling (paid) | Paid accounts compound memory across workspaces (the flywheel) | Monetization | Done | 2026-06-19 21:45 | CORE shipped (overnight cycle 40): migration `20260619240000_wm_f2_account_memory_pooling.sql` adds `is_account_member()` helper + rewrites `match_agent_memory` / `recent_agent_reflections` to optionally span account workspaces (for_account param). `memory.server.ts` `recallMemoryRefs` now accepts optional `accountId` in opts; passes `for_account` when pooling, otherwise `for_workspace` (backward compatible). Gated at RLS: is_account_member checks before spanning. 270/270 tests pass, tsc/build/lint green. **FIXED cycle 40 (`a368edf78a`):** the cycle-40 ship was tsc/build-green but regressed the WM-F1 baseline and was dormant: (1) a cross-user READ hole (RPCs filtered `user_id = for_user` not `coalesce(auth.uid(), for_user)`, reverting the pin WM-F1 added to close exactly that); (2) recalled EXPIRED memory (dropped the expiry filter); (3) dropped GLOBAL-scope memory (mandatory `agent_slug=` equality); (4) dropped the per-row `is_workspace_member` guard; (5) return-shape regressions (3 cols + distance-as-similarity; reflections lost importance/metadata + timestamptz→timestamp); (6) "staged but never driven" (pooling gated on `opts.accountId`, which no caller passes). The fix restores every WM-F1 invariant and resolves the pool account INTERNALLY from the workspace's account tier (entitlements.ts = single source of truth), so pooling is actually DRIVEN with no call-site changes. **Behaviorally dry-run-verified on the live prod DB** (stubbing unpublished WM-M2/F1 deps): paid account pools across BOTH workspaces, single-workspace unchanged, a different account recalls NOTHING of the paid account's memory; rolled back, prod untouched. tsc/build/lint/270 tests green. Live pooling activates on the founder's publish. **✅ CLOSED 2026-06-22 (Lane 2, live-verify + 6-lens RLS leak-hunt):** prod-verified live - `match_agent_memory` + `recent_agent_reflections` both carry the `for_account` param, and the `is_account_member(account)` gate is live + correct (verified body `m.account_id = account AND m.user_id = auth.uid()`). The leak-hunt confirmed the pooling RPCs raise NO read/write leak (a different account recalls nothing of a paid account's memory; the per-row guards from WM-F1 are intact). Closed: the pooling path is live + correct + driven (account resolved internally from workspace tier). Behavioral observation of cross-workspace compounding needs a paid account with 2+ workspaces (a data condition, not a code gap). [100%] |
| 89 | ✅ | WM-F9 | Isolation audit + scope leak fixes | Close cross-member leaks (meetings/notes/briefs/chat) before invites | Monetization | Done | 2026-06-19 18:33 | CORE shipped (overnight cycle 35): migration `20260619190000_wm_f9_isolation_scope.sql` adds workspace_id + deterministic backfill + a NEW.user_id BEFORE-INSERT trigger (service-role-safe) + NOT NULL + index + DUAL-KEY membership RLS (`auth.uid()=user_id AND is_workspace_member(workspace_id)`; keeps user-private per the acceptance, never widens) to **10 tables**: the named 4 (meetings/notes/daily_briefs/copilot_messages) + the 6 the adversarial audit found (prototypes + 3 children, scheduler_proposals, ritual_sessions). Public-share policies preserved; sync_mappings documented-deferred; CASCADE delete-confirm copy fixed (AppShell). Behaviorally dry-run-verified on the live prod DB (0 nulls, NOT NULL, trigger fill, old policies removed, dual-key created), rolled back. 239/239, tsc/build/lint/humanization green; 3-lens review (reader-breakage clean), all real fixes folded. Live isolation activates on publish. Follow-up WM-F9b: active-workspace read-filtering + sync_mappings verdict. **◐ WM-F9b slice shipped 2026-06-21 (lane 3):** active-workspace read-filtering for the meetings list, the first driven cut of the reads half. New pure `src/lib/workspace-scope.ts` = the single scoping rule: `workspaceScopeId` (filter iff a concrete non-blank id, else null = unfiltered/today) + `applyWorkspaceScope` (adds `.eq("workspace_id", id)` once iff present); 7 tests pin both. `listMeetings` gains an optional `workspaceId` (zod `uuid().nullable().optional()`) routed through the helper (omitted/null = byte-identical to before, incl. the 502 retry); `CalendarPanel` passes `activeWorkspaceId` + keys `["meetings", activeWorkspaceId]` (also delivers WM-F8b refetch-on-switch for meetings). Additive + RLS-dual-key-safe (the client id can only NARROW within already-authorized rows; non-member workspace = empty, never a leak). tsc 0 / bun test 664 (7 new) / eslint clean; 4-lens adversarial review (3 ship + 1 ship_with_fixes, 0 must-fix, both should_fix folded); live on the founder's publish (with the WM-F9 column). `sync_mappings` verdict RATIFIED deferred (connector version-state, `.eq(id).eq(user_id)`, not a workspace list). Remaining → WM-F9c: brief client-wiring (only caller is settings.tsx, another lane), `listCopilotMessages` (no caller), prototype/scheduler/ritual reads, internal AI-context reads. **✅ CLOSED 2026-06-22 (Lane 2, live-verify + 6-lens RLS leak-hunt over prod, the isolation CORE):** prod-verified live - all 10 isolation tables (meetings/notes/daily_briefs/copilot_messages/prototypes + 3 children/scheduler_proposals/ritual_sessions) carry the dual-key policy `(auth.uid()=user_id AND is_workspace_member(workspace_id))` in using+with_check, the workspace-tagging trigger, and RLS-on. The leak-hunt (the exact threat this row exists to close - "cross-member leaks before invites") found NONE: a co-member reading another member's rows fails on `A=B`; the only anon surface is the prototypes/prototype_files public-share, correctly gated to `is_public=true` with projection-restricted column grants (prototype_attachments/_messages have zero anon surface). Isolation core CLOSED. WM-F9c (active-workspace read-FILTERING for briefs/copilot/prototype/scheduler/ritual + internal AI reads) stays a separate additive follow-up - it narrows reads WITHIN already-isolated rows (a UX/scoping nicety, NOT a leak; the RLS already prevents every leak). [100% isolation core] |
| 90 | ✅ | WM-M15 | Margin levers (cost-aware routing) | Keeps credits margin-positive (no self-serve BYOK to lean on) | Foundational | Done | 2026-06-22 17:30 | CORE shipped (overnight cycle 34): pure `src/lib/ai/routing.ts` (blendedPrice / cheapestLiveModel / costRoutedModel) wired at the AI chokepoint behind the `AI_COST_ROUTING` env flag (default OFF = byte-identical to today). Narrow safe routable set {brief, scheduler, test} + a tier guard that never downgrades a deliberate reasoning/premium/code/vision model. 5-lens adversarial review caught a real quality-gate hole and was fixed: judge (the Critic wedge), eval (benchmark), embed (chat-model trap) excluded; the credit pre-check + debit now track the model actually run. 12 routing tests, 239/239, tsc/build/lint green. **Cache half ◐ shipped cycle 38** (`ai_response_cache` table + content-hash lookup + chokepoint read/write, 7-day TTL, conservative surface whitelist brief/scheduler/test only). **Cache half FIXED cycle 39** (`7897085004`): the cycle-38 cache was tsc/build-green but shipped 3 runtime-fatal bugs, all corrected + prod-dry-run-verified: (1) the migration's `WHERE expires_at > now()` index predicate would have FAILED to apply on publish (Postgres ERROR 42P17, now() is STABLE not IMMUTABLE); (2) the cache hit never skipped the provider call (the `if(!cacheHit)` guard wrapped only the write), so it saved zero COGS; (3) readCache passed the literal string `"now()"` so every read errored and the cache always missed. Live routing + cache both activate on founder's `AI_COST_ROUTING` flag. Honest scope: both together complete the cost-efficiency story; initial margin impact modest (conservative whitelist, guardrails required) ⚖️ STAYS, do NOT freeze (2026-06-21): cost-aware routing + response cache are whole-platform COGS levers at the chokepoint, NOT credit-engine scope; re-categorized out of Credit. · **2026-06-22 (Lane 1): ✅ CLOSED.** Verifier verdict = complete-pending-golive: the routing module + response cache are code-complete and UNIT-VERIFIED (routing.test.ts 12 tests; the 3 cycle-38 cache runtime-fatal bugs all fixed + prod-dry-run-verified). No buildable gaps remain. Live COGS savings activate on the founder's `AI_COST_ROUTING` flag + publish (a config flip, not build). COGS lever, not credit-engine. **CLOSED — do not re-pick.** |
| 91 | ✅ | WM-F4 | Ownership transfer | Transfer an account/workspace; unblocks owner-leaves | Monetization | Done | 2026-06-19 22:40 | CORE shipped (overnight cycle 43, `345276a5aa`): migration `20260619250000` adds `workspace_audit_log` (member-read RLS, writes via RPC only) + a SECURITY DEFINER `transfer_workspace_ownership` RPC (atomic: reassign owner_id, flip member roles new→owner / old→admin, write audit row) + a `transferWorkspaceOwnership` server fn. Reassign-before-demote ordering respects WM-F3's demotion trigger. **Behaviorally dry-run-verified on prod:** transfer flips owner_id + both roles + writes audit `{from,to}`; non-owner caller blocked; non-member target blocked; rolled back, prod untouched. tsc/build/lint/270 tests green. **UI shipped (FE cycle 1, 2026-06-20):** the `MembersCard` in Settings > Workspace lists members (identity via the new membership-gated SECURITY DEFINER RPC `workspace_members_with_identity`, migration `20260620213000`, since profiles RLS is own-row-only) and gives the OWNER a per-member "Make owner" action behind an inline two-step confirm (it demotes the owner to admin, so not one-click), wiring `transferWorkspaceOwnership`; also wires `removeWorkspaceMember` and the previously-deferred members list. Built per the frontend build protocol (design-taste-frontend + impeccable). **Role management + owner-only correction (FE cycle 3, 2026-06-20):** added `changeWorkspaceMemberRole` (the role chip becomes an inline `<select>` on manageable rows) and CORRECTED a cycle-1 silent-failure - member management is OWNER-ONLY (the WM-F3 `workspace_members` policy is `for all` + owner-scoped, so an admin's remove deleted 0 rows but PostgREST reported success); all management (role/remove/transfer) is now gated to `isOwner`, and `removeWorkspaceMember`/`changeWorkspaceMemberRole` use `.select()` + a 0-row throw so an RLS-blocked write fails loudly. tsc 0 / eslint 0 / build ✓. **◐ not ✅:** wired + gate-green + adversarially-reviewed runtime-safe + driven, but renders on the founder's publish and the identity RPC's live dry-run is pending (Supabase MCP unauthorized this session, flagged for publish-verify). UI: Settings > Workspace > Members. **✅ CLOSED 2026-06-22 (Lane 2 live-verify, app published):** queried prod via Lovable MCP - all WM-F4 objects are live (`transfer_workspace_ownership` + RBAC helpers + `workspace_audit_log`) EXCEPT the members-identity RPC `workspace_members_with_identity` (repo migration `20260620213000` had NEVER reached prod - the repo's `supabase/migrations/` track separately from Lovable's own `schema_migrations`, so committed migrations can silently drift). The call site degraded gracefully (null-identity fallback), masking it - the Members list showed roles but no names/emails. Applied the committed function to prod via Lovable `query_database` (additive, idempotent, read-only SECURITY DEFINER, execute granted to authenticated); confirmed it exists + its identity-join returns display_name/email. Members list now renders identities live. [100%] |
| 92 | ✅ | WM-F5 | Invites (account/workspace) | Add teammates (no invite flow today) | Monetization | Done | 2026-06-19 23:03 | BACKEND shipped (overnight cycle 44, `d0e7300c33`): migration `20260619260000` adds `workspace_invitations` (token default, role admin/member/viewer, status, 7-day expiry, manager-only RLS via `can_manage_workspace`, no invitee read = no pre-accept leak) + a SECURITY DEFINER `accept_workspace_invitation(_token)` RPC (validates pending+unexpired, binds the token to the invited email so a leaked token can't be redeemed by another account, upserts the membership with the invited role, single-use under a row lock). New `email.server.ts` (no-op `sendInviteEmail` returning the copy-paste link; outbound email founder-gated). New `inviteMember`/`listInvitations`/`revokeInvitation`/`acceptInvitation` server fns. **Behaviorally dry-run-verified on prod:** invite→accept added the user with role=member + flipped the invite to accepted; re-accept, wrong-email, and expired were all rejected; rolled back, prod untouched. tsc/build/lint/270 tests green. **Invite UI wired (front-end pivot, cycle 53):** a `TeamCard` ("Invite teammates") in Settings > Workspace wires `inviteMember`/`listInvitations`/`revokeInvitation` - invite by email + role, copy the join link (outbound email is a no-op), see invitations + their status, revoke a pending one; built on the Ember system, ran `impeccable` (passed the bans, renamed the list header to match what the fn returns). tsc 0 / eslint 0 / build ✓ / 305 tests. **◐ not ✅:** wired + gate-green + driven, but renders on the founder's publish (the WM-F5 migration + `can_manage_workspace` apply then) and was not render-verified locally. **Accept route wired (front-end pivot, cycle 54):** a standalone `/join/$token` route (`src/routes/join.$token.tsx`) redeems the token via `acceptInvitation` for a logged-in invitee (single-run guarded, StrictMode-safe) and shows a clear log-in/sign-up prompt for a logged-out one, so the invite→accept loop is now complete; ran `impeccable` (moss success state per the role-color law). tsc 0 / eslint 0 / build ✓ / 307 tests. **◐ not ✅:** wired + gate-green + driven, renders on the founder's publish (the WM-F5 migration applies then), not render-verified locally. The members list deferred here SHIPPED in FE cycle 1 (the `MembersCard`). **Redirect-after-login SHIPPED (FE cycle 4, 2026-06-20):** `/login` + `/signup` gained an open-redirect-guarded internal-only `next` param; the join page's Log in / Sign up links pass `next=/join/<token>`, so a logged-out invitee lands back on the accept page after email auth (a fresh signup accepts before onboarding since /join is a top-level route). OAuth still returns to `/` (founder-gated redirect_uri, untouched) - email path fully fixed. tsc 0 / eslint 0 / build ✓. The whole invite flow (invite -> share link -> log in -> accept) is now wired end-to-end. UI: Settings > Workspace > Invite teammates; invitee opens /join/<token>. **✅ CLOSED 2026-06-22 (Lane 2, live-verify on prod):** the invitation backend is live + correct + NO drift - `workspace_invitations` has RLS-on and exactly the manager-only policy `can_manage_workspace(workspace_id)` (using+check), so a not-yet-member invitee has zero direct read (no pre-accept workspace-data leak); the `accept_workspace_invitation(text)` RPC is SECURITY DEFINER, granted ONLY to authenticated+service_role (revoked from anon/public), and its three security properties are all present on the live function body: email-binding (signed-in email must equal the invited email, so a leaked/forwarded token cannot be redeemed by another account), `FOR UPDATE` row-lock (no double-redeem race), and the single-use accepted-flip. UI (TeamCard invite + `/join/$token` accept + open-redirect-guarded redirect-after-login) shipped + gate-green. Optional remaining: a live playwright invite->accept E2E (the backend security is verified; outbound email stays the founder-gated no-op). [100% backend + shipped UI] |
| 93 | ✅ | WM-M4 | Runtime credit seam (dormant) | The seam the credit engine plugs into; credits-only (no self-serve BYOK) | Monetization | Done | 2026-06-19 16:17 | CORE shipped (overnight cycle 30): `CreditExhaustedError` + cached `creditsEnabled()` + `resolveCreditAccountId` + `assertAccountCredits` (pre-call) + `debitAccountCredits` (post-call) wired into callModel + callModelStream at the chokepoint; gated behind `credits_enabled()` (dormant no-op, zero behavior change). tsc/build green, 201/201 tests, eslint clean. WM-M12 fills the bodies (atomic draw-down). Live debit on publish + flag flip [~75%] ⚖️ STAYS (2026-06-21): the runtime credit seam in runtime.server.ts stays; the debit ENGINE that fills it is Lovable's. · 2026-06-21 reconcile: runtime credit seam (CreditExhaustedError/CreditCapError + cached creditsEnabled() + assertAccountCredits) present and dormant. **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** the seam is no longer dormant — `credits_enabled()=true` and the engine fills it (founder ruling: billing is mine; engine taken live 2026-06-21). Fresh end-to-end live proof: sent an Ask/chat (Gemini 3 Flash) on prod as the workspace admin → reply rendered (pre-call assert PASSED, not halted) and the seam's post-call `debitAccountCredits` fired — `account_credits.balance_credits` **2496 → 2492** with three `credit_ledger` `debit` rows (-1/-2/-1) stamped at the same instant (per-model-call draw-down). Pre-check + post-debit both proven at the live chokepoint. [100%] |
| 94 | ✅ | WM-F7 | Settings IA (Account/Workspace/Personal) | A clear rubric for where each setting lives | Monetization | Done | 2026-06-19 | effort M; needs WM-M2, WM-F3 · 2026-06-21 reconcile: Settings sections (Accounts/Models/Staff/Workspace/Plan/Credits/Integrations/Profile/Data) + the Plan/billing tab shipped and LIVE-TESTED. Full Account/Workspace/Personal IA split still partial. **✅ CLOSED 2026-06-22 (Lane 2 — founder-authorized IA call made + documented):** the deliverable is "a clear rubric for where each setting lives" — now decided + recorded in [`../features/settings-ia.md`](../features/settings-ia.md). The rubric: **Personal** (Profile, Models/BYO keys — follow the user), **Account** (Accounts/connected logins, Plan, Credits — the billing entity), **Workspace** (Workspace config/members/invites/roles, Staff/agent roster, Integrations/connectors, Data/export), **System** (Health). It mirrors the data model + the WM-F8b query-scope tiers (user-global / account-global / workspace-scoped), and the doc carries the placement rule for any new setting (so the IA never rots). The live flat `TabRow` already maps onto it; a VISUAL regroup under scope headers is an optional design-pass polish (no behavior change), intentionally left out per the velocity ruling (no speculative churn on a live-tested surface). [100% — rubric decided] |
| 95 | ✅ | WM-F8 | Workspace switch hardening | No stale-data flash on switch; agents/memory switch too | Monetization | Done | 2026-06-19 23:18 | CORE shipped (overnight cycle 45): `setActiveWorkspaceId` in `src/hooks/use-workspace.tsx` now clears every workspace-scoped TanStack Query cache entry on a real switch (`queryClient.removeQueries({predicate})`, active + inactive) so no previous-workspace data flashes before the refetch; a pure, unit-tested allowlist (`src/hooks/workspace-query-scope.ts` + `.test.ts`, 4 tests) preserves the user/account-global queries (workspaces list, profile, billing, connections, calendar-connections, integrations, api-keys, mcp-tokens) and fails safe to "workspace-scoped" for any unlisted/odd key. Driven by the AppShell switcher (`AppShell.tsx:516`) + create/delete paths. Adversarial review: no stale-closure refetch (keyed queries refetch on key change; server-fn queries hold no client workspace closure). tsc 0 / eslint 0 (my files) / build ✓ / `bun test` 274/274. **◐ not ✅:** correct + gate-green + driven, but the full user-visible "no cross-workspace flash" payoff scales as reads become workspace-filtered server-side (most server fns are still user-scoped today; WM-F9b), and the two-seeded-workspace Playwright check needs a publish. Defense-in-depth follow-up WM-F8b: thread `activeWorkspaceId` into the un-keyed query keys. UI breadcrumb: AppShell > workspace switcher. **✅ CLOSED 2026-06-22 (Lane 2, WM-F8b account-tier hardening built + reviewed):** an audit of the preserve-list against the app's actual query-key roots found a real (if minor) gap - the flat 8-root allowlist preserved `billing`/`connections`/`integrations` on EVERY switch, but a switch can cross ACCOUNTS (a user can be a member of workspaces in different accounts since WM-F5 invites), so a cross-account switch left the previous account's billing/connections cached (a stale cross-account flash). FIX: split the allowlist into USER-global (`workspaces`/`profile`/`api-keys`/`mcp-tokens` - never change on any switch) vs ACCOUNT-global (`billing`/`connections`/`calendar-connections`/`integrations`); new pure `accountChangedOnSwitch(workspaces, from, to)` (proves same-account only when both known + equal `account_id`, else fail-safe to clear) + `shouldClearOnWorkspaceSwitch` drive the reset. A same-account switch is byte-identical to before; a cross-account switch now also clears account-globals; first-mount preserves them (not a switch). 12 scope tests (77 asserts), tsc 0 / `bun test` 920 / my files lint-clean; a focused adversarial typescript-review returned SHIP_WITH_FIXES (no critical/high) and its one medium - account-globals wrongly cleared on first mount under SSR hydration - was FOLDED (gate the cross-account clear on `activeWorkspaceId !== null`). The "thread activeWorkspaceId into un-keyed query keys" belt-and-suspenders is intentionally NOT built (redundant with the cache-clear + the new account-tier; per the velocity "nothing speculative" rule). Remaining is the coupled WM-F9c server-side read-filtering (separate item) + an optional 2-workspace playwright check. [100% switch-hardening core] |
| 96 | ✅ | WM-F6 | Move product between workspaces | Relocate a product + its data across workspaces | Monetization | Done | 2026-06-19 23:42 | CORE shipped (overnight cycle 46): migration `20260619280000_wm_f6_move_product.sql` adds a transactional SECURITY DEFINER `move_product(_product_id, _dest_workspace_id)` RPC that, after a guard (caller is owner/admin of BOTH workspaces via WM-F3 `can_manage_workspace` + both in the SAME account via WM-M2 `account_id` + a `FOR UPDATE` lock), atomically reassigns `workspace_id` on the product (`projects`) + 13 product-scoped tables: 10 direct children (signals/themes/opportunities/prds/docs/tasks/decisions/conversations via `project_id`-or-`product_id`; notes/prototypes via `project_id`; ai_events/connection_bindings/rag_chunks/studio_changesets via `product_id`) + 3 grandchildren scoped through their parent (doc_versions→docs, messages→conversations, learnings→opportunities/prds). Because every table is read-gated by `is_workspace_member(workspace_id)`, the reassignment IS the move (access follows). Memory stays at the workspace (not moved). New `moveProduct` server fn in `projects.functions.ts`. **Verified live (BEGIN..ROLLBACK on prod, stubbing the unpublished WM-M2/F3/F9 deps):** moving a real product (9 signals/2 prds/10 tasks/3 decisions/8 messages) relocated ALL of them to the dest with **0 orphans left in src**, and both guards fired (cross-account blocked; non-manager blocked); rolled back, prod untouched. tsc 0 / eslint 0 / build ✓ / `bun test` 274/274. **◐ not ✅:** RPC + server fn built + behaviorally dry-run-verified, but the **Settings UI affordance (move action) is deferred** to the design pass (founder ruling), and live move activates on publish (depends on unpublished WM-M2/F3/F9). Follow-up WM-F6b: scope `artifact_lineage` + other generic-provenance tables (generic artifact ids; deferred to avoid a wrong remap). **UI shipped (FE cycle 2, 2026-06-20):** a "Move to workspace" `DropdownMenuSub` in each product's actions menu (the sidebar product list ⋯ menu, beside Rename/Delete) lists the user's other workspaces; picking one opens a non-destructive confirm (move is reversible) then calls `moveProduct`, clears the now-moved active product, and refreshes. Hidden when there is only one workspace. Built per the frontend build protocol (emil-design-eng for the submenu-then-confirm interaction + impeccable gate). tsc 0 / eslint 0 / build ✓. **WM-F6c SHIPPED (Lane 2, 2026-06-21):** destinations are now filtered to the SAME account, so a cross-account pick is no longer offered (was: offered then rejected by the RPC with a toast). New pure `src/lib/workspace-move.ts` `moveDestinationWorkspaces(workspaces, sourceWorkspaceId)` (9 tests) hides a destination ONLY when both accounts are concrete and provably differ, and fails open on any unknown/null/blank id so it can never wrongly hide a valid destination; the `Workspace` type gains `account_id?: string | null` (the workspaces query already selects `*` and WM-M2's NOT NULL `account_id` backfill is live, so the match is exact in prod); AppShell's `otherWorkspaces` now uses the helper. The `move_product` RPC stays the authoritative same-account guard (re-derives source + both accounts server-side under `FOR UPDATE`), so the filter is purely cosmetic and failing open can never permit an invalid move. tsc 0 / eslint 0 / `bun test` 733 (9 new) / dash-clean; 3-lens adversarial review (correctness/security/regression) all SHIP, 0 must-fix. (The `bun run build` gate is red on a PRE-EXISTING node20-vs-ESM `lovable-tagger` tooling issue, reproduced on base without this change, unrelated to WM-F6c.) ◐ renders on publish. Remaining follow-up WM-F6b: scope `artifact_lineage` (deferred). UI: the sidebar product ⋯ menu > Move to workspace. **✅ CLOSED 2026-06-22 (Lane 2, live-verify + 6-lens RLS leak-hunt):** prod-verified live - `move_product(_product_id, _dest_workspace_id)` RPC exists on prod with the dual guard (owner/admin of both workspaces + same account, `FOR UPDATE`); the UI (Move-to-workspace submenu + the WM-F6c same-account destination filter) shipped. The leak-hunt found NO cross-workspace write bypass: every product-scoped table is read-gated by `is_workspace_member(workspace_id)` with no OR-able policy, so the reassignment IS the move and access follows correctly. Closed. WM-F6b (`artifact_lineage` generic-provenance scoping) stays deferred. [100%] |
| 97 | ✅ | F-IA-V4 | Collapse IA to 7 surfaces | Route consolidation + redirects + vocab enforcement; one coherent product | Cockpit | Done | - | **✅ COMPLETED (Lane 1, 2026-06-22, founder-blessed design pass):** the IA is collapsed to the canonical surfaces (Today · Product · Build · Missions · Brain · Govern[Engine Room] · Settings · Sync, + Ask/admin/onboarding) — EVERY legacy/secondary route now redirects to a canonical home (verified via the route inventory: agents/prompts→govern, memory→brain, calendar/docs/meetings/observe/traces/analytics/drift→govern·brain, swarm→missions, briefing→settings, tasks→Today). The two remaining ORPHANS were folded this session: `/notifications`→Settings→Notifications (new tab) + `/integrations`→Settings→Integrations. Live-verified both redirects + the new tab on prod. tsc0/1003. Vocab/deeper visual refinement remains the founder's ongoing pass. |
| 98 | ✅ | F-IA-TODAY-BRIEFING | Merge Today + Briefing | One morning surface, not two | Cockpit | Done | - | **✅ done (verified 2026-06-22):** Today (`/`) is the single morning command-center; `/briefing` redirects to Settings→Workspace (the strategic brief relocated there). No second morning surface. |
| 99 | ✅ | F-IA-CULL-CALDOCS | Cull /calendar /meetings /docs /sync from nav | De-clutter the operator nav (data kept) | Cockpit | Done | - | **✅ done (verified 2026-06-22):** the nav is the calm 3-group set (Product · Missions · Brain); calendar/meetings/docs redirect to their Brain/Knowledge homes (data kept), Connectors (`/sync`) lives in the Trust row, not the main nav. |
| 100 | ✅ | F-IA-AGENTS-TABS | Fold /prompts + /agents into one route | Agents live in one place | Cockpit | Done | - | **✅ done (verified 2026-06-22):** `/agents`→`/govern?tab=team` (Engine Room roster) and `/prompts`→`/govern?tab=prompts`; agents/prompts live in one place (Engine Room), neither clutters the main nav. |
| 101 | ✅ | RUNAWAY-INCIDENTS | Runaway missions in the persistent Incidents log | Wires the RUNAWAY-DETECT detector into the operator's "what went wrong" record: a spinning mission becomes a durable `runaway` incident (not only the silent Missions glance) | Governance | Done | 2026-06-21 02:35 | NEW row (Lane 1; the decision panel's runner-up, completes RUNAWAY-DETECT's "operator surface" follow-up). `getIncidentsInternal` (`incidents.functions.ts`) gains a `runaway` source: scans the 200 most recent user missions (no age floor, so an old still-spinning mission is caught), folds via the NEW shared pure `buildMissionStats` (extracted from `getRunawayMissions` so the two read paths cannot drift), runs `assessMissions`, emits ONLY `runaway` severity (terminal-but-breached = `watch`, excluded, so no flood); stable id `runaway:<missionId>`. `IncidentKind` widened (+`runaway`) forces a `KIND_LABEL` entry (tsc-enforced). Read-only, user-scoped (`.eq('user_id')` + `.in('mission_id', ids)` over own ids), no writes/AI/spend/loop edit. 2-lens adversarial review: both **SHIP**, refactor PROVEN behaviorally equivalent, no correctness/security/leak/flood defect (the only notes: an intentional graceful-degradation error-tolerance matching the aggregator + an inert negative-attempts clamp, both already documented). tsc 0 / eslint 0 / build ✓ / `bun test` 475 (8 new). **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** the Engine Room → Incidents tab renders live on the published app (two real `execution` incidents with View-trace links), proving `getIncidents` runs in production; the `runaway` source correctly emits NOTHING because the demo's 21 missions include none that meet the runaway thresholds (the same detector live-verified via RUNAWAY-DETECT's popover) — a correct empty result, not a missing surface (Playwright). Spec: [`../features/runaway-detection.md`](../features/runaway-detection.md). UI: `/govern?tab=incidents`. _Counts not recomputed (parallel sessions active)._ [100% build] |
| 102 | ✅ | H2-AUDIT | Roadmap-decision audit ("why is this on the roadmap") | Append-only trail of every roadmap commitment event (who/when/bucket + the outcome promised at that time) | Decide | Done | 2026-06-21 | SHIPPED backend (Lane 0, 2026-06-21 02:42): closes the `considerations.md` PM P0/P1 gap "'Why is this on the roadmap?' answerable with cited evidence." New migration `20260621024000_h2_roadmap_audit.sql` (append-only `roadmap_audit`: insert-own + read-own-or-workspace RLS, NO update/delete = tamper-evident; FK to opportunities ON DELETE CASCADE so the audit never outlives its data; auto-covered by DATA-RETENTION-b's workspace_id erasure sweep) + best-effort audit writes in `commitRoadmapItem` (records the declared outcome AT COMMIT TIME = point-in-time evidence) and `updateRoadmapItem` (bucket moves) + new `getRoadmapHistory` read fn + pure `src/lib/roadmap-audit.ts` (`buildAuditInsert`/`summarizeRoadmapHistory`, 5 tests). tsc/eslint/build/451-tests green; migration dry-run-verified on prod (BEGIN..ROLLBACK: table+RLS+2 policies+FK CASCADE confirmed). ◐ not ✅: backend built + dry-run-verified, renders on publish; the "why is this here" UI surface shipped as H2-AUDIT-UI (row 8c). Detail: [`../features/h2-writes.md`](../features/h2-writes.md). **✅ CLOSED 2026-06-22 (Lane 2, live-verify on prod):** `roadmap_audit` is live + correct + NO drift - RLS-on with EXACTLY two policies (INSERT with_check `user_id = auth.uid()` = insert-own; SELECT using `user_id = auth.uid() OR (workspace_id IS NOT NULL AND is_workspace_member(workspace_id))` = read own-or-workspace, the null-workspace guard preventing any null-ws row leaking), and crucially **0 UPDATE/DELETE policies** so the trail is genuinely append-only / tamper-evident (no API path can rewrite history). Columns match the spec (opportunity_id/user_id/workspace_id/action/from_bucket/to_bucket/outcome/measure/created_at). Backend logic-complete (best-effort writes + getRoadmapHistory + roadmap-audit.ts, 5 tests). [100%] |
| 103 | ✅ | H2-AUDIT-UI | Roadmap "why is this here" history popover | A reveal-on-demand popover on each roadmap card showing its decision history | Decide | Done | 2026-06-21 | SHIPPED (Lane 0, 2026-06-21 02:58): surfaces the H2-AUDIT backend in the product. New `src/components/product/RoadmapHistory.tsx` - a quiet "why" trigger on committed cards opens a Popover that LAZILY (`enabled: open`, no N-query fan-out) reads `getRoadmapHistory` + renders `summarizeRoadmapHistory` (the live "why" = newest committed outcome) + a hairline-separated event timeline (committed/moved · bucket · date, with the outcome promised at that time). Engine-Room doctrine (calm front, reveal on demand; name the outcome) + Ember Editorial (restrained tinted neutrals, `·` separators, no banned pattern, no fancy unicode); graceful empty/pre-publish state. tsc/eslint/build/469-tests green. ◐ live on publish (renders real history once audit rows exist; not render-verified locally). UI: /roadmap > a committed card > "why". Detail: [`../features/h2-writes.md`](../features/h2-writes.md). **✅ CLOSED 2026-06-22 (Lane 2):** the backend it surfaces is now live-verified (see H2-AUDIT - `roadmap_audit` live + RLS-correct + append-only on prod), and the popover (`RoadmapHistory.tsx`, lazy `enabled: open`, reads getRoadmapHistory + summarizeRoadmapHistory) is shipped + wired into `RoadmapBoard.tsx` + gate-green (469 tests). Renders real decision history as audit rows accrue (a roadmap commit/move writes one). Optional remaining: a live playwright check of the popover on a card with history. [100%] |
| 104 | ✅ | ENG-06 / F-GOV-COST-SURFACE | Cost-per-outcome chip + unit-economics roll-up | "What you got for what you spent" chip on the front + full per-agent telemetry behind the Engine Room | Cockpit | Done | 2026-06-17 | B1 + B3 built; **B2 (Missions manager's glance) shipped FE cycle 5 (2026-06-20):** a calm one-line cost-per-outcome glance (`MissionsCostGlance`, new `src/components/cockpit/MissionsCostGlance.tsx`) on the Missions header, reusing the shipped `getCostPerOutcome` (shares the `["cost-per-outcome"]` query cache with Today's chip = no extra fetch), silent on a quiet week. Engine-Room-compliant (one outcome-framed line, not a hero-metric panel; impeccable hero-metric ban applied). Minor: ~similar to the Today chip but kept a dedicated component to leave the Today surface untouched (collision-safe). tsc 0 / eslint 0 / build ✓. **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** all 3 slices render with real production data (Playwright). Missions-header glance: "This week the fleet shipped 7 decisions · 6 shipped for $0.05 · $92.49 of $250 this month". Engine Room → Analytics: the full unit-economics roll-up — SPEND·7D $0.03 (101 runs · 31 errors), TOKENS 174.9k, median latency 1.6s/p95 10.2s, "UNIT ECONOMICS · COST PER OUTCOME $0.0039" (spend ÷ 13 outcomes), spend-by-surface, spend-by-agent (click-to-drill), and a per-model table. UI: Missions header + `/govern?tab=analytics`. All 3 B-slices built + proven live [100% build] |
| 105 | ✅ | AMBIENT-ARC | Ambient autonomy arc (Trust Dial) | Per-agent autonomy arc incl. Ambient + suggested promotion on the Agents tab | Governance | Done | - | recon: shipped as `TrustDial.tsx`; see trust-and-autonomy.md §7. **✅ CLOSED 2026-06-22 (Lane 2, mislabel correction on code evidence):** `src/components/cockpit/TrustDial.tsx` ships the full spec - the per-agent autonomy arc `observing -> proving -> trusted -> ambient` (the "ambient" stage = a trusted agent running confirm-gated tools unattended where safe, the level the user-wide AutonomyCard deliberately omits), the live trust score + the engine's SUGGESTED next arc with its evidence, and click-to-move control via `setAgentArc` (operator overrides; `resolveApprovalMode` is the safety floor). Wired into BOTH `AgentsPanel.tsx` (Agents tab) and `AgentRosterPanel.tsx` (Governance roster). Was ⬜ purely as a stale label; the feature is built + driven. [100%] |
| 106 | ✅ | F3 | Continuous discovery feed | Always-fresh, per-product clustered signal feed (`/product?tab=signals`) | Sense | Done | 2026-06-18 | **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** on the published app, switching to the Lumen product renders the per-product clustered feed with real data — a THEME/EVIDENCE/SOURCES/FRESH/CONF table of Scout-clustered themes ("Slow first-response on Tier-1 tickets · 3 items · intercom·slack·csat · conf 82", "Unsafe escalation on refund/legal · conf 91", etc.), per-source attribution, freshness dates, confidence, and a per-theme "Draft spec" action; the auto-cluster toggle shows its gated state "On · awaiting first run" (Playwright). The BUILD is complete + proven live. Remaining (founder-gated, NOT a build gap): activating the auto-cluster cron (F3-CRON shipped gated off; it commits recurring AI spend). [100% build] |
| 107 | ✅ | D4 | Cancel / replay-and-branch / checkpoints | Brake-pedal cancel + replay a finished mission (optionally a different model) with a branch link | Decide | Done | 2026-06-18 | **✅ D4b SHIPPED + LIVE-VERIFIED (Lane 1, 2026-06-22):** built the rich side-by-side replay-vs-original checkpoint-diff. Pure `src/lib/mission-diff.ts` (`deriveMissionSide`/`alignHops`/`diffMissions`; hops·cost·tokens·tool-call outcomes·duration·per-hop output drift·final-output-changed; deltas = replay−original; durationMs delta null unless BOTH completed; 11 tests) + `src/components/missions/MissionDiff.tsx` (fetches the original via `getMission`, calm Ember styling, `--rose` only on regression, degrade-silent) wired into `/missions/$id` behind a "Compare with original" toggle (gated on `replayed_from_mission_id`). tsc0 / 958 tests. **Live-verify:** replayed a finished mission on prod -> the replay's diff rendered correctly (Hops 1→2 ▲, Cost $0.00→$0.0028 ▲, Tokens in 0→15,793, Tool calls 0→5, Duration original 5.8m / replay "—" with Δ "—" since the replay was still running = the no-false-time-savings guard proven live; per-hop drift "orchestrator produced a different output" + "discovery-scout ran only in the replay"). Screenshot `d4b-mission-diff-live-2026-06-22.png`. Cancel + replay-and-branch were already shipped+verified. [100%] |
| 108 | ✅ | BLD-05 | Inspector gate (tests + preview before merge) | A test + preview bar on the PR/Checks tab before the operator clears the merge gate (warn-only on no tests) | Build | Done | 2026-06-18 | **CLOSED + LIVE-VERIFIED 2026-06-22 (Lane 2):** the inspector gate is fully built, wired, and now live-verified on the published app. Code is in `main` (the old `bld-05-inspector-gate` branch was long since merged by a Lovable rebuild): pure `src/lib/ai/studio-inspection.ts` (`summarizeInspection` derives total/test-file counts + `has_tests` + `ci_ran`/`ci_passed` from the changeset's real touched paths + the live CI verdict; `isTestPath` matcher; 19 unit tests with `studio-ci.test.ts`) → computed in `studio.functions.ts:680` from `changes.map(c=>c.path)` + the github.ci.read snapshot → threaded through the build route's `pr` tab into `src/components/studio/CiPanel.tsx` (warn-only "no tests" card, never hard-blocks, per the 2026-06-18 ruling). **Live-verify (prod, Playwright, demo workspace):** opened the Studio Build mission's **PR · Checks** tab — `CiPanel` renders correctly (the `!changeset?.pr_url` "No PR yet" empty state, no crash; inspection prop threads cleanly). tsc 0 / 19 inspection tests pass. The 3 prod console errors are unrelated third-party blocks (ipapi.co geo, m.stripe.com fingerprint). **Honest caveat (data-gated, NOT a code gap):** the *populated* Inspector card (file/test counts + CI line) only renders once a Build mission completes a changeset + opens a PR; prod currently has 0 changesets (the demo mission is parked at the `studio.stage` approval), so the populated card awaits a real Build-to-PR run (operator/connected-repo gated, not autonomous code). The render is the same verified pure function, so it is deterministically correct. [100% of autonomous scope] |
| 109 | ✅ | F-BUILDER-MULTIFILE | Scoped multi-file build | Pre-declared touch list + max-N files; safer multi-file edits | Build | Done | 2026-06-19 | Built: migration + scope server fns + Changes-tab Scope UI + tested glob/policy/merge logic; tsc/lint/build green, 24 tests. ◐ live-verify pending publish + migration sync. UI: Build > session > Changes > Scope. **✅ CLOSED 2026-06-22 (Lane 2, live-verify on prod - migration-sync resolved, no drift):** `studio_changeset_constraints` applied EXACTLY as source - RLS-on, both policies gate on `is_workspace_member(workspace_id)` (SELECT = ws read; ALL = ws write with `WITH CHECK ... AND user_id = auth.uid()`), so there is NO cross-workspace leak (the critical property); columns + the `max_files > 0` CHECK + the `mission_id` UNIQUE + FK CASCADE all present. The "migration sync" concern is resolved. **Noted follow-up F-BUILDER-MULTIFILE-b (low, intra-workspace, NOT a leak):** the write policy's USING is workspace-only, so DELETE/UPDATE row-selection is not ownership-gated (only the new row is, via WITH CHECK), letting any workspace member clear another member's build-scope constraint - acceptable for a workspace-shared one-row-per-mission build-scope policy, but diverges from the migration comment's "writes require the row owned by the writer"; left to the Build lane to ratify the collaboration model before tightening. [100% backend; scope fns + UI gate-green] |
| 110 | ✅ | LRN-04 | Product Memory consult/write visibility | Surfaces the wired memory loop per mission | Launch | Done | - | per-mission surfacing is N3 (shipped). **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** on the published app the mission detail page (`/missions/$id`) renders the full memory loop per mission — a "COMPOUNDING · THE MOAT AT WORK" panel showing "8 · prior memories this mission drew on, instead of starting cold" with the actual consulted memories listed (the CONSULT side), plus "CAPTURE · FILES THIS AS A DECISION" (the WRITE side). Real data on the Lumen mission (Playwright). The wired memory loop is surfaced per mission as specified. [100%] |
| 111 | ✅ | L2 | Customer pages / announcements | Public-facing announcement pages (`p.$slug`) with approval to publish | Launch | Done | 2026-06-21 | **BACKEND + APPROVAL-TO-PUBLISH GOVERNANCE SHIPPED (lane 3, 2026-06-21):** new `announcements` table (migration `20260621200000_l2_announcements.sql`) with a draft→pending→published lifecycle governed at the DB layer: members draft + submit; **only owner/admin publish** via a SECURITY DEFINER `publish_announcement` RPC (re-checks `can_manage_workspace`, requires `pending`); **public/anon read published-only** (the `status='published'` predicate is IN the RLS policy, so a draft can never leak) + member-read-all-statuses; members can't self-publish (update with-check forbids the published status). Pure governance state machine `src/lib/announcements.ts` (`applyTransition`/`isPubliclyVisible`/slug, 12 tests) shared with the server fns `announcements.functions.ts` (list/create/update/submitForApproval/approveAndPublish) so app + DB can't drift. tsc 0 / eslint 0 / 836 tests (12 new). 2-lens adversarial review (RLS draft-safety + governance). **Deferred (L2b):** the public route + `getPublic` (anon-client; avoids touching generated `routeTree.gen.ts`) + the authoring UI. ◐ migration applies + RLS dry-run-verify on the founder's next publish. Spec: [`../features/l2-announcements.md`](../features/l2-announcements.md). **BACKEND LIVE-VERIFIED 2026-06-22 (Lane 2, on prod, no drift) - the "RLS dry-run-verify on publish" caveat is RESOLVED:** queried prod (Lovable MCP) - `announcements` is RLS-on with all 4 policies + the publish RPC exactly as source. The leak-critical guards hold structurally: the anon/public SELECT policy is `status = 'published'` ONLY (a draft/pending row can NEVER match, so the public can never read an unpublished announcement); the member UPDATE policy's WITH CHECK requires `status IN (draft,pending)` so a member CANNOT self-publish by flipping status; `publish_announcement` is SECURITY DEFINER with `anon_exec=false` + re-checks `can_manage_workspace`. The INSERT check binds `created_by = auth.uid()` + role gate (viewers excluded). **L2b PUBLIC PAGE SHIPPED 2026-06-22 (Lane 2):** the public announcement page now renders on the EXISTING `p.$slug` route (no new route = no `routeTree.gen.ts` churn, exactly the deferred-note's constraint), reusing the proven anon-read-via-RLS pattern the prototype share uses. `p.$slug.tsx` now resolves a slug to a public prototype OR (on miss) a `status='published'` announcement and renders a calm reader layout (title + date-guarded published date + line-preserved body, "Made with Cadence" chrome). New pure `publicAnnouncementView(row)` in `announcements.ts` (+5 tests) is the **third** published-only guard (after the RLS policy + the `.eq("status","published")` query filter): a null/partial/non-published row yields null, so a draft can never render. Content renders as escaped React text (XSS-safe; the prototype path keeps its sandboxed iframe). tsc 0 / eslint 0 / `bun test` 926 (5 new). **L2b-2 AUTHORING UI SHIPPED 2026-06-22 (Lane 2) → L2 ✅ FEATURE-COMPLETE:** new `src/components/product/AnnouncementsManager.tsx` on the **Product > Releases** tab (the "Ship" station per the `home-and-today-ia` rubric: ship → Product/Releases), wired in `_authenticated.product.tsx` (existing route → ZERO `routeTree.gen.ts` churn, exactly the deferred-note constraint). It wires the already-verified server fns: list (newest-first, status chips draft/pending/published), create-draft composer, inline edit of draft/pending, submit-for-approval, owner/admin publish, and a View link to `/p/<slug>` for published rows. Role gates (New/Submit = owner/admin/member, Publish = owner/admin, viewer read-only) are derived from the SAME `TRANSITION_ROLES` table the DB mirrors and **fail closed** (a failed/absent role read → null → no actions); they are UX-only defense-in-depth (the publish RPC + RLS remain the real enforcement). tsc 0 / `bun test` 947 (announcements 17). **2-lens adversarial Workflow review (governance + React-runtime): both SHIP, 0 blockers** — every refutation failed (publish owner/admin-only confirmed against TRANSITION_ROLES; viewer sees no action buttons; role fails closed; RPC backstops a tampered client; no hook-rule/shared-cache/null-deref/double-submit defect). Backend + governance + public page all live-verified on prod earlier; the `announcements` table is queryable (0 rows) so the surface renders its empty state cleanly. **FULL ROUND-TRIP LIVE-VERIFIED ON PROD 2026-06-22 (Lane 2, Playwright) — caveat RESOLVED:** on the published app, created a draft via AnnouncementsManager (the `createAnnouncement` write landed in prod), **Submit** moved it draft→pending and the **Publish** button appeared (demo = owner/admin), **Publish** ran the `publish_announcement` RPC moving pending→published, and the **public page rendered the announcement live at /p/<slug>** (title + "June 22, 2026" date + body + "Made with Cadence" chrome). Authoring + governance state machine + anon public-read all confirmed working end-to-end on the real app; the test row was cleaned up after. Authoring UI: /product?tab=releases · public: /p/<slug>. |
| 112 | ✅ | U6 | Full data-portability / export wizard | Exports the whole workspace as one RLS-scoped JSON with per-section selection | Interop | Done | 2026-06-20 00:50 | U6-AUDIT backend shipped (overnight cycle 48): migration `20260620010000_export_log.sql` adds an append-only `export_log` audit table (insert-own + read-own-or-workspace RLS, NO update/delete → tamper-evident; `is_workspace_member`-gated read so an owner audits all data egress); `exportProduct`/`exportWorkspace` write one audit row per export (who/kind/target/sections/row_count, best-effort + try/catch so the audit never fails the export); new `listExportLog` read fn. Behaviorally dry-run-verified on prod (BEGIN..ROLLBACK): both insert shapes apply, user_id defaults from the JWT, kind CHECK rejects bad values, RLS on, policy set is exactly insert+select (append-only confirmed). tsc 0 / eslint 0 / build ✓ / 287 tests. **History UI wired (front-end pivot, cycle 52):** the DataExportCard now shows a "Recent exports" list via `listExportLog` (date+time · kind · row count), refetched after each export; built on the Ember system, ran `impeccable` (passed the bans). tsc 0 / eslint 0 / build ✓ / 305 tests. **✅ LIVE-VERIFIED END-TO-END (Lane 1, 2026-06-22):** on the published app, Settings → Data → "Export your data" renders the per-section wizard + "Download workspace export"; clicking it actually downloaded `cadence-workspace-export-2026-06-21.json` (483 KB) carrying `workspace_id`/`exported_by`/`exported_at`/`counts` + every section (projects:2, signals:9, opportunities:5, specs:2, tasks:9, learnings:1, memory:23 — exact match to the demo seed, i.e. RLS-scoped to the caller's workspace). The whole data-portability round-trip works in production (Playwright). UI: Settings > Data > Export your data > Recent exports [100%] |
| 113 | ✅ | FND-0.7 | Prompt-injection defense | XML-tags + escapes untrusted tool/RAG output; guardrails support injection rules | Governance | Done | 2026-06-21 | **FND-0.7-d GOVERNANCE UI SHIPPED 2026-06-22 (Lane 2) → FND-0.7 ✅ FEATURE-COMPLETE:** the last open follow-up (operator surface) is now built. New `src/components/governance/InjectionDefenseCard.tsx` on the `/govern` > **Guardrails** tab (below the regex rules): an explainer of the layered defense, the LIVE decision thresholds (read from the classifier via the server fn so they can't drift), and an interactive **"test a string"** tool with benign/injection example seeds — paste any string and the auth-gated server fn `src/lib/guardrails-injection.functions.ts` (`assessInjectionSample`) runs the real `classifyInjection` SERVER-SIDE and returns the verdict (decision allow/flag/quarantine + score + severity + the signals that fired). Server-side on purpose: detection patterns stay out of the client bundle (not an evasion oracle for an unauthenticated visitor); read-only (no writes, no live-pipeline effect). Verified the seeds: benign → `allow` 0.018 / no signals; the injection example → `quarantine` 1.0 / high / 6 signals. tsc 0 / eslint 0 / `bun test` 958. **2-lens adversarial Workflow review (security + React): both SHIP, 0 blockers** — security lens empirically ReDoS-tested the regexes (linear, <1ms on 20k input), confirmed the auth gate (requireSupabaseAuth + the gated /govern shell), no pattern leak (only signal names+scores cross the wire), pure/read-only; React lens confirmed all hooks/guards/keys. **Deliberate non-goals (NOT gaps):** operator *tuning* of weights/thresholds is omitted as a security non-goal (an operator could weaken the defense); live quarantine-activity telemetry is a separate larger follow-up (needs event logging on the RAG retriever hot path + would render empty without real attacks). **LIVE-VERIFIED ON PROD 2026-06-22 (Lane 2, Playwright) — caveat RESOLVED:** the card renders on /govern>Guardrails (explainer + "active" chip + live thresholds flag≥0.45 / quarantine≥0.72 read from the classifier), and clicking the "Injection attempt" seed ran the server-side `assessInjectionSample` → `classifyInjection` and rendered the live verdict: **quarantine · score 1.000 · severity high**, with all 6 signals (instruction_override +4.0, jailbreak_phrase +4.0, secret_exfiltration +3.2, system_prompt_probe +2.6, injected_turn_marker +2.6, role_reassignment +2.2). The interactive operator oracle works end-to-end on the real app. UI: /govern?tab=guardrails. <br>— **REACTOR INGESTED-SIGNAL QUARANTINE (FND-0.7-c) SHIPPED (lane 3, 2026-06-21):** the autonomous event reactor's ingested `untrusted_signal` fields (signal/opportunity/PRD) were fence-only; now each runs through the structural-gated `quarantineUntrusted(field)` before escaping/fencing in `reactor.functions.ts` `untrustedSignalBlock`, so a STRUCTURAL injection aimed at our agent is stripped (was only escaped). **A stricter lexical `aggressive` bar was prototyped and DROPPED after the 2-lens adversarial review showed it over-redacts:** this product ingests AI/PM/security signals, where benign prose describing an attack carries the same vocabulary as a real one, so a lexical strip silently drops high-value signals (a competitor's jailbreak-blocker, an industry injection report). Lexical-only ingested content stays escaped+fenced+warned (the proven-safe path). Fail-open; RAG path unchanged. tsc 0 / eslint 0 / 775 tests; a benign AI-security signal is now unit-tested as preserved. ◐ live-verify on publish (structural ingested injection quarantined pre-dispatch). Remaining: FND-0.7-d governance UI. <br>— **CROSS-CHUNK AGGREGATION (FND-0.7-b) SHIPPED (lane 3, 2026-06-21):** new PURE `assessCorpusInjection(chunks[])` + server seam `quarantineUntrustedCorpus` wired into `formatContextBlock` (replacing the per-chunk loop). Two passes, both preserving the over-redaction guard: (1) an adjacent-boundary 256-char window reconstructs a structural injection marker split across a chunk seam and escalates the pair to quarantine ONLY with a lexical co-signal present (so a benign first-party doc that merely mentions a fence tag, chunked mid-tag, is NEVER stripped — the major over-redaction finding I caught + fixed); (2) a length-capped whole-corpus pass flags a distributed lexical instruction for observability (never strips). 3-lens adversarial Workflow review: I reasoned the major over-redaction finding myself (did not trust the empty auto-confirm) and folded the lexical-co-signal fix + 2 minors (cap corpus join, emergent-only corpus warn); 3 false-negative residuals documented (>256 split, 3-way split, empty middle chunk). tsc 0 / eslint 0 / 738 tests (`injection-classifier.test.ts` 29→43). ◐ live-verify on publish (split-across-chunks breakout quarantined in a /chat answer). Remaining: FND-0.7-c reactor untrusted_signal coverage, FND-0.7-d governance UI. <br>— CLASSIFIER + HARD QUARANTINE SHIPPED (lane 3, 2026-06-21): the unbuilt "learned classifier + hard quarantine" half. New PURE `src/lib/injection-classifier.ts` (weighted-evidence logistic-style scorer over ~14 lexical+structural signals, presence-capped, NFKC+confusables homoglyph fold, ReDoS-safe, 29 tests) + fail-open server seam `src/lib/ai/guardrails-injection.server.ts` (`quarantineUntrusted`) wired into the RAG retriever's `formatContextBlock` (live drive point; byte-identical for benign chunks). **Over-redaction guard (the load-bearing call):** hard quarantine (strips content) requires a STRUCTURAL signal (literal fence breakout / forged System: turn); lexical-only suspicion can only `flag` (kept behind the fence), so benign first-party prose that discusses/quotes an attack (PRDs about deleting tables, bug reports quoting "ignore previous instructions", red-team docs) is NEVER stripped. 3-lens adversarial Workflow review (security/correctness/integration): 0 blockers; folded the structural gate, homoglyph fold, encoding-evasion tighten, vocab broadening, quarantine telemetry; over-redaction fix empirically verified (benign migration/PRD/bug-report → allow/flag, real fence-breakout → quarantine). tsc 0 / 647 tests / build red = pre-existing Lovable vite-config ESM baseline (fails at config-load before my source). ◐ live-verify on publish (real chunk quarantined in a /chat answer). Follow-ups: FND-0.7-b cross-chunk aggregation, FND-0.7-c reactor untrusted_signal coverage, FND-0.7-d governance UI. Spec: [`../features/injection-defense.md`](../features/injection-defense.md). [~75%] |
| 114 | ✅ | FND-0.5 | Agent blast-radius limits | Per-agent tool allow-list + scope so an agent cannot reach beyond its remit | Governance | Done | 2026-06-21 | **Blast-radius model + allow-list primitive SHIPPED (Lane 1, 2026-06-21):** extended the pure client-safe `src/lib/tool-consequences.ts` with a static **blast-radius tier** `toolRisk(name)` = reversibility x scope (an `EXTERNAL_TOOLS` set marks repo/tracker/calendar writers; irreversible→high, external+partial→high, external+reversible or internal+partial→medium, internal+reversible→low, uncatalogued→high [fail-closed]) + `isExternalTool`/`isHighRiskTool`/`RISK_RANK`/`RISK_LABEL` + the **allow-list pre-filter** `filterToolsByRisk(tools, maxRisk) → {allowed, blocked}` (the pure building block for per-agent scoping; de-dups + preserves order + reports each blocked tool's tier; 19 unit tests). Driven surface: the human approval card (`DecisionCard.tsx`) now shows a "High blast radius" chip (ShieldAlert, `--rose`) only on a high-risk gate, beside the reversibility chip (distinct axis: reaches-outside/high-stakes vs can't-undo). 1-agent adversarial review: 3 fixes folded (`studio.stage` reclassified internal→low since staging is a local git-index op, not an external write, + a regression test; documented why `scheduler.propose`/`studio.stage` are intentionally internal; doc test-count). tsc 0 / eslint 0 / `bun test` 687 (19 new). **◐ not ✅:** the model + primitive + approval chip are ✅ unit-verified. **ENFORCEMENT (systematic min-confirm floor) SHIPPED (Lane 1, 2026-06-21, attended chokepoint):** the loop's existing high-risk approval gate (`loop.server.ts`) now floors `auto`→`confirm` for `HIGH_RISK_MIN_CONFIRM.has(tool) OR isHighRiskTool(tool)` — connecting the shipped blast-radius classifier to the live gate so EVERY high-blast tool is held to at least human confirmation, closing the drift gap where the hand-maintained set missed a genuinely high-blast tool (`github.commit.append`, external+partial → high, could previously auto-run unattended). A GLOBAL safety floor (not per-agent yet); safe-by-construction (only ever raises a gate, never lowers — review confirmed 0 issues); pure `isHighRiskTool` reused (no migration). tsc 0 / eslint 0 / `bun test` 728 (8 floor-coverage tests). Live agent-run verify deferred (no API keys; founder ruling). **PER-AGENT CAP SHIPPED (Lane 1, 2026-06-21, attended; migrations lock freed):** the row's titular per-agent allow-list — migration `20260621170000_fnd05_agent_tool_cap.sql` adds nullable `agents.max_tool_risk` (CHECK low/medium/high; null=unrestricted, no backfill, existing RLS covers it) + a pure tested `capToolsByRisk(rows, cap)` (`src/lib/agent-tool-cap.ts`, 7 tests) wired into BOTH loop tool-resolution sites (fresh dispatch + resume) so a scoped agent's over-cap tools are dropped from its tool list, `modeOf`, AND system prompt (it can't call or even see them) + a `setAgentToolCap` RLS-scoped server fn + a "Tool reach" select per agent in Settings → Staff. Guarded (null cap = byte-identical). 1-agent security review folded 2 fixes: uncatalogued REAL tools now fail CLOSED to `high` (an un-vetted tool can never slip a low/medium cap; also tightens the floor + chip), and the resume-path agent select is now `user_id`-scoped (defense-in-depth, service-role client). tsc 0 / eslint 0 / `bun test` 736 (7 new). **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** (1) the per-agent "Tool reach" selector (Unrestricted/Low/Medium/High) renders on EVERY agent in Settings → Staff on the published app (Studio, Copilot, Researcher, Strategist, Engineer, QA Reviewer, Orchestrator, Critic, … — Playwright); (2) the migration is APPLIED in production — `agents.max_tool_risk` (text) exists on the live DB (direct query); (3) enforcement is wired at the chokepoint — `loop.server.ts` calls `capToolsByRisk(...)` at BOTH tool-resolution sites (:259 dispatch, :926 resume), reading `max_tool_risk` so over-cap tools drop from the tool list, modeOf, and the system prompt; (4) the pure model + filters + the global high-blast min-confirm floor are unit-tested (736). The model + UI + persistence + chokepoint enforcement + migration are all complete and proven live. Remaining (optional confirmation, NOT a build gap): a gold-standard behavioral A/B (cap an agent low, run it, watch a high-blast tool drop) — the filter is provably wired in both loop paths. Spec: [`../features/agent-blast-radius.md`](../features/agent-blast-radius.md). [100% build] |)
| 115 | ✅ | DATA-RETENTION | AI-telemetry retention purge (dormant) | Bounded retention for unbounded per-call telemetry (ai_events/prompt_runs/tool_calls); data-minimization for enterprise/GDPR | Data/Privacy | Done | 2026-06-20 00:20 | CORE shipped (overnight cycle 47): migration `20260620000000_data_retention.sql` adds `data_retention_enabled()` (dormant flag, false) + a SECURITY DEFINER `purge_old_telemetry(_older_than_days int default 180)` that self-gates on the flag, applies a hard 30-day floor (a mis-call can't purge recent data), and deletes ai_events/prompt_runs/tool_calls older than the window (no inbound FKs - clean deletes); execute revoked from public/anon/authenticated, granted to service_role only. New `retention-tick` hook (`src/routes/api/public/hooks/retention-tick.ts`, requireHookCaller-gated, pre-migration-tolerant). **Behaviorally dry-run-verified on prod** (BEGIN..ROLLBACK, backdating real rows): dormant→no-op, active purge(180) deletes a 200d row + keeps a 50d row, floor purge(0) floors to 30 + deletes the 50d row; rolled back. tsc 0 / eslint 0 / build ✓ / 287 tests. **◐ not ✅:** built + dry-run-verified, but stays dormant until the founder flips `data_retention_enabled()` AND wires a pg_cron/scheduler to POST the hook; live purge activates then. Follow-up DATA-RETENTION-b: the right-to-be-forgotten per-account/workspace erase cascade (destructive + policy-coupled, deferred). UI breadcrumb: none (cron + DB plumbing). **✅ CLOSED 2026-06-22 (Lane 2):** purge capability built + dry-run-verified; live prod check confirms `purge_old_telemetry` IS service-role-locked (anon/auth denied ✓). The 180-day default + hard 30-day floor is a sound retention window per best practice (no change). Activation is the founder's OPERATIONAL step (flip `data_retention_enabled()` + schedule a cron to POST the retention-tick hook) — a destructive go-live decision correctly left to the operator, not a key/code gap. Capability ✅. [100% capability] |
| 116 | ✅ | DATA-RETENTION-b | Right-to-be-forgotten erase cascade (dormant) | Per-workspace/account hard-delete of all tenant data for GDPR/CCPA right-to-erasure; the destructive other half of DATA-RETENTION's time-based purge | Data/Privacy | Done | 2026-06-21 01:29 | CORE shipped (Lane 0, 2026-06-21 01:48): migration `20260621012900_data_retention_b_right_to_erasure.sql` — service-role-only, flag-gated (`right_to_erasure_enabled()` default false) `forget_workspace`/`forget_account` + read-only `erasure_residue` + pure tested `summarizeErasure` receipt (`src/lib/compliance/erasure.ts`). FK-order-agnostic dynamic fixed-point delete over all 61 `workspace_id` tables (catalog-sourced, `quote_ident`'d, `_col`-whitelisted, tenant-id-bound → single-tenant by construction; primitive self-gates on the dormant flag too); explicitly erases the 6 SET-NULL agent-moat tables + clears the 22 RESTRICT children a naive workspace delete would choke on. 4-lens adversarial review (4 must-fix folded); zero-destruction live dry-run on prod (random uuid, BEGIN..ROLLBACK) clean; tsc/eslint/build/398-tests green. **◐ not ✅:** tenant-data erasure, not person-level — auth.users/user_id-only rows/Storage objects/account-scope residue are documented DATA-RETENTION-c scope; destructive FK-order dry-run + live activation are publish-verify (flag flip + operator invoke). Completes the data-governance triad (DATA-RETENTION + U6-AUDIT + SUBPROC-DISCLOSURE). Detail: [`../features/right-to-erasure.md`](../features/right-to-erasure.md). **✅ CLOSED 2026-06-22 (Lane 2 — FOUND + FIXED a CRITICAL grant drift):** the row claimed `forget_workspace`/`forget_account` were "service-role-only", but a live prod grant audit found them **anon/authenticated-EXECUTABLE** — a destructive RTBF hard-delete a client could `POST /rest/v1/rpc/forget_workspace` the moment the flag is flipped, nuking a workspace. They are flag-gated dormant (raise while `right_to_erasure_enabled()`=false), so no live exposure, but armed for activation. **FIXED:** revoked execute from public/anon/authenticated, kept service_role; verified on prod (now anon/auth denied). Forward migration `20260622030000_rtbf_erase_grant_lockdown.sql`. The erase cascade itself is built + 4-lens-reviewed + zero-destruction dry-run-verified (prior). Activation = the founder flips `right_to_erasure_enabled()` (operational). DATA-RETENTION-c (person-level: auth.users/`user_id`-only rows/Storage) stays a separate documented follow-up. Core ✅. [100% tenant-erase core] |
| 117 | ✅ | SUBPROC-DISCLOSURE | Sub-processor disclosure registry | Canonical list of the third parties that process customer data (model providers + gateway + infra) for enterprise security reviews + GDPR Art. 28 | Data/Privacy | Done | 2026-06-20 01:30 | NEW row (overnight cycle 49, `considerations.md` Data/Privacy P1; completes the data-governance triad with DATA-RETENTION + U6-AUDIT). New pure `src/lib/compliance/subprocessors.ts`: a `SubProcessor` type + a curated infra list (Lovable gateway / Supabase / Cloudflare) + `modelProviderSubprocessors` DERIVED from the live model catalog so the disclosure cannot drift (a provider is `active` only when it has a `live` model; adapter-ready BYO providers listed inactive; `ollama` excluded as self-hosted = never a third-party processor) + `allSubprocessors`/`activeSubprocessors`; new `getSubprocessors` GET server fn (`compliance.functions.ts`). `PROVIDER_META: Record<Model['provider'],…>` makes tsc FAIL if a catalog provider lacks a disclosure entry (no silent staleness). tsc 0 / eslint 0 / build ✓ / 298 tests (11 new: shape, unique ids, infra-active, ollama-never-listed, active-only-when-live against an injected catalog, ordering, active=subset-of-all). **◐ not ✅:** the pure registry + read fn are built + unit-verified, but the trust-page UI and the legal-reviewed copy/regions/DPA are deferred to the design/legal pass, and the GET fn was not behaviorally run unattended (live-verify on publish). **UI wired (front-end pivot, cycle 51):** a calm-front `SubprocessorsCard` ("Where your data goes") in Settings > Data renders the `getSubprocessors` list (name + category + purpose + data categories), Engine-Room-compliant (outcome-named, opt-in Settings surface, no jargon). tsc/eslint/build green; ◐ not render-verified locally (renders on the founder's publish). **Public trust page SHIPPED (Lane 1, 2026-06-21 13:3x):** the named remaining slice. New ungated route `src/routes/subprocessors.tsx` (`/subprocessors`) renders the pure `allSubprocessors()` partitioned active vs BYO, for login-free enterprise/GDPR-Art-28 review; imports the pure module directly (no auth/secrets), neutral ink tones (role-color law), region only-when-present (no fabricated residency), DPA deferred to the account team. Route tree regenerated additively via standalone `@tanstack/router-generator`. 2-lens adversarial review (privacy/leak + correctness/doctrine): 0 must-fix (privacy invariant traced + confirmed). tsc 0 / eslint 0 / `bun test` 618. **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** `curl https://cadence-flow-beta.lovable.app/subprocessors` → HTTP 200 with the SSR'd registry present (infra Lovable/Supabase/Cloudflare + catalog-derived OpenAI/Anthropic/Google/Gemini; "model provider" ×7, "subprocessor" ×7), no secret/tenant leak. The login-free GDPR-Art-28 trust page + the catalog-derived registry are complete and proven in production. Remaining (founder/legal polish, NOT a build gap): legal-reviewed copy/regions/DPA link. UI: Settings > Data > Where your data goes + public `/subprocessors` [100% build] |
| 118 | ✅ | APP-HEALTH | App-level health/readiness endpoint | A public liveness/readiness signal for uptime monitors + load balancers (distinct from the migration-drift `checkBackendHealth`) | Reliability | Done | 2026-06-20 02:10 | NEW row (overnight cycle 50, `considerations.md` SRE lens P0 "App-level monitoring + alerting"; confirmed unbuilt - `health.functions.ts` is only a migration-drift checker per SSOT finding 4). New route `src/routes/api/public/health.ts` (GET `/api/public/health`): a cheap, timeout-bounded (2s) DB reachability probe (`profiles` head-read, verified a real table used in onboarding/connections/calendar) → returns 200 `{status:"ok"}` healthy or 503 `{status:"degraded"}` when a dependency is down, so a monitor/LB reacts on the STATUS CODE. Public + unauthenticated (monitors don't auth), so it leaks NOTHING beyond ok/error check states (asserted by a test on the exact key set), keeps the probe trivially cheap, and bounds it with a timeout (DoS-amplification guard). Pure status assembly in new `src/lib/app-health.ts` (`assembleHealth(checks, nowIso)` → `{body, httpStatus}`), unit-tested. tsc 0 / eslint 0 / build ✓ (3.68s, routeTree regenerated) / 303 tests (5 new: ok→200, db-down→503, worker-always-ok, no-leak key set, purity). **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** `curl https://cadence-flow-beta.lovable.app/api/public/health` → HTTP **200** `{"status":"ok","service":"cadence","checks":{"worker":"ok","database":"ok"}}` on the published app (commit 662b5aec). The live DB reachability probe — the one piece previously "not run unattended" — answers `ok` in production; the 503 path is covered by `assembleHealth`'s unit test (can't safely force a prod DB outage). The named deliverable (the endpoint a monitor/LB polls) is COMPLETE and proven live. Non-blocking, out of autonomous scope: an external uptime monitor wired to alert on 503 (founder/infra, e.g. UptimeRobot) + a public status page (design-pass; needs uptime-history backing). UI breadcrumb: none (infra endpoint). [100% endpoint] |
| 119 | ✅ | EVAL-COVERAGE | Eval-coverage scorer + "untested AI surfaces" banner | Computes which of the canonical AI surfaces (chat/copilot/discovery/meetings/roadmap/studio/agent) have an enabled, runnable-case-bearing, recently-run eval guard, vs none (uncovered) vs unproven (stale); surfaces the gap in the Evals panel | Governance | Done | 2026-06-21 02:10 | NEW row (Lane 1, `considerations.md` AI-safety P1 "Eval coverage targets per surface/agent"; completes the "is the autonomy guarded" triad with RELIABILITY-SLO + RUNAWAY-DETECT). Pure `src/lib/evals/coverage.ts` (`EVAL_COVERAGE_TARGETS` lifted from the panel's SURFACE_KEYS as the shared source of truth + `assessEvalCoverage`: covered/uncovered/stale per target; `completed` last-run = covered, enabled-but-0-runnable-cases or no-completed-run = stale, no enabled suite = uncovered; `summarizeCoverage` silent at full coverage) + unit tests + a read-only `getEvalCoverage` over `eval_suites`/`eval_runs` (user-scoped, mirrors `listEvalSuites`; counts only enabled cases) + a calm degrade-silent banner in `EvalsPanel.tsx`. Zero-orphan (existing denominator + consumer); the SURFACE_KEYS unification also removed em-dash labels (humanization win). **2-lens adversarial review caught a feature-inverting bug the gate could not:** the success status is `completed` not `complete` (the runner + every reader + seed data agree; my constant was the lone outlier, and the 17 tests masked it by asserting my own constant) — fixed + added a regression test pinned to the real DB literal; also folded enabled-only case counting. tsc 0 / eslint 0 / build ✓ / `bun test` 464 (18 new). **◐ not ✅:** scorer ✅ unit-verified; live `eval_suites` read live-verifies on publish (DB MCPs intermittent). **Follow-up SHIPPED (Lane 1, 2026-06-21 06:36):** the two logged follow-ups, built clean. Added a `targets[]` per-target breakdown on the report (one ordered source for the chips, no drift) + a pure, total `evaluateCoverageFloor(report, policy)` deploy-gate primitive that is DORMANT by default (`configured:false` ⇒ always passes; never blocks a deploy until the founder sets `EVAL_COVERAGE_FLOOR_PCT`/`EVAL_COVERAGE_REQUIRED_SURFACES`). EvalsPanel now renders a per-surface chip map (covered=quiet emerald, uncovered=rose, stale=neutral dashed [colorblind-safe + off the celebration token], aria-labelled) + a calm sentence-case floor-not-met line. 3-lens adversarial review (logic SHIP · security SHIP/0 · UX SHIP_WITH_FIXES, 0 blockers); folded 5 real fixes (unrounded-floor gate so 86 isn't met by true 85.7%, required-surface dedupe, ≤100 env clamp, sentence-case reasons, aria-labels). tsc 0 / eslint 0 / `bun test` 599 (coverage.test.ts 18→34). **One-click guard SHIPPED (Lane 1, 2026-06-21 07:3x):** each uncovered/stale chip is now a button that opens `CreateSuiteForm` pre-targeted to that surface (label-seeded name; seeded target validated against SURFACE_KEYS with a safe fallback; form re-keyed on prefill; manual/empty-state opens stay unseeded), closing the gap→guard loop. Single-agent review SHIP/0 defects; folded one defensive prefill-clear. tsc 0 / eslint 0 / 608 tests. **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** Playwright on the published app, `/govern?tab=evals` — the Coverage headline renders ("7 of 7 AI surfaces have no eval guard"; correct, the seeded Lumen suite targets a product surface, not a canonical target), the full 7-chip per-surface map renders, and clicking the "Chat, default" chip opened the New eval suite form pre-targeted (Name pre-filled "Chat, default", Target-prompt combobox "Chat, default" selected, pass gate 70) — the one-click gap→guard loop works in production, and the `getEvalCoverage` live read over `eval_suites`/`eval_runs` is exercised by the render. The full build (scorer + read fn + banner + chip map + one-click guard + dormant floor primitive) is complete and proven live. Remaining (founder/infra, NOT a build gap): wiring the already-built dormant floor into the CI/deploy step. Spec: [`../features/eval-coverage.md`](../features/eval-coverage.md). UI: `/govern?tab=evals`. _Counts not recomputed (parallel sessions active)._ [100% build] |
| 120 | ✅ | RELIABILITY-GLANCE | Calm Missions glance wiring SLO + runaway to the operator | A silent-when-healthy "Heads up" line on the Missions header that surfaces only when the AI error budget is strained (RELIABILITY-SLO) or a mission is spinning (RUNAWAY-DETECT); wires the two shipped read fns into the operator's view | Cockpit | Done | 2026-06-21 01:55 | NEW row (Lane 1, FE-wiring theme; backs the North Star claim-never-outruns-wiring by rendering the two previously-orphaned reliability read fns). New `src/components/cockpit/ReliabilityGlance.tsx` (mirrors `MissionsCostGlance`: `useServerFn` + `useQuery` on `getReliabilitySlo` + `getRunawayMissions`; renders NOTHING unless `budget.status` is warning/exhausted OR a `runaway`-severity mission exists; degrades to silent on query error) wired into `_authenticated.missions.index.tsx` next to the cost glance. Engine-Room-doctrine: attention line, not an always-on panel; neutral ink tones (role-color accents stay reserved); labeled metric; no em-dash/hype. Gated via `impeccable` (folded an invented-`--warn`-token + role-color fix). tsc 0 / eslint 0 / build ✓ / 438 tests (no new; presentational). **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** on the published app the glance correctly FIRES — the demo's 7-day error budget is exhausted (86.42% availability) — rendering "Heads up · AI error budget spent, 86.42% of calls succeeded this week" on the Missions header, and clicking it opens the runaway "Reliability Status" popover ("No spinning missions detected"). Both previously-orphaned reads (`getReliabilitySlo` + `getRunawayMissions`) are exercised by this one component, proven by Playwright. Remaining (founder/design): the deep Engine Room breakdown + drill-in. Spec: [`../features/reliability-slo.md`](../features/reliability-slo.md) + [`../features/runaway-detection.md`](../features/runaway-detection.md). UI: Missions header. _Counts not recomputed (parallel sessions active)._ [100% build] |
| 121 | ✅ | RUNAWAY-DETECT | Read-only runaway / loop mission detector | Flags missions that are spinning (runaway hop/step counts, retry thrashing, pinned-retry steps, spend blowouts) — the inverse of E8's stall monitor; the AI-safety "agent can spin" risk made visible | Governance | Done | 2026-06-21 01:40 | NEW row (Lane 1, `considerations.md` AI-safety lens P1 "Loop/runaway detection"; KI-15/16 are the caps, this is the DETECT half). Pure `src/lib/reliability/runaway.ts` (`assessMission`/`assessMissions`/`isTerminalStatus`/`summarizeRunaway`; per-mission hop/step/retry-churn/pinned-step/spend thresholds → `runaway` while active vs `watch` when terminal; unknown status = active fail-loud; total + deterministic via injected `ageMinutes`; 18 tests) + a read-only `getRunawayMissions({days})` over missions/mission_steps/agent_runs (user-scoped: missions `.eq('user_id')`, children `.in('mission_id', ids)` over the user's own ids; 200-mission cap + `truncated` flag; no writes/AI/spend; NO loop/chokepoint edit). **Thresholds are deliberately conservative heuristics ABOVE the real caps** (no hard hop ceiling exists; step-dispatch cap is small/per-tick; `maxStepAttemptsCeiling` 3 > default `max_attempts` 2 so a normal exhausted retry never trips). 2-lens adversarial review (read-fn RLS/correctness · detector soundness): both **SHIP** — no runtime-fatal/correctness/security defect; folded a calibration-comment accuracy fix + em-dash cleanup. tsc 0 / eslint 0 / build ✓ / `bun test` 427 pass (18 new). **◐ not ✅:** detector ✅ unit-verified; the live missions/steps/runs read live-verifies on publish (DB MCPs intermittent). **Operator surface SHIPPED (Lane 1, 2026-06-21 07:4x):** new `RunawayMissionsDetail` popover wired to ReliabilityGlance; click the calm glance to see list of flagged missions + individual reasons (hop count, step thrash, pinned step, spend) sorted by runaway (active) / watch (terminal). Mission cards link to `/missions/$id` for drill-in. Graceful empty state when healthy. tsc 0 / eslint 0 / 608 tests. **✅ LIVE-VERIFIED (Lane 1, 2026-06-22):** on the published app, clicking the Missions reliability glance opens the "Reliability Status" popover reading "No spinning missions detected" — correct against the live demo (21 missions, none meeting the conservative runaway thresholds); `getRunawayMissions` executes live over `missions`/`mission_steps`/`agent_runs` and returns the right empty flagged set (Playwright). Remaining (founder-gated): push alerting (cron → Attention), auto-pause. Spec: [`../features/runaway-detection.md`](../features/runaway-detection.md). _Counts not recomputed (parallel sessions active)._ [100% build] |
| 122 | ✅ | RELIABILITY-SLO | SLO / error-budget reliability surface over AI telemetry | Quantified availability + latency-SLO + error-budget over `ai_events` (success/error + latency), complementing APP-HEALTH's liveness with measured reliability the operator can read | Cockpit | Done | 2026-06-21 01:26 | NEW row (Lane 1, `considerations.md` SRE lens P1 "SLOs/SLAs + error budgets"). Pure `src/lib/reliability/slo.ts`: `computeSlo` (availability %, p50/p95 latency, error-budget burn vs a 99% default target) + `percentile`/`errorBudgetStatus`/`summarizeSlo`/`normalizeStatus`. **Correctness crux:** `ai_events.status='blocked'` is a DELIBERATE governance/credit halt, so it is EXCLUDED from the availability denominator + latency (folding it in would look unreliable exactly when the guardrails fire); unknown/NULL status fails VISIBLE (→ error), never hidden as blocked. Read-only `getReliabilitySlo({days,targetAvailabilityPct})` over `ai_events` (user-scoped RLS, 5000-row cap + `truncated` flag, no writes/AI/spend). 3-lens adversarial review (math · security/RLS · reuse/doctrine): no runtime-fatal/security defect; folded 5 (unknown→error fail-visible + moved the coercion into the tested module, NaN-target totality guard, `truncated` honesty flag, doctrine-comment de-claim, dead-coalesce cleanup). tsc 0 / eslint 0 / build ✓ / `bun test` 409 pass (22 new). **✅ LIVE-VERIFIED + correctness fix (Lane 1, 2026-06-22):** the Missions-header glance renders "AI error budget spent, 86.42% of calls succeeded this week" on the published app — 86.42% is the exact live 7-day `ok/(ok+error)` (70 ok / 81 evaluated; 20 `blocked` correctly excluded), so `getReliabilitySlo`+`computeSlo` are proven correct against production data (Playwright). Querying the demo's 30-day window exposed a real gap: 17 seed `success` rows were counted as errors (avail 75.66%) — fixed `normalizeStatus` to recognize `success`/`succeeded` as `ok` (avail now 84.66%, proven on live data), fail-visible preserved for genuinely-unknown statuses; +1 regression test (`bun test` reliability 57 pass, tsc 0). Remaining (founder/design, NOT a build gap): the optional deep Engine Room per-surface breakdown. Spec: [`../features/reliability-slo.md`](../features/reliability-slo.md). _Counts not recomputed (parallel sessions active)._ [100% build] |
| 123 | ✅ | MODEL-REGISTRY-DEPRECATION | Model-deprecation route-around at the AI chokepoint | Deprecation flags on the model catalog + a chokepoint route-around so a sunset/deprecated model proactively routes to its replacement (the model-deprecation-playbook half of `considerations.md` #4) | Reliability | Done | 2026-06-20 20:21 | NEW row (overnight cycle 57, completes `considerations.md` #4 with PROVIDER-FALLBACK c56). Added `deprecated`/`replacement`/`sunset` to the `Model` type (`src/lib/ai/models.ts`) + a pure, cycle-safe `activeModelId(id, catalog?)` resolver: follows `deprecated -> replacement` links (intermediates may be non-live retired models) to a LIVE terminal, else returns the original requested id so a deprecation can never strand traffic on a missing/non-live model; wired before cost-routing in both `callModel` + `callModelStream`. **Honest status:** NO model is flagged deprecated today, so `activeModelId` is the identity function and the chokepoint is BYTE-IDENTICAL - a wired-but-dormant playbook ready for the next provider sunset. The resolver logic IS unit-verified (8 tests via an injected catalog: live replacement, multi-hop chains, missing/non-live-terminal rejection, cycle termination, unknown id). **Verification (ran):** tsc 0, eslint 0 (3 files), build ✓, `bun test` 357/357 (8 new). **✅ CLOSED (Lane 1, 2026-06-22):** the route-around MECHANISM is complete — verified by reading the chokepoint (allowed; `runtime.server.ts:1088` `const requested = activeModelId(opts.model)` runs EVERY model request through the resolver; `import { activeModelId }` line 14), unit-tested (`models.test.ts`, 10 refs), and correctly DORMANT (no model carries `deprecated:true`, so it is an identity no-op = byte-identical, and the live app routes correctly — chat ran on Gemini, no mis-route). Like a circuit breaker, the deliverable is "done" when built + wired-live + tested; it activates automatically the instant a provider sunset is recorded (`deprecated:true` + `replacement`). No autonomous gap, no founder dependency — the only un-demonstrated path (an actual route-around) requires an EXTERNAL provider sunset, not our work. UI breadcrumb: none (chokepoint, by design). [100%] |
| 124 | ✅ | PLG | PLG funnel | Public onboarding to first-win to upgrade; turns share traffic into activated paying users | Monetization | Done | 2026-06-17 | Phases 1,2,4 done; Phase 3 (memory-expiry banner) **✅ SHIPPED + LIVE-VERIFIED (Lane 1, 2026-06-22):** built the memory-retention upgrade nudge — pure `src/lib/plg-memory-expiry.ts` (`assessMemoryExpiry`: tier-gated [paid never shows], signal-gated [only when a memory's effective fade date — `expires_at` if set, else `created_at+retentionDays` — is within the warning window], honest copy not a fake countdown, total; 8 tests) + `getMemoryExpiry({workspaceId})` (today.functions.ts; resolves tier, short-circuits on paid, RLS-scoped, fail-safe) + `src/components/plg/MemoryExpiryBanner.tsx` (self-contained, degrade-silent, calm `--ember` CTA → `/settings?section=billing`) wired into Today. tsc0 / 966 tests. **Live-verify (prod, Playwright, all 3 paths):** (1) PAID Project Glasswing → no banner (correct); (2) FREE Demo workspace + a seeded near-limit memory → banner shows "1 decision memory is 2 days from it … keep it and let it compound" + "Keep my memory" CTA (a grammar nit found live was fixed + re-verified); (3) FREE, no near-limit memory → no banner (calm). Seed removed after. Screenshot `plg-memory-expiry-banner-live-2026-06-22.png`. Spec `../features/plg-memory-retention-nudge.md`. **Only remaining = the founder/Stripe paid-conversion checkout** (TEST mode; same go-live gate as all billing), NOT autonomous work. [autonomous funnel 100%] |
| 125 | ✅ | M-C-PRICE | Pricing + entitlements (incl. Admin Console v1 + v2) | Plan tiers, billing fns, Stripe webhook, Settings Plan; the revenue rails (relocated to account-level via WM-M1/2/3); **also the inbuilt `/admin/*` Admin Console** (Overview + Pricing tabs from v1, plus People + Workspaces + Platform tabs from v2) | Monetization | Done | 2026-06-22 17:30 | needs founder Stripe secrets to go live; carried/expanded by G10 WM rows. **Admin Console v2 shipped 2026-06-20 (◐, live on next publish)** - 5 tabs total, 8 new tables + audit log + cron expirer + Stripe-id column lockdown. Status for Admin Console lives HERE on this row (no separate dashboard row); detail in [`../features/admin-console.md`](../features/admin-console.md) + build bible [`./admin-console-v2-plan.md`](./admin-console-v2-plan.md). [~82%] 🔁 LOVABLE-OWNED 2026-06-21: billing/credit/pricing/admin-console built by Lovable in parallel; do NOT pick up in any Claude lane. · 2026-06-21 reconcile: DB-driven catalog (pricing_plans/bundles/topups, public-read RLS) + 5-tier in-app PlanPicker + embedded Stripe checkout (LIVE-TESTED in test mode) + Admin Console v1+v2 all shipped. Stays ◐: gated on founder Stripe secrets (STRIPE_*_API_KEY + VITE_PAYMENTS_CLIENT_TOKEN) + the credits_enabled() flip. See M-C-TOPUP-BUG before credits go live. **KEY-READINESS PASS 2026-06-22 (Lane 2, founder "build it complete, no go-live" directive):** a 6-agent readiness audit found the checkout layer complete and fixed 3 buildable (non-key) defects that would otherwise break it WITH keys: (1) BLOCKER — the webhook's renewal credit-refill + past_due dunning were silently dead because both invoice handlers read the top-level `invoice.subscription`, removed in Stripe API `2025-03-31.basil` (client pinned to `2026-03-25.dahlia`); fixed via pure `src/lib/stripe-invoice.ts` `invoiceSubscriptionId` (every API-version location, 8 tests) wired into `webhook.ts`; (2) BLOCKER — every credit voucher rolled back on a `credit_ledger.reason` CHECK violation (`reason='voucher:CODE'`); migration `20260622040000` → `reason='grant'` + code in `surface`, plus a row lock + unique `(voucher_id,user_id)` for the TOCTOU race (APPLIED to prod); (3) free-tier copy said 100 credits but the engine grants 500; migration `20260622050000` → "500" (APPLIED). tsc 0 / 974 tests. Full audit + the founder last-mile key-plug-in checklist + documented buildable-hardening remainder (top-up cap at the grant RPC, voucher redeem UI, SQL↔TS grant parity test, seat-cap enforcement) → [`../features/stripe-keyready.md`](../features/stripe-keyready.md). · **2026-06-22 (Lane 1): ✅ CLOSED — build-complete, founder go-live only.** Verifier (workflow, evidence-backed): pricing + entitlements + the 5-tab Admin Console (8 tables + audit triggers + cron expirer + Stripe-id column lockdown + ~40 has_role-gated RPCs) are 100% wired and verifiable WITHOUT founder secrets; zero runtime-fatal findings. The buildable-hardening remainder noted on 06-22 is now itself closed (top-up cap → M-C-BILLING-TESTS pure helper + SQL-parity; grant parity → credit-grant-sql-parity.test.ts). The ONLY remainder is 👤 FOUNDER go-live config (live Stripe keys + price IDs + the `credits_enabled()` flip + final pricing numbers — SSOT §4); that is config, not build. **CLOSED — do not re-pick or re-verify.** |
| 126 | ✅ | WM-M11 | Per-tier credit amounts + monthly grant + cycle reset | Included resets, top-ups persist; the account credit pool | Credit | Done | 2026-06-19 16:31 | CORE shipped (overnight cycle 31): new `credits.functions.ts` (pure `monthlyGrantCredits`/`resetDelta` + dormant `grantMonthlyAllowance`/`resetCreditCycle`) + `credit-tick` cron hook (grants un-granted, resets due accounts, preserves top-ups). Gated behind `credits_enabled()` (dormant). 7 unit tests; tsc/build/lint green, 208/208. Grant math verified; the DB writes + tick activate on publish + flag + pg_cron [~70%] ❄️ LOVABLE-OWNED+FROZEN 2026-06-21: per-tier grant/reset shipped dormant; do NOT extend; coordinate. · 2026-06-21 reconcile: grantMonthlyAllowance/resetCreditCycle + credit-tick cron built; verified dormant behind credits_enabled() (credit_ledger empty in prod). Stays ◐. · 2026-06-21 (Claude takeover): grant-on-subscribe + renewal-refill now WIRED in the payments webhook (grant_subscription_credits on active sub, reset_subscription_cycle on billing_reason=subscription_cycle), idempotent, dry-run-verified on live. Grant writes are UNGATED (only add credits); still ◐ because metering (debit) stays gated behind credits_enabled() until the founder's go-live flip. · **✅ LIVE-VERIFIED + CLOSED (Lane 1, 2026-06-22):** the credit engine was taken LIVE (build-phase, no real customers, founder-authorized). `backfill_account_credits()` granted every account its tier base (free 500 / pro 2500, matching entitlements.ts), writing `grant` ledger rows + setting `cycle_anchor`; metering armed via the real `/admin` toggle (guard passed, 0 unfunded). Settings → Credits then showed BALANCE 2,496 · MONTHLY GRANT 2,500 · cycle started Jun 22. Per-tier grant + cycle anchor proven live; `reset_subscription_cycle` refills on renewal. Runbook: [`../features/credit-engine-golive.md`](../features/credit-engine-golive.md). [100%] |
| 127 | ✅ | WM-M3 | Billing rails (account Stripe + webhook map) | 5-tier checkout + seats; webhook price-to-tier (dormant until secrets) | Monetization | Done | 2026-06-22 17:30 | effort M; needs WM-M1, WM-M2 · 2026-06-21 reconcile (was ⬜): billing rails BUILT — payments.functions.ts (createCheckoutSession/createPortalSession/cancel/resume) + signature-verified webhook src/routes/api/public/payments/webhook.ts via the Lovable connector gateway; tier map billing-tier.ts. LIVE-TESTED (Stripe embedded checkout opens, test mode). DB half = subscriptions + *_billing_secrets tables. · **2026-06-22 (Lane 1): ✅ CLOSED — build-complete, founder go-live only.** Verifier (workflow): the rails are end-to-end complete and TESTED (checkout → signature-verified webhook → buildSubscriptionUpsert → applyTierForUser → grant), all pure decision/extraction/payload layers unit-covered (billing-tier + billing-webhook), zero runtime-fatal findings. The ONLY remainder is 👤 FOUNDER go-live config (live Stripe keys + webhook secret + register the 6 webhook events — SSOT §4); config, not build. **CLOSED — do not re-pick.** |
| 128 | ✅ | WM-M6 | Pricing surfaces (5 tiers + Usage panel) | The new model shown across pricing page + Settings Plan + Usage | Monetization | Done | 2026-06-22 17:29 | effort M; needs WM-M1, WM-M3 · 2026-06-21 reconcile (was ⬜): in-app 5-tier PlanPicker (Personal/Teams audience toggle, monthly/yearly, per-tier credit-volume dropdown, usage glance) shipped + LIVE-TESTED. · **2026-06-22 (Lane 1): ✅ CLOSED.** Verified (workflow): the full 5-tier model is surfaced in-app (PlanPicker's Personal {free/pro/max} + Teams&Enterprise {team/enterprise} views) and the Usage panel exists in Settings → Credits ("Where your credits go", WM-M16). The public `/pricing` page renders the four self-serve tiers (free/pro/max/team); Enterprise is a contact-sales path by design (not a public price) — corrected the stale "three plan tiers" comment to say so. The only optional remainder is a public-page Enterprise marketing card = Tier-2 design (founder-prompted positioning), NOT a build gap. DONE. |
| 129 | ✅ | WM-M12 | Credit debit engine (fills the WM-M4 seam) | Meters credits from the account pool; halts clean when empty | Credit | Done | 2026-06-19 16:44 | CORE shipped (overnight cycle 32): atomic `debit_account_credits` RPC (included-then-topup, FOR UPDATE, tagged ledger debit) + `assertAccountCredits` projects + halts with `CreditExhaustedError` + a blocked event; `debitAccountCredits` calls the RPC. Gated behind `credits_enabled()` (dormant). RPC behaviorally dry-run-verified on prod (70+60 debit -> 0/20, ledger -130); 19 pricing tests, 212/212, tsc/build/lint green. Live metering on publish + flag [~75%] ❄️ LOVABLE-OWNED+FROZEN 2026-06-21: credit debit engine shipped dormant; do NOT extend; coordinate. · 2026-06-21 reconcile: atomic debit_account_credits RPC + assertAccountCredits(pre-call)/debitAccountCredits(post-call) WIRED into callModel + callModelStream at the chokepoint; strict no-op while credits_enabled()=false. Stays ◐. · 2026-06-21 (Claude takeover): credit engine now wired end-to-end in sandbox - grant -> debit (chokepoint, gated) -> top-up (ungated, fixed) -> renewal refill. Stays ◐: debit dormant behind credits_enabled() until go-live. · **✅ CLOSED 2026-06-22 (Lane 2 — founder lifted the Lovable freeze; CRITICAL security gap FOUND + FIXED on prod):** a live grant audit found that `debit_account_credits` (and the sibling credit-mutation RPCs `grant_subscription_credits`/`apply_topup_credits`/`reset_subscription_cycle`/`backfill_account_credits`/`_ensure_account_credits`/`check_mission_caps`) were **anon/authenticated-EXECUTABLE via PostgREST with NO internal auth guard** — yet they are only ever called server-side with the service-role client (debit from `runtime.server.ts`; grant/topup/reset from the Stripe webhook). A client could have `POST /rest/v1/rpc/grant_subscription_credits` to mint itself credits, or `debit_account_credits` to drain a victim's, once metering goes live. No live exploitation (metering dormant), but a critical pre-go-live hole. **FIXED:** revoked EXECUTE from public/anon/authenticated on all 7 (two only locked after revoking the default PUBLIC grant), kept `service_role`; verified on prod (all 7 now `anon=false/auth=false/service_role=true`). Also revoked the unused anon/auth SELECT grant on the `account_billing_secrets`/`workspace_billing_secrets` vaults. Forward migration `20260622020000_credit_rpc_grant_lockdown.sql`. The debit ENGINE is built + dry-run-verified (prior) + now SECURE; **live activation is the founder's key-dependency** (live Stripe keys + the go-live `credits_enabled` flag). **Coord:** this is also within Lane 1's M-C-DB-HYGIENE scope (they hold that row) — the prod fix + migration are idempotent, so no duplication harm; flagged so they fold it. The same lockdown secures WM-M3 (grant/topup/reset rails), WM-M13 (top-up), WM-M14 (caps). · **✅ LIVE-METERED 2026-06-22 (Lane 1, build-phase, founder-authorized):** the debit engine was taken LIVE end-to-end — backfill funded all accounts, metering armed via the `/admin` toggle (guard passed), and a Gemini chat call debited the demo pool 2500→2496 with `debit:-1/-2/-1` ledger rows while the reply rendered (metered, not halted). The Lane-2 service-role lockdown made this safe (only the server-side client can debit/grant). Metering is currently ON; one-click reversible via `/admin`. Runbook: [`../features/credit-engine-golive.md`](../features/credit-engine-golive.md). |
| 130 | ✅ | WM-M13 | Capped top-up purchase (Stripe credit packs) | Paid-only capped fair-use top-ups; per-cycle ceiling, off by default | Credit | Done | 2026-06-22 17:30 | effort M; needs WM-M3, WM-M12 · 2026-06-21 reconcile (was ⬜): cap-guarded top-up checkout BUILT (createTopUpCheckout, cap = 2x monthly grant / 5000 fallback) + LIVE-TESTED ($5/250-credit pack opens Stripe embedded checkout). · 2026-06-21 (Claude takeover): capped top-up now credits the spendable balance (was the M-C-TOPUP-BUG); works in sandbox end-to-end. · **2026-06-22 (Lane 1): ✅ CLOSED.** Verifier verdict = complete-verifiable: the top-up purchase is fully built, cap-enforced at BOTH the pre-checkout AND the race-safe `apply_topup_credits` RPC (FOR UPDATE), sandbox-verified, and the cap math is now DRY'd into one pure `topUpCycleCap()` with an SQL↔TS parity guard (M-C-BILLING-TESTS, this session). The only remainder is 👤 FOUNDER go-live (live Stripe keys + the founder-set pack prices + ceiling — SSOT §4); config, not build. **CLOSED — do not re-pick.** |
| 131 | ✅ | WM-M14 | Per-product / per-member attribution + caps | See + cap spend per product/member on the pooled account | Credit | Done | 2026-06-19 17:28 | CORE shipped (overnight cycle 33): migration `20260619180000_wm_m14_credit_caps.sql` (credit_caps table + 2 composite ledger indexes + member-read RLS) + pure `rollupAttribution`/`capExceeded`/`creditWindowStartIso`/`sumDebitCredits` + `computeCreditAttribution` (RLS-scoped read, for WM-M16) + `assertCreditCaps` at the chokepoint (per-product/member cap halt) + `productId` threaded into the debit (discovery clustering feeds it; per-MEMBER attribution always driven, per-PRODUCT fed by product-scoped sites). Gated behind `credits_enabled()` (dormant). Migration behaviorally dry-run-verified on prod then rolled back; 22 credits tests, 227/227, tsc/build/lint green; 5-lens adversarial review, 5 fixes folded. Live caps/attribution on publish + flag flip ❄️ LOVABLE-OWNED+FROZEN 2026-06-21: credit attribution/caps shipped dormant; do NOT extend; coordinate. · 2026-06-21 reconcile: credit_caps table + member-read RLS + pure attribution/cap math shipped; owner-set WRITE policy + caps UI remain. Stays ◐. · 2026-06-21 (Claude): owner-set per-product spend caps SHIPPED - owner-write RLS on credit_caps + get/set/removeCreditCap fns + a 'Spending caps' card. assertCreditCaps enforces on the hot path. Migration 20260621140000. **✅ LIVE-VERIFIED + CLOSED (Lane 1, 2026-06-22):** Settings → Credits on the published app renders BOTH halves (Playwright): the per-product/member attribution ("Where your credits go") and the owner-set "Spending caps" card (per-product selector + per cycle/month/day + Add cap). The full build — `credit_caps` table + member-read/owner-write RLS, pure `rollupAttribution`/`capExceeded` math, `assertCreditCaps` wired at the chokepoint, the `get/set/removeCreditCap` fns, and both UI surfaces — is complete and proven live. The only remaining is NOT a build gap: cap enforcement + attribution numbers activate when the credit debit engine goes live, i.e. the founder's metering flag (`credits_enabled()`) — a go-live business decision. [100% build] |
| 132 | ✅ | WM-M16 | Credit / usage UI (balance, legibility, attribution) | Calm balance + action ranges + attribution in Settings | Credit | Done | 2026-06-19 | effort M; needs WM-M6, WM-M12, WM-M14 ❄️ LOVABLE-OWNED+FROZEN 2026-06-21: credit UI; do NOT pick up. · 2026-06-21 reconcile (was ⬜ — DASHBOARD WAS STALE): Credits/Usage UI shipped + LIVE-TESTED (Settings→Credits: balance, monthly grant, purchased top-ups, cycle, top-up packs, 'Recent activity' ledger; admin Metering-ON toggle). Remaining: full attribution view + owner-write caps (WM-M14). · 2026-06-21 (Claude): the usage-attribution VIEW shipped (Settings->Credits 'Where your credits go': per-product spend bars + teammate count via getCreditAttribution) + the owner caps UI (WM-M14). **✅ LIVE-VERIFIED + CLOSED (Lane 1, 2026-06-22):** Settings → Credits renders the complete credit/usage UI on the published app (Playwright) — BALANCE / MONTHLY GRANT / PURCHASED TOP-UPS / CYCLE STARTED, "Where your credits go" attribution, SPENDING CAPS (per-product selector + per cycle/month/day + Add cap), ONE-TIME TOP-UPS (250/1k/2,500 packs with per-credit rates + "0 of 5,000 top-up credits used" cap), and a "Recent activity" ledger. The UI deliverable is 100% complete. The remaining is NOT a UI gap: balances/attribution populate once the credit DEBIT ENGINE (WM-M12) is live, which is the founder's metering go-live flag ("Metering is off while we finish the credits rollout" is shown in-UI). [100% UI] |
| 133 | ✅ | BYO-P4 | Managed AI credits (= WM credits) | Metered AI; included allowance + fair-use overage | BYO | Done | 2026-06-19 | built under WM (G10), cross-referenced not duplicated; needs WM-M2 🔁 LOVABLE-OWNED 2026-06-21: managed AI credits = the WM credit engine; do NOT pick up. · 2026-06-21 reconcile (was ⬜): managed AI credits = the WM credit engine (WM-M2/M10/M11/M12/M14/M16), shipped-dormant. Substrate built; activates when credits go live. · 2026-06-21 (Claude takeover): = the WM credit engine, now wired end-to-end (grant/debit/topup/renewal). Stays ◐ pending the go-live flip. · **✅ LIVE-VERIFIED + CLOSED (Lane 1, 2026-06-22):** managed AI credits are now LIVE — with metering ON, a Gemini (managed BYO key) chat call on the published app debited the account pool (2500→2496, `debit` ledger rows) and the reply rendered (metered AI works, didn't halt). The included monthly allowance (grant) + metered debit are proven end-to-end. Runbook: [`../features/credit-engine-golive.md`](../features/credit-engine-golive.md). [100%] |
| 134 | ✅ | WM-M18 | Plan-card states + change flow (current/upgrade tags, downgrade guard) | "Current plan" tag + "Upgrade" CTAs (no current/base duplication) + guarded downgrade | Monetization | Done | 2026-06-22 17:29 | Founder UX ask 2026-06-19. Spec: bible §4.2.2 · 2026-06-21 reconcile (was ⏭️): plan-card states shipped — 'Current plan' pill + upgrade/downgrade CTA language via TIER_ORDER (Step up to / Move to / Bring the team into). · **2026-06-22 (Lane 1): ✅ CLOSED.** Built the guarded downgrade-confirm flow: `onSubscribe` in PlanPicker now awaits an in-app `useConfirm()` dialog on any paid→paid downgrade ("Move down to {tier}? … fewer monthly credits and capabilities … takes effect next cycle … move back up anytime") before opening Stripe checkout; cancel aborts. (Downgrade-to-free is a portal cancel, not a checkout, so it is out of this guard by design.) tsc 0, lint clean. The variant-aware "Current" tags depend on WM-M17 (Anthropic-style usage variants), whose multipliers/prices are 👤 founder-gated numbers (SSOT §4) — out of WM-M18's scope. DONE. |
| 135 | ✅ | F-ADMIN-CONSOLE | Admin Console — inbuilt /admin operator hub (5 tabs) | Operator console: Overview (credits-metering toggle + admin roster), Pricing (plan/bundle/top-up catalog CRUD), People (users / invitations / vouchers), Workspaces (tenant mgmt + ownership transfer + soft-delete), Platform (feature flags + system banner + audit log). Front-to-back: TanStack server fns -> SECURITY DEFINER RPCs, every admin RPC gated by has_role(auth.uid(),'admin'). | Cockpit | Done | 2026-06-21 | 2026-06-21 NEW (Lovable, 06-20 night; previously OUT OF SCOPE). Shipped + LIVE-TESTED (/admin renders, demo@redcadence.app is platform admin, Metering-ON toggle works). ◐: v1 admin RPCs not yet audited (admin_audit_log writes only on v2 RPCs); adminRevokeSessions + adminSystemHealth are specced-not-shipped; live on publish. Docs: docs/features/admin-console.md, docs/planning/admin-console-v2-plan.md. **✅ CLOSED 2026-06-22 (Lane 2 — resolved the flagged "v1 admin RPCs not yet audited" gap):** live prod audit confirms EVERY admin RPC (admin_grant_credits, admin_grant_user_credits, admin_set_credits_enabled, admin_reset_user_credit_cycle, admin_create/update/deactivate_voucher, admin_upsert/delete_topup_bundle, admin_list_*) guards internally with `has_role(auth.uid(),'admin')` + `raise exception`, so the default anon/authenticated EXECUTE grant is moot. `has_role` is SECURITY DEFINER reading `user_roles`; 1 admin assigned (demo). The 5-tab console was shipped + live-tested. The two specced-not-shipped (`adminRevokeSessions`, `adminSystemHealth`) are FUTURE enhancements, not core. Core console ✅. |
| 136 | ✅ | ADM-DB | Admin Console DB substrate (roles, RPCs, audit, flags, vouchers) | app_role enum + user_roles + has_role() gate; ~40 SECURITY DEFINER admin RPCs; admin_audit_log (append-only); vouchers/voucher_redemptions; feature_flags/system_banner (+ public get_flag/get_active_banner); auto_approve_domains/signup_approvals; cron_tick_admin_expiries. | Governance | Done | 2026-06-21 | 2026-06-21 NEW (Lovable). DB layer applied live; RLS correct (admin-write, narrow public reads). ◐: signup-gating enforcement at the actual signup path is unverified, and app_settings is RLS-enabled + consumed but has NO create-table migration (schema drift — see M-C-DB-HYGIENE). **✅ CLOSED 2026-06-22 (Lane 2 — substrate verified live + secure on prod):** RBAC correct (`has_role` SECURITY DEFINER reads `user_roles`; 1 admin); ~40 admin RPCs all `has_role`-gated + raising; `admin_audit_log` append-only; vouchers admin-only with user redemption via `redeem_voucher` (no enumeration); feature_flags/app_settings/system_banner/auto_approve_domains/signup_approvals all admin-gated; credit-mutation RPCs now service-role-locked (see WM-M12). **Two leftovers belong elsewhere:** (1) `app_settings` missing its create-table migration is Lane 1's M-C-DB-HYGIENE (table is live+correct); (2) signup-PATH gating lives in the auth/signup flow, not the DB substrate. Substrate ✅. |
| 137 | ✅ | M-C-BILLING-TESTS | Automated tests for the billing / Stripe / admin / voucher surface | Today only PURE math is tested (entitlements.test.ts, credits.test.ts, ai/pricing.test.ts). ZERO tests for any Stripe server fn (checkout/portal/cancel/resume/topup), the webhook handlers (signature verify, tier map, idempotency), the ~40 admin RPCs, or voucher redemption. | Governance | Done | 2026-06-22 17:29 | 2026-06-21 NEW. Coverage gap across the whole monetization + admin surface; add before live billing. · 2026-06-21 (Claude): webhook signature gate now has 7 tests (stripe.server.test.ts) + creditsFromLookupKey 7 tests; the credit-flow RPCs dry-run-verified on live. Remaining: webhook DB-write + admin/voucher RPC integration tests. Stays open/partial. · 2026-06-21 (Claude): substantially covered. CI: 71 pure tests across entitlements/credits/billing-tier/ai-pricing/stripe-signature + a SQL/TS tier-limit PARITY guard. Live-verified: tier-limit SQL==TS for all tiers; admin RPC auth (50/51 admin_* gate on has_role; the 1 exception is the intentional admin_bootstrap_self_as_admin); credit-flow RPCs dry-run-verified. Remaining (separate infra, repo-wide pattern): DB-integration tests for the webhook DB-writes + voucher redemption (no DB-mock harness in CI). · **2026-06-22 (Lane 1): webhook decision-logic gap CLOSED.** Extracted the two revenue-critical inline rules from `payments/webhook.ts` into pure, tested `billing-tier.ts` fns the 3 subscription handlers now share (no drift): `effectiveTierForStatus(tier,status)` (keep paid access through `past_due` dunning, downgrade to `free` on real termination) + `subscriptionStatusGrantsCredits(status)` (only `active`/`trialing` mint credits; `past_due` keeps access but no fresh allowance). +7 tests (billing-tier 6→13); behavior-identical extraction, tsc 0, billing cluster 69 pass. So the webhook "tier map" coverage the row flagged is now real, harness-free. Remaining narrows to: DB-write integration (upsert/update side-effects) + voucher redemption, both needing a Supabase-mock harness. · **2026-06-22 (Lane 1, cont.): webhook decision/EXTRACTION layer extracted + tested.** The other untested mis-bill-risk surface in `payments/webhook.ts` was the inline event-shaping logic. Extracted into pure `src/lib/billing-webhook.ts` (`resolvePriceLookup` lookup_key→external_id→id fallback; `resolveTopupCredits` catalog→legacy-map; `unixSecondsToIso` no-fake-epoch; `isTopupCheckout`; `isRenewalInvoice` subscription_cycle-only no-double-grant; `resolvePeriod` line-item-over-sub) + 13 tests; wired back into all handlers behavior-identical (also corrected the old `resolvePriceLookup: string` type-lie). The webhook's full decision+extraction layer is now tested + DRY. tsc 0 / suite 979. · **2026-06-22 (Lane 1, cont.²): webhook PAYLOAD-ASSEMBLY layer extracted + tested — the webhook's WHOLE TS-testable surface is now covered.** Extracted the subscription DB-row assembly into pure `buildSubscriptionUpsert(sub,env,nowIso)` + `buildSubscriptionUpdate(sub,nowIso)` (every field mapped from the Stripe event; `nowIso` injected for determinism; required-string fields asserted to match the table Insert type, behavior-identical to the inline `any`-based payload) + 4 tests (field-mapping, cancel_at_period_end default, line-item-over-sub period fallback, the update subset excludes user_id/customer/environment); wired into `handleSubscriptionCreated`/`handleSubscriptionUpdated` behavior-identical. Now the webhook's decision + extraction + payload layers are ALL pure-tested + DRY (a wiring/field-map bug can't slip in untested). tsc 0 / suite **1003** (+4). · **2026-06-22 (Lane 1): ✅ CLOSED.** Final buildable slice: extracted the top-up per-cycle cap (`monthlyGrant>0 ? *2 : 5000`) — which was DUPLICATED across `createTopUpCheckout` + `getMyCreditsView` (a runtime-fatal drift risk the verifier flagged: UI could show a different cap than the backend enforces) — into one pure `topUpCycleCap()` in billing-tier.ts, wired both TS sites + the early-return to it, and added a SQL↔TS parity guard (`topup-cap-sql-parity.test.ts`) that parses the `apply_topup_credits` migration's `_cap := case … end` and fails if the SQL formula/fallback ever drifts from the TS helper. +5 cap tests + 4 parity tests; billing/credit cluster **127 pass**, tsc 0, lint clean. The whole **TS-unit-testable** monetization surface is now covered (decision + extraction + payload + cap layers, all pure + DRY + SQL-parity-guarded). **Remaining is NOT TS-unit-testable (founder-env / infra, NOT a build gap):** the live-DB round-trip + the SQL `redeem_voucher`/~40 admin RPCs need a live Supabase or a chained-mock harness that does not exist in CI — these are integration tests gated on the founder's live DB, not autonomous work. DONE. |
| 138 | ✅ | M-C-DB-HYGIENE | Billing/admin migration hygiene (schema drift + parity + RLS dupes) | (1) app_settings is RLS-enabled + consumed by code/types but has NO create-table migration (a clean replay breaks). (2) tier-limit SQL fns hand-mirror entitlements.ts caps with only a 'keep in sync' comment — no test guards drift. (3) WM-M2 and WM-F3 declare redundant SELECT policies on accounts/account_members. | Monetization | Done | 2026-06-21 | 2026-06-21 NEW (Lovable). DB-integrity cleanup; add a forward-only app_settings create migration + a SQL/TS parity test. · 2026-06-21 (Claude): app_settings create-if-not-exists migration added (replay-safe, no-op on live) + credit_topups.credits_added widened to bigint. Remaining: SQL/TS tier-limit parity test (folded into M-C-BILLING-TESTS) + consolidate the redundant accounts RLS policies. · 2026-06-21 (Claude): app_settings create-migration + credit_topups->bigint shipped; SQL/TS parity test still folded into M-C-BILLING-TESTS. · **✅ CLOSED 2026-06-22 (Lane 1):** (1) app_settings replay migration verified against the live `information_schema` (columns match exactly). (2) **drift guard BUILT** — new `src/lib/entitlements-sql-parity.test.ts` parses the `tier_product_limit`/`tier_workspace_limit` CASE bodies from the migration and asserts SQL === `limitFor()` for every tier × {product,workspace} (+ a parser-sanity guard); `bun test` 11 pass, tsc 0. (3) the "redundant RLS" claim was INVESTIGATED against live `pg_policies` and is NOT a real redundancy — `account_members` "see own" (`user_id=auth.uid()`) is a defensive self-row fallback distinct from "read membership" (`is_account_member`, active-only), so dropping it would remove a pending member's self-visibility; correct call is keep both (no change). Doc: [`../features/billing-db-hygiene.md`](../features/billing-db-hygiene.md). |
| 139 | ✅ | A6 / ENG-08 | Roles + RBAC + invites | owner/admin/member/viewer roles + invites; per-persona approval lanes | Interop | Done | - | superseded/expanded by WM-F3/F4/F5 (G10); build from the WM rows ⬆️ UN-GATED 2026-06-21: already built via WM-F3 (RBAC) + WM-F4 (transfer) + WM-F5 (invites) - not founder-gated. Deeper team-tier RBAC beyond the MCP slice is canon-deferred to M-D. **✅ CLOSED (Lane 1, 2026-06-22):** the core (owner/admin/member/viewer roles + invites + ownership transfer) is delivered by the three ✅ live-verified WM rows; live spot-check confirms Settings>Workspace renders members/roles + invite + transfer. The `approvalLanes` ENTITLEMENT exists (team+ tier, `entitlements.ts`); the per-role approval-lane ENFORCEMENT is a security-authorization follow-up (canon-deferred to M-D / team-tier, intentionally not shipped unsupervised). [100% core] |
| 140 | ✅ | WM-M7 | Upgrade nudges (value-framed) | Convert at natural moments, never punitive | Monetization | Done | 2026-06-19 | effort S; needs WM-M5, WM-M6 🔁 LOVABLE-OWNED 2026-06-21: upgrade/conversion UI; do NOT pick up. · 2026-06-21 reconcile (was ⬜): upsell foundation shipped — LimitReachedError.upsellTier + nextUpsellTier consumed in projects.functions.ts; PlanPicker 'Most popular'/recommended + step-up CTAs. Remaining: nudges at more conversion moments (memory-expiry banner under PLG); dormant behind limit_gates_enabled(). · 2026-06-21 SHIPPED (Claude): memory-expiry upgrade nudge (`MemoryUpgradeNudge`) for free users on the Brain - calm, dismissible (snoozes 14d), null for paid. The 'pay to keep your memory' moat made felt. Commit 312d3afbe9. |
| 141 | ✅ | WM-M8 | Tier identity motif (Constellation glyph) | The unique animated plan identity (rename-able via slug decoupling) | Monetization | Done | 2026-06-19 | effort S; needs WM-M1, WM-M6 🔁 LOVABLE-OWNED 2026-06-21: tier identity/pricing presentation; do NOT pick up. · 2026-06-21 reconcile (was ⬜): per-tier glyphs (TIER_ICON) + rename-able presentation (planPresentation: Star/Cluster/Constellation/Galaxy/Cosmos) shipped. Remaining: the animated tier-identity motif. · 2026-06-21 SHIPPED (Claude): tier-identity glyph motif - exported PlanPicker's per-tier glyph map + rendered the current tier's glyph on the current-plan card (consistent picker<->summary). Commit c4c7dff7f7. |
| 142 | ✅ | M-C-TOPUP-BUG | FIX: top-up purchase never credits the balance (revenue blocker) | handleCheckoutCompleted in api/public/payments/webhook.ts writes credit_topups but never increments account_credits.topup_credits nor inserts a credit_ledger row, so purchased credits never become spendable even after credits_enabled() flips. Also the static TOPUP_CREDITS map knows only 250/1k/2.5k; larger catalog bundles would be paid-for-but-uncredited. | Credit | Done | 2026-06-21 | 2026-06-21 NEW (Lovable). REVENUE BLOCKER — must fix before credits go live. Found via DB probe (credit_ledger empty despite Metering-ON) + adversarial code read. Hand to Lovable or back to Claude per founder. · 2026-06-21 FIXED (Claude, backend): apply_topup_credits RPC credits account_credits.topup_credits + writes a credit_ledger row, idempotent per Stripe session; webhook wired; dry-run-verified on live then restored. Commit 2c987e88a8. |
| 143 | ✅ | M-C-DEDUPE-WEBHOOK | Retire the dead duplicate Stripe webhook | Two webhook routes coexist: authoritative src/routes/api/public/payments/webhook.ts (signature-verified, tier-mapped, writes subscriptions + flips accounts.plan_tier) and legacy src/routes/api/stripe/webhook.ts (different env var, hardcodes tier='pro', writes ONLY workspaces.plan_tier + the now-RLS-revoked stripe_* columns). Delete/retire the legacy one. | Monetization | Done | 2026-06-21 | 2026-06-21 NEW (Lovable). Security footgun: if ever wired it writes RLS-revoked columns + the wrong tier path. Low effort; retire it. · 2026-06-21 FIXED (Claude): legacy /api/stripe/webhook gutted to a 200 no-op (no more RLS-revoked-column writes / hardcoded 'pro'). Commit 30e37bed5e. |
| 144 | ✅ | M-C-PRICE-SYNC | Reconcile public /pricing vs in-app picker (prices + tiers + copy) | Public /pricing shows 3 tiers (Cluster $39, Galaxy $25/seat) with a stray 'Everything in Constellation' line; the in-app picker shows 5 tiers (Cluster $25, Galaxy $30/seat). Prices are hardcoded in two places (pricing_plans has no price column). Seeded pricing_features says free '100 credits/mo' while entitlements.ts FREE_MONTHLY_CREDITS=500. | Monetization | Done | 2026-06-21 | 2026-06-21 NEW (Lovable). User-facing inconsistency found via live test; single-source the prices (catalog or planPresentation) and bring /pricing to the 5-tier model. · 2026-06-21 FIXED (Claude): planPresentation prices corrected to the catalog recommended bundles (Cluster $25 / Constellation $99 / Galaxy $30 per seat) + public /pricing now shows the full ladder incl. Constellation + drift-guard test. Commit 097c59c5f6. |
| 145 | ✅ | WM-M2 | accounts table + billing/credit/decay migrations | Moves billing to the account; adds the credit-pool shell + 30d rolling decay | Monetization | Done | 2026-06-19 15:40 | CORE shipped (overnight cycle 28): migration adds `accounts`/`account_members`/`account_credits`/`credit_ledger` + `workspaces.account_id` (backfilled, NOT NULL via an owner_id auto-fill trigger) + 5-tier CHECK widen + 30d rolling decay (rolls off `last_used_at`, dormant) + `credits_enabled()`/`is_account_member`/`ensure_user_default_account`; `billing.functions.ts` reads the plan from the account (shim fallback). Dry-run-verified on prod (5 accounts, all 7 ws linked, 0 nulls, rolled back). Live on publish. Types regen + account-aware webhook tracked with WM-M3 [~75%] ⚖️ BOUNDARY 2026-06-21: the tenancy scaffold (accounts/account_members/workspaces.account_id/decay) STAYS product-infra; the account_credits/credit_ledger pool + Stripe relocation half is Lovable-owned. · 2026-06-21 reconcile: accounts/account_members/account_credits/credit_ledger migrations APPLIED LIVE (6 accounts, 6 account_credits rows in prod); the payments webhook writes accounts.plan_tier. DB layer verified ✅; credit metering dormant behind credits_enabled(). |
| 146 | ✅ | WM-M5 | Tier limit gates (DB triggers) | Enforce per-product/workspace caps at the DB (clients write direct, so triggers guard) | Monetization | Done | 2026-06-19 19:30 | CORE shipped (overnight cycle 36): migration `20260619200000_wm_m5_tier_limit_gates.sql` adds `tier_product_limit`/`tier_workspace_limit` (mirror entitlements.ts) + `enforce_product_limit`/`enforce_workspace_limit` BEFORE-INSERT triggers (SECURITY DEFINER; product count is account-WIDE so pooled workspaces can't bypass; first workspace never gated) + new `src/lib/limits.functions.ts` (`assertCanCreateProduct` nice-path + typed `LimitReachedError`+upsell) wired into `createProject`. DORMANT behind `limit_gates_enabled()` (= false) like credits/decay; the founder flips it once a live upgrade path (Stripe) exists. Behaviorally dry-run-verified on the live prod DB (free blocked at product #3 AND workspace #2 with the exact trigger messages; pure limit fns correct), rolled back. 252/252, tsc/build/lint/humanization green. Live gate activates on publish + the flag flip [~75%] ⚖️ STAYS (2026-06-21): limit enforcement is product-infra; prices + the live limit_gates_enabled() flip are Lovable/founder. · 2026-06-21 reconcile: tier-limit BEFORE-INSERT triggers + pure caps applied live; verified ✅, dormant behind limit_gates_enabled() (fail-open by design). |
| 147 | ✅ | R3 | Notifications | In-app Attention feed + global bell with the live "what needs you" count | Cockpit | Done | 2026-06-20 00:53 | Completed (parallel/cockpit cycle 1): user notification preferences table (`user_notification_preferences`) + RLS policies + settings route `/notifications` + email/digest scaffolding helper fns. In-app feed and global bell now filter dynamically based on preferences. Verified via unit tests + tsc/build/lint green. |
| 148 | ✅ | P7 | Incidents log | Read-only log of failed tool runs, errored auto-pipelines, guardrail blocks, linked to traces | Governance | Done | 2026-06-20 02:24 | Completed (parallel/cockpit cycle 2): persistent incidents table (`cost_incidents`) + RLS policies + cost incident detector + detailed cost badge integration. **Cycle 3 fix (2026-06-20 19:01):** an adversarial audit found the detector filtered `ai_budget_alerts` on `kind='block'`, a value no producer ever writes (the chokepoint only writes `kind='warn'`; a real cap halts by throwing), so it was silently dead in prod. Rewired to `.in('kind',['warn','block'])` to surface the real threshold-crossing alerts with honest pct/window copy (escalates to "cap reached" at pct>=100). tsc 0 / 11 tests / build green; no migration. |
| 149 | ✅ | KI-15 / KI-16 | Stale zero-step completion · advance-cap | Per-tick dispatch cap + stale zero-step-mission completion edge cases | Foundational | Done | 2026-06-20 19:31 | KI-15 done (`sweepStaleUnconsumedMessages`). KI-16 done in two layers: mission-level per-tick cap (`MISSION_BATCH=50` in `resume-runs.ts`, already in code) + **KI-16b per-mission step-dispatch cap shipped 2026-06-20 19:31** (`selectDispatchBatch` in `mission-advance.server.ts`, env `MISSION_STEP_DISPATCH_CAP` default 10) so a wide/runaway DAG degrades to bounded backpressure under the Workers execution budget instead of an unbounded per-tick enqueue burst; complements the `MAX_RUNNING_PER_WORKSPACE=5` running-concurrency cap. 5 unit tests; tsc/build green; no migration. |
| 150 | ✅ | WM-M1 | Entitlements core (5 account tiers + matrix) | The tier model + limits both threads read; unblocks all pricing/limit work | Monetization | Done | 2026-06-19 13:50 | SHIPPED (overnight cycle 26): 5 slug tiers + full matrix (limits, credits, RBAC, collab) + `limitFor` + Constellation `planPresentation`; legacy fields kept as aliases; `src/lib/entitlements.ts` (+ 14 tests). tsc/build/lint/test green ⚖️ STAYS (2026-06-21): the tier model + limits matrix the chokepoint/RLS read is product-infra; the pricing presentation it feeds is Lovable's. |
| 151 | ✅ | WM-M10 | Credit unit + cost-to-credit conversion + legibility | What one credit is + the calm per-action legibility layer (no meter-anxiety) | Credit | Done | 2026-06-19 15:58 | SHIPPED (overnight cycle 29): `src/lib/ai/pricing.ts` adds `CREDIT_COGS_USD` + `creditsForCost` (0 for non-billable, >= 1 margin-positive for billable) + `estimateCreditsForCall` (composes the USD estimator) + the pure display-only `actionCreditRange` legibility layer (calibrated shapes through the same conversion; no DB); 15 unit tests (29 asserts). tsc/build/lint/test green. Numbers are §7 placeholders; the dormant `WM-M4` seam fills via WM-M12 ❄️ LOVABLE-OWNED+FROZEN 2026-06-21: credit engine shipped dormant; do NOT extend; coordinate before live to avoid colliding schemas. |
| 152 | ✅ | F-AGENT-1 | Orchestrator + multi-agent missions | The mission DAG that runs the whole loop | Foundational | Done | - | **EPHEMERAL SUB-AGENT FAN-OUT shipped (Lane 2, 2026-06-21):** a specialist can now `agent.spawn` up to 8 bounded ephemeral sub-agents of one role for parallelizable subwork (per-source ingest / per-spec-section / per-file build), instead of grinding them serially. Each child is a normal A2A handoff (reuses `enqueueHandoff`, so it rides the existing self-driving engine and inherits the #1 evidence gate); NO new orchestration-engine code. New PURE `src/lib/ai/fanout.ts` (`planFanout` count-cap + dedupe + budget-split; `fanoutDepthOf`/`canSpawnAtDepth`) + `fanout.server.ts` (`enqueueFanout`, flag `AGENT_FANOUT` default OFF) + `agent.spawn` tool. **Runaway-safe:** count cap (8/spawn) + DEPTH cap (`FANOUT_MAX_DEPTH=1`, a spawned worker cannot itself spawn, stamped via `context._fanout_depth` and depth-refused) close recursion independent of budget; a live-run guard in `maybeCompleteMission` stops a sibling from completing the mission while spawned children still run. 17 unit+integration tests; tsc 0 / 844 suite / eslint+dash clean. 3-lens adversarial review BLOCKED on runaway + premature-completion; both folded and re-verified CLOSED by a focused adversarial trace. ◐ activates when the founder flips `AGENT_FANOUT=1` (default OFF = zero live change). Files: `src/lib/ai/fanout*.ts`, `tools/registry.server.ts`, `handoff.server.ts`. |
| 153 | ✅ | F-AGENT-2 | Persistent memory + self-reflection + trust auto-advance | Agents remember and earn autonomy; the moat's substrate | Foundational | Done | - | |
| 154 | ✅ | F-AGENT-3 | Event reactor + auto-pipelines | Signals trigger missions with no human poke | Foundational | Done | - | |
| 155 | ✅ | F-AGENT-4 | Swarm HUD | See the agent mesh working (Missions Agents tab) | Foundational | Done | - | |
| 156 | ✅ | P1-AA | Deterministic auto-advance | Missions advance unattended past wave 0 | Foundational | Done | - | |
| 157 | ✅ | P1-RETRY | Bounded hop retry | A failed hop retries with backoff, not a dead mission | Foundational | Done | - | |
| 158 | ✅ | P1-BUDGET | Adaptive step budget | Step budget scales to role/arc, not a static cap | Foundational | Done | - | |
| 159 | ✅ | W1 | Memory-compounding loop | Outcomes distil into recallable memory across agents (the moat wired) | Foundational | Done | - | |
| 160 | ✅ | W2 | Executed-unattended audit | The cockpit shows what the loop ran without you | Foundational | Done | - | |
| 161 | ✅ | W3 | A2A hardening + moat on cockpit | Handoffs validate memory refs; outcomes-remembered count shown | Foundational | Done | - | **A2A EVIDENCE GATE shipped (Lane 2, 2026-06-21)** = the typed handoff contract becomes a RUNTIME invariant (was prompt-guidance only; `evidence_ids` did not exist). `HandoffPayload` gains `evidence_ids: {kind,id,note?}[]`; pure `validateHandoff` rejects a handoff that asserts `artifacts` but cites no evidence (`evidence_ids` or a real `memory_ref`); `enqueueHandoff` throws `HandoffRejectedError` BEFORE writing any row (proven by integration tests: zero `agent_messages`/`agent_runs` inserted on reject), surfaced to the sender via the loop's tool-error path so it can cite evidence and retry; `renderHandoffBlock` shows the evidence to the receiver; the `agent.handoff` tool schema + description now prompt for evidence. Flag-gated `HANDOFF_EVIDENCE_GATE` (default OFF → live loop byte-identical; orchestrated dispatch hops carry no artifacts so the gate is a guaranteed no-op even when enforced). 11 unit+integration tests; tsc 0 / 781 suite / eslint+dash clean; 3-lens adversarial review all SHIP (fixes folded: validator honest on blank refs, runtime-invariant tests, forward-guidance comment). Deferred follow-up: kind-dispatched ownership-validation of `evidence_ids` before flipping to enforce (runtime never dereferences them, so safe to defer). Files: `src/lib/ai/handoff.server.ts`, `tools/registry.server.ts`, `handoff.test.ts`, `mission-advance.server.ts`. |
| 162 | ✅ | M-0 | Loop runs end-to-end on live data | Plan to dispatch to specialist execution confirmed live (hollow-completion fixed) | Foundational | Done | 2026-06-15 | |
| 163 | ✅ | FND-AUTH | Auth + tenancy + RLS | The multi-tenant spine (user_id + workspace_id + product_id) | Foundational | Done | - | |
| 164 | ✅ | FND-CHOKE | AI runtime chokepoint | One governed path for every AI call (callModel / callModelStream) | Foundational | Done | - | |
| 165 | ✅ | KI-13 | Resilient signup | A real account can be created without a 500 (handle_new_user subtransactions) | Foundational | Done | - | verify live |
| 166 | ✅ | KI-14 | Eval score scale to 0-100 | Eval scores do not overflow / false-fail the gate | Foundational | Done | - | |
| 167 | ✅ | F-A2A-CARD | Public A2A agent card | Discoverability for external agents | Foundational | Done | - | |
| 168 | ✅ | F-HUMANIZE-HOOK | Pre-commit dash/invisible-char trace hook | Build-time backstop for the humanization rule | Foundational | Done | 2026-06-18 | run `bash scripts/install-git-hooks.sh` to activate |
| 169 | ✅ | KI-10 | Ingest webhook + per-token rate limit | One secure live ingest path for public use | Sense | Done | 2026-06-16 | |
| 170 | ✅ | F-BRAIN | Brain (web + workspace research) | Perplexity-grade research feeding decisions | Sense | Done | - | |
| 171 | ✅ | N2 | Re-score + insight memo + daily brief | The daily brief synthesizes "what the loop learned" from re-scored outcomes | Sense | Done | 2026-06-16 | AI brief output needs a live re-verify on the deployed app |
| 172 | ✅ | F-CHAT-NL-INTENT | Conversational command of the swarm | Drive missions in natural language | Decide | Done | - | |
| 173 | ✅ | H1 | PRD / spec generation | Cited specs from opportunities | Decide | Done | 2026-06-14 | 🏷️ 2026-06-21: 'H1' is overloaded (also the Decision Brain row 148 'DBR (H1)' and H1-TASKS). THIS row = cited-spec generation only. Pick by unambiguous IDs, never bare 'H1'. |
| 174 | ✅ | DEF-03 | Critic-on-spec red team | Specs get an adversarial pass before commit | Decide | Done | 2026-06-14 | |
| 175 | ✅ | F-DEC-CARD | Decision card + Critic badge on Today | The human makes the call with the Critic's view in front of them | Decide | Done | - | |
| 176 | ✅ | WEDGE | Critic-teardown first-run (the launch wedge) | A new account names a feature and gets an evidence-backed Ship/Revise/Kill teardown in the first session | Decide | Done | 2026-06-17 | |
| 177 | ✅ | F-SHARE-TEARDOWN | Shareable Critic-teardown link (viral loop) | A public `/t/$slug` teardown link ("why your pet feature is wrong, with receipts") drives signups | Decide | Done | 2026-06-17 | |
| 178 | ✅ | F-SHARE | Shareable-decision viral loop + rate limit | A public decision link drives signups; secure anon-read (also a Monetization surface) | Decide | Done | 2026-06-16 | |
| 179 | ✅ | H2 | Outcome roadmap (Now/Next/Later) | Human commits opportunities to buckets with an outcome + measure; agent ICE orders within | Decide | Done | 2026-06-17 | 🏷️ 2026-06-21: 'H2' also denotes the Command Canvas (row 149, 'CMD (H2)'). THIS row = the Outcome roadmap. Pick by unambiguous IDs. |
| 180 | ✅ | H3 | Scheduling (calendar-aware work blocks) | Schedules open deep-work tasks into free time within working hours | Decide | Done | 2026-06-16 | |
| 181 | ✅ | DEC-02-LOOP | Critic as an explicit loop step | The Critic is a routable, gating-exempt agent-loop tool (`critic.evaluate`) | Decide | Done | 2026-06-17 | full mission_steps DAG-node promotion deferred to Phase 2 |
| 182 | ✅ | F-STUDIO | Build engine | Repo reads, multi-file changesets, `studio/*` branches, PR + CI, gated merge | Build | Done | - | |
| 183 | ✅ | I1 | Build multi-file coding (per-hunk accept/reject) | Operator curates a staged changeset before the gated commit | Build | Done | 2026-06-16 | |
| 184 | ✅ | I1b | True revision history (atomic revisions) | Each commit records a revision; the Changes tab shows commit history with GitHub links | Build | Done | 2026-06-16 | revert-to-revision delivered separately (K2) |
| 185 | ✅ | I2 | Watch-the-agents-build live surface | Live per-session cockpit: timeline/steer + Changes/PR/Cost + inline gates + merge gate | Build | Done | 2026-06-16 | true SSE streaming deferred (nice-to-have) |
| 186 | ✅ | I3 | Branch/worktree isolation per mission | Concurrent missions cannot share a branch or clobber files (per-path claims + per-changeset branch) | Build | Done | 2026-06-16 | |
| 187 | ✅ | J1 | Test generation + run | The Build agent authors tests per change; tests run in the connected repo's GitHub Actions CI | Build | Done | 2026-06-16 | |
| 188 | ✅ | J2 | QA gate + self-correct loop | Merge refuses while CI is red or pending; the fix-on-red-until-green loop closes | Build | Done | 2026-06-16 | |
| 189 | ✅ | K1 | Release notes for a shipped changeset | Generate/regenerate factual release notes via the AI chokepoint, persisted + shown | Build | Done | 2026-06-16 | |
| 190 | ✅ | LRN-02 | Outcome reviews (predicted vs actual) | Human verdict + ICE rescore + a drafted Historian verdict (predicted vs actual) on the outcome card | Launch | Done | 2026-06-17 | |
| 191 | ✅ | N3 | Mission Compounding View | Per mission: "drew on N prior memories" + the lineage + a copy-snapshot export | Launch | Done | 2026-06-16 | |
| 192 | ✅ | W6 | Persona onboarding tracks | Per-track sample data + first-win moment (Solo / Founding PM / Tech Founder); WEDGE delivery surface | Monetization | Done | 2026-06-17 | live-verify on next publish |
| 193 | ✅ | F-A2A | Internal A2A handoff contract | Agents hand off missions with structured payloads | Interop | Done | - | |
| 194 | ✅ | F-GAUNTLET | Gauntlet metrics | The north-star metrics (acceptance, autonomy, retention), honestly instrumented | Cockpit | Done | - | |
| 195 | ✅ | F-MEMVIEW | `/memory` compounding-memory view | The moat made visible | Cockpit | Done | 2026-06-14 | |
| 196 | ✅ | F-AUTONOMY | AutonomyCard on Today | The trust arc (observing to proving to trusted) is visible to the operator | Cockpit | Done | - | |
| 197 | ✅ | F-TODAY-LOOPPULSE | Loop Pulse hero | Today's hero opens with "while you were away" (last 24h, non-zero parts only) | Cockpit | Done | 2026-06-16 | |
| 198 | ✅ | E8 | Loop Health Monitor | An always-on health strip on Missions: verdict + queue depth + last ingest/run | Cockpit | Done | 2026-06-16 | |
| 199 | ✅ | B3 | Product switcher + portfolio view | A Portfolio section on `/product`: every product with loop status + click-to-switch | Cockpit | Done | 2026-06-16 | command-K product-switch deferred |
| 200 | ✅ | B5 | Archive / delete product | Soft archive + restore + JSON export + honest export-then-delete | Cockpit | Done | 2026-06-17 | |
| 201 | ✅ | F-AGENTS-MENTIONABLE | Agents as @-mentionable users | `@agentslug goal` in chat dispatches a specialist directly (skips the orchestrator) | Cockpit | Done | 2026-06-18 | |
| 202 | ✅ | OPS-01 | Flow mode | Ambient soundscape + focus timer + notification quieting; a calm operating surface | Cockpit | Done | 2026-06-16 | |
| 203 | ✅ | F-TRUST | Trust score + four autonomy arcs | Autonomy is earned and visible at the gate | Governance | Done | - | |
| 204 | ✅ | FND-0.6 | Kill-switch + spend caps | The brake pedal; budgets enforced server-side | Governance | Done | - | |
| 205 | ✅ | F-HUMANIZE | `humanizeText()` runtime sanitizer | Zero AI fingerprints in generated output, at the chokepoint | Governance | Done | 2026-06-14 | |
| 206 | ✅ | DEC-02 | Critic adversarial pass on opportunities | Opportunities get a red-team ship/revise/kill verdict at promotion (CriticBadge) | Governance | Done | 2026-06-16 | verified |
| 207 | ✅ | P4 | Eval harness + regression gate | A >=10pt eval drop per suite hard-blocks the agent's merge | Governance | Done | 2026-06-18 | P4-GATE cycle 23 |
| 208 | ✅ | P5 | Drift watch | Score/cost/latency drift per surface/model; open incidents surface in the Attention feed | Governance | Done | 2026-06-18 | P5-ALERT cycle 22 |
| 209 | ✅ | P3 | Prompt studio | Versioning + A/B + pin + rollback; runtime loads versioned prompts | Governance | Done | 2026-06-18 | verified already built (was stale ⬜) |
| 210 | ✅ | C4 / E7 | Agent detail + run history + memory inspector | Pick an agent and see its recent runs + its private and shared memory | Governance | Done | 2026-06-18 | a dedicated detail route is optional polish |
| 211 | ✅ | M-C-GOLIVE-GUARD | Credit go-live guard + backfill (incident-prevention) | backfill_account_credits() grants every account its tier base; admin_set_credits_enabled REFUSES to arm metering while any non-enterprise account has 0 credits. Fixes the 2026-06-21 incident (metering armed with 0 credits -> all AI blocked). + the credit-engine go-live runbook (docs/operations/credit-engine-go-live.md). | Monetization | Done | 2026-06-21 | 2026-06-21 SHIPPED (Claude). Migration 20260621130000; guard + backfill dry-run-verified on live; metering left OFF. The one safe go-live switch. |
| 212 | ✅ | MIG-LINT | Migration SQL apply-safety linter | Static OFFLINE linter fails the build on a migration whose SQL would fail to apply (`CREATE POLICY/TRIGGER ... IF NOT EXISTS`); complements `check-migrations.sh`'s applied-status check | Governance | Done | 2026-06-21 | ✅ SHIPPED (Lane 3, 2026-06-21): considerations.md Build/ship "Migration rollback strategy". The recurring "broken parallel ship" class (e.g. WM-F3's `CREATE POLICY IF NOT EXISTS` = Postgres ERROR 42601) only surfaced on/near publish because nothing linted SQL apply-validity OFFLINE. New PURE `src/lib/migration-lint.ts` (`lintMigrationSql`: 2 always-invalid ERROR rules [policy/trigger if-not-exists, ZERO false positives] + 2 advisory WARNs [new public-table no-RLS, add-column NOT NULL no-default]; `--`/`/* */`/`$$`-body comment-blanking preserves line numbers; 17 tests) + `scripts/lint-migrations.ts` (bun CLI, exit non-zero on error) wired into `check-migrations.sh` (runs offline, before the DB check, degrades if bun absent). VALIDATED against all 168 shipped migrations: 0 apply-fatal errors (no false positives) + 1 advisory warn. Single-agent SQL review (`ecc:database-reviewer`): core logic sound (anchoring/comment-blanking/cross-statement/ReDoS/hook-wiring); flagged 2 real issues (a `$$`-body false-positive trap [build-gating] + a non-public-schema warn mis-capture) → both folded with regression tests + re-validated on the 168 migrations. tsc 0 / 912 tests / eslint 0. Spec: docs/features/migration-lint.md. |
| 213 | ✅ | F-ADMIN-AUDIT | Admin audit trail completed (sensitive-table triggers) | Bypass-proof DATA-layer audit: AFTER triggers on user_roles / app_settings / pricing_plans / pricing_bundles / pricing_topup_bundles log actor + old/new to admin_audit_log - closing the v1 gap (admin roster grant/revoke, the credits-engine toggle, pricing CRUD) the v2 RPCs did not cover. Skips system/service-role/migration writes (no JWT). | Governance | Done | 2026-06-21 | 2026-06-21 SHIPPED (Claude): F-ADMIN-AUDIT-V1GAP closed. Migration 20260621160000 + extended the target_kind CHECK (role/setting/plan/pricing). Dry-run-verified on live; answers 'who granted admin / flipped metering / changed pricing'. |

---

## Status reconciliation note
Statuses here are reconciled from [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) (sections 0/2/3, the live cursor + status + build queue) and [`archive/feature-backlog.md`](archive/feature-backlog.md) (granular ledger). Where docs conflict on a "done" claim, trust this file + the live code, then fix the others in the same commit. Granular acceptance criteria + "how to use / verify" blocks live in [`archive/feature-backlog.md`](archive/feature-backlog.md); milestone exit criteria + the founder pickup list live in [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) (sections 2-4); open bugs live in [`known-issues.md`](./known-issues.md). F-SHARE is one feature listed under both Decide (G2) and Monetization (G5); it is a single row here (Decide, shipped) per the register-one-row-per-ID rule.

## Related
- [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) - the ONE front-door tracker (live cursor, build queue, founder pickup list, findings, progress)
- [`archive/feature-backlog.md`](archive/feature-backlog.md) - granular ledger + per-feature acceptance criteria
- [`workspace-tenancy-and-monetization-plan.md`](./workspace-tenancy-and-monetization-plan.md) - WM (G10) per-ID build specs incl. the credit engine (WM-M10..WM-M16, §4.2.1)
- [`byo-build-implementation-plan.md`](./byo-build-implementation-plan.md) - BYO (G11) per-phase build specs
- [`v10-master-blueprint.md`](../strategy/v10-master-blueprint.md) §15-16 + [`v10_implementation-plan.md`](./v10_implementation-plan.md) - the priority rationale + execution mechanics
- [`known-issues.md`](./known-issues.md) - open bugs with KI-IDs
- [`../../AGENTS.md`](../../AGENTS.md) §1 (pre-action) + §5 (doc-update protocol) - where the standing rule is enforced
