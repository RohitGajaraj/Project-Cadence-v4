 # Feature Dashboard - the one master prioritized feature register

> **SSOT first.** The single front-door tracker is [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) (live cursor, status, build queue, founder rulings, findings, progress). This file is the per-feature master register + live In-Dev claims (machine/loop view) it points to, not the tracker to follow day-to-day.

> **What this is.** ONE canonical, at-a-glance, prioritized register of **every** feature - what is built, in dev, partial, paused, deferred, blocked, or open - each with a one-line "what it does", a category, and a priority, so any session can pick the top open item cold. This is the **front door** to status. Per-feature acceptance detail lives in [`feature-backlog.md`](./feature-backlog.md); current-initiative build specs live in [`workspace-tenancy-and-monetization-plan.md`](./workspace-tenancy-and-monetization-plan.md) (G10) + [`byo-build-implementation-plan.md`](./byo-build-implementation-plan.md) (G11).
>
> **Created:** 2026-06-16 · **Last updated:** 2026-06-21 (STRATEGY RECONCILIATION: imposed the numbered **Build Sequence** + the pick-by-number law [AGENTS.md §3]; marked the credit/pricing block **Lovable-owned + frozen** (`M-C-PRICE`, `WM-M3/M6/M7/M8/M10/M11/M12/M13/M14/M16/M17/M18/M19`, `BYO-P4`); kept the `WM-F*` tenancy spine Claude-owned; added 9 Tier-1 rows (#152-160: `DBR-1.5` + loop-closers + 2 BBI fixes); reparented `O1`/`O3` to the Decision Brain + `WM-M15` out of Credit; superseded `F-COCKPIT-MACHINE-MODE`; **made the Priority column uniform** (exactly one of Tier 1/2/3/4 · Gated · Lovable · Deferred · Done, retiring the P0/P1/WM-*/BYO-* mix) and the `#` a **derived priority Rank** (#1 = highest, regenerated by `scripts/rerank-dashboard.py`), which also fixed the duplicate + lettered serials from the parallel merge; tally 64/167 = 38.3% strict / 60.2% weighted). Prior: 2026-06-20 22:20 (FE cycles 0-4, founder-directed FRONT-END theme wiring the shipped WM backends to the UI: **cycle 0** codified the frontend build protocol (§3.0d, full design-skill toolkit); **cycle 1** the **Members surface** (`MembersCard`, WM-F4 transfer + remove + members list via a new membership-gated identity RPC); **cycle 2** the **move-product UI** (WM-F6); **cycle 3** member **role management** + an OWNER-ONLY correction (fixed a cycle-1 admin silent-failure + hardened the write fns); **cycle 4** invite **redirect-after-login** (an open-redirect-guarded `next` param on /login + /signup completes the invite flow end-to-end). The founder-directed FE-wiring scope (members, move-product, invites) is COMPLETE; remaining WM FE is founder-gated (credits/pricing, WM-M6/M3). ◐ live on publish; the identity-RPC migration live dry-run is pending (Supabase MCP unauthorized this session, flagged for publish-verify). Prior: 2026-06-19 22:20 (WM-F3-fix → cycle 41: the shipped RBAC migration used invalid `create policy if not exists` (ERROR 42601) and would have FAILED to apply on publish; fixed to drop-then-create, `70c0ca7db5`, dry-run-verified on prod + owner-lockout data-checked. This is the 3rd broken parallel ship caught + fixed this session; WM-F2-fix → cycle 40: restored the cross-user-read + expiry + global-scope + return-shape invariants the cycle-40 WM-F2 ship regressed, and made account pooling actually driven (resolved internally, not a dormant opt), `a368edf78a`, dry-run-verified on prod; WM-M15b-fix → cycle 39: corrected 3 runtime-fatal bugs in the cycle-38 response cache (broken migration now()-predicate that would fail on apply + no-op cache hit + bad readCache "now()" literal), `7897085004`, dry-run-verified on prod; WM-F1b → ◐: agent-workspace isolation hardening, `user_id`-fed BEFORE-INSERT trigger (universal insert tagging) + dual-key membership RLS on all 5 agent tables, dry-run-verified on prod, overnight cycle 37; WM-F9 → ◐: tenancy isolation hardening, workspace_id + dual-key membership RLS on 10 tables, overnight cycle 35; pricing-strategy packaging: added 3 deferred rows WM-M17/M18/M19 (Anthropic-style usage variants, plan-card states, enterprise usage model), register 141 → 144; WM-M15 → ◐: cost-aware model routing margin lever, config-gated default-off, overnight cycle 34; WM-M14 → ◐: per-product/member credit attribution + caps, overnight cycle 33; WM-M12 → ◐: credit debit engine fills the WM-M4 seam, atomic draw-down + halt, overnight cycle 32; WM-M11 → ◐ cycle 31; WM-M4 → ◐ cycle 30; WM-M10 → ✅ cycle 29; earlier 05:30 IST: restructured into a single master prioritized register: every G0-G11 row is one numbered row; sorted open-first by priority, then done-by-category). **Maintainer rule:** Tier 1, continuous (update in the same commit as any status change; stamp the precise time on every update; recompute the "At a glance" % per the **Progress accounting** rule below).

---

## STANDING RULE - REGISTER FIRST, THEN BUILD (non-negotiable, read before any feature work)

**No feature is built that is not tracked here first.** Every tool (Claude Code, Antigravity, Gemini, Lovable, a future session) must:

1. **`git pull origin main`**, then read this register. It is checked before any activity starts so a new or parallel session knows what is already in flight, deferred, or done, and does not collide or redo.
2. **REGISTER a new feature HERE FIRST.** Before any code, add a row with an **ID + Category + Priority** (and a one-line "What it does"). If it is not in this sheet with an ID, it does not get built.
3. **Respect the claims.** If a row is `🔨 In Dev`, another session may be on it - do not start it. Pick a different row or coordinate. Check the **Active claims** table first.
4. **On pickup:** flip Status to `🔨 In Dev (<tool>, YYYY-MM-DD)`, add a line to the **Active claims** table, and commit + push immediately so others see the claim - same commit, before you write feature code. (The literal `In Dev (` substring is preserved so the session-start hook can grep live claims.)
5. **On completion:** flip to `✅` (or `◐` if partial) with the date, remove the Active-claims line, and update the linked detail doc + `plan.md` §4 in the same unit of work (the closed-doc loop).
6. **On defer/pause/block:** flip to `⏭️` / `⏸️` / `🚧` and put the SHORT REASON in the **Comments** column (e.g. "deferred: post-PMF", "blocked: needs founder OAuth").

> Same shared-cursor discipline as the SSOT live cursor (section 0) and [`feature-backlog.md`](./feature-backlog.md). When they disagree, fix all in the same commit.

### How to pick something up
Say **"pick `<ID>`"** (e.g. "pick WM-M1", "start SEN-01", "do F-IA-V4") and the agent resolves the ID here, reads the row, opens the linked detail (backlog / WM plan / BYO plan / feature doc), and builds. The IDs are stable and shared with [`feature-backlog.md`](./feature-backlog.md).

### STANDING RULE - PROGRESS ACCOUNTING (keep the % live, never hand-guess it)

**This is how the "what percentage is done" question is answered at a glance, forever, without the number rotting.** The overall % and the per-category roll-up are **derived from the register rows**, not separately hand-kept, so they stay true as long as you follow this:

1. **Every row already carries its own % - read it off the status icon** via the **Item %** column in the Status legend below (✅ 100%, ◐/⏸️/🔨 50%, ⬜/⏭️/🚧 0%). That is the *individual-item* completion. A ◐ partial may override its default 50% with an explicit `[~NN%]` token in its Comments when the remaining split is known (e.g. "remaining D4b ... `[~80%]`").
2. **The overall number is two figures, both shown in "At a glance":** *strict* = fully-done rows / total; *weighted* = sum of every row's Item % / total. Strict is the honest floor; weighted credits half-built work.
3. **Deferred rows stay in the denominator** - they are real planned scope, just not now. (For "% of active scope", subtract the deferred count from the denominator; the dashboard reports against all rows.)
4. **On ANY status change, recompute in the same commit.** Do not eyeball it - run the override-aware tally and paste the results in:
   ```
   awk -F'|' '/^\| [0-9]+ \|/{s=$3;gsub(/^[ \t]+|[ \t]+$/,"",s);c[s]++;t++; w=(s=="✅")?1:(s=="◐")?(match($0,/\[~[0-9]+%\]/)?substr($0,RSTART+2,RLENGTH-4)/100:0.5):(s=="⏸️"||s=="🔨")?0.5:0; W+=w} END{for(k in c)print k,c[k]; printf "total=%d strict=%.1f%% weighted=%.2f (%.1f%%)\n",t,(c["✅"]/t)*100,W,(W/t)*100}' docs/planning/feature-dashboard.md
   ```
   then update the "At a glance" strict/weighted/by-status/by-category numbers and the "Last recomputed" stamp.
5. **One number, one place.** [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) (sections 0 + 2) shows the same headline % and points back here for the method. Never maintain a second independent figure.

### Status legend (each status carries a completion weight - the **Item %** is the per-item progress you read off any row)
| Mark | Meaning | Item % |
| --- | --- | --- |
| ✅ | **Done** - built, on `main`, verified (date where known) | **100%** |
| 🔨 | **In Dev** - actively being built this/another session (see Active claims); keeps the literal `In Dev (` for the hook | **50%** |
| ◐ | **Partial** - foundation built, real remaining work; Comments says what is left | **50%** (override with `[~NN%]` in Comments) |
| ⏸️ | **Paused** - built or started but intentionally idle; reason in Comments | **50%** (built but idle) |
| ⏭️ | **Deferred** - deliberately not now (gate/sequence reason in Comments) | **0%** |
| 🚧 | **Blocked** - cannot proceed until a dependency clears (founder action / KI); reason in Comments | **0%** |
| ⬜ | **Open** - not started, ready to pick up | **0%** |

### Category legend (11 categories; mapped from the source groups)
| Category | What it covers | Source group |
| --- | --- | --- |
| Foundational | Core autonomous loop, memory engine, platform/auth/runtime infra | G0 + G9 |
| Sense | Get real signal in, cluster it, keep it fresh | G1 |
| Decide | Turn signal into governed decisions + specs (incl. the wedge) | G2 |
| Build | The autonomous Build -> QA -> Ship execution chain | G3 |
| Launch | Ship to market, learn from outcomes, feed it back | G4 |
| Monetization | Pricing, entitlements, tenancy/RBAC/billing, PLG | G5 + G10 (tenancy/RBAC/billing rows) |
| Credit | The credit metering engine (WM-M10..WM-M16) | G10 (credit rows) |
| Interop | External agents/tools (MCP, A2A), team interop, export | G6 |
| Cockpit | IA, observability, the operator's view of the machine | G7 |
| Governance | Trust, safety, evals, drift, critic, incidents, humanization | G8 |
| BYO | Provider-agnostic repos + managed Cadence Cloud runtime | G11 |

---

## ⚡ HOW TO PICK - the priority ranking (founder ruling 2026-06-21)

> **MANDATORY, non-negotiable.** The Master register below is **sorted by priority and ranked `#1, #2, #3 ...`** (`#1` = the single highest priority). **Any lane / worktree / tool builds the LOWEST-numbered (`#`) register row whose Priority is a Tier (Tier 1/2/3/4) and that is unclaimed. Do NOT judge, score, or spend tokens deciding what to pick - the rank IS the decision.** Skip rows marked `Gated` (founder must unblock), `Lovable` (Lovable builds these), `Done`, and `Deferred`. Claim atomically (`bash scripts/lane.sh claim <ID> <laneN> "<globs>"`), flip the row to `🔨 In Dev`, build it, and mark it `✅`/`◐` on the board in the SAME commit; then take the next rank. Full law + the real-time-board loop: [`../../AGENTS.md`](../../AGENTS.md) §3 "Build Sequence"; founder mirror: [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) §0.

### How priority is marked (the ONE uniform vocabulary - the Priority column is always exactly one of these)

| Priority class | Meaning | In the pick-order? |
| --- | --- | --- |
| **Tier 1** | Fundamentals / foundation / core features / core enablements / USPs (the moat, the loop, the wedge, chokepoint integrity). Build first. | yes, ranked first |
| **Tier 2** | Design + eye-for-detail on the core surfaces. Founder-prompted, once, after the core is solid. | yes, after Tier 1 |
| **Tier 3** | Non-essential / non-foundational (privacy, ops / reliability hygiene, enterprise-readiness, edge). Never outranks core. | yes, after Tier 2 |
| **Tier 4** | Final detailing + comprehensive polish (humanization sweep, lint, AI-trace, final design). Founder-prompted, last. | yes, last |
| **Gated** | Founder-gated; ranked in place but parked until the founder unblocks it (OAuth / secret / spend / taste). | skip until unblocked |
| **Lovable** | Lovable-owned (billing / credit / pricing); built in parallel by Lovable. | no by default; **yes if the founder explicitly hands it back to an agent** |
| **Deferred** | Cut / post-PMF / superseded. | no |
| **Done** | Shipped. | no |

**The tier law (build substance before surface; non-core last):** Tier 1 then Tier 2 then Tier 3 then Tier 4, in that strict order. This is why the rank puts every Tier-1 row above every Tier-2 row, and so on. It fixes the drift where the loop built Tier-3 ops hygiene while the Tier-1 moat work had no rows.

### Before you build a picked item: the sourcing gate + the autonomy doctrine (founder ruling 2026-06-21)

The Rank decides WHAT to build next; this decides HOW to source it, and WHO decides. Apply it to every item before writing code; the agent owns this call from its own web-grounded research (carry a greppable `BBI:` stamp on the deciding doc/PR). **The per-cluster verdict (what to build vs what to source elsewhere) lives in the [Sourcing Map](../strategy/sourcing-map.md): look up a picked item's cluster there FIRST, and do NOT build any sub-item the map marks obviated by a buy/integrate (wire the named provider behind the cluster's seam instead).** Canon: [`../strategy/build-buy-integrate.md`](../strategy/build-buy-integrate.md) + [`../../AGENTS.md`](../../AGENTS.md) §3.0c.

**The four sourcing decisions:**

- **BUILD** (in-house, from base) - the moat / USP: the typed decision ontology, the supersession engine, the Critic + precedent-salience, signal->ontology normalization, outcome->memory write-back, the system-of-record. **Never wrapped, never bought.** Default for anything that IS the differentiator.
- **BUY** (a commodity, always via the AI chokepoint `runtime.server.ts`, never called directly) - inference, embeddings, rerank, OCR, email, OAuth.
- **INTEGRATE** (a high-lock-in substrate, behind our OWN swappable seam, with a native default + graceful fallback) - e.g. the sandbox provider, a heavier graph engine only if we outgrow Postgres.
- **SELF-HOST** (open-source we run ourselves - the autonomy floor: a zero-external-paid-dep native default that must always hold) - e.g. SearXNG (`FIRECRAWL-FLOOR`), Postgres + pgvector for the graph.

**The Decision Brain verdict (the strongest opinion, since this was the open question):** the brain's INTELLIGENCE is **BUILD, in-house, forever** - the ontology, supersession (`DBR-1.5`), the Critic, the salience ranker. Never make the brain's reasoning a wrapper around someone else's product; that IS the moat. Its SUBSTRATE is **SELF-HOST/native** (Postgres + pgvector now), swappable to an **INTEGRATE'd** graph engine (Graphiti / Neo4j) behind a `GraphStore` seam only if scale demands; its embeddings + inference are **BUY** via the chokepoint; the Obsidian-style view **INTEGRATE**s a viz library (don't build a render engine). Net: the thinking is ours; only the plumbing is swappable.

**Autonomy doctrine (act on your own; escalate only when you must):**

- **ACT autonomously** - build the moat; buy a commodity via the chokepoint with NO new recurring spend; self-host an OSS native default; integrate behind a seam when a free/native default exists; any migration / server-fn needing no secret/spend/taste (gate it offline, flag for publish-verify).
- **THINK it through autonomously** (the agent decides via web research - do NOT ask the founder for this) - the sourcing call above, the architecture, the seam design, moat-vs-commodity. Carry the `BBI:` stamp on the deciding doc/PR.
- **ASK the founder** (escalate; mark the row `Gated`) - a new recurring SPEND; a SECRET / OAuth / paid-provider pick; a TASTE / positioning / pricing / product-tasting call; anything IRREVERSIBLE or OUTWARD-FACING (sending, publishing, the founder's accounts); a legal / policy call (data-retention scope, PII). When genuinely unsure whether it is a taste / spend / irreversible call, ask.

### The ranking is DERIVED - never hand-number it

The `#` Rank is computed, like the % tally. **To add or reprioritize an item:** add or edit its row with the right **Priority class** (and, for a Tier-1 item, place it among the Tier-1 rows in the order you want), then run **`python3 scripts/rerank-dashboard.py`**. It re-sorts the whole register by priority and renumbers `#1..N`, so a new high-priority item moves up and everything below it shifts down (nothing is left siloed, stuck on top, or stranded at the bottom). Then recompute the "At a glance" % (the awk in the Progress-accounting rule). Both run in the same commit as the change. The Tier-1 fine order lives in `scripts/rerank-dashboard.py` (the `T1` list).

### 🔁 Lovable-owned (mark `Lovable`; never a Claude pick - Lovable builds these in parallel)

Billing / Stripe: `M-C-PRICE`, `WM-M3`, `WM-M13`. Credit engine (already shipped dormant, do NOT extend; coordinate before live): `WM-M10`, `WM-M11`, `WM-M12`, `WM-M14`, `WM-M16`. Pricing / tier surfaces: `WM-M6`, `WM-M7`, `WM-M8`, `WM-M17`, `WM-M18`, `WM-M19`. Managed credits: `BYO-P4`. Split rows (Lovable owns the billing half only): `PLG` conversion leg, `R4` Plan/billing tab, `WM-F7` Plan/billing tab, `WM-M2` credit-pool/Stripe half. **STAYS product-infra (NOT Lovable):** every `WM-F*`, `WM-M1`, `WM-M4`, `WM-M5`, `WM-M9`, `WM-M15`, `M-C-EXPIRY`, `FND-AUTH`, `U6`. (The founder may ask Lovable to flip its rows to Done as that parallel work lands.)

---

## Active claims (who is on what, right now)

> **The real-time view of who is building what lives in a GIT-IGNORED file, not here (founder ruling 2026-06-21).** A git-tracked table can never be real-time (it only changes on commit -> push -> pull) and a cached one rots and causes session collisions, so it is deliberately not kept here.
>
> **➡️ Open [`active-claims.live.md`](./active-claims.live.md) for the LIVE status** - which lane is on which activity (with Rank), since when, plus the build queue showing what is `🔒` claimed vs `🟢` free. It is regenerated every ~8s by the `com.cadence.active-claims-sync` launchd watcher (`scripts/active-claims-watch.sh`) from the atomic claim ledger (`~/.cadence-parallel`) + origin/main, so it is always current and your editor live-reloads it. Instant CLI view: `bash scripts/lane.sh board`. (One-time install: `cp scripts/launchd/com.cadence.active-claims-sync.plist ~/Library/LaunchAgents/ && launchctl load ~/Library/LaunchAgents/com.cadence.active-claims-sync.plist`.)
>
> **The shared real-time space across all sessions / worktrees is the atomic ledger at `~/.cadence-parallel`, NOT git.** Every session on this Mac sees every claim INSTANTLY via `bash scripts/lane.sh list` - no push or pull needed. (git push is only for the durable record + other machines.)
>
> **THE COLLISION GATE (mandatory; this is what prevents two sessions picking the same item):** before ANY work, a session MUST run `bash scripts/lane.sh claim <ID> <laneN> "<globs>"` and proceed ONLY if it returns success (exit 0 = won). `HELD` (someone holds the item) or `CONFLICT` (overlapping files) means STOP and pick the next `🟢` free item. **Never read, plan, or build before a successful claim** - the atomic `mkdir` is race-free; the dashboard never was the guard. The lane then flips its register row to `🔨`/`✅` and commits (the git record + the at-a-glance %).

---

## At a glance

> **Live progress (derived from the rows, recompute on every status change - see the Progress accounting rule above).** **2026-06-21 (strategy reconciliation, this commit):** imposed a UNIFORM priority convention (every row's Priority is exactly one of Tier 1/2/3/4/Gated/Lovable/Deferred/Done) and a single DERIVED **Rank** (`#1` = highest, regenerated by `scripts/rerank-dashboard.py`), retiring the old `P0/P1/WM-*/BYO-*` chaos and the duplicate + lettered serials. Recomputed off the live awk: **total 167, strict 64/167 = 38.3%, weighted 100.47/167 = 60.2%** (167 because the tally now counts the lanes' lettered-serial rows the old `^\| \d+ \|` awk silently missed; dropped 1 duplicate `DATA-RETENTION-b`). Added the supersession engine `DBR-1.5` (Rank #1, TOPMOST) + the loop-closers `MOAT-VIS`/`MOAT-METRIC`/`W1-AUTO`/`H1-TASKS`/`F-IA-BRAIN-GRAPH` + the BBI fixes `EMBED-CHOKEPOINT`/`FIRECRAWL-FLOOR` as claimable Tier-1 rows; marked the credit/pricing block **Lovable-owned**; reparented `O1`/`O3` into the Decision Brain + `WM-M15` out of Credit; superseded `F-COCKPIT-MACHINE-MODE`. **Prior: Decision Brain merge (2026-06-20): NEW rows 148 DBR (H1, ⬜) + 149 CMD (H2, parked, ⬜)** are 0% horizon cues, so total 147→149, ⬜ 36→38, strict 63/149=42.3%, weighted 88.80/149=59.6% (the weighted sum is unchanged at 88.80). Prior: **2026-06-20 04:55 IST** (cycle 54, FRONT-END theme: wired WM-F5's accept route - a standalone `/join/$token` page that redeems an invite via `acceptInvitation`, completing the invite→accept loop; row 54 WM-F5 [~85%]→[~92%], weighted 88.73→88.80 = 60.4%, counts unchanged; ran `impeccable`. cycle 53, FRONT-END theme: wired WM-F5's invite UI - a `TeamCard` ("Invite teammates") in Settings > Workspace (invite by email + role, copy join link, list + revoke invitations); row 54 WM-F5 [~70%]→[~85%]; then rebased in the cockpit lane's P7 (which flipped P7 → ✅), so the canonical awk now reads ✅63 / ◐35 / ⬜36 / ⏸️3 / ⏭️10, strict 42.9% / weighted 88.73 = 60.4%; ran `impeccable`. cycle 52, FRONT-END theme: wired U6's export-history view - the DataExportCard now shows a "Recent exports" list via `listExportLog` (row 36 U6 [~95%]→[~98%]); weighted 88.15→88.18, counts unchanged; ran `impeccable` per the founder ruling. cycle 51, FRONT-END PIVOT per founder ruling: started stitching UI onto shipped backends so they are visible in the app. First build: a calm-front `SubprocessorsCard` ("Where your data goes") in Settings > Data wiring SUBPROC-DISCLOSURE's `getSubprocessors` (row 146 [~60%]→[~75%]). Rebased in the cockpit lane's R3-PREFS (which flipped R3 → ✅), so the canonical awk now reads ✅62 / ◐36 / ⬜36 / ⏸️3 / ⏭️10, strict 42.2% / weighted 88.15 = 60.0%. cycle 50: NEW row 147 APP-HEALTH → ◐ [~60%], a public app health/readiness endpoint (`/api/public/health`) + pure status-assembly module, Reliability P0; canonical awk now ✅61 / ◐37 / ⬜36 / ⏸️3 / ⏭️10, strict 41.5% / weighted 87.60 = 59.6%. cycle 49: NEW row 146 SUBPROC-DISCLOSURE → ◐ [~60%], a catalog-derived sub-processor disclosure registry + read fn, Data/Privacy P1; the awk recompute also corrected drift in the hand-kept status counts. cycle 48: U6-AUDIT backend (row 36 U6) shipped — an append-only `export_log` audit table + per-export audit-write in `exportProduct`/`exportWorkspace` + a `listExportLog` read fn, behaviorally dry-run-verified on prod (append-only confirmed: insert+select policies only); U6 [~85%]→[~95%], weighted 83.06→83.16 (counts unchanged, U6 stays ◐ pending its history UI); earlier cycle 47: NEW row 145 DATA-RETENTION → ◐) from the register (the per-status counts and per-category roll-up are tallied, not hand-kept).

- ### Overall completion: **38.1% (strict) / 60.3% (weighted)**
  - **Strict** = fully-done rows / total = **64 / 168 = 38.1%** (the honest floor).
  - **Weighted** = sum of every row's Item % / total = **101.27 / 168 = 60.3%** (✅ 100%; ◐ partial at its per-row `[~NN%]` estimate; ⏸️ paused + 🔨 in-dev 50%).
  - **Remaining: 62% open** - **104 of 168** rows not fully done.
  - _168 because the tally now counts the lanes' lettered-serial rows (`8b`/`44b` etc) that the old `^\| \d+ \|` awk silently missed; the rerank gives every row a clean unique Rank so the awk now sees all of them (it dropped 1 duplicate `DATA-RETENTION-b` and folded in the lanes' `RUNAWAY-INCIDENTS`). The Claude-buildable open set is far smaller than 104: 14 rows are `Lovable` (Lovable builds them) and 19 are `Gated` (need the founder)._
- **Total features = 168** · **Done = 64** · **Not done = 104** (⬜ 40 open + ◐ 50 partial + ⏸️ 3 paused + ⏭️ 11 deferred + 🔨 0 in dev + 🚧 0 blocked).
- _By priority class: **Tier 1 = 28**, Tier 2 = 4, Tier 3 = 28, Tier 4 = 1, Gated = 19, Lovable = 14, Deferred = 10, Done = 64. The active Claude build front is the Tier-1..4 rows (ranked first); pick the lowest `#`._

- **By status (of 168 total), with weighted contribution to the roll-up:**

| Status | Count | Item % | Weighted contribution |
| --- | --- | --- | --- |
| ✅ Done | 64 | 100% | 64.0 |
| ◐ Partial | 50 | per-row `[~NN%]` (avg ~72%) | 35.77 |
| ⏸️ Paused | 3 | 50% | 1.5 |
| 🔨 In Dev | 0 | 50% | 0.0 |
| ⬜ Open (ready to pick up) | 40 | 0% | 0.0 |
| ⏭️ Deferred | 11 | 0% | 0.0 |
| 🚧 Blocked | 0 | 0% | 0.0 |
| **Total** | **168** | - | **101.27 / 168 = 60.3%** |

- **By category (Total / Done / Open / Weighted %), most-complete first** - shows which lanes are nearly closed and which are barely started:

| Category | Total | Done | Open | Weighted % |
| --- | --- | --- | --- | --- |
| Decide | 15 | 10 | 5 | 89% |
| Governance | 16 | 9 | 7 | 78% |
| Foundational | 26 | 18 | 8 | 77% |
| Data/Privacy | 3 | 0 | 3 | 73% |
| Build | 14 | 8 | 6 | 71% |
| Reliability | 3 | 0 | 3 | 67% |
| Cockpit | 21 | 10 | 11 | 63% |
| Interop | 5 | 1 | 4 | 55% |
| Credit | 6 | 1 | 5 | 49% |
| Sense | 10 | 3 | 7 | 48% |
| Monetization | 31 | 2 | 29 | 44% |
| Launch | 10 | 2 | 8 | 40% |
| BYO | 8 | 0 | 8 | 0% |
| **Total** | **160** | **64** | **96** | **59%** |

> **Pick-order is THE BUILD SEQUENCE above** (founder ruling 2026-06-21): build the lowest open number, do not deliberate. The tier law (foundation/core/USP, then design, then non-essential, then final polish) and the rationale live in the Build Sequence section + [`../../AGENTS.md`](../../AGENTS.md) §3 "Build Sequence" + the SSOT ([`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) §0). Positioning rationale: v10 ([`v10-master-blueprint`](../strategy/v10-master-blueprint.md) §15-16) + moat.md. **The TOPMOST priority is the Decision Brain (H1); monetization + credit (WM-M*, M-C-*, BYO-P4) is Lovable-owned (not a Claude pick); the WM tenancy spine (WM-F*) stays Claude-owned; BYO (BYO-*) awaits founder greenlight.** The Priority column on each row is now superseded by the Build Sequence number for pick-order; it stays as a coarse tier hint.

**Priority key (uniform, founder ruling 2026-06-21):** every row's Priority is exactly one of `Tier 1` · `Tier 2` · `Tier 3` · `Tier 4` · `Gated` · `Lovable` · `Deferred` · `Done` (defined in the "How to pick" table above). The `#` column is the **Rank** (priority order, `#1` = highest); build the lowest-`#` `Tier`-class row that is unclaimed. The legacy `P0/P1/P2/WM-*/BYO-*` codes were retired into this scheme.

---

## Master register

> **Sorted by Rank = priority** (Tier 1 > Tier 2 > Tier 3 > Tier 4 > Gated > Lovable > Deferred > Done; within a tier by build order). The `#` column is the **Rank** (`#1` = highest priority); it is DERIVED, regenerated by `python3 scripts/rerank-dashboard.py` (never hand-numbered), so adding or reprioritizing a row reflows the whole list. Build the lowest-`#` `Tier`-class row that is unclaimed.

| # | Status | ID | Feature | What it does | Category | Priority | Added | Comments |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | ◐ | DBR-1.5 | Supersession engine (outcome to typed-edge write-path) | On recordOutcome, infer + write typed supersedes/contradicts edges (bi-temporal valid_to, invalidate-don't-delete) into artifact_lineage; flag-gated DECISION_BRAIN_SUPERSESSION default OFF | Foundational | Tier 1 | 2026-06-21 | SHIPPED ◐ (Lane 2, 2026-06-21): the moat's signature write-path. New PURE `src/lib/ai/supersession.ts` (`classifySupersession` verdict-conflict matrix + `resolveEndpoints` + `selectSupersessions` cap/dedup + `buildSupersessionEdge`; 17 tests) + gated orchestrator `supersession.server.ts` (`inferSupersession`, first line returns when the flag is off = zero embed/write/byte-identical) hooked into `recordOutcome` after `rememberOutcome`, fail-safe. Additive migration `20260621030000_decision_brain_supersession_bitemporal.sql` (3 nullable bi-temporal cols; explorer read surface byte-identical). Invalidate-don't-delete (never DELETE, only retires its OWN engine edges). 4-lens adversarial-review Workflow: 0 blockers, SHIP_WITH_FIXES — folded the 2 real write-path defects (zombie-edge-on-re-record + missing excludeId self-starvation) + the opportunity-dedup NIT. tsc 0 (my files) / eslint 0 / 492 tests (build red is a pre-existing Stripe-dep baseline failure, proven not mine). **◐ not ✅:** PURE matrix ✅ unit-verified; live edge-write + migration apply verify on a publish/dry-run, dormant-with-no-data until the founder flips the flag. Plan: supersession-engine-plan.md. |
| 2 | ◐ | MOAT-VIS | Make compounding visible | Render a rescore + the learning that caused it on Today's what-changed + Brain | Launch | Tier 1 | 2026-06-21 | SHIPPED ◐ (Lane 2, 2026-06-21): makes the outcome->learning->ICE-rescore compounding visible. New PURE `src/lib/moat-vis.ts` (`summarizeCompounding`/`rescoresOf`/`describeCompounding`; rounds to display precision so a sub-0.1 jitter never reads as a move; 10 tests) + the named-missing `getCompounding` server fn in `today.functions.ts` (RLS-scoped, mirrors listLearnings). Brain Learnings tab now renders a `CompoundingPanel` (aggregate "memory re-scored N decisions, net ICE" line + cause-carrying feed) instead of the old MemoryPanel landing; Today's "what changed" card gains the aggregate compounding line. 2-lens adversarial-review Workflow: 0 blockers, SHIP_WITH_FIXES - folded the raw-vs-rounded honesty regression + the error-disguised-as-empty defect. tsc 0 (my files) / eslint 0 / 519 tests (build red = pre-existing Stripe-dep baseline, not mine). **◐ not ✅:** pure logic ✅ unit-verified; the live read + render verify on publish. Follow-up MOAT-VIS-b (logged): delete the now-orphaned MemoryPanel.tsx + extract shared whenOf/VERDICT_TONE. |
| 3 | ◐ | F-IA-BRAIN-GRAPH | Decision Brain visual graph surface | The Obsidian-style visual graph front surface on Brain | Cockpit | Tier 1 | 2026-06-21 | The DBR-1 read view exists at /knowledge?tab=graph; this is the placement/IA half under H1. Coordinate with the knowledge lane. |
| 4 | ◐ | MOAT-METRIC | Outcome-accuracy lift per PM | Lift vs a generic-model baseline as one account's memory grows, on the Gauntlet | Governance | Tier 1 | 2026-06-21 | LIFT HALF SHIPPED (lane 1, 2026-06-21 04:38): the unbuilt half of MOAT-METRIC. A pure `computeMemoryLift` (memory-compounding.ts, 17 tests) + `getMemoryLift` server fn + a `MemoryDepthSplitCard` on the Gauntlet. Splits reviewed bets by memory-depth-at-decision (earlier/sparse vs later/rich half), reports the validated-share difference in whole pp, gated by a size floor (8/side), a material depth-contrast guard, AND a two-proportion 95% CI noise gate; correlational only (neutral colour either sign; the time/practice/survivorship confound named inline + footnote), never a causal A/B. Designed + red-teamed via two 4-agent workflows (honesty/stats/data lenses; review verdict ship, 0 must-fix). tsc 0 / 492 tests / build green. ◐ renders + behaviorally verifies on the founder's next publish (reads live learnings + agent_memory). [~90%] |
| 5 | ⬜ | EMBED-CHOKEPOINT | Route embeddings through the AI chokepoint | embed.server.ts calls the gateway directly today (no cost/credit/BYO/logging); route via callModel + add model_id/dimension cols + backfill | Foundational | Tier 1 | 2026-06-21 | BBI build-now #1 (a flagged chokepoint-bypass to fix). Touches the CHOKEPOINT pin: claim it solo + attended. |
| 6 | ◐ | FIRECRAWL-FLOOR | Firecrawl autonomy-floor fallback | firecrawl.server.ts hard-throws on a missing key; add a native SearXNG self-host sibling fallback | Foundational | Tier 1 | 2026-06-21 | SHIPPED ◐ (lane 1, 2026-06-21 05:17): the agent's `web.search` is now provider-agnostic. Additive seam in `webSearch` (pure `web-search-fallback.ts` backend selection + SearXNG normalization, 19 tests; `searxng.server.ts` shell): FIRECRAWL_API_KEY → existing path byte-identical; else SEARXNG_URL → native self-host SearXNG floor; else a clear error naming both. Leaf web tool only (no registry/chokepoint edit); untrusted-input guardrail contract preserved. 2-lens adversarial review: **ship, 0 must-fix** (query-injection-safe via URLSearchParams; wire-shape-defensive). tsc 0 / 528 tests / build green. ◐ live-verify on a deployment with SEARXNG_URL. **Follow-up FIRECRAWL-FLOOR-b:** widen `research.server.ts` L319 web gate to also accept SEARXNG_URL so the /research pipeline inherits the floor (out of this row's scope; review-confirmed not a regression). Spec: docs/features/firecrawl-floor.md. [~85%] |
| 7 | ◐ | H1-TASKS | PRD to engineering task graph (DAG) | An approved PRD decomposes into ordered work units feeding BUILD | Decide | Tier 1 | 2026-06-21 | CONSUMPTION SLICE SHIPPED ◐ (Lane 2, 2026-06-21): generation was already shipped (`generateTaskGraph` writes seq+depends_on); this makes the stored DAG READABLE. New PURE `src/lib/task-graph.ts` (`sanitizeDependsOn`/`validateTaskGraph` cycle+integrity/`topoOrder` Kahn-total/`readyTasks`/`summarizeTaskGraph`; 13 tests) + RLS-scoped `getTaskGraph` read fn (`task-graph.functions.ts`, mirrors listTasks) + a calm `TaskGraphPanel` "Build readiness" readout (done/ready/blocked/%/remaining-hours + DAG integrity issues) on `/prds/$id`. 2-lens adversarial-review Workflow: 0 blockers, SHIP_WITH_FIXES - folded a stale-panel cache-invalidation defect + a role-color fix (--rose for failures) + the self-dep double-report. tsc 0 (my files) / eslint 0 / 551 tests (build red = pre-existing Stripe baseline). **◐ not ✅:** pure logic ✅ unit-verified; the panel renders on publish. Deferred (lane boundary): the task-graph->mission-steps bridge needs studio.* (Build lane). Distinct from #104 (spec gen) + row 148 (Decision Brain). |
| 8 | ◐ | W1-AUTO | Outcome-memory auto-trigger | Auto-compound on decision close: recordOutcome to rememberOutcome embeds the outcome into agent_memory | Launch | Tier 1 | 2026-06-21 | Specializes the shipped W1 (#90): the auto-trigger leg, already wired. Mark done on live-publish verify; NOT net-new scope (do not double-count). [~90%] |
| 9 | ◐ | O1 | Knowledge graph + query | Provenance walk: "why is this on the roadmap / being built?" back to root source signals | Foundational | Tier 1 | 2026-06-20 | SHIPPED v1 (DBR-1) at `/knowledge?tab=graph` as TWO views behind a toggle: a bounded **visual graph** (parallel/knowledge: `projectGraph` over `artifact_lineage`, color-by-type, click-to-trace, "as of" time axis, supersession seam; 13 tests) + a **List** lineage tree (`buildLineageTree`/`getLineageTree`; 6 tests). Deterministic, migration-free, fail-safe; tsc0 / build green / 19 graph unit tests green, ◐ live-verify on publish. Spec [`../features/knowledge-graph-explorer.md`](../features/knowledge-graph-explorer.md). Remaining: O3 drift/skill-packs + the bi-temporal supersession engine (DBR-1 write-path, AI/attended lane) [~90%] ⬆️ 2026-06-21: ABSORBED+ELEVATED into the Decision Brain (H1, row 148) as the DBR-1 read-side; not a standalone Sense item. Build Sequence #9. |
| 10 | ◐ | O3 | Fact currency/drift + skill packs | Flags stale facts on the provenance graph; exports versioned skill bundles over MCP | Foundational | Tier 1 | 2026-06-20 | drift/currency SLICE shipped (parallel/knowledge): deterministic `computeStaleness` over the graph (a fact is stale when its newest supporting edge is older than 90d), surfaced as a stale count + a dashed-ring marker on `/knowledge?tab=graph`; pure + 3 tests, tsc0 / build green. Remaining: outcome-driven contradiction drift (needs the supersession engine, AI lane) + the versioned skill-pack export over MCP (needs Q1) [~40%] ⬆️ 2026-06-21: ABSORBED into the Decision Brain (H1) as DBR-3; the contradiction-drift half is the supersession engine DBR-1.5 (Build Sequence #1). Not a standalone Sense item. |
| 11 | ◐ | Q1 / ENG-07 / F-MCP-V1 | MCP server + read-only externals | Other agents/tools use Cadence as a tool (signals/opps/PRDs read, append decision) | Interop | Tier 1 | - | Phases 1-3 done (foundation + dispatch + token UI); remaining Q2/Phase 4: full streamable-HTTP transport + external discovery [~75%] |
| 12 | ◐ | H2-WRITES | Outcome roadmap governed writes | Extends the H2 Now/Next/Later board with governed write paths | Decide | Tier 1 | - | GOVERNED commit path shipped (Lane 0, 2026-06-21 02:20): new pure `src/lib/roadmap-governance.ts` (`validateCommitment`/`isCommitmentGoverned`/`findGovernanceGaps`, 8 tests) + `commitRoadmapItem` server fn (a now/next/later commitment REQUIRES a declared outcome+measure, the anti-feature-factory governance) + `getRoadmap` now returns `governanceGaps`; the board routes outcome-save through the governed path, surfaces a gap-count header + an ember `VerdictChip` ("Needs outcome") on ungoverned cards, lenient drag move untouched. tsc/eslint/build/446-tests green; `impeccable` design pass (VerdictChip per the verdict-chip ruling, role-color law honored). ◐ live on publish. UI: /roadmap (product roadmap board). Remaining: agent autonomous-commit wiring + richer write surface [~70%] |
| 13 | ◐ | WM-F1 | Scope agent memory/runs/roster to workspace | The moat compounds per workspace/account (today user-scoped) | Monetization | Tier 1 | 2026-06-19 14:45 | CORE shipped (overnight cycle 27): nullable `workspace_id` on all 5 agent tables (backfilled) + recall RPCs rewritten with `for_workspace` (service-role-safe, null-tolerant) + a security fix (cross-user reflection leak) + recall threading + outcome/reflection tagging. Verified by a BEGIN..ROLLBACK dry-run on prod (0 nulls). NOT NULL + RLS swap + roster unique-swap + remaining insert-path tagging -> WM-F1b. Live recall-isolation activates on publish [~70%] |
| 14 | ◐ | WM-F1b | Agent-workspace hardening (RLS swap + universal insert tagging) | Closes WM-F1's transitional isolation gaps so ALL new agent memory is workspace-scoped at the RLS layer | Monetization | Tier 1 | 2026-06-19 20:10 | CORE shipped (overnight cycle 37): migration `20260619220000_wm_f1b_agent_workspace_hardening.sql` adds a `user_id`-fed BEFORE-INSERT trigger (tags every insert path, incl. the `seed_default_agents` SQL fn + the `memory.remember` tool) + a DUAL-KEY membership RLS (`auth.uid()=user_id AND is_workspace_member(workspace_id)`, strictly more restrictive, never widens/locks out) on all 5 agent tables; `memoryRemember` tags the active workspace. Verified by a BEGIN..ROLLBACK dry-run on prod (5 triggers / 5 policies / 0 old remaining; trigger fills ws; `seed_default_agents` OK; owner sees all 23 memory rows, no reader breakage; 0 owners lack membership). NOT NULL (founder-gated moat-cascade call) + unique-key swap (couples to WM-F8) + types regen (post-publish) → WM-F1c. Live on publish [~65%] |
| 15 | ◐ | WM-M2 | accounts table + billing/credit/decay migrations | Moves billing to the account; adds the credit-pool shell + 30d rolling decay | Monetization | Tier 1 | 2026-06-19 15:40 | CORE shipped (overnight cycle 28): migration adds `accounts`/`account_members`/`account_credits`/`credit_ledger` + `workspaces.account_id` (backfilled, NOT NULL via an owner_id auto-fill trigger) + 5-tier CHECK widen + 30d rolling decay (rolls off `last_used_at`, dormant) + `credits_enabled()`/`is_account_member`/`ensure_user_default_account`; `billing.functions.ts` reads the plan from the account (shim fallback). Dry-run-verified on prod (5 accounts, all 7 ws linked, 0 nulls, rolled back). Live on publish. Types regen + account-aware webhook tracked with WM-M3 [~75%] ⚖️ BOUNDARY 2026-06-21: the tenancy scaffold (accounts/account_members/workspaces.account_id/decay) STAYS product-infra; the account_credits/credit_ledger pool + Stripe relocation half is Lovable-owned. |
| 16 | ◐ | WM-F3 | RBAC enforcement | Real owner/admin/member/viewer permissions for teams | Monetization | Tier 1 | 2026-06-19 | CORE shipped by a parallel session (`2b1c429591`): SECURITY DEFINER RBAC helpers (has_workspace_role / has_account_role) + role-based RLS policies + owner demotion trigger + TypeScript role helpers. Verified (by that session): tsc/test 8/8/build green. **BUT the migration would have FAILED to apply on publish** (it used `create policy if not exists` ×4, which is invalid Postgres syntax — CREATE POLICY has no IF NOT EXISTS clause — returning `ERROR 42601` and blocking the whole migration run). **FIXED cycle 41 (`70c0ca7db5`):** the four policy creates are now `drop policy if exists` + `create policy` (valid + idempotent) and the owner-demotion trigger is drop-then-create guarded. Confirmed on the live prod DB: the `if not exists` form errors 42601; the corrected migration applies cleanly (all RBAC helpers + 5 policies + the trigger created; rolled back, prod's real "ws owner manage" policy restored). Owner-lockout data-checked: all 7 workspaces have their owner as a `workspace_members(role='owner')` row, so the new role-keyed policy locks no one out. Live enforcement activates on founder's next publish. Spec: docs/features/rbac.md [~75%] |
| 17 | ◐ | WM-M5 | Tier limit gates (DB triggers) | Enforce per-product/workspace caps at the DB (clients write direct, so triggers guard) | Monetization | Tier 1 | 2026-06-19 19:30 | CORE shipped (overnight cycle 36): migration `20260619200000_wm_m5_tier_limit_gates.sql` adds `tier_product_limit`/`tier_workspace_limit` (mirror entitlements.ts) + `enforce_product_limit`/`enforce_workspace_limit` BEFORE-INSERT triggers (SECURITY DEFINER; product count is account-WIDE so pooled workspaces can't bypass; first workspace never gated) + new `src/lib/limits.functions.ts` (`assertCanCreateProduct` nice-path + typed `LimitReachedError`+upsell) wired into `createProject`. DORMANT behind `limit_gates_enabled()` (= false) like credits/decay; the founder flips it once a live upgrade path (Stripe) exists. Behaviorally dry-run-verified on the live prod DB (free blocked at product #3 AND workspace #2 with the exact trigger messages; pure limit fns correct), rolled back. 252/252, tsc/build/lint/humanization green. Live gate activates on publish + the flag flip [~75%] ⚖️ STAYS (2026-06-21): limit enforcement is product-infra; prices + the live limit_gates_enabled() flip are Lovable/founder. |
| 18 | ◐ | WM-F2 | Account-level memory pooling (paid) | Paid accounts compound memory across workspaces (the flywheel) | Monetization | Tier 1 | 2026-06-19 21:45 | CORE shipped (overnight cycle 40): migration `20260619240000_wm_f2_account_memory_pooling.sql` adds `is_account_member()` helper + rewrites `match_agent_memory` / `recent_agent_reflections` to optionally span account workspaces (for_account param). `memory.server.ts` `recallMemoryRefs` now accepts optional `accountId` in opts; passes `for_account` when pooling, otherwise `for_workspace` (backward compatible). Gated at RLS: is_account_member checks before spanning. 270/270 tests pass, tsc/build/lint green. **FIXED cycle 40 (`a368edf78a`):** the cycle-40 ship was tsc/build-green but regressed the WM-F1 baseline and was dormant: (1) a cross-user READ hole (RPCs filtered `user_id = for_user` not `coalesce(auth.uid(), for_user)`, reverting the pin WM-F1 added to close exactly that); (2) recalled EXPIRED memory (dropped the expiry filter); (3) dropped GLOBAL-scope memory (mandatory `agent_slug=` equality); (4) dropped the per-row `is_workspace_member` guard; (5) return-shape regressions (3 cols + distance-as-similarity; reflections lost importance/metadata + timestamptz→timestamp); (6) "staged but never driven" (pooling gated on `opts.accountId`, which no caller passes). The fix restores every WM-F1 invariant and resolves the pool account INTERNALLY from the workspace's account tier (entitlements.ts = single source of truth), so pooling is actually DRIVEN with no call-site changes. **Behaviorally dry-run-verified on the live prod DB** (stubbing unpublished WM-M2/F1 deps): paid account pools across BOTH workspaces, single-workspace unchanged, a different account recalls NOTHING of the paid account's memory; rolled back, prod untouched. tsc/build/lint/270 tests green. Live pooling activates on the founder's publish [~85%] |
| 19 | ◐ | WM-F9 | Isolation audit + scope leak fixes | Close cross-member leaks (meetings/notes/briefs/chat) before invites | Monetization | Tier 1 | 2026-06-19 18:33 | CORE shipped (overnight cycle 35): migration `20260619190000_wm_f9_isolation_scope.sql` adds workspace_id + deterministic backfill + a NEW.user_id BEFORE-INSERT trigger (service-role-safe) + NOT NULL + index + DUAL-KEY membership RLS (`auth.uid()=user_id AND is_workspace_member(workspace_id)`; keeps user-private per the acceptance, never widens) to **10 tables**: the named 4 (meetings/notes/daily_briefs/copilot_messages) + the 6 the adversarial audit found (prototypes + 3 children, scheduler_proposals, ritual_sessions). Public-share policies preserved; sync_mappings documented-deferred; CASCADE delete-confirm copy fixed (AppShell). Behaviorally dry-run-verified on the live prod DB (0 nulls, NOT NULL, trigger fill, old policies removed, dual-key created), rolled back. 239/239, tsc/build/lint/humanization green; 3-lens review (reader-breakage clean), all real fixes folded. Live isolation activates on publish. Follow-up WM-F9b: active-workspace read-filtering + sync_mappings verdict [~80%] |
| 20 | ◐ | WM-M15 | Margin levers (cost-aware routing) | Keeps credits margin-positive (no self-serve BYOK to lean on) | Foundational | Tier 1 | 2026-06-19 18:09 | CORE shipped (overnight cycle 34): pure `src/lib/ai/routing.ts` (blendedPrice / cheapestLiveModel / costRoutedModel) wired at the AI chokepoint behind the `AI_COST_ROUTING` env flag (default OFF = byte-identical to today). Narrow safe routable set {brief, scheduler, test} + a tier guard that never downgrades a deliberate reasoning/premium/code/vision model. 5-lens adversarial review caught a real quality-gate hole and was fixed: judge (the Critic wedge), eval (benchmark), embed (chat-model trap) excluded; the credit pre-check + debit now track the model actually run. 12 routing tests, 239/239, tsc/build/lint green. **Cache half ◐ shipped cycle 38** (`ai_response_cache` table + content-hash lookup + chokepoint read/write, 7-day TTL, conservative surface whitelist brief/scheduler/test only). **Cache half FIXED cycle 39** (`7897085004`): the cycle-38 cache was tsc/build-green but shipped 3 runtime-fatal bugs, all corrected + prod-dry-run-verified: (1) the migration's `WHERE expires_at > now()` index predicate would have FAILED to apply on publish (Postgres ERROR 42P17, now() is STABLE not IMMUTABLE); (2) the cache hit never skipped the provider call (the `if(!cacheHit)` guard wrapped only the write), so it saved zero COGS; (3) readCache passed the literal string `"now()"` so every read errored and the cache always missed. Live routing + cache both activate on founder's `AI_COST_ROUTING` flag. Honest scope: both together complete the cost-efficiency story; initial margin impact modest (conservative whitelist, guardrails required) [~85%] ⚖️ STAYS, do NOT freeze (2026-06-21): cost-aware routing + response cache are whole-platform COGS levers at the chokepoint, NOT credit-engine scope; re-categorized out of Credit. |
| 21 | ◐ | WM-F4 | Ownership transfer | Transfer an account/workspace; unblocks owner-leaves | Monetization | Tier 1 | 2026-06-19 22:40 | CORE shipped (overnight cycle 43, `345276a5aa`): migration `20260619250000` adds `workspace_audit_log` (member-read RLS, writes via RPC only) + a SECURITY DEFINER `transfer_workspace_ownership` RPC (atomic: reassign owner_id, flip member roles new→owner / old→admin, write audit row) + a `transferWorkspaceOwnership` server fn. Reassign-before-demote ordering respects WM-F3's demotion trigger. **Behaviorally dry-run-verified on prod:** transfer flips owner_id + both roles + writes audit `{from,to}`; non-owner caller blocked; non-member target blocked; rolled back, prod untouched. tsc/build/lint/270 tests green. **UI shipped (FE cycle 1, 2026-06-20):** the `MembersCard` in Settings > Workspace lists members (identity via the new membership-gated SECURITY DEFINER RPC `workspace_members_with_identity`, migration `20260620213000`, since profiles RLS is own-row-only) and gives the OWNER a per-member "Make owner" action behind an inline two-step confirm (it demotes the owner to admin, so not one-click), wiring `transferWorkspaceOwnership`; also wires `removeWorkspaceMember` and the previously-deferred members list. Built per the frontend build protocol (design-taste-frontend + impeccable). **Role management + owner-only correction (FE cycle 3, 2026-06-20):** added `changeWorkspaceMemberRole` (the role chip becomes an inline `<select>` on manageable rows) and CORRECTED a cycle-1 silent-failure - member management is OWNER-ONLY (the WM-F3 `workspace_members` policy is `for all` + owner-scoped, so an admin's remove deleted 0 rows but PostgREST reported success); all management (role/remove/transfer) is now gated to `isOwner`, and `removeWorkspaceMember`/`changeWorkspaceMemberRole` use `.select()` + a 0-row throw so an RLS-blocked write fails loudly. tsc 0 / eslint 0 / build ✓. **◐ not ✅:** wired + gate-green + adversarially-reviewed runtime-safe + driven, but renders on the founder's publish and the identity RPC's live dry-run is pending (Supabase MCP unauthorized this session, flagged for publish-verify). UI: Settings > Workspace > Members [~95%] |
| 22 | ◐ | WM-F5 | Invites (account/workspace) | Add teammates (no invite flow today) | Monetization | Tier 1 | 2026-06-19 23:03 | BACKEND shipped (overnight cycle 44, `d0e7300c33`): migration `20260619260000` adds `workspace_invitations` (token default, role admin/member/viewer, status, 7-day expiry, manager-only RLS via `can_manage_workspace`, no invitee read = no pre-accept leak) + a SECURITY DEFINER `accept_workspace_invitation(_token)` RPC (validates pending+unexpired, binds the token to the invited email so a leaked token can't be redeemed by another account, upserts the membership with the invited role, single-use under a row lock). New `email.server.ts` (no-op `sendInviteEmail` returning the copy-paste link; outbound email founder-gated). New `inviteMember`/`listInvitations`/`revokeInvitation`/`acceptInvitation` server fns. **Behaviorally dry-run-verified on prod:** invite→accept added the user with role=member + flipped the invite to accepted; re-accept, wrong-email, and expired were all rejected; rolled back, prod untouched. tsc/build/lint/270 tests green. **Invite UI wired (front-end pivot, cycle 53):** a `TeamCard` ("Invite teammates") in Settings > Workspace wires `inviteMember`/`listInvitations`/`revokeInvitation` - invite by email + role, copy the join link (outbound email is a no-op), see invitations + their status, revoke a pending one; built on the Ember system, ran `impeccable` (passed the bans, renamed the list header to match what the fn returns). tsc 0 / eslint 0 / build ✓ / 305 tests. **◐ not ✅:** wired + gate-green + driven, but renders on the founder's publish (the WM-F5 migration + `can_manage_workspace` apply then) and was not render-verified locally. **Accept route wired (front-end pivot, cycle 54):** a standalone `/join/$token` route (`src/routes/join.$token.tsx`) redeems the token via `acceptInvitation` for a logged-in invitee (single-run guarded, StrictMode-safe) and shows a clear log-in/sign-up prompt for a logged-out one, so the invite→accept loop is now complete; ran `impeccable` (moss success state per the role-color law). tsc 0 / eslint 0 / build ✓ / 307 tests. **◐ not ✅:** wired + gate-green + driven, renders on the founder's publish (the WM-F5 migration applies then), not render-verified locally. The members list deferred here SHIPPED in FE cycle 1 (the `MembersCard`). **Redirect-after-login SHIPPED (FE cycle 4, 2026-06-20):** `/login` + `/signup` gained an open-redirect-guarded internal-only `next` param; the join page's Log in / Sign up links pass `next=/join/<token>`, so a logged-out invitee lands back on the accept page after email auth (a fresh signup accepts before onboarding since /join is a top-level route). OAuth still returns to `/` (founder-gated redirect_uri, untouched) - email path fully fixed. tsc 0 / eslint 0 / build ✓. The whole invite flow (invite -> share link -> log in -> accept) is now wired end-to-end. UI: Settings > Workspace > Invite teammates; invitee opens /join/<token> [~97%] |
| 23 | ◐ | WM-M4 | Runtime credit seam (dormant) | The seam the credit engine plugs into; credits-only (no self-serve BYOK) | Monetization | Tier 1 | 2026-06-19 16:17 | CORE shipped (overnight cycle 30): `CreditExhaustedError` + cached `creditsEnabled()` + `resolveCreditAccountId` + `assertAccountCredits` (pre-call) + `debitAccountCredits` (post-call) wired into callModel + callModelStream at the chokepoint; gated behind `credits_enabled()` (dormant no-op, zero behavior change). tsc/build green, 201/201 tests, eslint clean. WM-M12 fills the bodies (atomic draw-down). Live debit on publish + flag flip [~75%] ⚖️ STAYS (2026-06-21): the runtime credit seam in runtime.server.ts stays; the debit ENGINE that fills it is Lovable's. |
| 24 | ⬜ | WM-F7 | Settings IA (Account/Workspace/Personal) | A clear rubric for where each setting lives | Monetization | Tier 1 | 2026-06-19 | effort M; needs WM-M2, WM-F3 |
| 25 | ◐ | WM-F8 | Workspace switch hardening | No stale-data flash on switch; agents/memory switch too | Monetization | Tier 1 | 2026-06-19 23:18 | CORE shipped (overnight cycle 45): `setActiveWorkspaceId` in `src/hooks/use-workspace.tsx` now clears every workspace-scoped TanStack Query cache entry on a real switch (`queryClient.removeQueries({predicate})`, active + inactive) so no previous-workspace data flashes before the refetch; a pure, unit-tested allowlist (`src/hooks/workspace-query-scope.ts` + `.test.ts`, 4 tests) preserves the user/account-global queries (workspaces list, profile, billing, connections, calendar-connections, integrations, api-keys, mcp-tokens) and fails safe to "workspace-scoped" for any unlisted/odd key. Driven by the AppShell switcher (`AppShell.tsx:516`) + create/delete paths. Adversarial review: no stale-closure refetch (keyed queries refetch on key change; server-fn queries hold no client workspace closure). tsc 0 / eslint 0 (my files) / build ✓ / `bun test` 274/274. **◐ not ✅:** correct + gate-green + driven, but the full user-visible "no cross-workspace flash" payoff scales as reads become workspace-filtered server-side (most server fns are still user-scoped today; WM-F9b), and the two-seeded-workspace Playwright check needs a publish. Defense-in-depth follow-up WM-F8b: thread `activeWorkspaceId` into the un-keyed query keys. UI breadcrumb: AppShell > workspace switcher [~70%] |
| 26 | ⬜ | WM-M9 | Remove BYOK from self-serve (enterprise-only) | Credits-only self-serve; retire the user-key path; model-agnostic routing (our keys) stays | Monetization | Tier 1 | 2026-06-19 | effort S; needs WM-M1 ⚖️ STAYS (2026-06-21): BYOK removal at the chokepoint is product-infra, attended; sequence with Lovable's pricing surfaces. |
| 27 | ◐ | WM-F6 | Move product between workspaces | Relocate a product + its data across workspaces | Monetization | Tier 1 | 2026-06-19 23:42 | CORE shipped (overnight cycle 46): migration `20260619280000_wm_f6_move_product.sql` adds a transactional SECURITY DEFINER `move_product(_product_id, _dest_workspace_id)` RPC that, after a guard (caller is owner/admin of BOTH workspaces via WM-F3 `can_manage_workspace` + both in the SAME account via WM-M2 `account_id` + a `FOR UPDATE` lock), atomically reassigns `workspace_id` on the product (`projects`) + 13 product-scoped tables: 10 direct children (signals/themes/opportunities/prds/docs/tasks/decisions/conversations via `project_id`-or-`product_id`; notes/prototypes via `project_id`; ai_events/connection_bindings/rag_chunks/studio_changesets via `product_id`) + 3 grandchildren scoped through their parent (doc_versions→docs, messages→conversations, learnings→opportunities/prds). Because every table is read-gated by `is_workspace_member(workspace_id)`, the reassignment IS the move (access follows). Memory stays at the workspace (not moved). New `moveProduct` server fn in `projects.functions.ts`. **Verified live (BEGIN..ROLLBACK on prod, stubbing the unpublished WM-M2/F3/F9 deps):** moving a real product (9 signals/2 prds/10 tasks/3 decisions/8 messages) relocated ALL of them to the dest with **0 orphans left in src**, and both guards fired (cross-account blocked; non-manager blocked); rolled back, prod untouched. tsc 0 / eslint 0 / build ✓ / `bun test` 274/274. **◐ not ✅:** RPC + server fn built + behaviorally dry-run-verified, but the **Settings UI affordance (move action) is deferred** to the design pass (founder ruling), and live move activates on publish (depends on unpublished WM-M2/F3/F9). Follow-up WM-F6b: scope `artifact_lineage` + other generic-provenance tables (generic artifact ids; deferred to avoid a wrong remap). **UI shipped (FE cycle 2, 2026-06-20):** a "Move to workspace" `DropdownMenuSub` in each product's actions menu (the sidebar product list ⋯ menu, beside Rename/Delete) lists the user's other workspaces; picking one opens a non-destructive confirm (move is reversible) then calls `moveProduct`, clears the now-moved active product, and refreshes. Hidden when there is only one workspace. Built per the frontend build protocol (emil-design-eng for the submenu-then-confirm interaction + impeccable gate). tsc 0 / eslint 0 / build ✓. Honest limit: destinations are not yet filtered to the same account (the client `Workspace` type lacks `account_id`), so a cross-account pick is offered then rejected by the RPC guard with a toast (refinement WM-F6c: add account_id to the hook). ◐ live on publish. UI: the sidebar product ⋯ menu > Move to workspace [~90%] |
| 28 | ⬜ | DBR (H1) | The Decision Brain (typed decision knowledge graph) | Evolve flat vector recall into a typed, bi-temporal, auto-built decision graph (nodes: signal/opportunity/assumption/decision/PRD/outcome; edges: cites/supersedes/validates/contradicts) with an Obsidian-style visual graph view; signature mechanic = outcome-labeled supersession | Foundational | Tier 1 | 2026-06-20 | **HORIZON BET, enrich first** (schema/sequencing/lane). Spec [`../features/decision-brain.md`](../features/decision-brain.md); strategy [`../strategy/horizon-bets.md`](../strategy/horizon-bets.md). Absorbs + elevates O1 (knowledge graph, partial) + O3 (drift/skill-packs, pending). DBR-0 slice DONE 2026-06-20 ◐ (a code read corrected the plan: the outcome loop was already closed via `recordOutcome`; the real first win was giving the **Critic** decision precedent: `formatDecisionPrecedent` + a workspace-outcomes query in `runCritic`; tsc 0 / 303 tests / build green; live-verify on publish). Increment 1 = Ambient Precedent ([`../features/ambient-precedent.md`](../features/ambient-precedent.md)) is **SHIPPED to `main` (◐), 2026-06-20 cycle 55, subagent-driven, all 6 tasks** - the proactive precedent nudge over our existing outcome memories. Done: T1 outcome titles -> T2 semantic engine + pure ranker (✅ unit-verified, 3 tests) -> T3 Critic uses the engine, DBR-0 recency deleted -> T4 getDecisionPrecedent fn -> T5 PrecedentNudge UI -> T6 nudge wired into the opportunity + spec seams (3 seams now live: Critic + opportunity + spec, ◐ render-on-publish). tsc 0 / build ✓ / 322 tests / eslint 0; opus whole-branch review "ready to merge". These are TASKS under this umbrella row, NOT new register rows, so the at-a-glance counts are unchanged. Build record: SSOT §3 + §6. The deeper graph (DBR-1+) stays ⬜ pending enrichment. [increment 1 shipped ◐] |
| 29 | ⬜ | F-IA-V4 | Collapse IA to 7 surfaces | Route consolidation + redirects + vocab enforcement; one coherent product | Cockpit | Tier 2 | - | design-adjacent; bundled with the founder-gated design pass |
| 30 | ⬜ | F-IA-TODAY-BRIEFING | Merge Today + Briefing | One morning surface, not two | Cockpit | Tier 2 | - | bundled with the design/IA pass |
| 31 | ⬜ | F-IA-CULL-CALDOCS | Cull /calendar /meetings /docs /sync from nav | De-clutter the operator nav (data kept) | Cockpit | Tier 2 | - | bundled with the design/IA pass |
| 32 | ⬜ | F-IA-AGENTS-TABS | Fold /prompts + /agents into one route | Agents live in one place | Cockpit | Tier 2 | - | bundled with the design/IA pass |
| 33 | ◐ | RUNAWAY-INCIDENTS | Runaway missions in the persistent Incidents log | Wires the RUNAWAY-DETECT detector into the operator's "what went wrong" record: a spinning mission becomes a durable `runaway` incident (not only the silent Missions glance) | Governance | Tier 3 | 2026-06-21 02:35 | NEW row (Lane 1; the decision panel's runner-up, completes RUNAWAY-DETECT's "operator surface" follow-up). `getIncidentsInternal` (`incidents.functions.ts`) gains a `runaway` source: scans the 200 most recent user missions (no age floor, so an old still-spinning mission is caught), folds via the NEW shared pure `buildMissionStats` (extracted from `getRunawayMissions` so the two read paths cannot drift), runs `assessMissions`, emits ONLY `runaway` severity (terminal-but-breached = `watch`, excluded, so no flood); stable id `runaway:<missionId>`. `IncidentKind` widened (+`runaway`) forces a `KIND_LABEL` entry (tsc-enforced). Read-only, user-scoped (`.eq('user_id')` + `.in('mission_id', ids)` over own ids), no writes/AI/spend/loop edit. 2-lens adversarial review: both **SHIP**, refactor PROVEN behaviorally equivalent, no correctness/security/leak/flood defect (the only notes: an intentional graceful-degradation error-tolerance matching the aggregator + an inert negative-attempts clamp, both already documented). tsc 0 / eslint 0 / build ✓ / `bun test` 475 (8 new). **◐ not ✅:** logic ✅ unit-verified (incl. a thenable-mock incidents test); the live missions read live-verifies on publish. Spec: [`../features/runaway-detection.md`](../features/runaway-detection.md). UI: `/govern?tab=incidents`. _Counts not recomputed (parallel sessions active)._ [~80%] |
| 34 | ◐ | H2-AUDIT | Roadmap-decision audit ("why is this on the roadmap") | Append-only trail of every roadmap commitment event (who/when/bucket + the outcome promised at that time) | Decide | Tier 3 | 2026-06-21 | SHIPPED backend (Lane 0, 2026-06-21 02:42): closes the `considerations.md` PM P0/P1 gap "'Why is this on the roadmap?' answerable with cited evidence." New migration `20260621024000_h2_roadmap_audit.sql` (append-only `roadmap_audit`: insert-own + read-own-or-workspace RLS, NO update/delete = tamper-evident; FK to opportunities ON DELETE CASCADE so the audit never outlives its data; auto-covered by DATA-RETENTION-b's workspace_id erasure sweep) + best-effort audit writes in `commitRoadmapItem` (records the declared outcome AT COMMIT TIME = point-in-time evidence) and `updateRoadmapItem` (bucket moves) + new `getRoadmapHistory` read fn + pure `src/lib/roadmap-audit.ts` (`buildAuditInsert`/`summarizeRoadmapHistory`, 5 tests). tsc/eslint/build/451-tests green; migration dry-run-verified on prod (BEGIN..ROLLBACK: table+RLS+2 policies+FK CASCADE confirmed). ◐ not ✅: backend built + dry-run-verified, renders on publish; the "why is this here" UI surface shipped as H2-AUDIT-UI (row 8c). Detail: [`../features/h2-writes.md`](../features/h2-writes.md) [~85%] |
| 35 | ◐ | H2-AUDIT-UI | Roadmap "why is this here" history popover | A reveal-on-demand popover on each roadmap card showing its decision history | Decide | Tier 3 | 2026-06-21 | SHIPPED (Lane 0, 2026-06-21 02:58): surfaces the H2-AUDIT backend in the product. New `src/components/product/RoadmapHistory.tsx` - a quiet "why" trigger on committed cards opens a Popover that LAZILY (`enabled: open`, no N-query fan-out) reads `getRoadmapHistory` + renders `summarizeRoadmapHistory` (the live "why" = newest committed outcome) + a hairline-separated event timeline (committed/moved · bucket · date, with the outcome promised at that time). Engine-Room doctrine (calm front, reveal on demand; name the outcome) + Ember Editorial (restrained tinted neutrals, `·` separators, no banned pattern, no fancy unicode); graceful empty/pre-publish state. tsc/eslint/build/469-tests green. ◐ live on publish (renders real history once audit rows exist; not render-verified locally). UI: /roadmap > a committed card > "why". Detail: [`../features/h2-writes.md`](../features/h2-writes.md) |
| 36 | ◐ | ENG-06 / F-GOV-COST-SURFACE | Cost-per-outcome chip + unit-economics roll-up | "What you got for what you spent" chip on the front + full per-agent telemetry behind the Engine Room | Cockpit | Tier 3 | 2026-06-17 | B1 + B3 built; **B2 (Missions manager's glance) shipped FE cycle 5 (2026-06-20):** a calm one-line cost-per-outcome glance (`MissionsCostGlance`, new `src/components/cockpit/MissionsCostGlance.tsx`) on the Missions header, reusing the shipped `getCostPerOutcome` (shares the `["cost-per-outcome"]` query cache with Today's chip = no extra fetch), silent on a quiet week. Engine-Room-compliant (one outcome-framed line, not a hero-metric panel; impeccable hero-metric ban applied). Minor: ~similar to the Today chip but kept a dedicated component to leave the Today surface untouched (collision-safe). tsc 0 / eslint 0 / build ✓. ◐ live on publish. UI: Missions header. All 3 B-slices now built [~92%] |
| 37 | ⬜ | AMBIENT-ARC | Ambient autonomy arc (Trust Dial) | Per-agent autonomy arc incl. Ambient + suggested promotion on the Agents tab | Governance | Tier 3 | - | recon: shipped as `TrustDial.tsx`; see trust-and-autonomy.md §7 |
| 38 | ◐ | F3 | Continuous discovery feed | Always-fresh, per-product clustered signal feed (`/product?tab=signals`) | Sense | Tier 3 | 2026-06-18 | remaining: auto-cluster cron (F3-CRON shipped gated off; activation founder-gated, commits recurring AI spend) [~80%] |
| 39 | ◐ | D4 | Cancel / replay-and-branch / checkpoints | Brake-pedal cancel + replay a finished mission (optionally a different model) with a branch link | Decide | Tier 3 | 2026-06-18 | remaining D4b: rich side-by-side checkpoint-diff; pending publish + live verify [~80%] |
| 40 | ◐ | K2 | Rollback triggers + one-action revert | One-action non-destructive revert-to-revision in the Build Changes tab | Build | Tier 3 | 2026-06-18 | remaining K2b: `studio.revert` agent tool (needs `agent_tools` migration) + feature-flag kill; parked on a backup branch, issue #4 [~70%] |
| 41 | ◐ | BLD-05 | Inspector gate (tests + preview before merge) | A test + preview bar on the PR/Checks tab before the operator clears the merge gate (warn-only on no tests) | Build | Tier 3 | 2026-06-18 | wiring + unit tests built; live-verify pending; on branch `bld-05-inspector-gate` (PR pending) [~80%] |
| 42 | ◐ | F-BUILDER-MULTIFILE | Scoped multi-file build | Pre-declared touch list + max-N files; safer multi-file edits | Build | Tier 3 | 2026-06-19 | Built: migration + scope server fns + Changes-tab Scope UI + tested glob/policy/merge logic; tsc/lint/build green, 24 tests. ◐ live-verify pending publish + migration sync. UI: Build > session > Changes > Scope |
| 43 | ◐ | LRN-04 | Product Memory consult/write visibility | Surfaces the wired memory loop per mission | Launch | Tier 3 | - | per-mission surfacing is N3 (shipped) [~70%] |
| 44 | ◐ | LCH-01 / L1 | Launch-kit mission | Turns a shipped changeset into 5 human-approved artifacts (changelog/blog/email/social/docs) | Launch | Tier 3 | 2026-06-18 | remaining: a launch MISSION template + governed outbound send (never auto-sends) [~40%] |
| 45 | ⬜ | L2 | Customer pages / announcements | Public-facing announcement pages (`p.$slug`) with approval to publish | Launch | Tier 3 | - | M2 milestone |
| 46 | ⬜ | M1 / LRN-01 | Support triage loop | Tickets to drafted replies to bug clusters to signals; support feeds back into Discover | Launch | Tier 3 | - | M2; needs an inbound channel |
| 47 | ◐ | R4 | Settings expansion | Self-serve control: budgets, guardrails, health, prefs, admin | Cockpit | Tier 3 | - | Plan tab shipped; rest partial [~30%] ⚖️ SPLIT 2026-06-21: budgets/guardrails/health/prefs/admin STAY; the Plan/billing/credit/pricing TAB is Lovable-owned. |
| 48 | ◐ | U6 | Full data-portability / export wizard | Exports the whole workspace as one RLS-scoped JSON with per-section selection | Interop | Tier 3 | 2026-06-20 00:50 | U6-AUDIT backend shipped (overnight cycle 48): migration `20260620010000_export_log.sql` adds an append-only `export_log` audit table (insert-own + read-own-or-workspace RLS, NO update/delete → tamper-evident; `is_workspace_member`-gated read so an owner audits all data egress); `exportProduct`/`exportWorkspace` write one audit row per export (who/kind/target/sections/row_count, best-effort + try/catch so the audit never fails the export); new `listExportLog` read fn. Behaviorally dry-run-verified on prod (BEGIN..ROLLBACK): both insert shapes apply, user_id defaults from the JWT, kind CHECK rejects bad values, RLS on, policy set is exactly insert+select (append-only confirmed). tsc 0 / eslint 0 / build ✓ / 287 tests. **History UI wired (front-end pivot, cycle 52):** the DataExportCard now shows a "Recent exports" list via `listExportLog` (date+time · kind · row count), refetched after each export; built on the Ember system, ran `impeccable` (passed the bans). tsc 0 / eslint 0 / build ✓ / 305 tests. **◐ not ✅:** wired + gate-green + driven, but renders on the founder's publish (the `export_log` migration + live rows apply then) and was not render-verified locally. UI: Settings > Data > Export your data > Recent exports [~98%] |
| 49 | ◐ | FND-0.7 | Prompt-injection defense | XML-tags + escapes untrusted tool/RAG output; guardrails support injection rules | Governance | Tier 3 | - | remaining: a learned injection classifier + hard quarantine (regex-only today) [~55%] |
| 50 | ◐ | FND-0.5 | Agent blast-radius limits | Per-agent tool allow-list + scope so an agent cannot reach beyond its remit | Governance | Tier 3 | - | next autonomous pick: pre-filter high-risk tools + product-scope the allow-list [~55%] |
| 51 | ◐ | DATA-RETENTION | AI-telemetry retention purge (dormant) | Bounded retention for unbounded per-call telemetry (ai_events/prompt_runs/tool_calls); data-minimization for enterprise/GDPR | Data/Privacy | Tier 3 | 2026-06-20 00:20 | CORE shipped (overnight cycle 47): migration `20260620000000_data_retention.sql` adds `data_retention_enabled()` (dormant flag, false) + a SECURITY DEFINER `purge_old_telemetry(_older_than_days int default 180)` that self-gates on the flag, applies a hard 30-day floor (a mis-call can't purge recent data), and deletes ai_events/prompt_runs/tool_calls older than the window (no inbound FKs - clean deletes); execute revoked from public/anon/authenticated, granted to service_role only. New `retention-tick` hook (`src/routes/api/public/hooks/retention-tick.ts`, requireHookCaller-gated, pre-migration-tolerant). **Behaviorally dry-run-verified on prod** (BEGIN..ROLLBACK, backdating real rows): dormant→no-op, active purge(180) deletes a 200d row + keeps a 50d row, floor purge(0) floors to 30 + deletes the 50d row; rolled back. tsc 0 / eslint 0 / build ✓ / 287 tests. **◐ not ✅:** built + dry-run-verified, but stays dormant until the founder flips `data_retention_enabled()` AND wires a pg_cron/scheduler to POST the hook; live purge activates then. Follow-up DATA-RETENTION-b: the right-to-be-forgotten per-account/workspace erase cascade (destructive + policy-coupled, deferred). UI breadcrumb: none (cron + DB plumbing) [~70%] |
| 52 | ◐ | DATA-RETENTION-b | Right-to-be-forgotten erase cascade (dormant) | Per-workspace/account hard-delete of all tenant data for GDPR/CCPA right-to-erasure; the destructive other half of DATA-RETENTION's time-based purge | Data/Privacy | Tier 3 | 2026-06-21 01:29 | CORE shipped (Lane 0, 2026-06-21 01:48): migration `20260621012900_data_retention_b_right_to_erasure.sql` — service-role-only, flag-gated (`right_to_erasure_enabled()` default false) `forget_workspace`/`forget_account` + read-only `erasure_residue` + pure tested `summarizeErasure` receipt (`src/lib/compliance/erasure.ts`). FK-order-agnostic dynamic fixed-point delete over all 61 `workspace_id` tables (catalog-sourced, `quote_ident`'d, `_col`-whitelisted, tenant-id-bound → single-tenant by construction; primitive self-gates on the dormant flag too); explicitly erases the 6 SET-NULL agent-moat tables + clears the 22 RESTRICT children a naive workspace delete would choke on. 4-lens adversarial review (4 must-fix folded); zero-destruction live dry-run on prod (random uuid, BEGIN..ROLLBACK) clean; tsc/eslint/build/398-tests green. **◐ not ✅:** tenant-data erasure, not person-level — auth.users/user_id-only rows/Storage objects/account-scope residue are documented DATA-RETENTION-c scope; destructive FK-order dry-run + live activation are publish-verify (flag flip + operator invoke). Completes the data-governance triad (DATA-RETENTION + U6-AUDIT + SUBPROC-DISCLOSURE). Detail: [`../features/right-to-erasure.md`](../features/right-to-erasure.md) [~75%] |
| 53 | ◐ | SUBPROC-DISCLOSURE | Sub-processor disclosure registry | Canonical list of the third parties that process customer data (model providers + gateway + infra) for enterprise security reviews + GDPR Art. 28 | Data/Privacy | Tier 3 | 2026-06-20 01:30 | NEW row (overnight cycle 49, `considerations.md` Data/Privacy P1; completes the data-governance triad with DATA-RETENTION + U6-AUDIT). New pure `src/lib/compliance/subprocessors.ts`: a `SubProcessor` type + a curated infra list (Lovable gateway / Supabase / Cloudflare) + `modelProviderSubprocessors` DERIVED from the live model catalog so the disclosure cannot drift (a provider is `active` only when it has a `live` model; adapter-ready BYO providers listed inactive; `ollama` excluded as self-hosted = never a third-party processor) + `allSubprocessors`/`activeSubprocessors`; new `getSubprocessors` GET server fn (`compliance.functions.ts`). `PROVIDER_META: Record<Model['provider'],…>` makes tsc FAIL if a catalog provider lacks a disclosure entry (no silent staleness). tsc 0 / eslint 0 / build ✓ / 298 tests (11 new: shape, unique ids, infra-active, ollama-never-listed, active-only-when-live against an injected catalog, ordering, active=subset-of-all). **◐ not ✅:** the pure registry + read fn are built + unit-verified, but the trust-page UI and the legal-reviewed copy/regions/DPA are deferred to the design/legal pass, and the GET fn was not behaviorally run unattended (live-verify on publish). **UI wired (front-end pivot, cycle 51):** a calm-front `SubprocessorsCard` ("Where your data goes") in Settings > Data renders the `getSubprocessors` list (name + category + purpose + data categories), Engine-Room-compliant (outcome-named, opt-in Settings surface, no jargon). tsc/eslint/build green; ◐ not render-verified locally (renders on the founder's publish). Remaining: the public trust page + legal-reviewed copy/DPA. UI: Settings > Data > Where your data goes [~75%] |
| 54 | ◐ | APP-HEALTH | App-level health/readiness endpoint | A public liveness/readiness signal for uptime monitors + load balancers (distinct from the migration-drift `checkBackendHealth`) | Reliability | Tier 3 | 2026-06-20 02:10 | NEW row (overnight cycle 50, `considerations.md` SRE lens P0 "App-level monitoring + alerting"; confirmed unbuilt - `health.functions.ts` is only a migration-drift checker per SSOT finding 4). New route `src/routes/api/public/health.ts` (GET `/api/public/health`): a cheap, timeout-bounded (2s) DB reachability probe (`profiles` head-read, verified a real table used in onboarding/connections/calendar) → returns 200 `{status:"ok"}` healthy or 503 `{status:"degraded"}` when a dependency is down, so a monitor/LB reacts on the STATUS CODE. Public + unauthenticated (monitors don't auth), so it leaks NOTHING beyond ok/error check states (asserted by a test on the exact key set), keeps the probe trivially cheap, and bounds it with a timeout (DoS-amplification guard). Pure status assembly in new `src/lib/app-health.ts` (`assembleHealth(checks, nowIso)` → `{body, httpStatus}`), unit-tested. tsc 0 / eslint 0 / build ✓ (3.68s, routeTree regenerated) / 303 tests (5 new: ok→200, db-down→503, worker-always-ok, no-leak key set, purity). **◐ not ✅:** the endpoint + pure assembly are built + unit-verified, but the live DB-probe behavior was not run unattended (no live worker; verify on publish), and the ALERTING half (wire a monitor → notify) + a status page are infra/founder + design-pass. UI breadcrumb: none (infra endpoint; a future status page is design-pass) [~60%] |
| 55 | ◐ | PROVIDER-FALLBACK | Multi-provider/model fallback chain at the AI chokepoint | An ordered fallback chain so a provider/model outage degrades gracefully instead of hard-failing (closes `considerations.md` #4 "graceful degradation when a provider/model is down") | Reliability | Tier 3 | 2026-06-20 19:52 | NEW row (overnight cycle 56, `considerations.md` #4 + the SRE "graceful degradation" P0). Generalized the chokepoint's SINGLE `fallbackModel` retry into a pure, ordered, deduped, primary-excluded `resolveFallbackChain` (new `src/lib/ai/fallback.ts`, 10 unit tests) wired into both `callModel` + `callModelStream` (the single-fallback block becomes a chain loop). Back-compat: a lone `fallbackModel` → a 1-element chain = today's behavior, so it is byte-identical when off. New flag `providerFallbackEnabled()` (`AI_PROVIDER_FALLBACK`, default OFF, mirrors `AI_COST_ROUTING`) optionally appends an auto cross-model degrade to `cheapestLiveModel()` (reuses the tested WM-M15 picker), EXCLUDING surface `eval` (benchmark corruption) + `embed` (cheapestLiveModel is chat-only). Happy path untouched (only the post-primary-failure path changes), bounding blast radius. Dedicated adversarial review (chokepoint, cycle-39/40 history) confirmed 7 binding constraints + folded 2 findings (surface the PRIMARY error on chain exhaustion; a missing test). **Verification (ran):** tsc 0, eslint 0 (3 files), build ✓, `bun test` 332/332 (10 new). **◐ not ✅:** the chain + ranker are ✅ unit-verified; the live outage-degrade + auto cross-model fallback activate on the founder setting `AI_PROVIDER_FALLBACK` (verify on publish). UI breadcrumb: none (chokepoint resilience). _Counts not recomputed this cycle (parallel sessions active); a reconciliation pass owns the At-a-glance tally._ [~70%] |
| 56 | ◐ | EVAL-COVERAGE | Eval-coverage scorer + "untested AI surfaces" banner | Computes which of the canonical AI surfaces (chat/copilot/discovery/meetings/roadmap/studio/agent) have an enabled, runnable-case-bearing, recently-run eval guard, vs none (uncovered) vs unproven (stale); surfaces the gap in the Evals panel | Governance | Tier 3 | 2026-06-21 02:10 | NEW row (Lane 1, `considerations.md` AI-safety P1 "Eval coverage targets per surface/agent"; completes the "is the autonomy guarded" triad with RELIABILITY-SLO + RUNAWAY-DETECT). Pure `src/lib/evals/coverage.ts` (`EVAL_COVERAGE_TARGETS` lifted from the panel's SURFACE_KEYS as the shared source of truth + `assessEvalCoverage`: covered/uncovered/stale per target; `completed` last-run = covered, enabled-but-0-runnable-cases or no-completed-run = stale, no enabled suite = uncovered; `summarizeCoverage` silent at full coverage) + unit tests + a read-only `getEvalCoverage` over `eval_suites`/`eval_runs` (user-scoped, mirrors `listEvalSuites`; counts only enabled cases) + a calm degrade-silent banner in `EvalsPanel.tsx`. Zero-orphan (existing denominator + consumer); the SURFACE_KEYS unification also removed em-dash labels (humanization win). **2-lens adversarial review caught a feature-inverting bug the gate could not:** the success status is `completed` not `complete` (the runner + every reader + seed data agree; my constant was the lone outlier, and the 17 tests masked it by asserting my own constant) — fixed + added a regression test pinned to the real DB literal; also folded enabled-only case counting. tsc 0 / eslint 0 / build ✓ / `bun test` 464 (18 new). **◐ not ✅:** scorer ✅ unit-verified; live `eval_suites` read live-verifies on publish (DB MCPs intermittent). Follow-up: per-target chips + a coverage-floor deploy gate. Spec: [`../features/eval-coverage.md`](../features/eval-coverage.md). UI: `/govern?tab=evals`. _Counts not recomputed (parallel sessions active)._ [~75%] |
| 57 | ◐ | RELIABILITY-GLANCE | Calm Missions glance wiring SLO + runaway to the operator | A silent-when-healthy "Heads up" line on the Missions header that surfaces only when the AI error budget is strained (RELIABILITY-SLO) or a mission is spinning (RUNAWAY-DETECT); wires the two shipped read fns into the operator's view | Cockpit | Tier 3 | 2026-06-21 01:55 | NEW row (Lane 1, FE-wiring theme; backs the North Star claim-never-outruns-wiring by rendering the two previously-orphaned reliability read fns). New `src/components/cockpit/ReliabilityGlance.tsx` (mirrors `MissionsCostGlance`: `useServerFn` + `useQuery` on `getReliabilitySlo` + `getRunawayMissions`; renders NOTHING unless `budget.status` is warning/exhausted OR a `runaway`-severity mission exists; degrades to silent on query error) wired into `_authenticated.missions.index.tsx` next to the cost glance. Engine-Room-doctrine: attention line, not an always-on panel; neutral ink tones (role-color accents stay reserved); labeled metric; no em-dash/hype. Gated via `impeccable` (folded an invented-`--warn`-token + role-color fix). tsc 0 / eslint 0 / build ✓ / 438 tests (no new; presentational). **◐ not ✅:** wired + gate-green; renders on the founder's next publish (no local render verify; DB MCPs intermittent). Follow-up: the deep Engine Room breakdown + drill-in. Spec: [`../features/reliability-slo.md`](../features/reliability-slo.md) + [`../features/runaway-detection.md`](../features/runaway-detection.md). UI: Missions header. _Counts not recomputed (parallel sessions active)._ [~70%] |
| 58 | ◐ | RUNAWAY-DETECT | Read-only runaway / loop mission detector | Flags missions that are spinning (runaway hop/step counts, retry thrashing, pinned-retry steps, spend blowouts) — the inverse of E8's stall monitor; the AI-safety "agent can spin" risk made visible | Governance | Tier 3 | 2026-06-21 01:40 | NEW row (Lane 1, `considerations.md` AI-safety lens P1 "Loop/runaway detection"; KI-15/16 are the caps, this is the DETECT half). Pure `src/lib/reliability/runaway.ts` (`assessMission`/`assessMissions`/`isTerminalStatus`/`summarizeRunaway`; per-mission hop/step/retry-churn/pinned-step/spend thresholds → `runaway` while active vs `watch` when terminal; unknown status = active fail-loud; total + deterministic via injected `ageMinutes`; 18 tests) + a read-only `getRunawayMissions({days})` over missions/mission_steps/agent_runs (user-scoped: missions `.eq('user_id')`, children `.in('mission_id', ids)` over the user's own ids; 200-mission cap + `truncated` flag; no writes/AI/spend; NO loop/chokepoint edit). **Thresholds are deliberately conservative heuristics ABOVE the real caps** (no hard hop ceiling exists; step-dispatch cap is small/per-tick; `maxStepAttemptsCeiling` 3 > default `max_attempts` 2 so a normal exhausted retry never trips). 2-lens adversarial review (read-fn RLS/correctness · detector soundness): both **SHIP** — no runtime-fatal/correctness/security defect; folded a calibration-comment accuracy fix + em-dash cleanup. tsc 0 / eslint 0 / build ✓ / `bun test` 427 pass (18 new). **◐ not ✅:** detector ✅ unit-verified; the live missions/steps/runs read live-verifies on publish (DB MCPs intermittent). Operator surface + alert/auto-pause = follow-ups. Spec: [`../features/runaway-detection.md`](../features/runaway-detection.md). _Counts not recomputed (parallel sessions active)._ [~75%] |
| 59 | ◐ | RELIABILITY-SLO | SLO / error-budget reliability surface over AI telemetry | Quantified availability + latency-SLO + error-budget over `ai_events` (success/error + latency), complementing APP-HEALTH's liveness with measured reliability the operator can read | Cockpit | Tier 3 | 2026-06-21 01:26 | NEW row (Lane 1, `considerations.md` SRE lens P1 "SLOs/SLAs + error budgets"). Pure `src/lib/reliability/slo.ts`: `computeSlo` (availability %, p50/p95 latency, error-budget burn vs a 99% default target) + `percentile`/`errorBudgetStatus`/`summarizeSlo`/`normalizeStatus`. **Correctness crux:** `ai_events.status='blocked'` is a DELIBERATE governance/credit halt, so it is EXCLUDED from the availability denominator + latency (folding it in would look unreliable exactly when the guardrails fire); unknown/NULL status fails VISIBLE (→ error), never hidden as blocked. Read-only `getReliabilitySlo({days,targetAvailabilityPct})` over `ai_events` (user-scoped RLS, 5000-row cap + `truncated` flag, no writes/AI/spend). 3-lens adversarial review (math · security/RLS · reuse/doctrine): no runtime-fatal/security defect; folded 5 (unknown→error fail-visible + moved the coercion into the tested module, NaN-target totality guard, `truncated` honesty flag, doctrine-comment de-claim, dead-coalesce cleanup). tsc 0 / eslint 0 / build ✓ / `bun test` 409 pass (22 new). **◐ not ✅:** engine ✅ unit-verified; the live `ai_events` read live-verifies on the founder's next publish (DB MCPs intermittent this session). Engine Room glance = wire-up follow-up. Spec: [`../features/reliability-slo.md`](../features/reliability-slo.md). _Counts not recomputed (parallel sessions active)._ [~75%] |
| 60 | ◐ | MODEL-REGISTRY-DEPRECATION | Model-deprecation route-around at the AI chokepoint | Deprecation flags on the model catalog + a chokepoint route-around so a sunset/deprecated model proactively routes to its replacement (the model-deprecation-playbook half of `considerations.md` #4) | Reliability | Tier 3 | 2026-06-20 20:21 | NEW row (overnight cycle 57, completes `considerations.md` #4 with PROVIDER-FALLBACK c56). Added `deprecated`/`replacement`/`sunset` to the `Model` type (`src/lib/ai/models.ts`) + a pure, cycle-safe `activeModelId(id, catalog?)` resolver: follows `deprecated -> replacement` links (intermediates may be non-live retired models) to a LIVE terminal, else returns the original requested id so a deprecation can never strand traffic on a missing/non-live model; wired before cost-routing in both `callModel` + `callModelStream`. **Honest status:** NO model is flagged deprecated today, so `activeModelId` is the identity function and the chokepoint is BYTE-IDENTICAL - a wired-but-dormant playbook ready for the next provider sunset. The resolver logic IS unit-verified (8 tests via an injected catalog: live replacement, multi-hop chains, missing/non-live-terminal rejection, cycle termination, unknown id). **Verification (ran):** tsc 0, eslint 0 (3 files), build ✓, `bun test` 357/357 (8 new). **◐ not ✅:** wired + logic unit-verified, but no live route-around happens until a model is actually flagged (and the chokepoint resolution path live-verifies on publish). UI breadcrumb: none (chokepoint). _Counts not recomputed (parallel sessions active)._ [~70%] |
| 61 | ⏭️ | HUMAN-SWEEP | Full-product humanization sweep | Sweep all UI strings + seed data for AI fingerprints | Foundational | Tier 4 | - | deferred: pre-launch gate, so screen churn does not force a re-sweep |
| 62 | ⏸️ | SEN-01 | Connector dock: 2nd live ingest | Adds a second real ingest source (Slack / GitHub issues / support) so the loop closes on real data | Sense | Gated | - | paused: needs founder to register one provider OAuth client (F-CONN) |
| 63 | ⬜ | Q2 | A2A server/client + Agent Cards + scopes/audit | Peer agents discover and call Cadence, governed by scopes + audit | Interop | Gated | - | needs founder decision on outward-facing surface + scopes/audit posture |
| 64 | ⬜ | SEN-05 | Quant analytics inbound | Ingests PostHog/Amplitude/Mixpanel product metrics as a first-class signal | Sense | Gated | - | gated on a product-analytics connector OAuth + recurring spend |
| 65 | ⬜ | F-ANALYTICS-1 | Cohort metrics + telemetry ingestion | Released features get real usage data into `product_analytics` | Launch | Gated | - | depends on SEN-05 |
| 66 | ⬜ | F-ANALYTICS-2 | Opportunity impact eval | Post-release cohort feeds Product Memory and auto-adjusts ICE; the loop learns if a bet paid | Launch | Gated | - | depends on F-ANALYTICS-1 |
| 67 | ◐ | PLG | PLG funnel | Public onboarding to first-win to upgrade; turns share traffic into activated paying users | Monetization | Gated | 2026-06-17 | Phases 1,2,4 done; Phase 3 (memory-expiry banner on Today/Brain) queued behind the autonomous loop [~75%] |
| 68 | ⬜ | SANDBOX | Sandbox / preview spine | A sandbox to run agent tests + preview a build before merge | Build | Gated | - | needs founder to pick a provider (Cloudflare Sandbox SDK / E2B / Vercel) + infra/spend |
| 69 | ⬜ | BLD-04 | Delegate-out to external coding agents | A2A-style hand-off of build work, still governed | Build | Gated | - | depends on Q2; founder posture call (BYO-key + governance) |
| 70 | ⬜ | A6 / ENG-08 | Roles + RBAC + invites | owner/admin/member/viewer roles + invites; per-persona approval lanes | Interop | Gated | - | superseded/expanded by WM-F3/F4/F5 (G10); build from the WM rows |
| 71 | ⬜ | BYO-P1a | RepoProvider interface + GitHub adapter | Lifts GitHub calls behind a provider-agnostic interface (behavior-preserving) | BYO | Gated | 2026-06-19 | keystone; pending founder greenlight (no code until approved) |
| 72 | ⬜ | BYO-P1b | Product-level repo binding + per-Product RLS | Moves binding workspace -> product; per-Product UI + RLS | BYO | Gated | 2026-06-19 | pending greenlight; parallel P1a; must honor workspace `agent_memory` RLS |
| 73 | ⬜ | BYO-P1c | Managed / auto-create repo (user's own org) | User creates a repo in their own account/org; portable, value-locked not hostage | BYO | Gated | 2026-06-19 | pending greenlight; needs BYO-P1a, BYO-P1b |
| 74 | ⬜ | BYO-P1d | Calm-front Build surface | One product-framed decision on a new repo; git behind the Engine Room | BYO | Gated | 2026-06-19 | pending greenlight; needs BYO-P1a, BYO-P1b |
| 75 | ⬜ | BYO-P2 | Multi-provider (GitLab; Bitbucket demand-gated) | GitLab launch pair; Bitbucket demand-gated; each adapter bounded by the interface | BYO | Gated | 2026-06-19 | pending greenlight; needs BYO-P1a |
| 76 | ⬜ | BYO-P3 | Autonomous Build to Ship + capture | Agent runs the whole chain; deploy capture + in-app changelog; PRD join | BYO | Gated | 2026-06-19 | pending greenlight; effort L; needs BYO-P1d |
| 77 | ⬜ | BYO-P5 | Managed end-to-end runtime | DB + auth + hosting so the user launches without leaving Cadence | BYO | Gated | 2026-06-19 | founder-gated; sequenced last (needs BYO-P3 + loop proven) |
| 78 | ⏸️ | F-CONN | Connector platform (OAuth) | The connector engine that brings external sources in; built but parked pending a founder OAuth-client registration | Sense | Gated | - | parked: needs founder to register an OAuth client; unblocks SEN-01 |
| 79 | ⏸️ | M-C-EXPIRY | Memory-expiry enforcement engine | Free-tier memory expiry, built but gated off via memory_expiry_enabled() | Monetization | Gated | - | dormant: gated off; WM-M2 reworks it to a 30-day rolling window ⚖️ STAYS (2026-06-21): memory decay is a product lock-in mechanic; only the live flip is monetization-timing. |
| 80 | ⬜ | CMD (H2) | The Command Canvas (NL command bar + live preview) | A natural-language intent bar (Lovable/Cursor-like, NOT a syntax CLI) as the primary surface with a live preview/canvas pane on the right; calm-front compliant (Linear pattern); preview half first. Resolves the overlap with today's Ask (`/chat`): the Canvas is the evolution of Ask, not a second box | Cockpit | Gated | 2026-06-20 | **PARKED 2026-06-20 (founder): only H1 (Decision Brain) is active; preserved, not deleted.** Spec [`../features/command-canvas.md`](../features/command-canvas.md). Would evolve the Ask/`CommandPalette.tsx` (⌘K nav → NL intent) + adds the canvas pane (CMD-0). GUI fallback preserved. 0% [not started] |
| 81 | ◐ | M-C-PRICE | Pricing + entitlements (incl. Admin Console v1 + v2) | Plan tiers, billing fns, Stripe webhook, Settings Plan; the revenue rails (relocated to account-level via WM-M1/2/3); **also the inbuilt `/admin/*` Admin Console** (Overview + Pricing tabs from v1, plus People + Workspaces + Platform tabs from v2) | Monetization | Lovable | 2026-06-20 23:55 | needs founder Stripe secrets to go live; carried/expanded by G10 WM rows. **Admin Console v2 shipped 2026-06-20 (◐, live on next publish)** - 5 tabs total, 8 new tables + audit log + cron expirer + Stripe-id column lockdown. Status for Admin Console lives HERE on this row (no separate dashboard row); detail in [`../features/admin-console.md`](../features/admin-console.md) + build bible [`./admin-console-v2-plan.md`](./admin-console-v2-plan.md). [~82%] 🔁 LOVABLE-OWNED 2026-06-21: billing/credit/pricing/admin-console built by Lovable in parallel; do NOT pick up in any Claude lane. |
| 82 | ◐ | WM-M11 | Per-tier credit amounts + monthly grant + cycle reset | Included resets, top-ups persist; the account credit pool | Credit | Lovable | 2026-06-19 16:31 | CORE shipped (overnight cycle 31): new `credits.functions.ts` (pure `monthlyGrantCredits`/`resetDelta` + dormant `grantMonthlyAllowance`/`resetCreditCycle`) + `credit-tick` cron hook (grants un-granted, resets due accounts, preserves top-ups). Gated behind `credits_enabled()` (dormant). 7 unit tests; tsc/build/lint green, 208/208. Grant math verified; the DB writes + tick activate on publish + flag + pg_cron [~70%] ❄️ LOVABLE-OWNED+FROZEN 2026-06-21: per-tier grant/reset shipped dormant; do NOT extend; coordinate. |
| 83 | ⬜ | WM-M3 | Billing rails (account Stripe + webhook map) | 5-tier checkout + seats; webhook price-to-tier (dormant until secrets) | Monetization | Lovable | 2026-06-19 | effort M; needs WM-M1, WM-M2; founder Stripe secrets to go live 🔁 LOVABLE-OWNED 2026-06-21: account-level Stripe checkout/webhook; do NOT pick up. |
| 84 | ⬜ | WM-M6 | Pricing surfaces (5 tiers + Usage panel) | The new model shown across pricing page + Settings Plan + Usage | Monetization | Lovable | 2026-06-19 | effort M; needs WM-M1, WM-M3 🔁 LOVABLE-OWNED 2026-06-21: pricing surfaces (pricing page + Settings Plan + Usage); do NOT pick up. |
| 85 | ◐ | WM-M12 | Credit debit engine (fills the WM-M4 seam) | Meters credits from the account pool; halts clean when empty | Credit | Lovable | 2026-06-19 16:44 | CORE shipped (overnight cycle 32): atomic `debit_account_credits` RPC (included-then-topup, FOR UPDATE, tagged ledger debit) + `assertAccountCredits` projects + halts with `CreditExhaustedError` + a blocked event; `debitAccountCredits` calls the RPC. Gated behind `credits_enabled()` (dormant). RPC behaviorally dry-run-verified on prod (70+60 debit -> 0/20, ledger -130); 19 pricing tests, 212/212, tsc/build/lint green. Live metering on publish + flag [~75%] ❄️ LOVABLE-OWNED+FROZEN 2026-06-21: credit debit engine shipped dormant; do NOT extend; coordinate. |
| 86 | ⬜ | WM-M13 | Capped top-up purchase (Stripe credit packs) | Paid-only capped fair-use top-ups; per-cycle ceiling, off by default | Credit | Lovable | 2026-06-19 | effort M; needs WM-M3, WM-M12; founder sets pack prices + ceiling 🔁 LOVABLE-OWNED 2026-06-21: Stripe credit-pack top-ups; do NOT pick up. |
| 87 | ◐ | WM-M14 | Per-product / per-member attribution + caps | See + cap spend per product/member on the pooled account | Credit | Lovable | 2026-06-19 17:28 | CORE shipped (overnight cycle 33): migration `20260619180000_wm_m14_credit_caps.sql` (credit_caps table + 2 composite ledger indexes + member-read RLS) + pure `rollupAttribution`/`capExceeded`/`creditWindowStartIso`/`sumDebitCredits` + `computeCreditAttribution` (RLS-scoped read, for WM-M16) + `assertCreditCaps` at the chokepoint (per-product/member cap halt) + `productId` threaded into the debit (discovery clustering feeds it; per-MEMBER attribution always driven, per-PRODUCT fed by product-scoped sites). Gated behind `credits_enabled()` (dormant). Migration behaviorally dry-run-verified on prod then rolled back; 22 credits tests, 227/227, tsc/build/lint green; 5-lens adversarial review, 5 fixes folded. Live caps/attribution on publish + flag flip ❄️ LOVABLE-OWNED+FROZEN 2026-06-21: credit attribution/caps shipped dormant; do NOT extend; coordinate. |
| 88 | ⬜ | WM-M7 | Upgrade nudges (value-framed) | Convert at natural moments, never punitive | Monetization | Lovable | 2026-06-19 | effort S; needs WM-M5, WM-M6 🔁 LOVABLE-OWNED 2026-06-21: upgrade/conversion UI; do NOT pick up. |
| 89 | ⬜ | WM-M8 | Tier identity motif (Constellation glyph) | The unique animated plan identity (rename-able via slug decoupling) | Monetization | Lovable | 2026-06-19 | effort S; needs WM-M1, WM-M6 🔁 LOVABLE-OWNED 2026-06-21: tier identity/pricing presentation; do NOT pick up. |
| 90 | ⬜ | WM-M16 | Credit / usage UI (balance, legibility, attribution) | Calm balance + action ranges + attribution in Settings | Credit | Lovable | 2026-06-19 | effort M; needs WM-M6, WM-M12, WM-M14 ❄️ LOVABLE-OWNED+FROZEN 2026-06-21: credit UI; do NOT pick up. |
| 91 | ⬜ | BYO-P4 | Managed AI credits (= WM credits) | Metered AI; included allowance + fair-use overage | BYO | Lovable | 2026-06-19 | built under WM (G10), cross-referenced not duplicated; needs WM-M2 🔁 LOVABLE-OWNED 2026-06-21: managed AI credits = the WM credit engine; do NOT pick up. |
| 92 | ⏭️ | WM-M17 | Clubbed usage variants (Max 5x/20x + Team Standard/Premium seats) | One plan name, multiple priced usage variants (Anthropic-style), chosen at checkout | Monetization | Lovable | 2026-06-19 | Deferred behind core builds; needs WM-M3, WM-M6, WM-M11. M17a Max (per-account) P2; M17b Team seats (per-seat) P3. Spec: bible §2.4.1 + §4.2.2; numbers founder-gated 🔁 LOVABLE-OWNED 2026-06-21: usage-variant pricing packaging; do NOT pick up. |
| 93 | ⏭️ | WM-M18 | Plan-card states + change flow (current/upgrade tags, downgrade guard) | "Current plan" tag + "Upgrade" CTAs (no current/base duplication) + guarded downgrade | Monetization | Lovable | 2026-06-19 | Deferred; needs WM-M6 (+ WM-M17 for variant-aware "Current"). Founder UX ask 2026-06-19. Spec: bible §4.2.2 🔁 LOVABLE-OWNED 2026-06-21: plan-card states + downgrade flow; do NOT pick up. |
| 94 | ⏭️ | WM-M19 | Enterprise usage model (per-seat + API-rate usage + per-user allocation) | Per-seat + metered API-rate usage + per-user credit allocation / spend limits (on WM-M14 caps) | Monetization | Lovable | 2026-06-19 | Deferred; needs WM-M3, WM-M14. Enterprise sequenced post-loop-proof. Spec: bible §4.2.2 🔁 LOVABLE-OWNED 2026-06-21: enterprise usage/billing model; do NOT pick up. |
| 95 | ⬜ | SEN-04 | Researcher watchtower | Scheduled competitor crawl briefs; ambient competitive signal without manual research | Sense | Deferred | - | cut/defer: post-PMF |
| 96 | ⬜ | F-AUDIO-1 | Speech transcription + chunking | Upload meeting audio to transcript to diarized chunks | Sense | Deferred | - | cut/defer: post-PMF |
| 97 | ⬜ | F-AUDIO-2 | Action-item extraction from transcripts | Meetings become drafted opportunities/PRDs citing the transcript | Sense | Deferred | - | cut/defer: post-PMF; depends on F-AUDIO-1 |
| 98 | ⏭️ | K1-deploy | Cadence-triggered deploy gate | Trigger the actual deploy from Cadence | Build | Deferred | - | deferred: needs a Cloudflare/Lovable deploy hook + founder config; deploy is external today |
| 99 | ⏭️ | F-COCKPIT-MACHINE-MODE | Human <-> Machine mode toggle | Full-screen dispatch board, the "watch the factory" view | Cockpit | Deferred | - | absorbed by F-IA-V4 ⏭️ SUPERSEDED 2026-06-21 by the Engine-Room Doctrine (a full-screen control-room mode is the control-room creep the doctrine forbids). Do NOT build as a distinct mode. |
| 100 | ⏭️ | WM-S1 | Sample workspace for every new account | Every signup + investors land in a populated space | Monetization | Deferred | 2026-06-19 | deferred: gate at ~50-60% platform complete |
| 101 | ⏭️ | WM-S2 | Guided tour | Teaches the loop in the sample workspace | Monetization | Deferred | 2026-06-19 | deferred: gate at ~50-60% platform complete |
| 102 | ⏭️ | WM-S3 | Onboarding Concierge agent | Seeds the real workspace from real context day one | Monetization | Deferred | 2026-06-19 | deferred: gate at ~50-60% platform complete |
| 103 | ⏭️ | WM-S4 | Workspace Steward agent | Nudges stale brief / outcome-less decisions (feeds the moat) | Monetization | Deferred | 2026-06-19 | deferred: gate at ~50-60% platform complete |
| 104 | ⏭️ | WM-S5 | Investor-demo rich population + reset | Every demo surface populated; self-serve reset | Monetization | Deferred | 2026-06-19 | deferred: gate at ~50-60% platform complete |
| 105 | ✅ | R3 | Notifications | In-app Attention feed + global bell with the live "what needs you" count | Cockpit | Done | 2026-06-20 00:53 | Completed (parallel/cockpit cycle 1): user notification preferences table (`user_notification_preferences`) + RLS policies + settings route `/notifications` + email/digest scaffolding helper fns. In-app feed and global bell now filter dynamically based on preferences. Verified via unit tests + tsc/build/lint green. |
| 106 | ✅ | P7 | Incidents log | Read-only log of failed tool runs, errored auto-pipelines, guardrail blocks, linked to traces | Governance | Done | 2026-06-20 02:24 | Completed (parallel/cockpit cycle 2): persistent incidents table (`cost_incidents`) + RLS policies + cost incident detector + detailed cost badge integration. **Cycle 3 fix (2026-06-20 19:01):** an adversarial audit found the detector filtered `ai_budget_alerts` on `kind='block'`, a value no producer ever writes (the chokepoint only writes `kind='warn'`; a real cap halts by throwing), so it was silently dead in prod. Rewired to `.in('kind',['warn','block'])` to surface the real threshold-crossing alerts with honest pct/window copy (escalates to "cap reached" at pct>=100). tsc 0 / 11 tests / build green; no migration. |
| 107 | ✅ | KI-15 / KI-16 | Stale zero-step completion · advance-cap | Per-tick dispatch cap + stale zero-step-mission completion edge cases | Foundational | Done | 2026-06-20 19:31 | KI-15 done (`sweepStaleUnconsumedMessages`). KI-16 done in two layers: mission-level per-tick cap (`MISSION_BATCH=50` in `resume-runs.ts`, already in code) + **KI-16b per-mission step-dispatch cap shipped 2026-06-20 19:31** (`selectDispatchBatch` in `mission-advance.server.ts`, env `MISSION_STEP_DISPATCH_CAP` default 10) so a wide/runaway DAG degrades to bounded backpressure under the Workers execution budget instead of an unbounded per-tick enqueue burst; complements the `MAX_RUNNING_PER_WORKSPACE=5` running-concurrency cap. 5 unit tests; tsc/build green; no migration. |
| 108 | ✅ | WM-M1 | Entitlements core (5 account tiers + matrix) | The tier model + limits both threads read; unblocks all pricing/limit work | Monetization | Done | 2026-06-19 13:50 | SHIPPED (overnight cycle 26): 5 slug tiers + full matrix (limits, credits, RBAC, collab) + `limitFor` + Constellation `planPresentation`; legacy fields kept as aliases; `src/lib/entitlements.ts` (+ 14 tests). tsc/build/lint/test green ⚖️ STAYS (2026-06-21): the tier model + limits matrix the chokepoint/RLS read is product-infra; the pricing presentation it feeds is Lovable's. |
| 109 | ✅ | WM-M10 | Credit unit + cost-to-credit conversion + legibility | What one credit is + the calm per-action legibility layer (no meter-anxiety) | Credit | Done | 2026-06-19 15:58 | SHIPPED (overnight cycle 29): `src/lib/ai/pricing.ts` adds `CREDIT_COGS_USD` + `creditsForCost` (0 for non-billable, >= 1 margin-positive for billable) + `estimateCreditsForCall` (composes the USD estimator) + the pure display-only `actionCreditRange` legibility layer (calibrated shapes through the same conversion; no DB); 15 unit tests (29 asserts). tsc/build/lint/test green. Numbers are §7 placeholders; the dormant `WM-M4` seam fills via WM-M12 ❄️ LOVABLE-OWNED+FROZEN 2026-06-21: credit engine shipped dormant; do NOT extend; coordinate before live to avoid colliding schemas. |
| 110 | ✅ | F-AGENT-1 | Orchestrator + multi-agent missions | The mission DAG that runs the whole loop | Foundational | Done | - | |
| 111 | ✅ | F-AGENT-2 | Persistent memory + self-reflection + trust auto-advance | Agents remember and earn autonomy; the moat's substrate | Foundational | Done | - | |
| 112 | ✅ | F-AGENT-3 | Event reactor + auto-pipelines | Signals trigger missions with no human poke | Foundational | Done | - | |
| 113 | ✅ | F-AGENT-4 | Swarm HUD | See the agent mesh working (Missions Agents tab) | Foundational | Done | - | |
| 114 | ✅ | P1-AA | Deterministic auto-advance | Missions advance unattended past wave 0 | Foundational | Done | - | |
| 115 | ✅ | P1-RETRY | Bounded hop retry | A failed hop retries with backoff, not a dead mission | Foundational | Done | - | |
| 116 | ✅ | P1-BUDGET | Adaptive step budget | Step budget scales to role/arc, not a static cap | Foundational | Done | - | |
| 117 | ✅ | W1 | Memory-compounding loop | Outcomes distil into recallable memory across agents (the moat wired) | Foundational | Done | - | |
| 118 | ✅ | W2 | Executed-unattended audit | The cockpit shows what the loop ran without you | Foundational | Done | - | |
| 119 | ✅ | W3 | A2A hardening + moat on cockpit | Handoffs validate memory refs; outcomes-remembered count shown | Foundational | Done | - | |
| 120 | ✅ | M-0 | Loop runs end-to-end on live data | Plan to dispatch to specialist execution confirmed live (hollow-completion fixed) | Foundational | Done | 2026-06-15 | |
| 121 | ✅ | FND-AUTH | Auth + tenancy + RLS | The multi-tenant spine (user_id + workspace_id + product_id) | Foundational | Done | - | |
| 122 | ✅ | FND-CHOKE | AI runtime chokepoint | One governed path for every AI call (callModel / callModelStream) | Foundational | Done | - | |
| 123 | ✅ | KI-13 | Resilient signup | A real account can be created without a 500 (handle_new_user subtransactions) | Foundational | Done | - | verify live |
| 124 | ✅ | KI-14 | Eval score scale to 0-100 | Eval scores do not overflow / false-fail the gate | Foundational | Done | - | |
| 125 | ✅ | F-A2A-CARD | Public A2A agent card | Discoverability for external agents | Foundational | Done | - | |
| 126 | ✅ | F-HUMANIZE-HOOK | Pre-commit dash/invisible-char trace hook | Build-time backstop for the humanization rule | Foundational | Done | 2026-06-18 | run `bash scripts/install-git-hooks.sh` to activate |
| 127 | ✅ | KI-10 | Ingest webhook + per-token rate limit | One secure live ingest path for public use | Sense | Done | 2026-06-16 | |
| 128 | ✅ | F-BRAIN | Brain (web + workspace research) | Perplexity-grade research feeding decisions | Sense | Done | - | |
| 129 | ✅ | N2 | Re-score + insight memo + daily brief | The daily brief synthesizes "what the loop learned" from re-scored outcomes | Sense | Done | 2026-06-16 | AI brief output needs a live re-verify on the deployed app |
| 130 | ✅ | F-CHAT-NL-INTENT | Conversational command of the swarm | Drive missions in natural language | Decide | Done | - | |
| 131 | ✅ | H1 | PRD / spec generation | Cited specs from opportunities | Decide | Done | 2026-06-14 | 🏷️ 2026-06-21: 'H1' is overloaded (also the Decision Brain row 148 'DBR (H1)' and H1-TASKS). THIS row = cited-spec generation only. Pick by unambiguous IDs, never bare 'H1'. |
| 132 | ✅ | DEF-03 | Critic-on-spec red team | Specs get an adversarial pass before commit | Decide | Done | 2026-06-14 | |
| 133 | ✅ | F-DEC-CARD | Decision card + Critic badge on Today | The human makes the call with the Critic's view in front of them | Decide | Done | - | |
| 134 | ✅ | WEDGE | Critic-teardown first-run (the launch wedge) | A new account names a feature and gets an evidence-backed Ship/Revise/Kill teardown in the first session | Decide | Done | 2026-06-17 | |
| 135 | ✅ | F-SHARE-TEARDOWN | Shareable Critic-teardown link (viral loop) | A public `/t/$slug` teardown link ("why your pet feature is wrong, with receipts") drives signups | Decide | Done | 2026-06-17 | |
| 136 | ✅ | F-SHARE | Shareable-decision viral loop + rate limit | A public decision link drives signups; secure anon-read (also a Monetization surface) | Decide | Done | 2026-06-16 | |
| 137 | ✅ | H2 | Outcome roadmap (Now/Next/Later) | Human commits opportunities to buckets with an outcome + measure; agent ICE orders within | Decide | Done | 2026-06-17 | 🏷️ 2026-06-21: 'H2' also denotes the Command Canvas (row 149, 'CMD (H2)'). THIS row = the Outcome roadmap. Pick by unambiguous IDs. |
| 138 | ✅ | H3 | Scheduling (calendar-aware work blocks) | Schedules open deep-work tasks into free time within working hours | Decide | Done | 2026-06-16 | |
| 139 | ✅ | DEC-02-LOOP | Critic as an explicit loop step | The Critic is a routable, gating-exempt agent-loop tool (`critic.evaluate`) | Decide | Done | 2026-06-17 | full mission_steps DAG-node promotion deferred to Phase 2 |
| 140 | ✅ | F-STUDIO | Build engine | Repo reads, multi-file changesets, `studio/*` branches, PR + CI, gated merge | Build | Done | - | |
| 141 | ✅ | I1 | Build multi-file coding (per-hunk accept/reject) | Operator curates a staged changeset before the gated commit | Build | Done | 2026-06-16 | |
| 142 | ✅ | I1b | True revision history (atomic revisions) | Each commit records a revision; the Changes tab shows commit history with GitHub links | Build | Done | 2026-06-16 | revert-to-revision delivered separately (K2) |
| 143 | ✅ | I2 | Watch-the-agents-build live surface | Live per-session cockpit: timeline/steer + Changes/PR/Cost + inline gates + merge gate | Build | Done | 2026-06-16 | true SSE streaming deferred (nice-to-have) |
| 144 | ✅ | I3 | Branch/worktree isolation per mission | Concurrent missions cannot share a branch or clobber files (per-path claims + per-changeset branch) | Build | Done | 2026-06-16 | |
| 145 | ✅ | J1 | Test generation + run | The Build agent authors tests per change; tests run in the connected repo's GitHub Actions CI | Build | Done | 2026-06-16 | |
| 146 | ✅ | J2 | QA gate + self-correct loop | Merge refuses while CI is red or pending; the fix-on-red-until-green loop closes | Build | Done | 2026-06-16 | |
| 147 | ✅ | K1 | Release notes for a shipped changeset | Generate/regenerate factual release notes via the AI chokepoint, persisted + shown | Build | Done | 2026-06-16 | |
| 148 | ✅ | LRN-02 | Outcome reviews (predicted vs actual) | Human verdict + ICE rescore + a drafted Historian verdict (predicted vs actual) on the outcome card | Launch | Done | 2026-06-17 | |
| 149 | ✅ | N3 | Mission Compounding View | Per mission: "drew on N prior memories" + the lineage + a copy-snapshot export | Launch | Done | 2026-06-16 | |
| 150 | ✅ | W6 | Persona onboarding tracks | Per-track sample data + first-win moment (Solo / Founding PM / Tech Founder); WEDGE delivery surface | Monetization | Done | 2026-06-17 | live-verify on next publish |
| 151 | ✅ | F-A2A | Internal A2A handoff contract | Agents hand off missions with structured payloads | Interop | Done | - | |
| 152 | ✅ | F-GAUNTLET | Gauntlet metrics | The north-star metrics (acceptance, autonomy, retention), honestly instrumented | Cockpit | Done | - | |
| 153 | ✅ | F-MEMVIEW | `/memory` compounding-memory view | The moat made visible | Cockpit | Done | 2026-06-14 | |
| 154 | ✅ | F-AUTONOMY | AutonomyCard on Today | The trust arc (observing to proving to trusted) is visible to the operator | Cockpit | Done | - | |
| 155 | ✅ | F-TODAY-LOOPPULSE | Loop Pulse hero | Today's hero opens with "while you were away" (last 24h, non-zero parts only) | Cockpit | Done | 2026-06-16 | |
| 156 | ✅ | E8 | Loop Health Monitor | An always-on health strip on Missions: verdict + queue depth + last ingest/run | Cockpit | Done | 2026-06-16 | |
| 157 | ✅ | B3 | Product switcher + portfolio view | A Portfolio section on `/product`: every product with loop status + click-to-switch | Cockpit | Done | 2026-06-16 | command-K product-switch deferred |
| 158 | ✅ | B5 | Archive / delete product | Soft archive + restore + JSON export + honest export-then-delete | Cockpit | Done | 2026-06-17 | |
| 159 | ✅ | F-AGENTS-MENTIONABLE | Agents as @-mentionable users | `@agentslug goal` in chat dispatches a specialist directly (skips the orchestrator) | Cockpit | Done | 2026-06-18 | |
| 160 | ✅ | OPS-01 | Flow mode | Ambient soundscape + focus timer + notification quieting; a calm operating surface | Cockpit | Done | 2026-06-16 | |
| 161 | ✅ | F-TRUST | Trust score + four autonomy arcs | Autonomy is earned and visible at the gate | Governance | Done | - | |
| 162 | ✅ | FND-0.6 | Kill-switch + spend caps | The brake pedal; budgets enforced server-side | Governance | Done | - | |
| 163 | ✅ | F-HUMANIZE | `humanizeText()` runtime sanitizer | Zero AI fingerprints in generated output, at the chokepoint | Governance | Done | 2026-06-14 | |
| 164 | ✅ | DEC-02 | Critic adversarial pass on opportunities | Opportunities get a red-team ship/revise/kill verdict at promotion (CriticBadge) | Governance | Done | 2026-06-16 | verified |
| 165 | ✅ | P4 | Eval harness + regression gate | A >=10pt eval drop per suite hard-blocks the agent's merge | Governance | Done | 2026-06-18 | P4-GATE cycle 23 |
| 166 | ✅ | P5 | Drift watch | Score/cost/latency drift per surface/model; open incidents surface in the Attention feed | Governance | Done | 2026-06-18 | P5-ALERT cycle 22 |
| 167 | ✅ | P3 | Prompt studio | Versioning + A/B + pin + rollback; runtime loads versioned prompts | Governance | Done | 2026-06-18 | verified already built (was stale ⬜) |
| 168 | ✅ | C4 / E7 | Agent detail + run history + memory inspector | Pick an agent and see its recent runs + its private and shared memory | Governance | Done | 2026-06-18 | a dedicated detail route is optional polish |

---

## Status reconciliation note
Statuses here are reconciled from [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) (sections 0/2/3, the live cursor + status + build queue) and [`feature-backlog.md`](./feature-backlog.md) (granular ledger). Where docs conflict on a "done" claim, trust this file + the live code, then fix the others in the same commit. Granular acceptance criteria + "how to use / verify" blocks live in [`feature-backlog.md`](./feature-backlog.md); milestone exit criteria + the founder pickup list live in [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) (sections 2-4); open bugs live in [`known-issues.md`](./known-issues.md). F-SHARE is one feature listed under both Decide (G2) and Monetization (G5); it is a single row here (Decide, shipped) per the register-one-row-per-ID rule.

## Related
- [`SOURCE-OF-TRUTH.md`](./SOURCE-OF-TRUTH.md) - the ONE front-door tracker (live cursor, build queue, founder pickup list, findings, progress)
- [`feature-backlog.md`](./feature-backlog.md) - granular ledger + per-feature acceptance criteria
- [`workspace-tenancy-and-monetization-plan.md`](./workspace-tenancy-and-monetization-plan.md) - WM (G10) per-ID build specs incl. the credit engine (WM-M10..WM-M16, §4.2.1)
- [`byo-build-implementation-plan.md`](./byo-build-implementation-plan.md) - BYO (G11) per-phase build specs
- [`v10-master-blueprint.md`](../strategy/v10-master-blueprint.md) §15-16 + [`v10_implementation-plan.md`](./v10_implementation-plan.md) - the priority rationale + execution mechanics
- [`known-issues.md`](./known-issues.md) - open bugs with KI-IDs
- [`../../AGENTS.md`](../../AGENTS.md) §1 (pre-action) + §5 (doc-update protocol) - where the standing rule is enforced
