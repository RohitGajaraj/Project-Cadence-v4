# plan.md — Feature scope & build log

> [!IMPORTANT]
> **CADENCE ➔ CIRCUIT RENAME DISCLAIMER:**
> _This project has been renamed from **Cadence** to **Circuit**. Any legacy folder structures, database tables, migrations, environment variables, or APIs that still contain the string `cadence` or `Cadence` are to be treated as equivalent to `circuit` or `Circuit` to prevent breakages in existing configurations and caches._
>
> _Do not perform destructive refactoring of database column/table names or folder structures that are bound to Lovable Cloud and pgvector schemas. All modifications should map the name logically at the user and documentation level while keeping underlying plumbing compatible._

> The canonical record of **what Circuit is built to be** (the full feature scope), **the order we build it**, and **what already exists to reuse**. Product framing: [`README.md`](./README.md). Operating rules: [`AGENTS.md`](./AGENTS.md). Architecture: [`architecture/`](./architecture/). UI contract: [`design.md`](./design.md).
>
> **No MVP1/MVP2/MVP3 gating, no far-future phase deferral.** We build the full intended scope on the current stack ([`docs/decisions/tech-stack.md`](./docs/decisions/tech-stack.md)) and ship continuously. This doc is written to be read straight into a coding session (Claude Code / Lovable) and acted on.

---

## How this file is organized

1. **Feature scope** — the whole product, stress-tested as if nothing is built. The high-level feature list.
2. **Competitive feature comparison** — factory.ai · hyperagent · Linear → what Circuit should build.
3. **Granular feature catalog** — every feature from login/logout to agent-transfer/workspace level, with _what_ and _why_. This is the dev-ready map.
4. **Build order** — the sequence to build it, production-grade, reusing legacy code where it fits.
5. **Active build log** — what we actually ship, updated as we go (starts near-empty).
6. **Legacy build log (retained)** — the previous source build, kept as a reuse reference, not the plan.
7. **Product-agent roster** — re-examined (explicit vs. implicit).
8. **Testing, lessons, out-of-scope.**

---

## 1. Feature scope (stress-tested from scratch)

> [!IMPORTANT]
> **v4 supersedes this section for scope (2026-06-11).** The canonical feature scope is now [`docs/strategy/v4-feature-map-2026-06-11.md`](./docs/strategy/v4-feature-map-2026-06-11.md) — 7 platform laws, 6 stations over the 12-stage engine, the 19-agent mesh with handoff contract + HITL gate matrix, the 7-surface IA, station feature catalogs (SEN/DEC/DEF/BLD/LCH/LRN/ENG/OPS), and milestones M1–M5. The adversarial reasoning behind it: [`docs/strategy/v4-stress-test-2026-06-11.md`](./docs/strategy/v4-stress-test-2026-06-11.md). The 12-stage engine description below remains valid as the *internal* loop reference; where wording differs, **the v4 map wins**. **v5 overlay (2026-06-11):** the *felt product* — wedge UX, nav, vocabulary, June 22 demo scope — is governed by [`docs/strategy/v5-chief-of-staff-2026-06-11.md`](./docs/strategy/v5-chief-of-staff-2026-06-11.md) (PM Chief of Staff; 4 felt surfaces + Trust drawer; phases `F-V5-RITUAL` → `F-V5-DEMO`). v4 remains the expansion map.

Circuit runs the full product lifecycle as one governed, autonomous loop. The scope below is the target product — not a discovery tool. Each stage is run by autonomous agents; the human approves at the gates.

### Who we serve, what they expect, and the gap (grounding for the feature list)

The feature list below is a deliberate response to who the customer is and what the market already sets as table-stakes — not a wish list. Inspiration drawn from products worth learning from is captured in [`docs/references/competitive-reference.md`](docs/references/competitive-reference.md) (deferred; inspiration only).

**Personas (who orchestrates Circuit):**

- **P1 — Enterprise Director / VP of Product (The Portfolio Governor)**: owns portfolio-level alignment, agent spend budgets, and compliance. Pain: Swarm drift, lack of visibility into agent actions, security audits, budget overruns, and multi-product roadmap health.
- **P2 — Lead / Senior PM (The Daily Cockpit Operator)**: runs day-to-day discovery, spec definition, and feature delivery. Pain: Mechanical process work (spec writing, ticket updates, release logs, alert triage) eating up time needed for strategic judgment.
- **P3 — Engineering Lead / Tech Architect (The Code Gatekeeper)**: guards repository health, code quality, and branch stability. Pain: Agent-written code drifting from internal architecture guidelines, introducing regressions, or breaking sandboxed environments.
- **P4 — UX/UI Designer (The Visual Validator)**: ensures design tokens and brand style guide alignment. Pain: Generated code failing to match design system tokens or looking off-brand in mockups.
- **P5 — GTM Lead / Product Marketer**: handles positioning and product launch. Pain: Generating changelogs, marketing collateral, newsletter posts, and distribution updates by hand for every release.
- **P6 — Customer Success & Support Lead**: monitors product support feedback. Pain: High ticket volumes, slow bug triage, and user signals getting lost before reaching the product manager.

**Market expectations (table-stakes — must-haves, or we're not credible):** auth + SSO-ready tenancy; real (not mocked) data; connectors to the tools they live in; an assistant/chat surface; observability + cost visibility; approvals/audit for anything autonomous; multi-project support; export/no-lock-in.

**The gap (where the market is underserved — our opening):** point tools own single bands (engineering autonomy, or issue tracking, or research synthesis). **No one runs the _whole_ lifecycle — discover → build → ship → launch → support → learn — as one governed, autonomous, multi-product loop.** That gap is the spine of the feature catalog in section 2.

**What the customer actually wants (the job):** "Take this intent and run it end-to-end — autonomously, in parallel, across my products — and only stop me for the calls that matter." Every feature below maps to that job.

**The feature list derives from the PM's actual day.** The product manager is the cockpit operator — the features below exist to run the PM's day (triage inbox → decide → synthesize → spec → tickets → build/test/ship → launch/update → answer "why is this on the roadmap?" → report → repeat). The day-to-features mapping and the PM/eng-lead lenses are in [`docs/planning/considerations.md`](docs/planning/considerations.md). If a feature does not serve a real PM task, question it.

### Lifecycle stages (the 12-Stage Product Loop)

**S1 — Signal Capture**

- Multi-source signal ingest: support tickets, churn, usage telemetry, sales notes, reviews, NPS, interviews.
- Agent clustering → themes → ICE-scored opportunities; continuous (not one-shot) synthesis; dedupe by similarity.
- A continuously-updated discovery feed per product.

**S2 — Audio Sync (WhisperFlow)**

- High-context audio recording, transcription, and meeting spec extraction.
- Translates voice memos or team meetings into feature specs and backlog tickets with zero friction.

**S3 — ICE Prioritization**

- Prioritization queue management: opportunity scoring based on Impact, Confidence, and Ease.
- Automated re-prioritization as new signals arrive or existing ones scale in severity.

**S4 — Spec Definition**

- Opportunity → PRD/spec, retrieval-grounded and cited; inline `/ai` editing; versioned with diff.
- Acceptance criteria, non-goals, risks, success metrics auto-drafted; human edits and approves.

**S5 — Sprint Planning**

- Spec → dependency-aware task graph with estimates, owners, risk flags.
- Outcome-oriented roadmap (Now/Next/Later) synced automatically to Linear/Jira.

**S6 — Agentic Build**

- In-product multi-file coding: Builder Agent claims file locks, checks out branches, and writes code.
- Surgical modifications across files, committing atomically and resolving conflict flags.

**S7 — Visual QA**

- Sandbox preview environments generated automatically for visual validation of UI changes.
- Let designers and PMs audit layout and styling against design system tokens before merging.

**S8 — Safe Release**

- Merges PRs, runs deploy checklists, deploys, and monitors production health.
- Automated rollback triggered if error metrics or exception counts spike post-deploy.

**S9 — GTM Launch**

- Growth agent drafts release changelogs, marketing blog posts, newsletter copies, and announcement text.

**S10 — Support Triage**

- Support agent triages incoming customer tickets, drafts answers, and routes bug logs back into Signal Capture (S1).

**S11 — Cohort Analytics**

- Automated feature adoption tracking, cohort analysis, and outcome funnel monitoring.

**S12 — Learn & Reflect**

- Outcome evaluation: compares performance telemetry with the original PRD spec.
- Updates the Product Memory Graph and publishes a learning brief to the portfolio dashboard.

### Cross-cutting platform (mandatory, spans all stages)

**X1 — Orchestration & automation engine**

- Workflow definitions across stages; event/schedule/webhook triggers; an automation builder.
- **Many sub-agents running in parallel** within a mission; **many agent sessions running in parallel** across missions.
- A live orchestration view (the mission/agent graph) with per-node status, cost, and approval state.
- Planner/executor loop with max-step/max-cost caps, replay-and-branch, cancellation, checkpoints.

**X2 — Multi-product & multi-workspace**

- **Multiple products in flight — Product A/B/C — grouped under Workspaces (A/B/C).**
- Per-product/workspace isolation of data, agents, memory, budgets, and access (RLS by `user_id` + `workspace_id`; see [`architecture/data.md`](./architecture/data.md)).
- Product switcher; cross-product portfolio view.

**X3 — Trust & governance (the AI trust stack)**

- Per-call telemetry, LLM-as-judge scoring, guardrails (input + output), RAG with citations, prompt versioning + A/B, eval harness with regression gate, drift watch, budgets, live trace waterfall, incidents log, approval gates everywhere.

**X4 — Product Memory**

- Queryable graph of signals → themes → opportunities → decisions → experiments → outcomes, with `supersedes` lineage; the institutional context agents reason over; exportable as skill packs.

**X5 — Interop (agent-native)**

- MCP server (Circuit as a tool surface) + MCP client (consume external tools) + A2A (Agent Cards, delegate-to-agent, peer registry). Capability scopes, rate limits, prompt-injection guard on external results, audit log.

**X6 — Identity, auth, billing**

- Auth (email + OAuth), session management, per-workspace membership (future: roles/teams), BYO model keys (encrypted), usage/billing metering against budgets. See [`architecture/security.md`](./architecture/security.md).

---

## 2. Granular feature catalog (login to agent-transfer, with what + why)

> **Sub-feature-level expansion:** [`docs/planning/feature-backlog.md`](./docs/planning/feature-backlog.md) takes every area below down to addressable sub-features with stable IDs, states, acceptance signals, dependencies, and a status × build-order rollup. This section stays the narrative catalog; the backlog is the flat, ticket-ready list. Keep both true.

Every feature, grouped by area. Format: **Feature — what it is — _why we need it_.** This is the build checklist; each becomes an issue/spec. Status tags: `[reuse]` legacy survives, `[extend]` legacy base + new work, `[new]` build from scratch.

> **Inspiration, not comparison.** This catalog absorbs the best ideas from products worth learning from — factory.ai's autonomous build/ship + live "mission" view (we extend it past code into launch/support), hyperagent's team-of-agents with per-agent budgets and self-improving memory, and Linear's agents-as-first-class-users + saved-workflow skills + event automations. We take the inspiration and build it into _our_ features below; we are not maintaining a competitor scorecard.

### A. Identity & access

- **Sign up / onboarding** — email+password + Google OAuth; create first workspace + product; set display name, timezone, working hours. — _Entry point; personalization and scheduling depend on it._ `[reuse]`
- **Login / logout** — authenticated session, sign-out everywhere, session refresh, `Last-Event-ID` resume for streams. — _Baseline security and reliable streaming._ `[reuse]`
- **Password reset / email verification** — standard flows; verification on by default. — _Account safety._ `[reuse]`
- **Profile & preferences** — display name, role, timezone, working hours, default model per surface. — _Drives greeting, scheduling, model routing._ `[reuse]`
- **BYO model keys** — add/test/rotate/delete provider keys, encrypted; masked display. — _Model-agnostic; cost control; enterprise trust._ `[reuse]`
- **Roles & membership (future)** — invite, per-workspace roles, SSO/SAML. — _Team and enterprise readiness._ `[new]`

### B. Workspaces & products

- **Create / switch workspace** — top-level tenancy boundary. — _Isolates a client/company/initiative._ `[extend]`
- **Create product under workspace** — Product A/B/C inside a workspace; vision/problem/target-users/metrics/stage. — _Run several products in parallel without bleed._ `[extend]`
- **Product switcher + portfolio view** — fast switch; cross-product overview. — _One operator, many products._ `[new]`
- **Per-product isolation** — data, agents, memory, budgets scoped by `user_id`+`workspace_id`+product. — _Security + correctness across products._ `[extend]`
- **Archive / delete product** — lifecycle hygiene. — _Avoid clutter; data governance._ `[new]`

### C. Agents — configuration

- **Agent roster** — list durable agents + spawned sub-agents per product. — _See the team you command._ `[extend]`
- **Create / clone / configure agent** — system prompt, tool allow-list, model, temperature/top*p/seed, max tokens, schedule, approval mode, memory toggle. — \_Tailor behavior + governance per role.* `[reuse]`
- **Enable / disable / schedule agent** — cron schedules with next-run preview. — _Autonomy on a clock._ `[reuse]`
- **Agent detail + run history** — last runs (status/duration/score/cost), memory inspector. — _Trust through observability._ `[reuse]`

### D. Agent execution

- **Run on-demand / scheduled** — manual trigger or cron fan-out. — _Both reactive and proactive autonomy._ `[reuse]`
- **Planner/executor loop** — plan → tool calls → observe → reflect → answer; max-step/max-cost caps. — _Reliable multi-step autonomy._ `[reuse]`
- **Approval gates + Decision Queue** — `auto|confirm|review`; queue of runs awaiting review; one-click resume from checkpoint. — _Governed autonomy; the trust core._ `[reuse]`
- **Cancellation / replay-and-branch / checkpoints** — stop mid-run (save partial), re-run against different model/prompt, resume. — _Control + iteration._ `[reuse]`

### E. Agent communication, coordination & transfer (mandatory)

- **Sub-agent spawning** — Orchestrator spawns ephemeral specialists per task. — _Scale to the work without fixed seats._ `[new]`
- **Agent-to-agent messaging** — structured internal A2A messages between agents in a mission. — _Coordination without a human relaying context._ `[new]`
- **Agent transfer / handoff** — pass a mission + its context, memory, and artifacts from one agent to another (e.g. Planner → Engineer → QA → Release). — _Work flows across the lifecycle with zero context loss; this is the spine of the autonomous loop._ `[new]`
- **Parallel sub-agents** — many specialists working a single mission concurrently. — _Speed; mirrors a real team._ `[new]`
- **Parallel agent sessions** — many missions running at once across products. — _One operator, many things in flight._ `[new]`
- **Orchestration / mission graph view** — live DAG of agents/sessions with per-node status, cost, approval state; pause/steer. — _See and command the swarm._ `[new]`
- **Shared vs. private memory** — mission-shared context + per-agent long-term memory with importance decay. — _Coordination without leakage._ `[extend]`

### F. Discover

- **Signal ingest** — paste/CSV now; connectors (Intercom/Zendesk/reviews/sales) next; sentiment on insert. — _Raw material of discovery._ `[extend]`
- **Clustering → themes → opportunities** — embed + cluster + synthesize with evidence ids; ICE scoring; dedupe by similarity. — _Turn noise into ranked opportunities._ `[reuse]`
- **Continuous discovery feed** — always-fresh per-product feed. — _The lead use case; ongoing, not one-shot._ `[extend]`

### G. Define

- **PRD/spec generation** — opportunity → structured cited draft. — _Specs downstream of evidence, not vibes._ `[reuse]`
- **Doc editor + `/ai` + versions/diff** — Tiptap, inline AI commands, autosave, version diff, citation pills. — _Author + iterate with provenance._ `[reuse]`

### H. Plan

- **Task graph** — spec → dependency-aware tasks with estimates/owners/risk. — _Executable plan agents can run._ `[reuse]`
- **Outcome roadmap** — Now/Next/Later; each item declares its outcome + measure. — _Anti-feature-factory; ties work to results._ `[extend]`
- **Scheduling** — agent proposes work blocks in working hours. — _Time-aware execution._ `[reuse]`

### I. Build (autonomous)

- **Studio multi-file coding** — agents write across files; per-hunk diff accept/reject; atomic revisions. — _Agents build, not just suggest._ `[extend]`
- **Watch-the-agents-build surface** — live per-session view: step, files, tool calls, cost, status; pause/steer/approve. — _Trust + control over autonomous code._ `[new]`
- **Branch/worktree isolation per mission** — each mission on its own branch. — _Parallel safe building._ `[new]`

### J. Test (autonomous)

- **Test generation + run** — agents author and run unit/integration/E2E + evals. — _Quality without a human writing every test._ `[new]`
- **QA gate + self-correct loop** — failing tests feed back to the build agent until green or escalate. — _Closed-loop quality before ship._ `[new]`

### K. Ship

- **PR / deploy / release notes** — open PRs, run deploy checklist, deploy, draft notes — behind approval. — _Autonomous delivery, governed._ `[new]`
- **Rollback triggers** — documented per release. — _Safe shipping._ `[new]`

### L. Launch / GTM / price

- **Launch + positioning + pricing + distribution drafts** — agent-drafted, human-approved before external. — _The lifecycle past code, that point tools skip._ `[new]`
- **Customer-facing pages / announcements** — generated, approved. — _Close the go-to-market gap._ `[new]`

### M. Operate / support

- **Ticket triage + draft answers + route/escalate** — agents handle inbound; themes flow back to Discover. — _Support closes the loop into discovery._ `[new]`

### N. Learn

- **Decisions + `supersedes`** — outcomes write decisions with lineage. — _Auditable "why," and belief updates._ `[reuse]`
- **Re-score + insight memo + daily brief** — outcomes re-rank opportunities; memo surfaces in the brief. — _The loop compounds._ `[extend]`

### O. Product Memory

- **Knowledge graph + query** — signals→decisions→outcomes, typed nodes/edges. — _Institutional context agents reason over._ `[new]`
- **Currency / drift on facts + skill packs** — flag stale facts; export versioned skill bundles over MCP. — _Memory stays true; reusable expertise._ `[new]`

### P. Trust & observability

- **Telemetry + traces + judge scores** — per-call events, trace waterfall, LLM-as-judge. — _Measure every AI action._ `[reuse]`
- **Guardrails + prompt studio + eval harness + drift + budgets + incidents** — input/output guards, versioned prompts + A/B, regression-gated evals, drift watch, budget caps, incident log. — _Make autonomy safe, improvable, affordable._ `[reuse]`
- **AI message contract** — score/model/latency/tokens/cost/citations/feedback/trace/replay on every AI message. — _Trust is the UI._ `[reuse]`

### Q. Interop (agent-native)

- **MCP server + client** — expose Circuit as tools; consume external tools. — _Be the place agents plug in._ `[new]`
- **A2A server/client + Agent Cards + scopes/limits/audit** — peer agents, delegate-to-agent, governed. — _Cross-vendor agent ecosystem._ `[new]`

### R. Platform & ops

- **Command palette (⌘K) + global search** — resolve every destination/action/artifact. — _Keyboard-first speed._ `[reuse]`
- **Connectors / integrations** — Docs, Notion, Linear, Calendar, GitHub, Slack, CRM. — _Act on the systems the operator lives in._ `[extend]`
- **Notifications + budgets config + settings** — approvals, breaches, guardrail rules, integration health. — _Operate the platform._ `[extend]`

---

## 3. Build order (v4 milestones — each independently demo-able)

> Canonical milestone definitions with proof bars: [`docs/strategy/v4-feature-map-2026-06-11.md`](./docs/strategy/v4-feature-map-2026-06-11.md) §9. The foundation invariants (blast-radius limits, spend caps, kill-switch, prompt-injection defense, provider fallback, durable runtime, sandboxed execution — see [`docs/planning/considerations.md`](docs/planning/considerations.md)) remain P0 architecture, already largely in place; every milestone below builds on them, never around them.

1. **M1 — The Golden Path (demo-ready).** One continuous, watchable run: connect Slack/GitHub → Scout themes → Strategist ranks (Critic attached) → approve → Scribe cited spec → approve → Planner task graph → Builder PR + CI green → release-note draft → outcome card. Includes `F-IA-V4` (collapse 34 routes to the 7-surface IA), vocabulary enforcement, cost-per-mission chip, narrated empty states. *This fixes "not demo-ready" before anything new is added.*
2. **M2 — The Loop Closes (the differentiator).** The right half becomes real: support triage loop (LRN-01), analytics inbound (SEN-05), launch kits (LCH-01), outcome reviews (LRN-02), visible re-score (LRN-03), memory consult/write on every mission (ENG-05), cost-per-artifact (ENG-06). Proof: a support-ticket cluster re-ranks the opportunity queue with zero human transport.
3. **M3 — The Team Arrives.** Roles + per-persona approval lanes (ENG-08), design scaffolds + handoff (DEF-04/05), visual validation (BLD-06), enablement notes (LCH-04), spec comments, multi-seat.
4. **M4 — The Org Runs Itself.** Portfolio view, budget enforcement UX, MCP server/client + A2A delegate-out (ENG-07, BLD-04), skill packs (LRN-05), enterprise plane (ENG-09), pricing briefs (LCH-03).
5. **M5 — Compounding.** Memory-graph querying as product surface, stale-fact drift, cross-product learnings, connector breadth, outcome-priced enterprise plans.

Sequencing rule unchanged: architecture first, so later stages are _additions, not rewrites_. See [`docs/decisions/tech-stack.md`](./docs/decisions/tech-stack.md).

---

## 4. Active build log (update as we ship)

> Append one line per shipped, real change as development proceeds in Claude Code / Lovable. Keep it true. Dedupe against the legacy log (section 5) — when a legacy capability is rebuilt or confirmed-reused, move its entry here and mark the legacy entry superseded.

- **2026-06-12 — `F-DESIGN-EMBER` screen 5 wave A ported (`ed8ac196c4` shared layer · `c51bccddd8` Product · `915cffaa48` Knowledge): Product + Knowledge mirror `design-reference/cadence/loop.jsx`; the screen-4 parallel method, scaled.** Method: the four loop-screen shared pieces (`SurfaceHeader`, `TabRow` w/ production id+badge contract, `EmptyState`, `RiskTag`) were promoted into `Primitives.tsx` by one owner BEFORE fan-out, then two workflow agents rewrote the two surfaces concurrently on disjoint files (route + panels each), side-by-side Playwright-verified after (all 10 tabs). **Product (6 tabs):** reference table/card anatomy 1:1 on real data — Signals capture + evidence-expand rows w/ strength bar (column honestly relabeled **Conf** — production datum is cluster confidence), Opportunities ranked serif rows w/ ICE + rescore deltas as **VerdictChips** (law; real `prior_ice→new_ice`), Specs table w/ CriticBadge + "Hand to **Studio**" behind the real GitHub-issue gate, Roadmap lanes + sprint-plan preview (real `planSprint`, honest capacity caption), Tasks kanban w/ orchid agent attribution, Releases split into two real groups (completed missions · agent runs w/ duration/tokens/cost). New additive `listSpecs` export (Critic/Cites columns). **Knowledge (4 tabs):** Company-brain strip w/ seven REAL head counts (new additive `getCompanyBrainStats`), Calendar list/Month/Year — contribution-style pixel month + year occupancy from real events+meetings (production's seeded placeholder events DELETED per no-filler; quick-add creates a real synced hold), Memory w/ VerdictChip-led learnings + real Product-memory stats (Ask-memory bento unported — no endpoint yet; queued hand-in-hand), Decisions table w/ verdict-chip status + real filters/log-decision, Docs card grid + tiptap editor w/ real autosave and **Push to Signals wired to `createSignal`** (reference toast contract). Truthful-copy corrections logged in both agents' reports (memory-write claims dropped where production writes nothing; "Historian/Strategist" attributions removed — no such agent identities). Mid-wave a concurrent session pushed to main; rebased clean (generated/docs files only). tsc ✓ scoped lint ✓. **Gate re-checked:** migration `20260612120000` (`messages.metadata`) still NOT applied — live probe via the app's Supabase client returns `column messages.metadata does not exist`; stays on the Lovable-sync path. **Files:** `src/components/cadence/Primitives.tsx`, `src/routes/_authenticated.{product,knowledge}.tsx`, `src/components/product/*` (+ new `format.ts`), `src/components/knowledge/*`, `src/lib/{discovery,brain}.functions.ts` (additive), this log.

- **2026-06-12 — Live published app fix: protected routes initialize again after publish.** Browser verification on `https://cadence-flow-beta.lovable.app/chat` showed the root error boundary was caused by the client auth bundle missing public backend URL/key values (`SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`) before the auth redirect could run. `vite.config.ts` now defines the public `import.meta.env` values from Vite-loaded env plus checked-in public fallbacks, preserving the existing SSR wrapper and auth flow while making the published browser bundle deterministic even when publish-time `process.env` is empty during config evaluation. **WHY:** the app was loading a 200 response but hydrating into the fallback page for every user-facing protected route. **Files:** `vite.config.ts`, live board, `architecture/frontend.md`, this log.

- **2026-06-12 — `F-DESIGN-EMBER` screen 4/10 ported (`85e5062dba`): Missions list + detail mirror `design-reference/cadence/missions.jsx`; first parallel co-development screen (founder directive).** Method: data layer extended first by one owner — `listMissions` now returns per-row step statuses + summed real cost (batched checkpoint-trace→`ai_events`; null renders "—", never a fake $0.00), `getMission` returns `usage` (cost/tokens/trace) — then two workflow agents built list and detail concurrently on disjoint files, side-by-side verified after. **List:** reference header/kicker/ember-underlined tabs (production `?tab=` kept), MissionRow 1:1 (bento, StepDot strip, mono cost + started stamp), Agents tab leads with the reference 4-col card grid from live swarm data with the telemetry HUD token-swept beneath; composer kept quiet-Ember; attention-queue actions relabeled consequence-first. **Detail:** hero-editorial band with real started/cost/tokens/trace stats (trace → `/traces/$traceId`), Plan List|Graph toggle, the reference Ember DAG replaces the old `MissionGraph` (hub, dashed dispatch, status-barred nodes, click-to-inspect; `.gnode-live` added), screen-3 GatePanel on live-hop approvals, TraceHop tinted execution rail with timing bars, failed-mission retry (real `startOrchestratedMission`, truthful copy), all production extras kept (memory context, handoff payloads, input/output, capture-as-decision, polling, Advance). **Post-fan-out fix:** agents-grid track blowout from sentence-long role kickers (min-width 0 + ellipsis). Deviations are itemized in the two agents' reports (status-vocab extras `completed_with_failures`→failed, `paused`→waiting; date format; adapted empty-state copy; reactor QuietPill). Verified live: rows w/ real dots, contained grid, orchestrated-mission hero/plan/graph/trace, honest empty states. tsc ✓ scoped lint ✓. **Files:** `src/lib/missions.functions.ts`, `src/routes/_authenticated.missions.{index,$missionId}.tsx`, `src/components/cockpit/{MissionsPanel,AgentsPanel}.tsx`, `src/components/cadence/MissionGraph.tsx`, `src/styles.css`, this log.

- **2026-06-12 — `F-DESIGN-EMBER` screen 3/10 ported (`cc34fee8c2`): Brain/Chat mirrors `design-reference/cadence/chat.jsx`; the AI footer contract is now real, end to end.** Layout/spacing/copy verbatim from the reference: 224px threads rail (mono header + ghost plus), 720px conversation column (gap 22), Bubble authorship contract (user = soft-stone 14/14/4/14 + ember-ringed initials chip right; AI = Butterfly 24), spinner "thinking" + stream caret states, composer (hairline 14px ring on canvas, ArrowUp `btn-primary`, footer line "Enter to send · goals become missions · gates always come back to you"), MissionCockpit (ai-glow while working, numbered StepDot specialist rows, raw-trace drawer, open-mission link), GatePanel (ember 9%/35%, consequence-labeled `btn-approve`/`btn-reject`). **Hand-in-hand functionality built (founder rule 2):** LLM-as-judge score on every chat reply (post-completion `callModel` surface `judge`, time-capped 8s, tolerant → moss judge pill); "Replay with…" genuinely re-asks the question with the chosen model in-thread; client-measured ttft; tokens "Nk in / N out"; migration `20260612120000` adds `messages.metadata` so footers + citation chips survive reloads (insert retries without metadata pre-apply — **migration NOT yet applied: supabase MCP read-only/unauthorized this session; applies via the usual Lovable sync**). AppShell children wrapper → flex column (reference `app.jsx` main wrapper) so Chat pins to the viewport with internal scroll; `.spinner` + `.cmdk-item` ported into `src/styles.css`. **Truth-over-mock deviations (listed per founder rule 1):** replay popover header says "the reply lands in this thread" (production replay produces no diff); gate labels generic ("Approve · runs the tool") — approvals carry no per-call consequence copy yet; surface name is **Brain** (platform rename) where the reference says Chat; thread unread-dots + mission-branch markers pending (no per-thread unread/mission data); reference's seeded-thread script not ported (production threads are real). Verified live (Playwright on the dev server, demo login): composer, footer w/ judge=100 pill, replay popover, bubbles, citations, sources row. tsc ✓ scoped lint ✓. **Files:** `src/routes/_authenticated.chat.tsx` (rewritten), `src/components/chat/{MessageMeta,ModelSwitcher,ResearchActivity}.tsx`, `src/components/cadence/AppShell.tsx`, `src/styles.css`, `src/routes/api/chat.ts`, migration `20260612120000`, this log.

- **2026-06-12 — Verdict chips platform-wide (`0cd80cabe3`): the founder's inline annotation pattern (KEEP / CORRECT / ADD NEXT) is now a codified DESIGN.md rule + canonical primitive.** Founder ruling (reference image, design-review annotations): when content carries a judgment, lead with a mono-caps OUTLINE pill in the role color — moss keep/ship · ember needs-correction · indigo next-action · orchid agent-performed · saffron highlight · madder kill/missed; no dot/icon/fill (VerdictChip = rendered judgment ≠ StatusBadge = live state); applies to every current and future screen, no-filler rule intact. New `VerdictChip` in `src/components/cadence/Primitives.tsx`; applied where verdict data already exists: `CriticBadge` (ship/revise/kill — shed the pre-Ember emerald/amber/rose-500 dark palette) and `OutcomeCard` (validated/mixed/missed display + picker). **Rollout queue for remaining surfaces:** eval results, drift findings, rescore deltas, brief callouts — apply on touch. DESIGN.md gained the full "Inline verdict chips" section + production-mapping row (sync-back to the design project flagged). **Files:** `DESIGN.md`, `src/components/cadence/Primitives.tsx`, `src/components/governance/CriticBadge.tsx`, `src/components/product/OutcomeCard.tsx`, this log, decisions log.

- **2026-06-12 — `F-STUDIO` hardened: first-class nav + 17-agent adversarial audit, 8 confirmed bugs fixed pre-runtime.** Founder ruling: palette/shortcut/URL are accelerators, not navigation — **Studio is now a sidebar entry** (Product group, `AppShell`). Then a dynamic workflow (6 finder lenses → per-finding adversarial refutation; 17 agents) audited the logic loops; 11 findings confirmed, 8 fixed: **(1)** pause-gate checkpoint off-by-one (`checkpoint(i+1)` → `checkpoint(i)`) — a gate hit on the final step resumed past the loop bound and falsely finalized "step limit"; **(2)** `studio.commit` idempotency key was message-only — a CI-fix commit reusing the message returned the cached result and pushed nothing (now keyed `changeset:contentFNV:message`); **(3)** `resolveApproval` (Today calls queue path) approved WITHOUT executing — paused sessions blocked forever; now executes on approve, unified with `decideApproval`; **(4)** `handle_new_user` replacement seeds studio tools for new signups (backfill only covered existing users); **(5)** steers were marked consumed before the checkpoint persisted them (losable on eviction; now consume-after-checkpoint); **(6)** double-resume race guarded with compare-and-swap status promotion; **(7+8)** error states + retry on both `/studio` routes. 3 findings accepted as design and documented in `studio.md` §Graceful degradation (idempotency retries failures by intent; CI snapshot owner-scoped by RLS; 7-day gate expiry). **Local smoke walkthrough passed** (browser-driven: sidebar entry, `/studio` + composer + legacy-session list, session detail, `/build` redirect, "Send to Studio" in PRD menu, zero console errors; pre-migration degradations behave as documented). Verify: lint ✓ tsc ✓ build:dev ✓. **Files:** edited `src/lib/ai/loop.server.ts`, `src/lib/ai/tools/registry.server.ts`, `src/lib/governance.functions.ts`, `src/components/cadence/AppShell.tsx`, `src/routes/_authenticated.studio.{index,$missionId}.tsx`, migration `20260612100000` (§7 added pre-apply), `docs/features/studio.md`, this log.

- **2026-06-12 — `F-DESIGN-EMBER` shipped: the Ember Editorial design system is the platform's UI layer; screen-by-screen migration begun (Home·Today = screen 1, ported 1:1).** Source of truth: root `DESIGN.md` + `design-reference/` (the runnable design of record, consolidated this session). **Token layer (`0238a3c1fd`):** `src/styles.css` rewritten from `design-reference/cadence/tokens.css` — parchment light + char-night dark, exclusive color roles (ember=needs-human · indigo=live/links · orchid=agent-only · saffron=highlight · moss/madder=outcomes · espresso=approve), legacy aliases (`--coral`→ember, `--violet`→orchid, `--cyan`→indigo, `--amber`→saffron) flip all 24+ token-consuming surfaces without per-component edits; Newsreader/Schibsted Grotesk/JetBrains Mono replace Fraunces/Inter (`__root.tsx`); **aurora theme retired** (Rule 0) — themes are Light (default) / Dark, stored `aurora` migrates to dark; sidebar on `--sidebar` (char: one step darker); CookingBanner → subtle ember sweep. A parallel 8-agent workflow audited every route/component slice: 72 findings, 61 high-confidence fixed (hardcoded hexes→tokens, cool grays→warm equivalents, violet-300→`text-agent`, …), 11 medium flagged for review. **Screen 1 port (`2d086f9940`):** Home·Today now mirrors `design-reference/cadence/home.jsx` exactly — hero band (ghost butterfly, call-word headline, mono stat row, calls-cleared X/Y ring that fills as calls are cleared), reference-anatomy calls queue with consequence-labeled gates, brief, agent rail, Overview/Agent-activity tabs, footer build stamp; new `src/components/cadence/Primitives.tsx` (CadenceMark/MonoLabel/StepDot/StatusBadge/Cite) + token utility classes. **Port notes (reference mock → production truth):** greeting localized (not hardcoded "Morning"); hero subline is a real-counts sentence (mock "14 signals overnight" copy had no source); budget cap omitted from the hero stat (only spend is wired here); PriorityBars = real per-product task completion (no trend arrows — no source); reference's "State of the product" + "Stakeholder pulse" mock-metric panels omitted per DESIGN.md anti-filler rule; throughput panel shows real run counts/median; agent-chip notes are status-derived ("running now"/"idle · ready"); chips keep production tap-to-dispatch. Dropped from Home per the contract: 4-tab layout, DecisionPanel, CopilotChat, quick-forms (their jobs live on Knowledge/Brain/Product). **Incident logged:** a concurrent session's git ops wiped the first uncommitted application of this work and stubbed 2 route files (restored in `a83e19678e`; `routeTree.gen 2.ts` artifact untracked + gitignored) — work is now committed at each milestone. Verify: tsc 0 · lint clean · build:dev ✓ · Playwright side-by-side vs the served reference, light + char-dark. **Next screens:** shell (sidebar/topbar) → Chat → Missions → Product/Knowledge/Govern/Settings → onboarding. **Files:** `src/styles.css`, `src/routes/__root.tsx`, `src/hooks/use-theme.tsx`, `src/components/cadence/{AppShell,CookingBanner,Primitives}.tsx`, `src/routes/_authenticated.index.tsx`, 20+ workflow-fixed files (swept into `f4b66457e4`), `DESIGN.md` (root, + production mapping), this log, decisions log.

- **2026-06-12 — `F-STUDIO` code landed: Studio, the in-platform development engine — the "Send to Builder" human seam is gone.** Spec/feature doc: `docs/features/studio.md` (supersedes Bundle 9's handoff UX; Builder agent slug + tables stay as legacy ≡ Studio — disclaimer added to CLAUDE.md beside Cadence→Circuit). **Engine:** new migration `20260612100000_f_studio_engine.sql` (`studio_changesets` + `studio_changes` w/ workspace RLS; `agent_approvals.run_id/mission_id/workspace_id`; `agent_runs.model`; agent display name → Studio + dev-engine prompt (explore-before-edit, stage→ship→verify, forbidden paths); `seed_studio_tools` — repo.\*/studio.stage `auto`, commit/PR `confirm`, merge `review`) — **applies via Lovable sync (KI-08 pattern); golden-path QA + demo inclusion follow the apply per the 2026-06-12 ruling**. 7 new registry tools: `repo.tree/read/search` (GitHub reads via `requireGithub`), `studio.stage` (multi-file DB changeset, base snapshots, forbidden-path rejection), `studio.commit` (Git Data API blobs→tree→commit→ref onto isolated `studio/<mission>` branch; claims every path in `builder_file_claims`), `studio.pr.open` (multi-file PR), `studio.pr.merge` (squash; releases claims) — all GitHub mutations `withIdempotency`. **Loop (surgical):** slug `builder` → 24 steps; mid-session steer injection (`agent_messages kind='steer'`, consumed per step; `consumeInboundHandoff` now filters `kind='handoff'` so steers are never eaten); safety floors (commit/PR ≥ `confirm`, merge always `review`); **shipping gates pause the run** — checkpoint at i+1, `status='waiting_approval'`, sweeper re-enters once the gate is decided AND executed, injecting the approval outcome (tracked by id in checkpoint state, no double-inject); `executeApproval` carries mission ctx. **KI-02 tick hardening:** resume-runs now also picks `running` runs with NULL `last_checkpoint_at` + stale `created_at` (SQL `lt` never matches NULL — these were invisible) and gate-resolved `waiting_approval` runs. **Server (`studio.functions.ts`, new):** `dispatchStudioSession` (PRD/opportunity/prompt work order, queue-then-return `{missionId}`, model on the run row, prd→mission lineage — `ArtifactKind` gained `'mission'`), `listStudioSessions` (legacy Builder missions included; cost via checkpoint-trace→`ai_events`), `getStudioSession` (timeline steps from checkpoints, changeset+changes, gates, CI snapshot, steers, per-run cost), `steerStudioSession`, `getChangesetDiff`, `refreshStudioCi`. **Surface (`/studio`, first Monaco use):** index = composer (NL prompt, approved-PRD picker, `ModelSwitcher`) + live session list (5s poll; `waiting_approval` reads "waiting on you"); `$missionId` = left timeline (thoughts/tool calls/finals + steers interleaved + inline approve/reject gate cards via `decideApproval`) and right tabs `?tab=changes|pr|cost` (lazy `DiffEditor` per file, PR+CI with refresh, per-run cost); `/build` → `/studio`; CommandPalette "Studio" (G B). Send-to-Studio rename completed in SpecsPanel/PRD detail/product/chat copy (landed pre-interrupt this morning, now compiles against the real engine). Verify: lint ✓ (0 errors) · `tsc --noEmit` ✓ exit 0 · `build:dev` ✓. **WHY:** founder — the platform's day-one intent is everything unified in-platform; with Research+Brain shipped, build was the last human-shaped seam. Two doors by design (~80% agent / 20% human, both first-class); model-agnostic via the chokepoint (no new `CallSurface`). **Files:** new migration, `src/lib/studio.functions.ts`, `src/routes/_authenticated.studio.{index,$missionId}.tsx`, `src/components/studio/*` (7); edited `src/lib/ai/{loop,handoff}.server.ts`, `src/lib/ai/tools/registry.server.ts`, `src/lib/lineage.functions.ts`, `src/routes/api/public/hooks/resume-runs.ts`, `src/components/cadence/{CommandPalette,LineageDrawer}.tsx`, `src/routes/_authenticated.build.tsx`, rename-sweep files, `CLAUDE.md`, `docs/features/{studio,bundle-9-builder}.md`, `docs/strategy/v4-feature-map-2026-06-11.md`, `docs/README.md`, this log, decisions log, `active-task.md`.

- **2026-06-12 — `F-BRAIN` shipped: the surface is the Brain, and it remembers.** Rail/palette/header renamed **Brain** (lucide Brain icon; tagline "Everything inward and outward — captured, cited, compounding."). New `src/lib/rag/findings.server.ts` (`indexFinding`: embed best-effort — nullable embedding column confirmed —, per-conversation chunk_index to honor the rag_chunks unique index) + `src/lib/brain.functions.ts` (`rememberMessage`, `getBrainStatus`). Auto-retention: research answers (mode ≠ chat, >300 chars) fire-and-forget distilled into `rag_chunks` as `source_kind='finding'` with their numbered sources — future questions recall them as "Past finding: …" citations (kind `finding`, Brain icon, href `/chat`). Message actions: **Remember this** + **Capture as decision** (createDecision, manual/pending) in the answer footer; **Brain status** popover ("The brain knows · N signals · docs · meetings · decisions · PRDs · findings · updated X ago"). **Security:** same unit fixes the citation-chip XSS flagged by automated review — scheme allowlist (https?:// web, root-relative internal; `javascript:`/`data:`/`//` degrade to unlinked chips), rule added to the citations contract in the operator doc. Verify: lint ✓ tsc ✓ build ✓. Decision entry: company-brain positioning (YC thesis = moat pillar #4 surfaced). **Files:** new `src/lib/rag/findings.server.ts`, `src/lib/brain.functions.ts`; edited `src/routes/api/chat.ts`, `src/lib/ai/research.server.ts`, `src/routes/_authenticated.chat.tsx`, `src/components/chat/MessageMeta.tsx`, `src/components/cadence/{AppShell,CommandPalette}.tsx`, `docs/features/brain.md` (retitled Brain + safety rule), board, decisions log.

- **2026-06-12 — `F-RESEARCH` shipped: the chat surface is now "Research" — Perplexity-grade over both worlds.** New `src/lib/ai/research.server.ts`: classifier v3 routes chat|web|internal|both; web mode decomposes into ≤3 sub-queries fired in parallel (top 6 deduped sources); internal mode = RAG k=8 + structured snapshots (top-5 opportunities by ICE, roadmap lanes — adapted to reality: no `roadmap_items` table exists, lanes read `opportunities` statuses —, newest decisions, running missions); ONE numbered citation space across web+workspace; SSE protocol v2 streams research-progress events (plan/search/read/workspace/synthesize) before tokens; meta sources carry `{kind, href}` deep links (prd→`/prds/<id>`, opportunity→Product·Opportunities, decision→Knowledge·Decisions, …). UI: rail/palette renamed **Research** (Sparkles), sidebar = Threads, live activity line collapsing to a persistent summary row, kind-iconed source chips (web=new tab, workspace=deep link), inline `[n]` badges that scroll-highlight their chip, new starter prompts. Verify: lint ✓ tsc ✓ build ✓. Operator doc: `docs/features/brain.md` (+ docs/README row). **WHY:** founder — chat must be a Perplexity-equivalent researcher across the external and internal worlds, with citations as receipts and model choice in-window. **Files:** new `src/lib/ai/research.server.ts`, `src/components/chat/ResearchActivity.tsx`, `docs/features/brain.md`; edited `src/routes/api/chat.ts`, `src/routes/_authenticated.chat.tsx`, `src/components/chat/{MessageMeta,ChatMarkdown}.tsx`, `src/components/cadence/{AppShell,CommandPalette}.tsx`, board, docs/README.

- **2026-06-12 — `F-CHAT-V2` shipped: chat is a real assistant — web-grounded, workspace-grounded, model-switchable, never raw-errors. Plus the root cause of "chat never responds": a missing auth header since day one.** Server (`api/chat.ts`): classifier extended with `{needs_web, web_query}`; web path runs `webSearch` (4 results, scraped, injected as `<untrusted_web_source>` blocks with `[n]`-citation instructions) and degrades gracefully when Firecrawl is absent ("couldn't verify live data"); workspace RAG (`retrieve` k=4 MMR) injected every turn; `body.model` persisted to `conversations.model`; BYO models without a key stream a friendly add-your-key message; every failure path streams a readable sentence; a final SSE `meta` event carries `{model, via, latency_ms, tokens, cost_usd, sources, web_used, workspace_chunks}`. UI (`_authenticated.chat.tsx` + new `src/components/chat/{ChatMarkdown,MessageMeta,ModelSwitcher}.tsx`; frontend-design + emil-design-eng skills invoked): centered reading column, explicitly-styled markdown (discovered the `prose` plugin was never loaded — the "boilerplate look" explained), code blocks with copy, blinking-caret streaming, pinned-aware autoscroll, Stop button, design.md footer (model·via·latency·cost + source chips + 👍/👎 wired to `submitFeedback`), ChatGPT-style model switcher (Built-in vs Your-keys groups, keyless rows link to Settings → AI). **ROOT-CAUSE FIX:** the chat fetch never attached the `Authorization: Bearer` header in ANY prior version — every `/api/chat` call 401'd silently (network trace evidence), masquerading as "does not respond"; now attaches the session token + friendly 401 copy. **Proof (Playwright, founder's Gemini key):** "What is the weather in Munich?" → graceful, smart, disclosed-unverified answer + live footer `Gemini 3 Flash · 16.7s · $0.0001`. Verify: lint ✓ tsc ✓ build ✓ (verifier also repaired a second iCloud `node_modules` eviction — KI-11 recurring). **WHY:** founder — chat must speak to the internal and external world and actually deliver outcomes on the user's key, with ChatGPT-grade feel. **Files:** edited `src/routes/api/chat.ts`, `src/routes/_authenticated.chat.tsx`; new `src/components/chat/*` (3); this log, board.

- **2026-06-12 — Settings reorganized + Connections redesigned (founder design feedback; impeccable/emil-design-eng skills applied); calendar accounts merged into Connections.** Settings (`_authenticated.settings.tsx`) is now sectioned — Profile · Workspace (brief + voice anchor) · Connections · AI & models — with a compact sticky section nav driven by `?section=` (legacy values remapped: `brief`→workspace, `calendar`→connections), a modest text-xl header replacing the display typography, small right-aligned save buttons replacing giant pills, and provider/BYO blocks restyled as hairline-divided rows. Connected accounts (`AccountConnectionsSection` + new `ConnectionRow`; `ProviderCard` deleted) is one dense bordered list — h-8 icon tile, name, one-line description, status dot + small action per row; "Admin setup required" panels collapsed to an amber dot + tooltip + one shared footnote. **Calendar merge:** Google Calendar/Outlook are real rows wired to the existing calendar OAuth layer (multi-account sub-row chips, connect/disconnect preserved); `CalendarAccountsSection` removed; "Managed via Calendar settings" path gone. **Proof run (Playwright, dummy client IDs injected):** rows flip from "Setup required" to live Connect buttons purely on env secrets — screenshot evidence that each founder registration enables its provider with zero code changes. Verify: lint + build + `tsc --noEmit` all green. **WHY:** founder — settings page was "blown up", icons oversized, organization poor; calendar belonged with connectors; and the disabled buttons needed to be provably env-gated, not assumed. **Files:** edited `src/routes/_authenticated.settings.tsx`, `src/components/connections/AccountConnectionsSection.tsx`; new `src/components/connections/ConnectionRow.tsx`; deleted `ProviderCard.tsx`; this log.

- **2026-06-12 — `F-CONN` OAuth-only correction shipped (founder ruling): one Connect button per provider, no API-key paste anywhere.** `registry.ts`: linear/notion/google_docs/figma/jira flip to `oauth_gateway` auth (connector client-ID envs per provider; Google client shared by Calendar+Docs with docs/drive readonly scopes); firecrawl de-listed from the UI (`userFacing:false` — env fallback for the web tools intact); per-provider `setupHint`. `connections.functions.ts`: generalized `startGatewayConnect`/`saveGatewayConnection` (web_message popup, gateway holds all tokens, we store only the connection handle); `connectWithApiKey` removed; `providerAvailability` now reports `{configured, missingEnv[]}` per provider. UI: `ApiKeyConnectDialog` **deleted**; ProviderCard renders an explanatory "⚙ Admin setup required" panel (registry hint + checklist pointer) instead of a mute disabled button; GitHub pending copy names the exact missing secrets. Verify caught + fixed a real bug pre-ship (OAuth save persisted the start sessionId instead of the gateway connection_id — calendar precedent restored). Same unit: 4 strict-typing errors exposed by the regenerated route tree fixed (AppShell pause-banner Link search, learn redirect search, missions.index from-path) — **repo `tsc --noEmit` now exits 0**. Founder ops: per-provider OAuth client-ID checklist in `active-task.md`; each secret flips that card live with zero code changes. **WHY:** founder correction — end users authenticate at the provider and grant permissions; keys-in-our-DB rejected; disabled states must explain themselves. **Files:** edited `src/lib/connectors/registry.ts`, `src/lib/connections.functions.ts`, `src/lib/ai/tools/firecrawl.server.ts`, `src/components/connections/{ProviderCard,AccountConnectionsSection}.tsx` (ApiKeyConnectDialog deleted), `src/components/cadence/AppShell.tsx`, `src/routes/_authenticated.{learn,missions.index}.tsx`, `src/routeTree.gen.ts` (regenerated); this log, decisions log, `active-task.md`.

- **2026-06-12 — `F-CONN` Phase 1 code landed (4-agent parallel build): the Connector Platform base — connect once at account level, bind resources per workspace, one credential chain.** New `src/lib/connectors/` namespace: typed provider registry (9 providers), WebCrypto AES-256-GCM vault (`crypto.server.ts`, key `CONNECTOR_SECRETS_KEY`), `resolveProviderAuth` chokepoint (workspace binding → user connection → env fallback flagged deprecated → actionable error; works session-less for crons via service-role keyed on workspace_id), GitHub App adapter (RS256 app JWT → ephemeral installation tokens cached ~50min, never stored; HMAC-signed connect state; `resolveGitHub` absorbs the duplicated repo normalizers) + public OAuth callback route. New migration `20260612080000` (`connections` own-row RLS · `connection_bindings` membership RLS + service-role grant · `connection_secrets` ciphertext-only no-authenticated-policy vault · `user_api_keys` cipher columns) — **applies via Lovable sync**. New `connections.functions.ts` (10 server fns) + Settings **"Connected accounts"** section (provider cards, connect/verify/disconnect, setup-pending states) + `/sync` **"Workspace bindings"** section (repo picker → bound chip with owner attribution). **9 GitHub call sites migrated** off `process.env`: `build/discovery/outcome.functions.ts`, `outcome-tick` (now groups PRDs per workspace), and the 4 GitHub agent tools via `requireGithub(ctx)`; env fallback keeps current behavior bit-for-bit until a binding exists. Docs corrected: CLAUDE.md stale vendored-`nango` claim, `architecture/{integrations,security}.md` pgsodium→AES-GCM + the new invariant (no provider env read outside `resolveProviderAuth`). Verify: lint + `build:dev` green; migration audited idempotent/additive; verify agent reconciled two cross-builder contract seams and repaired an iCloud-evicted `node_modules` (KI-11). Same unit: `.env` untracked from git (was tracked pre-gitignore; historical content public-safe; `GEMINI_API_KEY` never committed). **WHY:** founder directive — enterprises keep their stacks; Circuit connects to them, end-user self-serve, zero env vars; this is the base that connects the broken pieces. **Files:** new migration, `src/lib/connectors/**` (7 files), `src/lib/connections.functions.ts`, `src/components/connections/**` (5), `src/routes/api/public/connect/github/callback.ts`; edited `build/discovery/outcome.functions.ts`, `outcome-tick.ts`, `ai/tools/registry.server.ts`, `_authenticated.{settings,sync}.tsx`, `CLAUDE.md`, `architecture/{integrations,security}.md`, doc set.

- **2026-06-12 — `F-V5-INGEST-WEBHOOK` code landed: the continuous-ingest door, universal half.** New `ingest_tokens` migration (`20260611190000`, idempotent, workspace RLS — applies via Lovable sync per the Lovable-operated-DB decision); new `src/lib/ingest.functions.ts` (`getIngestToken` / `rotateIngestToken` / `revokeIngestToken`, 64-hex server-generated tokens); new public `POST /api/public/ingest-signals` (Bearer ingest-token auth via service-role lookup, liberal zod body — single object or `{signals:[…]}` max 50 — stamped with the token's user/workspace, source defaults `webhook`); "Webhook ingest" card on Connectors (`/sync`): endpoint + copy, masked token with reveal/rotate(two-click)/revoke, curl example with `YOUR_TOKEN` placeholder. **Confirmed:** webhook inserts fire the existing `signals_reactor_fanout` AFTER-INSERT trigger → `signal.created` auto-pipeline → Scout, so external feedback reaches discovery with zero human transport. Verify: lint + `build:dev` green. Deferred risks logged (KI-10): no per-token rate cap (rotation is the mitigation), plaintext tokens, workspace-wide rotation. **WHY:** the operator audit's #1 abandonment moment was "why isn't my feedback in here?" — this ships the v5 Phase C door via the universal fallback while the Slack OAuth connector stays gated on founder creds (a Slack outgoing webhook can feed this today). **Files:** new migration, `src/lib/ingest.functions.ts`, `src/routes/api/public/ingest-signals.ts`; edited `src/routes/_authenticated.sync.tsx`; this log, backlog board, `active-task.md`, `docs/planning/known-issues.md` (KI-09/KI-10). _Also: `F-V5-LOOP-CLOSE` was founder-verified e2e on the hosted app late 2026-06-11 (Lovable applied its migration; real issue close → shipped → outcome → re-score)._

- **2026-06-11 — `F-V5-LOOP-CLOSE` code landed (parallel agent build): the ship→measure→learn→re-rank loop is real on the wedge.** New migration `20260611161500_f_v5_loop_close_learnings.sql` (NOT yet applied — awaiting founder authorization): `learnings` table (verdict ∈ validated/missed/mixed, prior_ice/new_ice audit trail, workspace RLS), `prds.shipped_at` + `prds.outcome`, `profiles.voice_anchor_text`, hourly `outcome-tick` pg_cron registration. Server (`outcome.functions.ts`): `checkPrdShipped` reads the linked GitHub issue state and stamps `status='shipped'` idempotently; `recordOutcome` writes the outcome, adjusts the source opportunity's confidence by verdict (±2 clamped), inserts the learning with prior/new ICE; `listLearnings`; new `api/public/hooks/outcome-tick.ts` polls up to 20 approved+issue-linked PRDs hourly. UI: `OutcomeCard` on PRD detail (check-ship → outcome form → recorded verdict + re-score line) mounted after Citations; re-score delta chip beside ICE in `OpportunitiesPanel`. Loop layer: **KI-07 fixed** — non-governance model errors now set `agent_runs.status='failed'` + `missions.status='halted'` instead of zombie "running"; recalled memories persisted in checkpoint state and surfaced as a "Memory context · N" pill on mission hops; **voice anchor built end-to-end** (settings textarea → `profiles.voice_anchor_text` → injected into agent system prompts in both run/resume paths; the earlier claim that this existed was confirmed false). Verify agent: lint + `build:dev` green; migration audited purely additive. **WHY:** this is M2's proof bar in miniature on the v5 wedge — the differentiating right half of the loop, where the stress test said we were weakest. **Files:** new migration, `src/components/product/OutcomeCard.tsx`, `src/routes/api/public/hooks/outcome-tick.ts`; edited `src/lib/outcome.functions.ts`, `src/lib/ai/loop.server.ts`, `src/lib/profile.functions.ts`, `src/lib/missions.functions.ts`, `src/lib/discovery.functions.ts` (prettier only), `src/routes/_authenticated.{prds.$id,missions.$missionId,settings}.tsx`, `src/components/product/OpportunitiesPanel.tsx`; this log, backlog board, `active-task.md`, `docs/planning/known-issues.md` (KI-07 → resolved).

- **2026-06-11 — Gemini local-dev fallback shipped in the AI chokepoint (KI-06 mitigation).** New `resolveGateway()` in `src/lib/ai/runtime.server.ts`: when `LOVABLE_API_KEY` is absent (local `.env`), `google/*` models route directly to Google's OpenAI-compatible endpoint via `GEMINI_API_KEY` (model prefix stripped); the Lovable gateway always wins when its key exists, so cloud behavior is byte-identical. Wired into both `callGateway` (awaited) and the streaming attempt path; clearer error names both keys. `.env.example` documents the var; `architecture/runtime.md` §Provider adapters records the contract; embeddings stay Lovable-only (indexing degraded locally, retrieval of seeded chunks unaffected). `bun run build:dev` green. **WHY:** the v5 walkthrough proved every model path dead in local dev for want of one secret — a free AI Studio key now unblocks local founder velocity without touching the cloud path. **Files:** edited `src/lib/ai/runtime.server.ts`, `.env.example`, `architecture/runtime.md`, `docs/planning/known-issues.md` (KI-06), this log.

- **2026-06-11 — `F-V5-RITUAL` + `F-V5-MOTHBALL` code landed (parallel agent build): Today is the Calls queue; nav cut to the v5 felt product.** Today (`_authenticated.index.tsx`) gains a "Needs you · N calls" section between hero and brief — pending `agent_approvals` as cards with inline "Approve · run <tool>"/Reject (`resolveApproval`), specs in review and Critic-challenged opportunities with `CriticBadge` + deep links — fed by new `src/lib/today.functions.ts` (`getNeedsYou`: approvals + prdCalls + oppCalls + `spendTodayUsd`). Header gains a cost chip (today's `ai_events` spend) and a **Start mission** popover calling `startOrchestratedMission` directly (kills the chat-classifier dependency); hero copy is call-count-aware. `ensureTodayBrief` now leads with the operator's calls, then overnight agent runs, then one focus. Nav (AppShell): rail = Today · **Approvals** · Chat; groups = Product · Missions · Knowledge; Build/Learn/Agents/Govern groups removed; new **Trust** icon row in the sidebar footer (Approvals · Budgets · Engine Room `/govern` · Connectors); `/build` → `/`, `/learn` → `/knowledge`, `/agents` → `/missions?tab=agents` beforeLoad redirects (code preserved); CommandPalette repointed; "Orchestrator" → "Chief of Staff" in hardcoded UI strings (slugs/DB untouched). Verified: `bun run build:dev` exit 0; all touched files lint clean (3,385 pre-existing prettier errors in untouched files logged for a separate cleanup). Demo-account walkthrough (v5 bar 1) pending. **WHY:** the daily ritual is the v5 product — the calls queue puts the PM's judgment at the front door, and the mothball removes the engine-room costume that made the product feel overwhelming. **Files:** new `src/lib/today.functions.ts`; edited `src/routes/_authenticated.index.tsx`, `src/lib/copilot.functions.ts`, `src/components/cadence/AppShell.tsx`, `src/components/cadence/CommandPalette.tsx`, `src/routes/_authenticated.{build,learn,agents}.tsx` (redirects), `src/components/cockpit/{AgentsPanel,MissionsPanel}.tsx`, `src/routes/_authenticated.missions.$missionId.tsx` (vocabulary); `active-task.md`, `docs/planning/feature-backlog.md`, this log.

- **2026-06-11 — v5 Chief-of-Staff wedge adopted (docs landed; `F-V5-RITUAL` begins).** Founder ratified identity (PM Chief of Staff — the daily Calls-queue ritual is the felt product; cockpit = expansion), mothball-hard surface cut (nav → Today · Product · Knowledge · Chat + Trust drawer; Govern → drawer-accessed Engine Room; `/build`/`/learn`/agents-config hidden via redirects), and Slack as the first ingest door. Grounded in a three-agent audit: the engine (signals → Critic → cited PRD → decision) is real; the ~50–70-destination org-OS costume is the overwhelm; vitality gaps = daily ritual, continuous ingest, loop closure. Phases A–E minted as `F-V5-RITUAL` / `F-V5-MOTHBALL` / `F-V5-SLACK` / `F-V5-LOOP-CLOSE` / `F-V5-DEMO`, sequenced to June 22. **WHY:** post-F-IA-V4 the founder still found the product "overwhelming, disconnected, not vital" — the fix is focus + completion of the real spine, not more scope. **Files:** new `docs/strategy/v5-chief-of-staff-2026-06-11.md`; edited `docs/strategy/README.md`, `docs/strategy/session-decisions.md`, `plan.md` (§1 banner + this entry), `README.md`, `docs/planning/feature-backlog.md` (status board), `active-task.md`, `Ai_Cofounder.md`, `CLAUDE.md`, `GEMINI.md`, `.lovable-config.txt`.

- **2026-06-11 — Doc hygiene: live known-issues tracker created; anti-redirect-rot rule codified; plan.md §6 heading repaired.** New `docs/planning/known-issues.md` (stable KI-IDs; seeded with KI-01 calendar OAuth secrets, KI-02 FND-RUNTIME kill-test, KI-03 Lovable Knowledge re-paste; doubles as the June 22 demo punch list) — closes the constitution's `KNOWN_ISSUES.md` gap, concordance row updated. AGENTS.md §7 gained the "stale redirect rot" friction pattern (relative in-repo links only in redirect stubs). The §6 heading corruption in this file (spliced mid-line into a legacy bullet) repaired. **WHY:** the constitution's one unmapped mandate needed a real home, and an 18-stub failure class met §7's add-a-row threshold many times over. **Files:** new `docs/planning/known-issues.md`; edited `AGENTS.md` §7, `plan.md` (§6 heading), `docs/README.md`, `ENTRY.md`, `Ai_Cofounder.md` (concordance row), `docs/strategy/session-decisions.md` (footer date).

- **2026-06-11 — `Ai_Cofounder.md` adopted as founding constitution and interlinked across the doc graph.** The founder added `Ai_Cofounder.md` (co-founder operating posture, north star, model-agnostic + BYOK mandates, documentation-first development, agent-ecosystem expectations) at repo root — untracked and referenced nowhere. Rather than creating its 13 mandated root-level living docs (which would duplicate the existing canon and repeat the root-stub rot pattern), a **Repo Concordance** section was added inside it mapping each mandated doc to its live equivalent (PRODUCT.md → v4 feature map, CHANGELOG.md → plan.md §4, DECISIONS.md → session-decisions.md + docs/decisions/, BACKLOG.md → feature-backlog.md, SESSION.md/TASKS.md → active-task.md + v4 handoff doc, AGENTS.md name-collision documented). The constitution is now linked from every tool entry point: CLAUDE.md + GEMINI.md read orders (new step 0.5), AGENTS.md "Rule of the repo" header, README.md (orientation ¶ + Documentation map), ENTRY.md (entry-point table + repo tree), and the Lovable Knowledge field (`.lovable-config.txt` — **must be re-pasted into Lovable Settings → Knowledge**). Root `TASKS.md` stale redirect fixed — it pointed into the retired `project-Cadence-v3` repo, routing any tool that followed it out of this codebase — and the same sweep retargeted **17 more stale v3 redirect stubs** to their live in-repo paths (root: `commits.md`, `skills.md`, `memory.md`, `hooks.md`, `subagents.md`, `tools.md`; `docs/agent-ecosystem-plan.md`; under `docs/`: feature-backlog, considerations, foundation-audit, git-discipline, demo-credentials, fnd-runtime-restart-playbook, auth-flows, web-access, trust-and-autonomy, github-issue-approval-flow, a2a-handoff). Zero `project-Cadence-v3` file links remain in the repo. Four open strategy conflicts (north-star framing, agent roster, June 22 MVP date, category size) logged for founder ruling in session-decisions.md. **WHY:** an unreferenced constitution is invisible to Lovable/Antigravity/Gemini and orphaned in the doc graph; concordance-not-creation honors both its "no orphan documents" rule and the repo's anti-duplication closed-doc-loop. **Files:** edited `Ai_Cofounder.md` (Repo Concordance; original content untouched), `CLAUDE.md`, `GEMINI.md`, `AGENTS.md`, `README.md`, `ENTRY.md`, `.lovable-config.txt`, `TASKS.md` (redirect retarget), `plan.md` (§4), `docs/strategy/session-decisions.md`.

- **2026-06-11 — `F-CRITIC-AGENT` + `F-SCRIBE-CITATIONS` shipped: M1 Golden Path middle hops are now demoable.** The Critic agent (DEC-02) runs inline on every new opportunity (theme- or signal-promoted) and every freshly generated PRD; verdict (ship / revise / kill) + risks + kill criteria + missing evidence land in `opportunities.critic_review` / `prds.critic_review` before the operator sees the row. `OpportunitiesPanel` and the PRD detail header render a verdict chip that opens a side sheet with the full review and a re-run button. `generatePrd` now calls the existing hybrid RAG retriever (`retrieve()`) against the opportunity title+brief, injects up to 8 top chunks as `[n]`-numbered context, instructs Gemini to cite inline, and persists the chunk list to `prds.citations`; a Citations card on the PRD detail page deep-links each entry back to its source (signal → Product · Signals, doc → Knowledge · Docs, meeting → Knowledge · Calendar with `?meeting=`). Migration adds `critic_review` on opportunities, `critic_review` + `citations` on prds, and seeds a new `critic` agent row (role + rose-toned system prompt) per existing user. **WHY:** before this slice, Strategist → operator-approval → Scribe → operator-approval was the demo's hollow middle: operator was approving raw ICE scores with no challenge and reading PRDs with no proof. Critic + Citations turns those two gates into "AI red-teams its own work and cites sources, human governs" — the M1 product claim verbatim. **Files:** new migration `_critic_and_citations`; edited `src/lib/discovery.functions.ts` (added `runCritic` helper + `runCriticReview` server fn + wired into `promoteThemeToOpportunity`, `promoteSignalToOpportunity`, `generatePrd`; RAG retrieval + citation persistence in `generatePrd`); new `src/components/governance/CriticBadge.tsx`, `src/components/product/CitationsCard.tsx`; edited `src/components/product/OpportunitiesPanel.tsx`, `src/routes/_authenticated.prds.$id.tsx`; `docs/planning/feature-backlog.md`, `architecture/runtime.md`, `architecture/orchestration.md`, `docs/features/critic-agent.md` (new), `docs/features/prd-rag-citations.md` (new), `active-task.md`.

- **2026-06-11 — `F-DECISIONS-CAPTURE` shipped: Decisions log is real, mission/spec/meeting sources wired.** `/knowledge?tab=decisions` replaced its stub with a filterable Decisions log (source chips · status chips · search · side sheet · "Log decision" dialog · inline approve/reject). Migration extends `public.decisions` with `mission_id`, `prd_id`, `source_kind` (check-constrained), `decided_by_agent_slug`; existing rows backfilled to `meeting` / `manual`. Auto-capture (conservative): `maybeCompleteMission` writes an `approved` decision per completed mission (idempotent on `mission_id`); `savePrd` writes one on PRD status flip to `approved` (idempotent on `prd_id`); `extractMeeting` already wrote, now stamps `source_kind='meeting'`. Operator capture: "Capture as decision" button on mission detail + PRD detail. Today page got a "View all →" link to `/knowledge?tab=decisions`. Same turn closed `F-SEC-REALTIME-RLS` by dropping `public.agent_runs` from the `supabase_realtime` publication (no in-app subscriber; raw broadcast leaked rows across users). **WHY:** decisions were being made inside missions, specs, and meetings with no shared surface — the swarm needs one place to query what's been decided, and the leak couldn't sit any longer once spotted. **Files:** new migration; edited `src/lib/decisions.functions.ts`, `src/lib/ai/handoff.server.ts`, `src/lib/discovery.functions.ts`, `src/lib/meetings.functions.ts`, `src/routes/_authenticated.missions.$missionId.tsx`, `src/routes/_authenticated.prds.$id.tsx`, `src/routes/_authenticated.index.tsx`; rewrote `src/components/knowledge/DecisionsPanel.tsx`; `docs/planning/feature-backlog.md`, `architecture/frontend.md`.

- **2026-06-11 — `F-IA-V4` Phase 2 (operator-directed) shipped: rail trimmed, Calendar folded into Knowledge, floating dock kept.** Pinned workspace rail trimmed to **Today · Chat** (Missions still reachable in the Agents group). Floating bottom-right **Approvals + Calendar** dock kept as the fast path for the two highest-frequency daily gates; Calendar in the dock now deep-links to `/knowledge?tab=calendar` to stay consistent with Calendar's new home. `/knowledge` mounts **Calendar · Memory · Decisions · Docs** with Calendar as the default tab; `?meeting=` deep-link preserved across tab changes. `/calendar` flips to a `beforeLoad` redirect → `/knowledge?tab=calendar` (forwards `?meeting=`). The earlier spec-literal Phase 2 (`Home · Chat · Missions` with Approvals/Calendar dropped) is **superseded** by this operator IA call. **WHY:** pin only what's used every minute (Today, Chat); keep the two daily gates one click away via the dock; give Calendar a real home in Knowledge so the swarm can pull threads across events + memory + decisions + docs as one substrate. **Files:** edited `src/components/cadence/AppShell.tsx`, `src/routes/_authenticated.knowledge.tsx`; rewrote `src/routes/_authenticated.calendar.tsx` as redirect; `docs/planning/feature-backlog.md`, `architecture/frontend.md`.

- **2026-06-11 — `F-IA-V4` Phase 2 navigation polish shipped: Daily shortcuts relocated.** Approvals + Calendar no longer float over the top-right of routed pages, where they overlapped the ambient time/weather strip and page actions like Refresh brief. They now live as a compact **Daily** icon pair in the fixed sidebar footer above budget / mission mode. **WHY:** the two shortcuts are daily-use utilities, but they should feel stable and intentional without competing with page headers or ambient chrome. **Files:** edited `src/components/cadence/AppShell.tsx`, `architecture/frontend.md`, `docs/planning/feature-backlog.md`.

- **2026-06-11 — `F-IA-V4` Phase 1d shipped: Knowledge + Learn surfaces live.** New `/knowledge` route with `validateSearch` mounts 4 tabs (Calendar · Memory · Decisions · Docs) and forwards a `meeting` search param into the Calendar tab so the meeting detail sheet still deep-links. New `/learn` route mounts 3 tabs (Outcomes · Support · Learnings) sourced from `getOutcomeData` (Releases lived in `/product` since Phase 1c). Extracted panels: `src/components/knowledge/{Calendar,Memory,Decisions,Docs}Panel.tsx` and `src/components/learn/{Outcomes,Support,Learnings}Panel.tsx`. Memory + Decisions ship as stubs with empty-state copy (follow-up: `F-DECISIONS-CAPTURE` to wire mission/spec/meeting → decisions). `/calendar`, `/docs`, `/outcome`, `/meetings`, `/meetings/$id` flipped to `beforeLoad` redirects. Sidebar: replaced the pinned `Calendar` slot with `Knowledge` (Calendar is the default tab so muscle memory still works); collapsed the prior `Outcome` group into a new `Learn` group; dropped the standalone `Docs` entry from the Build group. Command palette repointed to the consolidated surfaces. **WHY:** Knowledge is one place to find anything the swarm knows or scheduled; pinning Knowledge while making Calendar its default tab keeps daily-loop reach without re-fragmenting the IA, matching the user's smart-consolidated-knowledge goal. The 7-surface IA collapse (`F-IA-V4`) is now structurally complete. **Files:** new `src/components/knowledge/{Calendar,Memory,Decisions,Docs}Panel.tsx`, `src/components/learn/{Outcomes,Support,Learnings}Panel.tsx`, `src/routes/_authenticated.knowledge.tsx`, `src/routes/_authenticated.learn.tsx`; rewrote `src/routes/_authenticated.{calendar,docs,outcome,meetings,meetings.$id}.tsx` as redirects; edited `src/components/cadence/AppShell.tsx`, `src/components/cadence/CommandPalette.tsx`, `active-task.md`, `docs/planning/feature-backlog.md`.

- **2026-06-11 — `F-IA-V4` Phase 1c shipped: Product surface live.** New `/product` route with `validateSearch` mounts 6 tabs: Signals · Opportunities · Specs · Roadmap · Tasks · Releases. Extracted panel bodies from `/discovery`, `/opportunities`, `/prds`, `/roadmap`, `/tasks`, and the Releases slice of `/outcome` into `src/components/product/{Signals,Opportunities,Specs,Roadmap,Tasks,Releases}Panel.tsx` (no behavior change). `/discovery`, `/opportunities`, `/roadmap`, `/tasks` flipped to `beforeLoad` redirects to `/product?tab=…`. `/prds` split into an `<Outlet/>` layout + new `prds.index.tsx` redirect so `/prds/$id` still renders. `/outcome` stays put — Launches/Support/Learnings move to `/learn` in Phase 1d. Sidebar: `discover` + `deliver` groups collapsed into a single **Product** group with deep-link items; Builder + Docs spun off into a new **Build** group. **WHY:** the discover→deliver loop was scattered across 6 routes and 2 sidebar groups doing one job (turn signals into shipped releases); one surface with 6 tabs lets an operator walk the whole loop without leaving the page. **Files:** new `src/components/product/{Signals,Opportunities,Specs,Roadmap,Tasks,Releases}Panel.tsx`, `src/routes/_authenticated.product.tsx`, `src/routes/_authenticated.prds.index.tsx`; rewrote `src/routes/_authenticated.{discovery,opportunities,roadmap,tasks}.tsx` as redirects and `src/routes/_authenticated.prds.tsx` as an Outlet layout; edited `src/components/cadence/AppShell.tsx`, `active-task.md`, `docs/planning/feature-backlog.md`.

- **2026-06-11 — `F-IA-V4` Phase 1b-2 shipped: Prompts + Evals folded into Govern.** Extracted `EvalsPanel` (~617 lines) and `PromptsPanel` (~655 lines) from their inline route files into `src/components/governance/{EvalsPanel,PromptsPanel}.tsx` (no behavior change). `/govern` now mounts **9 tabs**: Controls · Approvals · Guardrails · Budgets · Prompts · Evals · Analytics · Traces · Drift; container widened to `max-w-[1400px]` so the prompt diff + eval split layouts breathe. `/evals` and `/prompts` flipped to `beforeLoad` redirects preserving the tab. Sidebar: Prompts moved out of the Agents group and into the Govern group; both Prompts + Evals now deep-link to `/govern?tab=…`. **WHY:** Prompts and Evals are how operators keep the swarm safe alongside Controls/Approvals/Guardrails — they belonged on Govern from day one. Two more routes off the rail, zero deep-link regressions. **Files:** new `src/components/governance/EvalsPanel.tsx`, `src/components/governance/PromptsPanel.tsx`; rewrote `src/routes/_authenticated.evals.tsx`, `src/routes/_authenticated.prompts.tsx` as redirects; edited `src/routes/_authenticated.govern.tsx`, `src/components/cadence/AppShell.tsx`, `active-task.md`, `docs/planning/feature-backlog.md`.

- **2026-06-11 — `F-IA-V4` Phase 1b shipped: Govern surface live.** New `/govern` route mounts 7 tabs (Controls · Approvals · Guardrails · Budgets · Analytics · Traces · Drift) with `validateSearch`. Extracted the inline ~580-line ControlsPanel out of `_authenticated.governance.tsx` into `src/components/governance/ControlsPanel.tsx` (no behavior change). `/governance` + `/observe` now `beforeLoad` redirect to `/govern` preserving `?tab` 1:1; thin redirects `/guardrails`, `/budgets`, `/drift`, `/traces`, `/inbox`, `/analytics` all repoint at `/govern?tab=…`. Sidebar collapses `Run` + `Govern` groups into one `Govern` group (Govern · Evals · Integrations); pause-state footer banner + command palette repointed. **WHY:** two adjacent sidebar groups were doing the same operator job — keep the swarm safe — across six routes; one Govern surface with seven tabs collapses that into a single stop while preserving every deep link. **Files:** new `src/components/governance/ControlsPanel.tsx`, `src/routes/_authenticated.govern.tsx`; rewrote `src/routes/_authenticated.{governance,observe,guardrails,budgets,drift,traces,inbox,analytics}.tsx` as redirects; edited `src/components/cadence/AppShell.tsx`, `src/components/cadence/CommandPalette.tsx`, `active-task.md`, `docs/planning/feature-backlog.md`.

- **2026-06-11 — `F-IA-V4` Phase 1a shipped: Missions surface promoted.** `/missions` is now the v4 IA "Missions" station with `?tab=missions|agents`, reusing extracted `MissionsPanel` + `AgentsPanel`. `/cockpit` and `/swarm` flip to `beforeLoad` redirects preserving query params. Sidebar and command palette repointed. **WHY:** first concrete slice of the 7-surface IA collapse, scoped to where panel components already exist (no risky panel extractions in this turn). **Files:** rewrote `src/routes/_authenticated.missions.index.tsx`, `src/routes/_authenticated.cockpit.tsx`, edited `src/routes/_authenticated.swarm.tsx`, `src/components/cadence/AppShell.tsx`, `src/components/cadence/CommandPalette.tsx`, `src/routes/_authenticated.agents.tsx`, `src/routes/_authenticated.missions.$missionId.tsx`, `active-task.md`, `docs/planning/feature-backlog.md`.

- **2026-06-11 — v4 strategic rebuild landed (docs only, no product code).** Commissioned adversarial stress test + market research + full feature-map rebuild. New: [`docs/strategy/v4-feature-map-2026-06-11.md`](./docs/strategy/v4-feature-map-2026-06-11.md) (strategic source of truth — 7 laws, 6 stations, 19-agent mesh + HandoffEnvelope contract + HITL gate matrix, 7-surface IA, station catalogs, milestones M1–M5, frontier-absorption policy per node), [`docs/strategy/v4-stress-test-2026-06-11.md`](./docs/strategy/v4-stress-test-2026-06-11.md) (10 argued failure verdicts F1–F10), [`docs/references/competitive-landscape-2026-06-11.md`](./docs/references/competitive-landscape-2026-06-11.md) (June-2026 market scan with links — lifecycle whitespace confirmed), [`docs/planning/v4-rebuild-handoff-2026-06-11.md`](./docs/planning/v4-rebuild-handoff-2026-06-11.md) (session tracker + resume instructions for any fresh tool/account). Decisions: PLG wedge → enterprise (pain-point-first); naming DEFERRED to final activity — Circuit stays interim ([`docs/decisions/naming.md`](./docs/decisions/naming.md) updated with rejected list + fresh directions). Edited: `README.md` (six stations replace 8 pillars; GTM posture; doc map), `plan.md` (§1 superseded-banner, §3 → M1–M5), `design.md` (7-surface IA contract), `CLAUDE.md`/`GEMINI.md` read-order 1.5 → v4 map, `active-task.md` (v4 complete; calendar OAuth stays open), `docs/strategy/session-decisions.md`, `docs/planning/feature-backlog.md` (v4 overlay), `docs/README.md`, `.lovable-config.txt` (current positioning pointer). **WHY:** founder verdict — substrate strong but "overwhelming, not demo-ready, no storyline"; stress test confirmed (engine room exposed as 34 nav routes, no golden path, loop's right half scaffolding). v4 makes the platform agent-run at every station with humans only at gates, and makes the docs buildable-from by Lovable/Antigravity/Claude Code without context loss. **Next:** M1 Golden Path, starting `F-IA-V4`.

- **2026-06-11 — `F-COCKPIT-MERGE` shipped: Unified Swarm HUD & Missions Cockpit.** Consolidated the live Swarm HUD (`/swarm`) and the orchestrated Missions list (`/missions`) into a single tabbed flight deck at `/cockpit?tab=agents|missions`. Added TanStack Router `beforeLoad` redirects on the old routes so existing bookmarks and deep links redirect seamlessly. Simplified the global AppShell navigation rail (replacing Swarm and Missions with Cockpit) and the Command Palette. Repointed all internal application links to point to the new tabbed cockpit. **WHY:** Reduces cognitive clutter for operators by collapsing distinct views into a single cockpit control surface. **Files:** new `src/routes/_authenticated.cockpit.tsx`, `src/components/cockpit/AgentsPanel.tsx`, `src/components/cockpit/MissionsPanel.tsx`; edited `src/routes/_authenticated.swarm.tsx`, `src/routes/_authenticated.missions.index.tsx`, `src/routes/_authenticated.agents.tsx`, `src/routes/_authenticated.missions.$missionId.tsx`, `src/components/cadence/AppShell.tsx`, `src/components/cadence/CommandPalette.tsx`, `docs/planning/feature-backlog.md`, `plan.md`.

- **2026-06-10 — Documentation Restructuring & Strategic Pivot to Circuit (B2B Enterprise).** Completed renaming references from "Cadence" to "Circuit" across all main documentation and orientation manuals. Reorganized scattered files into clean logical subdirectories: guides under `docs/features/`, operations under `docs/operations/`, and planning/backlogs under `docs/planning/`. Re-categorized the entire feature backlog under the 8 Cockpit Pillars representing the 12-stage lifecycle. Created thin redirect pointer files at the 19 old paths containing a co-development move disclaimer to prevent contextual breaks for concurrent development tools. **WHY:** B2B Enterprise target personas require a clean, well-governed cockpit structure, and restructuring scattered files makes onboarding and multi-tool development highly scalable without causing file-not-found breaks. **Files:** edited `README.md`, `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `ENTRY.md`, `.lovable-config.txt`, `docs/README.md`, `docs/planning/feature-backlog.md`, `docs/strategy/README.md`, `docs/strategy/session-decisions.md`, `plan.md`, and created 19 redirection markdown files.

- **2026-06-07 — `F-CHAT-NL-INTENT` shipped: Natural Language Intent Detection & Inline Mission progress on Chat.** Chat now automatically intercepts user prompts, detects execution intent via a fast Gemini classification call, seeds the orchestrator, and asynchronously launches multi-agent missions. Hides agent details by rendering a clean inline progress cockpit directly in the chat window, showing live step progress, inline governance controls (Approve/Reject gates), toggles for raw trace, and links to the full mission cockpit page. Falls back gracefully to regular chat if pre-flight checks fail (e.g. no specialist agents enabled). **WHY:** allows users to control the entire swarm of specialists in natural language without managing agent handoffs, while keeping governance simple and visually calming. **Files:** edited `src/routes/api/chat.ts`, `src/routes/_authenticated.chat.tsx`, `docs/planning/feature-backlog.md`, `design.md`, plan.md.

- **2026-06-06 — Global ambient weather visibility fix (UX-only).** The top ambient bar now keeps weather visible even when browser GPS is blocked or times out: GPS → network location → timezone city fallback, then Open-Meteo weather. Cache moved to `cadence.ambient.v3`, stale `v1/v2` entries are cleared, and the playful weather colors now use semantic CSS utilities instead of component color fields. Verified in preview on `/calendar`: the bar renders time + location + `20°` weather pill. **WHY:** the prior GPS-only path made the weather temperature/icon vanish entirely for users without location permission, so the global personalization failed silently. **Files:** edited `src/components/cadence/AmbientChip.tsx`, `src/styles.css`, `architecture/frontend.md`, `docs/planning/feature-backlog.md`.

- **2026-06-06 — Calendar header polish (UX-only, no F-ID).** Dropped the dismissible "Connect your calendar" banner (the header `<Link2>` chip is the single connect entry now), dropped the duplicate Month/Year `<select>` controls on the grid (chevrons + double-chevrons already do this), added a `WeatherChip` next to the connect icon — browser geolocation → Open-Meteo (no key) → WMO weather code → colorful Lucide icon (`Sun`/`Cloud`/`CloudRain`/`CloudSnow`/`CloudLightning`/`CloudFog`) + tonal background + reverse-geocoded city, silently hidden when geo is denied. Mirrored calendar account connect/disconnect into Settings as a new `CalendarAccountsSection` so users can manage providers from their profile too. **WHY:** the banner and dropdowns duplicated affordances already present; the weather chip adds personality without inventing a new integration. **Files:** edited `src/routes/_authenticated.calendar.tsx`, `src/routes/_authenticated.settings.tsx`; `docs/planning/feature-backlog.md` (status board + recent log).

- **2026-06-06 — Calendar Parts B + C shipped (pending provider Client IDs).** Per-user calendar OAuth (`F-CALENDAR-PERUSER`): new table `public.user_calendar_connections` (RLS-scoped to `auth.uid()`); canonical app-user OAuth helpers landed at `src/integrations/lovable/appUserConnector.ts` (server-only) + `appUserConnectorClient.ts` (popup-driver, browser-safe); new `src/lib/calendar-connections.functions.ts` (`startCalendarConnect` · `saveCalendarConnection` · `listMyCalendarConnections` · `disconnectCalendar` · `getPrimaryConnection`); `src/lib/calendar.functions.ts` `syncCalendar` / `createCalendarEvent` / `updateCalendarEvent` / `deleteCalendarEvent` are now provider-aware — Google → `google_calendar` connector path, Microsoft → `microsoft_outlook` (Graph) via `callAsAppUser`, and the existing workspace connector stays as fallback when the user has no per-user connection. Apple dropped (no public OAuth surface). Calendar route gets a connections strip (Google · Microsoft chips with disconnect ×, auto-tooltip when provider credentials are missing), a full month grid with month/year navigation + Today (cells show up to 3 event chips, click chip → edit dialog), and an in-app event editor dialog (edit title/start/end/notes, save → PATCH, delete → DELETE with typed confirm via `useConfirm`). **Pending:** two backend secrets — `GOOGLE_APP_USER_CONNECTOR_CLIENT_ID`, `MICROSOFT_APP_USER_CONNECTOR_CLIENT_ID`. When missing, Connect buttons render disabled with a tooltip; `startCalendarConnect` throws a clean "Connect setup pending — admin must configure provider credentials." error. **WHY:** the workspace connector was tied to the developer's calendar; real users need their own Google / Microsoft accounts and the ability to manage events without context-switching to a provider tab. Shipping the full surface now, gated only on a 10-minute provider-console step, keeps the engineering velocity up and isolates the remaining unlock to one secrets edit. **Files:** new migration creating `public.user_calendar_connections` + `public.calendar_provider` enum; new `src/integrations/lovable/{appUserConnector,appUserConnectorClient}.ts`; new `src/lib/calendar-connections.functions.ts`; edited `src/lib/calendar.functions.ts` (dispatcher + Microsoft Graph paths); rewrote `src/routes/_authenticated.calendar.tsx` (connections strip · month grid · event editor); new `docs/decisions/calendar-oauth-credentials.md`; new `active-task.md` (open until secrets land); `docs/planning/feature-backlog.md` (status board + F-CALENDAR-PERUSER row + recent log); `plan.md` (this entry).

- **2026-06-06 — Calendar Part A polish shipped.** `/calendar` view toggle now reads **List | Calendar** (icon swapped `LayoutGrid` → `CalIcon`; internal value `"grid"` unchanged so `localStorage` `cadence.calendar.view` stays back-compat). Added a muted subhead line "Showing the next 14 days." The List feed is now clamped to `today − 12h → today + 14d` and sorted **ascending** (next event at the top) instead of the prior all-time descending dump. **WHY:** users couldn't tell the grid view _was_ the calendar (the label said "Grid"), and the list was showing months of past events first, hiding what was actually next. Two trivial UI changes, no schema/server changes. Part B (per-user Google/Microsoft connect) and Part C (month grid + full event CRUD) are queued per the approved plan. **Files:** `src/routes/_authenticated.calendar.tsx`, `plan.md` (this entry).

- **2026-06-06 — `F-IA-CALENDAR-MERGE` + `F-IA-BRIEFING-SETTINGS` shipped (paired pinned-rail trim).** Pinned workspace rail shrinks 6 → 4 (Today · Approvals · Calendar · Chat) by applying a new **Pin test** (used daily, entry to an active workflow, not derivable from another pinned surface). `/calendar` now hosts a List view (default — meetings + Google Calendar events in one chronological feed) and a Grid view toggle (the prior day-grouped event cards); preference persists per-user in `localStorage`. Meeting rows open a right-side `Sheet` showing the existing transcript/extract/commit flow via a new shared `src/components/cadence/MeetingDetailBody.tsx`; `?meeting=<id>` is a validated search param so deep-links open the sheet on load. `/meetings` and `/meetings/$id` become `beforeLoad` redirects to `/calendar` (and `/calendar?meeting=<id>` respectively). Briefing folds into Settings: `/briefing` redirects to `/settings?section=brief`; settings page reads `section` via `validateSearch`, scrolls + ring-highlights a new inline "Strategic brief" section that reuses `getActiveBrief`/`upsertBrief` unchanged. Command palette: "Meetings" entry repointed to Calendar; `g m` shortcut now goes to `/calendar`. Zero DB or server-fn changes. Convention update: Pin test rule added to `docs/conventions/inline-management.md`, plus a row for Briefing-in-Settings. **WHY:** the pinned rail was carrying two items that didn't earn their pin — Calendar+Meetings were one mental model split in two ("what's on my time, and what came out of it"), and Briefing was set-once workspace context posing as daily nav. Trimming returns the rail to the daily-use surfaces; folding the brief into Settings matches the inline-management convention (workspace-scoped settings live next to the workspace, not on the global rail). **Files:** new `src/components/cadence/MeetingDetailBody.tsx`; rewrote `src/routes/_authenticated.calendar.tsx`; replaced `src/routes/_authenticated.meetings.tsx`, `src/routes/_authenticated.meetings.$id.tsx`, `src/routes/_authenticated.briefing.tsx` with redirects; edited `src/routes/_authenticated.settings.tsx` (validateSearch + WorkspaceBriefSection), `src/components/cadence/AppShell.tsx` (pinned rail 6 → 4), `src/components/cadence/CommandPalette.tsx` (Calendar entry + `g m` route); `docs/conventions/inline-management.md` (Pin test rule + Briefing-in-settings row); `docs/planning/feature-backlog.md` (status board + Recent log); `plan.md` (this entry); `architecture/frontend.md` (pinned rail list + Calendar sheet pattern + redirect map).

- **2026-06-06 — `F-IA-MERGE-GOVERN` + `F-IA-RENAMES` shipped (paired IA cleanup).** `/governance` becomes a 4-tab page (Controls · Approvals · Guardrails · Budgets) using the same `validateSearch` pattern as `/observe`; panels live in `src/components/governance/{Approvals,Guardrails,Budgets}Panel.tsx`; `/inbox`, `/guardrails`, `/budgets` are now `beforeLoad` redirects. Sidebar workspace rail's "Approvals" deep-links to `/governance?tab=approvals` (NavRow now forwards `search` to `<Link>` and the active-state helper is search-tab-aware so the same path can have two entries highlight correctly). Govern group shrinks from 4 → 2 items (Governance, Integrations). Rename sweep: AI Chat→Chat, Build Console→Builder, Swarm HUD→Swarm, Prompt Studio→Prompts, Sync Inbox→Connectors (URLs unchanged, so no redirects; page titles + H1s + command palette updated). **WHY:** Govern was four sidebar items pointing at one job — running the swarm safely. One tabbed surface mirrors the `/observe` pattern, keeps the daily Approvals badge next to Today/Briefing where the operator actually starts, and the rename sweep removes the last enterprise-jargon labels per the voice anchor. URLs stayed put because redirecting working URLs has no UX upside and breaks browser history.

- **2026-06-06 — `F-IA-MERGE-OBSERVE` shipped: Run group + `/observe` tabs.** Sidebar group `aiops`/`AI Ops` renamed to `run`/`Run` with two items only — Observe → `/observe`, Evals → `/evals`. New route `src/routes/_authenticated.observe.tsx` is an AppShell page with three tabs (Analytics · Traces · Drift); tab state lives in `?tab=…` via `validateSearch`; Traces and Drift labels carry live count badges (today's traces · open drift incidents). Panels lifted from the old route bodies into `src/components/observe/{Analytics,Traces,Drift}Panel.tsx` with no logic changes (same server fns, same queries). Old route files `_authenticated.{analytics,traces,drift}.tsx` reduced to `throw redirect({ to: "/observe", search: { tab: … } })` — `/traces/$traceId` preserved untouched. Command palette gains an Observe entry. Govern group (Guardrails · Governance · Budgets · Integrations) is **not** touched — watching agents ≠ governing them, so they stay two groups. Dropped the now-unused `TrendingUp` import from `AppShell.tsx`. **WHY:** "AI Ops" was 4 items of jargon for one job (watching agents work); operators couldn't tell from the rail what each surface did. One Observe page with loud count badges makes "how are agents performing?" answerable in one click, and renaming the group to `Run` finishes the verb-group pattern (Discover · Deliver · Run · Govern) and kills the last enterprise buzzword on the rail. **Files:** new `src/routes/_authenticated.observe.tsx`, `src/components/observe/{AnalyticsPanel,TracesPanel,DriftPanel}.tsx`; edited `src/routes/_authenticated.{analytics,traces,drift}.tsx` (now redirects), `src/components/cadence/AppShell.tsx`, `src/components/cadence/CommandPalette.tsx`, `docs/planning/feature-backlog.md` (status board · recent log · F-IA-MERGE-OBSERVE row ☑ + How-to-verify), `architecture/frontend.md` (Observe tabs pattern), `plan.md` (this entry).

- **2026-06-06 — `F-NAV-ACCORDION` shipped: sidebar single-open accordion.** Operator pushed back on the planned `F-IA-MERGE-OBSERVE` step, asking whether the merge actually fixed left-rail crowding and whether features would lose spotlight. Split the work: ship the sidebar accordion first (smaller, reversible, addresses the UX pain directly), then do the route merge with loud count-carrying tabs. `useOpenGroups` in `src/components/cadence/AppShell.tsx` now tracks a single `openId: string | null` instead of a `Record<string, boolean>`; clicking a group toggles only that id; navigating to a route inside another group auto-opens that group (which auto-closes the previous one); `localStorage` key `cadence.nav.open` now stores a JSON string id (back-compat: old object payloads are ignored). **WHY:** the planned IA merge alone wouldn't have fixed the "too many groups open at once" feeling — accordion behavior does, in 20 lines, with no route surgery. Unblocks `F-IA-MERGE-OBSERVE` to focus purely on the merge + tab-spotlight question. **Files:** edited `src/components/cadence/AppShell.tsx`, `docs/planning/feature-backlog.md` (status board + new P0 row), `plan.md` (this entry).

- **2026-06-06 — P0 voice + governance batch shipped (8 F-IDs) + scanner finding fix.** Closed the entire P0 row from the v3 audit triage in one batch. `F-VOICE-LOGIN`: `/login` subhead now "Your product org, run by a swarm of agents." `F-VOICE-AINATIVE`: scrubbed "AI-native" from operator surfaces (login, briefing placeholder, `/api/chat` system prompt). `F-VOICE-VERSIONS`: stripped `Phase N` / `Bundle N` kickers from `/build`, `/discovery`, `/opportunities`, `/prds`, `/roadmap`, `/meetings`, `/integrations`. `F-VOICE-EMPTY-TODAY`: rewrote Today brief empty state (drop "hit refresh") + Swarm attention-queue empty state (drop "humming"). `F-VOICE-CASE`: dropped `neural-text` serif gradients on `/calendar` (Upcoming meetings) + `/tasks` (All tasks); removed the Workstream kicker on `/tasks`. `F-GOV-APPROVAL-COPY`: `/inbox` + `/governance` approval rows now lead with consequence ("Approve · run {tool}" + tooltip naming the rollback; "Reject · nothing runs"). `F-TODAY-AUTOSEED`: extracted `ensureTodayBrief(supabase, userId)` helper in `src/lib/copilot.functions.ts` (routed through `runtime.server.ts`); `getDashboard` calls it on first sign-in so the operator never has to click Refresh to see today's brief; Refresh still forces a regen. `F-AGENTS-ROSTER-CUT`: migration cuts `seed_default_agents` from 9 → 4 (Discovery Scout, Strategist, PRD Writer, Builder); Orchestrator stays as the 5th, seeded by its own function; all other historically-seeded agents are flipped to `enabled = false` for existing users (kept for audit / mission attribution). Same migration tightens `prompt_runs_ws_write` WITH CHECK to require `user_id = auth.uid()` (closes the `prompt_runs_unauthorized_write` Lovable scanner finding — any workspace member could previously insert prompt-run rows attributed to another user). **WHY:** the v3 audit found 8 quick wins where every existing surface needed to speak the v3 thesis before we layered on more routes; doing them in one batch flips the whole product to the new voice without adding any new surfaces, while also closing the security finding that surfaced at the same time. **Files:** new migration `<ts>_roster_cut_and_prompt_runs_rls.sql`; edited `src/routes/login.tsx`, `src/routes/_authenticated.briefing.tsx`, `src/routes/api/chat.ts`, `src/routes/_authenticated.build.tsx`, `src/routes/_authenticated.discovery.tsx`, `src/routes/_authenticated.opportunities.tsx`, `src/routes/_authenticated.prds.tsx`, `src/routes/_authenticated.roadmap.tsx`, `src/routes/_authenticated.meetings.tsx`, `src/routes/_authenticated.integrations.tsx`, `src/routes/_authenticated.index.tsx`, `src/routes/_authenticated.swarm.tsx`, `src/routes/_authenticated.calendar.tsx`, `src/routes/_authenticated.tasks.tsx`, `src/routes/_authenticated.inbox.tsx`, `src/routes/_authenticated.governance.tsx`, `src/lib/copilot.functions.ts`, `src/lib/dashboard.functions.ts`, `docs/planning/feature-backlog.md` (status board · recent log · 8 F-ID rows flipped ☑), `plan.md` (this entry); deleted `active-task.md` on completion.

- **2026-06-06 — Phase B shipped: `F-OUTCOME-SURFACE` (`/outcome`).** New route `_authenticated.outcome.tsx` with four tabs (Releases · Launches · Support · Learnings) reading real data via `getOutcomeData` in `src/lib/outcome.functions.ts`: completed missions + completed agent runs (Releases), approvals on outbound tools `send_slack`/`send_email`/`publish_changelog`/`post_announcement`/`notify_channel` (Launches), signals from `support`/`ticket`/`helpdesk`/`email`/`zendesk`/`intercom`/`freshdesk` (Support), opportunities re-scored after creation (Learnings). All reads RLS-scoped via `requireSupabaseAuth`; no new agent logic; no `runtime.server.ts` bypass; no mocks. Empty states are first-class and name the loop step + why it lights up. Sidebar gained a new **Outcome** group. Closes Proof Platform v1.1 bundles 10/11/12 — the right half of the lifecycle now has one named URL even when empty. **WHY:** operators couldn't _see_ the loop they were sold; the loop closer needed a surface before bundles 10–12 could be claimed shipped. **Files:** new `src/lib/outcome.functions.ts`, `src/routes/_authenticated.outcome.tsx`; edited `src/components/cadence/AppShell.tsx`, `docs/planning/feature-backlog.md`, `plan.md`; deleted `active-task.md`.

- **2026-06-06 — Phase A verified + Phase C v3 audit triage shipped (A→C→B sequence, op-approved).** Doc + backlog only, no product code. **A:** confirmed FND-RUNTIME 0.9 substrate is intact (checkpoints upserted before every provider call · `resumeAgentLoop` rehydrates from `agent_run_checkpoints` · `withIdempotency` wraps every external write · `/api/public/hooks/resume-runs` swept once a minute by pg_cron). Existing playbook at [`docs/operations/fnd-runtime-restart-playbook.md`](./docs/operations/fnd-runtime-restart-playbook.md) is operator-runnable in ≤ 5 min; the row stays 🟡 until an operator actually kills a worker mid-loop (can't happen from inside the dev sandbox). **C:** triaged both v3 audits ([`v3-audit-2026-06-06`](./docs/strategy/v3-audit-2026-06-06.md) + [`v3-audit-language-2026-06-06`](./docs/strategy/v3-audit-language-2026-06-06.md)) into a single addressable index at [`docs/planning/feature-backlog.md` § v3 Audit Triage](./docs/planning/feature-backlog.md#v3-audit-triage-2026-06-06) — 22 new F-IDs (8 P0 + 11 P1 + 3 P2), 2 already shipped (LANG-07 popups → `F-VOICE-DIALOGS`; LANG-10 voice guide → `F-VOICE-GUIDE`), Phase-B target consolidated as `F-OUTCOME-SURFACE` (REC-07 + LANG-NEW-OUTCOME + Proof Platform v1.1 bundles 10–12). Each F-ID is a thin entry pointing back to the audit doc — no prose duplicated. Operator's default-applied answers to the two audits' §10 questions captured in the language audit's new Triage status section. Also opened `F-SEC-REALTIME-RLS` to track the deferred realtime-topic finding and marked the scanner finding ignored with that F-ID. **WHY:** the audits had been sitting as prose for half a day; until every rec became a backlog F-ID, no other tool (Claude / Antigravity / Gemini) could pick up parallel work, and the Live status board's "Next up" stayed mush. Phase B (`F-OUTCOME-SURFACE`) is now the unambiguous next-up. **Files:** edited `docs/planning/feature-backlog.md` (status board, recent log, new triage section), `docs/strategy/v3-audit-2026-06-06.md` + `docs/strategy/v3-audit-language-2026-06-06.md` (Triage status sections + rec→F-ID maps), `docs/strategy/session-decisions.md` (decision-log entry), `plan.md`.

- **2026-06-06 — Cross-tool memory fix: `mem://` → `docs/conventions/`.** Doc-only. Operator caught that the 6 `mem://` files from the earlier doc-closure pass were Lovable-private — invisible to Claude Code, Antigravity, and Gemini — breaking the cross-tool contract in `AGENTS.md` §10. Created [`docs/conventions/`](./docs/conventions/) as the git-tracked home for durable rules with 5 rule files (`ui-chrome`, `ui-voice`, `destructive-actions`, `inline-management`, `doc-closure-checklist`) + an index `README.md`. Wired into every tool entry point: `AGENTS.md` §3 (5 new rule one-liners with links) + §5 (2 new rows in the update matrix); `CLAUDE.md` and `GEMINI.md` read-order step 1.6; `.lovable-config.txt` SOURCE OF TRUTH HIERARCHY. Reduced the 6 `mem://` files to ≤ 3-line pointers that defer to their git twin and rewrote `mem://index.md` to make the indirection explicit. Linked the new conventions from `architecture/frontend.md`, `design.md`, and `docs/strategy/v3-audit-language-voice-2026-06-06.md`. Added a Conventions section to `docs/README.md`. **WHY:** "the git repo is the only shared substrate" (`AGENTS.md` §10) — tool-private memory creates silent cross-tool drift; rules must live in `git` to survive a tool switch. **Files:** new `docs/conventions/{README,ui-chrome,ui-voice,destructive-actions,inline-management,doc-closure-checklist}.md`; edited `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `.lovable-config.txt`, `docs/README.md`, `architecture/frontend.md`, `design.md`, `docs/strategy/v3-audit-language-voice-2026-06-06.md`, `docs/strategy/session-decisions.md`, `docs/planning/feature-backlog.md`, `plan.md`; reduced 6 `mem://` files + `mem://index.md` to pointers.

- **2026-06-06 — Doc-closure pass for voice / popups / inline-mgmt.** Doc + memory only, no product code. Extended `docs/strategy/v3-audit-language-voice-2026-06-06.md` with a "How to use / verify" block, phased rollout (P0 shipped · P1 + P2 open), Learnings, and a Related cross-link block. Added "Confirmation, toasts & dialogs" + "Inline workspace & product management" subsections to `architecture/frontend.md`. Added the "Voice & language" contract section to `design.md` (length budgets, AI-tell denylist, confirm-copy pattern). Added owner-gated server-fn invariant to `architecture/security.md`. New project memory under `mem://` (no-native-browser-chrome, no-em-en-dashes-in-ui, voice-anchor, destructive-actions, inline-workspace-product-mgmt, doc-loop-checklist) + two new Core lines in `mem://index.md`. **WHY:** the prior turn shipped the work but only half-closed the loop — without contract entries in `architecture/*` and `design.md` plus durable memory, the next session reintroduces a `confirm()` or an em dash. **Files:** edited `docs/strategy/v3-audit-language-voice-2026-06-06.md`, `architecture/frontend.md`, `design.md`, `architecture/security.md`, `docs/strategy/session-decisions.md`, `docs/planning/feature-backlog.md`, `plan.md`; new memory files under `mem://`.

- **2026-06-06 — Zero browser popups + inline workspace/product management.** Course-correct from operator feedback that browser-native popups (`window.prompt`/`window.confirm`) feel "hard" and off-brand, plus a parallel ask for inline workspace + product management. Built `src/hooks/use-confirm.tsx` exporting `ConfirmProvider` + `useConfirm()` + `usePrompt()` (promise-based, themed shadcn `AlertDialog`/`Dialog`, focus-trapped, supports `destructive` styling and a `typedConfirm` typed-name guard for irreversible actions). Provider mounted once in `__root.tsx` inside `ThemeProvider`. ESLint guardrail added to `eslint.config.js` (`no-restricted-globals` blocking `alert`/`confirm`/`prompt`; `no-restricted-syntax` blocking `window.alert|confirm|prompt|onbeforeunload`) so future regressions fail lint with a clear pointer to the in-app primitives. Swept 12 popup call sites across `AppShell` (new workspace, new product, delete product), `_authenticated.evals.tsx` (delete suite, delete case), `_authenticated.guardrails.tsx` (delete rule), `_authenticated.docs.tsx` (Google Docs import, icon picker, delete doc), `DocEditor.tsx` (link, figma toolbar + slash). New server fns `renameWorkspace` / `deleteWorkspace` / `leaveWorkspace` / `listWorkspaceMembers` / `removeWorkspaceMember` in `src/lib/workspaces.functions.ts` (RLS already owner-only on workspaces.update + workspace_members.write; owner cannot leave their own workspace — server-side guard). Added `updateProject` to `projects.functions.ts`. `AppShell` workspace switcher popover now exposes an inline Manage section (Rename, Workspace settings, Leave, Delete with typed-name guard) so operators never leave the current surface to administer the workspace. Each product row in the sidebar now has a `MoreHorizontal` dropdown (Set active, Rename, Delete with typed-name guard) instead of a bare trash icon. Every flow surfaces feedback via sonner toasts. **WHY:** the popup feedback was a blocker for trust ("feels like we are not taking care of this") and the missing inline management forced operators into `/settings` for every rename. Both are first-impression failures the v3 audit flagged and the v3 language audit specified. Em dashes and the rest of the AI-tell list ship as the P1 sweep next. **Audit doc:** [`docs/strategy/v3-audit-language-voice-2026-06-06.md`](./docs/strategy/v3-audit-language-voice-2026-06-06.md) — voice rules, full AI-tell list (em dashes are one symptom, not the whole list), popup sweep table, inline-management spec, P1 sweep targets. **Files:** new `src/hooks/use-confirm.tsx`, `src/lib/workspaces.functions.ts`, `docs/strategy/v3-audit-language-voice-2026-06-06.md`; edited `src/routes/__root.tsx`, `src/components/cadence/AppShell.tsx`, `src/components/cadence/DocEditor.tsx`, `src/routes/_authenticated.evals.tsx`, `src/routes/_authenticated.guardrails.tsx`, `src/routes/_authenticated.docs.tsx`, `src/lib/projects.functions.ts`, `eslint.config.js`, `docs/strategy/README.md`, `docs/strategy/session-decisions.md`, `docs/planning/feature-backlog.md`.

- **2026-06-06 — v3 end-to-end product & platform audit landed.** Brutally honest review of Cadence (product, UX, AI-native posture, IA, competitive position, thesis). Method: full doc + code read, live preview walk against seeded demo workspace, three parallel competitive-research subagents (engineering-autonomy lane / product-OS lane / agent-OS + governance lane — see audit §8). 22 prioritized recommendations with Impact × Effort × Horizon × Strategic × Benefit on every one; Top-5 / Top-10 / Top-20 rollup; investor readiness scorecard (median 6.5; Vision 8 and AI Readiness 8 vs. UX 4 and Differentiation 5 as the blocking dimensions). Headline thesis refinement proposed (not yet adopted): "autonomous product OS" → **product-org cockpit** — same substrate, sharper noun. **No code changes, no backlog mutations** — recommendations graduate to features only on operator triage. **WHY:** the product surface had drifted from v2 positioning faster than the closed-doc loop was catching it (login still said "AI-native"; Today asked the operator to refresh; `/swarm` shows 18 agents on day one) — operator commissioned the audit before opening a new bundle. **Files:** new `docs/strategy/v3-audit-2026-06-06.md`; edited `docs/strategy/README.md`, `docs/strategy/session-decisions.md`, `docs/planning/feature-backlog.md`, `plan.md`.

- **2026-06-06 — v3 language / naming / microcopy companion audit landed.** Same operator-walk method as the main v3 audit, focused entirely on the words: naming, sidebar/IA copy, page H1s, empty states, buttons, placeholders, tooltips, approval-gate prompts, agent/AI surface vocabulary, governance verbs, marketing/public copy. **Findings:** (1) `/login` headline + subhead ("AI-native product operating system") still contradict v2 + v3 thesis on the single most-seen surface — closed-doc loop is broken at its most expensive seam. (2) Sidebar speaks two languages — user-time nouns (Today/Approvals) next to industry jargon (`AI Ops`, `Eval Harness`, `Swarm HUD`, `Build Console`, `Sync Inbox`, `Prompt Studio`). (3) `Phase N` / `Bundle N` internal version labels leak onto 4 operator routes (`/build`, `/discovery`, `/opportunities`, `/prds`) — commit topology shown as product structure. (4) Work-unit nouns drift across screens (Mission · Run · Trajectory · Step · Trace; Agents · Swarm · Specialists; Approvals · Inbox · Reactor activity · Sync Inbox · Attention queue) — operator can't form a stable mental model because the words won't hold still. (5) Tooltips paper over weak labels (`Autonomy dial` needs a tooltip just to define itself; trust chips carry paragraph-long tooltips because `Trust 72 · Proving` doesn't carry the meaning). **Output:** [`docs/strategy/v3-audit-language-2026-06-06.md`](./docs/strategy/v3-audit-language-2026-06-06.md) — one-page voice guide (operator-grade · reporter · coach; banned words like _unlock_/_seamless_/_humming_; preferred verbs per agent state); naming integrity matrix (11 concepts × current names × canonical proposal × blast radius); full sidebar rename mapping (31 routes → 12 surfaces with old→new label table and back-compat redirect plan); tooltip Keep/Delete/Rewrite/Add audit per route with proposed copy; 5 verbatim rewrites of the most-costly strings; agent/AI vocabulary spec (agent states, trust stages, approval modes, trace step labels, AI-chip copy contract that formalizes `design.md`); P0–P3 rewrite roadmap (LANG-01..10, TOOLTIP-DEL/REW, LANG-IA-12, LANG-NEW-OUTCOME, LANG-CHIP) with Impact × Effort × Horizon × Strategic × Benefit on every row. Headline ask: pick LANG-01 (`/login` rewrite) + LANG-02 (delete Phase/Bundle labels) + LANG-06 (Today/Swarm empty states) + LANG-08 (sentence-case H1s) for a week-1 ship — zero engineering risk, maximum first-impression lift. **No code changes, no backlog mutations** — same contract as the main audit. **WHY:** the product audit graded the _what_; the operator asked for the _words_ graded next. The two together give a single triage queue. Brand truth, IA legibility, and tooltip discipline are now the cheapest, highest-impact moves on the board — every one is a few-string edit that lifts the 10-second test the v3 audit failed. **Out of scope:** rename of the product itself (held — see [`docs/naming.md`](./docs/decisions/naming.md)), i18n, implementation. **Files:** new `docs/strategy/v3-audit-language-2026-06-06.md`; edited `docs/strategy/README.md`, `docs/strategy/session-decisions.md`, `docs/planning/feature-backlog.md`, `plan.md`.

- **2026-06-06 — Bundle 9 Slices 2 + 3 shipped — Builder now closes its own CI loop and runs safely in parallel. FND-RUNTIME 0.9 forced-restart playbook published.** **Slice 2 (CI loop):** two new built-in tools — `github.ci.read` (`read`/`auto`: `GET /pulls/{n}` → head sha → `GET /commits/{sha}/check-runs` + `/status`; returns `{overall: pending|success|failure|neutral, checks[], head_sha, …}`; cached via `withIdempotency('github_ci', '<pr>-<head_sha>', …)` so re-polls don't burn quota and the cache invalidates naturally when the branch's head_sha moves) and `github.commit.append` (`write`/`confirm`: PUTs a single new file on the PR's head branch via Contents API; same `.github/` / `supabase/migrations/` / `.env` / lockfile deny-list as `github.pr.open`; wrapped in `withIdempotency('github_commit', a.idempotency_key, …)` keyed by `issue-<n>-fix-<k>`). The Builder system prompt now mandates: after `github.pr.open`, call `github.ci.read` up to 3× per loop step; on `success`/`neutral` finalize; on `pending` finalize with a wait note and let `resume-runs` re-enter; on `failure` propose ONE follow-up `github.commit.append` whose message names the failing check, then finalize. `listBuilderRuns()` extended to surface the latest `github.ci.read` result per run and expose `failing.{name,html_url,summary}` so the `/build` card and `/missions/$id` timeline render a `CI · green | red · <check> | pending | n/a` chip that links to the failing check. **Slice 3 (conflict guard):** new `builder_file_claims` table — `(user_id, workspace_id, run_id, mission_id, mission_title, repo, path, status, claimed_at, released_at, released_reason)` with a **partial unique index `(repo, path) WHERE status='held'`** so only one held claim per `(repo, path)` exists at a time; RLS = workspace members read, owner writes; PostgREST grants in the same migration. `github.pr.open` now INSERTs the claim row BEFORE the Contents-API PUT and surfaces a typed `BuilderFileConflict: path "<p>" is already claimed by another Builder mission ("<title>")` error on the unique violation — second-mission PR never opens, no branch is created. New SECURITY DEFINER trigger `release_claims_for_terminal_run` (`AFTER UPDATE OF status ON agent_runs`) auto-releases every `held` claim for a run that hits `completed | completed_with_failures | halted | failed`. Operator can force-release via the new **Active file claims** panel on `/build` (5s refresh) which calls new server fn `releaseBuilderClaim({claim_id})` — the Release button is only enabled for the claim owner. **FND-RUNTIME 0.9 playbook:** new `docs/operations/fnd-runtime-restart-playbook.md` is the operator-driven verification — start a Builder mission, restart the worker mid-loop, verify the resume-runs sweeper picks the run up within ≤60s, every idempotency-keyed external write (`github_pr`, `github_commit`, `github_ci`) returns its cached result, repo state matches the single-mission contract (one branch, one PR, one initial commit, optionally one fix commit). `docs/planning/foundation-audit.md` row 0.9 flipped ❌ → 🟡 with a pointer to the playbook (✅ on a clean pass). **Docs loop closed in the same commit:** new `docs/features/bundle-9-builder.md` (canonical per-feature page covering Slice 1 + 2 + 3 — demo script, verification per slice, tool contracts, governance, known limits), new `docs/operations/fnd-runtime-restart-playbook.md`, both indexed in `docs/README.md` + `docs/features/README.md`; `architecture/orchestration.md` Bundle 9 paragraph extended with the new tools + claims ledger. **WHY:** Slice 1 proved the Plan→Build hop end-to-end against a real repo but left two open gaps that block claim C2 ("one governed loop") at production volume — (a) the Builder couldn't see whether its own PR passed CI, so the operator was still the test pipeline, and (b) two parallel missions could silently race on the same file, breaking the autonomy story the moment more than one PRD hit Builder. With Slices 2 + 3 in place the Builder closes its own loop on red and the conflict guard makes parallel Builder dispatch safe — both are prerequisites for an operator-trustable autonomous build lane. **Out of scope this turn (queued):** `github.ci.logs` tool (Builder still only sees check name + summary, not raw job log), `github.pr.update` / `github.pr.merge` (operator owns merge), per-workspace `GITHUB_REPO` (still a single global secret), Restructure Phases 3–4 (theme polish; operator deferred until feature tweaks settle), Proof Platform bundles 10–12 (Ship · Launch · Support→Learn). **Gotchas:** (1) the claim is held by `run_id`, so a single mission re-running `github.pr.open` (e.g. via the idempotency cache path) skips the conflict branch because the holder check matches `existing.run_id === runId`. (2) The CI cache key is `<pr>-<head_sha>`, so after `github.commit.append` lands a new commit the next `github.ci.read` call sees a new head_sha and bypasses the cache — exactly what we want. (3) The auto-release trigger keys off `OLD.status IS DISTINCT FROM NEW.status` so a no-op UPDATE on a terminal run won't try to release already-released rows. (4) `releaseBuilderClaim` is RLS-scoped to the claim's `user_id` — workspace peers can SEE the claim but cannot release each other's; this is intentional until we add a workspace-admin role. **Files:** new `supabase/migrations/<ts>_builder_ci_loop_tools.sql` (Builder prompt + 2 new tool seeds + backfill), new `supabase/migrations/<ts>_builder_file_claims.sql` (claims table + index + grants + RLS + auto-release trigger), new `docs/features/bundle-9-builder.md`, new `docs/operations/fnd-runtime-restart-playbook.md`; edited `src/lib/ai/tools/registry.server.ts` (two new tools + claim acquire in `github.pr.open`), `src/lib/build.functions.ts` (CI surface in `listBuilderRuns`, new `listBuilderClaims` + `releaseBuilderClaim`), `src/routes/_authenticated.build.tsx` (CI chip + active-claims panel), `docs/README.md`, `docs/features/README.md`, `docs/planning/foundation-audit.md`, `docs/planning/feature-backlog.md`, `architecture/orchestration.md`, `plan.md`.

- **2026-06-06 — F-AGENT-4: Swarm HUD + per-feature demo/operator docs pattern.** Closes the agent-ecosystem bundle. New route `/swarm` (nav slot: Agents → Swarm HUD, between Missions and Prompt Studio) is a single read-only governor's cockpit over the existing substrate — no new tables, no new tools, no new mutations. One server fn `getSwarmHud()` in `src/lib/swarm.functions.ts` parallel-fires every read (RLS-scoped to `auth.uid()` + workspace via `current_user_default_workspace`): agents + latest non-terminal `agent_runs` per agent (priority `running > paused > queued > terminal`) joined to `agent_autonomy.arc`; missions filtered to `planning|running|completed_with_failures` OR `completed_at >= now()-1h` with `mission_steps` `done/total/failed` aggregates; last 50 `agent_messages` for the handoff feed; pending `agent_approvals`; last 50 `event_queue` rows; throughput from `ai_events` last 60 min bucketed in JS into 5-min windows (count, sum `est_cost_usd`, p50 `latency_ms`); `guardrail_hits` count for the same hour. The page (`src/routes/_authenticated.swarm.tsx`) refreshes every 2s via TanStack Query (`refetchIntervalInBackground: false`), composes 7 inline section components, and has error + not-found boundaries. Writes are zero net-new: inline Approve/Reject calls `resolveApproval` from `governance.functions.ts`; Dispatch/Skip on pending `confirm` reactor rows calls `decideEventDispatch` from `reactor.functions.ts`. **Docs pattern alongside the build:** new `docs/features/` folder with one canonical operator/demo page per shipped feature — `docs/features/README.md` (index + template), `f-agent-1-orchestrator.md`, `f-agent-2-memory-reflection.md`, `f-agent-3-event-reactor.md` (backfilled, ~80 lines each, consolidated from the existing plan §4 entries + architecture bullets — no new claims), and `f-agent-4-swarm-hud.md` (full). Each follows the same skeleton: What it does · Why · Where to find · Demo script ≤ 90s · How it works · Governance & guardrails · Verification checklist · Limits · Related. `docs/README.md` got a new "Per-feature operator & demo guides" section linking the four pages. **WHY:** F-AGENT-1 made Cadence plan, F-AGENT-2 made it learn, F-AGENT-3 made it react — together they produce more concurrent activity than the operator can track from four separate surfaces (`/missions`, `/inbox`, `/agents`, `/governance`). The HUD makes governance scale with the swarm. The features folder makes every shipped capability demo-ready and onboardable from one page, so future demos, sales calls, and onboarding don't have to grep §4 entries + architecture bullets to reconstruct what a feature actually does. **Out of scope (deferred):** pause/steer-from-graph controls, `agent.spawn` fan-out + parent merge (E4 polish), per-mission message-cap loop guard, cross-workspace HUD, retro-priced cost re-calculation, numeric `trust_score` column on `agent_autonomy` (only the arc is shown), and any new mutations beyond the four reused buttons. **Gotchas:** (1) `agent_autonomy` does not have a `trust_score` column — only `arc`. (2) `getSwarmHud` returns jsonb blobs typed as `any` to satisfy TanStack Start's serialization checker. (3) Latest-run-per-agent is computed in JS (not SQL) so we can apply the `running > paused > queued > terminal` priority — an 80-row query window is sufficient for an active swarm. (4) No new index ships with this; if `getSwarmHud()` p50 climbs above ~200ms on a large workspace, add covering indexes on `agent_messages(created_at desc)` and `event_queue(created_at desc)` in a small follow-on migration. **Files:** new `src/lib/swarm.functions.ts`, new `src/routes/_authenticated.swarm.tsx`, new `docs/features/README.md` + 4 feature pages; edited `src/components/cadence/AppShell.tsx` (Swarm HUD nav item), `docs/README.md`, `architecture/orchestration.md`, `docs/planning/feature-backlog.md`, `plan.md`.

- **2026-06-06 — F-AGENT-3: Event reactor + auto-pipelines.** Closes the last "agents do, not just react when summoned" gap. Two new tables: `event_subscriptions` (per-workspace rules: `event_type ∈ {signal.created, opportunity.scored, prd.approved} → target_agent_slug` with `approval_mode auto|confirm` and jsonb `filter`; RLS workspace-member read + owner write, `is_default` flag distinguishes seeded vs operator rules) and `event_queue` (one row per firing, idempotent UNIQUE `(subscription_id, source_id)` so re-firing triggers is a no-op; statuses `pending → dispatched | skipped | failed`; carries `mission_id`/`run_id`/`error`). Three SECURITY DEFINER trigger fns fan-out matching subs into the queue: `signals` (AFTER INSERT), `opportunities` (only on insert or ICE-input change AND `NEW.ice_score >= COALESCE(filter->>'min_score', 8.0)`), `prds` (only on transition into `status='approved'`). New `seed_default_event_subscriptions(uuid)` seeds three defaults per workspace owner — `signal.created → discovery (auto)`, `opportunity.scored → strategist (confirm, min_score 8.0)`, `prd.approved → orchestrator (confirm)`; wired into `handle_new_user` for new signups and backfilled for every existing workspace owner. New `src/lib/reactor.functions.ts` exposes `listEventSubscriptions`, `upsertEventSubscription` (verifies the target agent exists for the user before saving), `deleteEventSubscription`, `listEventQueue`, `decideEventDispatch` (operator approve/reject for `confirm` rows), and a shared `dispatchEvent()` helper that creates a real mission via `createMission` + runs `runAgentLoop` with a templated, event-type-specific goal. New cron route `/api/public/hooks/event-reactor-tick` drains pending `auto`-mode rows (BATCH=10) every minute via pg_cron job `event-reactor-tick`. `confirm`-mode rows stay pending until the operator clicks Dispatch/Skip on the new **Reactor activity** panel on `/governance` (5s auto-refresh). `/governance` also gained an **Auto-pipelines** panel: rule list with enable/disable/delete + add-rule composer (event type / agent slug / mode / min-ICE filter). **WHY:** F-AGENT-1 gave Cadence a planner, F-AGENT-2 gave it memory + autonomy; without a reactor the system still required an operator to push every button. With the reactor in place, a high-ICE opportunity auto-queues a PRD-draft mission, an approved PRD queues an orchestrator plan, and a fresh signal auto-clusters — the discover→define→plan chain runs itself end-to-end behind governance gates. **Out of scope (queued):** F-AGENT-4 (Swarm HUD). **Gotchas:** (1) The dispatch helper calls `runAgentLoop` synchronously inside the cron handler — fine for `auto` orchestrator rows because backpressure already enqueues over-cap missions, but high-frequency event types should wrap with `withIdempotency` or move behind a true queue. (2) Opportunity trigger only checks `>=` against `filter->>'min_score'` (default 8.0); no other comparators yet. (3) Trigger functions are SECURITY DEFINER because the inserting role may not have direct INSERT on `event_queue` — do NOT switch to SECURITY INVOKER. (4) Defaults use `is_default=true` so a future "reset" button can re-seed without clobbering operator rules. **Files:** new `supabase/migrations/<ts>_event_reactor_and_subscriptions.sql`, new `src/lib/reactor.functions.ts`, new `src/routes/api/public/hooks/event-reactor-tick.ts`; edited `src/routes/_authenticated.governance.tsx`, `docs/planning/feature-backlog.md`, `architecture/orchestration.md`, `plan.md`.
- **2026-06-06 — F-AGENT-1: Orchestrator + multi-agent mission decomposition.** First half of the "agent ecosystem" bundle. The substrate (chokepoint, agent*runs, agent_messages, checkpoints, trust, guardrails, missions) already existed but every mission was effectively single-agent — `agent.handoff` was a primitive without a meta-planner. New seed agent `orchestrator` (created on first use via `seed_orchestrator_agent(p_user_id)` SECURITY DEFINER fn; idempotent re-seed bumps the system-prompt + tool wiring). New `mission_steps` table: `(mission_id, idx, agent_slug, sub_goal, depends_on int[], status, run_id, message_id, result, error, rationale, dispatched_at, completed_at)`; RLS scoped to `auth.uid()=user_id` with workspace_member read; unique on `(mission_id, idx)`. New SQL helper `next_ready_mission_steps(mission_id)` returns step rows whose `depends_on` are all `status='done'`. Four new agent tools registered: `mission.plan` calls a sub-model with a planning system prompt (lists the user's actual specialist roster, asks for strict JSON `{summary, steps:[{agent_slug, sub_goal, depends_on, rationale}]}`, 1–8 steps cap, validates every slug is real before persisting); `mission.dispatch` reflects in-flight child-run statuses, then for every ready step calls `enqueueHandoff()` (existing primitive) to insert a child `agent_runs` row + `agent_messages` row, marks the step `dispatched`, records `run_id` + `message_id`; `mission.observe` reflects + returns per-step state + a status histogram + `all_terminal` boolean; `mission.finalize` requires `all_terminal`, marks the mission `completed` or `completed_with_failures`. Loop step cap is per-agent now via `maxStepsFor(agentSlug)` — orchestrator gets 14 to fit plan + observe/dispatch cycles + finalize; specialists keep 6. Three new server fns in `src/lib/orchestrator.functions.ts`: `ensureOrchestrator()` for explicit bootstrap, `startOrchestratedMission({goal,title?,model?})` for the canonical entry (idempotent seed + roster pre-flight + mission row + orchestrator loop), `advanceMission({missionId})` for the operator to push the next round of dispatches manually until F-AGENT-3 wires automatic re-wake, plus `listMissionSteps({missionId})` for read. UI: `/missions` gained an inline "Plan & dispatch" composer (title + goal textarea + one button) so the operator never needs to escape to `/agents` to launch a multi-agent mission; `/missions/$id` gained an "Orchestrator plan" panel above the existing timeline + graph showing the DAG with status pills + deps + an **Advance** button when work remains. Specialist child runs execute asynchronously via the existing `resume-runs` cron — no new infra. **WHY:** the substrate was 95% there but the \_behavior* was missing — until something planned a goal into a graph and dispatched it, "agent-native OS" was the marketing phrase for a single-agent planner-executor. Closes the first of three F-AGENT- gaps that block the autonomous-product-OS thesis. **Out of scope this turn (queued):** F-AGENT-2 (persistent agent memory + auto self-reflection + trust-arc auto-advance), F-AGENT-3 (event reactor + auto-pipelines), F-AGENT-4 (Swarm HUD), Restructure Phases 3–4 (deferred per operator). **Files:** new `supabase/migrations/<ts>_mission_steps_and_orchestrator.sql` (+ a second migration patching the seed function with required `display_name`/`description` on `agent_tools` inserts), new `src/lib/ai/tools/orchestrator.server.ts`, new `src/lib/orchestrator.functions.ts`; edited `src/lib/ai/tools/registry.server.ts`, `src/lib/ai/loop.server.ts`, `src/routes/_authenticated.missions.index.tsx`, `src/routes/_authenticated.missions.$missionId.tsx`, `docs/planning/feature-backlog.md`, `plan.md`, `architecture/orchestration.md`.
- **2026-06-06 — F-AGENT-2: Persistent agent memory + auto self-reflection + trust-arc auto-advance.** Second half of the agent-ecosystem bundle (F-AGENT-3 reactor + F-AGENT-4 Swarm HUD still queued). New `src/lib/ai/reflection.server.ts` exports `autoReflect()` (calls Gemini Flash through the chokepoint with `surface='agent', surface_ref='reflect:<slug>'`, expects strict JSON `{lesson, what_worked, what_to_change, importance:1-5}`, writes a `kind='reflection'` row to `agent_memory` with embedding + structured metadata — `run_id`, `trace_id`, `goal`, `what_worked`, `what_to_change`) and `maybeAutoAdvanceArc()` (wraps the new `auto_advance_agent_arc(p_user_id, p_agent_id)` SECURITY DEFINER RPC). `loop.server.ts` calls both from BOTH finalize paths (fresh run + resumed run) when `!halted` — halted runs skip reflection by design, so a governance halt never turns into a self-confirming lesson without operator review. `recallMemory()` now merges two sources: the existing `match_agent_memory` semantic search and the new `recent_agent_reflections` RPC (importance + recency ranked, top 3), deduped by content, capped at 8 — so reflections surface even when the embedding store is sparse or the goal is novel. Two new tools in the registry: `memory.reflect` (category=memory; agent/operator on-demand reflection — reads the current run's input/output if available, wraps `autoReflect`) and `memory.promote` (escalate a memory's scope agent→global so every agent in the workspace recalls it; e.g. "Q3 priority is retention"). Autonomy RPC: `auto_advance_agent_arc` SECURITY DEFINER promotes Observing→Proving at 5 clean completed runs and Proving→Trusted at 20, only when no `agent_approvals.status='rejected'` row exists since the last `agent_autonomy.set_at`; never auto-promotes past Trusted (Ambient stays an explicit operator decision); never downgrades; tolerant of both legacy `status='complete'` (`runAgent` one-shot) and modern `status='completed'` (`loop.server.ts`); sets `set_by=NULL` on system promotion so the UI can distinguish operator vs system moves later. New `recent_agent_reflections(for_user, for_agent_slug, match_count)` SECURITY DEFINER RPC + new `listAgentReflections` server fn (`src/lib/agents.functions.ts`) power a **Recent reflections** panel on `/agents` showing each lesson with importance pill + what-worked / what-to-change sub-blocks pulled out of `metadata`. New cron route `/api/public/hooks/memory-tick` (daily 03:30 UTC via `memory-tick-daily` pg*cron job) deletes `importance <= 2 AND COALESCE(last_used_at, created_at) < now()-30d`, two split passes so the new `agent_memory_decay_idx` is actually used. Migration also fixed the long-standing `agent_memory` grants — table was created without `GRANT SELECT/INSERT/UPDATE/DELETE … TO authenticated`, only `sandbox_exec` had access; PostgREST + admin paths now work. Two new SECURITY DEFINER fns had EXECUTE revoked from PUBLIC (anon can't call them; only `authenticated` + `service_role`). **WHY:** F-AGENT-1 gave us a planner that decomposes goals — F-AGENT-2 makes the resulting work \_compound*. Without persistent reflection every run started cold; without auto-advance the trust dial was operator-only, defeating the point of an autonomy ladder. Together these two close the "agents that learn and earn autonomy" half of the autonomous-product-OS thesis. **Out of scope (queued):** F-AGENT-3 (event reactor: `signal.created`→Discovery, `opportunity.scored>=80`→Strategist, `prd.approved`→Orchestrator + `/governance` subscriptions CRUD), F-AGENT-4 (Swarm HUD route with live missions / handoffs / pending approvals / auto-pipeline firings panels), Restructure Phases 3–4. **Gotchas for the next tool:** (1) reflections write under the per-user supabase client RLS path, so they only land when `runId` exists; ad-hoc loop runs without an `agent_runs` row still reflect via the same helper but the `metadata.run_id` is null — UI handles that. (2) `auto_advance_agent_arc` is called with `set_by=NULL` for system promotions; if you add a "promoted by" badge later, NULL = system, non-NULL = operator. (3) the cron-side admin client bypasses RLS for `memory-tick` deletes — keep importance≤2 + 30d cutoffs tight so we never destroy operator-authored memories (`memory.remember` writes default importance=3, above the floor). (4) `memory.reflect` is intentionally `category=memory` (not `write`), so the autonomy dial cannot loosen it past `auto`; reflections should not be approval-gated. **Files:** new `src/lib/ai/reflection.server.ts`, new `src/routes/api/public/hooks/memory-tick.ts`, two new migrations (`<ts>_agent_memory_grants_and_autonomy_advance.sql` + `<ts>_revoke_public_execute_on_helpers.sql`); edited `src/lib/ai/loop.server.ts`, `src/lib/ai/tools/registry.server.ts`, `src/lib/agents.functions.ts`, `src/routes/_authenticated.agents.tsx`, `docs/planning/feature-backlog.md`, `plan.md`, `architecture/orchestration.md`.
- **2026-06-05 — Demo credentials doc interlinked across all index + tool-entry files.** `docs/operations/demo-credentials.md` existed but was only referenced from the plan/backlog log entries — no index pointed at it, so a fresh Claude Code / Gemini / Antigravity session reading the standard read-order would not discover it. Added a row in `docs/README.md` operator-guides table, a **Try it — demo accounts** subsection in top-level `README.md` (both emails + shared password + link to full doc), a one-line pointer in `AGENTS.md` §0, a step-5 bullet in the read-order of `CLAUDE.md` and `GEMINI.md`, and a row + file-tree entry in `ENTRY.md`. **WHY:** the closed-doc-loop rule says a doc nothing links to is invisible — every co-developing tool now finds the demo logins via its normal entry flow, not by grep. **Files:** edited `README.md`, `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `ENTRY.md`, `docs/README.md`, `docs/planning/feature-backlog.md`, `plan.md`.
- **2026-06-04 — Demo workspace seeding repaired + credentials documented + favicon shipped.** `public.seed_demo_workspace` was silently failing for both demo accounts (`demo@redcadence.app`, `demo2@redcadence.app`) because it tried to insert `slug='demo'` into `workspaces.slug`, which is globally `UNIQUE` and was already taken by an older demo row from another owner — the credential migration's `EXCEPTION WHEN OTHERS` swallowed the constraint violation, so both accounts only got an empty "My Workspace". New migration restores the full seeder body with a per-owner slug (`'demo-' || substr(user_id::text, 1, 8)`, NULL fallback if even that is taken), cleans the empty `My workspace` rows for both demo users, and re-runs `seed_demo_workspace` — both accounts now sign in to a fully populated Demo workspace (Lumen project, 3 themes, 9 signals, 5 opportunities, 2 PRDs, 10 tasks, 4 docs, 2 meetings, 3 decisions, 18 AI events, eval suite, drift snapshots, daily briefs). New `docs/operations/demo-credentials.md` documents both logins + shared password + seeded content + re-seed instructions. Favicon added: generated a violet→magenta "C" brand mark, uploaded via `lovable-assets` to the CDN, wired into `__root.tsx` head via `icon` / `shortcut icon` / `apple-touch-icon` links so the browser tab now shows the Cadence mark on every route including `/login` and the published site. **WHY:** the operator could log in with the demo credentials but landed in an empty workspace, breaking every demo flow; also the published tab was nameless. **Files:** new `supabase/migrations/<ts>_restore_seed_demo_workspace.sql`, new `docs/operations/demo-credentials.md`, new `src/assets/favicon.png.asset.json`; edited `src/routes/__root.tsx`.
- **2026-06-04 — Restructure Phase 2: Today / Briefing / Approvals / PRDs restyled to Cohere editorial (Phase 2/4).** Five highest-traffic surfaces moved from the dark-Nightshift decoration to the Cohere editorial light system. `/` Today: hero replaced with a full-bleed `band-deep-green` block (focus score readout in canvas-white, white-on-green pills); `ShaderAnimation` removed; per-agent gradient palette (`AGENT_COLORS`) collapsed to one neutral stone accent so differentiation is name+role, not color; progress bars switched from violet→cyan gradients to flat `bg-foreground` / `bg-[var(--deep-green)]`; tabs/decisions/copilot panels rebuilt with `mono-label` headings and `btn-pill` / `btn-pill-outline` CTAs; Product health became a `band-stone` panel. `/briefing`: editorial header (mono-label tag + Instrument Serif H1), `btn-pill` save, 24px field cards, verify-hint switched to `band-stone`. `/inbox`: research-table list — `rule-hairline` rows, mono-label metadata row (`agent / tool / status`), serif rationale, dot+label `StatusBadge` in deep-green/coral/ink, coral left-rule for errors, `btn-pill` Approve & `btn-pill-outline` Reject; status filter became an underline tab strip. `/prds` list: bento grid replaced with `rule-hairline` rows under a single mono-label, neutral file icon, mono-label metadata (`status / Updated… / #issue`), generator uses `btn-pill`. `/prds/$id`: sticky actions bar swapped to `btn-pill` Save + `btn-pill-outline` GitHub/Builder + foreground-inverted Edit/Preview toggle; doc mode swapped from `prose-invert` on `bento` to `prose-neutral` on a clean card; metadata row uses mono-label with `link-action` GitHub link. Verified zero remaining `text-violet-*` / `from-violet*` / `neural-gradient` / `prose-invert` in these five routes. **WHY:** Phase 1 reset tokens but the high-traffic surfaces still rendered as the old dark theme — operator’s first three clicks (Today → Approvals → a PRD) had to land in the new editorial system for the redesign to _feel_ real. **Out of scope (queued):** Phase 3 (Discover/Deliver/Agents), Phase 4 (AI Ops/Govern + Settings/Calendar/Meetings/Chat/auth). **Files:** edited `src/routes/_authenticated.index.tsx`, `src/routes/_authenticated.briefing.tsx`, `src/routes/_authenticated.inbox.tsx`, `src/routes/_authenticated.prds.tsx`, `src/routes/_authenticated.prds.$id.tsx`, `active-task.md`, `docs/planning/feature-backlog.md`, `plan.md`.
- **2026-06-04 — Restructure Phase 1: nav IA + Cohere editorial design system (Phase 1/4).** Sidebar reorganized from 6 pinned + 4 collapsible groups into a 6-pillar enterprise model: **Workspace** (always-on rail: Today, Briefing, Approvals, Calendar, Meetings, AI Chat) + collapsible **Discover** (Discovery, Opportunities) · **Deliver** (PRDs, Docs, Roadmap, Tasks, Build Console) · **Agents** (Agents, Missions, Prompt Studio, Sync Inbox) · **AI Ops** (Analytics, Traces, Evals, Drift) · **Govern** (Guardrails, Governance, Budgets, Integrations) + Settings footer. Calendar and Meetings moved out of Discover (they're inputs to every phase, not signals); Sync Inbox moved under Agents (that's where agents resolve external state); Govern split out as its own pillar because in an agent-native product governance is peer to product work. No route URLs changed — only sidebar grouping. Design tokens in `src/styles.css` fully rewritten from the dark Nightshift palette (violet/cyan/amber on near-black) to the Cohere editorial light system: stark white canvas, `#212121` ink, `#17171c` near-black primary CTA, `#003c33` deep-green product band, `#071829` dark navy, `#1863dc` action blue, `#ff7759` coral for editorial taxonomy, `#eeece7` soft-stone / `#edfce9` pale-green / `#f1f5ff` pale-blue surface washes, `#d9d9dd` hairline. Radius base dropped from 12px to 8px (Cohere "sm"); new utilities `mono-label`, `btn-pill`, `btn-pill-outline`, `band-deep-green`, `band-dark-navy`, `band-stone`, `rule-hairline`, `chip-taxonomy`, `link-action`. Display type switched to **Instrument Serif** (free analog to CohereText carved feel), UI stays Inter, mono labels JetBrains Mono — fonts wired via Google Fonts in `__root.tsx`. Legacy decorative utilities (`neural-gradient`, `animate-aurora`, `ring-glow-violet`, `bento`, `glass`, `indigo-grid`) neutralized to soft light-theme equivalents so pages from the old dark theme don't visually break before Phases 2–4 restyle them per page. AppShell sidebar restyled: white canvas, mono-label group headings, hairline dividers, near-black active indicator (no gradient ring), workspace selector as a near-black logomark tile. **WHY:** the operator's words — "in a couple of places we have Calendar and Meetings at the Discover phase, why don't they even have the meeting and calendar thing? Move something on top to a general section and have Discover, Build, AI Ops only at a focus level" + "mimic the style and inputs from the Cohere design.md file." Closing the IA mess and resetting the design surface for the enterprise pivot. **Out of scope this phase (queued for Phases 2–4):** restyling individual pages (Today/Briefing/Inbox/PRDs in Phase 2; Discover/Deliver/Agents in Phase 3; AI Ops/Govern in Phase 4); replacing direct uses of `neural-gradient`/`ring-glow-violet` in route components; dark mode (deferred — Cohere is a light system). **Files:** edited `src/styles.css`, `src/components/cadence/AppShell.tsx`, `src/routes/__root.tsx`, `docs/planning/feature-backlog.md`, `plan.md`.
- **2026-06-04 — Build Console accepts free-form input; PRD UI surfaces fixed.** `/build` now opens with a **Start a build** composer (free-form Goal + optional Reference PRD + optional Reference links + three issue-resolution modes: use linked PRD issue · explicit issue # · auto-create from goal). New server fn `dispatchBuilderMission` in `src/lib/build.functions.ts` resolves the issue (reuses PRD's linked issue, takes an explicit number, or opens a fresh issue via GitHub REST with the goal + PRD body + reference links as context), then creates a mission and runs the Builder loop — same single-file / confirm-gated / idempotent contract. `/prds` list cards now show Create-GitHub-issue / Open-issue / Send-to-Builder inline plus a `#N` chip when linked, so the operator doesn't need to drill in to wire a PRD. `/prds/$id` rebuilt as a real document view: metadata row + **sticky actions bar** with Edit/Preview, Save, GitHub issue, Send to Builder, and AI assist co-located. `listPrds` now returns `github_issue_url` so cards and the composer can branch on it. **WHY:** the operator's words: "PRD doesn't have the right structure and right actions" and "why should Build Console only take inputs from PRD?" — closing those two dead-ends. **Files:** edited `src/lib/build.functions.ts`, `src/lib/discovery.functions.ts`, `src/routes/_authenticated.prds.tsx`, `src/routes/_authenticated.prds.$id.tsx`, `src/routes/_authenticated.build.tsx`, `active-task.md`, `docs/planning/feature-backlog.md`.
- **2026-06-04 — Bundle 9 Slice 1 shipped — Builder agent + `github.pr.open` + Build Console + nav reshuffle (Code Studio → Prototype Sandbox).** New `builder` agent seeded for every existing profile and new signups with a tight operator prompt (one file per PR, must read issue first, `idempotency_key = issue-<n>`, never auto-merges, allow-list excludes `.github/`, `supabase/migrations/`, `.env`, lockfiles). New `github.pr.open` tool in `src/lib/ai/tools/registry.server.ts` (`write/confirm`) runs the REST-only PR pipeline against `${GITHUB_REPO}` (default branch → create ref → PUT base64 contents → POST PR), wrapped in `withIdempotency('github_pr', …)` so re-approval / sweeper-resume returns the cached `{number,url,branch,path}`. New `/build` Build Console page — 5-column Kanban over Builder runs joined to `tool_calls` (PR chip) and pending `agent_approvals` (badge), 2s refresh. New **Send to Builder** chip on `/prds/$id` (only when `github_issue_url` is set) extracts the issue number and dispatches a Builder mission via `runAgent({ asMission: true })`, routes you straight to its Mission Graph. Nav: Code Studio renamed to **Prototype Sandbox** and moved out of Build → into Discover (route `/studio` unchanged so links still resolve); new **Build Console** added under Build. **WHY:** the operator asked for a real "agent picks up the PRD → ships code → tests it → ships it" lane with parallel visualization à la Claude Code; Slice 1 closes the first leg (PR open, single-file, confirm-gated) and frees the Build nav from the misnamed Code Studio. Slices 2 (CI read + commit append on red) and 3 (proper conflict detection on shared file paths) tracked in `active-task.md`. **Files:** new `supabase/migrations/<ts>_add_builder_agent_and_github_pr_open.sql`, `src/lib/build.functions.ts`, `src/routes/_authenticated.build.tsx`; edited `src/lib/ai/tools/registry.server.ts`, `src/components/cadence/AppShell.tsx`, `src/routes/_authenticated.prds.$id.tsx`, `architecture/orchestration.md`, `docs/planning/feature-backlog.md`.
- **2026-06-04 — GitHub issue approval flow documented + `GITHUB_REPO` / `GITHUB_TOKEN` rotated to the operator's real repo (`RohitGajaraj/Test-Project-Cadence`).** New canonical operator doc `docs/github-issue-approval-flow.md` covers the Approve walkthrough, single-repo POST contract, `withIdempotency` guarantee, failure modes, verification checklist, and rotation playbook. Cross-linked from `architecture/orchestration.md` (Bundle 6 paragraph), `architecture/integrations.md` (new GitHub connector line), and `docs/README.md` (operator guides table). Secrets rotated via the Cloud secrets UI — no code change, picked up at next tool call. **WHY:** Bundle 6 shipped the capability but the "what does Approve actually do, which repo, which token" question kept recurring in chat; closing the doc loop makes the governance gate self-explanatory and unblocks the operator's real-repo demo. **Files:** new `docs/github-issue-approval-flow.md`; edited `architecture/orchestration.md`, `architecture/integrations.md`, `docs/README.md`.
- **2026-06-04 — Bundle 6 lifecycle close shipped — Discover→Define→Plan can now exit to a real GitHub issue + PRD link-back.** The two tool handlers (`github.issue.create`, `prd.link_issue`) already lived in `src/lib/ai/tools/registry.server.ts` from FND-RUNTIME 0.9 + Bundle 2, but `prd.link_issue` was missing from `seed_pm_lifecycle_tools` and `github.issue.create` was seeded for new signups at `mode='review'` (and unseeded for legacy users), so the Planner couldn't actually pick either up under the trust dial. New migration rewrites both seed functions: `seed_pm_lifecycle_tools` now also installs `prd.link_issue` (write/`confirm`), `seed_default_agent_tools` re-affirms `github.issue.create` at write/`confirm` (was `review`) so the Plan stage can ship under arc-based gating, and both functions then run inside a backfill `DO` loop over every existing `profiles` row so missions starting today have the right `agent_tools` rows. UI surface: `/prds/$id` now renders a clickable `Github · GitHub issue #N` chip immediately below the back-link when `prds.github_issue_url` is set (number parsed from the URL). Idempotency: the `github.issue.create` handler is already wrapped in `withIdempotency('github_issue', a.idempotency_key, …)` so re-execution after a worker restart or the resume-runs sweeper picking up a stalled run returns the cached `{ number, url, id, repo, cached: true }` — it never double-creates the issue. **WHY:** claim C3 ("one governed loop") only becomes literally true once the Plan stage can exit the platform into the engineering system of record under an operator-controlled gate; without the seed + the link-back chip the loop visibly stopped at "draft PRD" and the operator had to copy/paste into GitHub. **Out of scope this turn (deferred to Bundle 9):** `github.pr.open`, CI read into the Mission Graph, branch/PR back-link on the PRD page, `github.issue.update`/`close`. **Files:** new `supabase/migrations/<ts>_seed_github_issue_and_link.sql`; edited `src/routes/_authenticated.prds.$id.tsx`, `architecture/orchestration.md`, `docs/planning/feature-backlog.md`.
- **2026-06-04 — Bundle 5 Live Mission Graph (E6) shipped.** `/missions/$id` only had a linear timeline + per-hop progress panel; multi-agent missions couldn't be _seen_ as a graph, which is the literal success criterion for Step 5 of the build-order rollup ("graph updates in real time as agents act; clicking a node opens that agent's trace + cost + tokens + approval state"). Added a pure-SVG DAG component `src/components/cadence/MissionGraph.tsx` and mounted it above the existing timeline + hops list. **Nodes** = one per `agent_runs` row in the mission; status color + glyph + agent name + slug + status text. **Edges** = one per `agent_messages` row, drawn `source_run_id → consumed_by_run_id`; for in-flight handoffs not yet consumed, fall back to the earliest later hop targeting `to_agent_slug`. Edges are violet bezier paths with arrow markers + the message `kind` label. **Layout** = BFS-depth columns (root hops at column 0, each handoff bumps the child's column) × chronological rows within column, so a single-agent mission renders 1 node / 0 edges and a Discovery → Strategist → Builder mission renders 3 nodes connected by 2 labelled edges; multiple outbound messages from the same parent stack as parallel children in the next column (E4 fan-out polish ready without a new tool). **Interaction**: nodes are real `role="button"` SVG groups with `onKeyDown` Enter/Space; clicking expands the matching hop card (adds to the existing `expanded` Set) and smooth-scrolls via `id="hop-{run_id}"` + `scroll-mt-24`. Re-uses the existing `refetchInterval: 2000` while mission `status='running'`/`queued`, so the graph fills in live. Pure read model — no schema change, no new server fn, consumes the existing `getMission` payload. WHY: Step 5 of the build-order rollup needs the graph to be literally true, and Bundle 4's claim ("A2A is first-class, no human in the routing path") only becomes operator-legible once the routing is visible as a graph rather than a list. **Out of scope this turn:** pause/steer-from-graph controls, zoom/collapse for >20-node graphs, per-node cost/token rollup pill (needs `getMission` to surface it), explicit `agent.spawn` tool. **Files:** new `src/components/cadence/MissionGraph.tsx`; edited `src/routes/_authenticated.missions.$missionId.tsx`, `architecture/orchestration.md`, `docs/planning/feature-backlog.md`.
- **2026-06-04 — Mission page: live progress panel + agent timeline + Firecrawl search fix.** Operator dispatched a Discovery mission and watched it sit at `running` with 0 hops and no visible activity. Two issues. (1) `web.search` was halting the loop on step 1 with `rows.map is not a function` — Firecrawl v2 `/search` returns `{data:{web,news,images}}` (object), not an array. Parser in `src/lib/ai/tools/firecrawl.server.ts` now handles array, `{web,news}` object, and the legacy `web`/`results` shapes. (2) `/missions/$id` had no live observability — once a hop was running there was nothing to look at. Extended `getMission` (`src/lib/missions.functions.ts`) to also fetch the latest `agent_run_checkpoints` row per run (extracts `traceId` + `steps[]`) and join `tool_calls` by trace id, then rebuilt `src/routes/_authenticated.missions.$missionId.tsx`: an **Agent timeline** strip at the top (one chip per hop with status icon, agent slug, elapsed duration, handoff arrows showing lineage end-to-end), a **live progress panel inside each hop card** listing the loop's `steps[]` (thought · tool*call · final, with per-call status + args/error preview), and a **Tool spans** subsection (per-tool name + ok/err + latency from `tool_calls`). Always visible while `running`/`queued`; collapses behind Detail when finished. Refresh interval tightened to 2s while live; header shows a live indicator; each hop card now exposes a one-click `/traces/$traceId` link. WHY: claim C1 ("agents \_do*, humans govern") requires the operator to _see_ the doing in real time — without per-step visibility a mission feels frozen even when it's running, and silent tool failures (like the Firecrawl bug) hide forever. **Out of scope this turn:** DAG view for parallel fan-out (still queued for Bundle 5), per-step token/cost rollup on the hop card, websocket push (we still poll). **Files:** edited `src/lib/missions.functions.ts`, `src/routes/_authenticated.missions.$missionId.tsx`, `src/lib/ai/tools/firecrawl.server.ts`, `docs/planning/feature-backlog.md`, `plan.md`.
- **2026-06-04 — Web access for agents shipped (Firecrawl-backed `web.*` tools).** Agents were siloed: a goal like _"scout how Linear's AI triage is positioned and give me a non-reactive one-pager"_ needed the public internet but had no tool to call, so they hallucinated or stopped short. Added four governed tools to `TOOL_REGISTRY` — `web.search` (ranked results + optional in-line scrape), `web.fetch` (single URL → markdown), `web.map` (URL discovery), `web.crawl` (bounded crawl, max 25 pages / depth 2) — all routed through one helper `src/lib/ai/tools/firecrawl.server.ts` that reads `FIRECRAWL_API_KEY` from `process.env` only. Search/fetch/map default to `auto`; `web.crawl` defaults to `confirm` because it spends real Firecrawl credits. Returns are clipped (search snippets 2 KB, fetch markdown 8 KB default / 20 KB max, crawl pages 4 KB each) so token spend stays predictable. Results re-enter the loop as untrusted input — the next `callModel()` already runs them through pre-guardrails (PII / prompt-injection / secret keyword) per `architecture/runtime.md`, no new code required. Trust arc + kill-switch + per-mission spend cap still apply unchanged because tools sit in the same registry. Connected Firecrawl as a workspace connector (`FIRECRAWL_API_KEY`). Migration updates `seed_default_agent_tools` for new signups and backfills the four tool rows for every existing user. WHY: closes the "agents _do_, humans govern" gap — without outbound web I/O the platform was a workspace-only reasoner, not an operator. **Out of scope this turn:** per-user domain allow/deny UI, a higher-level `web.research` chain tool, browser automation, swapping providers. **Files:** new `src/lib/ai/tools/firecrawl.server.ts`, new `docs/web-access.md`, new `supabase/migrations/<ts>_web_tools_seed.sql`; edited `src/lib/ai/tools/registry.server.ts`, `architecture/integrations.md`, `docs/planning/feature-backlog.md`, `docs/README.md`.
- **2026-06-04 — Bundle 4 Agent-to-Agent handoff (E1–E5 MVP) shipped.** New `missions` + `agent_messages` tables (RLS-scoped to workspace members; hop counter + current-agent kept in sync via trigger); `agent_runs.mission_id` added (nullable, single-agent runs still valid). New module `src/lib/ai/handoff.server.ts` exports `createMission`, `enqueueHandoff`, `consumeInboundHandoff`, `renderHandoffBlock`, `maybeCompleteMission`. New `agent.handoff` tool in `TOOL_REGISTRY` (write/`confirm`; structured payload = `task + context + artifacts + open_questions + constraints`) — inserts the message AND enqueues a child `agent_runs` row with the same `mission_id`; the existing FND-RUNTIME 0.9 resume-runs sweeper picks it up. `ToolCtx` extended with `missionId` + `workspaceId`. The loop (both fresh `runAgentLoop` and `resumeAgentLoop`) consumes the latest inbound handoff at run start and prepends a `--- Handoff from {agent} ---` block to the system prompt right after the workspace brief — receiver gets a typed JSON payload, never a pasted prompt. Receiver's autonomy arc continues to gate its own tool calls. Failure policy is option-b: hop failures stop the mission; operator re-dispatches manually (documented in `docs/a2a-handoff.md`). New `/missions` (list) and `/missions/$missionId` (hops timeline + collapsible payload viewer + auto-refresh while running). "Start as mission" checkbox added to `/agents` dispatch form. Sidebar pinned "Missions" entry. WHY: claim C2 ("A2A handoff is first-class — no human in the routing path") only becomes literally true once a mission can travel Discovery → Strategist → PRD Writer with each receiver reading the previous agent's structured output and the operator able to inspect each hop. **Out of scope this turn (deferred to Bundle 5):** E6 Mission Graph DAG view; explicit `agent.spawn` fan-out tool + parent merge step (E4 polish); per-mission message-cap loop guard. Files: new `supabase/migrations/<ts>_a2a_handoff.sql`, new `supabase/migrations/<ts>_agent_handoff_seed.sql`, new `src/lib/ai/handoff.server.ts`, new `src/lib/missions.functions.ts`, new `src/routes/_authenticated.missions.tsx`, new `src/routes/_authenticated.missions.$missionId.tsx`, new `docs/a2a-handoff.md`; edited `src/lib/ai/tools/registry.server.ts`, `src/lib/ai/loop.server.ts`, `src/lib/agent_loop.functions.ts`, `src/routes/_authenticated.agents.tsx`, `src/components/cadence/AppShell.tsx`.
- **2026-06-04 — C6 Trust score made operator-legible: qualitative labels + Radix tooltips + canonical doc.** `/agents` Trust chip now renders `Trust <n> · <label>` where the label is derived from the score band (`At-risk` / `Observing` / `Proving` / `Trusted` / `Ambient`; `New` until ≥3 samples) so a number like `48` is no longer ambiguous to a founder. Replaced native `title=` strings with Radix `Tooltip`/`TooltipProvider`: the chip tooltip explains the 0–100 scale and Bayesian shrinkage, then shows the weighted breakdown (Missions 40% · Approvals 30% · Evals 30%) with raw counts and the suggested arc; the "Autonomy dial" label has its own tooltip explaining how the arc composes with each tool's mode; every arc button (Observing/Proving/Trusted/Ambient) has a per-arc tooltip describing exactly what it does to `auto` / `confirm` / `review` tools. New canonical doc `docs/trust-and-autonomy.md` — score meaning, formula, the four arc levels, safety floors, operator playbook — linked from C6's "How to use / verify" block and from `architecture/orchestration.md`. WHY: the operator asked what "Trust 48" actually meant; numeric-only scores require context the UI wasn't providing. Files: `src/routes/_authenticated.agents.tsx`, `docs/trust-and-autonomy.md`, `docs/planning/feature-backlog.md`, `architecture/orchestration.md`.
- **2026-06-04 — Bundle 3 Agent Trust Score + Autonomy Dial (C6) shipped.** New `agent_autonomy` table (one row per user+agent, arc enum `observing|proving|trusted|ambient`, owner-only RLS, standard GRANTs). Trust score is computed on read in `src/lib/ai/trust.server.ts` (`computeAllAgentTrust`) from real signals — mission completion rate (`agent_runs.status`), approval acceptance rate (`agent_approvals.status`), and eval mean (`ai_evals.score` joined via `ai_events.agent_id`) — Bayesian-shrunk toward 0.5 when sample <10 so a single-run agent doesn't render as 100. `resolveApprovalMode(toolMode, arc)` is the single combiner the loop calls at the gate decision: Observing forces `review` even on `auto` tools, Proving forces `confirm`, Trusted runs `confirm` inline, Ambient runs everything inline. Safety floor: `review` is sticky (never downgraded by the dial), and `calendar.create` keeps `confirm` even at Ambient. Server fns `getAllAgentTrust` + `setAgentArc` in `src/lib/trust.functions.ts` (both `requireSupabaseAuth`). `src/lib/ai/loop.server.ts` now loads the arc once per run (both fresh and resumed paths) and uses `resolveApprovalMode` at every tool-call gate instead of reading `modeOf.get(name)` directly. UI: `/agents` renders a color-tiered Trust chip (with hover tooltip showing the full breakdown + formula + suggested arc) on every roster button and the detail header, plus an inline AutonomyDial (4 arc buttons + suggested-arc hint + plain-language explainer) inside the detail card; the dispatch mutation also invalidates the trust query so a completed mission updates the score in place. WHY: makes C4 ("trust is dialed, not assumed") concrete and operator-controllable in the same surface where missions are dispatched — the dial visibly converts earned trust into reduced friction. Files: new `supabase/migrations/<ts>_agent_autonomy.sql`, `src/lib/ai/trust.server.ts`, `src/lib/trust.functions.ts`; edited `src/lib/ai/loop.server.ts`, `src/routes/_authenticated.agents.tsx`.
- **2026-06-04 — `/agents` page rewired to loop-based runAgent (brief + traces now verifiable).** Dispatches from `/_authenticated/agents` were calling the legacy single-shot `runAgent` in `src/lib/agents.functions.ts`, which goes straight to `callModel` with no `traceId` and no workspace-brief load. Result: `ai_events` rows for those runs had `trace_id = NULL` (invisible on `/traces`, which filters non-null) and the operator's brief never reached the system prompt. Switched the import in `src/routes/_authenticated.agents.tsx` to `runAgent` from `src/lib/agent_loop.functions.ts` (which calls `runAgentLoop` → generates a trace id, loads `workspace_briefs`, injects `renderBriefBlock` between persona and memory recall, and stamps `trace_id` on every event). Mutation signature flipped from `{agentId, input}` to `{agentSlug, goal}`. Added an inline "View traces →" link next to Dispatch so verification is one click. WHY: the brief-injection feature was complete server-side but unobservable from the UI the operator was actually using.
- **2026-06-04 — Briefing save permanent fix.** Root cause: `/briefing` could render with editable fields while `activeWorkspaceId` was still null, and the database did not permanently guarantee default workspace membership for every existing/future account. Added migrations `20260604073742_232596ba-54c3-441b-b733-02dd49a22807.sql`, `20260604073901_fece0306-1ee5-4307-a30a-d45b980a95c9.sql`, and `20260604074200_bd5e9f3b-8834-4531-bdf3-04837c5c189a.sql`: `ensure_user_default_workspace(user)` creates/returns a workspace + owner membership, `current_user_default_workspace()` delegates to it, `handle_new_user()` calls it, existing profiles are backfilled, and direct helper permissions are restricted. Updated `src/lib/briefs.functions.ts` so `upsertBrief` accepts a missing workspace id and resolves it server-side. Updated `src/routes/_authenticated.briefing.tsx` so Save is disabled only while unchanged/loading/saving, not because client workspace context is late. Verified in preview twice: edited Current focus/Notes, Save enabled, server function returned 200, button returned to Saved, no briefing console errors.
- **2026-06-04 — Bundle 2 Strategic Briefing (C5) + `prd.link_issue` shipped.** New `workspace_briefs` table (one row per workspace; mission / target user / current focus / anti-goals / notes; workspace-member read, owner write; RLS + GRANTs). New server module `src/lib/briefs.functions.ts` with `getActiveBrief` + `upsertBrief` and a pure helper `renderBriefBlock()` that emits a labelled, fenced text block (skipped entirely when every field is empty so we never inject noise). Agent loop (`src/lib/ai/loop.server.ts`) now reads the brief for the resolved workspace and injects the rendered block **between the agent's own system prompt and the memory recall**, so every mission — Discovery, Strategist, Builder — sees the operator's shared context first. New route `/_authenticated/briefing` provides the editor UI (5 textareas with hint copy + character counts, single Save button, mutation-driven toast, AppShell layout). Pinned to the sidebar nav as **"Briefing"** (Crosshair icon) right after Today. Also added the small `prd.link_issue` agentic tool to `TOOL_REGISTRY` (write category, default `confirm` mode, idempotent: sets `prds.github_issue_url`) closing the Bundle 6 PRD↔issue link-back loop; backfilled into `agent_tools` for every existing user. Migration adds `prds.github_issue_url`. Verification: edit the brief on `/briefing`, run a mission from `/agents`, observe the Strategist's draft honoring Current focus / Anti-goals. Files: `src/lib/briefs.functions.ts` (new), `src/routes/_authenticated.briefing.tsx` (new), `src/lib/ai/loop.server.ts`, `src/lib/ai/tools/registry.server.ts`, `src/components/cadence/AppShell.tsx`, plus the migration. Next on the YC-demo path: Bundle 3 (Agent Roster + Trust Score + Autonomy Dial).
- **2026-06-03 — Repaletted to "Carbon & Ember" (supersedes Midnight Indigo).** Operator unhappy with the indigo look; wanted factory.ai-adjacent darker base with Pantone orange contrast. Shifted `src/styles.css` token values only (no rename, no component touches): `--paper` → warm carbon `oklch(0.13 .006 60)` (Pantone Black 6 C ~ #101820); `--violet` (still the canonical accent token) repointed to **Pantone Orange 021 C** `oklch(0.70 .22 38)` (~#FE5000); `--primary` now aliases `--violet` so every CTA/ring/focus reads ember; `--amber` warmed for the gradient halo; `--cyan` desaturated to ice as a cool counter-accent for charts. `.neural-gradient`, `.neural-text`, `.indigo-grid`, `.ring-glow-violet` automatically pick up the new palette. WCAG AA ≈ 7.4:1 ember-on-carbon. Documented in `design.md` §Tokens → "Active palette: Carbon & Ember" per the AI Color Selection Protocol.
- **2026-06-03 — PM lifecycle tools shipped (`research.synthesize`, `prd.draft`, `backlog.prioritize`).** Added 3 PM-centric agentic tools to `src/lib/ai/tools/registry.server.ts` and seeded them into `agent_tools` for every existing user (and for new signups via the updated `seed_default_agent_tools(uuid)`), all default `confirm` mode. (1) `research.synthesize` — pulls recent ungrouped `signals` (filterable by tag/sentiment/lookback), asks the model (via `callModel`, surface=`discovery`, `responseFormat=json_object`) to cluster them into 2–6 cohesive themes, writes `themes` rows (severity 1–5, confidence 0–1, frequency = signal count) and links source signals via `signals.theme_id`. (2) `prd.draft(opportunity_id)` — reads the opportunity, joins its `theme` + top 8 supporting signals, asks the model (surface=`prd`) to draft a structured PRD (Problem / Target user / Goals / Non-goals / Stories / Solution sketch / Success metrics / Risks) and writes a `prds` row in `draft` status with `model` recorded. (3) `backlog.prioritize` — for each backlog opportunity, gathers supporting-signal count + recency (last 7d), asks the model to re-score ICE (1–10 integers) with rationale, updates `opportunities.{impact,confidence,ease}` (ice_score is a generated column so it recomputes), returns the new ranked list with rationales. All three flow through the AI chokepoint so they hit guardrails + budgets + tracing, and through the agent loop they pick up `runId`/`stepIndex` automatically for idempotent retry on resume. No per-agent assignment needed — the loop filters `TOOL_REGISTRY` by what the user has enabled, so Discovery Scout / PRD Writer / Strategist see them immediately. Closes the Define + Plan halves of the Bundle 6 lifecycle slice that previously had only the GitHub-issue write on the Build edge. Files: `src/lib/ai/tools/registry.server.ts`, migration `seed_pm_lifecycle_tools(uuid)` + folded into `seed_default_agent_tools(uuid)` with a backfill loop.
- **2026-06-03 — FND-RUNTIME 0.9 runtime instrumentation + N1 `github.issue.create` shipped.** Added `src/lib/runtime/idempotency.server.ts` (`withIdempotency(scope,key,userId,runId,fn)` → execute → INSERT-on-conflict-fall-back-to-cached against the new `idempotency_keys` table). Refactored `src/lib/ai/loop.server.ts` so both `runAgentLoop()` (fresh) and new exported `resumeAgentLoop(runId)` (rehydrates from latest `agent_run_checkpoints` row) feed a shared `executeLoop()`; every iteration upserts a checkpoint _before_ the provider call (so a `GovernanceHaltError` mid-stream never re-bills on resume) and wraps tool execution in `withIdempotency` keyed `tool:{runId}:{stepIndex}:{toolName}` — replaying a step short-circuits to the cached result instead of re-invoking the side-effecting tool. Added per-workspace **backpressure** (default 5 concurrent `running` runs); over-cap missions insert as `status='queued'` and are promoted by the new `/api/public/hooks/resume-runs` sweeper, which also resumes `running` runs whose `last_checkpoint_at` is >2 min stale (worker eviction). Sweeper is `pg_cron`-driven every minute against the stable preview URL. Added **`github.issue.create`** agentic tool to `TOOL_REGISTRY` (category `write`, default `confirm` mode): writes to the single allow-listed `GITHUB_REPO` env, validated `owner/name` format, idempotent via caller-supplied `idempotency_key` (e.g. PRD id) so re-execution from a checkpoint never double-creates an issue. ToolCtx extended with optional `runId` + `stepIndex` so future tools can key their own idempotency without re-deriving. Files: `src/lib/runtime/idempotency.server.ts` (new), `src/lib/ai/tools/registry.server.ts`, `src/lib/ai/loop.server.ts`, `src/routes/api/public/hooks/resume-runs.ts` (new), plus a `pg_cron` migration scheduling `resume-runs` every minute. Forced-restart integration test still TODO. Lifecycle slice (Bundle 6 Discover→Define→Plan against a real Cadence signal, approving the real GitHub-issue write) is now the operator-driven next step.
- **2026-06-03 — FND-RUNTIME 0.9 foundation landed (decision + schema + secrets).** Wrote `docs/decisions/durable-runtime.md` selecting a **DB-backed durable job table** over Cloudflare Queues — rationale: matches existing `/api/public/hooks/*` + `pg_cron` + tenancy patterns; zero new infra/runtime/auth model; portable off Cloudflare; visible to the human governor as ordinary rows. Applied a migration adding (1) `agent_run_checkpoints` — append-only, one row per loop iteration, `UNIQUE (run_id, step_index)`, `state JSONB`, full tenancy keys, RLS scoped to `auth.uid()`; (2) `idempotency_keys` — `UNIQUE (scope, key)` dedup table for `/api/public/hooks/*` ticks (`tick:{hook}:{run_id}:{step_index}`) and tool execution (`tool:{run_id}:{step_index}:{tool_name}`) so a resumed step never re-executes a side-effecting tool; (3) `agent_runs.step_index` + `last_checkpoint_at` for heartbeat + resume cursor. Both new tables have `GRANT … TO authenticated`/`service_role` and RLS. Requested + received `GITHUB_TOKEN` + `GITHUB_REPO` secrets so the next stage — first lifecycle slice end-to-end on the Cadence repo (Bundle 6 Discover→Define→Plan + N1 `github.issue.create`) — has no hard blocker. `active-task.md` extended with the loop-checkpoint, `resumeAgentLoop`, `/api/public/hooks/resume-runs` sweeper, backpressure, and lifecycle-slice sub-steps; Live status board now shows "Now building: FND-RUNTIME (0.9) · Lovable · main".
- **2026-06-03 — Extended Agentic Proof Platform → v1.1: full PM lifecycle on real systems.** Un-deferred S4–S6, L, M (previously deferred from the YC cut) under one **realism rule**: agents orchestrate existing tools (GitHub, GitHub Actions, deploy webhooks, Slack/email, support channel) — they don't replace IDEs, CI, or helpdesks. Added 4 new capability bundles to `docs/planning/feature-backlog.md` (9 Build+Test, 10 Ship, 11 Launch, 12 Support→Learn), 7 new reserved IDs (N1 GitHub-issues write, I-thin Builder scoped PR, J-thin CI read, K-thin merge+deploy ingest, L-thin changelog+one channel, M-thin one inbound channel, Z1 Analyst learn loop), expanded build sequence 8→12 steps, added Demo narrative ("one continuous mission, ~3 minutes" — real signal → real GitHub issue → real PR → real CI → real merge → real deploy → real outbound message → real inbound ticket → re-scored opportunity → next Discovery cycle). Confirmed Cadence-on-Cadence as the real-data seed including PR writes on this repo (Builder needs `GITHUB_TOKEN` with `repo` scope, branch protection on `main` makes it safe; secret added when Bundle 9 starts, not now). Logged in `docs/strategy/session-decisions.md`. `active-task.md` unchanged — FND-RUNTIME 0.9 still next.
- **2026-06-03 — Reframed "YC demo cut" → "Agentic Proof Platform (v1)".** Same 8 capability bundles, same sequence, same reserved IDs (C5, C6, U6); what changed is the bar: each bundle now ships against an explicit **proof bar** (end-to-end behavior on real data) mapped to four claims legacy PM tools cannot make — C1 agents operate/humans govern · C2 A2A handoff is first-class · C3 one governed loop · C4 trust is dialed. Default real-data seed = **Cadence-on-Cadence** (we run our own roadmap on Cadence) so bundle 6 has no design-partner dependency. YC application becomes a by-product of the proof platform, not its driver. Updated `docs/planning/feature-backlog.md` (renamed section, added four-claims table + per-bundle proof bars + Real-data seeding subsection) and logged in `docs/strategy/session-decisions.md`. `active-task.md` unchanged (FND-RUNTIME 0.9 still next).
- **2026-06-03 — Locked YC demo cut (scope overlay, no new roadmap).** Defined the 8 capability bundles that compose the YC application demo and mapped each to existing backlog IDs in `docs/planning/feature-backlog.md` → new top section "▶ YC demo cut". Centerpiece is bundle #4 (E1–E6: sub-agent spawning, A2A messaging, mission handoff, parallel sub-agents/sessions, Mission Graph) — the agent-to-agent thesis. Decisions: persona = Founder-as-PM; S4–S6 (autonomous Build/Test/Ship) deferred and positioned as "foundation built, next milestone"; demo data = real product. Sequence: finish foundation (0.9 durable runtime → 0.2 cache) → C5 Strategic Briefing → C1/C6 roster + Trust Score → E1–E5 A2A primitives → E6 Mission Graph → wire F→G→H through handoff → D3 polish → O1/O2 + U6 export. Reserved IDs C5, C6, U6 with stub entries. Logged in `docs/strategy/session-decisions.md`; seeded `active-task.md` for FND-RUNTIME (0.9).
- **2026-06-03 — Completed FND-KILLSWITCH (0.6).** Added governance layer on top of existing spend caps: new `kill_switches` table (system + per-workspace) with admin-only system row and owner/admin-writable workspace rows; `current_kill_state(ws)` `SECURITY DEFINER` RPC the chokepoint reads before every call; `agent_runs` extended with `workspace_id`, `mission_spend_cap_usd`, `mission_token_cap`, `tokens_used`, `spend_used_usd`, `halted_reason`/`halted_at` and atomically incremented via `record_mission_usage()`; `agent_approvals` extended with `escalation_state` + 24h default `expires_at`; new public route `/api/public/hooks/approvals-tick` driven by `pg_cron` every minute to auto-expire stale approvals. Chokepoint enforcement in `callModel()` and `callModelStream()` throws a typed `GovernanceHaltError` that logs `ai_events` with `status='blocked'` and `error_message='governance_halt:<kind>'`; agent loop catches it and finalizes the run with `status='halted'`. New `/_authenticated/governance` UI shows pause panel (reason field, admin-gated), per-mission token/spend bars, and a stale-approvals panel with extend/approve/reject. `AppShell` shows a red paused banner when the current workspace is paused. Files: `src/lib/ai/runtime.server.ts`, `src/lib/ai/loop.server.ts`, `src/lib/governance.functions.ts`, `src/routes/_authenticated.governance.tsx`, `src/routes/api/public/hooks/approvals-tick.ts`, `src/components/cadence/AppShell.tsx`, plus two migrations. Foundation-audit §0.6: 🟡 → ✅.
- **2026-06-03 — Documented FND-KILLSWITCH (0.6) UI + persisted doc-loop rules to memory.** Added a "How to use / verify" block under `docs/planning/feature-backlog.md` §0.6 (operator walkthrough of the Governance page panels, paused banner, server enforcement points, and a three-step verification checklist), added a one-paragraph "Governance surface (operator UI)" pointer to `architecture/security.md` so the architecture doc links to the operator guide, and refreshed the Live status board. Promoted the closed documentation-loop rules (update backlog status board + plan §4 + matching architecture/foundation-audit entry + add a "How to use / verify" block when a feature has a user-facing surface; commit to `main` with one-line WHY; speak in product terms, never expose Supabase) to `mem://index.md` Core so every future Lovable session applies them without re-prompting.
- **2026-05-31 — Completed implementation of 0.7 (FND-INJECTION).** Hardened the AI runtime and agent planner loop by XML-escaping and quarantining RAG context chunks and tool execution results inside isolation tags. Added instructions to treat these outputs as passive text. Programmatically elevated prompt-injection guardrails to block immediately.
- **2026-05-31 — Completed code-side implementation of 0.2 (FND-CHOKEPOINT-STREAM).** Implemented `callModelStream` in `runtime.server.ts` to enforce budgets, input/output guardrails, prompt templates, and vector RAG context. Refactored `/api/chat` and `/api/studio-chat` routes to use `callModelStream`, ensuring all streaming chat interactions pass through the central AI runtime chokepoint.
- **2026-05-31 — Completed code-side implementation of 0.1 (FND-TENANCY).** Created `useWorkspace` hook and provider to manage select workspace/product states; wrapped `_authenticated` layout in `WorkspaceProvider`; refactored `AppShell` with sidebar dropdown switcher and project tabs; updated `projects.functions.ts` server functions with default workspace fallbacks.
- **2026-05-31 — Registered closed-loop documentation verification and context boot hooks.** Added [remind-doc-loop.sh](file:///Users/rohitgajaraj/Documents/My Projects/My Builds/Project-Cadence/.claude/hooks/remind-doc-loop.sh) and [load-project-memory.sh](file:///Users/rohitgajaraj/Documents/My Projects/My Builds/Project-Cadence/.claude/hooks/load-project-memory.sh) to `.claude/hooks/`. Registered them under `Stop`/`SubagentStop` and `SessionStart` events respectively in [.claude/settings.json](file:///Users/rohitgajaraj/Documents/My Projects/My Builds/Project-Cadence/.claude/settings.json). The startup hook boots new sessions with context by printing core doc locations, active tasks, and recent memories. The shutdown hook verifies that changes were documented. Set the status board in [feature-backlog.md](file:///Users/rohitgajaraj/Documents/My Projects/My Builds/Project-Cadence/docs/planning/feature-backlog.md) to show `0.1 (FND-TENANCY)` is active under `Antigravity`.
- **2026-05-30 — Tenancy retrofit migrations authored (0.1, not yet applied).** Three forward-only migrations under `supabase/migrations/` (`…tenancy_a_scaffolding`, `…_b_add_keys_backfill`, `…_c_tighten_policies`): add `workspaces` + `workspace_members` + `is_workspace_member()` helper; add `workspace_id` to 22 NOW-set tables + `product_id` to 10 (backfilled from existing `project_id` on 8 first-slice tables); membership-keyed RLS replacing `user_id`-only; `current_user_default_workspace()` default bridge so existing app/Lovable inserts keep working; RAG `match_*` functions scoped to workspace + optional product. Design + table-by-table scope: [`docs/decisions/tenancy-retrofit.md`](./docs/decisions/tenancy-retrofit.md). **Pending:** apply via Supabase flow (migration-safety hook runs on apply); then server-fn context plumbing (O2) to set keys explicitly; then flip 0.1 → ✅ in [`docs/planning/foundation-audit.md`](./docs/planning/foundation-audit.md).
- **2026-05-30 — Cross-tool co-dev substrate established.** Added `AGENTS.md` §10 (portability matrix + layer-ownership + per-tool entry points + git-reconciliation workflow): the repo is the only shared substrate; skills/subagents/hooks/plugins are Claude-Code-only, MCP + `AGENTS.md` are the portable layers. New portable MCP manifest `.mcp.json` (Supabase read-only + Playwright, env-driven, no secrets). New `.claude/skills/` (policy README + `cadence-feature-pair` project skill encoding the two-files-in-lockstep convention) and `.claude/agents/` policy README. Renamed `agents.md` → `AGENTS.md` (case-only; was a latent bug on case-sensitive FS / non-Mac tools). **Security:** `.env` was tracked + un-ignored — added `.env`/`.env.*` to `.gitignore`, added `.env.example`, ran `git rm --cached .env`. **Pending (needs operator decision):** rotate Supabase keys + scrub history; mirror `.mcp.json` into Antigravity/Gemini config.
- **2026-05-30 — Removed `nango/` from the repo; deleted stray `playwright-cli`.** Audit found **zero** references to nango anywhere in `src/` (incl. `sync.functions.ts` / `integrations.functions.ts`), no `@nangohq` dep in `package.json`, and nango absent from the build graph (vite/tsconfig/wrangler) — it was ~200M of the self-hostable Nango _platform_ (its own clone of `NangoHQ/nango@7faf2c30`), disconnected from the app and present only as a broken gitlink (no `.gitmodules` → empty on fresh clone). Untracked it and added `nango/` to `.gitignore` (working copy kept on disk). **Decision:** when integrations are wired, run Nango as a separate backend (Nango Cloud or self-hosted via its own docker-compose) and consume it via `@nangohq/node` + `@nangohq/frontend` + `NANGO_HOST`/`NANGO_SECRET_KEY` env — same service-behind-a-URL pattern as Supabase. Also deleted `playwright-cli` (stray archived MS clone).

---

## 5. Legacy build log (retained — the previous source, for reuse)

> This is the earlier build (the "vibe-coded" source). **It is a reuse reference, not the plan.** Mine it for what survives the stress-test; supersede entries as they're rebuilt into section 4. Do not treat anything here as current truth without checking the code.

- **Foundation** — Supabase auth (email + Google OAuth), `profiles`, workspaces + extended `projects`, persistent SSE chat with workspace grounding, model-agnostic router with per-surface defaults, command palette (⌘K), Mission Control widgets, OKLCH dark theme, RLS, loader+Suspense pattern, route boundaries, global auth middleware. _(Largely reusable.)_
- **Discovery & specs** — `signals` ingest (paste/CSV, chunked inserts; sentiment on insert), clustering (k-medoid + synthesis with `evidence_ids[]`), themes → ICE opportunities, one-click PRD → Tiptap with `/ai` slash menu, `prd_versions` autosave + diff, early pgvector RAG (512/64 chunks, 1536-d). _(Reusable; this is the first-slice base.)_
- **Planning & meeting intelligence** — Now/Next/Later board (`@dnd-kit`). _(Tail of this legacy entry was lost in an earlier edit collision that spliced the §6 heading mid-line — recover details from git history if ever needed.)_

## 6. Product-agent roster (re-examined: explicit vs. implicit)

**The question:** is a fixed roster of 16 named agents right? **Answer:** no — split it. Use a small set of **durable, named lifecycle agents** (explicit, one owner per stage) plus an orchestrator that **dynamically spawns ephemeral specialist sub-agents** for sub-tasks (implicit). Fixed seats for micro-roles create clutter; dynamic sub-agents scale with the work.

**6 outcome-focused durable agents (explicit, one per lifecycle band):**

The roster is kept narrow and delivery-oriented. Each agent owns a clear band of the product lifecycle. Fixed seats for micro-roles are avoided — those collapse into ephemeral specialists.

| Agent            | Lifecycle band                                                  | Owns                                                                                                             | Primary pain point addressed                                    |
| ---------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| **Orchestrator** | All stages                                                      | Mission coordination, spawns ephemeral specialists, manages approval routing, parallelism                        | Coordination overhead, context loss between stages              |
| **Discovery**    | S1 Signal Capture + S2 Audio Sync                               | Signal Ingest (Zendesk/Intercom/Slack), clustering opportunities, WhisperFlow transcription and audio extraction | Shallow discovery, anecdotal signals, missing verbal alignments |
| **Strategist**   | S3 ICE Prioritization + S4 Spec Definition + S5 Sprint Planning | Opportunity scoring, cited PRD generation, Linear/GitHub Issues sync, dependency-aware task graphs               | Spec drafting overhead, weak prioritization, feature factories  |
| **Builder**      | S6 Agentic Build + S7 Visual QA + S8 Safe Release               | Multi-file coding, file claims ledger, sandbox previews, PR merge checks and deploy monitoring                   | Developer bottlenecks, styling drift, release quality checks    |
| **Growth**       | S9 GTM Launch                                                   | Outbound changelogs, blog posts, Slack notifications and release announcements                                   | Launch overhead, delayed go-to-market communication             |
| **Analyst**      | S10 Support Triage + S11 Cohort Analytics + S12 Learn & Reflect | Support ticket routing to S1, adoption funnels, performance metrics vs PRD specs, Product Memory                 | Closed-loop support, un-measured features, memory decay         |

**Ephemeral specialists (spawned by Orchestrator per task):** competitor scan, UX critique, stakeholder brief, data analysis, security review, code review, and any other short-lived specialist. They inherit the chokepoint, tool allow-list, and governance. No permanent seats for micro-roles.

**Persona mapping:**

- **P1 (Director / VP of Product)**: strategic oversight, budget controls, audit log reviews, portfolio health, uses Analyst + Orchestrator.
- **P2 (Lead / Senior PM)**: daily operator, runs swarm briefs, PRD editor, discovery feeds, uses Discovery + Strategist + Analyst.
- **P3 (Engineering Lead)**: code review, Builder agent claims review, CI verification, uses Builder.
- **P4 (UX/UI Designer)**: sandbox preview validation, style token audit, uses Builder (Visual QA).
- **P5 (GTM Lead)**: launch copywriting, distribution approvals, uses Growth.
- **P6 (Support Lead)**: triage validation, bug signal loops, uses Analyst.

Schema per agent: `slug, name, system_prompt, tool_allowlist[], default_model, temperature/top_p/seed, max_tokens, schedule_cron?, approval_mode (auto|confirm|review), memory_enabled`. Roster changes need a migration + a build-order entry.

---

## 7. Testing strategy

- **Unit:** pricing math, each guardrail rule kind, RAG chunker boundaries, ICE/cron/date helpers.
- **Integration:** chokepoint (one `ai_events` row + trace; budget throws before provider; cache short-circuits but logs; guardrail block aborts + logs; ticks idempotent); orchestration (parallel sessions isolated; checkpoints resume; cancellation saves partial trace); RLS isolation across products/workspaces.
- **Autonomous build/test loop:** the Builder agent's output must pass the QA agent's generated tests before Ship; a failing eval suite ("Circuit core") blocks deploy (≥0.1 regression without override).
- **E2E:** intent → discover → define → plan → build → test → ship, watched in the orchestration view; multi-product isolation; ⌘K across routes; `/observe` renders on seeded data.
- **Manual QA before release:** auth + tenancy isolation; every route empty/partial/full; AI message contract present ([`design.md`](./design.md)); guardrail block works; budget breach is friendly; light-theme Cohere editorial contrast.

---

## 8. Lessons learned (kept from the legacy build)

One AI chokepoint paid for itself (judge added in half a day). Loader + `useSuspenseQuery` killed `useEffect+fetch` regressions. OKLCH tokens ended dark-mode contrast bugs. Bugs fixed: name-vs-email leak; duplicate agent fan-out (advisory lock); action-item due-date glitch; Notion BFS reorder; trace recursion blow-up; cache cross-user poisoning (salted key); CSV params cap (chunked inserts). Known limits: clustering degrades past ~1000 signals/run; Studio truncates files >2k lines; Docs sync drops comments; `model_pricing` hand-maintained.

---

## 9. Out of scope (for now)

Fine-tuning workflows from `ai_events`; cross-workspace vector sharing; public agent/skill-pack marketplace; cross-workspace SSO; fully autonomous _strategic_ decisions (strategic conclusions keep a human gate by design — tactical ones may auto-apply). These are deliberate exclusions, revisited when the core loop is solid.
