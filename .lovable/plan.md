Update the banner text in `src/components/cadence/CookingBanner.tsx` to:

"Agents are building in the back. Fresh build loading."

This is a single-line copy change — no layout, color, or placement adjustments needed.
